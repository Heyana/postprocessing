import * as Wt from "run-scene-core";
import { Scene as zt, Camera as as, Mesh as Ft, BasicDepthPacking as $, WebGLRenderTarget as A, Material as mi, Texture as It, BufferGeometry as pi, BufferAttribute as vn, ShaderMaterial as N, NoBlending as B, Uniform as a, LinearFilter as F, UnsignedByteType as se, SRGBColorSpace as V, EventDispatcher as jr, Vector2 as g, BackSide as Et, DoubleSide as jt, FrontSide as hr, Color as y, HalfFloatType as K, RGBAFormat as ne, FloatType as Y, RawShaderMaterial as vi, GLSL3 as Te, Matrix3 as Ot, Matrix4 as C, RGBADepthPacking as ce, MeshDepthMaterial as gi, NearestFilter as b, DepthTexture as Oe, DepthStencilFormat as os, UnsignedInt248Type as ls, UnsignedIntType as cs, LinearSRGBColorSpace as ir, PerspectiveCamera as ke, Data3DTexture as _t, ClampToEdgeWrapping as tt, Vector3 as T, DataTexture as le, Loader as Xr, LoadingManager as kt, FileLoader as xi, CanvasTexture as Si, RepeatWrapping as G, NoColorSpace as Fe, Vector4 as ze, REVISION as Vt, LessDepth as gn, GreaterDepth as us, GreaterEqualDepth as fs, LessEqualDepth as hs, NotEqualDepth as Mt, EqualDepth as Ue, AlwaysDepth as ds, NeverDepth as ms, UniformsUtils as oe, MathUtils as Ee, TextureLoader as Ie, LuminanceFormat as yi, RedFormat as Bt, RGFormat as ps, OneFactor as xn, AddEquation as Lt, CustomBlending as vs, LinearMipmapLinearFilter as gs, LinearMipMapLinearFilter as nt, EquirectangularReflectionMapping as Mi, MeshPhysicalMaterial as xs, Quaternion as Yr, ShaderChunk as ue, ShaderLib as He, Clock as wi, WebGLMultipleRenderTargets as be, FramebufferTexture as Zr, MeshNormalMaterial as Kr, OrthographicCamera as Nr, MeshBasicMaterial as gt, PlaneGeometry as Ss, NoToneMapping as ys, TangentSpaceNormalMap as Ms, Source as ws, Float32BufferAttribute as Sn, UnsignedShortType as Qr, OneMinusSrcAlphaFactor as sr, SrcAlphaFactor as ar, NormalBlending as pt, Plane as Ts } from "run-scene-core";
const Zf = "6.37.12";
class Kf {
  /**
   * Frees internal resources.
   */
  dispose() {
  }
}
const Mr = 1 / 1e3, bs = 1e3;
class Ds {
  /**
   * Constructs a new timer.
   */
  constructor() {
    this.startTime = performance.now(), this.previousTime = 0, this.currentTime = 0, this._delta = 0, this._elapsed = 0, this._fixedDelta = 1e3 / 60, this.timescale = 1, this.useFixedDelta = !1, this._autoReset = !1;
  }
  /**
   * Enables or disables auto reset based on page visibility.
   *
   * If enabled, the timer will be reset when the page becomes visible. This effectively pauses the timer when the page
   * is hidden. Has no effect if the API is not supported.
   *
   * @type {Boolean}
   * @see https://developer.mozilla.org/en-US/docs/Web/API/Page_Visibility_API
   */
  get autoReset() {
    return this._autoReset;
  }
  set autoReset(e) {
    typeof document < "u" && document.hidden !== void 0 && (e ? document.addEventListener("visibilitychange", this) : document.removeEventListener("visibilitychange", this), this._autoReset = e);
  }
  get delta() {
    return this._delta * Mr;
  }
  get fixedDelta() {
    return this._fixedDelta * Mr;
  }
  set fixedDelta(e) {
    this._fixedDelta = e * bs;
  }
  get elapsed() {
    return this._elapsed * Mr;
  }
  /**
   * Updates this timer.
   *
   * @param {Boolean} [timestamp] - The current time in milliseconds.
   */
  update(e) {
    this.useFixedDelta ? this._delta = this.fixedDelta : (this.previousTime = this.currentTime, this.currentTime = (e !== void 0 ? e : performance.now()) - this.startTime, this._delta = this.currentTime - this.previousTime), this._delta *= this.timescale, this._elapsed += this._delta;
  }
  /**
   * Resets this timer.
   */
  reset() {
    this._delta = 0, this._elapsed = 0, this.currentTime = performance.now() - this.startTime;
  }
  getDelta() {
    return this.delta;
  }
  getElapsed() {
    return this.elapsed;
  }
  handleEvent(e) {
    document.hidden || (this.currentTime = performance.now() - this.startTime);
  }
  dispose() {
    this.autoReset = !1;
  }
}
const Ps = /* @__PURE__ */ (() => {
  const c = new Float32Array([-1, -1, 0, 3, -1, 0, -1, 3, 0]), e = new Float32Array([0, 0, 2, 0, 0, 2]), t = new pi();
  return t.setAttribute("position", new vn(c, 3)), t.setAttribute("uv", new vn(e, 2)), t;
})();
let X = class zr {
  /**
   * A shared fullscreen triangle.
   *
   * The screen size is 2x2 units (NDC). A triangle needs to be 4x4 units to fill the screen.
   * @see https://michaldrobot.com/2014/04/01/gcn-execution-patterns-in-full-screen-passes/
   * @type {BufferGeometry}
   * @internal
   */
  static get fullscreenGeometry() {
    return Ps;
  }
  /**
   * Constructs a new pass.
   *
   * @param {String} [name] - The name of this pass. Does not have to be unique.
   * @param {Scene} [scene] - The scene to render. The default scene contains a single mesh that fills the screen.
   * @param {Camera} [camera] - A camera. Fullscreen effect passes don't require a camera.
   */
  constructor(e = "Pass", t = new zt(), r = new as()) {
    this.name = e, this.renderer = null, this.scene = t, this.camera = r, this.screen = null, this.rtt = !0, this.needsSwap = !0, this.needsDepthTexture = !1, this.needsNormalTexture = !1, this.enabled = !0;
  }
  /**
   * Sets the render to screen flag.
   *
   * If this flag is changed, the fullscreen material will be updated as well.
   *
   * @type {Boolean}
   */
  get renderToScreen() {
    return !this.rtt;
  }
  set renderToScreen(e) {
    if (this.rtt === e) {
      const t = this.fullscreenMaterial;
      t !== null && (t.needsUpdate = !0), this.rtt = !e;
    }
  }
  /**
   * Sets the main scene.
   *
   * @type {Scene}
   */
  set mainScene(e) {
  }
  /**
   * Sets the main camera.
   *
   * @type {Camera}
   */
  set mainCamera(e) {
  }
  /**
   * Sets the renderer
   *
   * @deprecated
   * @param {WebGLRenderer} renderer - The renderer.
   */
  setRenderer(e) {
    this.renderer = e;
  }
  /**
   * Indicates whether this pass is enabled.
   *
   * @deprecated Use enabled instead.
   * @return {Boolean} Whether this pass is enabled.
   */
  isEnabled() {
    return this.enabled;
  }
  /**
   * Enables or disables this pass.
   *
   * @deprecated Use enabled instead.
   * @param {Boolean} value - Whether the pass should be enabled.
   */
  setEnabled(e) {
    this.enabled = e;
  }
  /**
   * The fullscreen material.
   *
   * @type {Material}
   */
  get fullscreenMaterial() {
    return this.screen !== null ? this.screen.material : null;
  }
  set fullscreenMaterial(e) {
    let t = this.screen;
    t !== null ? t.material = e : (t = new Ft(zr.fullscreenGeometry, e), t.frustumCulled = !1, this.scene === null && (this.scene = new zt()), this.scene.add(t), this.screen = t);
  }
  /**
   * Returns the current fullscreen material.
   *
   * @deprecated Use fullscreenMaterial instead.
   * @return {Material} The current fullscreen material, or null if there is none.
   */
  getFullscreenMaterial() {
    return this.fullscreenMaterial;
  }
  /**
   * Sets the fullscreen material.
   *
   * @deprecated Use fullscreenMaterial instead.
   * @protected
   * @param {Material} value - A fullscreen material.
   */
  setFullscreenMaterial(e) {
    this.fullscreenMaterial = e;
  }
  /**
   * Returns the current depth texture.
   *
   * @deprecated Use getDepthTexture instead.
   * @return {Texture} The current depth texture, or null if there is none.
   */
  getDepthTexture() {
    return null;
  }
  /**
   * Sets the depth texture.
   *
   * This method will be called automatically by the {@link EffectComposer}.
   * You may override this method if your pass relies on the depth information of a preceding {@link RenderPass}.
   *
   * @param {Texture} depthTexture - A depth texture.
   * @param {DepthPackingStrategy} [depthPacking=BasicDepthPacking] - The depth packing.
   */
  setDepthTexture(e, t = $) {
  }
  /**
   * Returns the current normal texture.
   *
   * @return {Texture} The current normal texture, or null if there is none.
   */
  getNormalTexture() {
    return null;
  }
  /**
   * Sets the normal texture.
   *
   * @param {Texture} normalTexture - A normal texture.
   */
  setNormalTexture(e) {
  }
  /**
   * Renders this pass.
   *
   * This is an abstract method that must be overridden.
   *
   * @abstract
   * @throws {Error} An error is thrown if the method is not overridden.
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   * @param {DepthPass} [depthPass] - An optional shared depth pass for optimizing multiple effects.
   */
  render(e, t, r, n, i, s) {
    throw new Error("Render method not implemented!");
  }
  /**
   * Sets the size.
   *
   * You may override this method if you want to be informed about the size of the backbuffer/canvas.
   * This method is called before {@link initialize} and every time the size of the {@link EffectComposer} changes.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
  }
  /**
   * Performs initialization tasks.
   *
   * This method is called when this pass is added to an {@link EffectComposer}.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
  }
  /**
   * Performs a shallow search for disposable properties and deletes them.
   *
   * The {@link EffectComposer} calls this method when it is being destroyed. You can use it independently to free
   * memory when you're certain that you don't need this pass anymore.
   */
  dispose() {
    for (const e of Object.keys(this)) {
      const t = this[e];
      (t instanceof A || t instanceof mi || t instanceof It || t instanceof zr) && this[e].dispose();
    }
    this.fullscreenMaterial !== null && this.fullscreenMaterial.dispose();
  }
};
class Cs extends X {
  /**
   * Constructs a new clear mask pass.
   */
  constructor() {
    super("ClearMaskPass", null, null), this.needsSwap = !1;
  }
  /**
   * Disables the global stencil test.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i) {
    const s = e.state.buffers.stencil;
    s.setLocked(!1), s.setTest(!1);
  }
}
const As = `#include <common>\r
#include <dithering_pars_fragment>\r
\r
#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D inputBuffer;\r
\r
#else\r
\r
	uniform lowp sampler2D inputBuffer;\r
\r
#endif\r
\r
uniform float opacity;\r
\r
varying vec2 vUv;\r
\r
void main() {\r
\r
	vec4 texel = texture2D(inputBuffer, vUv);\r
	gl_FragColor = opacity * texel;\r
\r
	#include <colorspace_fragment>\r
	#include <dithering_fragment>\r
\r
}\r
`, Be = `varying vec2 vUv;\r
\r
void main() {\r
\r
	vUv = position.xy * 0.5 + 0.5;\r
	gl_Position = vec4(position.xy, 1.0, 1.0);\r
\r
}\r
`;
class dr extends N {
  /**
   * Constructs a new copy material.
   */
  constructor() {
    super({
      name: "CopyMaterial",
      uniforms: {
        inputBuffer: new a(null),
        opacity: new a(1)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: As,
      vertexShader: Be
    });
  }
  /**
   * The input buffer.
   *
   * @type {Texture}
   */
  set inputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * Sets the input buffer.
   *
   * @deprecated Use inputBuffer instead.
   * @param {Number} value - The buffer.
   */
  setInputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * Returns the opacity.
   *
   * @deprecated Use opacity instead.
   * @return {Number} The opacity.
   */
  getOpacity(e) {
    return this.uniforms.opacity.value;
  }
  /**
   * Sets the opacity.
   *
   * @deprecated Use opacity instead.
   * @param {Number} value - The opacity.
   */
  setOpacity(e) {
    this.uniforms.opacity.value = e;
  }
}
let Ti = class extends X {
  /**
   * Constructs a new save pass.
   *
   * @param {WebGLRenderTarget} [renderTarget] - A render target.
   * @param {Boolean} [autoResize=true] - Whether the render target size should be updated automatically.
   */
  constructor(e, t = !0) {
    super("CopyPass"), this.fullscreenMaterial = new dr(), this.needsSwap = !1, this.renderTarget = e, e === void 0 && (this.renderTarget = new A(1, 1, {
      minFilter: F,
      magFilter: F,
      stencilBuffer: !1,
      depthBuffer: !1
    }), this.renderTarget.texture.name = "CopyPass.Target"), this.autoResize = t;
  }
  /**
   * Enables or disables auto resizing of the render target.
   *
   * @deprecated Use autoResize instead.
   * @type {Boolean}
   */
  get resize() {
    return this.autoResize;
  }
  set resize(e) {
    this.autoResize = e;
  }
  /**
   * The output texture.
   *
   * @type {Texture}
   */
  get texture() {
    return this.renderTarget.texture;
  }
  /**
   * Returns the output texture.
   *
   * @deprecated Use texture instead.
   * @return {Texture} The texture.
   */
  getTexture() {
    return this.renderTarget.texture;
  }
  /**
   * Enables or disables auto resizing of the render target.
   *
   * @deprecated Use autoResize instead.
   * @param {Boolean} value - Whether the render target size should be updated automatically.
   */
  setAutoResizeEnabled(e) {
    this.autoResize = e;
  }
  /**
   * Saves the input buffer.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i) {
    this.fullscreenMaterial.inputBuffer = t.texture, e.setRenderTarget(this.renderToScreen ? null : this.renderTarget), e.render(this.scene, this.camera);
  }
  /**
   * Updates the size of this pass.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.autoResize && this.renderTarget.setSize(e, t);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - A renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    r !== void 0 && (this.renderTarget.texture.type = r, r !== se ? this.fullscreenMaterial.defines.FRAMEBUFFER_PRECISION_HIGH = "1" : e !== null && e.outputColorSpace === V && (this.renderTarget.texture.colorSpace = V));
  }
};
const je = -1;
class W extends jr {
  /**
   * Constructs a new resolution.
   *
   * TODO Remove resizable param.
   * @param {Resizable} resizable - A resizable object.
   * @param {Number} [width=Resolution.AUTO_SIZE] - The preferred width.
   * @param {Number} [height=Resolution.AUTO_SIZE] - The preferred height.
   * @param {Number} [scale=1.0] - A resolution scale.
   */
  constructor(e, t = je, r = je, n = 1) {
    super(), this.resizable = e, this.baseSize = new g(1, 1), this.preferredSize = new g(t, r), this.target = this.preferredSize, this.s = n, this.effectiveSize = new g(), this.addEventListener("change", () => this.updateEffectiveSize()), this.updateEffectiveSize();
  }
  /**
   * Calculates the effective size.
   *
   * @private
   */
  updateEffectiveSize() {
    const e = this.baseSize, t = this.preferredSize, r = this.effectiveSize, n = this.scale;
    t.width !== je ? r.width = t.width : t.height !== je ? r.width = Math.round(t.height * (e.width / Math.max(e.height, 1))) : r.width = Math.round(e.width * n), t.height !== je ? r.height = t.height : t.width !== je ? r.height = Math.round(t.width / Math.max(e.width / Math.max(e.height, 1), 1)) : r.height = Math.round(e.height * n);
  }
  /**
   * The effective width.
   *
   * If the preferred width and height are set to {@link Resizer.AUTO_SIZE}, the base width will be returned.
   *
   * @type {Number}
   */
  get width() {
    return this.effectiveSize.width;
  }
  set width(e) {
    this.preferredWidth = e;
  }
  /**
   * The effective height.
   *
   * If the preferred width and height are set to {@link Resizer.AUTO_SIZE}, the base height will be returned.
   *
   * @type {Number}
   */
  get height() {
    return this.effectiveSize.height;
  }
  set height(e) {
    this.preferredHeight = e;
  }
  /**
   * Returns the effective width.
   *
   * If the preferred width and height are set to {@link Resizer.AUTO_SIZE}, the base width will be returned.
   *
   * @deprecated Use width instead.
   * @return {Number} The effective width.
   */
  getWidth() {
    return this.width;
  }
  /**
   * Returns the effective height.
   *
   * If the preferred width and height are set to {@link Resizer.AUTO_SIZE}, the base height will be returned.
   *
   * @deprecated Use height instead.
   * @return {Number} The effective height.
   */
  getHeight() {
    return this.height;
  }
  /**
   * The resolution scale.
   *
   * @type {Number}
   */
  get scale() {
    return this.s;
  }
  set scale(e) {
    this.s !== e && (this.s = e, this.preferredSize.setScalar(je), this.dispatchEvent({ type: "change" }), this.resizable.setSize(this.baseSize.width, this.baseSize.height));
  }
  /**
   * Returns the current resolution scale.
   *
   * @deprecated Use scale instead.
   * @return {Number} The scale.
   */
  getScale() {
    return this.scale;
  }
  /**
   * Sets the resolution scale.
   *
   * Also sets the preferred resolution to {@link Resizer.AUTO_SIZE}.
   *
   * @deprecated Use scale instead.
   * @param {Number} value - The scale.
   */
  setScale(e) {
    this.scale = e;
  }
  /**
   * The base width.
   *
   * @type {Number}
   */
  get baseWidth() {
    return this.baseSize.width;
  }
  set baseWidth(e) {
    this.baseSize.width !== e && (this.baseSize.width = e, this.dispatchEvent({ type: "change" }), this.resizable.setSize(this.baseSize.width, this.baseSize.height));
  }
  /**
   * Returns the base width.
   *
   * @deprecated Use baseWidth instead.
   * @return {Number} The base width.
   */
  getBaseWidth() {
    return this.baseWidth;
  }
  /**
   * Sets the base width.
   *
   * @deprecated Use baseWidth instead.
   * @param {Number} value - The width.
   */
  setBaseWidth(e) {
    this.baseWidth = e;
  }
  /**
   * The base height.
   *
   * @type {Number}
   */
  get baseHeight() {
    return this.baseSize.height;
  }
  set baseHeight(e) {
    this.baseSize.height !== e && (this.baseSize.height = e, this.dispatchEvent({ type: "change" }), this.resizable.setSize(this.baseSize.width, this.baseSize.height));
  }
  /**
   * Returns the base height.
   *
   * @deprecated Use baseHeight instead.
   * @return {Number} The base height.
   */
  getBaseHeight() {
    return this.baseHeight;
  }
  /**
   * Sets the base height.
   *
   * @deprecated Use baseHeight instead.
   * @param {Number} value - The height.
   */
  setBaseHeight(e) {
    this.baseHeight = e;
  }
  /**
   * Sets the base size.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setBaseSize(e, t) {
    (this.baseSize.width !== e || this.baseSize.height !== t) && (this.baseSize.set(e, t), this.dispatchEvent({ type: "change" }), this.resizable.setSize(this.baseSize.width, this.baseSize.height));
  }
  /**
   * The preferred width.
   *
   * @type {Number}
   */
  get preferredWidth() {
    return this.preferredSize.width;
  }
  set preferredWidth(e) {
    this.preferredSize.width !== e && (this.preferredSize.width = e, this.dispatchEvent({ type: "change" }), this.resizable.setSize(this.baseSize.width, this.baseSize.height));
  }
  /**
   * Returns the preferred width.
   *
   * @deprecated Use preferredWidth instead.
   * @return {Number} The preferred width.
   */
  getPreferredWidth() {
    return this.preferredWidth;
  }
  /**
   * Sets the preferred width.
   *
   * Use {@link Resizer.AUTO_SIZE} to automatically calculate the width based on the height and aspect ratio.
   *
   * @deprecated Use preferredWidth instead.
   * @param {Number} value - The width.
   */
  setPreferredWidth(e) {
    this.preferredWidth = e;
  }
  /**
   * The preferred height.
   *
   * @type {Number}
   */
  get preferredHeight() {
    return this.preferredSize.height;
  }
  set preferredHeight(e) {
    this.preferredSize.height !== e && (this.preferredSize.height = e, this.dispatchEvent({ type: "change" }), this.resizable.setSize(this.baseSize.width, this.baseSize.height));
  }
  /**
   * Returns the preferred height.
   *
   * @deprecated Use preferredHeight instead.
   * @return {Number} The preferred height.
   */
  getPreferredHeight() {
    return this.preferredHeight;
  }
  /**
   * Sets the preferred height.
   *
   * Use {@link Resizer.AUTO_SIZE} to automatically calculate the height based on the width and aspect ratio.
   *
   * @deprecated Use preferredHeight instead.
   * @param {Number} value - The height.
   */
  setPreferredHeight(e) {
    this.preferredHeight = e;
  }
  /**
   * Sets the preferred size.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setPreferredSize(e, t) {
    (this.preferredSize.width !== e || this.preferredSize.height !== t) && (this.preferredSize.set(e, t), this.dispatchEvent({ type: "change" }), this.resizable.setSize(this.baseSize.width, this.baseSize.height));
  }
  /**
   * Copies the given resolution.
   *
   * @param {Resolution} resolution - The resolution.
   */
  copy(e) {
    this.s = e.scale, this.baseSize.set(e.baseWidth, e.baseHeight), this.preferredSize.set(e.preferredWidth, e.preferredHeight), this.dispatchEvent({ type: "change" }), this.resizable.setSize(this.baseSize.width, this.baseSize.height);
  }
  /**
   * An auto sizing constant.
   *
   * Can be used to automatically calculate the width or height based on the original aspect ratio.
   *
   * @type {Number}
   */
  static get AUTO_SIZE() {
    return je;
  }
}
let wr = !1;
class or {
  /**
   * Constructs a new override material manager.
   *
   * @param {Material} [material=null] - An override material.
   */
  constructor(e = null) {
    this.originalMaterials = /* @__PURE__ */ new Map(), this.material = null, this.materials = null, this.materialsBackSide = null, this.materialsDoubleSide = null, this.materialsFlatShaded = null, this.materialsFlatShadedBackSide = null, this.materialsFlatShadedDoubleSide = null, this.setMaterial(e), this.meshCount = 0, this.replaceMaterial = (t) => {
      if (t.isMesh) {
        let r;
        if (t.material.flatShading)
          switch (t.material.side) {
            case jt:
              r = this.materialsFlatShadedDoubleSide;
              break;
            case Et:
              r = this.materialsFlatShadedBackSide;
              break;
            default:
              r = this.materialsFlatShaded;
              break;
          }
        else
          switch (t.material.side) {
            case jt:
              r = this.materialsDoubleSide;
              break;
            case Et:
              r = this.materialsBackSide;
              break;
            default:
              r = this.materials;
              break;
          }
        this.originalMaterials.set(t, t.material), t.isSkinnedMesh ? t.material = r[2] : t.isInstancedMesh ? t.material = r[1] : t.material = r[0], ++this.meshCount;
      }
    };
  }
  /**
   * Clones the given material.
   *
   * @private
   * @param {Material} material - The material.
   * @return {Material} The cloned material.
   */
  cloneMaterial(e) {
    if (!(e instanceof N))
      return e.clone();
    const t = e.uniforms, r = /* @__PURE__ */ new Map();
    for (const i in t) {
      const s = t[i].value;
      s.isRenderTargetTexture && (t[i].value = null, r.set(i, s));
    }
    const n = e.clone();
    for (const i of r)
      t[i[0]].value = i[1], n.uniforms[i[0]].value = i[1];
    return n;
  }
  /**
   * Sets the override material.
   *
   * @param {Material} material - The material.
   */
  setMaterial(e) {
    if (this.disposeMaterials(), this.material = e, e !== null) {
      const t = this.materials = [
        this.cloneMaterial(e),
        this.cloneMaterial(e),
        this.cloneMaterial(e)
      ];
      for (const r of t)
        r.uniforms = Object.assign({}, e.uniforms), r.side = hr;
      t[2].skinning = !0, this.materialsBackSide = t.map((r) => {
        const n = this.cloneMaterial(r);
        return n.uniforms = Object.assign({}, e.uniforms), n.side = Et, n;
      }), this.materialsDoubleSide = t.map((r) => {
        const n = this.cloneMaterial(r);
        return n.uniforms = Object.assign({}, e.uniforms), n.side = jt, n;
      }), this.materialsFlatShaded = t.map((r) => {
        const n = this.cloneMaterial(r);
        return n.uniforms = Object.assign({}, e.uniforms), n.flatShading = !0, n;
      }), this.materialsFlatShadedBackSide = t.map((r) => {
        const n = this.cloneMaterial(r);
        return n.uniforms = Object.assign({}, e.uniforms), n.flatShading = !0, n.side = Et, n;
      }), this.materialsFlatShadedDoubleSide = t.map((r) => {
        const n = this.cloneMaterial(r);
        return n.uniforms = Object.assign({}, e.uniforms), n.flatShading = !0, n.side = jt, n;
      });
    }
  }
  /**
   * Renders the scene with the override material.
   *
   * @private
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Scene} scene - A scene.
   * @param {Camera} camera - A camera.
   */
  render(e, t, r, n = {}) {
    const i = e.shadowMap.enabled;
    e.shadowMap.enabled = !1;
    let s = null;
    if (wr) {
      const o = this.originalMaterials;
      this.meshCount = 0, t.traverse(this.replaceMaterial), s = e.render(t, r, n);
      for (const l of o)
        l[0].material = l[1];
      this.meshCount !== o.size && o.clear();
    } else {
      const o = t.overrideMaterial;
      t.overrideMaterial = this.material, s = e.render(t, r, n), t.overrideMaterial = o;
    }
    return e.shadowMap.enabled = i, s;
  }
  /**
   * Deletes cloned override materials.
   *
   * @private
   */
  disposeMaterials() {
    if (this.material !== null) {
      const e = this.materials.concat(this.materialsBackSide).concat(this.materialsDoubleSide).concat(this.materialsFlatShaded).concat(this.materialsFlatShadedBackSide).concat(this.materialsFlatShadedDoubleSide);
      for (const t of e)
        t.dispose();
    }
  }
  /**
   * Performs cleanup tasks.
   */
  dispose() {
    this.originalMaterials.clear(), this.disposeMaterials();
  }
  /**
   * Indicates whether the override material workaround is enabled.
   *
   * @type {Boolean}
   */
  static get workaroundEnabled() {
    return wr;
  }
  /**
   * Enables or disables the override material workaround globally.
   *
   * This only affects post processing passes and effects.
   *
   * @type {Boolean}
   */
  static set workaroundEnabled(e) {
    wr = e;
  }
}
const yn = /* @__PURE__ */ new y();
class De extends X {
  /**
   * Constructs a new clear pass.
   *
   * @param {Boolean} [color=true] - Determines whether the color buffer should be cleared.
   * @param {Boolean} [depth=true] - Determines whether the depth buffer should be cleared.
   * @param {Boolean} [stencil=false] - Determines whether the stencil buffer should be cleared.
   */
  constructor(e = !0, t = !0, r = !1) {
    super("ClearPass", null, null), this.needsSwap = !1, this.color = e, this.depth = t, this.stencil = r, this.overrideClearColor = null, this.overrideClearAlpha = -1;
  }
  /**
   * Sets the clear flags.
   *
   * @param {Boolean} color - Whether the color buffer should be cleared.
   * @param {Boolean} depth - Whether the depth buffer should be cleared.
   * @param {Boolean} stencil - Whether the stencil buffer should be cleared.
   */
  setClearFlags(e, t, r) {
    this.color = e, this.depth = t, this.stencil = r;
  }
  /**
   * Returns the override clear color. Default is null.
   *
   * @deprecated Use overrideClearColor instead.
   * @return {Color} The clear color.
   */
  getOverrideClearColor() {
    return this.overrideClearColor;
  }
  /**
   * Sets the override clear color.
   *
   * @deprecated Use overrideClearColor instead.
   * @param {Color} value - The clear color.
   */
  setOverrideClearColor(e) {
    this.overrideClearColor = e;
  }
  /**
   * Returns the override clear alpha. Default is -1.
   *
   * @deprecated Use overrideClearAlpha instead.
   * @return {Number} The clear alpha.
   */
  getOverrideClearAlpha() {
    return this.overrideClearAlpha;
  }
  /**
   * Sets the override clear alpha.
   *
   * @deprecated Use overrideClearAlpha instead.
   * @param {Number} value - The clear alpha.
   */
  setOverrideClearAlpha(e) {
    this.overrideClearAlpha = e;
  }
  /**
   * Clears the input buffer or the screen.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i) {
    const s = this.overrideClearColor, o = this.overrideClearAlpha, l = e.getClearAlpha(), u = s !== null, f = o >= 0;
    u ? (e.getClearColor(yn), e.setClearColor(s, f ? o : l)) : f && e.setClearAlpha(o), e.setRenderTarget(this.renderToScreen ? null : t), e.clear(this.color, this.depth, this.stencil), u ? e.setClearColor(yn, l) : f && e.setClearAlpha(l);
  }
}
const Es = !1;
class dt {
  /**
      * 单例实例
      * @type {PerformanceLogger}
      * @private
      */
  static instance = null;
  /**
      * 获取单例实例
      * @return {PerformanceLogger} 日志实例
      */
  static getInstance() {
    return dt.instance === null && (dt.instance = new dt()), dt.instance;
  }
  /**
      * 创建一个新的性能日志实例
      * @param {Boolean} enabled - 是否启用日志
      */
  constructor(e = Es) {
    this.enabled = e, this.timers = /* @__PURE__ */ new Map(), this.collectTimes = !1, this.timeData = /* @__PURE__ */ new Map();
  }
  /**
      * 启用日志
      */
  enable() {
    this.enabled = !0;
  }
  /**
      * 禁用日志
      */
  disable() {
    this.enabled = !1;
  }
  /**
      * 启用时间数据收集，用于性能分析
      */
  enableTimeCollection() {
    this.collectTimes = !0;
  }
  /**
      * 禁用时间数据收集
      */
  disableTimeCollection() {
    this.collectTimes = !1;
  }
  /**
      * 获取收集的时间数据
      * @return {Object} 时间数据
      */
  getTimeData() {
    const e = {};
    for (const [t, r] of this.timeData.entries())
      if (r.length > 0) {
        const n = r.reduce((i, s) => i + s, 0);
        e[t] = {
          count: r.length,
          total: n,
          average: n / r.length,
          min: Math.min(...r),
          max: Math.max(...r)
        };
      }
    return e;
  }
  /**
      * 清除收集的时间数据
      */
  clearTimeData() {
    this.timeData.clear();
  }
  /**
      * 开始计时
      * @param {String} label - 计时标签
      */
  time(e) {
    this.enabled && (console.time(e), this.timers.set(e, performance.now()));
  }
  /**
      * 结束计时并打印结果
      * @param {String} label - 计时标签
      */
  timeEnd(e) {
    if (this.enabled && (console.timeEnd(e), this.collectTimes && this.timers.has(e))) {
      const t = this.timers.get(e), n = performance.now() - t;
      this.timeData.has(e) || this.timeData.set(e, []), this.timeData.get(e).push(n), this.timers.delete(e);
    }
  }
  /**
      * 打印日志
      * @param {...any} args - 日志参数
      */
  log(...e) {
    this.enabled && console.log(...e);
  }
  /**
      * 打印警告
      * @param {...any} args - 警告参数
      */
  warn(...e) {
    this.enabled && console.warn(...e);
  }
  /**
      * 打印错误
      * @param {...any} args - 错误参数
      */
  error(...e) {
    console.error(...e);
  }
  /**
      * 打印性能摘要
      */
  printSummary() {
    if (!this.enabled || !this.collectTimes)
      return;
    console.group("性能日志摘要");
    const e = this.getTimeData();
    for (const [t, r] of Object.entries(e))
      console.log(
        `${t}: ${r.count}次调用, 平均: ${r.average.toFixed(2)}ms, 总计: ${r.total.toFixed(2)}ms, 最小: ${r.min.toFixed(2)}ms, 最大: ${r.max.toFixed(2)}ms`
      );
    console.groupEnd();
  }
}
const Re = dt.getInstance(), qf = () => Re.enable(), Jf = () => Re.disable(), _f = () => Re.enableTimeCollection(), $f = () => Re.disableTimeCollection(), eh = () => Re.getTimeData(), th = () => Re.clearTimeData(), rh = () => Re.printSummary(), ve = (c) => Re.time(c), ge = (c) => Re.timeEnd(c), bi = (...c) => Re.log(...c), nh = (...c) => Re.warn(...c), ih = (...c) => Re.error(...c);
class pe extends X {
  isRenderPass = !0;
  /**
   * Constructs a new render pass.
   *
   * @param {Scene} scene - The scene to render.
   * @param {Camera} camera - The camera to use to render the scene.
   * @param {Material} [overrideMaterial=null] - An override material.
   * @param {Object} [options={}] - Additional options.
   * @param {Boolean} [options.enableGBuffer=false] - Whether to enable G-Buffer generation.
   * @param {Number} [options.resolutionScale=1.0] - Resolution scale for G-Buffer.
   */
  constructor(e, t, r = null, n = {}) {
    super("RenderPass", e, t), this.needsSwap = !1, this.enableGBuffer = n.enableGBuffer || !1, this.resolutionScale = n.resolutionScale || 1, this.clearPass = new De(), this.overrideMaterialManager = r === null ? null : new or(r), this.gBufferRenderTarget = null, this.gBufferMaterial = null, this.enableGBuffer && this.initializeGBuffer(), this.ignoreBackground = !1, this.skipShadowMapUpdate = !1, this.selection = null, this.useRealSize = !0;
  }
  set mainScene(e) {
    this.scene = e;
  }
  set mainCamera(e) {
    this.camera = e;
  }
  get renderToScreen() {
    return super.renderToScreen;
  }
  set renderToScreen(e) {
    super.renderToScreen = e, this.clearPass.renderToScreen = e;
  }
  /**
   * The current override material.
   *
   * @type {Material}
   */
  get overrideMaterial() {
    const e = this.overrideMaterialManager;
    return e !== null ? e.material : null;
  }
  set overrideMaterial(e) {
    const t = this.overrideMaterialManager;
    e !== null ? t !== null ? t.setMaterial(e) : this.overrideMaterialManager = new or(e) : t !== null && (t.dispose(), this.overrideMaterialManager = null);
  }
  /**
   * Returns the current override material.
   *
   * @deprecated Use overrideMaterial instead.
   * @return {Material} The material.
   */
  getOverrideMaterial() {
    return this.overrideMaterial;
  }
  /**
   * Sets the override material.
   *
   * @deprecated Use overrideMaterial instead.
   * @return {Material} value - The material.
   */
  setOverrideMaterial(e) {
    this.overrideMaterial = e;
  }
  /**
   * Indicates whether the target buffer should be cleared before rendering.
   *
   * @type {Boolean}
   * @deprecated Use clearPass.enabled instead.
   */
  get clear() {
    return this.clearPass.enabled;
  }
  set clear(e) {
    this.clearPass.enabled = e;
  }
  /**
   * Returns the selection. Default is `null` (no restriction).
   *
   * @deprecated Use selection instead.
   * @return {Selection} The selection.
   */
  getSelection() {
    return this.selection;
  }
  /**
   * Sets the selection. Set to `null` to disable.
   *
   * @deprecated Use selection instead.
   * @param {Selection} value - The selection.
   */
  setSelection(e) {
    this.selection = e;
  }
  /**
   * Indicates whether the scene background is disabled.
   *
   * @deprecated Use ignoreBackground instead.
   * @return {Boolean} Whether the scene background is disabled.
   */
  isBackgroundDisabled() {
    return this.ignoreBackground;
  }
  /**
   * Enables or disables the scene background.
   *
   * @deprecated Use ignoreBackground instead.
   * @param {Boolean} value - Whether the scene background should be disabled.
   */
  setBackgroundDisabled(e) {
    this.ignoreBackground = e;
  }
  /**
   * Indicates whether the shadow map auto update is disabled.
   *
   * @deprecated Use skipShadowMapUpdate instead.
   * @return {Boolean} Whether the shadow map update is disabled.
   */
  isShadowMapDisabled() {
    return this.skipShadowMapUpdate;
  }
  /**
   * Enables or disables the shadow map auto update.
   *
   * @deprecated Use skipShadowMapUpdate instead.
   * @param {Boolean} value - Whether the shadow map auto update should be disabled.
   */
  setShadowMapDisabled(e) {
    this.skipShadowMapUpdate = e;
  }
  /**
   * Returns the clear pass.
   *
   * @deprecated Use clearPass.enabled instead.
   * @return {ClearPass} The clear pass.
   */
  getClearPass() {
    return this.clearPass;
  }
  /**
   * Renders the scene.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i, s, o = {}, l) {
    ve("RenderPass.render");
    const u = this.scene, f = this.camera, h = this.selection, m = f.layers.mask, p = u.background, d = e.shadowMap.autoUpdate, v = this.renderToScreen ? null : t;
    let x = null;
    if (h !== null && f.layers.set(h.getLayer()), this.skipShadowMapUpdate && (e.shadowMap.autoUpdate = !1), (this.ignoreBackground || this.clearPass.overrideClearColor !== null) && (u.background = null), this.clearPass.enabled && this.clearPass.render(e, t), e.setRenderTarget(v), this.overrideMaterialManager !== null ? x = this.overrideMaterialManager.render(e, u, f, o) : x = e.render(u, f, o), this.enableGBuffer && this.gBufferRenderTarget && this.gBufferMaterial) {
      this.prepareGBufferMaterial();
      const w = u.overrideMaterial;
      this.gBufferMaterial.uniformsNeedUpdate = !0, u.overrideMaterial = this.gBufferMaterial, e.setRenderTarget(this.gBufferRenderTarget), e.clear(), e.render(u, f), u.overrideMaterial = w;
    } else
      this.enableGBuffer;
    return f.layers.mask = m, u.background = p, e.shadowMap.autoUpdate = d, ge("RenderPass.render"), x;
  }
  /**
   * Initialize G-Buffer rendering targets and materials.
   *
   * @private
   */
  initializeGBuffer() {
    const e = Math.max(1, Math.floor(512 * this.resolutionScale)), t = Math.max(1, Math.floor(512 * this.resolutionScale));
    this.gBufferRenderTarget = new A(e, t, {
      count: 4,
      // MRT: color, normal, depth, position
      type: Y,
      format: ne,
      minFilter: F,
      magFilter: F,
      type: K,
      depthBuffer: !0,
      stencilBuffer: !1
    }), this.gBufferMaterial = new vi({
      name: "G-Buffer Material (RenderPass)",
      vertexShader: (
        /* glsl */
        `
				in vec3 position;
				in vec3 normal;
				in vec2 uv;

				out vec3 vNormal;
				out vec3 vViewPosition;
				out vec2 vUv;
				out float vDepth;

				uniform mat4 modelViewMatrix;
				uniform mat4 projectionMatrix;
				uniform mat4 modelMatrix;
				uniform mat3 normalMatrix;
				uniform float cameraNear;
				uniform float cameraFar;

				void main() {
					vUv = uv;
					
					// 计算视图空间位置
					vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
					vViewPosition = mvPosition.xyz;
					
					// 计算世界空间法线
					vec3 transformedNormal = normalMatrix * normal;
					vNormal = normalize(transformedNormal);
					
					// 计算归一化深度 (0=near, 1=far)
					vDepth = (-mvPosition.z - cameraNear) / (cameraFar - cameraNear);
					vDepth = clamp(vDepth, 0.0, 1.0);
					
					gl_Position = projectionMatrix * mvPosition;
				}
			`
      ),
      fragmentShader: (
        /* glsl */
        `
				precision highp float;
				precision highp int;

				// MRT 输出：同时输出到4个纹理
				layout(location = 0) out vec4 gColor;    // 颜色 + metalness (alpha)
				layout(location = 1) out vec4 gNormal;   // 法线 + roughness (alpha)
				layout(location = 2) out vec4 gDepth;    // 深度
				layout(location = 3) out vec4 gPosition; // 位置

				in vec3 vNormal;
				in vec3 vViewPosition;
				in vec2 vUv;
				in float vDepth;

				// 使用默认的材质属性
				uniform vec3 diffuse;
				uniform float metalness;
				uniform float roughness;

				void main() {
					// 基础颜色 - 使用uniform提供的默认值
					vec3 baseColor = diffuse;

					// 金属度
					float metallicValue = metalness;

					// 粗糙度  
					float roughnessValue = roughness;

					// 输出1：基础颜色 + metalness (在alpha通道)
					gColor = vec4(baseColor, metallicValue);
					
					// 输出2：视图空间法线 + roughness (在alpha通道)
					gNormal = vec4(normalize(vNormal) * 0.5 + 0.5, roughnessValue);
					
					// 输出3：归一化深度（只使用R通道）
					gDepth = vec4(vDepth, vDepth, vDepth, 1.0);
					
					// 输出4：视图空间位置
					gPosition = vec4(vViewPosition, 1.0);
				}
			`
      ),
      uniforms: {
        // RawShaderMaterial需要手动提供所有uniform，包括Three.js内置uniform
        // 相机参数
        diffuse: { value: new y(1, 1, 1) },
        metalness: { value: 0 },
        roughness: { value: 1 },
        cameraNear: { value: 0.1 },
        cameraFar: { value: 1e3 },
        // Three.js内置uniform - RawShaderMaterial需要手动提供
        modelViewMatrix: { value: new C() },
        projectionMatrix: { value: new C() },
        modelMatrix: { value: new C() },
        normalMatrix: { value: new Ot() }
      },
      glslVersion: Te
    });
  }
  /**
   * Returns the G-Buffer textures.
   *
   * @returns {Object|null} An object containing the G-Buffer textures, or null if G-Buffer is disabled.
   */
  getGBufferTextures() {
    return !this.enableGBuffer || !this.gBufferRenderTarget ? null : {
      gColor: this.gBufferRenderTarget.textures[0],
      gNormal: this.gBufferRenderTarget.textures[1],
      gDepth: this.gBufferRenderTarget.textures[2],
      gPosition: this.gBufferRenderTarget.textures[3]
    };
  }
  /**
   * Enable G-Buffer generation.
   *
   * @param {Number} [resolutionScale=1.0] - Resolution scale for G-Buffer.
   */
  enableGBufferGeneration(e = 1) {
    this.enableGBuffer = !0, this.resolutionScale = e, this.gBufferRenderTarget || this.initializeGBuffer();
  }
  /**
   * Disable G-Buffer generation.
   */
  disableGBufferGeneration() {
    this.enableGBuffer = !1, this.gBufferRenderTarget && (this.gBufferRenderTarget.dispose(), this.gBufferRenderTarget = null), this.gBufferMaterial && (this.gBufferMaterial.dispose(), this.gBufferMaterial = null);
  }
  /**
   * Set the size of the render targets.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    if (this.enableGBuffer && this.gBufferRenderTarget) {
      const r = Math.max(1, Math.floor(e * this.resolutionScale)), n = Math.max(1, Math.floor(t * this.resolutionScale));
      this.gBufferRenderTarget.setSize(r, n);
    }
  }
  /**
   * Prepare G-Buffer material uniforms.
   *
   * @private
   */
  prepareGBufferMaterial() {
    this.gBufferMaterial && (this.gBufferMaterial.uniforms.cameraNear.value = this.camera.near, this.gBufferMaterial.uniforms.cameraFar.value = this.camera.far, this.gBufferMaterial.uniforms.projectionMatrix.value.copy(this.camera.projectionMatrix), this.gBufferMaterial.uniforms.diffuse.value.setHex(16777215), this.gBufferMaterial.uniforms.metalness.value = 0, this.gBufferMaterial.uniforms.roughness.value = 1);
  }
  /**
   * Dispose of resources.
   */
  dispose() {
    this.disableGBufferGeneration(), this.clearPass && this.clearPass.dispose(), this.overrideMaterialManager && this.overrideMaterialManager.dispose();
  }
}
class Me extends X {
  /**
   * Constructs a new depth pass.
   *
   * @param {Scene} scene - The scene to render.
   * @param {Camera} camera - The camera to use to render the scene.
   * @param {Object} [options] - The options.
   * @param {WebGLRenderTarget} [options.renderTarget] - A custom render target.
   * @param {Number} [options.resolutionScale=1.0] - The resolution scale.
   * @param {Number} [options.resolutionX=Resolution.AUTO_SIZE] - The horizontal resolution.
   * @param {Number} [options.resolutionY=Resolution.AUTO_SIZE] - The vertical resolution.
   * @param {Number} [options.width=Resolution.AUTO_SIZE] - Deprecated. Use resolutionX instead.
   * @param {Number} [options.height=Resolution.AUTO_SIZE] - Deprecated. Use resolutionY instead.
   */
  constructor(e, t, {
    renderTarget: r,
    resolutionScale: n = 1,
    width: i = W.AUTO_SIZE,
    height: s = W.AUTO_SIZE,
    resolutionX: o = i,
    resolutionY: l = s
  } = {}) {
    super("DepthPass"), this.needsSwap = !1, this.isDepthPass = !0, this.depthPacking = ce, this.renderPass = new pe(e, t, new gi({
      depthPacking: ce
    }));
    const u = this.renderPass;
    u.skipShadowMapUpdate = !0, u.ignoreBackground = !0;
    const f = u.clearPass;
    f.overrideClearColor = new y(16777215), f.overrideClearAlpha = 1, this.renderTarget = r, this.renderTarget === void 0 && (this.renderTarget = new A(1, 1, {
      minFilter: b,
      magFilter: b
    }), this.renderTarget.texture.name = "DepthPass.Target");
    const h = this.resolution = new W(this, o, l, n);
    h.addEventListener("change", (m) => this.setSize(h.baseWidth, h.baseHeight));
  }
  set mainScene(e) {
    this.renderPass.mainScene = e;
  }
  set mainCamera(e) {
    this.renderPass.mainCamera = e;
  }
  /**
   * The depth texture.
   *
   * @type {Texture}
   */
  get texture() {
    return this.renderTarget.texture;
  }
  /**
   * Returns the depth texture.
   *
   * @deprecated Use texture instead.
   * @return {Texture} The texture.
   */
  getTexture() {
    return this.renderTarget.texture;
  }
  /**
   * Returns the resolution settings.
   *
   * @deprecated Use resolution instead.
   * @return {Resolution} The resolution.
   */
  getResolution() {
    return this.resolution;
  }
  /**
   * Returns the current resolution scale.
   *
   * @return {Number} The resolution scale.
   * @deprecated Use resolution instead.
   */
  getResolutionScale() {
    return this.resolution.scale;
  }
  /**
   * Sets the resolution scale.
   *
   * @param {Number} scale - The new resolution scale.
   * @deprecated Use resolution instead.
   */
  setResolutionScale(e) {
    this.resolution.scale = e;
  }
  /**
   * Renders the scene depth.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i, s, o = {}) {
    ve("DepthPass.render");
    const l = this.renderToScreen ? null : this.renderTarget;
    this.renderPass.render(e, l, r, n, i, s, o), ge("DepthPass.render");
  }
  /**
   * Updates the size of this pass.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.resolution;
    r.setBaseSize(e, t), this.renderTarget.setSize(r.width, r.height);
  }
}
class Rs extends X {
  /**
   * Constructs a new mask pass.
   *
   * @param {Scene} scene - The scene to render.
   * @param {Camera} camera - The camera to use.
   */
  constructor(e, t) {
    super("MaskPass", e, t), this.needsSwap = !1, this.clearPass = new De(!1, !1, !0), this.inverse = !1;
  }
  set mainScene(e) {
    this.scene = e;
  }
  set mainCamera(e) {
    this.camera = e;
  }
  /**
   * Indicates whether the mask should be inverted.
   *
   * @type {Boolean}
   */
  get inverted() {
    return this.inverse;
  }
  set inverted(e) {
    this.inverse = e;
  }
  /**
   * Indicates whether this pass should clear the stencil buffer.
   *
   * @type {Boolean}
   * @deprecated Use clearPass.enabled instead.
   */
  get clear() {
    return this.clearPass.enabled;
  }
  set clear(e) {
    this.clearPass.enabled = e;
  }
  /**
   * Returns the internal clear pass.
   *
   * @deprecated Use clearPass.enabled instead.
   * @return {ClearPass} The clear pass.
   */
  getClearPass() {
    return this.clearPass;
  }
  /**
   * Indicates whether the mask is inverted.
   *
   * @deprecated Use inverted instead.
   * @return {Boolean} Whether the mask is inverted.
   */
  isInverted() {
    return this.inverted;
  }
  /**
   * Enables or disable mask inversion.
   *
   * @deprecated Use inverted instead.
   * @param {Boolean} value - Whether the mask should be inverted.
   */
  setInverted(e) {
    this.inverted = e;
  }
  /**
   * Renders the effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i) {
    const s = e.getContext(), o = e.state.buffers, l = this.scene, u = this.camera, f = this.clearPass, h = this.inverted ? 0 : 1, m = 1 - h;
    o.color.setMask(!1), o.depth.setMask(!1), o.color.setLocked(!0), o.depth.setLocked(!0), o.stencil.setTest(!0), o.stencil.setOp(s.REPLACE, s.REPLACE, s.REPLACE), o.stencil.setFunc(s.ALWAYS, h, 4294967295), o.stencil.setClear(m), o.stencil.setLocked(!0), this.clearPass.enabled && (this.renderToScreen ? f.render(e, null) : (f.render(e, t), f.render(e, r))), this.renderToScreen ? (e.setRenderTarget(null), e.render(l, u)) : (e.setRenderTarget(t), e.render(l, u), e.setRenderTarget(r), e.render(l, u)), o.color.setLocked(!1), o.depth.setLocked(!1), o.stencil.setLocked(!1), o.stencil.setFunc(s.EQUAL, 1, 4294967295), o.stencil.setOp(s.KEEP, s.KEEP, s.KEEP), o.stencil.setLocked(!0);
  }
}
const Ns = {
  getRenderOpts: () => ({
    depthPass: {},
    renderPassReault: {
      projectionResult: null
    },
    texture: {
      normal: {
        render: !1,
        target: null
      }
    }
  })
}, Je = {
  getAllOpts: () => ({
    projectObject: !0,
    // 控制是否执行对象投影
    updateMatrixWorld: !0,
    sortObjects: !0,
    renderBackground: !0,
    clippingEnabled: !0,
    shadowsEnabled: !0,
    useProgramCache: !1
    // 控制是否使用着
  }),
  getAllCloseOpts: () => ({
    projectObject: !0,
    // 控制是否执行对象投影
    updateMatrixWorld: !0,
    sortObjects: !0,
    renderBackground: !0,
    clippingEnabled: !0,
    shadowsEnabled: !0,
    useProgramCache: !1
    // 控制是否使用着
  }),
  getSubOpths: (c) => ({
    sortObjects: c,
    renderBackground: c
  }),
  getStandardOpts: (c = {}, e = {
    subOptsState: void 0
  }) => {
    const t = e.subOptsState === void 0 ? {} : Je.getSubOpths(e.subOptsState);
    return {
      ...c,
      ...t
    };
  },
  setRenderState: ({
    renderer: c,
    renderState: e,
    scene: t,
    forceState: r
  }) => {
    const n = Je.keepRenderState({
      renderer: c,
      scene: t
    });
    return c.shadowMap.needsUpdate = e ? e.renderer.shadowMap.needsUpdate : r, t && (t.matrixWorldAutoUpdate = e ? e.scene.matrixWorldAutoUpdate : r), n;
  },
  keepRenderState: ({
    renderer: c,
    scene: e,
    state: t
  }) => ({
    renderer: {
      shadowMap: {
        needsUpdate: t === void 0 ? c.shadowMap.needsUpdate : t
      }
    },
    scene: e ? {
      matrixWorldAutoUpdate: t === void 0 ? e.matrixWorldAutoUpdate : t
    } : {}
  }),
  opts: {
    getDepthParamsOpts: () => ({
      projectObject: !0,
      updateMatrixWorld: !1,
      useProgramCache: !1,
      bindingStates: {
        setupFast: !1
      },
      ...Je.getSubOpths(!1)
    })
  }
};
class sh {
  /**
   * Constructs a new effect composer.
   *
   * @param {WebGLRenderer} renderer - The renderer that should be used.
   * @param {Object} [options] - The options.
   * @param {Boolean} [options.depthBuffer=true] - Whether the main render targets should have a depth buffer.
   * @param {Boolean} [options.stencilBuffer=false] - Whether the main render targets should have a stencil buffer.
   * @param {Boolean} [options.alpha] - Deprecated. Buffers are always RGBA since three r137.
   * @param {Number} [options.multisampling=0] - The number of samples used for multisample antialiasing. Requires WebGL 2.
   * @param {Number} [options.frameBufferType] - The type of the internal frame buffers. It's recommended to use HalfFloatType if possible.
   */
  constructor(e = null, {
    depthBuffer: t = !0,
    stencilBuffer: r = !1,
    multisampling: n = 0,
    frameBufferType: i
  } = {}) {
    this.renderer = null, this.inputBuffer = this.createBuffer(t, r, i, n), this.outputBuffer = this.inputBuffer.clone(), this.copyPass = new Ti(), this.depthTexture = null, this.normalTarget = null, this.passes = [], this.timer = new Ds(), this.autoRenderToScreen = !0, this._postProcessingResolutionScale = 1, this.setRenderer(e);
  }
  /**
   * The current amount of samples used for multisample anti-aliasing.
   *
   * @type {Number}
   */
  get multisampling() {
    return this.inputBuffer.samples || 0;
  }
  /**
   * Sets the amount of MSAA samples.
   *
   * Requires WebGL 2. Set to zero to disable multisampling.
   *
   * @type {Number}
   */
  set multisampling(e) {
    const t = this.inputBuffer, r = this.multisampling;
    r > 0 && e > 0 ? (this.inputBuffer.samples = e, this.outputBuffer.samples = e, this.inputBuffer.dispose(), this.outputBuffer.dispose()) : r !== e && (this.inputBuffer.dispose(), this.outputBuffer.dispose(), this.inputBuffer = this.createBuffer(
      t.depthBuffer,
      t.stencilBuffer,
      t.texture.type,
      e
    ), this.inputBuffer.depthTexture = this.depthTexture, this.outputBuffer = this.inputBuffer.clone());
  }
  /**
   * 获取后处理分辨率缩放比例。
   *
   * @type {Number}
   */
  get postProcessingResolutionScale() {
    return this._postProcessingResolutionScale;
  }
  /**
   * 设置后处理分辨率缩放比例。
   * 
   * 值为1.0表示使用完整分辨率，0.5表示使用一半分辨率。
   * 这可以显著提高性能，特别是在高DPI设备上。
   * 
   * 注意：修改此值后需要调用 setSize() 来更新所有后处理通道的分辨率。
   *
   * @type {Number}
   */
  set postProcessingResolutionScale(e) {
    if (e <= 0)
      throw new Error("后处理分辨率缩放比例必须大于0");
    if (this._postProcessingResolutionScale !== e && (this._postProcessingResolutionScale = e, this.renderer)) {
      const t = this.renderer.getSize(new g());
      this.setSize(t.width, t.height);
    }
  }
  /**
   * Returns the internal timer.
   *
   * @return {Timer} The timer.
   */
  getTimer() {
    return this.timer;
  }
  /**
   * Returns the renderer.
   *
   * @return {WebGLRenderer} The renderer.
   */
  getRenderer() {
    return this.renderer;
  }
  /**
   * Sets the renderer.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   */
  setRenderer(e) {
    if (this.renderer = e, e !== null) {
      const t = e.getSize(new g()), r = e.getContext().getContextAttributes().alpha, n = this.inputBuffer.texture.type;
      n === se && e.outputColorSpace === V && (this.inputBuffer.texture.colorSpace = V, this.outputBuffer.texture.colorSpace = V, this.inputBuffer.dispose(), this.outputBuffer.dispose()), e.autoClear = !1, this.setSize(t.width, t.height);
      for (const i of this.passes)
        i.initialize(e, r, n);
      this.depthPass = new Me(this.scene, this.camera), this.depthPass.initialize(e, !1, 0);
    }
  }
  /**
   * Replaces the current renderer with the given one.
   *
   * The auto clear mechanism of the provided renderer will be disabled. If the new render size differs from the
   * previous one, all passes will be updated.
   *
   * By default, the DOM element of the current renderer will automatically be removed from its parent node and the DOM
   * element of the new renderer will take its place.
   *
   * @deprecated Use setRenderer instead.
   * @param {WebGLRenderer} renderer - The new renderer.
   * @param {Boolean} updateDOM - Indicates whether the old canvas should be replaced by the new one in the DOM.
   * @return {WebGLRenderer} The old renderer.
   */
  replaceRenderer(e, t = !0) {
    const r = this.renderer, n = r.domElement.parentNode;
    return this.setRenderer(e), t && n !== null && (n.removeChild(r.domElement), n.appendChild(e.domElement)), r;
  }
  /**
   * Creates a depth texture attachment that will be provided to all passes.
   *
   * Note: When a shader reads from a depth texture and writes to a render target that uses the same depth texture
   * attachment, the depth information will be lost. This happens even if `depthWrite` is disabled.
   *
   * @private
   * @return {DepthTexture} The depth texture.
   */
  createDepthTexture() {
    const e = this.depthTexture = new Oe();
    return this.inputBuffer.depthTexture = e, this.inputBuffer.dispose(), this.inputBuffer.stencilBuffer ? (e.format = os, e.type = ls) : e.type = cs, e;
  }
  createNormalTarget() {
    if (this.normalTarget)
      return this.normalTarget;
    const e = new A(this.inputBuffer.width, this.inputBuffer.height, {
      minFilter: F,
      magFilter: F,
      format: ne,
      type: K
    });
    return this.normalTarget = e, e.texture.name = "EffectComposer.NormalTarget", e.texture.generateMipmaps = !1, e;
  }
  /**
   * Deletes the current depth texture.
   *
   * @private
   */
  deleteDepthTexture() {
    if (this.depthTexture !== null) {
      this.depthTexture.dispose(), this.depthTexture = null, this.inputBuffer.depthTexture = null, this.inputBuffer.dispose();
      for (const e of this.passes)
        e.setDepthTexture(null);
    }
  }
  /**
   * Creates a new render target.
   *
   * @deprecated Create buffers manually via WebGLRenderTarget instead.
   * @param {Boolean} depthBuffer - Whether the render target should have a depth buffer.
   * @param {Boolean} stencilBuffer - Whether the render target should have a stencil buffer.
   * @param {Number} type - The frame buffer type.
   * @param {Number} multisampling - The number of samples to use for antialiasing.
   * @return {WebGLRenderTarget} A new render target that equals the renderer's canvas.
   */
  createBuffer(e, t, r, n) {
    const i = this.renderer, s = i === null ? new g() : i.getDrawingBufferSize(new g()), o = {
      minFilter: F,
      magFilter: F,
      stencilBuffer: t,
      depthBuffer: e,
      type: r
    }, l = new A(s.width, s.height, o);
    return l.textures || (l.textures = []), n > 0 && (l.ignoreDepthForMultisampleCopy = !1, l.samples = n), r === se && i !== null && i.outputColorSpace === V && (l.texture.colorSpace = V), l.texture.name = "EffectComposer.Buffer", l.texture.generateMipmaps = !1, l;
  }
  /**
   * Can be used to change the main scene for all registered passes and effects.
   *
   * @param {Scene} scene - The scene.
   */
  scene = null;
  setMainScene(e) {
    for (const t of this.passes)
      t.mainScene = e;
    this.scene = e;
  }
  /**
   * Can be used to change the main camera for all registered passes and effects.
   *
   * @param {Camera} camera - The camera.
   */
  setMainCamera(e) {
    for (const t of this.passes)
      t.mainCamera = e;
  }
  /**
   * Adds a pass, optionally at a specific index.
   *
   * @param {Pass} pass - A new pass.
   * @param {Number} [index] - An index at which the pass should be inserted.
   */
  addPass(e, t) {
    const r = this.passes, n = this.renderer, i = n.getDrawingBufferSize(new g()), s = n.getContext().getContextAttributes().alpha, o = this.inputBuffer.texture.type;
    if (e.setRenderer(n), e.setSize(i.width, i.height), e.initialize(n, s, o), this.autoRenderToScreen && (r.length > 0 && (r[r.length - 1].renderToScreen = !1), e.renderToScreen && (this.autoRenderToScreen = !1)), t !== void 0 ? r.splice(t, 0, e) : r.push(e), this.autoRenderToScreen && (r[r.length - 1].renderToScreen = !0), e.needsDepthTexture || this.depthTexture !== null)
      if (this.depthTexture === null) {
        const l = this.createDepthTexture();
        for (e of r)
          e.setDepthTexture(l);
      } else
        e.setDepthTexture(this.depthTexture);
  }
  /**
   * Removes a pass.
   *
   * @param {Pass} pass - The pass.
   */
  removePass(e) {
    const t = this.passes, r = t.indexOf(e);
    if (r !== -1 && t.splice(r, 1).length > 0) {
      if (this.depthTexture !== null) {
        const s = (l, u) => l || u.needsDepthTexture;
        t.reduce(s, !1) || (e.getDepthTexture() === this.depthTexture && e.setDepthTexture(null), this.deleteDepthTexture());
      }
      this.autoRenderToScreen && r === t.length && (e.renderToScreen = !1, t.length > 0 && (t[t.length - 1].renderToScreen = !0));
    }
  }
  /**
   * Removes all passes.
   */
  removeAllPasses() {
    const e = this.passes;
    this.deleteDepthTexture(), e.length > 0 && (this.autoRenderToScreen && (e[e.length - 1].renderToScreen = !1), this.passes = []);
  }
  /**
   * Renders all enabled passes in the order in which they were added.
   *
   * @param {Number} [deltaTime] - The time since the last frame in seconds.
   */
  oldMap = {};
  render(e) {
    ve("EffectComposer.render");
    const t = this.renderer, r = this.copyPass;
    let n = this.inputBuffer, i = this.outputBuffer, s = !1, o, l, u, f = this.depthPass;
    e === void 0 && (this.timer.update(), e = this.timer.getDelta());
    const h = Ns.getRenderOpts(), m = this.passes[0];
    if (m?.isRenderPass) {
      const d = m.render(t, n, i, e, s, f, {
        // normalsRenderTarget: this.createNormalTarget(),
        // renderNormals: true
      }, h);
      h.renderPassReault = d;
    }
    const p = Je.setRenderState({
      renderer: t,
      scene: this.scene,
      forceState: !1
    });
    for (const d of this.passes)
      d.enabled && !d.isRenderPass && (d.render(t, n, i, e, s, f, h), d.needsSwap && (s && (r.renderToScreen = d.renderToScreen, o = t.getContext(), l = t.state.buffers.stencil, l.setFunc(o.NOTEQUAL, 1, 4294967295), r.render(t, n, i, e, s), l.setFunc(o.EQUAL, 1, 4294967295)), u = n, n = i, i = u), d instanceof Rs ? s = !0 : d instanceof Cs && (s = !1));
    Je.setRenderState({
      renderer: t,
      scene: this.scene,
      renderState: p
    }), ge("EffectComposer.render");
  }
  /**
   * Sets the size of the buffers, passes and the renderer.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   * @param {Boolean} [updateStyle] - Determines whether the style of the canvas should be updated.
   */
  setSize(e, t, r) {
    const n = this.renderer, i = n.getSize(new g());
    (e === void 0 || t === void 0) && (e = i.width, t = i.height), (i.width !== e || i.height !== t) && n.setSize(e, t, r);
    const s = n.getDrawingBufferSize(new g());
    this.inputBuffer.setSize(s.width, s.height), this.outputBuffer.setSize(s.width, s.height), this.normalTarget !== null && this.normalTarget.setSize(s.width, s.height);
    const o = Math.max(1, Math.round(s.width * this._postProcessingResolutionScale)), l = Math.max(1, Math.round(s.height * this._postProcessingResolutionScale));
    for (const u of this.passes)
      u.useRealSize ? u.setSize(s.width, s.height) : u.setSize(o, l);
  }
  /**
   */
  reset() {
    this.dispose(), this.autoRenderToScreen = !0;
  }
  /**
   * Disposes this composer and all passes.
   */
  dispose() {
    for (const e of this.passes)
      e.dispose();
    this.passes = [], this.inputBuffer !== null && this.inputBuffer.dispose(), this.outputBuffer !== null && this.outputBuffer.dispose(), this.deleteDepthTexture(), this.copyPass.dispose(), this.timer.dispose(), X.fullscreenGeometry.dispose();
  }
}
const Q = {
  NONE: 0,
  DEPTH: 1,
  CONVOLUTION: 2
}, _ = {
  FRAGMENT_HEAD: "FRAGMENT_HEAD",
  FRAGMENT_MAIN_UV: "FRAGMENT_MAIN_UV",
  FRAGMENT_MAIN_IMAGE: "FRAGMENT_MAIN_IMAGE",
  VERTEX_HEAD: "VERTEX_HEAD",
  VERTEX_MAIN_SUPPORT: "VERTEX_MAIN_SUPPORT"
};
class zs {
  /**
   * Constructs new shader data.
   */
  constructor() {
    this.shaderParts = /* @__PURE__ */ new Map([
      [_.FRAGMENT_HEAD, null],
      [_.FRAGMENT_MAIN_UV, null],
      [_.FRAGMENT_MAIN_IMAGE, null],
      [_.VERTEX_HEAD, null],
      [_.VERTEX_MAIN_SUPPORT, null]
    ]), this.defines = /* @__PURE__ */ new Map(), this.uniforms = /* @__PURE__ */ new Map(), this.blendModes = /* @__PURE__ */ new Map(), this.extensions = /* @__PURE__ */ new Set(), this.attributes = Q.NONE, this.varyings = /* @__PURE__ */ new Set(), this.uvTransformation = !1, this.readDepth = !1, this.colorSpace = ir;
  }
}
function Mn(c) {
  let e;
  if (c === 0)
    e = new Float64Array(0);
  else if (c === 1)
    e = new Float64Array([1]);
  else if (c > 1) {
    let t = new Float64Array(c), r = new Float64Array(c);
    for (let n = 1; n <= c; ++n) {
      for (let i = 0; i < n; ++i)
        r[i] = i === 0 || i === n - 1 ? 1 : t[i - 1] + t[i];
      e = r, r = t, t = e;
    }
  }
  return e;
}
class Fs {
  /**
   * Constructs a new Gauss kernel.
   *
   * @param {Number} kernelSize - The kernel size. Should be an odd number in the range [3, 1020].
   * @param {Number} [edgeBias=2] - Determines how many edge coefficients should be cut off for increased accuracy.
   */
  constructor(e, t = 2) {
    this.weights = null, this.offsets = null, this.linearWeights = null, this.linearOffsets = null, this.generate(e, t);
  }
  /**
   * The number of steps for discrete sampling.
   *
   * @type {Number}
   */
  get steps() {
    return this.offsets === null ? 0 : this.offsets.length;
  }
  /**
   * The number of steps for linear sampling.
   *
   * @type {Number}
   */
  get linearSteps() {
    return this.linearOffsets === null ? 0 : this.linearOffsets.length;
  }
  /**
   * Generates the kernel.
   *
   * @private
   * @param {Number} kernelSize - The kernel size.
   * @param {Number} edgeBias - The amount of edge coefficients to ignore.
   */
  generate(e, t) {
    if (e < 3 || e > 1020)
      throw new Error("The kernel size must be in the range [3, 1020]");
    const r = e + t * 2, n = t > 0 ? Mn(r).slice(t, -t) : Mn(r), i = Math.floor((n.length - 1) / 2), s = n.reduce((m, p) => m + p, 0), o = n.slice(i), l = [...Array(i + 1).keys()], u = new Float64Array(Math.floor(l.length / 2)), f = new Float64Array(u.length);
    u[0] = o[0] / s;
    for (let m = 1, p = 1, d = l.length - 1; m < d; m += 2, ++p) {
      const v = l[m], x = l[m + 1], w = o[m], M = o[m + 1], D = w + M, P = (v * w + x * M) / D;
      u[p] = D / s, f[p] = P;
    }
    for (let m = 0, p = o.length, d = 1 / s; m < p; ++m)
      o[m] *= d;
    const h = (u.reduce((m, p) => m + p, 0) - u[0] * 0.5) * 2;
    if (h !== 0)
      for (let m = 0, p = u.length, d = 1 / h; m < p; ++m)
        u[m] *= d;
    this.offsets = l, this.weights = o, this.linearOffsets = f, this.linearWeights = u;
  }
}
class ah {
  /**
   * The current delta time in seconds.
   *
   * @type {Number}
   */
  getDelta() {
    return NaN;
  }
  /**
   * The elapsed time in seconds.
   *
   * @type {Number}
   */
  getElapsed() {
    return NaN;
  }
}
class oh {
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - A renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
  }
}
class lh {
  /**
   * Sets the size of this object.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
  }
}
class Is {
  /**
   * Constructs a new ID manager.
   *
   * @param initialId - The first ID.
   */
  constructor(e = 0) {
    this.nextId = e;
  }
  /**
   * Returns the next unique ID.
   *
   * @return The ID.
   */
  getNextId() {
    return this.nextId++;
  }
  /**
   * Resets the ID counter.
   *
   * @param initialId - The first ID.
   * @return This manager.
   */
  reset(e = 0) {
    return this.nextId = e, this;
  }
}
const Tr = /* @__PURE__ */ new Is(2);
class Se extends Set {
  /**
   * Constructs a new selection.
   *
   * @param {Iterable<Object3D>} [iterable] - A collection of objects that should be added to this selection.
   * @param {Number} [layer] - A dedicated render layer for selected objects. Range is `[2, 31]`. Starts at 2 if omitted.
   */
  constructor(e, t = Tr.getNextId()) {
    super(), this.exclusive = !1, this._layer = t, (this._layer < 1 || this._layer > 31) && (console.warn("Layer out of range, resetting to 2"), Tr.reset(2), this._layer = Tr.getNextId()), e !== void 0 && this.set(e);
  }
  /**
   * The render layer for selected objects.
   *
   * @type {Number}
   */
  get layer() {
    return this._layer;
  }
  set layer(e) {
    const t = this._layer;
    for (const r of this)
      r.layers.disable(t), r.layers.enable(e);
    this._layer = e;
  }
  /**
   * Returns the current render layer for selected objects.
   *
   * The default layer is 2. If this collides with your own custom layers, please change it before rendering!
   *
   * @deprecated Use layer instead.
   * @return {Number} The layer.
   */
  getLayer() {
    return this.layer;
  }
  /**
   * Sets the render layer for selected objects.
   *
   * The current selection will be updated accordingly.
   *
   * @deprecated Use layer instead.
   * @param {Number} value - The layer. Range is [0, 31].
   */
  setLayer(e) {
    this.layer = e;
  }
  /**
   * Indicates whether objects that are added to this selection will be removed from all other layers.
   *
   * @deprecated Use exclusive instead.
   * @return {Number} Whether this selection is exclusive. Default is false.
   */
  isExclusive() {
    return this.exclusive;
  }
  /**
   * Controls whether objects that are added to this selection should be removed from all other layers.
   *
   * @deprecated Use exclusive instead.
   * @param {Number} value - Whether this selection should be exclusive.
   */
  setExclusive(e) {
    this.exclusive = e;
  }
  /**
   * Clears this selection.
   *
   * @return {Selection} This selection.
   */
  clear() {
    const e = this.layer;
    for (const t of this)
      t.layers.disable(e);
    return super.clear();
  }
  /**
   * Clears this selection and adds the given objects.
   *
   * @param {Iterable<Object3D>} objects - The objects that should be selected.
   * @return {Selection} This selection.
   */
  set(e) {
    this.clear();
    for (const t of e)
      this.add(t);
    return this;
  }
  /**
   * An alias for {@link has}.
   *
   * @param {Object3D} object - An object.
   * @return {Number} Returns 0 if the given object is currently selected, or -1 otherwise.
   * @deprecated Added for backward-compatibility.
   */
  indexOf(e) {
    return this.has(e) ? 0 : -1;
  }
  /**
   * Adds an object to this selection.
   *
   * If {@link exclusive} is set to `true`, the object will also be removed from all other layers.
   *
   * @param {Object3D} object - The object that should be selected.
   * @return {Selection} This selection.
   */
  add(e) {
    return this.exclusive ? e.layers.set(this.layer) : e.layers.enable(this.layer), super.add(e);
  }
  /**
   * Removes an object from this selection.
   *
   * @param {Object3D} object - The object that should be deselected.
   * @return {Boolean} Returns true if an object has successfully been removed from this selection; otherwise false.
   */
  delete(e) {
    return this.has(e) && e.layers.disable(this.layer), super.delete(e);
  }
  /**
   * Removes an existing object from the selection. If the object doesn't exist it's added instead.
   *
   * @param {Object3D} object - The object.
   * @return {Boolean} Returns true if the object is added, false otherwise.
   */
  toggle(e) {
    let t;
    return this.has(e) ? (this.delete(e), t = !1) : (this.add(e), t = !0), t;
  }
  /**
   * Sets the visibility of all selected objects.
   *
   * This method enables or disables render layer 0 of all selected objects.
   *
   * @param {Boolean} visible - Whether the selected objects should be visible.
   * @return {Selection} This selection.
   */
  setVisible(e) {
    for (const t of this)
      e ? t.layers.enable(0) : t.layers.disable(0);
    return this;
  }
}
const ch = () => ({
  camera: ke
}), Bs = {
  SCALE_UP: "lut.scaleup"
};
function wn(c, e, t) {
  const r = document.createElement("canvas"), n = r.getContext("2d");
  if (r.width = c, r.height = e, t instanceof Image)
    n.drawImage(t, 0, 0);
  else {
    const i = n.createImageData(c, e);
    i.data.set(t), n.putImageData(i, 0, 0);
  }
  return r;
}
class Ge {
  /**
   * Constructs a new image data container.
   *
   * @param {Number} [width=0] - The width of the image.
   * @param {Number} [height=0] - The height of the image.
   * @param {Uint8ClampedArray} [data=null] - The image data.
   */
  constructor(e = 0, t = 0, r = null) {
    this.width = e, this.height = t, this.data = r;
  }
  /**
   * Creates a canvas from this image data.
   *
   * @return {Canvas} The canvas, or null if it couldn't be created.
   */
  toCanvas() {
    return typeof document > "u" ? null : wn(this.width, this.height, this.data);
  }
  /**
   * Creates a new image data container.
   *
   * @param {ImageData|Image} image - An image or plain image data.
   * @return {RawImageData} The image data.
   */
  static from(e) {
    const { width: t, height: r } = e;
    let n;
    if (e instanceof Image) {
      const i = wn(t, r, e);
      i !== null && (n = i.getContext("2d").getImageData(0, 0, t, r).data);
    } else
      n = e.data;
    return new Ge(t, r, n);
  }
}
const Ls = "data:text/plain;base64,", Tn = /* @__PURE__ */ new y();
class rt extends _t {
  /**
   * Constructs a cubic 3D lookup texture.
   *
   * @param {TypedArray} data - The pixel data. The default format is RGBA.
   * @param {Number} size - The sidelength.
   */
  constructor(e, t) {
    super(e, t, t, t), this.type = Y, this.format = ne, this.minFilter = F, this.magFilter = F, this.wrapS = tt, this.wrapT = tt, this.wrapR = tt, this.unpackAlignment = 1, this.needsUpdate = !0, this.colorSpace = ir, this.domainMin = new T(0, 0, 0), this.domainMax = new T(1, 1, 1);
  }
  /**
   * Indicates that this is an instance of LookupTexture3D.
   *
   * @type {Boolean}
   * @deprecated
   */
  get isLookupTexture3D() {
    return !0;
  }
  /**
   * Scales this LUT up to a given target size using tetrahedral interpolation.
   *
   * @param {Number} size - The target sidelength.
   * @param {Boolean} [transferData=true] - Extra fast mode. Set to false to keep the original data intact.
   * @return {Promise<LookupTexture>} A promise that resolves with a new LUT upon completion.
   */
  scaleUp(e, t = !0) {
    const r = this.image;
    let n;
    return e <= r.width ? n = Promise.reject(new Error("The target size must be greater than the current size")) : n = new Promise((i, s) => {
      const o = URL.createObjectURL(new Blob([Ls], {
        type: "text/javascript"
      })), l = new Worker(o);
      l.addEventListener("error", (f) => s(f.error)), l.addEventListener("message", (f) => {
        const h = new rt(f.data, e);
        this.colorSpace = h.colorSpace, h.type = this.type, h.name = this.name, URL.revokeObjectURL(o), i(h);
      });
      const u = t ? [r.data.buffer] : [];
      l.postMessage({
        operation: Bs.SCALE_UP,
        data: r.data,
        size: e
      }, u);
    }), n;
  }
  /**
   * Applies the given LUT to this one.
   *
   * @param {LookupTexture} lut - A LUT. Must have the same dimensions, type and format as this LUT.
   * @return {LookupTexture} This texture.
   */
  applyLUT(e) {
    const t = this.image, r = e.image, n = Math.min(t.width, t.height, t.depth), i = Math.min(r.width, r.height, r.depth);
    if (n !== i)
      console.error("Size mismatch");
    else if (e.type !== Y || this.type !== Y)
      console.error("Both LUTs must be FloatType textures");
    else if (e.format !== ne || this.format !== ne)
      console.error("Both LUTs must be RGBA textures");
    else {
      const s = t.data, o = r.data, l = n, u = l ** 2, f = l - 1;
      for (let h = 0, m = l ** 3; h < m; ++h) {
        const p = h * 4, d = s[p + 0] * f, v = s[p + 1] * f, x = s[p + 2] * f, w = Math.round(d + v * l + x * u) * 4;
        s[p + 0] = o[w + 0], s[p + 1] = o[w + 1], s[p + 2] = o[w + 2];
      }
      this.needsUpdate = !0;
    }
    return this;
  }
  /**
   * Converts the LUT data into unsigned byte data.
   *
   * This is a lossy operation which should only be performed after all other transformations have been applied.
   *
   * @return {LookupTexture} This texture.
   */
  convertToUint8() {
    if (this.type === Y) {
      const e = this.image.data, t = new Uint8Array(e.length);
      for (let r = 0, n = e.length; r < n; ++r)
        t[r] = e[r] * 255 + 0.5;
      this.image.data = t, this.type = se, this.needsUpdate = !0;
    }
    return this;
  }
  /**
   * Converts the LUT data into float data.
   *
   * @return {LookupTexture} This texture.
   */
  convertToFloat() {
    if (this.type === se) {
      const e = this.image.data, t = new Float32Array(e.length);
      for (let r = 0, n = e.length; r < n; ++r)
        t[r] = e[r] / 255;
      this.image.data = t, this.type = Y, this.needsUpdate = !0;
    }
    return this;
  }
  /**
   * Converts this LUT into RGBA data.
   *
   * @deprecated LUTs are RGBA by default since three r137.
   * @return {LookupTexture} This texture.
   */
  convertToRGBA() {
    return console.warn("LookupTexture", "convertToRGBA() is deprecated, LUTs are now RGBA by default"), this;
  }
  /**
   * Converts the output of this LUT into sRGB color space.
   *
   * @return {LookupTexture} This texture.
   */
  convertLinearToSRGB() {
    const e = this.image.data;
    if (this.type === Y) {
      for (let t = 0, r = e.length; t < r; t += 4)
        Tn.fromArray(e, t).convertLinearToSRGB().toArray(e, t);
      this.colorSpace = V, this.needsUpdate = !0;
    } else
      console.error("Color space conversion requires FloatType data");
    return this;
  }
  /**
   * Converts the output of this LUT into linear color space.
   *
   * @return {LookupTexture} This texture.
   */
  convertSRGBToLinear() {
    const e = this.image.data;
    if (this.type === Y) {
      for (let t = 0, r = e.length; t < r; t += 4)
        Tn.fromArray(e, t).convertSRGBToLinear().toArray(e, t);
      this.colorSpace = ir, this.needsUpdate = !0;
    } else
      console.error("Color space conversion requires FloatType data");
    return this;
  }
  /**
   * Converts this LUT into a 2D data texture.
   *
   * Please note that custom input domains are not carried over to 2D textures.
   *
   * @return {DataTexture} The texture.
   */
  toDataTexture() {
    const e = this.image.width, t = this.image.height * this.image.depth, r = new le(this.image.data, e, t);
    return r.name = this.name, r.type = this.type, r.format = this.format, r.minFilter = F, r.magFilter = F, r.wrapS = this.wrapS, r.wrapT = this.wrapT, r.generateMipmaps = !1, r.needsUpdate = !0, this.colorSpace = r.colorSpace, r;
  }
  /**
   * Creates a new 3D LUT by copying a given LUT.
   *
   * Common image-based textures will be converted into 3D data textures.
   *
   * @param {Texture} texture - The LUT. Assumed to be cubic.
   * @return {LookupTexture} A new 3D LUT.
   */
  static from(e) {
    const t = e.image, { width: r, height: n } = t, i = Math.min(r, n);
    let s;
    if (t instanceof Image) {
      const u = Ge.from(t).data;
      if (r > n) {
        s = new Uint8Array(u.length);
        for (let f = 0; f < i; ++f)
          for (let h = 0; h < i; ++h)
            for (let m = 0; m < i; ++m) {
              const p = (m + f * i + h * i * i) * 4, d = (m + h * i + f * i * i) * 4;
              s[d + 0] = u[p + 0], s[d + 1] = u[p + 1], s[d + 2] = u[p + 2], s[d + 3] = u[p + 3];
            }
      } else
        s = new Uint8Array(u.buffer);
    } else
      s = t.data.slice();
    const o = new rt(s, i);
    return o.type = e.type, o.name = e.name, e.colorSpace = o.colorSpace, o;
  }
  /**
   * Creates a neutral 3D LUT.
   *
   * @param {Number} size - The sidelength.
   * @return {LookupTexture} A neutral 3D LUT.
   */
  static createNeutral(e) {
    const t = new Float32Array(e ** 3 * 4), r = e ** 2, n = 1 / (e - 1);
    for (let s = 0; s < e; ++s)
      for (let o = 0; o < e; ++o)
        for (let l = 0; l < e; ++l) {
          const u = (s + o * e + l * r) * 4;
          t[u + 0] = s * n, t[u + 1] = o * n, t[u + 2] = l * n, t[u + 3] = 1;
        }
    const i = new rt(t, e);
    return i.name = "neutral", i;
  }
}
class uh extends Xr {
  /**
   * Loads a LUT.
   *
   * @param {String} url - The URL of the 3dl-file.
   * @param {Function} [onLoad] - A callback that receives the loaded lookup texture.
   * @param {Function} [onProgress] - A progress callback that receives the XMLHttpRequest instance.
   * @param {Function} [onError] - An error callback that receives the URL of the file that failed to load.
   * @return {Promise<LookupTexture>} A promise that returns the lookup texture.
   */
  load(e, t = () => {
  }, r = () => {
  }, n = null) {
    const i = this.manager, s = new kt(), o = new xi(s);
    return o.setPath(this.path), o.setResponseType("text"), new Promise((l, u) => {
      s.onError = (f) => {
        i.itemError(f), n !== null ? (n(`Failed to load ${f}`), l()) : u(`Failed to load ${f}`);
      }, i.itemStart(e), o.load(e, (f) => {
        try {
          const h = this.parse(f);
          i.itemEnd(e), t(h), l(h);
        } catch (h) {
          console.error(h), s.onError(e);
        }
      }, r);
    });
  }
  /**
   * Parses the given data.
   *
   * @param {String} input - The LUT data.
   * @return {LookupTexture} The lookup texture.
   * @throws {Error} Fails if the data is invalid.
   */
  parse(e) {
    const t = /^[\d ]+$/m, r = /^([\d.e+-]+) +([\d.e+-]+) +([\d.e+-]+) *$/gm;
    let n = t.exec(e);
    if (n === null)
      throw new Error("Missing grid information");
    const i = n[0].trim().split(/\s+/g).map((d) => Number(d)), s = i[1] - i[0], o = i.length, l = o ** 2;
    for (let d = 1, v = i.length; d < v; ++d)
      if (s !== i[d] - i[d - 1])
        throw new Error("Inconsistent grid size");
    const u = new Float32Array(o ** 3 * 4);
    let f = 0, h = 0;
    for (; (n = r.exec(e)) !== null; ) {
      const d = Number(n[1]), v = Number(n[2]), x = Number(n[3]);
      f = Math.max(f, d, v, x);
      const w = h % o, M = Math.floor(h / o) % o, D = Math.floor(h / l) % o, P = (w * l + M * o + D) * 4;
      u[P + 0] = d, u[P + 1] = v, u[P + 2] = x, u[P + 3] = 1, ++h;
    }
    const m = Math.ceil(Math.log2(f)), p = Math.pow(2, m);
    for (let d = 0, v = u.length; d < v; d += 4)
      u[d + 0] /= p, u[d + 1] /= p, u[d + 2] /= p;
    return new rt(u, o);
  }
}
class fh extends Xr {
  /**
   * Loads a LUT.
   *
   * @param {String} url - The URL of the CUBE-file.
   * @param {Function} [onLoad] - A callback that receives the loaded lookup texture.
   * @param {Function} [onProgress] - A progress callback that receives the XMLHttpRequest instance.
   * @param {Function} [onError] - An error callback that receives the URL of the file that failed to load.
   * @return {Promise<LookupTexture>} A promise that returns the lookup texture.
   */
  load(e, t = () => {
  }, r = () => {
  }, n = null) {
    const i = this.manager, s = new kt(), o = new xi(s);
    return o.setPath(this.path), o.setResponseType("text"), new Promise((l, u) => {
      s.onError = (f) => {
        i.itemError(f), n !== null ? (n(`Failed to load ${f}`), l()) : u(`Failed to load ${f}`);
      }, i.itemStart(e), o.load(e, (f) => {
        try {
          const h = this.parse(f);
          i.itemEnd(e), t(h), l(h);
        } catch (h) {
          console.error(h), s.onError(e);
        }
      }, r);
    });
  }
  /**
   * Parses the given data.
   *
   * @param {String} input - The LUT data.
   * @return {LookupTexture} The lookup texture.
   * @throws {Error} Fails if the data is invalid.
   */
  parse(e) {
    const t = /TITLE +"([^"]*)"/, r = /LUT_3D_SIZE +(\d+)/, n = /DOMAIN_MIN +([\d.]+) +([\d.]+) +([\d.]+)/, i = /DOMAIN_MAX +([\d.]+) +([\d.]+) +([\d.]+)/, s = /^([\d.e+-]+) +([\d.e+-]+) +([\d.e+-]+) *$/gm;
    let o = t.exec(e);
    const l = o !== null ? o[1] : null;
    if (o = r.exec(e), o === null)
      throw new Error("Missing LUT_3D_SIZE information");
    const u = Number(o[1]), f = new Float32Array(u ** 3 * 4), h = new T(0, 0, 0), m = new T(1, 1, 1);
    if (o = n.exec(e), o !== null && h.set(Number(o[1]), Number(o[2]), Number(o[3])), o = i.exec(e), o !== null && m.set(Number(o[1]), Number(o[2]), Number(o[3])), h.x > m.x || h.y > m.y || h.z > m.z)
      throw h.set(0, 0, 0), m.set(1, 1, 1), new Error("Invalid input domain");
    let p = 0;
    for (; (o = s.exec(e)) !== null; )
      f[p++] = Number(o[1]), f[p++] = Number(o[2]), f[p++] = Number(o[3]), f[p++] = 1;
    const d = new rt(f, u);
    return d.domainMin.copy(h), d.domainMax.copy(m), l !== null && (d.name = l), d;
  }
}
const Fr = "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAEAAAAAQCAYAAACm53kpAAAAeElEQVRYR+2XSwqAMAxEJ168ePEqwRSKhIIiuHjJqiU0gWE+1CQdApcVAMUAuARaMGCX1MIL/Ow13++9lW2s3mW9MWvsnWc/2fvGygwPAN4E8QzAA4CXAB6AHjG4JTHYI1ey3pcx6FHnEfhLDOIBKAmUBK6/ANUDTlROXAHd9EC1AAAAAElFTkSuQmCC", Ir = "data:image/png;base64,";
class hh extends Xr {
  /**
   * Loads the SMAA data images.
   *
   * @param {Function} [onLoad] - A callback that receives the search image and area image as a pair.
   * @param {Function} [onError] - An error callback that receives the URL of the image that failed to load.
   * @return {Promise<Image[]>} A promise that returns the search image and area image as a pair.
   */
  load(e = () => {
  }, t = null) {
    arguments.length === 4 ? (e = arguments[1], t = arguments[3]) : (arguments.length === 3 || typeof arguments[0] != "function") && (e = arguments[1], t = null);
    const r = this.manager, n = new kt();
    return new Promise((i, s) => {
      const o = new Image(), l = new Image();
      n.onError = (u) => {
        r.itemError(u), t !== null ? (t(`Failed to load ${u}`), i()) : s(`Failed to load ${u}`);
      }, n.onLoad = () => {
        const u = [o, l];
        e(u), i(u);
      }, o.addEventListener("error", (u) => {
        n.itemError("smaa-search");
      }), l.addEventListener("error", (u) => {
        n.itemError("smaa-area");
      }), o.addEventListener("load", () => {
        r.itemEnd("smaa-search"), n.itemEnd("smaa-search");
      }), l.addEventListener("load", () => {
        r.itemEnd("smaa-area"), n.itemEnd("smaa-area");
      }), r.itemStart("smaa-search"), r.itemStart("smaa-area"), n.itemStart("smaa-search"), n.itemStart("smaa-area"), o.src = Fr, l.src = Ir;
    });
  }
}
const E = {
  SKIP: 9,
  SET: 30,
  ADD: 0,
  ALPHA: 1,
  AVERAGE: 2,
  COLOR: 3,
  COLOR_BURN: 4,
  COLOR_DODGE: 5,
  DARKEN: 6,
  DIFFERENCE: 7,
  DIVIDE: 8,
  DST: 9,
  EXCLUSION: 10,
  HARD_LIGHT: 11,
  HARD_MIX: 12,
  HUE: 13,
  INVERT: 14,
  INVERT_RGB: 15,
  LIGHTEN: 16,
  LINEAR_BURN: 17,
  LINEAR_DODGE: 18,
  LINEAR_LIGHT: 19,
  LUMINOSITY: 20,
  MULTIPLY: 21,
  NEGATION: 22,
  NORMAL: 23,
  OVERLAY: 24,
  PIN_LIGHT: 25,
  REFLECT: 26,
  SATURATION: 27,
  SCREEN: 28,
  SOFT_LIGHT: 29,
  SRC: 30,
  SUBTRACT: 31,
  VIVID_LIGHT: 32
}, Us = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, x + y, opacity);\r
\r
}\r
`, Os = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, y, min(y.a, opacity));\r
\r
}\r
`, ks = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, (x + y) * 0.5, opacity);\r
\r
}\r
`, Vs = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	vec3 xHSL = RGBToHSL(x.rgb);\r
	vec3 yHSL = RGBToHSL(y.rgb);\r
	vec3 z = HSLToRGB(vec3(yHSL.rg, xHSL.b));\r
	return vec4(mix(x.rgb, z, opacity), y.a);\r
\r
}\r
`, Hs = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	vec4 z = mix(step(0.0, y) * (1.0 - min(vec4(1.0), (1.0 - x) / y)), vec4(1.0), step(1.0, x));\r
	return mix(x, z, opacity);\r
\r
}\r
`, Gs = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	vec4 z = step(0.0, x) * mix(min(vec4(1.0), x / max(1.0 - y, 1e-9)), vec4(1.0), step(1.0, y));\r
	return mix(x, z, opacity);\r
\r
}\r
`, Ws = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, min(x, y), opacity);\r
\r
}\r
`, js = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, abs(x - y), opacity);\r
\r
}\r
`, Xs = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, x / max(y, 1e-12), opacity);\r
\r
}\r
`, Ys = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, (x + y - 2.0 * x * y), opacity);\r
\r
}\r
`, Zs = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	vec4 a = min(x, 1.0), b = min(y, 1.0);\r
	vec4 z = mix(2.0 * a * b, 1.0 - 2.0 * (1.0 - a) * (1.0 - b), step(0.5, y));\r
	return mix(x, z, opacity);\r
\r
}\r
`, Ks = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, step(1.0, x + y), opacity);\r
\r
}\r
`, Qs = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	vec3 xHSL = RGBToHSL(x.rgb);\r
	vec3 yHSL = RGBToHSL(y.rgb);\r
	vec3 z = HSLToRGB(vec3(yHSL.r, xHSL.gb));\r
	return vec4(mix(x.rgb, z, opacity), y.a);\r
\r
}\r
`, qs = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, 1.0 - y, opacity);\r
\r
}\r
`, Js = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, y * (1.0 - x), opacity);\r
\r
}\r
`, _s = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, max(x, y), opacity);\r
\r
}\r
`, $s = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, clamp(y + x - 1.0, 0.0, 1.0), opacity);\r
\r
}\r
`, ea = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, min(x + y, 1.0), opacity);\r
\r
}\r
`, ta = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, clamp(2.0 * y + x - 1.0, 0.0, 1.0), opacity);\r
\r
}\r
`, ra = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	vec3 xHSL = RGBToHSL(x.rgb);\r
	vec3 yHSL = RGBToHSL(y.rgb);\r
	vec3 z = HSLToRGB(vec3(xHSL.rg, yHSL.b));\r
	return vec4(mix(x.rgb, z, opacity), y.a);\r
\r
}\r
`, na = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, x * y, opacity);\r
\r
}\r
`, ia = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, 1.0 - abs(1.0 - x - y), opacity);\r
\r
}\r
`, sa = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, y, opacity);\r
\r
}\r
`, aa = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	vec4 z = mix(2.0 * y * x, 1.0 - 2.0 * (1.0 - y) * (1.0 - x), step(0.5, x));\r
	return mix(x, z, opacity);\r
\r
}\r
`, oa = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	vec4 y2 = 2.0 * y;\r
\r
	vec4 z = mix(\r
		mix(y2, x, step(0.5 * x, y)),\r
		max(vec4(0.0), y2 - 1.0), \r
		step(x, (y2 - 1.0))\r
	);\r
\r
	return mix(x, z, opacity);\r
\r
}\r
`, la = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	vec4 z = mix(min(x * x / max(1.0 - y, 1e-12), 1.0), y, step(1.0, y));\r
	return mix(x, z, opacity);\r
\r
}\r
`, ca = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	vec3 xHSL = RGBToHSL(x.rgb);\r
	vec3 yHSL = RGBToHSL(y.rgb);\r
	vec3 z = HSLToRGB(vec3(xHSL.r, yHSL.g, xHSL.b));\r
	return vec4(mix(x.rgb, z, opacity), y.a);\r
\r
}\r
`, ua = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, x + y - min(x * y, 1.0), opacity);\r
\r
}\r
`, fa = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	vec4 y2 = 2.0 * y;\r
	vec4 w = step(0.5, y);\r
\r
	vec4 z = mix(\r
		x - (1.0 - y2) * x * (1.0 - x), \r
		mix(\r
			x + (y2 - 1.0) * (sqrt(x) - x),\r
			x + (y2 - 1.0) * x * ((16.0 * x - 12.0) * x + 3.0),\r
			w * (1.0 - step(0.25, x))\r
		),\r
		w\r
	);\r
\r
	return mix(x, z, opacity);\r
\r
}\r
`, ha = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return y;\r
\r
}\r
`, da = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	return mix(x, max(x + y - 1.0, 0.0), opacity);\r
\r
}\r
`, ma = `vec4 blend(const in vec4 x, const in vec4 y, const in float opacity) {\r
\r
	vec4 z = mix(\r
		max(1.0 - min((1.0 - x) / (2.0 * y), 1.0), 0.0),\r
		min(x / (2.0 * (1.0 - y)), 1.0),\r
		step(0.5, y)\r
	);\r
\r
	return mix(x, z, opacity);\r
\r
}\r
`, pa = /* @__PURE__ */ new Map([
  [E.ADD, Us],
  [E.ALPHA, Os],
  [E.AVERAGE, ks],
  [E.COLOR, Vs],
  [E.COLOR_BURN, Hs],
  [E.COLOR_DODGE, Gs],
  [E.DARKEN, Ws],
  [E.DIFFERENCE, js],
  [E.DIVIDE, Xs],
  [E.DST, null],
  [E.EXCLUSION, Ys],
  [E.HARD_LIGHT, Zs],
  [E.HARD_MIX, Ks],
  [E.HUE, Qs],
  [E.INVERT, qs],
  [E.INVERT_RGB, Js],
  [E.LIGHTEN, _s],
  [E.LINEAR_BURN, $s],
  [E.LINEAR_DODGE, ea],
  [E.LINEAR_LIGHT, ta],
  [E.LUMINOSITY, ra],
  [E.MULTIPLY, na],
  [E.NEGATION, ia],
  [E.NORMAL, sa],
  [E.OVERLAY, aa],
  [E.PIN_LIGHT, oa],
  [E.REFLECT, la],
  [E.SATURATION, ca],
  [E.SCREEN, ua],
  [E.SOFT_LIGHT, fa],
  [E.SRC, ha],
  [E.SUBTRACT, da],
  [E.VIVID_LIGHT, ma]
]);
class va extends jr {
  /**
   * Constructs a new blend mode.
   *
   * @param {BlendFunction} blendFunction - The blend function.
   * @param {Number} opacity - The opacity of the color that will be blended with the base color.
   */
  constructor(e, t = 1) {
    super(), this._blendFunction = e, this.opacity = new a(t);
  }
  /**
   * Returns the opacity.
   *
   * @return {Number} The opacity.
   */
  getOpacity() {
    return this.opacity.value;
  }
  /**
   * Sets the opacity.
   *
   * @param {Number} value - The opacity.
   */
  setOpacity(e) {
    this.opacity.value = e;
  }
  /**
   * The blend function.
   *
   * @type {BlendFunction}
   */
  get blendFunction() {
    return this._blendFunction;
  }
  set blendFunction(e) {
    this._blendFunction = e, this.dispatchEvent({ type: "change" });
  }
  /**
   * Returns the blend function.
   *
   * @deprecated Use blendFunction instead.
   * @return {BlendFunction} The blend function.
   */
  getBlendFunction() {
    return this.blendFunction;
  }
  /**
   * Sets the blend function.
   *
   * @deprecated Use blendFunction instead.
   * @param {BlendFunction} value - The blend function.
   */
  setBlendFunction(e) {
    this.blendFunction = e;
  }
  /**
   * Returns the blend function shader code.
   *
   * @return {String} The blend function shader code.
   */
  getShaderCode() {
    return pa.get(this.blendFunction);
  }
}
class ga extends Si {
  /**
   * Constructs a new ASCII texture.
   *
   * @param {Object} [options] - The options.
   * @param {String} [options.characters] - The character set to render. Defaults to a common ASCII art charset.
   * @param {String} [options.font="Arial"] - The font.
   * @param {Number} [options.fontSize=54] - The font size in pixels.
   * @param {Number} [options.size=1024] - The texture size.
   * @param {Number} [options.cellCount=16] - The cell count along each side of the texture.
   */
  constructor({
    characters: e = " .:,'-^=*+?!|0#X%WM@",
    font: t = "Arial",
    fontSize: r = 54,
    size: n = 1024,
    cellCount: i = 16
  } = {}) {
    super(
      document.createElement("canvas"),
      void 0,
      G,
      G
    );
    const s = this.image;
    s.width = s.height = n;
    const o = s.getContext("2d"), l = n / i;
    o.font = `${r}px ${t}`, o.textAlign = "center", o.textBaseline = "middle", o.fillStyle = "#ffffff";
    for (let u = 0, f = e.length; u < f; ++u) {
      const h = e[u], m = u % i, p = Math.floor(u / i);
      o.fillText(h, m * l + l / 2, p * l + l / 2);
    }
    this.characterCount = e.length, this.cellCount = i;
  }
}
class I extends jr {
  /**
   * Constructs a new effect.
   *
   * @param {String} name - The name of this effect. Doesn't have to be unique.
   * @param {String} fragmentShader - The fragment shader. This shader is required.
   * @param {Object} [options] - Additional options.
   * @param {EffectAttribute} [options.attributes=EffectAttribute.NONE] - The effect attributes that determine the execution priority and resource requirements.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.NORMAL] - The blend function of this effect.
   * @param {Map<String, String>} [options.defines] - Custom preprocessor macro definitions. Keys are names and values are code.
   * @param {Map<String, Uniform>} [options.uniforms] - Custom shader uniforms. Keys are names and values are uniforms.
   * @param {Set<WebGLExtension>} [options.extensions] - WebGL extensions.
   * @param {String} [options.vertexShader=null] - The vertex shader. Most effects don't need one.
   */
  constructor(e, t, {
    attributes: r = Q.NONE,
    blendFunction: n = E.NORMAL,
    defines: i = /* @__PURE__ */ new Map(),
    uniforms: s = /* @__PURE__ */ new Map(),
    extensions: o = null,
    vertexShader: l = null
  } = {}) {
    super(), this.name = e, this.renderer = null, this.attributes = r, this.fragmentShader = t, this.vertexShader = l, this.defines = i, this.uniforms = s, this.extensions = o, this.blendMode = new va(n), this.blendMode.addEventListener("change", (u) => this.setChanged()), this._inputColorSpace = ir, this._outputColorSpace = Fe;
  }
  /**
   * The input color space.
   *
   * @type {ColorSpace}
   * @experimental
   */
  get inputColorSpace() {
    return this._inputColorSpace;
  }
  /**
   * @type {ColorSpace}
   * @protected
   * @experimental
   */
  set inputColorSpace(e) {
    this._inputColorSpace = e, this.setChanged();
  }
  /**
   * The output color space.
   *
   * Should only be changed if this effect converts the input colors to a different color space.
   *
   * @type {ColorSpace}
   * @experimental
   */
  get outputColorSpace() {
    return this._outputColorSpace;
  }
  /**
   * @type {ColorSpace}
   * @protected
   * @experimental
   */
  set outputColorSpace(e) {
    this._outputColorSpace = e, this.setChanged();
  }
  /**
   * Sets the main scene.
   *
   * @type {Scene}
   */
  set mainScene(e) {
  }
  /**
   * Sets the main camera.
   *
   * @type {Camera}
   */
  set mainCamera(e) {
  }
  /**
   * Returns the name of this effect.
   *
   * @deprecated Use name instead.
   * @return {String} The name.
   */
  getName() {
    return this.name;
  }
  /**
   * Sets the renderer.
   *
   * @deprecated
   * @param {WebGLRenderer} renderer - The renderer.
   */
  setRenderer(e) {
    this.renderer = e;
  }
  /**
   * Returns the preprocessor macro definitions.
   *
   * @deprecated Use defines instead.
   * @return {Map<String, String>} The extensions.
   */
  getDefines() {
    return this.defines;
  }
  /**
   * Returns the uniforms of this effect.
   *
   * @deprecated Use uniforms instead.
   * @return {Map<String, Uniform>} The extensions.
   */
  getUniforms() {
    return this.uniforms;
  }
  /**
   * Returns the WebGL extensions that are required by this effect.
   *
   * @deprecated Use extensions instead.
   * @return {Set<WebGLExtension>} The extensions.
   */
  getExtensions() {
    return this.extensions;
  }
  /**
   * Returns the blend mode.
   *
   * The result of this effect will be blended with the result of the previous effect using this blend mode.
   *
   * @deprecated Use blendMode instead.
   * @return {BlendMode} The blend mode.
   */
  getBlendMode() {
    return this.blendMode;
  }
  /**
   * Returns the effect attributes.
   *
   * @return {EffectAttribute} The attributes.
   */
  getAttributes() {
    return this.attributes;
  }
  /**
   * Sets the effect attributes.
   *
   * Effects that have the same attributes will be executed in the order in which they were registered. Some attributes
   * imply a higher priority.
   *
   * @protected
   * @param {EffectAttribute} attributes - The attributes.
   */
  setAttributes(e) {
    this.attributes = e, this.setChanged();
  }
  /**
   * Returns the fragment shader.
   *
   * @return {String} The fragment shader.
   */
  getFragmentShader() {
    return this.fragmentShader;
  }
  /**
   * Sets the fragment shader.
   *
   * @protected
   * @param {String} fragmentShader - The fragment shader.
   */
  setFragmentShader(e) {
    this.fragmentShader = e, this.setChanged();
  }
  /**
   * Returns the vertex shader.
   *
   * @return {String} The vertex shader.
   */
  getVertexShader() {
    return this.vertexShader;
  }
  /**
   * Sets the vertex shader.
   *
   * @protected
   * @param {String} vertexShader - The vertex shader.
   */
  setVertexShader(e) {
    this.vertexShader = e, this.setChanged();
  }
  /**
   * Informs the associated {@link EffectPass} that this effect requires a shader recompilation.
   *
   * Should be called after changing macros or extensions and after adding/removing uniforms.
   *
   * @protected
   */
  setChanged() {
    this.dispatchEvent({ type: "change" });
  }
  setUpdateEffectPass(e) {
    this.dispatchEvent({ type: "updateEffectPass", map: e });
  }
  /**
   * Sets the depth texture.
   *
   * You may override this method if your effect requires direct access to the depth texture that is bound to the
   * associated {@link EffectPass}.
   *
   * @param {Texture} depthTexture - A depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing.
   */
  setDepthTexture(e, t = $) {
  }
  /**
   * Updates this effect by performing supporting operations.
   *
   * This method is called by the {@link EffectPass} right before the main fullscreen render operation, even if the
   * blend function is set to `SKIP`.
   *
   * You may override this method if you need to update custom uniforms or render additional off-screen textures.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   */
  update(e, t, r) {
  }
  /**
   * Updates the size of this effect.
   *
   * You may override this method if you want to be informed about the size of the backbuffer/canvas.
   * This method is called before {@link initialize} and every time the size of the {@link EffectComposer} changes.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
  }
  /**
   * Performs initialization tasks.
   *
   * This method is called when the associated {@link EffectPass} is added to an {@link EffectComposer}.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   * @example if(!alpha && frameBufferType === UnsignedByteType) { this.myRenderTarget.texture.format = RGBFormat; }
   */
  initialize(e, t, r) {
  }
  /**
   * Performs a shallow search for properties that define a dispose method and deletes them.
   *
   * The {@link EffectComposer} calls this method when it is being destroyed.
   */
  dispose() {
    for (const e of Object.keys(this)) {
      const t = this[e];
      (t instanceof A || t instanceof mi || t instanceof It || t instanceof X) && this[e].dispose();
    }
  }
}
const xa = `uniform sampler2D asciiTexture;\r
uniform vec4 cellCount; // XY = cell count, ZW = inv cell count\r
\r
#ifdef USE_COLOR\r
\r
	uniform vec3 color;\r
\r
#endif\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	vec2 pixelizedUv = cellCount.zw * (0.5 + floor(uv * cellCount.xy));\r
	vec4 texel = texture(inputBuffer, pixelizedUv);\r
\r
	// Use the luminance of the cell as a factor from 0 to 1 to index the character.\r
	float lum = min(luminance(texel.rgb), 1.0);\r
\r
	#ifdef INVERTED\r
\r
		lum = 1.0 - lum;\r
\r
	#endif\r
\r
	float characterIndex = floor(CHAR_COUNT_MINUS_ONE * lum);\r
	vec2 characterPosition = vec2(mod(characterIndex, TEX_CELL_COUNT), floor(characterIndex * INV_TEX_CELL_COUNT));\r
	vec2 offset = vec2(characterPosition.x, -characterPosition.y) * INV_TEX_CELL_COUNT;\r
	vec2 characterUv = mod(uv * (cellCount.xy * INV_TEX_CELL_COUNT), INV_TEX_CELL_COUNT);\r
	characterUv = characterUv - vec2(0.0, INV_TEX_CELL_COUNT) + offset;\r
	float asciiCharacter = texture(asciiTexture, characterUv).r;\r
\r
	#ifdef USE_COLOR\r
\r
		outputColor = vec4(color * asciiCharacter, inputColor.a);\r
\r
	#else\r
\r
		outputColor = vec4(texel.rgb * asciiCharacter, inputColor.a);\r
\r
	#endif\r
\r
}`;
class dh extends I {
  /**
   * Constructs a new ASCII effect.
   *
   * @param {Object} [options] - The options.
   * @param {ASCIITexture} [options.asciiTexture] - An ASCII character lookup texture.
   * @param {Number} [options.cellSize=16] - The cell size. It's recommended to use even numbers.
   * @param {Number} [options.color=null] - A color to use instead of the scene colors.
   * @param {Boolean} [options.inverted=false] - Inverts the effect.
   */
  constructor({
    asciiTexture: e = new ga(),
    cellSize: t = 16,
    color: r = null,
    inverted: n = !1
  } = {}) {
    super("ASCIIEffect", xa, {
      uniforms: /* @__PURE__ */ new Map([
        ["asciiTexture", new a(null)],
        ["cellCount", new a(new ze())],
        ["color", new a(new y())]
      ])
    }), this._cellSize = -1, this.resolution = new g(), this.asciiTexture = e, this.cellSize = t, this.color = r, this.inverted = n;
  }
  /**
   * The current ASCII lookup texture.
   *
   * @type {ASCIITexture}
   */
  get asciiTexture() {
    return this.uniforms.get("asciiTexture").value;
  }
  set asciiTexture(e) {
    const t = this.uniforms.get("asciiTexture").value;
    if (this.uniforms.get("asciiTexture").value = e, t !== null && t !== e && t.dispose(), e !== null) {
      const r = e.cellCount;
      this.defines.set("CHAR_COUNT_MINUS_ONE", (e.characterCount - 1).toFixed(1)), this.defines.set("TEX_CELL_COUNT", r.toFixed(1)), this.defines.set("INV_TEX_CELL_COUNT", (1 / r).toFixed(9)), this.setChanged();
    }
  }
  /**
   * A color that overrides the scene colors.
   *
   * @type {Color | String | Number | null}
   */
  get color() {
    return this.uniforms.get("color").value;
  }
  set color(e) {
    e !== null && this.uniforms.get("color").value.set(e), this.defines.has("USE_COLOR") && e === null ? (this.defines.delete("USE_COLOR"), this.setChanged()) : !this.defines.has("USE_COLOR") && e !== null && (this.defines.set("USE_COLOR", "1"), this.setChanged());
  }
  /**
   * Controls whether the effect should be inverted.
   *
   * @type {Boolean}
   */
  get inverted() {
    return this.defines.has("INVERTED");
  }
  set inverted(e) {
    this.inverted !== e && (e ? this.defines.set("INVERTED", "1") : this.defines.delete("INVERTED"), this.setChanged());
  }
  /**
   * The cell size.
   *
   * @type {Number}
   */
  get cellSize() {
    return this._cellSize;
  }
  set cellSize(e) {
    this._cellSize !== e && (this._cellSize = e, this.updateCellCount());
  }
  /**
   * Updates the cell count uniform.
   *
   * @private
   */
  updateCellCount() {
    const e = this.uniforms.get("cellCount").value, t = this.resolution;
    e.x = t.width / this.cellSize, e.y = t.height / this.cellSize, e.z = 1 / e.x, e.w = 1 / e.y;
  }
  /**
   * Updates the size of this pass.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.resolution.set(e, t), this.updateCellCount();
  }
  /**
   * Deletes internal render targets and textures.
   */
  dispose() {
    this.asciiTexture !== null && this.asciiTexture.dispose(), super.dispose();
  }
}
const Le = {
  VERY_SMALL: 0,
  SMALL: 1,
  MEDIUM: 2,
  LARGE: 3,
  VERY_LARGE: 4,
  HUGE: 5
}, Sa = `#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D inputBuffer;\r
\r
#else\r
\r
	uniform lowp sampler2D inputBuffer;\r
\r
#endif\r
\r
varying vec2 vUv0;\r
varying vec2 vUv1;\r
varying vec2 vUv2;\r
varying vec2 vUv3;\r
\r
void main() {\r
\r
	vec4 sum = texture2D(inputBuffer, vUv0); // Top left\r
	sum += texture2D(inputBuffer, vUv1); // Top right\r
	sum += texture2D(inputBuffer, vUv2); // Bottom right\r
	sum += texture2D(inputBuffer, vUv3); // Bottom left\r
	gl_FragColor = sum * 0.25; // Compute the average\r
\r
	#include <colorspace_fragment>\r
\r
}\r
`, ya = `uniform vec4 texelSize; // XY = texel size, ZW = half texel size\r
uniform float kernel;\r
uniform float scale;\r
\r
varying vec2 vUv0;\r
varying vec2 vUv1;\r
varying vec2 vUv2;\r
varying vec2 vUv3;\r
\r
void main() {\r
\r
	vec2 uv = position.xy * 0.5 + 0.5;\r
	vec2 dUv = (texelSize.xy * vec2(kernel) + texelSize.zw) * scale;\r
\r
	vUv0 = vec2(uv.x - dUv.x, uv.y + dUv.y);\r
	vUv1 = vec2(uv.x + dUv.x, uv.y + dUv.y);\r
	vUv2 = vec2(uv.x + dUv.x, uv.y - dUv.y);\r
	vUv3 = vec2(uv.x - dUv.x, uv.y - dUv.y);\r
\r
	gl_Position = vec4(position.xy, 1.0, 1.0);\r
\r
}\r
`, Ma = [
  new Float32Array([0, 0]),
  new Float32Array([0, 1, 1]),
  new Float32Array([0, 1, 1, 2]),
  new Float32Array([0, 1, 2, 2, 3]),
  new Float32Array([0, 1, 2, 3, 4, 4, 5]),
  new Float32Array([0, 1, 2, 3, 4, 5, 7, 8, 9, 10])
];
class Di extends N {
  /**
   * Constructs a new convolution material.
   *
   * TODO Remove texelSize param.
   * @param {Vector4} [texelSize] - Deprecated.
   */
  constructor(e = new ze()) {
    super({
      name: "KawaseBlurMaterial",
      uniforms: {
        inputBuffer: new a(null),
        texelSize: new a(new ze()),
        scale: new a(1),
        kernel: new a(0)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: Sa,
      vertexShader: ya
    }), this.setTexelSize(e.x, e.y), this.kernelSize = Le.MEDIUM;
  }
  /**
   * The input buffer.
   *
   * @type {Texture}
   */
  set inputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * Sets the input buffer.
   *
   * @deprecated Use inputBuffer instead.
   * @param {Texture} value - The input buffer.
   */
  setInputBuffer(e) {
    this.inputBuffer = e;
  }
  /**
   * The kernel sequence for the current kernel size.
   *
   * @type {Float32Array}
   */
  get kernelSequence() {
    return Ma[this.kernelSize];
  }
  /**
   * The blur scale.
   *
   * @type {Number}
   */
  get scale() {
    return this.uniforms.scale.value;
  }
  set scale(e) {
    this.uniforms.scale.value = e;
  }
  /**
   * Returns the blur scale.
   *
   * @deprecated Use scale instead.
   * @return {Number} The scale.
   */
  getScale() {
    return this.uniforms.scale.value;
  }
  /**
   * Sets the blur scale.
   *
   * @deprecated Use scale instead.
   * @return {Number} value - The scale.
   */
  setScale(e) {
    this.uniforms.scale.value = e;
  }
  /**
   * Returns the kernel.
   *
   * @return {Float32Array} The kernel.
   * @deprecated Implementation detail, removed with no replacement.
   */
  getKernel() {
    return null;
  }
  /**
   * The current kernel.
   *
   * @type {Number}
   */
  get kernel() {
    return this.uniforms.kernel.value;
  }
  set kernel(e) {
    this.uniforms.kernel.value = e;
  }
  /**
   * Sets the current kernel.
   *
   * @deprecated Use kernel instead.
   * @param {Number} value - The kernel.
   */
  setKernel(e) {
    this.kernel = e;
  }
  /**
   * Sets the texel size.
   *
   * @deprecated Use setSize() instead.
   * @param {Number} x - The texel width.
   * @param {Number} y - The texel height.
   */
  setTexelSize(e, t) {
    this.uniforms.texelSize.value.set(e, t, e * 0.5, t * 0.5);
  }
  /**
   * Sets the size of this object.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = 1 / e, n = 1 / t;
    this.uniforms.texelSize.value.set(r, n, r * 0.5, n * 0.5);
  }
}
class at extends X {
  /**
   * Constructs a new Kawase blur pass.
   *
   * @param {Object} [options] - The options.
   * @param {KernelSize} [options.kernelSize=KernelSize.MEDIUM] - The blur kernel size.
   * @param {Number} [options.resolutionScale=0.5] - The resolution scale.
   * @param {Number} [options.resolutionX=Resolution.AUTO_SIZE] - The horizontal resolution.
   * @param {Number} [options.resolutionY=Resolution.AUTO_SIZE] - The vertical resolution.
   * @param {Number} [options.width=Resolution.AUTO_SIZE] - Deprecated. Use resolutionX instead.
   * @param {Number} [options.height=Resolution.AUTO_SIZE] - Deprecated. Use resolutionY instead.
   */
  constructor({
    kernelSize: e = Le.MEDIUM,
    resolutionScale: t = 0.5,
    width: r = W.AUTO_SIZE,
    height: n = W.AUTO_SIZE,
    resolutionX: i = r,
    resolutionY: s = n
  } = {}) {
    super("KawaseBlurPass"), this.renderTargetA = new A(1, 1, { depthBuffer: !1 }), this.renderTargetA.texture.name = "Blur.Target.A", this.renderTargetB = this.renderTargetA.clone(), this.renderTargetB.texture.name = "Blur.Target.B";
    const o = this.resolution = new W(this, i, s, t);
    o.addEventListener("change", (l) => this.setSize(o.baseWidth, o.baseHeight)), this._blurMaterial = new Di(), this._blurMaterial.kernelSize = e, this.copyMaterial = new dr();
  }
  /**
   * Returns the resolution settings.
   *
   * @deprecated Use resolution instead.
   * @return {Resolution} The resolution.
   */
  getResolution() {
    return this.resolution;
  }
  /**
   * The blur material.
   *
   * @type {KawaseBlurMaterial}
   */
  get blurMaterial() {
    return this._blurMaterial;
  }
  /**
   * The blur material.
   *
   * @type {KawaseBlurMaterial}
   * @protected
   */
  set blurMaterial(e) {
    this._blurMaterial = e;
  }
  /**
   * Indicates whether dithering is enabled.
   *
   * @type {Boolean}
   * @deprecated Use copyMaterial.dithering instead.
   */
  get dithering() {
    return this.copyMaterial.dithering;
  }
  set dithering(e) {
    this.copyMaterial.dithering = e;
  }
  /**
   * The kernel size.
   *
   * @type {KernelSize}
   * @deprecated Use blurMaterial.kernelSize instead.
   */
  get kernelSize() {
    return this.blurMaterial.kernelSize;
  }
  set kernelSize(e) {
    this.blurMaterial.kernelSize = e;
  }
  /**
   * The current width of the internal render targets.
   *
   * @type {Number}
   * @deprecated Use resolution.width instead.
   */
  get width() {
    return this.resolution.width;
  }
  /**
   * Sets the render width.
   *
   * @type {Number}
   * @deprecated Use resolution.preferredWidth instead.
   */
  set width(e) {
    this.resolution.preferredWidth = e;
  }
  /**
   * The current height of the internal render targets.
   *
   * @type {Number}
   * @deprecated Use resolution.height instead.
   */
  get height() {
    return this.resolution.height;
  }
  /**
   * Sets the render height.
   *
   * @type {Number}
   * @deprecated Use resolution.preferredHeight instead.
   */
  set height(e) {
    this.resolution.preferredHeight = e;
  }
  /**
   * The current blur scale.
   *
   * @type {Number}
   * @deprecated Use blurMaterial.scale instead.
   */
  get scale() {
    return this.blurMaterial.scale;
  }
  set scale(e) {
    this.blurMaterial.scale = e;
  }
  /**
   * Returns the current blur scale.
   *
   * @deprecated Use blurMaterial.scale instead.
   * @return {Number} The scale.
   */
  getScale() {
    return this.blurMaterial.scale;
  }
  /**
   * Sets the blur scale.
   *
   * @deprecated Use blurMaterial.scale instead.
   * @param {Number} value - The scale.
   */
  setScale(e) {
    this.blurMaterial.scale = e;
  }
  /**
   * Returns the kernel size.
   *
   * @deprecated Use blurMaterial.kernelSize instead.
   * @return {KernelSize} The kernel size.
   */
  getKernelSize() {
    return this.kernelSize;
  }
  /**
   * Sets the kernel size.
   *
   * Larger kernels require more processing power but scale well with larger render resolutions.
   *
   * @deprecated Use blurMaterial.kernelSize instead.
   * @param {KernelSize} value - The kernel size.
   */
  setKernelSize(e) {
    this.kernelSize = e;
  }
  /**
   * Returns the current resolution scale.
   *
   * @return {Number} The resolution scale.
   * @deprecated Use resolution instead.
   */
  getResolutionScale() {
    return this.resolution.scale;
  }
  /**
   * Sets the resolution scale.
   *
   * @param {Number} scale - The new resolution scale.
   * @deprecated Use resolution instead.
   */
  setResolutionScale(e) {
    this.resolution.scale = e;
  }
  /**
   * Renders the blur.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i) {
    const s = this.scene, o = this.camera, l = this.renderTargetA, u = this.renderTargetB, f = this.blurMaterial, h = f.kernelSequence;
    let m = t;
    this.fullscreenMaterial = f;
    for (let p = 0, d = h.length; p < d; ++p) {
      const v = (p & 1) === 0 ? l : u;
      f.kernel = h[p], f.inputBuffer = m.texture, e.setRenderTarget(v), e.render(s, o), m = v;
    }
    this.fullscreenMaterial = this.copyMaterial, this.copyMaterial.inputBuffer = m.texture, e.setRenderTarget(this.renderToScreen ? null : r), e.render(s, o);
  }
  /**
   * Updates the size of this pass.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.resolution;
    r.setBaseSize(e, t);
    const n = r.width, i = r.height;
    this.renderTargetA.setSize(n, i), this.renderTargetB.setSize(n, i), this.blurMaterial.setSize(e, t);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    r !== void 0 && (this.renderTargetA.texture.type = r, this.renderTargetB.texture.type = r, r !== se ? (this.blurMaterial.defines.FRAMEBUFFER_PRECISION_HIGH = "1", this.copyMaterial.defines.FRAMEBUFFER_PRECISION_HIGH = "1") : e !== null && e.outputColorSpace === V && (this.renderTargetA.texture.colorSpace = V, this.renderTargetB.texture.colorSpace = V));
  }
  /**
   * An auto sizing flag.
   *
   * @type {Number}
   * @deprecated Use {@link Resolution.AUTO_SIZE} instead.
   */
  static get AUTO_SIZE() {
    return W.AUTO_SIZE;
  }
}
const wa = `#include <common>\r
\r
#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D inputBuffer;\r
\r
#else\r
\r
	uniform lowp sampler2D inputBuffer;\r
\r
#endif\r
\r
#ifdef RANGE\r
\r
	uniform vec2 range;\r
\r
#elif defined(THRESHOLD)\r
\r
	uniform float threshold;\r
	uniform float smoothing;\r
\r
#endif\r
\r
varying vec2 vUv;\r
\r
void main() {\r
\r
	vec4 texel = texture2D(inputBuffer, vUv);\r
	float l = luminance(texel.rgb);\r
\r
	#ifdef RANGE\r
\r
		// Apply a luminance range mask.\r
		float low = step(range.x, l);\r
		float high = step(l, range.y);\r
\r
		l *= low * high;\r
\r
	#elif defined(THRESHOLD)\r
\r
		l = smoothstep(threshold, threshold + smoothing, l) * l;\r
\r
	#endif\r
\r
	#ifdef COLOR\r
\r
		gl_FragColor = vec4(texel.rgb * clamp(l, 0.0, 1.0), l);\r
\r
	#else\r
\r
		gl_FragColor = vec4(l);\r
\r
	#endif\r
\r
}\r
`;
class Ta extends N {
  /**
   * Constructs a new luminance material.
   *
   * @param {Boolean} [colorOutput=false] - Defines whether the shader should output colors scaled with their luminance value.
   * @param {Vector2} [luminanceRange] - If provided, the shader will mask out texels that aren't in the specified luminance range.
   */
  constructor(e = !1, t = null) {
    super({
      name: "LuminanceMaterial",
      defines: {
        THREE_REVISION: Vt.replace(/\D+/g, "")
      },
      uniforms: {
        inputBuffer: new a(null),
        threshold: new a(0),
        smoothing: new a(1),
        range: new a(null)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: wa,
      vertexShader: Be
    }), this.colorOutput = e, this.luminanceRange = t;
  }
  /**
   * The input buffer.
   *
   * @type {Texture}
   */
  set inputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * Sets the input buffer.
   *
   * @deprecated Use inputBuffer instead.
   * @param {Texture} value - The input buffer.
   */
  setInputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * The luminance threshold.
   *
   * @type {Number}
   */
  get threshold() {
    return this.uniforms.threshold.value;
  }
  set threshold(e) {
    this.smoothing > 0 || e > 0 ? this.defines.THRESHOLD = "1" : delete this.defines.THRESHOLD, this.uniforms.threshold.value = e;
  }
  /**
   * Returns the luminance threshold.
   *
   * @deprecated Use threshold instead.
   * @return {Number} The threshold.
   */
  getThreshold() {
    return this.threshold;
  }
  /**
   * Sets the luminance threshold.
   *
   * @deprecated Use threshold instead.
   * @param {Number} value - The threshold.
   */
  setThreshold(e) {
    this.threshold = e;
  }
  /**
   * The luminance threshold smoothing.
   *
   * @type {Number}
   */
  get smoothing() {
    return this.uniforms.smoothing.value;
  }
  set smoothing(e) {
    this.threshold > 0 || e > 0 ? this.defines.THRESHOLD = "1" : delete this.defines.THRESHOLD, this.uniforms.smoothing.value = e;
  }
  /**
   * Returns the luminance threshold smoothing factor.
   *
   * @deprecated Use smoothing instead.
   * @return {Number} The smoothing factor.
   */
  getSmoothingFactor() {
    return this.smoothing;
  }
  /**
   * Sets the luminance threshold smoothing factor.
   *
   * @deprecated Use smoothing instead.
   * @param {Number} value - The smoothing factor.
   */
  setSmoothingFactor(e) {
    this.smoothing = e;
  }
  /**
   * Indicates whether the luminance threshold is enabled.
   *
   * @type {Boolean}
   * @deprecated Adjust the threshold or smoothing factor instead.
   */
  get useThreshold() {
    return this.threshold > 0 || this.smoothing > 0;
  }
  set useThreshold(e) {
  }
  /**
   * Indicates whether color output is enabled.
   *
   * @type {Boolean}
   */
  get colorOutput() {
    return this.defines.COLOR !== void 0;
  }
  set colorOutput(e) {
    e ? this.defines.COLOR = "1" : delete this.defines.COLOR, this.needsUpdate = !0;
  }
  /**
   * Indicates whether color output is enabled.
   *
   * @deprecated Use colorOutput instead.
   * @return {Boolean} Whether color output is enabled.
   */
  isColorOutputEnabled(e) {
    return this.colorOutput;
  }
  /**
   * Enables or disables color output.
   *
   * @deprecated Use colorOutput instead.
   * @param {Boolean} value - Whether color output should be enabled.
   */
  setColorOutputEnabled(e) {
    this.colorOutput = e;
  }
  /**
   * Indicates whether luminance masking is enabled.
   *
   * @type {Boolean}
   * @deprecated
   */
  get useRange() {
    return this.luminanceRange !== null;
  }
  set useRange(e) {
    this.luminanceRange = null;
  }
  /**
   * The luminance range. Set to null to disable.
   *
   * @type {Boolean}
   */
  get luminanceRange() {
    return this.uniforms.range.value;
  }
  set luminanceRange(e) {
    e !== null ? this.defines.RANGE = "1" : delete this.defines.RANGE, this.uniforms.range.value = e, this.needsUpdate = !0;
  }
  /**
   * Returns the current luminance range.
   *
   * @deprecated Use luminanceRange instead.
   * @return {Vector2} The luminance range.
   */
  getLuminanceRange() {
    return this.luminanceRange;
  }
  /**
   * Sets a luminance range. Set to null to disable.
   *
   * @deprecated Use luminanceRange instead.
   * @param {Vector2} value - The luminance range.
   */
  setLuminanceRange(e) {
    this.luminanceRange = e;
  }
}
class Pi extends X {
  /**
   * Constructs a new luminance pass.
   *
   * @param {Object} [options] - The options. See {@link LuminanceMaterial} for additional options.
   * @param {WebGLRenderTarget} [options.renderTarget] - A custom render target.
   * @param {Number} [options.resolutionScale=1.0] - The resolution scale.
   * @param {Number} [options.resolutionX=Resolution.AUTO_SIZE] - The horizontal resolution.
   * @param {Number} [options.resolutionY=Resolution.AUTO_SIZE] - The vertical resolution.
   * @param {Number} [options.width=Resolution.AUTO_SIZE] - Deprecated. Use resolutionX instead.
   * @param {Number} [options.height=Resolution.AUTO_SIZE] - Deprecated. Use resolutionY instead.
   */
  constructor({
    renderTarget: e,
    luminanceRange: t,
    colorOutput: r,
    resolutionScale: n = 1,
    width: i = W.AUTO_SIZE,
    height: s = W.AUTO_SIZE,
    resolutionX: o = i,
    resolutionY: l = s
  } = {}) {
    super("LuminancePass"), this.fullscreenMaterial = new Ta(r, t), this.needsSwap = !1, this.renderTarget = e, this.renderTarget === void 0 && (this.renderTarget = new A(1, 1, { depthBuffer: !1 }), this.renderTarget.texture.name = "LuminancePass.Target");
    const u = this.resolution = new W(this, o, l, n);
    u.addEventListener("change", (f) => this.setSize(u.baseWidth, u.baseHeight));
  }
  /**
   * The luminance texture.
   *
   * @type {Texture}
   */
  get texture() {
    return this.renderTarget.texture;
  }
  /**
   * Returns the luminance texture.
   *
   * @deprecated Use texture instead.
   * @return {Texture} The texture.
   */
  getTexture() {
    return this.renderTarget.texture;
  }
  /**
   * Returns the resolution settings.
   *
   * @deprecated Use resolution instead.
   * @return {Resolution} The resolution.
   */
  getResolution() {
    return this.resolution;
  }
  /**
   * Renders the luminance.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i) {
    const s = this.fullscreenMaterial;
    s.inputBuffer = t.texture, e.setRenderTarget(this.renderToScreen ? null : this.renderTarget), e.render(this.scene, this.camera);
  }
  /**
   * Updates the size of this pass.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.resolution;
    r.setBaseSize(e, t), this.renderTarget.setSize(r.width, r.height);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - A renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    r !== void 0 && r !== se && (this.renderTarget.texture.type = r, this.fullscreenMaterial.defines.FRAMEBUFFER_PRECISION_HIGH = "1");
  }
}
const ba = `#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D inputBuffer;\r
\r
#else\r
\r
	uniform lowp sampler2D inputBuffer;\r
\r
#endif\r
\r
// (1 / 4) * 0.5 = 0.125\r
#define WEIGHT_INNER 0.125\r
// (1 / 9) * 0.5 = 0.0555555\r
#define WEIGHT_OUTER 0.0555555\r
\r
varying vec2 vUv;\r
varying vec2 vUv00;\r
varying vec2 vUv01;\r
varying vec2 vUv02;\r
varying vec2 vUv03;\r
varying vec2 vUv04;\r
varying vec2 vUv05;\r
varying vec2 vUv06;\r
varying vec2 vUv07;\r
varying vec2 vUv08;\r
varying vec2 vUv09;\r
varying vec2 vUv10;\r
varying vec2 vUv11;\r
\r
float clampToBorder(const in vec2 uv) {\r
\r
	return float(uv.s >= 0.0 && uv.s <= 1.0 && uv.t >= 0.0 && uv.t <= 1.0);\r
\r
}\r
\r
void main() {\r
\r
	vec4 c = vec4(0.0);\r
\r
	vec4 w = WEIGHT_INNER * vec4(\r
		clampToBorder(vUv00),\r
		clampToBorder(vUv01),\r
		clampToBorder(vUv02),\r
		clampToBorder(vUv03)\r
	);\r
\r
	c += w.x * texture2D(inputBuffer, vUv00);\r
	c += w.y * texture2D(inputBuffer, vUv01);\r
	c += w.z * texture2D(inputBuffer, vUv02);\r
	c += w.w * texture2D(inputBuffer, vUv03);\r
\r
	w = WEIGHT_OUTER * vec4(\r
		clampToBorder(vUv04),\r
		clampToBorder(vUv05),\r
		clampToBorder(vUv06),\r
		clampToBorder(vUv07)\r
	);\r
\r
	c += w.x * texture2D(inputBuffer, vUv04);\r
	c += w.y * texture2D(inputBuffer, vUv05);\r
	c += w.z * texture2D(inputBuffer, vUv06);\r
	c += w.w * texture2D(inputBuffer, vUv07);\r
\r
	w = WEIGHT_OUTER * vec4(\r
		clampToBorder(vUv08),\r
		clampToBorder(vUv09),\r
		clampToBorder(vUv10),\r
		clampToBorder(vUv11)\r
	);\r
\r
	c += w.x * texture2D(inputBuffer, vUv08);\r
	c += w.y * texture2D(inputBuffer, vUv09);\r
	c += w.z * texture2D(inputBuffer, vUv10);\r
	c += w.w * texture2D(inputBuffer, vUv11);\r
\r
	c += WEIGHT_OUTER * texture2D(inputBuffer, vUv);\r
	gl_FragColor = c;\r
\r
	#include <colorspace_fragment>\r
\r
}\r
`, Da = `uniform vec2 texelSize;\r
\r
varying vec2 vUv;\r
varying vec2 vUv00;\r
varying vec2 vUv01;\r
varying vec2 vUv02;\r
varying vec2 vUv03;\r
varying vec2 vUv04;\r
varying vec2 vUv05;\r
varying vec2 vUv06;\r
varying vec2 vUv07;\r
varying vec2 vUv08;\r
varying vec2 vUv09;\r
varying vec2 vUv10;\r
varying vec2 vUv11;\r
\r
void main() {\r
\r
	vUv = position.xy * 0.5 + 0.5;\r
\r
	vUv00 = vUv + texelSize * vec2(-1.0, 1.0);\r
	vUv01 = vUv + texelSize * vec2(1.0, 1.0);\r
	vUv02 = vUv + texelSize * vec2(-1.0, -1.0);\r
	vUv03 = vUv + texelSize * vec2(1.0, -1.0);\r
\r
	vUv04 = vUv + texelSize * vec2(-2.0, 2.0);\r
	vUv05 = vUv + texelSize * vec2(0.0, 2.0);\r
	vUv06 = vUv + texelSize * vec2(2.0, 2.0);\r
	vUv07 = vUv + texelSize * vec2(-2.0, 0.0);\r
	vUv08 = vUv + texelSize * vec2(2.0, 0.0);\r
	vUv09 = vUv + texelSize * vec2(-2.0, -2.0);\r
	vUv10 = vUv + texelSize * vec2(0.0, -2.0);\r
	vUv11 = vUv + texelSize * vec2(2.0, -2.0);\r
\r
	gl_Position = vec4(position.xy, 1.0, 1.0);\r
\r
}\r
`;
class Pa extends N {
  /**
   * Constructs a new downsampling material.
   */
  constructor() {
    super({
      name: "DownsamplingMaterial",
      uniforms: {
        inputBuffer: new a(null),
        texelSize: new a(new g())
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: ba,
      vertexShader: Da
    });
  }
  /**
   * The input buffer.
   *
   * @type {Texture}
   */
  set inputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * Sets the size of this object.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.uniforms.texelSize.value.set(1 / e, 1 / t);
  }
}
const Ca = `#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D inputBuffer;\r
	uniform mediump sampler2D supportBuffer;\r
\r
#else\r
\r
	uniform lowp sampler2D inputBuffer;\r
	uniform lowp sampler2D supportBuffer;\r
\r
#endif\r
\r
uniform float radius;\r
\r
varying vec2 vUv;\r
varying vec2 vUv0;\r
varying vec2 vUv1;\r
varying vec2 vUv2;\r
varying vec2 vUv3;\r
varying vec2 vUv4;\r
varying vec2 vUv5;\r
varying vec2 vUv6;\r
varying vec2 vUv7;\r
\r
void main() {\r
\r
	vec4 c = vec4(0.0);\r
\r
	c += texture2D(inputBuffer, vUv0) * 0.0625;\r
	c += texture2D(inputBuffer, vUv1) * 0.125;\r
	c += texture2D(inputBuffer, vUv2) * 0.0625;\r
	c += texture2D(inputBuffer, vUv3) * 0.125;\r
	c += texture2D(inputBuffer, vUv) * 0.25;\r
	c += texture2D(inputBuffer, vUv4) * 0.125;\r
	c += texture2D(inputBuffer, vUv5) * 0.0625;\r
	c += texture2D(inputBuffer, vUv6) * 0.125;\r
	c += texture2D(inputBuffer, vUv7) * 0.0625;\r
\r
	vec4 baseColor = texture2D(supportBuffer, vUv);\r
	gl_FragColor = mix(baseColor, c, radius);\r
\r
	#include <colorspace_fragment>\r
\r
}\r
`, Aa = `uniform vec2 texelSize;\r
\r
varying vec2 vUv;\r
varying vec2 vUv0;\r
varying vec2 vUv1;\r
varying vec2 vUv2;\r
varying vec2 vUv3;\r
varying vec2 vUv4;\r
varying vec2 vUv5;\r
varying vec2 vUv6;\r
varying vec2 vUv7;\r
\r
void main() {\r
\r
	vUv = position.xy * 0.5 + 0.5;\r
\r
	vUv0 = vUv + texelSize * vec2(-1.0, 1.0);\r
	vUv1 = vUv + texelSize * vec2(0.0, 1.0);\r
	vUv2 = vUv + texelSize * vec2(1.0, 1.0);\r
	vUv3 = vUv + texelSize * vec2(-1.0, 0.0);\r
\r
	vUv4 = vUv + texelSize * vec2(1.0, 0.0);\r
	vUv5 = vUv + texelSize * vec2(-1.0, -1.0);\r
	vUv6 = vUv + texelSize * vec2(0.0, -1.0);\r
	vUv7 = vUv + texelSize * vec2(1.0, -1.0);\r
\r
	gl_Position = vec4(position.xy, 1.0, 1.0);\r
\r
}\r
`;
class Ea extends N {
  /**
   * Constructs a new upsampling material.
   */
  constructor() {
    super({
      name: "UpsamplingMaterial",
      uniforms: {
        inputBuffer: new a(null),
        supportBuffer: new a(null),
        texelSize: new a(new g()),
        radius: new a(0.85)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: Ca,
      vertexShader: Aa
    });
  }
  /**
   * The input buffer.
   *
   * @type {Texture}
   */
  set inputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * A support buffer.
   *
   * @type {Texture}
   */
  set supportBuffer(e) {
    this.uniforms.supportBuffer.value = e;
  }
  /**
   * The blur radius.
   *
   * @type {Number}
   */
  get radius() {
    return this.uniforms.radius.value;
  }
  set radius(e) {
    this.uniforms.radius.value = e;
  }
  /**
   * Sets the size of this object.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.uniforms.texelSize.value.set(1 / e, 1 / t);
  }
}
class Ra extends X {
  /**
   * Constructs a new mipmap blur pass.
   *
   * @param {Object} [options] - The options.
   */
  constructor() {
    super("MipmapBlurPass"), this.needsSwap = !1, this.renderTarget = new A(1, 1, { depthBuffer: !1 }), this.renderTarget.texture.name = "Upsampling.Mipmap0", this.downsamplingMipmaps = [], this.upsamplingMipmaps = [], this.downsamplingMaterial = new Pa(), this.upsamplingMaterial = new Ea(), this.resolution = new g();
  }
  /**
   * A texture that contains the blurred result.
   *
   * @type {Texture}
   */
  get texture() {
    return this.renderTarget.texture;
  }
  /**
   * The MIP levels. Default is 8.
   *
   * @type {Number}
   */
  get levels() {
    return this.downsamplingMipmaps.length;
  }
  set levels(e) {
    if (this.levels !== e) {
      const t = this.renderTarget;
      this.dispose(), this.downsamplingMipmaps = [], this.upsamplingMipmaps = [];
      for (let r = 0; r < e; ++r) {
        const n = t.clone();
        n.texture.name = "Downsampling.Mipmap" + r, this.downsamplingMipmaps.push(n);
      }
      this.upsamplingMipmaps.push(t);
      for (let r = 1, n = e - 1; r < n; ++r) {
        const i = t.clone();
        i.texture.name = "Upsampling.Mipmap" + r, this.upsamplingMipmaps.push(i);
      }
      this.setSize(this.resolution.x, this.resolution.y);
    }
  }
  /**
   * The blur radius.
   *
   * @type {Number}
   */
  get radius() {
    return this.upsamplingMaterial.radius;
  }
  set radius(e) {
    this.upsamplingMaterial.radius = e;
  }
  /**
   * Renders the blur.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i) {
    const { scene: s, camera: o } = this, { downsamplingMaterial: l, upsamplingMaterial: u } = this, { downsamplingMipmaps: f, upsamplingMipmaps: h } = this;
    let m = t;
    this.fullscreenMaterial = l;
    for (let p = 0, d = f.length; p < d; ++p) {
      const v = f[p];
      l.setSize(m.width, m.height), l.inputBuffer = m.texture, e.setRenderTarget(v), e.render(s, o), m = v;
    }
    this.fullscreenMaterial = u;
    for (let p = h.length - 1; p >= 0; --p) {
      const d = h[p];
      u.setSize(m.width, m.height), u.inputBuffer = m.texture, u.supportBuffer = f[p].texture, e.setRenderTarget(d), e.render(s, o), m = d;
    }
  }
  /**
   * Updates the size of this pass.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.resolution;
    r.set(e, t);
    let n = r.width, i = r.height;
    for (let s = 0, o = this.downsamplingMipmaps.length; s < o; ++s)
      n = Math.round(n * 0.5), i = Math.round(i * 0.5), this.downsamplingMipmaps[s].setSize(n, i), s < this.upsamplingMipmaps.length && this.upsamplingMipmaps[s].setSize(n, i);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    if (r !== void 0) {
      const n = this.downsamplingMipmaps.concat(this.upsamplingMipmaps);
      for (const i of n)
        i.texture.type = r;
      if (r !== se)
        this.downsamplingMaterial.defines.FRAMEBUFFER_PRECISION_HIGH = "1", this.upsamplingMaterial.defines.FRAMEBUFFER_PRECISION_HIGH = "1";
      else if (e !== null && e.outputColorSpace === V)
        for (const i of n)
          i.texture.colorSpace = V;
    }
  }
  /**
   * Deletes internal render targets and textures.
   */
  dispose() {
    super.dispose();
    for (const e of this.downsamplingMipmaps.concat(this.upsamplingMipmaps))
      e.dispose();
  }
}
const Na = `#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D map;\r
\r
#else\r
\r
	uniform lowp sampler2D map;\r
\r
#endif\r
\r
uniform float intensity;\r
uniform vec3 bloomColor;\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	vec4 texel = texture2D(map, uv);\r
	outputColor = vec4(texel.rgb * bloomColor * intensity, texel.a);\r
\r
}\r
`;
class za extends I {
  /**
   * Constructs a new bloom effect.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.SCREEN] - The blend function of this effect.
   * @param {Number} [options.luminanceThreshold=0.9] - The luminance threshold. Raise this value to mask out darker elements in the scene.
   * @param {Number} [options.luminanceSmoothing=0.025] - Controls the smoothness of the luminance threshold.
   * @param {Boolean} [options.mipmapBlur=false] - Enables or disables mipmap blur.
   * @param {Number} [options.intensity=1.0] - The bloom intensity.
   * @param {Number} [options.radius=0.85] - The blur radius. Only applies to mipmap blur.
   * @param {Number} [options.levels=8] - The amount of MIP levels. Only applies to mipmap blur.
   * @param {Color|String|Number} [options.bloomColor=0xffffff] - The color of the bloom effect.
   * @param {KernelSize} [options.kernelSize=KernelSize.LARGE] - Deprecated. Use mipmapBlur instead.
   * @param {Number} [options.resolutionScale=0.5] - Deprecated. Use mipmapBlur instead.
   * @param {Number} [options.resolutionX=Resolution.AUTO_SIZE] - Deprecated. Use mipmapBlur instead.
   * @param {Number} [options.resolutionY=Resolution.AUTO_SIZE] - Deprecated. Use mipmapBlur instead.
   * @param {Number} [options.width=Resolution.AUTO_SIZE] - Deprecated. Use mipmapBlur instead.
   * @param {Number} [options.height=Resolution.AUTO_SIZE] - Deprecated. Use mipmapBlur instead.
   */
  constructor({
    blendFunction: e = E.SCREEN,
    luminanceThreshold: t = 0.9,
    luminanceSmoothing: r = 0.025,
    mipmapBlur: n = !1,
    intensity: i = 1,
    radius: s = 0.85,
    levels: o = 8,
    bloomColor: l = 16777215,
    kernelSize: u = Le.LARGE,
    resolutionScale: f = 0.5,
    width: h = W.AUTO_SIZE,
    height: m = W.AUTO_SIZE,
    resolutionX: p = h,
    resolutionY: d = m
  } = {}) {
    super("BloomEffect", Na, {
      blendFunction: e,
      uniforms: /* @__PURE__ */ new Map([
        ["map", new a(null)],
        ["intensity", new a(i)],
        ["bloomColor", new a(new y(l))]
      ])
    }), this.renderTarget = new A(1, 1, { depthBuffer: !1 }), this.renderTarget.texture.name = "Bloom.Target", this.blurPass = new at({ kernelSize: u }), this.luminancePass = new Pi({ colorOutput: !0 }), this.luminanceMaterial.threshold = t, this.luminanceMaterial.smoothing = r, this.mipmapBlurPass = new Ra(), this.mipmapBlurPass.enabled = n, this.mipmapBlurPass.radius = s, this.mipmapBlurPass.levels = o, this.uniforms.get("map").value = n ? this.mipmapBlurPass.texture : this.renderTarget.texture;
    const v = this.resolution = new W(this, p, d, f);
    v.addEventListener("change", (x) => this.setSize(v.baseWidth, v.baseHeight));
  }
  /**
   * A texture that contains the intermediate result of this effect.
   *
   * @type {Texture}
   */
  get texture() {
    return this.mipmapBlurPass.enabled ? this.mipmapBlurPass.texture : this.renderTarget.texture;
  }
  /**
   * Returns the generated bloom texture.
   *
   * @deprecated Use texture instead.
   * @return {Texture} The texture.
   */
  getTexture() {
    return this.texture;
  }
  /**
   * Returns the resolution settings.
   *
   * @deprecated Use resolution instead.
   * @return {Resolution} The resolution.
   */
  getResolution() {
    return this.resolution;
  }
  /**
   * Returns the blur pass.
   *
   * @deprecated
   * @return {KawaseBlurPass} The blur pass.
   */
  getBlurPass() {
    return this.blurPass;
  }
  /**
   * Returns the luminance pass.
   *
   * @deprecated Use luminancePass instead.
   * @return {LuminancePass} The luminance pass.
   */
  getLuminancePass() {
    return this.luminancePass;
  }
  /**
   * The luminance material.
   *
   * @type {LuminanceMaterial}
   */
  get luminanceMaterial() {
    return this.luminancePass.fullscreenMaterial;
  }
  /**
   * Returns the luminance material.
   *
   * @deprecated Use luminanceMaterial instead.
   * @return {LuminanceMaterial} The material.
   */
  getLuminanceMaterial() {
    return this.luminancePass.fullscreenMaterial;
  }
  /**
   * The current width of the internal render targets.
   *
   * @type {Number}
   * @deprecated
   */
  get width() {
    return this.resolution.width;
  }
  set width(e) {
    this.resolution.preferredWidth = e;
  }
  /**
   * The current height of the internal render targets.
   *
   * @type {Number}
   * @deprecated
   */
  get height() {
    return this.resolution.height;
  }
  set height(e) {
    this.resolution.preferredHeight = e;
  }
  /**
   * Indicates whether dithering is enabled.
   *
   * @type {Boolean}
   * @deprecated Use EffectPass.dithering instead.
   */
  get dithering() {
    return this.blurPass.dithering;
  }
  set dithering(e) {
    this.blurPass.dithering = e;
  }
  /**
   * The blur kernel size.
   *
   * @type {KernelSize}
   * @deprecated
   */
  get kernelSize() {
    return this.blurPass.kernelSize;
  }
  set kernelSize(e) {
    this.blurPass.kernelSize = e;
  }
  /**
   * @type {Number}
   * @deprecated
   */
  get distinction() {
    return console.warn(this.name, "distinction was removed"), 1;
  }
  set distinction(e) {
    console.warn(this.name, "distinction was removed");
  }
  /**
   * The bloom intensity.
   *
   * @type {Number}
   */
  get intensity() {
    return this.uniforms.get("intensity").value;
  }
  set intensity(e) {
    this.uniforms.get("intensity").value = e;
  }
  /**
   * The bloom intensity.
   *
   * @deprecated Use intensity instead.
   * @return {Number} The intensity.
   */
  getIntensity() {
    return this.intensity;
  }
  /**
   * Sets the bloom intensity.
   *
   * @deprecated Use intensity instead.
   * @param {Number} value - The intensity.
   */
  setIntensity(e) {
    this.intensity = e;
  }
  /**
   * Returns the current resolution scale.
   *
   * @return {Number} The resolution scale.
   * @deprecated
   */
  getResolutionScale() {
    return this.resolution.scale;
  }
  /**
   * Sets the resolution scale.
   *
   * @param {Number} scale - The new resolution scale.
   * @deprecated
   */
  setResolutionScale(e) {
    this.resolution.scale = e;
  }
  /**
   * Updates this effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   */
  update(e, t, r) {
    const n = this.renderTarget, i = this.luminancePass;
    i.enabled ? (i.render(e, t), this.mipmapBlurPass.enabled ? this.mipmapBlurPass.render(e, i.renderTarget) : this.blurPass.render(e, i.renderTarget, n)) : this.mipmapBlurPass.enabled ? this.mipmapBlurPass.render(e, t) : this.blurPass.render(e, t, n);
  }
  /**
   * Updates the size of internal render targets.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.resolution;
    r.setBaseSize(e, t), this.renderTarget.setSize(r.width, r.height), this.blurPass.resolution.copy(r), this.luminancePass.setSize(e, t), this.mipmapBlurPass.setSize(e, t);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    this.blurPass.initialize(e, t, r), this.luminancePass.initialize(e, t, r), this.mipmapBlurPass.initialize(e, t, r), r !== void 0 && (this.renderTarget.texture.type = r, e !== null && e.outputColorSpace === V && (this.renderTarget.texture.colorSpace = V));
  }
  /**
   * The bloom color.
   *
   * @type {Color}
   */
  get bloomColor() {
    return this.uniforms.get("bloomColor").value;
  }
  set bloomColor(e) {
    this.uniforms.get("bloomColor").value.copy(new y(e));
  }
  /**
   * Returns the bloom color.
   *
   * @return {Color} The color.
   */
  getBloomColor() {
    return this.bloomColor;
  }
  /**
   * Sets the bloom color.
   *
   * @param {Color|String|Number} value - The color.
   */
  setBloomColor(e) {
    this.bloomColor = e;
  }
}
const we = {
  DEFAULT: 0,
  KEEP_MAX_DEPTH: 1,
  DISCARD_MAX_DEPTH: 2
}, Fa = `#include <common>
#include <packing>

#ifdef GL_FRAGMENT_PRECISION_HIGH

	uniform highp sampler2D depthBuffer0;
	uniform highp sampler2D depthBuffer1;

#else

	uniform mediump sampler2D depthBuffer0;
	uniform mediump sampler2D depthBuffer1;

#endif

uniform sampler2D inputBuffer;
uniform vec2 cameraNearFar;

float getViewZ(const in float depth) {

	#ifdef PERSPECTIVE_CAMERA

		return perspectiveDepthToViewZ(depth, cameraNearFar.x, cameraNearFar.y);

	#else

		return orthographicDepthToViewZ(depth, cameraNearFar.x, cameraNearFar.y);

	#endif

}

varying vec2 vUv;

void main() {

	vec2 depth;

	#if DEPTH_PACKING_0 == 3201

		depth.x = unpackRGBAToDepth(texture2D(depthBuffer0, vUv));

	#else

		depth.x = texture2D(depthBuffer0, vUv).r;

		#ifdef LOG_DEPTH

			float d = pow(2.0, depth.x * log2(cameraNearFar.y + 1.0)) - 1.0;
			float a = cameraNearFar.y / (cameraNearFar.y - cameraNearFar.x);
			float b = cameraNearFar.y * cameraNearFar.x / (cameraNearFar.x - cameraNearFar.y);
			depth.x = a + b / d;

		#endif

	#endif

	#if DEPTH_PACKING_1 == 3201

		depth.y = unpackRGBAToDepth(texture2D(depthBuffer1, vUv));

	#else

		depth.y = texture2D(depthBuffer1, vUv).r;

		#ifdef LOG_DEPTH

			float d = pow(2.0, depth.y * log2(cameraNearFar.y + 1.0)) - 1.0;
			float a = cameraNearFar.y / (cameraNearFar.y - cameraNearFar.x);
			float b = cameraNearFar.y * cameraNearFar.x / (cameraNearFar.x - cameraNearFar.y);
			depth.y = a + b / d;

		#endif

	#endif

	bool isMaxDepth = (depth.x == 1.0);

	#ifdef PERSPECTIVE_CAMERA

		// Linearize.
		depth.x = viewZToOrthographicDepth(getViewZ(depth.x), cameraNearFar.x, cameraNearFar.y);
		depth.y = viewZToOrthographicDepth(getViewZ(depth.y), cameraNearFar.x, cameraNearFar.y);

	#endif

	#if DEPTH_TEST_STRATEGY == 0

		// Decide based on depth test.
		bool keep = depthTest(depth.x, depth.y);

	#elif DEPTH_TEST_STRATEGY == 1

		// Always keep max depth.
		bool keep = isMaxDepth || depthTest(depth.x, depth.y);

	#else

		// Always discard max depth.
		bool keep = !isMaxDepth && depthTest(depth.x, depth.y);

	#endif

	if(keep) {

		gl_FragColor = texture2D(inputBuffer, vUv);

	} else {

		discard;

	}

}
`;
class _e extends N {
  /**
   * Constructs a new depth mask material.
   */
  constructor() {
    super({
      name: "DepthMaskMaterial",
      defines: {
        DEPTH_EPSILON: "0.0001",
        DEPTH_PACKING_0: "0",
        DEPTH_PACKING_1: "0",
        DEPTH_TEST_STRATEGY: we.KEEP_MAX_DEPTH
      },
      uniforms: {
        inputBuffer: new a(null),
        depthBuffer0: new a(null),
        depthBuffer1: new a(null),
        cameraNearFar: new a(new g(1, 1))
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: Fa,
      vertexShader: Be
    }), this.depthMode = gn;
  }
  /**
   * The primary depth buffer.
   *
   * @type {Texture}
   */
  set depthBuffer0(e) {
    this.uniforms.depthBuffer0.value = e;
  }
  /**
   * The primary depth packing strategy.
   *
   * @type {DepthPackingStrategies}
   */
  set depthPacking0(e) {
    this.defines.DEPTH_PACKING_0 = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Sets the base depth buffer.
   *
   * @deprecated Use depthBuffer0 and depthPacking0 instead.
   * @param {Texture} buffer - The depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing strategy.
   */
  setDepthBuffer0(e, t = $) {
    this.depthBuffer0 = e, this.depthPacking0 = t;
  }
  /**
   * The secondary depth buffer.
   *
   * @type {Texture}
   */
  set depthBuffer1(e) {
    this.uniforms.depthBuffer1.value = e;
  }
  /**
   * The secondary depth packing strategy.
   *
   * @type {DepthPackingStrategies}
   */
  set depthPacking1(e) {
    this.defines.DEPTH_PACKING_1 = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Sets the depth buffer that will be compared with the base depth buffer.
   *
   * @deprecated Use depthBuffer1 and depthPacking1 instead.
   * @param {Texture} buffer - The depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing strategy.
   */
  setDepthBuffer1(e, t = $) {
    this.depthBuffer1 = e, this.depthPacking1 = t;
  }
  /**
   * The strategy for handling maximum depth.
   *
   * @type {DepthTestStrategy}
   */
  get maxDepthStrategy() {
    return Number(this.defines.DEPTH_TEST_STRATEGY);
  }
  set maxDepthStrategy(e) {
    this.defines.DEPTH_TEST_STRATEGY = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Indicates whether maximum depth values should be preserved.
   *
   * @type {Boolean}
   * @deprecated Use maxDepthStrategy instead.
   */
  get keepFar() {
    return this.maxDepthStrategy;
  }
  set keepFar(e) {
    this.maxDepthStrategy = e ? we.KEEP_MAX_DEPTH : we.DISCARD_MAX_DEPTH;
  }
  /**
   * Returns the strategy for dealing with maximum depth values.
   *
   * @deprecated Use maxDepthStrategy instead.
   * @return {DepthTestStrategy} The strategy.
   */
  getMaxDepthStrategy() {
    return this.maxDepthStrategy;
  }
  /**
   * Sets the strategy for dealing with maximum depth values.
   *
   * @deprecated Use maxDepthStrategy instead.
   * @param {DepthTestStrategy} value - The strategy.
   */
  setMaxDepthStrategy(e) {
    this.maxDepthStrategy = e;
  }
  /**
   * A small error threshold that is used for `EqualDepth` and `NotEqualDepth` tests. Default is `1e-4`.
   *
   * @type {Number}
   */
  get epsilon() {
    return Number(this.defines.DEPTH_EPSILON);
  }
  set epsilon(e) {
    this.defines.DEPTH_EPSILON = e.toFixed(16), this.needsUpdate = !0;
  }
  /**
   * Returns the current error threshold for depth comparisons.
   *
   * @deprecated Use epsilon instead.
   * @return {Number} The error threshold.
   */
  getEpsilon() {
    return this.epsilon;
  }
  /**
   * Sets the depth comparison error threshold.
   *
   * @deprecated Use epsilon instead.
   * @param {Number} value - The new error threshold.
   */
  setEpsilon(e) {
    this.epsilon = e;
  }
  /**
   * The depth mode.
   *
   * @see https://threejs.org/docs/#api/en/constants/Materials
   * @type {DepthModes}
   */
  get depthMode() {
    return Number(this.defines.DEPTH_MODE);
  }
  set depthMode(e) {
    let t;
    switch (e) {
      case ms:
        t = "false";
        break;
      case ds:
        t = "true";
        break;
      case Ue:
        t = "abs(d1 - d0) <= DEPTH_EPSILON";
        break;
      case Mt:
        t = "abs(d1 - d0) > DEPTH_EPSILON";
        break;
      case gn:
        t = "d0 > d1";
        break;
      case hs:
        t = "d0 >= d1";
        break;
      case fs:
        t = "d0 <= d1";
        break;
      case us:
      default:
        t = "d0 < d1";
        break;
    }
    this.defines.DEPTH_MODE = e.toFixed(0), this.defines["depthTest(d0, d1)"] = t, this.needsUpdate = !0;
  }
  /**
   * Returns the current depth mode.
   *
   * @deprecated Use depthMode instead.
   * @return {DepthModes} The depth mode. Default is `LessDepth`.
   */
  getDepthMode() {
    return this.depthMode;
  }
  /**
   * Sets the depth mode.
   *
   * @deprecated Use depthMode instead.
   * @param {DepthModes} mode - The depth mode.
   */
  setDepthMode(e) {
    this.depthMode = e;
  }
  /**
   * Copies the settings of the given camera.
   *
   * @deprecated Use copyCameraSettings instead.
   * @param {Camera} camera - A camera.
   */
  adoptCameraSettings(e) {
    this.copyCameraSettings(e);
  }
  /**
   * Copies the settings of the given camera.
   *
   * @param {Camera} camera - A camera.
   */
  copyCameraSettings(e) {
    e && (this.uniforms.cameraNearFar.value.set(e.near, e.far), e instanceof ke ? this.defines.PERSPECTIVE_CAMERA = "1" : delete this.defines.PERSPECTIVE_CAMERA, this.needsUpdate = !0);
  }
}
const Ia = `#include <common>\r
\r
uniform sampler2D tDiffuse;\r
uniform vec3 defaultColor;\r
uniform float defaultOpacity;\r
uniform float luminosityThreshold;\r
uniform float smoothWidth;\r
\r
varying vec2 vUv;\r
\r
void main() {\r
\r
	vec4 texel = texture2D(tDiffuse, vUv);\r
\r
	float v = luminance(texel.xyz);\r
\r
	vec4 outputColorValue = vec4(defaultColor.rgb, defaultOpacity);\r
\r
	float alpha = smoothstep(luminosityThreshold, luminosityThreshold + smoothWidth, v);\r
\r
	gl_FragColor = mix(outputColorValue, texel, alpha);\r
\r
}\r
\r
`;
class Ci extends N {
  /**
   * Constructs a new luminosity high pass material.
   */
  constructor() {
    super({
      name: "LuminosityHighPassMaterial",
      uniforms: {
        tDiffuse: new a(null),
        luminosityThreshold: new a(1),
        smoothWidth: new a(1),
        defaultColor: new a(new y(0)),
        defaultOpacity: new a(0)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: Ia,
      vertexShader: Be
    });
  }
}
const Ba = `uniform sampler2D colorTexture;\r
uniform vec2 invSize;\r
uniform vec2 direction;\r
uniform float gaussianCoefficients[KERNEL_RADIUS];\r
\r
varying vec2 vUv;\r
\r
void main() {\r
\r
	float weightSum = gaussianCoefficients[0];\r
	vec3 diffuseSum = texture2D(colorTexture, vUv).rgb * weightSum;\r
\r
	for(int i = 1; i < KERNEL_RADIUS; i++) {\r
\r
		float x = float(i);\r
		float w = gaussianCoefficients[i];\r
		vec2 uvOffset = direction * invSize * x;\r
		vec3 sample1 = texture2D(colorTexture, vUv + uvOffset).rgb;\r
		vec3 sample2 = texture2D(colorTexture, vUv - uvOffset).rgb;\r
		diffuseSum += (sample1 + sample2) * w;\r
		weightSum += 2.0 * w;\r
\r
	}\r
\r
	gl_FragColor = vec4(diffuseSum / weightSum, 1.0);\r
\r
}\r
\r
`;
class Ai extends N {
  /**
   * Constructs a new separable Gaussian blur material.
   *
   * @param {Number} kernelRadius - The kernel radius.
   */
  constructor(e) {
    const t = [];
    for (let r = 0; r < e; r++)
      t.push(0.39894 * Math.exp(-0.5 * r * r / (e * e)) / e);
    super({
      name: "SeparableGaussianBlurMaterial",
      defines: {
        KERNEL_RADIUS: e
      },
      uniforms: {
        colorTexture: new a(null),
        invSize: new a(new g(0.5, 0.5)),
        direction: new a(new g(0.5, 0.5)),
        gaussianCoefficients: new a(t)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: Ba,
      vertexShader: Be
    });
  }
  /**
   * Sets the size of this object.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.uniforms.invSize.value.set(1 / e, 1 / t);
  }
}
const La = `uniform sampler2D blurTexture1;\r
uniform sampler2D blurTexture2;\r
uniform sampler2D blurTexture3;\r
uniform sampler2D blurTexture4;\r
uniform sampler2D blurTexture5;\r
uniform float bloomStrength;\r
uniform float bloomRadius;\r
uniform float bloomFactors[NUM_MIPS];\r
uniform vec3 bloomTintColors[NUM_MIPS];\r
\r
varying vec2 vUv;\r
\r
float lerpBloomFactor(const in float factor) {\r
\r
	float mirrorFactor = 1.2 - factor;\r
	return mix(factor, mirrorFactor, bloomRadius);\r
\r
}\r
\r
void main() {\r
\r
	gl_FragColor = bloomStrength * (\r
		lerpBloomFactor(bloomFactors[0]) * vec4(bloomTintColors[0], 1.0) * texture2D(blurTexture1, vUv) +\r
		lerpBloomFactor(bloomFactors[1]) * vec4(bloomTintColors[1], 1.0) * texture2D(blurTexture2, vUv) +\r
		lerpBloomFactor(bloomFactors[2]) * vec4(bloomTintColors[2], 1.0) * texture2D(blurTexture3, vUv) +\r
		lerpBloomFactor(bloomFactors[3]) * vec4(bloomTintColors[3], 1.0) * texture2D(blurTexture4, vUv) +\r
		lerpBloomFactor(bloomFactors[4]) * vec4(bloomTintColors[4], 1.0) * texture2D(blurTexture5, vUv)\r
	);\r
\r
}\r
\r
`;
class Ei extends N {
  /**
   * Constructs a new UnrealBloom composite material.
   *
   * @param {Number} nMips - The number of MIP levels.
   */
  constructor(e = 5) {
    const t = [1, 0.8, 0.6, 0.4, 0.2], r = [
      new T(1, 1, 1),
      new T(1, 1, 1),
      new T(1, 1, 1),
      new T(1, 1, 1),
      new T(1, 1, 1)
    ];
    super({
      name: "UnrealBloomCompositeMaterial",
      defines: {
        NUM_MIPS: e
      },
      uniforms: {
        blurTexture1: new a(null),
        blurTexture2: new a(null),
        blurTexture3: new a(null),
        blurTexture4: new a(null),
        blurTexture5: new a(null),
        bloomStrength: new a(1),
        bloomFactors: new a(t),
        bloomTintColors: new a(r),
        bloomRadius: new a(0)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: La,
      vertexShader: Be
    });
  }
}
class ee extends X {
  /**
   * Constructs a new shader pass.
   *
   * @param {ShaderMaterial} material - A shader material.
   * @param {String} [input="inputBuffer"] - The name of the input buffer uniform.
   */
  constructor(e, t = "inputBuffer") {
    super("ShaderPass"), this.fullscreenMaterial = e, this.input = t;
  }
  /**
   * Sets the name of the input buffer uniform.
   *
   * @param {String} input - The name of the input buffer uniform.
   * @deprecated Use input instead.
   */
  setInput(e) {
    this.input = e;
  }
  /**
   * Renders the effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i, s = {}) {
    const o = this.fullscreenMaterial.uniforms;
    t !== null && o !== void 0 && o[this.input] !== void 0 && (o[this.input].value = t.texture), e.setRenderTarget(this.renderToScreen ? null : r), e.render(this.scene, this.camera, s);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - A renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    r !== void 0 && r !== se && (this.fullscreenMaterial.defines.FRAMEBUFFER_PRECISION_HIGH = "1");
  }
}
const Ri = `#ifdef FRAMEBUFFER_PRECISION_HIGH

	uniform mediump sampler2D map;

#else

	uniform lowp sampler2D map;

#endif

uniform float intensity;
uniform vec3 bloomColor;

void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {

	// 获取泛光纹理
	// composite shader 的输出已经包含了 bloomStrength 和 bloomTintColors
	// 格式：bloomStrength * (lerpBloomFactor * tintColor * blurTexture for each MIP)
	vec4 bloomTexel = texture2D(map, uv);
	
	// 应用额外的颜色和强度调整（如果需要）
	// composite shader 已经应用了 bloomStrength，所以这里只做微调
	vec3 bloom = bloomTexel.rgb * bloomColor * intensity;

	// 输出泛光纹理，让 blendFunction 来处理混合
	// 使用 ADD blendFunction 时，会执行：inputColor + bloom
	// 这完全模拟了 UnrealBloomPass 的 AdditiveBlending 行为
	outputColor = vec4(bloom, bloomTexel.a);

}

`;
class mh extends I {
  /**
   * Constructs a new selective unreal bloom effect.
   *
   * @param {Scene} scene - The main scene.
   * @param {Camera} camera - The main camera.
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.SCREEN] - The blend function.
   * @param {Number} [options.strength=1.0] - The bloom strength.
   * @param {Number} [options.radius=0.4] - The bloom radius.
   * @param {Number} [options.threshold=0.85] - The luminosity threshold.
   * @param {Number} [options.smoothWidth=0.01] - The smooth width for threshold.
   * @param {Number} [options.nMips=5] - The number of MIP levels.
   * @param {Object} [options.gBufferTextures=null] - G-Buffer textures from RenderPass.
   * @param {Texture} [options.gBufferTextures.gDepth] - G-Buffer depth texture.
   */
  constructor(e, t, r = {}) {
    const {
      blendFunction: n = E.ADD,
      // 使用 ADD，模拟 UnrealBloomPass 的 AdditiveBlending
      strength: i = 1,
      radius: s = 0.4,
      // UnrealBloomPass 默认使用 0.1，但这里使用 0.4 以获得更明显的效果
      threshold: o = 0.85,
      smoothWidth: l = 0.01,
      nMips: u = 5,
      gBufferTextures: f = null
    } = r;
    super("SelectiveUnrealBloomEffect", Ri, {
      blendFunction: n,
      attributes: Q.DEPTH,
      uniforms: /* @__PURE__ */ new Map([
        ["map", new a(null)],
        ["intensity", new a(1)],
        // 默认 1.0，因为 strength 已经在 composite 中应用
        ["bloomColor", new a(new y(16777215))]
        // 默认白色，不改变颜色
      ])
    }), this.scene = e, this.camera = t, this.gBufferTextures = f || null, this.strength = i, this.radius = s, this.threshold = o, this.smoothWidth = l, this.nMips = u, this.depthPass = new Me(e, t), this.clearPass = new De(!0, !1, !1), this.clearPass.overrideClearColor = new y(0), this.depthMaskPass = new ee(new _e());
    const h = this.depthMaskMaterial;
    h.copyCameraSettings(t), h.depthBuffer1 = this.depthPass.texture, h.depthPacking1 = ce, h.depthMode = Ue, this.depthMaskMaterial.epsilon = 9e-6, this.gBufferTextures && this.gBufferTextures.gDepth && (h.depthBuffer0 = this.gBufferTextures.gDepth, h.depthPacking0 = $), this.renderTargetMasked = new A(1, 1, { depthBuffer: !1 }), this.renderTargetMasked.texture.name = "UnrealBloom.Masked", this.renderTargetsHorizontal = [], this.renderTargetsVertical = [], this.renderTargetBright = new A(1, 1, { type: K }), this.renderTargetBright.texture.name = "UnrealBloom.bright", this.renderTargetBright.texture.generateMipmaps = !1, this.materialHighPassFilter = new Ci(), this.materialHighPassFilter.uniforms.luminosityThreshold.value = o, this.materialHighPassFilter.uniforms.smoothWidth.value = l, this.separableBlurMaterials = [], this.compositeMaterial = new Ei(u), this.compositeMaterial.uniforms.bloomStrength.value = i, this.compositeMaterial.uniforms.bloomRadius.value = s, this.bloomFactors = [1, 0.8, 0.6, 0.4, 0.2], this.bloomTintColors = [
      new T(1, 1, 1),
      new T(1, 1, 1),
      new T(1, 1, 1),
      new T(1, 1, 1),
      new T(1, 1, 1)
    ], this.blurDirectionX = new g(1, 0), this.blurDirectionY = new g(0, 1), this.selection = new Se(), this._inverted = !1, this._ignoreBackground = !1, this._initializeRenderTargets();
  }
  /**
   * Initializes render targets and blur materials.
   *
   * @private
   */
  _initializeRenderTargets() {
    const e = [3, 5, 7, 9, 11];
    let t = Math.round(1 / 2), r = Math.round(1 / 2);
    for (let n = 0; n < this.nMips; n++) {
      const i = new A(t, r, { type: K });
      i.texture.name = "UnrealBloom.h" + n, i.texture.generateMipmaps = !1, this.renderTargetsHorizontal.push(i);
      const s = new A(t, r, { type: K });
      s.texture.name = "UnrealBloom.v" + n, s.texture.generateMipmaps = !1, this.renderTargetsVertical.push(s), this.separableBlurMaterials.push(new Ai(e[n])), this.separableBlurMaterials[n].uniforms.invSize.value = new g(1 / t, 1 / r), t = Math.round(t / 2), r = Math.round(r / 2);
    }
    this.compositeMaterial.uniforms.blurTexture1.value = this.renderTargetsVertical[0].texture, this.compositeMaterial.uniforms.blurTexture2.value = this.renderTargetsVertical[1].texture, this.compositeMaterial.uniforms.blurTexture3.value = this.renderTargetsVertical[2].texture, this.compositeMaterial.uniforms.blurTexture4.value = this.renderTargetsVertical[3].texture, this.compositeMaterial.uniforms.blurTexture5.value = this.renderTargetsVertical[4].texture, this.compositeMaterial.uniforms.bloomFactors.value = this.bloomFactors, this.compositeMaterial.uniforms.bloomTintColors.value = this.bloomTintColors;
  }
  set mainScene(e) {
    this.depthPass.mainScene = e;
  }
  set mainCamera(e) {
    this.camera = e, this.depthPass.mainCamera = e, this.depthMaskMaterial.copyCameraSettings(e);
  }
  /**
   * Returns the selection.
   *
   * @deprecated Use selection instead.
   * @return {Selection} The selection.
   */
  getSelection() {
    return this.selection;
  }
  /**
   * The depth mask material.
   *
   * @type {DepthMaskMaterial}
   * @private
   */
  get depthMaskMaterial() {
    return this.depthMaskPass.fullscreenMaterial;
  }
  /**
   * Indicates whether the selection should be considered inverted.
   *
   * @type {Boolean}
   */
  get inverted() {
    return this._inverted;
  }
  set inverted(e) {
    this._inverted = e, this.depthMaskMaterial.depthMode = e ? Mt : Ue;
  }
  /**
   * Indicates whether the background colors will be ignored.
   *
   * @type {Boolean}
   */
  get ignoreBackground() {
    return this._ignoreBackground;
  }
  set ignoreBackground(e) {
    this._ignoreBackground = e, this.depthMaskMaterial.maxDepthStrategy = e ? we.DISCARD_MAX_DEPTH : we.KEEP_MAX_DEPTH;
  }
  /**
   * Sets the depth texture.
   *
   * @param {Texture} depthTexture - A depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing.
   */
  setDepthTexture(e, t = $) {
    this.depthMaskMaterial.depthBuffer0 = e, this.depthMaskMaterial.depthPacking0 = t;
  }
  /**
   * Sets the G-Buffer textures.
   *
   * @param {Object|null} gBufferTextures - The G-Buffer textures.
   * @param {Texture} [gBufferTextures.gDepth] - The depth texture from G-Buffer.
   */
  setGBufferTextures(e) {
    this.gBufferTextures = e, e && e.gDepth && (this.depthMaskMaterial.depthBuffer0 = e.gDepth, this.depthMaskMaterial.depthPacking0 = $);
  }
  /**
   * Updates this effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   */
  update(e, t, r) {
    const n = this.camera, i = this.selection, s = this.inverted;
    let o = t;
    if (ve("SelectiveUnrealBloomEffect.update"), this.ignoreBackground || !s || i.size > 0) {
      ve("SelectiveUnrealBloomEffect.update.depthPass");
      const h = n.layers.mask;
      n.layers.set(i.layer), this.depthPass.render(e, void 0, void 0, void 0, void 0, void 0, {
        projectObject: !0,
        updateMatrixWorld: !1,
        useProgramCache: !1
      }), n.layers.mask = h, ge("SelectiveUnrealBloomEffect.update.depthPass"), ve("SelectiveUnrealBloomEffect.update.maskRender"), o = this.renderTargetMasked, this.clearPass.render(e, o), this.depthMaskPass.render(e, t, o, void 0, void 0, {
        projectObject: !0,
        updateMatrixWorld: !1,
        useProgramCache: !1
      }), ge("SelectiveUnrealBloomEffect.update.maskRender");
    }
    ve("SelectiveUnrealBloomEffect.update.unrealBloom");
    const l = new ee(this.materialHighPassFilter, "tDiffuse"), u = new ee(this.compositeMaterial);
    this.materialHighPassFilter.uniforms.tDiffuse.value = o.texture, this.materialHighPassFilter.uniforms.luminosityThreshold.value = this.threshold, this.materialHighPassFilter.uniforms.smoothWidth.value = this.smoothWidth, l.render(e, o, this.renderTargetBright);
    let f = this.renderTargetBright;
    for (let h = 0; h < this.nMips; h++) {
      const m = this.separableBlurMaterials[h], p = new ee(m, "colorTexture");
      m.uniforms.colorTexture.value = f.texture, m.uniforms.direction.value = this.blurDirectionX, p.render(e, f, this.renderTargetsHorizontal[h]), m.uniforms.colorTexture.value = this.renderTargetsHorizontal[h].texture, m.uniforms.direction.value = this.blurDirectionY, p.render(e, this.renderTargetsHorizontal[h], this.renderTargetsVertical[h]), f = this.renderTargetsVertical[h];
    }
    this.compositeMaterial.uniforms.bloomStrength.value = this.strength, this.compositeMaterial.uniforms.bloomRadius.value = this.radius, this.compositeMaterial.uniforms.bloomFactors.value = this.bloomFactors, this.compositeMaterial.uniforms.bloomTintColors.value = this.bloomTintColors, u.render(e, this.renderTargetsVertical[0], this.renderTargetsHorizontal[0]), this.uniforms.get("map").value = this.renderTargetsHorizontal[0].texture, ge("SelectiveUnrealBloomEffect.update.unrealBloom"), ge("SelectiveUnrealBloomEffect.update");
  }
  /**
   * Updates the size of internal render targets.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    let r = Math.round(e / 2), n = Math.round(t / 2);
    this.renderTargetBright.setSize(r, n), this.renderTargetMasked.setSize(e, t), this.depthPass.setSize(e, t);
    for (let i = 0; i < this.nMips; i++)
      this.renderTargetsHorizontal[i].setSize(r, n), this.renderTargetsVertical[i].setSize(r, n), this.separableBlurMaterials[i].setSize(r, n), r = Math.round(r / 2), n = Math.round(n / 2);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    this.clearPass.initialize(e, t, r), this.depthPass.initialize(e, t, r), this.depthMaskPass.initialize(e, t, r), e !== null && e.capabilities.logarithmicDepthBuffer && (this.depthMaskPass.fullscreenMaterial.defines.LOG_DEPTH = "1"), r !== void 0 && (this.renderTargetMasked.texture.type = r, e !== null && e.outputColorSpace === V && (this.renderTargetMasked.texture.colorSpace = V));
  }
  /**
   * The bloom intensity.
   *
   * This is a separate control from strength. Strength is applied in the composite shader,
   * while intensity is applied in the final blend shader for fine-tuning.
   *
   * @type {Number}
   */
  get intensity() {
    return this.uniforms.get("intensity").value;
  }
  set intensity(e) {
    this.uniforms.get("intensity").value = e;
  }
  /**
   * The bloom color.
   *
   * @type {Color}
   */
  get bloomColor() {
    return this.uniforms.get("bloomColor").value;
  }
  set bloomColor(e) {
    this.uniforms.get("bloomColor").value.copy(new y(e));
  }
}
const Ua = (
  /* glsl */
  `
varying vec2 vUv;
void main() {
	vUv = uv;
	gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
}
`
), Oa = (
  /* glsl */
  `
varying vec2 vUv;
uniform sampler2D inputBuffer;
void main() {
	gl_FragColor = texture2D(inputBuffer, vUv);
}
`
);
class me extends I {
  /**
   * @param {Object} [options]
   * @param {BlendFunction} [options.blendFunction=BlendFunction.ADD] - 最终混合函数，建议 ADD 以模拟 UnrealBloomPass 的 AdditiveBlending
   * @param {number} [options.strength=1.0] - 泛光强度（应用在合成材质中）
   * @param {number} [options.radius=0.1] - 泛光半径（影响 bloomFactors 的插值）
   * @param {number} [options.threshold=0.85] - 亮度阈值（高通滤波）
   * @param {number} [options.smoothWidth=0.01] - 阈值平滑宽度
   * @param {number} [options.nMips=5] - MIP 级数
   * @param {number} [options.intensity=1.0] - 额外的强度细调（在最终片元中乘上）
   * @param {Color|number|string} [options.bloomColor=0xffffff] - 泛光颜色调节
   */
  constructor({
    blendFunction: e = E.ADD,
    strength: t = 1,
    radius: r = 0.1,
    threshold: n = 0.85,
    smoothWidth: i = 0.01,
    nMips: s = 5,
    intensity: o = 1,
    bloomColor: l = 16777215,
    useHighPass: u = !0
  } = {}) {
    super("UnrealBloomEffect", Ri, {
      blendFunction: e,
      uniforms: /* @__PURE__ */ new Map([
        ["map", new a(null)],
        ["intensity", new a(o)],
        ["bloomColor", new a(new y(l))]
      ])
    }), this.output = me.OUTPUT.Default, this.useHighPass = u, this.setAttributes(this.getAttributes() | Q.DEPTH), this.materialHighPassFilter = this._createHighPassMaterial(), this.materialHighPassFilter.uniforms.luminosityThreshold.value = n, this.materialHighPassFilter.uniforms.smoothWidth.value = i, this.compositeMaterial = this._createCompositeMaterial(s), this.compositeMaterial.uniforms.bloomStrength.value = t, this.compositeMaterial.uniforms.bloomRadius.value = r, this.nMips = s, this.bloomFactors = [1, 0.8, 0.6, 0.4, 0.2].slice(0, s), this.bloomTintColors = new Array(s).fill(0).map(() => new T(1, 1, 1)), this.renderTargetBright = new A(1, 1, { type: K }), this.renderTargetBright.texture.name = "UnrealBloom.bright", this.renderTargetBright.texture.generateMipmaps = !1, this.renderTargetsHorizontal = [], this.renderTargetsVertical = [], this.separableBlurMaterials = [], this.blurDirectionX = new g(1, 0), this.blurDirectionY = new g(0, 1), this.scene = null, this.camera = null, this.depthPass = null, this.clearPass = new De(!0, !1, !1), this.clearPass.overrideClearColor = new y(0), this.depthMaskPass = new ee(new _e()), this.renderTargetMasked = new A(1, 1, { depthBuffer: !1 }), this.renderTargetMasked.texture.name = "UnrealBloom.Masked", this.renderTargetSelection = new A(1, 1, {
      depthBuffer: !1,
      format: ne,
      stencilBuffer: !1
    }), this.renderTargetSelection.texture.name = "UnrealBloom.Selection", this.renderTargetSelection.texture.generateMipmaps = !1, this.selectionCopyMaterial = new N({
      uniforms: {
        inputBuffer: new a(null)
      },
      vertexShader: Ua,
      fragmentShader: Oa,
      depthWrite: !1,
      depthTest: !1
    }), this.selectionCopyPass = new ee(this.selectionCopyMaterial), this.selection = new Se(), this._inverted = !1, this._ignoreBackground = !1, this.gBufferTextures = null, this.sceneDepthPass = null, this._initializeRenderTargetsAndMaterials(), this._depthEpsilon = this.depthMaskMaterial.epsilon;
  }
  // —— 选择性泛光配置 ——
  set mainScene(e) {
    this.scene = e, this.depthPass && (this.depthPass.mainScene = e), this.sceneDepthPass ? this.sceneDepthPass.mainScene = e : e && this.camera && (this.sceneDepthPass = new Me(e, this.camera), this.depthMaskMaterial.depthBuffer0 = this.sceneDepthPass.texture, this.depthMaskMaterial.depthPacking0 = this.sceneDepthPass.depthPacking || ce);
  }
  set mainCamera(e) {
    this.camera = e, this.depthPass || (this.depthPass = new Me(this.scene, e)), this.depthPass.mainCamera = e, this.sceneDepthPass || (this.sceneDepthPass = new Me(this.scene, e)), this.sceneDepthPass.mainCamera = e, this.depthMaskMaterial.copyCameraSettings(e), this.depthMaskMaterial.depthBuffer1 = this.depthPass.texture, this.depthMaskMaterial.depthPacking1 = this.depthPass.depthPacking || ce, this.depthMaskMaterial.depthBuffer0 = this.sceneDepthPass.texture, this.depthMaskMaterial.depthPacking0 = this.sceneDepthPass.depthPacking || ce, this.depthMaskMaterial.depthMode = Ue, this.depthMaskMaterial.epsilon = 9e-6;
  }
  get depthMaskMaterial() {
    return this.depthMaskPass.fullscreenMaterial;
  }
  get inverted() {
    return this._inverted;
  }
  set inverted(e) {
    this._inverted = e, this.depthMaskMaterial.depthMode = e ? Mt : Ue;
  }
  get ignoreBackground() {
    return this._ignoreBackground;
  }
  set ignoreBackground(e) {
    this._ignoreBackground = e, this.depthMaskMaterial.maxDepthStrategy = e ? we.DISCARD_MAX_DEPTH : we.KEEP_MAX_DEPTH;
  }
  get depthEpsilon() {
    return this.depthMaskMaterial.epsilon;
  }
  set depthEpsilon(e) {
    this._depthEpsilon = e, this.depthMaskMaterial.epsilon = e;
  }
  get selectionTexture() {
    return this.renderTargetSelection.texture;
  }
  get selectionRenderTarget() {
    return this.renderTargetSelection;
  }
  getSelection() {
    return this.selection;
  }
  setDepthTexture(e, t) {
    this.depthMaskMaterial.depthBuffer0 = e, t !== void 0 && (this.depthMaskMaterial.depthPacking0 = t);
  }
  setGBufferTextures(e) {
    this.gBufferTextures = e, e && e.gDepth && (this.depthMaskMaterial.depthBuffer0 = e.gDepth);
  }
  _initializeRenderTargetsAndMaterials() {
    const e = [3, 5, 7, 9, 11];
    let t = Math.round(1 / 2), r = Math.round(1 / 2);
    for (let n = 0; n < this.nMips; n++) {
      const i = new A(t, r, { type: K });
      i.texture.name = "UnrealBloom.h" + n, i.texture.generateMipmaps = !1, this.renderTargetsHorizontal.push(i);
      const s = new A(t, r, { type: K });
      s.texture.name = "UnrealBloom.v" + n, s.texture.generateMipmaps = !1, this.renderTargetsVertical.push(s);
      const o = this._getSeparableBlurMaterial(e[n] ?? e[e.length - 1]);
      o.uniforms.invSize.value = new g(1 / t, 1 / r), this.separableBlurMaterials.push(o), t = Math.max(1, Math.round(t / 2)), r = Math.max(1, Math.round(r / 2)), this.compositeMaterial.uniforms.blurTexture1.value = this.renderTargetsVertical[0] && this.renderTargetsVertical[0].texture ? this.renderTargetsVertical[0].texture : null, this.compositeMaterial.uniforms.blurTexture2.value = this.renderTargetsVertical[1] && this.renderTargetsVertical[1].texture ? this.renderTargetsVertical[1].texture : null, this.compositeMaterial.uniforms.blurTexture3.value = this.renderTargetsVertical[2] && this.renderTargetsVertical[2].texture ? this.renderTargetsVertical[2].texture : null, this.compositeMaterial.uniforms.blurTexture4.value = this.renderTargetsVertical[3] && this.renderTargetsVertical[3].texture ? this.renderTargetsVertical[3].texture : null, this.compositeMaterial.uniforms.blurTexture5.value = this.renderTargetsVertical[4] && this.renderTargetsVertical[4].texture ? this.renderTargetsVertical[4].texture : null, this.compositeMaterial.uniforms.bloomFactors.value = this.bloomFactors, this.compositeMaterial.uniforms.bloomTintColors.value = this.bloomTintColors;
    }
  }
  _createHighPassMaterial() {
    const e = oe.clone({
      tDiffuse: { value: null },
      luminosityThreshold: { value: 1 },
      smoothWidth: { value: 1 },
      defaultColor: { value: new y(0) },
      defaultOpacity: { value: 0 }
    });
    return new N({
      uniforms: e,
      vertexShader: `varying vec2 vUv;
				void main() {
					vUv = uv;
					gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );
				}`,
      fragmentShader: `uniform sampler2D tDiffuse;
				uniform float luminosityThreshold;
				uniform float smoothWidth;
				uniform vec3 defaultColor;
				uniform float defaultOpacity;

				varying vec2 vUv;

				void main() {
					vec4 texel = texture2D( tDiffuse, vUv );
					vec3 luma = vec3( 0.299, 0.587, 0.114 );
					float v = dot( texel.xyz, luma );

					vec4 outputColor = vec4( defaultColor.rgb, defaultOpacity );
					float alpha = smoothstep( luminosityThreshold, luminosityThreshold + smoothWidth, v );

					gl_FragColor = mix( outputColor, texel, alpha );
				}`
    });
  }
  _getSeparableBlurMaterial(e) {
    const t = [];
    for (let r = 0; r < e; r++)
      t.push(0.39894 * Math.exp(-0.5 * r * r / (e * e)) / e);
    return new N({
      defines: {
        KERNEL_RADIUS: e
      },
      uniforms: {
        colorTexture: { value: null },
        invSize: { value: new g(0.5, 0.5) },
        direction: { value: new g(0.5, 0.5) },
        gaussianCoefficients: { value: t }
      },
      vertexShader: `varying vec2 vUv;
				void main() {
					vUv = uv;
					gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );
				}`,
      fragmentShader: `varying vec2 vUv;
				uniform sampler2D colorTexture;
				uniform vec2 invSize;
				uniform vec2 direction;
				uniform float gaussianCoefficients[KERNEL_RADIUS];

				void main() {
					float weightSum = gaussianCoefficients[0];
					vec3 diffuseSum = texture2D( colorTexture, vUv ).rgb * weightSum;
					for( int i = 1; i < KERNEL_RADIUS; i ++ ) {
						float x = float(i);
						float w = gaussianCoefficients[i];
						vec2 uvOffset = direction * invSize * x;
						vec3 sample1 = texture2D( colorTexture, vUv + uvOffset ).rgb;
						vec3 sample2 = texture2D( colorTexture, vUv - uvOffset ).rgb;
						diffuseSum += (sample1 + sample2) * w;
						weightSum += 2.0 * w;
					}
					gl_FragColor = vec4(diffuseSum/weightSum, 1.0);
				}`
    });
  }
  _createCompositeMaterial(e) {
    return new N({
      defines: { NUM_MIPS: e },
      uniforms: {
        blurTexture1: { value: null },
        blurTexture2: { value: null },
        blurTexture3: { value: null },
        blurTexture4: { value: null },
        blurTexture5: { value: null },
        bloomStrength: { value: 1 },
        bloomFactors: { value: null },
        bloomTintColors: { value: null },
        bloomRadius: { value: 0 }
      },
      vertexShader: `varying vec2 vUv;
				void main() {
					vUv = uv;
					gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );
				}`,
      fragmentShader: `varying vec2 vUv;
				uniform sampler2D blurTexture1;
				uniform sampler2D blurTexture2;
				uniform sampler2D blurTexture3;
				uniform sampler2D blurTexture4;
				uniform sampler2D blurTexture5;
				uniform float bloomStrength;
				uniform float bloomRadius;
				uniform float bloomFactors[NUM_MIPS];
				uniform vec3 bloomTintColors[NUM_MIPS];

				float lerpBloomFactor(const in float factor) {
					float mirrorFactor = 1.2 - factor;
					return mix(factor, mirrorFactor, bloomRadius);
				}

				void main() {
					gl_FragColor = bloomStrength * ( lerpBloomFactor(bloomFactors[0]) * vec4(bloomTintColors[0], 1.0) * texture2D(blurTexture1, vUv) +
						lerpBloomFactor(bloomFactors[1]) * vec4(bloomTintColors[1], 1.0) * texture2D(blurTexture2, vUv) +
						lerpBloomFactor(bloomFactors[2]) * vec4(bloomTintColors[2], 1.0) * texture2D(blurTexture3, vUv) +
						lerpBloomFactor(bloomFactors[3]) * vec4(bloomTintColors[3], 1.0) * texture2D(blurTexture4, vUv) +
						lerpBloomFactor(bloomFactors[4]) * vec4(bloomTintColors[4], 1.0) * texture2D(blurTexture5, vUv) );
				}`
    });
  }
  update(e, t, r) {
    let n = t;
    if (this.camera)
      if (this.sceneDepthPass && this.sceneDepthPass.render(e, void 0, void 0, void 0, void 0, void 0, {
        projectObject: !0,
        updateMatrixWorld: !1,
        useProgramCache: !1
      }), this.ignoreBackground || !this.inverted || this.selection.size > 0) {
        const l = this.camera.layers.mask, u = this.selection.layer;
        this.camera.layers.set(u);
        const f = [];
        this.selection.forEach((m) => {
          m && m.traverse((p) => {
            p.layers && p.visible && (p.layers.isEnabled(u) || (f.push({ child: p, mask: p.layers.mask }), this.selection.exclusive ? p.layers.set(u) : p.layers.enable(u)));
          });
        }), this.depthPass || (this.depthPass = new Me(this.scene, this.camera)), this.depthPass.render(e, void 0, void 0, void 0, void 0, void 0, {
          projectObject: !0,
          updateMatrixWorld: !1,
          useProgramCache: !1
        }), this.camera.layers.mask = l, f.forEach(({ child: m, mask: p }) => {
          m.layers.mask = p;
        }), this.depthMaskMaterial.depthBuffer1 = this.depthPass.texture, this.depthMaskMaterial.depthBuffer0 = this.sceneDepthPass ? this.sceneDepthPass.texture : null, this.depthMaskMaterial.uniforms.inputBuffer.value = t.texture, n = this.renderTargetMasked, this.clearPass.render(e, n), this.depthMaskPass.render(e, t, n, void 0, void 0, {
          projectObject: !0,
          updateMatrixWorld: !1,
          useProgramCache: !1
        }), this.selectionCopyPass.render(e, n, this.renderTargetSelection), this.selectionOnlyMaterial || (this.selectionOnlyMaterial = new N({
          uniforms: {
            tOriginal: { value: null },
            tSelection: { value: null }
          },
          vertexShader: `
                            varying vec2 vUv;
                            void main() {
                                vUv = uv;
                                gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
                            }
                        `,
          fragmentShader: `
                            uniform sampler2D tOriginal;
                            uniform sampler2D tSelection;
                            varying vec2 vUv;
                            
                            void main() {
                                vec4 originalTexel = texture2D(tOriginal, vUv);
                                vec4 selectionTexel = texture2D(tSelection, vUv);
                                
                                // 使用RGB通道的平均值作为选择掩码
                                float selection = (selectionTexel.r + selectionTexel.g + selectionTexel.b) / 3.0;
                                
                                // 只在选择区域内显示原始模型，非选择区域完全透明
                                if (selection > 0.01) {
                                    gl_FragColor = vec4(originalTexel.rgb, 1.0);
                                } else {
                                    discard; // 完全丢弃未选中的像素
                                }
                            }
                        `
        })), this.selectionOnlyMaterial.uniforms.tOriginal.value = t.texture, this.selectionOnlyMaterial.uniforms.tSelection.value = n.texture, new ee(this.selectionOnlyMaterial, "tOriginal").render(e, t, this.renderTargetSelection);
      } else
        this.clearPass.render(e, this.renderTargetSelection);
    let i = n;
    this.useHighPass && (this.materialHighPassFilter.uniforms.tDiffuse.value = n.texture, new ee(this.materialHighPassFilter, "tDiffuse").render(e, n, this.renderTargetBright), i = this.renderTargetBright);
    let s = this.useHighPass ? this.renderTargetBright : i;
    for (let l = 0; l < this.nMips; l++) {
      const u = this.separableBlurMaterials[l], f = new ee(u, "colorTexture");
      u.uniforms.colorTexture.value = s.texture, u.uniforms.direction.value = this.blurDirectionX, f.render(e, s, this.renderTargetsHorizontal[l]), u.uniforms.colorTexture.value = this.renderTargetsHorizontal[l].texture, u.uniforms.direction.value = this.blurDirectionY, f.render(e, this.renderTargetsHorizontal[l], this.renderTargetsVertical[l]), s = this.renderTargetsVertical[l];
    }
    switch (this.compositeMaterial.uniforms.bloomTintColors.value = this.bloomTintColors, new ee(this.compositeMaterial).render(e, this.renderTargetsVertical[0], this.renderTargetsHorizontal[0]), this.uniforms.get("map").value = this.renderTargetsHorizontal[0].texture, this.output) {
      case me.OUTPUT.Beauty:
        this.uniforms.get("map").value = n.texture;
        break;
      case me.OUTPUT.Brightness:
        this.uniforms.get("map").value = this.renderTargetBright.texture;
        break;
      case me.OUTPUT.Blur1:
        this.uniforms.get("map").value = this.renderTargetsVertical[0].texture;
        break;
      case me.OUTPUT.Blur2:
        this.renderTargetsVertical[1] && (this.uniforms.get("map").value = this.renderTargetsVertical[1].texture);
        break;
      case me.OUTPUT.Blur3:
        this.renderTargetsVertical[2] && (this.uniforms.get("map").value = this.renderTargetsVertical[2].texture);
        break;
      case me.OUTPUT.Blur4:
        this.renderTargetsVertical[3] && (this.uniforms.get("map").value = this.renderTargetsVertical[3].texture);
        break;
      case me.OUTPUT.Blur5:
        this.renderTargetsVertical[4] && (this.uniforms.get("map").value = this.renderTargetsVertical[4].texture);
        break;
      case me.OUTPUT.Composite:
        this.uniforms.get("map").value = this.renderTargetsHorizontal[0].texture;
        break;
      case me.OUTPUT.Selection:
        this.renderTargetSelection && (this.renderTargetSelection.texture.format !== ne && (this.renderTargetSelection.texture.format = ne), this.uniforms.get("map").value = this.renderTargetSelection.texture);
        break;
      case me.OUTPUT.Default:
    }
  }
  setSize(e, t) {
    let r = Math.max(1, Math.round(e / 2)), n = Math.max(1, Math.round(t / 2));
    this.renderTargetBright.setSize(r, n), this.renderTargetMasked.setSize(e, t), this.renderTargetSelection.setSize(e, t), this.depthPass && this.depthPass.setSize(e, t), this.sceneDepthPass && this.sceneDepthPass.setSize(e, t);
    for (let i = 0; i < this.nMips; i++)
      this.renderTargetsHorizontal[i].setSize(r, n), this.renderTargetsVertical[i].setSize(r, n), this.separableBlurMaterials[i].uniforms.invSize.value.set(1 / r, 1 / n), r = Math.max(1, Math.round(r / 2)), n = Math.max(1, Math.round(n / 2));
  }
  initialize(e, t, r) {
    if (r !== void 0) {
      this.renderTargetBright.texture.type = r, this.renderTargetMasked.texture.type = r, this.renderTargetSelection.texture.type = r;
      for (let n = 0; n < this.nMips; n++)
        this.renderTargetsHorizontal[n].texture.type = r, this.renderTargetsVertical[n].texture.type = r;
      if (e !== null && e.outputColorSpace === V) {
        this.renderTargetBright.texture.colorSpace = V, this.renderTargetMasked.texture.colorSpace = V, this.renderTargetSelection.texture.colorSpace = V;
        for (let n = 0; n < this.nMips; n++)
          this.renderTargetsHorizontal[n].texture.colorSpace = V, this.renderTargetsVertical[n].texture.colorSpace = V;
      }
    }
    this.selectionCopyPass.initialize(e, t, r), this.clearPass.initialize(e, t, r), this.depthMaskPass.initialize(e, t, r), this.depthPass && this.depthPass.initialize(e, t, r), this.sceneDepthPass && this.sceneDepthPass.initialize(e, t, r);
  }
  get intensity() {
    return this.uniforms.get("intensity").value;
  }
  set intensity(e) {
    this.uniforms.get("intensity").value = e;
  }
  get bloomColor() {
    return this.uniforms.get("bloomColor").value;
  }
  set bloomColor(e) {
    this.uniforms.get("bloomColor").value.copy(new y(e));
  }
  /**
   * 输出模式枚举
   */
  static get OUTPUT() {
    return {
      Default: "Default",
      Beauty: "Beauty",
      Brightness: "Brightness",
      Blur1: "Blur1",
      Blur2: "Blur2",
      Blur3: "Blur3",
      Blur4: "Blur4",
      Blur5: "Blur5",
      Composite: "Composite",
      Selection: "Selection"
    };
  }
  /**
   * 设置输出模式
   * @param {string} mode - 输出模式
   */
  setOutputMode(e) {
    Object.values(me.OUTPUT).includes(e) ? this.output = e : console.warn(`[UnrealBloomEffect] Invalid output mode: ${e}`);
  }
  /**
   * 循环切换输出模式
   */
  cycleOutputMode() {
    const e = Object.values(me.OUTPUT), r = (e.indexOf(this.output) + 1) % e.length;
    return this.output = e[r], this.output;
  }
}
const ka = `#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D map;\r
\r
#else\r
\r
	uniform lowp sampler2D map;\r
\r
#endif\r
\r
uniform float intensity;\r
uniform vec3 bloomColor;\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	// 获取泛光纹理\r
	// composite shader 的输出已经包含了 bloomStrength 和 bloomTintColors\r
	// 格式：bloomStrength * (lerpBloomFactor * tintColor * blurTexture for each MIP)\r
	vec4 bloomTexel = texture2D(map, uv);\r
	\r
	// 应用额外的颜色和强度调整（如果需要）\r
	// composite shader 已经应用了 bloomStrength，所以这里只做微调\r
	vec3 bloom = bloomTexel.rgb * bloomColor * intensity;\r
\r
	// 使用深度信息来限制泛光，避免穿透到被遮挡的区域\r
	// 比较选中对象的深度和场景深度，只让泛光出现在选中对象的深度范围内\r
	#ifdef USE_DEPTH\r
		float sceneDepth = texture2D(depthBuffer, uv).r;\r
		// 如果场景深度接近1.0（背景/天空），则允许泛光\r
		// 如果场景深度较近（有对象在选中对象前面），则减少泛光\r
		// 这样可以避免泛光穿透到前景对象前面\r
		float depthFactor = smoothstep(0.95, 1.0, sceneDepth);\r
		bloom *= depthFactor;\r
	#endif\r
\r
	// 输出泛光纹理，让 blendFunction 来处理混合\r
	// 使用 ADD blendFunction 时，会执行：inputColor + bloom\r
	// 这完全模拟了 UnrealBloomPass 的 AdditiveBlending 行为\r
	outputColor = vec4(bloom, bloomTexel.a);\r
\r
}\r
\r
`;
class ph extends I {
  /**
   * Constructs a new layer-based glow effect.
   *
   * @param {Scene} scene - The main scene.
   * @param {Camera} camera - The main camera.
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.ADD] - The blend function.
   * @param {Number} [options.strength=1.0] - The glow strength.
   * @param {Number} [options.radius=0.4] - The glow radius.
   * @param {Number} [options.threshold=0.85] - The luminosity threshold.
   * @param {Number} [options.smoothWidth=0.01] - The smooth width for threshold.
   * @param {Number} [options.nMips=5] - The number of MIP levels.
   * @param {Boolean} [options.useHighPass=true] - Whether to use high-pass filter.
   * @param {Color|number|string} [options.bloomColor=0xffffff] - The glow color.
   */
  constructor(e, t, {
    blendFunction: r = E.ADD,
    strength: n = 1,
    radius: i = 0.4,
    threshold: s = 0.3,
    smoothWidth: o = 0.2,
    nMips: l = 5,
    useHighPass: u = !0,
    bloomColor: f = 16777215
  } = {}) {
    super("LayerBasedGlowEffect", ka, {
      blendFunction: r,
      attributes: Q.DEPTH,
      uniforms: /* @__PURE__ */ new Map([
        ["map", new a(null)],
        ["intensity", new a(1)],
        ["bloomColor", new a(new y(f))]
      ])
    }), this.scene = e, this.camera = t, this.selection = new Se(), this.strength = n, this.radius = i, this.threshold = s, this.smoothWidth = o, this.nMips = l, this.useHighPass = u, this.layerRenderPass = new pe(e, t), this.layerRenderPass.clear = !1, this.layerRenderPass.clearDepth = !1, this.layerRenderPass.selection = this.selection, this.clearPass = new De(!0, !0, !1), this.clearPass.overrideClearColor = new y(0), this.clearPass.renderToScreen = !1, this.renderTargetLayer = new A(1, 1, { depthBuffer: !0 }), this.renderTargetLayer.texture.name = "LayerBasedGlow.Layer", this.depthPass = new Me(e, t), this.renderTargetBright = new A(1, 1, { type: K }), this.renderTargetBright.texture.name = "LayerBasedGlow.bright", this.renderTargetBright.texture.generateMipmaps = !1, this.renderTargetsHorizontal = [], this.renderTargetsVertical = [], this.materialHighPassFilter = new Ci(), this.materialHighPassFilter.uniforms.luminosityThreshold.value = s, this.materialHighPassFilter.uniforms.smoothWidth.value = o, this.separableBlurMaterials = [], this.compositeMaterial = new Ei(l), this.compositeMaterial.uniforms.bloomStrength.value = n, this.compositeMaterial.uniforms.bloomRadius.value = i, this.bloomFactors = [1, 0.8, 0.6, 0.4, 0.2].slice(0, l), this.bloomTintColors = new Array(l).fill(0).map(() => new T(1, 1, 1)), this.blurDirectionX = new g(1, 0), this.blurDirectionY = new g(0, 1), this._initializeRenderTargetsAndMaterials();
  }
  /**
   * Initializes render targets and blur materials.
   *
   * @private
   */
  _initializeRenderTargetsAndMaterials() {
    const e = [3, 5, 7, 9, 11];
    let t = Math.round(1 / 2), r = Math.round(1 / 2);
    for (let n = 0; n < this.nMips; n++) {
      const i = new A(t, r, { type: K });
      i.texture.name = "LayerBasedGlow.h" + n, i.texture.generateMipmaps = !1, this.renderTargetsHorizontal.push(i);
      const s = new A(t, r, { type: K });
      s.texture.name = "LayerBasedGlow.v" + n, s.texture.generateMipmaps = !1, this.renderTargetsVertical.push(s);
      const o = new Ai(e[n] ?? e[e.length - 1]);
      o.uniforms.invSize.value = new g(1 / t, 1 / r), this.separableBlurMaterials.push(o), t = Math.max(1, Math.round(t / 2)), r = Math.max(1, Math.round(r / 2));
    }
    this.compositeMaterial.uniforms.blurTexture1.value = this.renderTargetsVertical[0]?.texture || null, this.compositeMaterial.uniforms.blurTexture2.value = this.renderTargetsVertical[1]?.texture || null, this.compositeMaterial.uniforms.blurTexture3.value = this.renderTargetsVertical[2]?.texture || null, this.compositeMaterial.uniforms.blurTexture4.value = this.renderTargetsVertical[3]?.texture || null, this.compositeMaterial.uniforms.blurTexture5.value = this.renderTargetsVertical[4]?.texture || null, this.compositeMaterial.uniforms.bloomFactors.value = this.bloomFactors, this.compositeMaterial.uniforms.bloomTintColors.value = this.bloomTintColors;
  }
  /**
   * Sets the main scene.
   *
   * @param {Scene} value - The scene.
   */
  set mainScene(e) {
    this.scene = e, this.layerRenderPass && (this.layerRenderPass.mainScene = e);
  }
  /**
   * Sets the main camera.
   *
   * @param {Camera} value - The camera.
   */
  set mainCamera(e) {
    this.camera = e, this.layerRenderPass && (this.layerRenderPass.mainCamera = e);
  }
  /**
   * Returns the selection.
   *
   * @deprecated Use selection instead.
   * @return {Selection} The selection.
   */
  getSelection() {
    return this.selection;
  }
  /**
   * Updates this effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   */
  update(e, t, r) {
    if (this.clearPass.render(e, this.renderTargetLayer, this.renderTargetLayer), this.selection.size > 0) {
      const o = this.camera.layers.mask;
      this.camera.layers.set(this.selection.layer), this.depthPass.render(e, void 0, void 0, void 0, void 0, void 0, {
        projectObject: !0,
        updateMatrixWorld: !1,
        useProgramCache: !1
      }), this.camera.layers.mask = o;
    }
    this.layerRenderPass.render(e, this.renderTargetLayer, this.renderTargetLayer);
    let n = this.renderTargetLayer;
    this.useHighPass && (this.materialHighPassFilter.uniforms.tDiffuse.value = this.renderTargetLayer.texture, this.materialHighPassFilter.uniforms.luminosityThreshold.value = this.threshold, this.materialHighPassFilter.uniforms.smoothWidth.value = this.smoothWidth, new ee(this.materialHighPassFilter, "tDiffuse").render(e, this.renderTargetLayer, this.renderTargetBright), n = this.renderTargetBright);
    let i = n;
    for (let o = 0; o < this.nMips; o++) {
      const l = this.separableBlurMaterials[o], u = new ee(l, "colorTexture");
      l.uniforms.colorTexture.value = i.texture, l.uniforms.direction.value = this.blurDirectionX, u.render(e, i, this.renderTargetsHorizontal[o]), l.uniforms.colorTexture.value = this.renderTargetsHorizontal[o].texture, l.uniforms.direction.value = this.blurDirectionY, u.render(e, this.renderTargetsHorizontal[o], this.renderTargetsVertical[o]), i = this.renderTargetsVertical[o];
    }
    this.compositeMaterial.uniforms.bloomStrength.value = this.strength, this.compositeMaterial.uniforms.bloomRadius.value = this.radius, this.compositeMaterial.uniforms.bloomFactors.value = this.bloomFactors, this.compositeMaterial.uniforms.bloomTintColors.value = this.bloomTintColors, new ee(this.compositeMaterial).render(e, this.renderTargetsVertical[0], this.renderTargetsHorizontal[0]), this.uniforms.get("map").value = this.renderTargetsHorizontal[0].texture;
  }
  /**
   * Updates the size of internal render targets.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    let r = Math.max(1, Math.round(e / 2)), n = Math.max(1, Math.round(t / 2));
    this.renderTargetLayer.setSize(e, t), this.renderTargetBright.setSize(r, n), this.depthPass && this.depthPass.setSize(e, t);
    for (let i = 0; i < this.nMips; i++)
      this.renderTargetsHorizontal[i].setSize(r, n), this.renderTargetsVertical[i].setSize(r, n), this.separableBlurMaterials[i].setSize(r, n), r = Math.max(1, Math.round(r / 2)), n = Math.max(1, Math.round(n / 2));
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    if (this.clearPass.initialize(e, t, r), this.layerRenderPass.initialize(e, t, r), this.depthPass && this.depthPass.initialize(e, t, r), r !== void 0) {
      this.renderTargetLayer.texture.type = r, this.renderTargetBright.texture.type = r;
      for (let n = 0; n < this.nMips; n++)
        this.renderTargetsHorizontal[n].texture.type = r, this.renderTargetsVertical[n].texture.type = r;
      if (e !== null && e.outputColorSpace === V) {
        this.renderTargetLayer.texture.colorSpace = V, this.renderTargetBright.texture.colorSpace = V;
        for (let n = 0; n < this.nMips; n++)
          this.renderTargetsHorizontal[n].texture.colorSpace = V, this.renderTargetsVertical[n].texture.colorSpace = V;
      }
    }
  }
  /**
   * The glow intensity.
   *
   * @type {Number}
   */
  get intensity() {
    return this.uniforms.get("intensity").value;
  }
  set intensity(e) {
    this.uniforms.get("intensity").value = e;
  }
  /**
   * The glow color.
   *
   * @type {Color}
   */
  get bloomColor() {
    return this.uniforms.get("bloomColor").value;
  }
  set bloomColor(e) {
    this.uniforms.get("bloomColor").value.copy(new y(e));
  }
}
const Va = `uniform float focus;\r
uniform float dof;\r
uniform float aperture;\r
uniform float maxBlur;\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, const in float depth, out vec4 outputColor) {\r
\r
	vec2 aspectCorrection = vec2(1.0, aspect);\r
\r
	#ifdef PERSPECTIVE_CAMERA\r
\r
		float viewZ = perspectiveDepthToViewZ(depth, cameraNear, cameraFar);\r
		float linearDepth = viewZToOrthographicDepth(viewZ, cameraNear, cameraFar);\r
\r
	#else\r
\r
		float linearDepth = depth;\r
\r
	#endif\r
\r
	float focusNear = clamp(focus - dof, 0.0, 1.0);\r
	float focusFar = clamp(focus + dof, 0.0, 1.0);\r
\r
	// Calculate a DoF mask.\r
	float low = step(linearDepth, focusNear);\r
	float high = step(focusFar, linearDepth);\r
\r
	float factor = (linearDepth - focusNear) * low + (linearDepth - focusFar) * high;\r
	vec2 dofBlur = vec2(clamp(factor * aperture, -maxBlur, maxBlur));\r
\r
	vec2 dofblur9 = dofBlur * 0.9;\r
	vec2 dofblur7 = dofBlur * 0.7;\r
	vec2 dofblur4 = dofBlur * 0.4;\r
\r
	vec4 color = inputColor;\r
	color += texture2D(inputBuffer, uv + (vec2( 0.0,   0.4 ) * aspectCorrection) * dofBlur);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.15,  0.37) * aspectCorrection) * dofBlur);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.29,  0.29) * aspectCorrection) * dofBlur);\r
	color += texture2D(inputBuffer, uv + (vec2(-0.37,  0.15) * aspectCorrection) * dofBlur);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.40,  0.0 ) * aspectCorrection) * dofBlur);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.37, -0.15) * aspectCorrection) * dofBlur);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.29, -0.29) * aspectCorrection) * dofBlur);\r
	color += texture2D(inputBuffer, uv + (vec2(-0.15, -0.37) * aspectCorrection) * dofBlur);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.0,  -0.4 ) * aspectCorrection) * dofBlur);\r
	color += texture2D(inputBuffer, uv + (vec2(-0.15,  0.37) * aspectCorrection) * dofBlur);\r
	color += texture2D(inputBuffer, uv + (vec2(-0.29,  0.29) * aspectCorrection) * dofBlur);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.37,  0.15) * aspectCorrection) * dofBlur);\r
	color += texture2D(inputBuffer, uv + (vec2(-0.4,   0.0 ) * aspectCorrection) * dofBlur);\r
	color += texture2D(inputBuffer, uv + (vec2(-0.37, -0.15) * aspectCorrection) * dofBlur);\r
	color += texture2D(inputBuffer, uv + (vec2(-0.29, -0.29) * aspectCorrection) * dofBlur);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.15, -0.37) * aspectCorrection) * dofBlur);\r
\r
	color += texture2D(inputBuffer, uv + (vec2( 0.15,  0.37) * aspectCorrection) * dofblur9);\r
	color += texture2D(inputBuffer, uv + (vec2(-0.37,  0.15) * aspectCorrection) * dofblur9);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.37, -0.15) * aspectCorrection) * dofblur9);\r
	color += texture2D(inputBuffer, uv + (vec2(-0.15, -0.37) * aspectCorrection) * dofblur9);\r
	color += texture2D(inputBuffer, uv + (vec2(-0.15,  0.37) * aspectCorrection) * dofblur9);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.37,  0.15) * aspectCorrection) * dofblur9);\r
	color += texture2D(inputBuffer, uv + (vec2(-0.37, -0.15) * aspectCorrection) * dofblur9);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.15, -0.37) * aspectCorrection) * dofblur9);\r
\r
	color += texture2D(inputBuffer, uv + (vec2( 0.29,  0.29) * aspectCorrection) * dofblur7);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.40,  0.0 ) * aspectCorrection) * dofblur7);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.29, -0.29) * aspectCorrection) * dofblur7);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.0,  -0.4 ) * aspectCorrection) * dofblur7);\r
	color += texture2D(inputBuffer, uv + (vec2(-0.29,  0.29) * aspectCorrection) * dofblur7);\r
	color += texture2D(inputBuffer, uv + (vec2(-0.4,   0.0 ) * aspectCorrection) * dofblur7);\r
	color += texture2D(inputBuffer, uv + (vec2(-0.29, -0.29) * aspectCorrection) * dofblur7);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.0,   0.4 ) * aspectCorrection) * dofblur7);\r
\r
	color += texture2D(inputBuffer, uv + (vec2( 0.29,  0.29) * aspectCorrection) * dofblur4);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.4,   0.0 ) * aspectCorrection) * dofblur4);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.29, -0.29) * aspectCorrection) * dofblur4);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.0,  -0.4 ) * aspectCorrection) * dofblur4);\r
	color += texture2D(inputBuffer, uv + (vec2(-0.29,  0.29) * aspectCorrection) * dofblur4);\r
	color += texture2D(inputBuffer, uv + (vec2(-0.4,   0.0 ) * aspectCorrection) * dofblur4);\r
	color += texture2D(inputBuffer, uv + (vec2(-0.29, -0.29) * aspectCorrection) * dofblur4);\r
	color += texture2D(inputBuffer, uv + (vec2( 0.0,   0.4 ) * aspectCorrection) * dofblur4);\r
\r
	outputColor = color / 41.0;\r
\r
}\r
`;
class vh extends I {
  /**
   * Constructs a new bokeh effect.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction] - The blend function of this effect.
   * @param {Number} [options.focus=0.5] - The focus distance ratio, ranging from 0.0 to 1.0.
   * @param {Number} [options.dof=0.02] - Depth of field. An area in front of and behind the focal point that still appears sharp.
   * @param {Number} [options.aperture=0.015] - Camera aperture scale. Bigger values for stronger blur and shallower depth of field.
   * @param {Number} [options.maxBlur=1.0] - The maximum blur strength.
   */
  constructor({
    blendFunction: e,
    focus: t = 0.5,
    dof: r = 0.02,
    aperture: n = 0.015,
    maxBlur: i = 1
  } = {}) {
    super("BokehEffect", Va, {
      blendFunction: e,
      attributes: Q.CONVOLUTION | Q.DEPTH,
      uniforms: /* @__PURE__ */ new Map([
        ["focus", new a(t)],
        ["dof", new a(r)],
        ["aperture", new a(n)],
        ["maxBlur", new a(i)]
      ])
    });
  }
}
const Ha = `uniform float brightness;\r
uniform float contrast;\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	vec3 color = inputColor.rgb + vec3(brightness - 0.5);\r
\r
	if(contrast > 0.0) {\r
\r
		color /= vec3(1.0 - contrast);\r
\r
	} else {\r
\r
		color *= vec3(1.0 + contrast);\r
\r
	}\r
\r
	outputColor = vec4(color + vec3(0.5), inputColor.a);\r
\r
}\r
`;
class gh extends I {
  /**
   * Constructs a new brightness/contrast effect.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.SRC] - The blend function of this effect.
   * @param {Number} [options.brightness=0.0] - The brightness factor, ranging from -1 to 1, where 0 means no change.
   * @param {Number} [options.contrast=0.0] - The contrast factor, ranging from -1 to 1, where 0 means no change.
   */
  constructor({ blendFunction: e = E.SRC, brightness: t = 0, contrast: r = 0 } = {}) {
    super("BrightnessContrastEffect", Ha, {
      blendFunction: e,
      uniforms: /* @__PURE__ */ new Map([
        ["brightness", new a(t)],
        ["contrast", new a(r)]
      ])
    }), this.inputColorSpace = V;
  }
  /**
   * The brightness.
   *
   * @type {Number}
   */
  get brightness() {
    return this.uniforms.get("brightness").value;
  }
  set brightness(e) {
    this.uniforms.get("brightness").value = e;
  }
  /**
   * Returns the brightness.
   *
   * @deprecated Use brightness instead.
   * @return {Number} The brightness.
   */
  getBrightness() {
    return this.brightness;
  }
  /**
   * Sets the brightness.
   *
   * @deprecated Use brightness instead.
   * @param {Number} value - The brightness.
   */
  setBrightness(e) {
    this.brightness = e;
  }
  /**
   * The contrast.
   *
   * @type {Number}
   */
  get contrast() {
    return this.uniforms.get("contrast").value;
  }
  set contrast(e) {
    this.uniforms.get("contrast").value = e;
  }
  /**
   * Returns the contrast.
   *
   * @deprecated Use contrast instead.
   * @return {Number} The contrast.
   */
  getContrast() {
    return this.contrast;
  }
  /**
   * Sets the contrast.
   *
   * @deprecated Use contrast instead.
   * @param {Number} value - The contrast.
   */
  setContrast(e) {
    this.contrast = e;
  }
}
const Ga = `void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	outputColor = vec4(vec3(average(inputColor.rgb)), inputColor.a);\r
\r
}\r
`;
class xh extends I {
  /**
   * Constructs a new color average effect.
   *
   * @param {BlendFunction} [blendFunction] - The blend function of this effect.
   */
  constructor(e) {
    super("ColorAverageEffect", Ga, { blendFunction: e });
  }
}
const Wa = `uniform float factor;\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	outputColor = vec4(floor(inputColor.rgb * factor + 0.5) / factor, inputColor.a);\r
\r
}\r
`;
class Sh extends I {
  /**
   * Constructs a new color depth effect.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction] - The blend function of this effect.
   * @param {Number} [options.bits=16] - The color bit depth.
   */
  constructor({ blendFunction: e, bits: t = 16 } = {}) {
    super("ColorDepthEffect", Wa, {
      blendFunction: e,
      uniforms: /* @__PURE__ */ new Map([
        ["factor", new a(1)]
      ])
    }), this.bits = 0, this.bitDepth = t;
  }
  /**
   * The virtual amount of color bits.
   *
   * Each color channel effectively uses a fourth of the total amount of bits. Alpha remains unaffected.
   *
   * @type {Number}
   */
  get bitDepth() {
    return this.bits;
  }
  set bitDepth(e) {
    this.bits = e, this.uniforms.get("factor").value = Math.pow(2, e / 3);
  }
  /**
   * Returns the current color bit depth.
   *
   * @return {Number} The bit depth.
   */
  getBitDepth() {
    return this.bitDepth;
  }
  /**
   * Sets the virtual amount of color bits.
   *
   * @param {Number} value - The bit depth.
   */
  setBitDepth(e) {
    this.bitDepth = e;
  }
}
const ja = `#ifdef RADIAL_MODULATION\r
\r
	uniform float modulationOffset;\r
\r
#endif\r
\r
varying float vActive;\r
varying vec2 vUvR;\r
varying vec2 vUvB;\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	vec2 ra = inputColor.ra;\r
	vec2 ba = inputColor.ba;\r
\r
	#ifdef RADIAL_MODULATION\r
\r
		const vec2 center = vec2(0.5);\r
		float d = distance(uv, center) * 2.0;\r
		d = max(d - modulationOffset, 0.0);\r
\r
		if(vActive > 0.0 && d > 0.0) {\r
\r
			ra = texture2D(inputBuffer, mix(uv, vUvR, d)).ra;\r
			ba = texture2D(inputBuffer, mix(uv, vUvB, d)).ba;\r
\r
		}\r
\r
	#else\r
\r
		if(vActive > 0.0) {\r
\r
			ra = texture2D(inputBuffer, vUvR).ra;\r
			ba = texture2D(inputBuffer, vUvB).ba;\r
\r
		}\r
\r
	#endif\r
\r
	outputColor = vec4(ra.x, inputColor.g, ba.x, max(max(ra.y, ba.y), inputColor.a));\r
\r
}\r
`, Xa = `uniform vec2 offset;\r
\r
varying float vActive;\r
varying vec2 vUvR;\r
varying vec2 vUvB;\r
\r
void mainSupport(const in vec2 uv) {\r
\r
	vec2 shift = offset * vec2(1.0, aspect);\r
	vActive = (shift.x != 0.0 || shift.y != 0.0) ? 1.0 : 0.0;\r
	vUvR = uv + shift;\r
	vUvB = uv - shift;\r
\r
}\r
`;
class yh extends I {
  /**
   * Constructs a new chromatic aberration effect.
   *
   * @param {Object} [options] - The options.
   * @param {Vector2} [options.offset] - The color offset.
   * @param {Boolean} [options.radialModulation=false] - Whether the effect should be modulated with a radial gradient.
   * @param {Number} [options.modulationOffset=0.15] - The modulation offset. Only applies if `radialModulation` is enabled.
   */
  constructor({
    offset: e = new g(1e-3, 5e-4),
    radialModulation: t = !1,
    modulationOffset: r = 0.15
  } = {}) {
    super("ChromaticAberrationEffect", ja, {
      vertexShader: Xa,
      attributes: Q.CONVOLUTION,
      uniforms: /* @__PURE__ */ new Map([
        ["offset", new a(e)],
        ["modulationOffset", new a(r)]
      ])
    }), this.radialModulation = t;
  }
  /**
   * The color offset.
   *
   * @type {Vector2}
   */
  get offset() {
    return this.uniforms.get("offset").value;
  }
  set offset(e) {
    this.uniforms.get("offset").value = e;
  }
  /**
   * Indicates whether radial modulation is enabled.
   *
   * When enabled, the effect will be weaker in the middle and stronger towards the screen edges.
   *
   * @type {Boolean}
   */
  get radialModulation() {
    return this.defines.has("RADIAL_MODULATION");
  }
  set radialModulation(e) {
    e ? this.defines.set("RADIAL_MODULATION", "1") : this.defines.delete("RADIAL_MODULATION"), this.setChanged();
  }
  /**
   * The modulation offset.
   *
   * @type {Number}
   */
  get modulationOffset() {
    return this.uniforms.get("modulationOffset").value;
  }
  set modulationOffset(e) {
    this.uniforms.get("modulationOffset").value = e;
  }
  /**
   * Returns the color offset vector.
   *
   * @deprecated Use offset instead.
   * @return {Vector2} The offset.
   */
  getOffset() {
    return this.offset;
  }
  /**
   * Sets the color offset vector.
   *
   * @deprecated Use offset instead.
   * @param {Vector2} value - The offset.
   */
  setOffset(e) {
    this.offset = e;
  }
}
const Ya = `// 日夜循环天空效果着色器
// 基于László Matuska (@BitOfGold)的Shadertoy代码
// 原始着色器: https://www.shadertoy.com/view/ltlSWB

// uniform sampler2D inputBuffer; // 已由Effect基类定义
// uniform sampler2D depthBuffer; // 已由Effect基类定义
uniform sampler2D noiseTexture; // 噪声纹理
uniform vec2 resolution; // 分辨率
uniform float time; // 时间
uniform vec3 sunPosition; // 太阳位置
uniform vec3 moonPosition; // 月亮位置
uniform int animateClouds; // 云动画开关
uniform float cloudy; // 云密度
uniform float height; // 观察者高度
uniform float haze; // 雾霾程度
uniform float cloudyhigh; // 高层云密度
uniform int enableStars; // 启用星空
uniform float starThreshold; // 星星密度阈值
uniform float skyMaskThreshold; // 天空深度阈值
uniform mat4 viewMatrix; // 视图矩阵
uniform float fov; // 视场角

// varying vec2 vUv; // 已由Effect基类定义

// 常量定义
const float M_PI = 3.1415926535;
const float DEGRAD = M_PI / 180.0;

// 渲染质量参数
const int steps = 30; // 采样步数，对应原始Shadertoy中的steps
const int stepss = 6; // 光线采样步数，对应原始Shadertoy中的stepss

// 云层参数
const float cloudnear = 1.0; // 云层最近距离
const float cloudfar = 1e3;  // 云层最远距离

// 散射参数
const float I = 10.0; // 太阳光强度
const float g = 0.45; // 光线集中度
const float g2 = g * g;

// 瑞利散射 (天空色彩, 大气散射 - 8km)
const vec3 bR = vec3(5.8e-6, 13.5e-6, 33.1e-6); // 地球正常散射系数

// 米氏散射 (水分子, 雾 - 1km)
const vec3 bM = vec3(21e-6); // 正常米氏散射

// 大气层定义
const float Hr = 8000.0; // 瑞利散射顶层高度
const float Hm = 1000.0; // 米氏散射顶层高度
const float R0 = 6360e3; // 星球半径
const float Ra = 6380e3; // 大气层半径
const vec3 C = vec3(0.0, -R0, 0.0); // 星球中心

//--------------------------------------------------------------------------
// 星空随机函数

// 返回范围为[0.0, 1.0]的随机噪声
float Noise2d(in vec2 x) {
    float xhash = cos(x.x * 37.0);
    float yhash = cos(x.y * 57.0);
    return fract(415.92653 * (xhash + yhash));
}

// 将Noise2d转换为"星空", 通过阈值过滤低于fThreshhold的值
float NoisyStarField(in vec2 vSamplePos, float fThreshhold) {
    float StarVal = Noise2d(vSamplePos);
    if (StarVal >= fThreshhold)
        StarVal = pow((StarVal - fThreshhold) / (1.0 - fThreshhold), 6.0);
    else
        StarVal = 0.0;
    return StarVal;
}

// 通过仅在整数值位置采样来稳定NoisyStarField
float StableStarField(in vec2 vSamplePos, float fThreshhold) {
    // 四个样本之间的线性插值
    float fractX = fract(vSamplePos.x);
    float fractY = fract(vSamplePos.y);
    vec2 floorSample = floor(vSamplePos);    
    float v1 = NoisyStarField(floorSample, fThreshhold);
    float v2 = NoisyStarField(floorSample + vec2(0.0, 1.0), fThreshhold);
    float v3 = NoisyStarField(floorSample + vec2(1.0, 0.0), fThreshhold);
    float v4 = NoisyStarField(floorSample + vec2(1.0, 1.0), fThreshhold);

    float StarVal = v1 * (1.0 - fractX) * (1.0 - fractY)
        + v2 * (1.0 - fractX) * fractY
        + v3 * fractX * (1.0 - fractY)
        + v4 * fractX * fractY;
    return StarVal;
}

//--------------------------------------------------------------------------
// 云层噪声函数

float Noise(in vec3 x) {
    vec3 p = floor(x);
    vec3 f = fract(x);
    f = f * f * (3.0 - 2.0 * f);

    vec2 uv = (p.xy + vec2(37.0, 17.0) * p.z) + f.xy;
    vec2 rg = texture(noiseTexture, (uv + 0.5) / 256.0).yx;
    return mix(rg.x, rg.y, f.z);
}

float fnoise(vec3 p, in float t) {
    p *= 0.25;
    float f;

    f = 0.5000 * Noise(p); p = p * 3.02; p.y -= t * 0.2;
    f += 0.2500 * Noise(p); p = p * 3.03; p.y += t * 0.06;
    f += 0.1250 * Noise(p); p = p * 3.01;
    f += 0.0625 * Noise(p); p = p * 3.03;
    f += 0.03125 * Noise(p); p = p * 3.02;
    f += 0.015625 * Noise(p);
    return f;
}

//--------------------------------------------------------------------------
// 云层和散射函数

float cloud(vec3 p, in float t) {
    float cld = fnoise(p * 2e-4, t) + cloudy * 0.1;
    cld = smoothstep(0.4 + 0.04, 0.6 + 0.04, cld);
    cld *= 70.0;
    return cld + haze;
}

void densities(in vec3 pos, out float rayleigh, out float mie, in float t) {
    float h = length(pos - C) - R0;
    rayleigh = exp(-h / Hr);
    vec3 d = pos;
    d.y = 0.0;
    float dist = length(d);
    
    // 云层处理
    float cld = 0.0;
    if (5e3 < h && h < 8e3) {
        cld = cloud(pos + vec3(23175.7, 0.0, -t * 3e3), t);
        cld *= sin(3.1415 * (h - 5e3) / 5e3) * cloudy;
    }
    
    // 高云层处理
    float cld2 = 0.0;
    if (12e3 < h && h < 15.5e3) {
        cld2 = fnoise(pos * 3e-4, t) * cloud(pos * 32.0 + vec3(27612.3, 0.0, -t * 15e3), t);
        cld2 *= sin(3.1413 * (h - 12e3) / 12e3) * cloudyhigh;
        cld2 = clamp(cld2, 0.0, 1.0);
    }

    // 云层距离处理
    if (dist < cloudfar) {
        float factor = clamp(1.0 - ((cloudfar - dist) / (cloudfar - cloudnear)), 0.0, 1.0);
        cld *= factor;
    }

    mie = exp(-h / Hm) + cld + haze + cld2;
}

float escape(in vec3 p, in vec3 d, in float R) {
    vec3 v = p - C;
    float b = dot(v, d);
    float c = dot(v, v) - R * R;
    float det2 = b * b - c;
    if (det2 < 0.0) return -1.0;
    float det = sqrt(det2);
    float t1 = -b - det, t2 = -b + det;
    return (t1 >= 0.0) ? t1 : t2;
}

// 大气散射函数
void scatter(vec3 o, vec3 d, out vec3 col, out float scatteringFactor, in float t) {
    float L = escape(o, d, Ra);
    // 计算太阳方向
    vec3 Ds = normalize(sunPosition);
    float mu = dot(d, Ds);
    float opmu2 = 1.0 + mu * mu;
    float phaseR = 0.0596831 * opmu2;
    float phaseM = 0.1193662 * (1.0 - g2) * opmu2 / ((2.0 + g2) * pow(1.0 + g2 - 2.0 * g * mu, 1.5));

    float depthR = 0.0, depthM = 0.0;
    vec3 R = vec3(0.0), M = vec3(0.0);

    float dl = L / float(steps);
    for (int i = 0; i < steps; ++i) {
        float l = float(i) * dl;
        vec3 p = o + d * l;

        float dR, dM;
        densities(p, dR, dM, t);
        dR *= dl; dM *= dl;
        depthR += dR;
        depthM += dM;

        float Ls = escape(p, Ds, Ra);
        if (Ls > 0.0) {
            float dls = Ls / float(stepss);
            float depthRs = 0.0, depthMs = 0.0;
            for (int j = 0; j < stepss; ++j) {
                float ls = float(j) * dls;
                vec3 ps = p + Ds * ls;
                float dRs, dMs;
                densities(ps, dRs, dMs, t);
                depthRs += dRs * dls;
                depthMs += dMs * dls;
            }

            vec3 A = exp(-(bR * (depthRs + depthR) + bM * (depthMs + depthM)));
            vec3 dA = (A - exp(-(bR * depthR + bM * depthM))) * I;
            R += dA * dR * phaseR;
            M += dA * dM * phaseM;
        }
    }
    
    // 添加月亮光照
    vec3 Dm = normalize(moonPosition);
    float muM = dot(d, Dm);
    float moonPhase = 0.1193662 * (1.0 - g2) * (1.0 + muM * muM) / ((2.0 + g2) * pow(1.0 + g2 - 2.0 * g * muM, 1.5));
    float moonLight = 0.2 * smoothstep(0.9985, 0.9995, muM); // 月亮光斑
    
    // 直接计算最终颜色和散射系数
    vec3 moonCol = vec3(0.8, 0.8, 1.0) * moonPhase * 0.05; // 月光颜色
    col = (R * bR) + (M * bM) + moonCol; // 确保使用标量-向量乘法
    scatteringFactor = (1.0 - exp(-length(bM) * depthM)) - (moonLight * 2.0); // 使用bM的长度确保是标量计算
}

void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
    // 读取深度缓冲区
    float depth = texture(depthBuffer, uv).r;
    
    // 计算UV方向
    vec2 q = uv * 2.0 - 1.0;
    q.x *= resolution.x / resolution.y;
    
    // 计算视图方向
    vec3 vuv = vec3(viewMatrix[0][1], viewMatrix[1][1], viewMatrix[2][1]);
    vec3 vrgt = vec3(viewMatrix[0][0], viewMatrix[1][0], viewMatrix[2][0]);
    vec3 vfwd = -vec3(viewMatrix[0][2], viewMatrix[1][2], viewMatrix[2][2]);
    
    // 根据FOV计算视线方向
    float rad = fov * DEGRAD / 2.0;
    float vtan = tan(rad);
    vec3 dir = normalize(vfwd + q.x * vrgt * vtan + q.y * vuv * vtan);
    
    // 计算观察者位置（加上高度）
    vec3 ro = vec3(0.0, height, 0.0);
    
    // 天空渲染
    vec3 sky = vec3(0.0);
    float scatteringFactor = 0.0;
    float t = time * 0.04 * float(animateClouds); // 云动画时间
    
    // 计算地平线以上的天空
    if (dir.y > -0.02) {
        // 大气散射计算
        scatter(ro, dir, sky, scatteringFactor, t);
        
        // 星空计算
        if (enableStars == 1) {
            // 计算夜空比例（根据太阳高度）
            float nightSky = smoothstep(-0.15, 0.05, -sunPosition.y);
            
            // 星空计算
            if (nightSky > 0.0) {
                // 使用球面投影扭曲坐标，使星星分布在球形天空上
                vec2 starCoord = vec2(
                    atan(dir.z, dir.x) / (2.0 * M_PI),
                    acos(dir.y) / M_PI
                );
                
                // 添加缓慢移动（跟随云层）
                starCoord.x += t * 0.002;
                starCoord.y += t * 0.001;
                
                // 计算星空密度
                float stars = StableStarField(starCoord * 500.0, starThreshold);
                
                // 只在天空较暗的部分显示星星
                float starsMask = 1.0 - min(1.0, length(sky * 25.0));
                starsMask *= smoothstep(0.0, 0.5, dir.y); // 靠近地平线减少星星
                
                // 添加星星到天空颜色
                sky += stars * vec3(0.8, 0.9, 1.0) * starsMask * nightSky;
            }
        }
    } else {
        // 对于地平线以下，使用简单渐变
        sky = mix(
            vec3(0.1, 0.2, 0.4) * 0.2, // 地面颜色
            vec3(0.3, 0.4, 0.6) * 0.3, // 地平线颜色
            smoothstep(-0.1, -0.03, dir.y)
        );
    }
    
    // 添加月亮
    float moonDot = dot(dir, normalize(moonPosition));
    float moonSize = 0.004;
    float moonMask = smoothstep(0.9988 - moonSize, 0.9999 - moonSize * 0.5, moonDot);
    // 月亮只在夜晚可见（基于太阳位置）
    float moonVisibility = smoothstep(-0.1, -0.2, sunPosition.y);
    sky = mix(sky, vec3(0.9, 0.95, 1.0), moonMask * moonVisibility);
    
    // 根据深度混合天空和原始图像
    // 当深度值接近1.0（远处）时，使用天空颜色；否则使用原始图像颜色
    float skyMask = smoothstep(skyMaskThreshold, skyMaskThreshold + 0.0005, depth);
    
    // 输出结果：混合原始颜色和天空颜色
    outputColor = mix(inputColor, vec4(sky, 1.0), skyMask);
}

void main() {
    vec4 inputColor = texture(inputBuffer, vUv);
    vec4 outputColor = vec4(0.0);
    
    mainImage(inputColor, vUv, outputColor);
    
    gl_FragColor = outputColor;
} `;
class Mh extends I {
  /**
      * 构造函数
      *
      * @param {Object} [options] - 效果选项
      * @param {BlendFunction} [options.blendFunction=BlendFunction.SCREEN] - 混合模式
      * @param {Vector3} [options.sunPosition=new Vector3(0.1, 0.05, -1).normalize()] - 太阳位置（方向向量）
      * @param {Vector3} [options.moonPosition=new Vector3(-0.1, 0.05, -1).normalize()] - 月亮位置（方向向量）
      * @param {Boolean} [options.animateClouds=true] - 是否启用云层动画
      * @param {Number} [options.cloudy=0.6] - 云层密度 (0-1)
      * @param {Number} [options.height=500.0] - 观察者高度
      * @param {Number} [options.haze=0.1] - 雾霾程度
      * @param {Number} [options.cloudyhigh=0.05] - 高层云密度
      * @param {Number} [options.enableStars=true] - 是否启用星空
      * @param {Number} [options.starThreshold=0.99] - 星星密度阈值 (0.9-1.0)
      * @param {Number} [options.skyMaskThreshold=0.998] - 天空深度阈值 (0.9-1.0)
      * @param {Number} [options.fov=60.0] - 视场角（度）
      */
  constructor({
    blendFunction: e = E.SCREEN,
    sunPosition: t = new T(0.1, 0.05, -1).normalize(),
    moonPosition: r = new T(-0.1, 0.05, -1).normalize(),
    animateClouds: n = !0,
    cloudy: i = 0.6,
    height: s = 500,
    haze: o = 0.1,
    cloudyhigh: l = 0.05,
    enableStars: u = !0,
    starThreshold: f = 0.99,
    skyMaskThreshold: h = 0.998,
    fov: m = 60
  } = {}) {
    super("DayNightCycleEffect", Ya, {
      blendFunction: e,
      attributes: Q.DEPTH,
      defines: /* @__PURE__ */ new Map([
        ["USE_DEPTH", "1"]
      ]),
      uniforms: /* @__PURE__ */ new Map([
        ["sunPosition", new a(t)],
        ["moonPosition", new a(r)],
        ["animateClouds", new a(n ? 1 : 0)],
        ["cloudy", new a(i)],
        ["height", new a(s)],
        ["haze", new a(o)],
        ["cloudyhigh", new a(l)],
        ["enableStars", new a(u ? 1 : 0)],
        ["starThreshold", new a(f)],
        ["skyMaskThreshold", new a(h)],
        ["time", new a(0)],
        ["resolution", new a(new g(1, 1))],
        ["noiseTexture", new a(null)],
        ["viewMatrix", new a(new C())],
        ["fov", new a(m)]
      ])
    }), this.sunPosition = t, this.moonPosition = r, this.animateClouds = n, this._cloudy = i, this._height = s, this._haze = o, this._cloudyhigh = l, this._enableStars = u, this._starThreshold = f, this._skyMaskThreshold = h, this._fov = m, this._camera = null, this.createDefaultNoiseTexture();
  }
  /**
      * 获取云层密度
      * @return {Number} 云层密度
      */
  get cloudy() {
    return this._cloudy;
  }
  /**
      * 设置云层密度
      * @param {Number} value - 云层密度
      */
  set cloudy(e) {
    this._cloudy = e, this.uniforms.get("cloudy").value = e;
  }
  /**
      * 获取观察者高度
      * @return {Number} 观察者高度
      */
  get height() {
    return this._height;
  }
  /**
      * 设置观察者高度
      * @param {Number} value - 观察者高度
      */
  set height(e) {
    this._height = e, this.uniforms.get("height").value = e;
  }
  /**
      * 获取雾霾程度
      * @return {Number} 雾霾程度
      */
  get haze() {
    return this._haze;
  }
  /**
      * 设置雾霾程度
      * @param {Number} value - 雾霾程度
      */
  set haze(e) {
    this._haze = e, this.uniforms.get("haze").value = e;
  }
  /**
      * 获取高层云密度
      * @return {Number} 高层云密度
      */
  get cloudyhigh() {
    return this._cloudyhigh;
  }
  /**
      * 设置高层云密度
      * @param {Number} value - 高层云密度
      */
  set cloudyhigh(e) {
    this._cloudyhigh = e, this.uniforms.get("cloudyhigh").value = e;
  }
  /**
      * 获取星空启用状态
      * @return {Boolean} 是否启用星空
      */
  get enableStars() {
    return this._enableStars;
  }
  /**
      * 设置星空启用状态
      * @param {Boolean} value - 是否启用星空
      */
  set enableStars(e) {
    this._enableStars = e, this.uniforms.get("enableStars").value = e ? 1 : 0;
  }
  /**
      * 获取星星密度阈值
      * @return {Number} 星星密度阈值
      */
  get starThreshold() {
    return this._starThreshold;
  }
  /**
      * 设置星星密度阈值
      * @param {Number} value - 星星密度阈值
      */
  set starThreshold(e) {
    this._starThreshold = e, this.uniforms.get("starThreshold").value = e;
  }
  /**
      * 获取天空深度阈值
      * @return {Number} 天空深度阈值
      */
  get skyMaskThreshold() {
    return this._skyMaskThreshold;
  }
  /**
      * 设置天空深度阈值
      * @param {Number} value - 天空深度阈值
      */
  set skyMaskThreshold(e) {
    this._skyMaskThreshold = e, this.uniforms.get("skyMaskThreshold").value = e;
  }
  /**
      * 获取视场角
      * @return {Number} 视场角（度）
      */
  get fov() {
    return this._fov;
  }
  /**
      * 设置视场角
      * @param {Number} value - 视场角（度）
      */
  set fov(e) {
    this._fov = e, this.uniforms.get("fov").value = e;
  }
  /**
      * 创建默认噪声纹理
      * 用于在没有设置纹理时提供基本云噪声
      * @private
      */
  createDefaultNoiseTexture() {
    const t = new Uint8Array(16384);
    for (let n = 0; n < 4096; n++)
      t[n * 4] = Math.random() * 255, t[n * 4 + 1] = Math.random() * 255, t[n * 4 + 2] = Math.random() * 255, t[n * 4 + 3] = 255;
    const r = new le(t, 64, 64);
    r.wrapS = r.wrapT = G, r.minFilter = F, r.magFilter = F, r.needsUpdate = !0, this.uniforms.get("noiseTexture").value = r;
  }
  /**
      * 设置相机
      *
      * @param {Camera} camera - 相机对象
      */
  setCamera(e) {
    this._camera = e, e && (this.fov = e.fov);
  }
  /**
      * 设置太阳位置
      * 使用高度角和方位角设置太阳位置
      *
      * @param {Number} elevation - 高度角（度）
      * @param {Number} azimuth - 方位角（度）
      */
  setSunPosition(e, t) {
    const r = Ee.degToRad(90 - e), n = Ee.degToRad(t), i = Math.sin(r) * Math.cos(n), s = Math.cos(r), o = Math.sin(r) * Math.sin(n);
    this.sunPosition.set(i, s, o).normalize(), this.uniforms.get("sunPosition").value.copy(this.sunPosition);
  }
  /**
      * 设置月亮位置
      * 使用高度角和方位角设置月亮位置
      *
      * @param {Number} elevation - 高度角（度）
      * @param {Number} azimuth - 方位角（度）
      */
  setMoonPosition(e, t) {
    const r = Ee.degToRad(90 - e), n = Ee.degToRad(t), i = Math.sin(r) * Math.cos(n), s = Math.cos(r), o = Math.sin(r) * Math.sin(n);
    this.moonPosition.set(i, s, o).normalize(), this.uniforms.get("moonPosition").value.copy(this.moonPosition);
  }
  /**
      * 设置噪声纹理
      *
      * @param {Texture} texture - 噪声纹理
      */
  setNoiseTexture(e) {
    e !== null && (e.minFilter = F, e.magFilter = F, e.wrapS = e.wrapT = G), this.uniforms.get("noiseTexture").value = e;
  }
  /**
      * 更新效果
      *
      * @param {WebGLRenderer} renderer - WebGL渲染器
      * @param {WebGLRenderTarget} inputBuffer - 输入缓冲区
      * @param {Number} [deltaTime] - 时间增量
      */
  update(e, t, r) {
    this.uniforms.get("time").value += r;
    const n = e.getSize(new g());
    this.uniforms.get("resolution").value.set(n.width, n.height), this.uniforms.get("animateClouds").value = this.animateClouds ? 1 : 0, this.uniforms.get("cloudy").value = this._cloudy, this.uniforms.get("height").value = this._height, this.uniforms.get("haze").value = this._haze, this.uniforms.get("cloudyhigh").value = this._cloudyhigh, this.uniforms.get("enableStars").value = this._enableStars ? 1 : 0, this.uniforms.get("starThreshold").value = this._starThreshold, this.uniforms.get("skyMaskThreshold").value = this._skyMaskThreshold, this._camera && (this.uniforms.get("viewMatrix").value.copy(this._camera.matrixWorld), this._camera.fov !== this._fov && (this.fov = this._camera.fov));
  }
}
const Za = `void mainImage(const in vec4 inputColor, const in vec2 uv, const in float depth, out vec4 outputColor) {\r
\r
	#ifdef INVERTED\r
\r
		vec3 color = vec3(1.0 - depth);\r
\r
	#else\r
\r
		vec3 color = vec3(depth);\r
\r
	#endif\r
\r
	outputColor = vec4(color, inputColor.a);\r
\r
}\r
`;
class wh extends I {
  /**
   * Constructs a new depth effect.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.SRC] - The blend function of this effect.
   * @param {Boolean} [options.inverted=false] - Whether the depth should be inverted.
   */
  constructor({ blendFunction: e = E.SRC, inverted: t = !1 } = {}) {
    super("DepthEffect", Za, {
      blendFunction: e,
      attributes: Q.DEPTH
    }), this.inverted = t;
  }
  /**
   * Indicates whether depth should be inverted.
   *
   * @type {Boolean}
   */
  get inverted() {
    return this.defines.has("INVERTED");
  }
  set inverted(e) {
    this.inverted !== e && (e ? this.defines.set("INVERTED", "1") : this.defines.delete("INVERTED"), this.setChanged());
  }
  /**
   * Indicates whether the rendered depth is inverted.
   *
   * @deprecated Use inverted instead.
   * @return {Boolean} Whether the rendered depth is inverted.
   */
  isInverted() {
    return this.inverted;
  }
  /**
   * Enables or disables depth inversion.
   *
   * @deprecated Use inverted instead.
   * @param {Boolean} value - Whether depth should be inverted.
   */
  setInverted(e) {
    this.inverted = e;
  }
}
const mt = {
  RED: 0,
  GREEN: 1,
  BLUE: 2,
  ALPHA: 3
}, Ni = {
  DISCARD: 0,
  MULTIPLY: 1,
  MULTIPLY_RGB_SET_ALPHA: 2,
  MULTIPLY_RGB: 3
}, Ka = `#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D inputBuffer;\r
\r
#else\r
\r
	uniform lowp sampler2D inputBuffer;\r
\r
#endif\r
\r
#if PASS == 1\r
\r
	uniform vec4 kernel64[32];\r
\r
#else\r
\r
	uniform vec4 kernel16[8];\r
\r
#endif\r
\r
uniform lowp sampler2D cocBuffer;\r
uniform vec2 texelSize;\r
uniform float scale;\r
\r
varying vec2 vUv;\r
\r
void main() {\r
\r
	#ifdef FOREGROUND\r
\r
		vec2 cocNearFar = texture2D(cocBuffer, vUv).rg * scale;\r
		float coc = cocNearFar.x;\r
\r
	#else\r
\r
		float coc = texture2D(cocBuffer, vUv).g * scale;\r
\r
	#endif\r
\r
	if(coc == 0.0) {\r
\r
		// Skip blurring.\r
		gl_FragColor = texture2D(inputBuffer, vUv);\r
\r
	} else {\r
\r
		#ifdef FOREGROUND\r
\r
			// Use far CoC to avoid weak blurring around foreground objects.\r
			vec2 step = texelSize * max(cocNearFar.x, cocNearFar.y);\r
\r
		#else\r
\r
			vec2 step = texelSize * coc;\r
\r
		#endif\r
\r
		#if PASS == 1\r
\r
			vec4 acc = vec4(0.0);\r
\r
			// Each vector contains two sampling points (64 in total).\r
			for(int i = 0; i < 32; ++i) {\r
\r
				vec4 kernel = kernel64[i];\r
\r
				vec2 uv = step * kernel.xy + vUv;\r
				acc += texture2D(inputBuffer, uv);\r
\r
				uv = step * kernel.zw + vUv;\r
				acc += texture2D(inputBuffer, uv);\r
\r
			}\r
\r
			gl_FragColor = acc / 64.0;\r
\r
		#else\r
\r
			vec4 maxValue = texture2D(inputBuffer, vUv);\r
\r
			// Each vector contains two sampling points (16 in total).\r
			for(int i = 0; i < 8; ++i) {\r
\r
				vec4 kernel = kernel16[i];\r
\r
				vec2 uv = step * kernel.xy + vUv;\r
				maxValue = max(texture2D(inputBuffer, uv), maxValue);\r
\r
				uv = step * kernel.zw + vUv;\r
				maxValue = max(texture2D(inputBuffer, uv), maxValue);\r
\r
\r
			}\r
\r
			gl_FragColor = maxValue;\r
\r
		#endif\r
\r
	}\r
\r
}\r
`;
class Xt extends N {
  /**
   * Constructs a new bokeh material.
   *
   * @param {Boolean} [fill=false] - Enables or disables the bokeh highlight fill mode.
   * @param {Boolean} [foreground=false] - Determines whether this material will be applied to foreground colors.
   */
  constructor(e = !1, t = !1) {
    super({
      name: "BokehMaterial",
      defines: {
        PASS: e ? "2" : "1"
      },
      uniforms: {
        inputBuffer: new a(null),
        cocBuffer: new a(null),
        texelSize: new a(new g()),
        kernel64: new a(null),
        kernel16: new a(null),
        scale: new a(1)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: Ka,
      vertexShader: Be
    }), t && (this.defines.FOREGROUND = "1"), this.generateKernel();
  }
  /**
   * The input buffer.
   *
   * @type {Texture}
   */
  set inputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * Sets the input buffer.
   *
   * @deprecated Use inputBuffer instead.
   * @param {Texture} value - The buffer.
   */
  setInputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * The circle of confusion buffer.
   *
   * @type {Texture}
   */
  set cocBuffer(e) {
    this.uniforms.cocBuffer.value = e;
  }
  /**
   * Sets the circle of confusion buffer.
   *
   * @deprecated Use cocBuffer instead.
   * @param {Texture} value - The buffer.
   */
  setCoCBuffer(e) {
    this.uniforms.cocBuffer.value = e;
  }
  /**
   * The blur scale.
   *
   * @type {Number}
   */
  get scale() {
    return this.uniforms.scale.value;
  }
  set scale(e) {
    this.uniforms.scale.value = e;
  }
  /**
   * Returns the blur scale.
   *
   * @deprecated Use scale instead.
   * @return {Number} The scale.
   */
  getScale(e) {
    return this.scale;
  }
  /**
   * Sets the blur scale.
   *
   * @deprecated Use scale instead.
   * @param {Number} value - The scale.
   */
  setScale(e) {
    this.scale = e;
  }
  /**
   * Generates the blur kernel.
   *
   * @private
   */
  generateKernel() {
    const e = 2.39996323, t = new Float64Array(128), r = new Float64Array(32);
    let n = 0, i = 0;
    for (let s = 0, o = Math.sqrt(80); s < 80; ++s) {
      const l = s * e, u = Math.sqrt(s) / o, f = u * Math.cos(l), h = u * Math.sin(l);
      s % 5 === 0 ? (r[i++] = f, r[i++] = h) : (t[n++] = f, t[n++] = h);
    }
    this.uniforms.kernel64.value = t, this.uniforms.kernel16.value = r;
  }
  /**
   * Sets the texel size.
   *
   * @deprecated Use setSize() instead.
   * @param {Number} x - The texel width.
   * @param {Number} y - The texel height.
   */
  setTexelSize(e, t) {
    this.uniforms.texelSize.value.set(e, t);
  }
  /**
   * Sets the size of this object.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.uniforms.texelSize.value.set(1 / e, 1 / t);
  }
}
function $e(c, e, t) {
  return c * (e - t) - e;
}
function Qe(c, e, t) {
  return Math.min(Math.max((c + e) / (e - t), 0), 1);
}
const Qa = `#include <common>\r
#include <packing>\r
\r
#ifdef GL_FRAGMENT_PRECISION_HIGH\r
\r
	uniform highp sampler2D depthBuffer;\r
\r
#else\r
\r
	uniform mediump sampler2D depthBuffer;\r
\r
#endif\r
\r
uniform float focusDistance;\r
uniform float focusRange;\r
uniform float cameraNear;\r
uniform float cameraFar;\r
\r
varying vec2 vUv;\r
\r
float readDepth(const in vec2 uv) {\r
\r
	#if DEPTH_PACKING == 3201\r
\r
		float depth = unpackRGBAToDepth(texture2D(depthBuffer, uv));\r
\r
	#else\r
\r
		float depth = texture2D(depthBuffer, uv).r;\r
\r
	#endif\r
\r
	#ifdef LOG_DEPTH\r
\r
		float d = pow(2.0, depth * log2(cameraFar + 1.0)) - 1.0;\r
		float a = cameraFar / (cameraFar - cameraNear);\r
		float b = cameraFar * cameraNear / (cameraNear - cameraFar);\r
		depth = a + b / d;\r
\r
	#endif\r
\r
	return depth;\r
\r
}\r
\r
void main() {\r
\r
	float depth = readDepth(vUv);\r
\r
	#ifdef PERSPECTIVE_CAMERA\r
\r
		float viewZ = perspectiveDepthToViewZ(depth, cameraNear, cameraFar);\r
		float linearDepth = viewZToOrthographicDepth(viewZ, cameraNear, cameraFar);\r
\r
	#else\r
\r
		float linearDepth = depth;\r
\r
	#endif\r
\r
	float signedDistance = linearDepth - focusDistance;\r
	float magnitude = smoothstep(0.0, focusRange, abs(signedDistance));\r
\r
	gl_FragColor.rg = magnitude * vec2(\r
		step(signedDistance, 0.0),\r
		step(0.0, signedDistance)\r
	);\r
\r
}\r
`;
class qa extends N {
  /**
   * Constructs a new CoC material.
   *
   * @param {Camera} camera - A camera.
   */
  constructor(e) {
    super({
      name: "CircleOfConfusionMaterial",
      defines: {
        DEPTH_PACKING: "0"
      },
      uniforms: {
        depthBuffer: new a(null),
        focusDistance: new a(0),
        focusRange: new a(0),
        cameraNear: new a(0.3),
        cameraFar: new a(1e3)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: Qa,
      vertexShader: Be
    }), this.uniforms.focalLength = this.uniforms.focusRange, this.copyCameraSettings(e);
  }
  /**
   * The current near plane setting.
   *
   * @type {Number}
   * @private
   */
  get near() {
    return this.uniforms.cameraNear.value;
  }
  /**
   * The current far plane setting.
   *
   * @type {Number}
   * @private
   */
  get far() {
    return this.uniforms.cameraFar.value;
  }
  /**
   * The depth buffer.
   *
   * @type {Texture}
   */
  set depthBuffer(e) {
    this.uniforms.depthBuffer.value = e;
  }
  /**
   * The depth packing strategy.
   *
   * @type {DepthPackingStrategies}
   */
  set depthPacking(e) {
    this.defines.DEPTH_PACKING = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Sets the depth buffer.
   *
   * @deprecated Use depthBuffer and depthPacking instead.
   * @param {Texture} buffer - The depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing strategy.
   */
  setDepthBuffer(e, t = $) {
    this.depthBuffer = e, this.depthPacking = t;
  }
  /**
   * The focus distance. Range: [0.0, 1.0].
   *
   * @type {Number}
   */
  get focusDistance() {
    return this.uniforms.focusDistance.value;
  }
  set focusDistance(e) {
    this.uniforms.focusDistance.value = e;
  }
  /**
   * The focus distance in world units.
   *
   * @type {Number}
   */
  get worldFocusDistance() {
    return -$e(this.focusDistance, this.near, this.far);
  }
  set worldFocusDistance(e) {
    this.focusDistance = Qe(-e, this.near, this.far);
  }
  /**
   * Returns the focus distance.
   *
   * @deprecated Use focusDistance instead.
   * @return {Number} The focus distance.
   */
  getFocusDistance(e) {
    this.uniforms.focusDistance.value = e;
  }
  /**
   * Sets the focus distance.
   *
   * @deprecated Use focusDistance instead.
   * @param {Number} value - The focus distance.
   */
  setFocusDistance(e) {
    this.uniforms.focusDistance.value = e;
  }
  /**
   * The focal length.
   *
   * @deprecated Renamed to focusRange.
   * @type {Number}
   */
  get focalLength() {
    return this.focusRange;
  }
  set focalLength(e) {
    this.focusRange = e;
  }
  /**
   * The focus range. Range: [0.0, 1.0].
   *
   * @type {Number}
   */
  get focusRange() {
    return this.uniforms.focusRange.value;
  }
  set focusRange(e) {
    this.uniforms.focusRange.value = e;
  }
  /**
   * The focus range in world units.
   *
   * @type {Number}
   */
  get worldFocusRange() {
    return -$e(this.focusRange, this.near, this.far);
  }
  set worldFocusRange(e) {
    this.focusRange = Qe(-e, this.near, this.far);
  }
  /**
   * Returns the focal length.
   *
   * @deprecated Use focusRange instead.
   * @return {Number} The focal length.
   */
  getFocalLength(e) {
    return this.focusRange;
  }
  /**
   * Sets the focal length.
   *
   * @deprecated Use focusRange instead.
   * @param {Number} value - The focal length.
   */
  setFocalLength(e) {
    this.focusRange = e;
  }
  /**
   * Copies the settings of the given camera.
   *
   * @deprecated Use copyCameraSettings instead.
   * @param {Camera} camera - A camera.
   */
  adoptCameraSettings(e) {
    this.copyCameraSettings(e);
  }
  /**
   * Copies the settings of the given camera.
   *
   * @param {Camera} camera - A camera.
   */
  copyCameraSettings(e) {
    e && (this.uniforms.cameraNear.value = e.near, this.uniforms.cameraFar.value = e.far, e instanceof ke ? this.defines.PERSPECTIVE_CAMERA = "1" : delete this.defines.PERSPECTIVE_CAMERA, this.needsUpdate = !0);
  }
}
const Ja = `#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D inputBuffer;\r
\r
#else\r
\r
	uniform lowp sampler2D inputBuffer;\r
\r
#endif\r
\r
#ifdef MASK_PRECISION_HIGH\r
\r
	uniform mediump sampler2D maskTexture;\r
\r
#else\r
\r
	uniform lowp sampler2D maskTexture;\r
\r
#endif\r
\r
#if MASK_FUNCTION != 0\r
\r
	uniform float strength;\r
\r
#endif\r
\r
varying vec2 vUv;\r
\r
void main() {\r
\r
	#if COLOR_CHANNEL == 0\r
\r
		float mask = texture2D(maskTexture, vUv).r;\r
\r
	#elif COLOR_CHANNEL == 1\r
\r
		float mask = texture2D(maskTexture, vUv).g;\r
\r
	#elif COLOR_CHANNEL == 2\r
\r
		float mask = texture2D(maskTexture, vUv).b;\r
\r
	#else\r
\r
		float mask = texture2D(maskTexture, vUv).a;\r
\r
	#endif\r
\r
	#if MASK_FUNCTION == 0\r
\r
		#ifdef INVERTED\r
\r
			// (mask > 0.0) ? 0.0 : 1.0;\r
			mask = step(mask, 0.0);\r
\r
		#else\r
\r
			// (mask > 0.0) ? 1.0 : 0.0;\r
			mask = 1.0 - step(mask, 0.0);\r
\r
		#endif\r
\r
	#else\r
\r
		mask = clamp(mask * strength, 0.0, 1.0);\r
\r
		#ifdef INVERTED\r
\r
			mask = 1.0 - mask;\r
\r
		#endif\r
\r
	#endif\r
\r
	#if MASK_FUNCTION == 3\r
\r
		// MULTIPLY_RGB\r
		vec4 texel = texture2D(inputBuffer, vUv);\r
		gl_FragColor = vec4(mask * texel.rgb, texel.a);\r
\r
	#elif MASK_FUNCTION == 2\r
\r
		// MULTIPLY_RGB_SET_ALPHA\r
		gl_FragColor = vec4(mask * texture2D(inputBuffer, vUv).rgb, mask);\r
\r
	#else\r
\r
		// MULTIPLY\r
		gl_FragColor = mask * texture2D(inputBuffer, vUv);\r
\r
	#endif\r
\r
}\r
`;
class _a extends N {
  /**
   * Constructs a new mask material.
   *
   * @param {Texture} [maskTexture] - The mask texture.
   */
  constructor(e = null) {
    super({
      name: "MaskMaterial",
      uniforms: {
        maskTexture: new a(e),
        inputBuffer: new a(null),
        strength: new a(1)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: Ja,
      vertexShader: Be
    }), this.colorChannel = mt.RED, this.maskFunction = Ni.DISCARD;
  }
  /**
   * The input buffer.
   *
   * @type {Texture}
   */
  set inputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * Sets the input buffer.
   *
   * @deprecated Use inputBuffer instead.
   * @param {Texture} value - The input buffer.
   */
  setInputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * The mask texture.
   *
   * @type {Texture}
   */
  set maskTexture(e) {
    this.uniforms.maskTexture.value = e, delete this.defines.MASK_PRECISION_HIGH, e.type !== se && (this.defines.MASK_PRECISION_HIGH = "1"), this.needsUpdate = !0;
  }
  /**
   * Sets the mask texture.
   *
   * @deprecated Use maskTexture instead.
   * @param {Texture} value - The texture.
   */
  setMaskTexture(e) {
    this.maskTexture = e;
  }
  /**
   * Sets the color channel to use for masking. Default is `ColorChannel.RED`.
   *
   * @type {ColorChannel}
   */
  set colorChannel(e) {
    this.defines.COLOR_CHANNEL = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Sets the color channel to use for masking. Default is `ColorChannel.RED`.
   *
   * @deprecated Use colorChannel instead.
   * @param {ColorChannel} value - The channel.
   */
  setColorChannel(e) {
    this.colorChannel = e;
  }
  /**
   * The masking technique. Default is `MaskFunction.DISCARD`.
   *
   * @type {MaskFunction}
   */
  set maskFunction(e) {
    this.defines.MASK_FUNCTION = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Sets the masking technique. Default is `MaskFunction.DISCARD`.
   *
   * @deprecated Use maskFunction instead.
   * @param {MaskFunction} value - The function.
   */
  setMaskFunction(e) {
    this.maskFunction = e;
  }
  /**
   * Indicates whether the masking is inverted.
   *
   * @type {Boolean}
   */
  get inverted() {
    return this.defines.INVERTED !== void 0;
  }
  set inverted(e) {
    this.inverted && !e ? delete this.defines.INVERTED : e && (this.defines.INVERTED = "1"), this.needsUpdate = !0;
  }
  /**
   * Indicates whether the masking is inverted.
   *
   * @deprecated Use inverted instead.
   * @return {Boolean} Whether the masking is inverted.
   */
  isInverted() {
    return this.inverted;
  }
  /**
   * Determines whether the masking should be inverted.
   *
   * @deprecated Use inverted instead.
   * @param {Boolean} value - Whether the masking should be inverted.
   */
  setInverted(e) {
    this.inverted = e;
  }
  /**
   * The current mask strength.
   *
   * Individual mask values will be clamped to [0.0, 1.0]. Has no effect when the mask function is set to `DISCARD`.
   *
   * @type {Number}
   */
  get strength() {
    return this.uniforms.strength.value;
  }
  set strength(e) {
    this.uniforms.strength.value = e;
  }
  /**
   * Returns the current mask strength.
   *
   * @deprecated Use strength instead.
   * @return {Number} The mask strength.
   */
  getStrength() {
    return this.strength;
  }
  /**
   * Sets the mask strength.
   *
   * Has no effect when the mask function is set to `DISCARD`.
   *
   * @deprecated Use strength instead.
   * @param {Number} value - The mask strength.
   */
  setStrength(e) {
    this.strength = e;
  }
}
const $a = `#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D nearColorBuffer;\r
	uniform mediump sampler2D farColorBuffer;\r
\r
#else\r
\r
	uniform lowp sampler2D nearColorBuffer;\r
	uniform lowp sampler2D farColorBuffer;\r
\r
#endif\r
\r
uniform lowp sampler2D nearCoCBuffer;\r
uniform lowp sampler2D farCoCBuffer;\r
uniform float scale;\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, const in float depth, out vec4 outputColor) {\r
\r
	vec4 colorNear = texture2D(nearColorBuffer, uv);\r
	vec4 colorFar = texture2D(farColorBuffer, uv);\r
\r
	#if MASK_FUNCTION == 1\r
\r
		// Use the CoC from the far color buffer's alpha channel.\r
		vec2 cocNearFar = vec2(texture2D(nearCoCBuffer, uv).r, colorFar.a);\r
		cocNearFar.x = min(cocNearFar.x * scale, 1.0);\r
\r
	#else\r
\r
		vec2 cocNearFar = vec2(texture2D(nearCoCBuffer, uv).r, texture2D(farCoCBuffer, uv).g);\r
		cocNearFar = min(cocNearFar * scale, 1.0);\r
\r
	#endif\r
\r
	// The far color buffer has been premultiplied with the CoC buffer.\r
	vec4 result = inputColor * (1.0 - cocNearFar.y) + colorFar;\r
	result = mix(result, colorNear, cocNearFar.x);\r
\r
	outputColor = result;\r
\r
}\r
`;
class Th extends I {
  /**
   * Constructs a new depth of field effect.
   *
   * @param {Camera} camera - The main camera.
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction] - The blend function of this effect.
   * @param {Number} [options.worldFocusDistance] - The focus distance in world units.
   * @param {Number} [options.worldFocusRange] - The focus distance in world units.
   * @param {Number} [options.focusDistance=0.0] - The normalized focus distance. Range is [0.0, 1.0].
   * @param {Number} [options.focusRange=0.1] - The focus range. Range is [0.0, 1.0].
   * @param {Number} [options.focalLength=0.1] - Deprecated.
   * @param {Number} [options.bokehScale=1.0] - The scale of the bokeh blur.
   * @param {Number} [options.resolutionScale=0.5] - The resolution scale.
   * @param {Number} [options.resolutionX=Resolution.AUTO_SIZE] - The horizontal resolution.
   * @param {Number} [options.resolutionY=Resolution.AUTO_SIZE] - The vertical resolution.
   * @param {Number} [options.width=Resolution.AUTO_SIZE] - Deprecated. Use resolutionX instead.
   * @param {Number} [options.height=Resolution.AUTO_SIZE] - Deprecated. Use resolutionY instead.
   * @param {Boolean} [options.useWorldSpaceAutoFocus=false] - Whether to use world space distance for auto focus instead of normalized depth.
   * @param {Boolean} [options.invertFocusDistance=false] - Whether to invert the focus distance calculation.
   */
  version = "0.01";
  constructor(e, {
    blendFunction: t,
    worldFocusDistance: r,
    worldFocusRange: n,
    focusDistance: i = 0,
    focalLength: s = 0.1,
    focusRange: o = s,
    bokehScale: l = 1,
    resolutionScale: u = 1,
    width: f = W.AUTO_SIZE,
    height: h = W.AUTO_SIZE,
    resolutionX: m = f,
    resolutionY: p = h,
    useWorldSpaceAutoFocus: d = !1,
    invertFocusDistance: v = !1
  } = {}) {
    super("DepthOfFieldEffect", $a, {
      blendFunction: t,
      attributes: Q.DEPTH,
      uniforms: /* @__PURE__ */ new Map([
        ["nearColorBuffer", new a(null)],
        ["farColorBuffer", new a(null)],
        ["nearCoCBuffer", new a(null)],
        ["farCoCBuffer", new a(null)],
        ["scale", new a(1)]
      ])
    }), this.camera = e, this.renderTarget = new A(1, 1, { depthBuffer: !1 }), this.renderTarget.texture.name = "DoF.Intermediate", this.renderTargetMasked = this.renderTarget.clone(), this.renderTargetMasked.texture.name = "DoF.Masked.Far", this.renderTargetNear = this.renderTarget.clone(), this.renderTargetNear.texture.name = "DoF.Bokeh.Near", this.uniforms.get("nearColorBuffer").value = this.renderTargetNear.texture, this.renderTargetFar = this.renderTarget.clone(), this.renderTargetFar.texture.name = "DoF.Bokeh.Far", this.uniforms.get("farColorBuffer").value = this.renderTargetFar.texture, this.renderTargetCoC = this.renderTarget.clone(), this.renderTargetCoC.texture.name = "DoF.CoC", this.uniforms.get("farCoCBuffer").value = this.renderTargetCoC.texture, this.renderTargetCoCBlurred = this.renderTargetCoC.clone(), this.renderTargetCoCBlurred.texture.name = "DoF.CoC.Blurred", this.uniforms.get("nearCoCBuffer").value = this.renderTargetCoCBlurred.texture, this.cocPass = new ee(new qa(e));
    const x = this.cocMaterial;
    x.focusDistance = i, x.focusRange = o, r !== void 0 && (x.worldFocusDistance = r), n !== void 0 && (x.worldFocusRange = n), this.blurPass = new at({ resolutionScale: u, resolutionX: m, resolutionY: p, kernelSize: Le.MEDIUM }), this.maskPass = new ee(new _a(this.renderTargetCoC.texture));
    const w = this.maskPass.fullscreenMaterial;
    w.colorChannel = mt.GREEN, this.maskFunction = Ni.MULTIPLY_RGB, this.bokehNearBasePass = new ee(new Xt(!1, !0)), this.bokehNearBasePass.fullscreenMaterial.cocBuffer = this.renderTargetCoCBlurred.texture, this.bokehNearFillPass = new ee(new Xt(!0, !0)), this.bokehNearFillPass.fullscreenMaterial.cocBuffer = this.renderTargetCoCBlurred.texture, this.bokehFarBasePass = new ee(new Xt(!1, !1)), this.bokehFarBasePass.fullscreenMaterial.cocBuffer = this.renderTargetCoC.texture, this.bokehFarFillPass = new ee(new Xt(!0, !1)), this.bokehFarFillPass.fullscreenMaterial.cocBuffer = this.renderTargetCoC.texture, this.target = null, this._useWorldSpaceAutoFocus = d, this._invertFocusDistance = v;
    const M = this.resolution = new W(this, m, p, u);
    M.addEventListener("change", (D) => this.setSize(M.baseWidth, M.baseHeight)), this.bokehScale = l;
  }
  set mainCamera(e) {
    this.camera = e, this.cocMaterial.copyCameraSettings(e);
  }
  /**
   * The circle of confusion texture.
   *
   * @type {Texture}
   */
  get cocTexture() {
    return this.renderTargetCoC.texture;
  }
  /**
   * The mask function. Default is `MULTIPLY_RGB`.
   *
   * @type {MaskFunction}
   */
  get maskFunction() {
    return this.maskPass.fullscreenMaterial.maskFunction;
  }
  set maskFunction(e) {
    this.maskFunction !== e && (this.defines.set("MASK_FUNCTION", e.toFixed(0)), this.maskPass.fullscreenMaterial.maskFunction = e, this.setChanged());
  }
  /**
   * The circle of confusion material.
   *
   * @type {CircleOfConfusionMaterial}
   */
  get cocMaterial() {
    return this.cocPass.fullscreenMaterial;
  }
  /**
   * The circle of confusion material.
   *
   * @deprecated Use cocMaterial instead.
   * @type {CircleOfConfusionMaterial}
   */
  get circleOfConfusionMaterial() {
    return this.cocMaterial;
  }
  /**
   * Returns the circle of confusion material.
   *
   * @deprecated Use cocMaterial instead.
   * @return {CircleOfConfusionMaterial} The material.
   */
  getCircleOfConfusionMaterial() {
    return this.cocMaterial;
  }
  /**
   * Returns the pass that blurs the foreground CoC buffer to soften edges.
   *
   * @deprecated Use blurPass instead.
   * @return {KawaseBlurPass} The blur pass.
   */
  getBlurPass() {
    return this.blurPass;
  }
  /**
   * Returns the resolution settings.
   *
   * @deprecated Use resolution instead.
   * @return {Resolution} The resolution.
   */
  getResolution() {
    return this.resolution;
  }
  /**
   * The current bokeh scale.
   *
   * @type {Number}
   */
  get bokehScale() {
    return this.uniforms.get("scale").value;
  }
  set bokehScale(e) {
    this.bokehNearBasePass.fullscreenMaterial.scale = e, this.bokehNearFillPass.fullscreenMaterial.scale = e, this.bokehFarBasePass.fullscreenMaterial.scale = e, this.bokehFarFillPass.fullscreenMaterial.scale = e, this.maskPass.fullscreenMaterial.strength = e, this.uniforms.get("scale").value = e;
  }
  /**
   * Returns the current bokeh scale.
   *
   * @deprecated Use bokehScale instead.
   * @return {Number} The scale.
   */
  getBokehScale() {
    return this.bokehScale;
  }
  /**
   * Sets the bokeh scale.
   *
   * @deprecated Use bokehScale instead.
   * @param {Number} value - The scale.
   */
  setBokehScale(e) {
    this.bokehScale = e;
  }
  /**
   * Returns the current auto focus target.
   *
   * @deprecated Use target instead.
   * @return {Vector3} The target.
   */
  getTarget() {
    return this.target;
  }
  /**
   * Sets the auto focus target.
   *
   * @deprecated Use target instead.
   * @param {Vector3} value - The target.
   */
  setTarget(e) {
    this.target = e;
  }
  /**
   * Calculates the focus distance from the camera to the given position.
   *
   * @param {Vector3} target - The target.
   * @return {Number} The normalized focus distance.
   */
  calculateFocusDistance(e) {
    const t = this.camera, r = t.position.distanceTo(e);
    return Qe(-r, t.near, t.far);
  }
  /**
   * Sets the depth texture.
   *
   * @param {Texture} depthTexture - A depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing.
   */
  setDepthTexture(e, t = $) {
    this.cocMaterial.depthBuffer = e, this.cocMaterial.depthPacking = t;
  }
  /**
   * Whether to use world space distance for auto focus.
   *
   * @type {Boolean}
   */
  get useWorldSpaceAutoFocus() {
    return this._useWorldSpaceAutoFocus;
  }
  set useWorldSpaceAutoFocus(e) {
    this._useWorldSpaceAutoFocus = e;
  }
  /**
   * Whether to invert the focus distance calculation.
   *
   * @type {Boolean}
   */
  get invertFocusDistance() {
    return this._invertFocusDistance;
  }
  set invertFocusDistance(e) {
    this._invertFocusDistance = e;
  }
  /**
   * Updates this effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   */
  update(e, t, r) {
    const n = this.renderTarget, i = this.renderTargetCoC, s = this.renderTargetCoCBlurred, o = this.renderTargetMasked;
    if (this.target !== null)
      if (this.useWorldSpaceAutoFocus) {
        const l = this.camera.position.distanceTo(this.target), u = this.camera;
        u.far / u.near, this.cocMaterial.worldFocusDistance = l;
      } else {
        let l = this.calculateFocusDistance(this.target);
        this.cocMaterial.focusDistance = l;
      }
    this.cocPass.render(e, null, i), this.blurPass.render(e, i, s), this.maskPass.render(e, t, o), this.bokehFarBasePass.render(e, o, n), this.bokehFarFillPass.render(e, n, this.renderTargetFar), this.bokehNearBasePass.render(e, t, n), this.bokehNearFillPass.render(e, n, this.renderTargetNear);
  }
  /**
   * Updates the size of internal render targets.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.resolution;
    r.setBaseSize(e, t);
    const n = r.width, i = r.height;
    this.cocPass.setSize(e, t), this.blurPass.setSize(e, t), this.maskPass.setSize(e, t), this.renderTargetFar.setSize(e, t), this.renderTargetCoC.setSize(e, t), this.renderTargetMasked.setSize(e, t), this.renderTarget.setSize(n, i), this.renderTargetNear.setSize(n, i), this.renderTargetCoCBlurred.setSize(n, i), this.bokehNearBasePass.fullscreenMaterial.setSize(e, t), this.bokehNearFillPass.fullscreenMaterial.setSize(e, t), this.bokehFarBasePass.fullscreenMaterial.setSize(e, t), this.bokehFarFillPass.fullscreenMaterial.setSize(e, t);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    this.cocPass.initialize(e, t, r), this.maskPass.initialize(e, t, r), this.bokehNearBasePass.initialize(e, t, r), this.bokehNearFillPass.initialize(e, t, r), this.bokehFarBasePass.initialize(e, t, r), this.bokehFarFillPass.initialize(e, t, r), this.blurPass.initialize(e, t, se), e.capabilities.logarithmicDepthBuffer && (this.cocPass.fullscreenMaterial.defines.LOG_DEPTH = "1"), r !== void 0 && (this.renderTarget.texture.type = r, this.renderTargetNear.texture.type = r, this.renderTargetFar.texture.type = r, this.renderTargetMasked.texture.type = r, e !== null && e.outputColorSpace === V && (this.renderTarget.texture.colorSpace = V, this.renderTargetNear.texture.colorSpace = V, this.renderTargetFar.texture.colorSpace = V, this.renderTargetMasked.texture.colorSpace = V));
  }
}
const eo = `uniform vec2 angle;\r
uniform float scale;\r
\r
float pattern(const in vec2 uv) {\r
\r
	vec2 point = scale * vec2(\r
		dot(angle.yx, vec2(uv.x, -uv.y)),\r
		dot(angle, uv)\r
	);\r
\r
	return (sin(point.x) * sin(point.y)) * 4.0;\r
\r
}\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	vec3 color = vec3(inputColor.rgb * 10.0 - 5.0 + pattern(uv * resolution));\r
	outputColor = vec4(color, inputColor.a);\r
\r
}\r
`;
class bh extends I {
  /**
   * Constructs a new dot screen effect.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction] - The blend function of this effect.
   * @param {Number} [options.angle=1.57] - The angle of the dot pattern.
   * @param {Number} [options.scale=1.0] - The scale of the dot pattern.
   */
  constructor({ blendFunction: e, angle: t = Math.PI * 0.5, scale: r = 1 } = {}) {
    super("DotScreenEffect", eo, {
      blendFunction: e,
      uniforms: /* @__PURE__ */ new Map([
        ["angle", new a(new g())],
        ["scale", new a(r)]
      ])
    }), this.angle = t;
  }
  /**
   * The angle.
   *
   * @type {Number}
   */
  get angle() {
    return Math.acos(this.uniforms.get("angle").value.y);
  }
  set angle(e) {
    this.uniforms.get("angle").value.set(Math.sin(e), Math.cos(e));
  }
  /**
   * Returns the pattern angle.
   *
   * @deprecated Use angle instead.
   * @return {Number} The angle in radians.
   */
  getAngle() {
    return this.angle;
  }
  /**
   * Sets the pattern angle.
   *
   * @deprecated Use angle instead.
   * @param {Number} value - The angle in radians.
   */
  setAngle(e) {
    this.angle = e;
  }
  /**
   * The scale.
   *
   * @type {Number}
   */
  get scale() {
    return this.uniforms.get("scale").value;
  }
  set scale(e) {
    this.uniforms.get("scale").value = e;
  }
}
const to = `// Frozen wasteland
// By Dave Hoskins
// https://www.shadertoy.com/view/Xls3D2
// License Creative Commons Attribution-NonCommercial-ShareAlike 3.0 Unported License.
// 修改版：只保留天空和雾气效果，并支持深度测试

// 统一变量声明
uniform float time;       // 替代 iTime
uniform vec2 resolution;  // 替代 iResolution
uniform vec2 mouse;       // 替代 iMouse

// 相机参数 - 从Three.js传入
uniform vec3 cameraPosition;
uniform vec3 cameraForward;
uniform vec3 cameraUp;
uniform vec3 cameraRight;
uniform float cameraFov;

// 深度纹理，用于深度测试
uniform float cameraNear;
uniform float cameraFar;

// 效果参数
uniform float fogDensity; // 雾气浓度控制

#define ITR 40
#define FAR 110.
#define MOD3 vec3(.16532,.17369,.15787)
#define SUN_COLOUR  vec3(1., .95, .85)

#define TRIANGLE_NOISE	    // .. This
//#define TEXTURE_NOISE		// .. Or this (faster, but not as sharp edged)
//#define VALUE_NOISE 		// .. or more normal noise.
//#define FOUR_D_NOISE	    // ...Or movement

#define MOD2 vec2(.16632,.17369)

float tri(in float x){return abs(fract(x)-.5);}

float hash12(vec2 p)
{
	p  = fract(p * MOD2);
    p += dot(p.xy, p.yx+19.19);
    return fract(p.x * p.y);
}

//========================================================================
// ################ DIFFERENT NOISE FUNCTIONS ################
#ifdef TRIANGLE_NOISE
vec3 tri3(in vec3 p){return vec3( tri(p.z+tri(p.y)), tri(p.z+tri(p.x)), tri(p.y+tri(p.x)));}
float Noise3d(in vec3 p)
{
    float z=1.4;
	float rz = 0.;
    vec3 bp = p;
	for (float i=0.; i<= 2.; i++ )
	{
        vec3 dg = tri3(bp);
        p += (dg);

        bp *= 2.;
		z *= 1.5;
		p *= 1.3;
        
        rz+= (tri(p.z+tri(p.x+tri(p.y))))/z;
        bp += 0.14;
	}
	return rz;
}
#endif

//--------------------------------------------------------------------------------
#ifdef FOUR_D_NOISE
vec4 quad(in vec4 p){return abs(fract(p.yzwx+p.wzxy)-.5);}

float Noise3d(in vec3 q)
{
    float z=1.4;
    vec4 p = vec4(q, time*.1);
	float rz = 0.;
    vec4 bp = p;
	for (float i=0.; i<= 2.; i++ )
	{
        vec4 dg = quad(bp);
        p += (dg);

		z *= 1.5;
		p *= 1.3;
        
        rz+= (tri(p.z+tri(p.w+tri(p.y+tri(p.x)))))/z;
        
        bp = bp.yxzw*2.0+.14;
	}
	return rz;
}
#endif

//--------------------------------------------------------------------------------
#ifdef TEXTURE_NOISE
float Noise3d(in vec3 x)
{
    x*=10.0;
    float h = 0.0;
    float a = .28;
    for (int i = 0; i < 4; i++)
    {
        vec3 p = floor(x);
        vec3 f = fract(x);
        f = f*f*(3.0-2.0*f);

        vec2 uv = (p.xy+vec2(37.0,17.0)*p.z) + f.xy;
        vec2 rg = textureLod( iChannel0, (uv+ 0.5)/256.0, 0.0 ).yx;
        h += mix( rg.x, rg.y, f.z )*a;
        a*=.5;
        x+=x;
    }
    return h;
}
#endif

//--------------------------------------------------------------------------------
#ifdef VALUE_NOISE
float Hash(vec3 p)
{
	p  = fract(p * MOD3);
    p += dot(p.xyz, p.yzx + 19.19);
    return fract(p.x * p.y * p.z);
}

float Noise3d(in vec3 p)
{
    vec2 add = vec2(1.0, 0.0);
	p *= 10.0;
    float h = 0.0;
    float a = .3;
    for (int n = 0; n < 4; n++)
    {
        vec3 i = floor(p);
        vec3 f = fract(p); 
        f *= f * (3.0-2.0*f);

        h += mix(
            mix(mix(Hash(i), Hash(i + add.xyy),f.x),
                mix(Hash(i + add.yxy), Hash(i + add.xxy),f.x),
                f.y),
            mix(mix(Hash(i + add.yyx), Hash(i + add.xyx),f.x),
                mix(Hash(i + add.yxx), Hash(i + add.xxx),f.x),
                f.y),
            f.z)*a;
         a*=.5;
        p += p;
    }
    return h;
}
#endif

// 深度处理函数
float getLinearDepth(sampler2D depthBuffer, vec2 uv) {
    float depth = texture2D(depthBuffer, uv).r;
    
    // 转换为线性深度 (0-1)
    float linearDepth = 2.0 * cameraNear * cameraFar / (cameraFar + cameraNear - (2.0 * depth - 1.0) * (cameraFar - cameraNear));
    return linearDepth / cameraFar; // 归一化到0-1
}

// 雾气效果
float fogmap(in vec3 p)
{
    p.xz -= time*7.+sin(p.z*.3)*3.;
    p.y -= time*.5;
    return (max(Noise3d(p*.008+.1)-.1,0.0)*Noise3d(p*.1))*.3;
}

// 雾气颜色处理
vec3 fogColour(in vec3 col, float t)
{
    vec3 ext = exp2(-t*0.0001*vec3(1.,1.5,3.)); 
    return col*ext + (1.0-ext)*vec3(1.);
}

vec3 Clouds(vec3 sky, vec3 rd)
{
    rd.y = max(rd.y, 0.0);
    float ele = rd.y;
    float v = (200.0)/(abs(rd.y)+.01);

    rd.y = v;
    rd.xz = rd.xz * v - time*8.0;
	rd.xz *= .0004;
    
	float f = Noise3d(rd.xzz*3.) * Noise3d(rd.zxx*1.3)*2.5;
    f = f*pow(ele, .5)*2.;
  	f = clamp(f-.15, 0.01, 1.0);

    return mix(sky, vec3(1), f);
}

vec3 Sky(vec3 rd, vec3 ligt)
{
    rd.y = max(rd.y, 0.0);
    
    vec3 sky = mix(vec3(.1, .15, .25), vec3(.8), pow(.8-rd.y, 3.0));
    return mix(sky, SUN_COLOUR, min(pow(max(dot(rd, ligt), 0.0), 4.5)*1.2, 1.0));
}

// 收集沿射线方向的雾气
float collectFog(in vec3 ro, in vec3 rd, in float maxDist)
{
    float totalFog = 0.0;
    float stepSize = maxDist / float(ITR);
    
    for (int i = 0; i < ITR; i++)
    {
        float t = float(i) * stepSize;
        vec3 pos = ro + rd * t;
        totalFog += fogmap(pos);
    }
    
    // 应用雾气浓度控制
    return min(totalFog * 0.05 * fogDensity, 1.0);
}

void mainImage(out vec4 fragColor, in vec2 fragCoord)
{	
	vec2 p = fragCoord.xy/resolution.xy-0.5;
    vec2 q = fragCoord.xy/resolution.xy;
	p.x*=resolution.x/resolution.y;
   
    
    // 深度测试：只在远处（接近背景）渲染效果
    // 当depth > 0.99时，认为是远景（没有前景物体）
  
    
    // 使用传入的相机参数
    vec3 ro = cameraPosition;
    vec3 rd = normalize((p.x*cameraRight + p.y*cameraUp) + cameraForward);
 
    vec3 ligt = normalize(vec3(1.5, .9, -.5));
	vec3 sky = Sky(rd, ligt);
    
    // 渲染天空和云
    vec3 col = sky;
    col = Clouds(col, rd);
    
    // 计算雾气
    float fg = collectFog(ro, rd, FAR);
    
    // 混合雾气 - 雾气密度受fogDensity影响
    col = mix(col, vec3(0.6, .65, .7), fg);
  
    // 后期处理
    col = fogColour(col, FAR);
    col = col*col * (3.0 - 2. * col);
	col = sqrt(col);
    
    // 边缘效果
    float f = smoothstep(0.0, 3.0, time)*.5;
    col *= f+f*pow(70. *q.x*q.y*(1.0-q.x)*(1.0-q.y), .2);
   
	fragColor = vec4(col, 1.0);
}

// 将mainImage函数转换为适应postprocessing框架的mainImage函数
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
    vec2 fragCoord = uv * resolution.xy;
    vec4 skyAndFogColor;
    mainImage(skyAndFogColor, fragCoord);

    // 读取深度值
    float depth = texture2D(depthBuffer, uv).r;
    
    // 将深度值转换为线性深度 (0-1)
    float linearDepth = 2.0 * cameraNear * cameraFar / (cameraFar + cameraNear - (2.0 * depth - 1.0) * (cameraFar - cameraNear));
    linearDepth = linearDepth / cameraFar; // 归一化到0-1
    
    // 基于深度的雾气混合因子
    // 1. 远处(深度接近1)：使用完整的天空和雾气效果
    // 2. 近处：根据距离逐渐应用雾气效果
    float skyMask = step(0.9999, depth); // 纯天空部分
    
    // 基于线性深度的雾气混合因子
    float fogFactor = clamp(linearDepth * fogDensity * 2.0, 0.0, 1.0);
    
    // 为近处的物体应用雾气效果，为远处应用完整的天空+雾气效果
    vec4 foggedColor = mix(inputColor, vec4(skyAndFogColor.rgb, 1.0), fogFactor);
    outputColor = mix(foggedColor, skyAndFogColor, skyMask);
} `;
class Dh extends I {
  /**
      * 构造函数
      *
      * @param {Object} [options] - 配置选项
      * @param {BlendFunction} [options.blendFunction=BlendFunction.NORMAL] - 混合模式
      * @param {Number} [options.speed=1.0] - 动画速度
      * @param {Number} [options.fogDensity=1.0] - 雾气浓度
      */
  constructor({
    blendFunction: e = E.NORMAL,
    composer: t,
    speed: r = 1,
    fogDensity: n = 1
  } = {}) {
    super("FrozenWastelandEffect", to, {
      blendFunction: e,
      attributes: Q.DEPTH,
      defines: /* @__PURE__ */ new Map([
        ["USE_DEPTH", "1"]
      ]),
      uniforms: /* @__PURE__ */ new Map([
        ["time", new a(0)],
        ["resolution", new a(new g(1, 1))],
        ["mouse", new a(new g(0.5, 0.5))],
        ["cameraPosition", new a(new T(0, 0, 0))],
        ["cameraForward", new a(new T(0, 0, -1))],
        ["cameraUp", new a(new T(0, 1, 0))],
        ["cameraRight", new a(new T(1, 0, 0))],
        ["cameraFov", new a(60)],
        ["cameraNear", new a(0.1)],
        ["cameraFar", new a(1e3)],
        ["fogDensity", new a(n)]
      ])
    }), this._speed = r, this._fogDensity = n, this._mouse = this.uniforms.get("mouse").value, this._camera = null, this.composer = t;
  }
  /**
      * 动画速度 - 控制云和雾气的移动速度
      * @type {Number}
      */
  get speed() {
    return this._speed;
  }
  set speed(e) {
    this._speed = e;
  }
  /**
      * 雾气浓度 - 控制雾气的浓度
      * @type {Number}
      */
  get fogDensity() {
    return this._fogDensity;
  }
  set fogDensity(e) {
    this._fogDensity = e, this.uniforms.get("fogDensity").value = e;
  }
  /**
      * 设置相机
      * @param {Camera} camera - Three.js相机对象
      */
  setCamera(e) {
    this._camera = e, e && (this.uniforms.get("cameraNear").value = e.near, this.uniforms.get("cameraFar").value = e.far);
  }
  /**
      * 设置鼠标位置
      * @param {Number} x - X坐标 (0-1)
      * @param {Number} y - Y坐标 (0-1)
      */
  setMousePosition(e, t) {
    this._mouse.set(e, t);
  }
  /**
      * 更新相机参数
      * @private
      */
  _updateCameraParameters() {
    if (!this._camera)
      return;
    this.uniforms.get("cameraPosition").value.copy(this._camera.position);
    const e = new T(0, 0, -1);
    e.applyQuaternion(this._camera.quaternion), this.uniforms.get("cameraForward").value.copy(e);
    const t = new T(0, 1, 0);
    t.applyQuaternion(this._camera.quaternion), this.uniforms.get("cameraUp").value.copy(t);
    const r = new T(1, 0, 0);
    r.applyQuaternion(this._camera.quaternion), this.uniforms.get("cameraRight").value.copy(r), this.uniforms.get("cameraFov").value = this._camera.fov, this.uniforms.get("cameraNear").value = this._camera.near, this.uniforms.get("cameraFar").value = this._camera.far;
  }
  /**
      * 更新效果
      * @param {WebGLRenderer} renderer - WebGL渲染器
      * @param {WebGLRenderTarget} inputBuffer - 输入缓冲
      * @param {Number} deltaTime - 时间增量
      */
  update(e, t, r) {
    this.uniforms.get("time").value += r * this._speed, this.uniforms.get("resolution").value.set(
      t.width,
      t.height
    ), this._updateCameraParameters();
  }
  /**
      * 更新大小
      * @param {Number} width - 宽度
      * @param {Number} height - 高度
      */
  setSize(e, t) {
    this.uniforms.get("resolution").value.set(e, t);
  }
  /**
      * 设置主相机
      * @param {Camera} value - 主相机
      * @override
      */
  set mainCamera(e) {
    this.setCamera(e);
  }
}
const ro = `// Foggy Mountains 2
// 基于Shadertoy效果: https://www.shadertoy.com/view/MdsGzS
// 修改版：移除山脉，仅保留天空和雾气，添加相机矩阵支持

// 统一变量声明
uniform float time;       // 替代 iTime
uniform vec2 resolution;  // 替代 iResolution
uniform vec2 mouse;       // 替代 iMouse

// 相机参数 - 从Three.js传入
uniform vec3 cameraPosition;
uniform mat4 viewMatrix;  // 添加视图矩阵支持
uniform float cameraFov;
uniform float cameraNear;
uniform float cameraFar;

// 效果参数
uniform float fogDensity; // 雾气浓度控制

// 噪声纹理
uniform sampler2D noiseTexture; // 替代 iChannel0

// 辅助函数
vec3 rotate(vec3 r, float v){ 
    return vec3(r.x*cos(v)+r.z*sin(v), r.y, r.z*cos(v)-r.x*sin(v));
}

float noise(in vec3 x) {
    float z = x.z*64.0;
    vec2 offz = vec2(0.317,0.123);
    vec2 uv1 = x.xy + offz*floor(z); 
    vec2 uv2 = uv1 + offz;
    
    // 使用纹理采样
    float n1 = texture2D(noiseTexture, uv1).x;
    float n2 = texture2D(noiseTexture, uv2).x;
    
    return mix(n1, n2, fract(z)) - 0.5;
}

float noises(in vec3 p) {
    float a = 0.0;
    for(float i=1.0; i<6.0; i++) {
        a += noise(p)/i;
        p = p*2.0 + vec3(0.0, a*0.001/i, a*0.0001/i);
    }
    return a;
}

float clouds(in vec3 p) {
    float height = 500.0;
    p.y += height;
    return noises(vec3(p.x*0.3+((time+mouse.y)*30.0), p.y, p.z)*0.00002) - max(p.y, 0.0)*0.00009;
}

// 简化版本的mainImage函数，移除山脉部分，只保留天空和雾
void mainImage(out vec4 fragColor, in vec2 fragCoord) {
    float localTime = time*5.0 + floor(time*0.1)*150.0;
    
    // 计算观察方向向量，使用视图矩阵
    float fovFactor = tan(radians(cameraFov * 0.5));
    vec2 screenPos = (fragCoord / resolution) * 2.0 - 1.0;
    vec3 viewDir = normalize(vec3(screenPos.x * resolution.x / resolution.y * fovFactor, 
                             screenPos.y * fovFactor, -1.0));
    
    // 将视图方向转换到世界空间
    vec4 worldDir = viewMatrix * vec4(viewDir, 0.0);
    vec3 ray = normalize(worldDir.xyz);
    
    // 设置相机位置和太阳位置
    vec3 campos = cameraPosition + vec3(0.0, 0.0, localTime*8.0);
    vec3 sun = normalize(vec3(0.0, 0.6, -0.4));
    
    // 雾气和颜色计算
    float fog = 0.0;
    vec3 pos = campos;
    
    // 简化的雾气计算，不再进行地形碰撞检测
    for(float i=1.0; i<50.0; i++) {
        float step = i*i*0.5;
        vec3 p = pos + ray * step;
        fog += clouds(p) * 0.5;
    }
    
    float l = sin(dot(ray, sun));
    vec3 light = vec3(l, 0.0, -l) + ray.y*0.2;
    
    // 简化的颜色计算
    vec3 cloudColor = vec3(0.70, 0.72, 0.70) + light*0.05 + sin(fog*0.0002)*0.2;
    vec3 skyColor = cloudColor + ray.y*0.1 - 0.02;
    
    // 应用雾气浓度
    float f = smoothstep(0.0, 800.0, fog * fogDensity);
    
    // 最终颜色混合
    vec3 finalColor = mix(skyColor, cloudColor, f);
    
    // 添加边缘暗角效果
    vec2 uv = fragCoord / resolution;
    finalColor = sqrt(smoothstep(0.2, 1.0, finalColor - dot(uv*2.0-1.0, uv*2.0-1.0)*0.1));
    
    fragColor = vec4(finalColor, 1.0);
}

// 将mainImage函数转换为适应postprocessing框架的mainImage函数
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
    vec2 fragCoord = uv * resolution.xy;
    vec4 foggyMountainsColor;
    mainImage(foggyMountainsColor, fragCoord);

    // 读取深度值
    float depth = texture2D(depthBuffer, uv).r;
    
    // 将深度值转换为线性深度 (0-1)
    float linearDepth = 2.0 * cameraNear * cameraFar / (cameraFar + cameraNear - (2.0 * depth - 1.0) * (cameraFar - cameraNear));
    linearDepth = linearDepth / cameraFar; // 归一化到0-1
    
    // 基于深度的混合策略:
    // 1. 远处(深度接近1)：使用完整的天空和雾气效果
    // 2. 近处：根据距离逐渐应用雾气效果
    float skyMask = step(0.9999, depth); // 纯天空部分（最远处）
    
    // 基于线性深度的雾气混合因子
    float fogFactor = clamp(linearDepth * fogDensity * 2.0, 0.0, 1.0);
    
    // 两步混合:
    // 1. 先将输入颜色与雾气颜色基于深度混合，得到带雾的前景
    vec4 foggedColor = mix(inputColor, vec4(foggyMountainsColor.rgb, 1.0), fogFactor);
    
    // 2. 在天空区域（最远处）使用完整的天空+雾气效果
    outputColor = mix(foggedColor, foggyMountainsColor, skyMask);
} `;
class Ph extends I {
  /**
      * 构造函数
      *
      * @param {Object} [options] - 配置选项
      * @param {BlendFunction} [options.blendFunction=BlendFunction.NORMAL] - 混合模式
      * @param {Number} [options.speed=1.0] - 动画速度
      * @param {Number} [options.fogDensity=1.0] - 雾气浓度
      * @param {String} [options.noiseTexturePath="img/textures/noise/blue-noise.png"] - 噪声纹理路径
      */
  constructor({
    blendFunction: e = E.NORMAL,
    composer: t,
    speed: r = 1,
    fogDensity: n = 1,
    noiseTexturePath: i = "img/textures/noise/blue-noise.png"
  } = {}) {
    const o = new Ie().load(i);
    o.wrapS = G, o.wrapT = G, super("FoggyMountainsEffect", ro, {
      blendFunction: e,
      attributes: Q.DEPTH,
      defines: /* @__PURE__ */ new Map([
        ["USE_DEPTH", "1"]
      ]),
      uniforms: /* @__PURE__ */ new Map([
        ["time", new a(0)],
        ["resolution", new a(new g(1, 1))],
        ["mouse", new a(new g(0.5, 0.5))],
        ["cameraPosition", new a(new T(0, 0, 0))],
        ["viewMatrix", new a(new C())],
        ["cameraFov", new a(60)],
        ["cameraNear", new a(0.1)],
        ["cameraFar", new a(1e3)],
        ["fogDensity", new a(n)],
        ["noiseTexture", new a(o)]
      ])
    }), this._speed = r, this._fogDensity = n, this._mouse = this.uniforms.get("mouse").value, this._camera = null, this.composer = t;
  }
  /**
      * 动画速度 - 控制云和雾气的移动速度
      * @type {Number}
      */
  get speed() {
    return this._speed;
  }
  set speed(e) {
    this._speed = e;
  }
  /**
      * 雾气浓度 - 控制雾气的浓度
      * @type {Number}
      */
  get fogDensity() {
    return this._fogDensity;
  }
  set fogDensity(e) {
    this._fogDensity = e, this.uniforms.get("fogDensity").value = e;
  }
  /**
      * 设置相机
      * @param {Camera} camera - Three.js相机对象
      */
  setCamera(e) {
    this._camera = e, e && (this.uniforms.get("cameraNear").value = e.near, this.uniforms.get("cameraFar").value = e.far);
  }
  /**
      * 设置鼠标位置
      * @param {Number} x - X坐标 (0-1)
      * @param {Number} y - Y坐标 (0-1)
      */
  setMousePosition(e, t) {
    this._mouse.set(e, t);
  }
  /**
      * 更新相机参数
      * @private
      */
  _updateCameraParameters() {
    if (!this._camera)
      return;
    this.uniforms.get("cameraPosition").value.copy(this._camera.position), this.uniforms.get("viewMatrix").value.copy(this._camera.matrixWorld).invert(), this.uniforms.get("cameraFov").value = this._camera.fov, this.uniforms.get("cameraNear").value = this._camera.near, this.uniforms.get("cameraFar").value = this._camera.far;
  }
  /**
      * 更新效果
      * @param {WebGLRenderer} renderer - WebGL渲染器
      * @param {WebGLRenderTarget} inputBuffer - 输入缓冲
      * @param {Number} deltaTime - 时间增量
      */
  update(e, t, r) {
    this.uniforms.get("time").value += r * this._speed, this.uniforms.get("resolution").value.set(
      t.width,
      t.height
    ), this._updateCameraParameters();
  }
  /**
      * 更新大小
      * @param {Number} width - 宽度
      * @param {Number} height - 高度
      */
  setSize(e, t) {
    this.uniforms.get("resolution").value.set(e, t);
  }
  /**
      * 设置主相机
      * @param {Camera} value - 主相机
      * @override
      */
  set mainCamera(e) {
    this.setCamera(e);
  }
}
const no = `/**\r
 * FXAA 3.11 by Timothy Lottes\r
 *\r
 * Copyright (c) 2011, NVIDIA CORPORATION. All rights reserved.\r
 *\r
 * TO THE MAXIMUM EXTENT PERMITTED BY APPLICABLE LAW, THIS SOFTWARE IS PROVIDED "AS IS" AND NVIDIA AND ITS SUPPLIERS\r
 * DISCLAIM ALL WARRANTIES, EITHER EXPRESS OR IMPLIED, INCLUDING, BUT NOT LIMITED TO, IMPLIED WARRANTIES OF\r
 * MERCHANTABILITY AND FITNESS FOR A PARTICULAR PURPOSE. IN NO EVENT SHALL NVIDIA OR ITS SUPPLIERS BE LIABLE FOR ANY\r
 * SPECIAL, INCIDENTAL, INDIRECT, OR CONSEQUENTIAL DAMAGES WHATSOEVER (INCLUDING, WITHOUT LIMITATION, DAMAGES FOR LOSS\r
 * OF BUSINESS PROFITS, BUSINESS INTERRUPTION, LOSS OF BUSINESS INFORMATION, OR ANY OTHER PECUNIARY LOSS) ARISING OUT OF\r
 * THE USE OF OR INABILITY TO USE THIS SOFTWARE, EVEN IF NVIDIA HAS BEEN ADVISED OF THE POSSIBILITY OF SUCH DAMAGES.\r
 */\r
\r
#define QUALITY(q) ((q) < 5 ? 1.0 : ((q) > 5 ? ((q) < 10 ? 2.0 : ((q) < 11 ? 4.0 : 8.0)) : 1.5))\r
#define ONE_OVER_TWELVE 0.08333333333333333\r
\r
varying vec2 vUvDown;\r
varying vec2 vUvUp;\r
varying vec2 vUvLeft;\r
varying vec2 vUvRight;\r
\r
varying vec2 vUvDownLeft;\r
varying vec2 vUvUpRight;\r
varying vec2 vUvUpLeft;\r
varying vec2 vUvDownRight;\r
\r
vec4 fxaa(const in vec4 inputColor, const in vec2 uv) {\r
\r
	// Luma at the current fragment.\r
	float lumaCenter = luminance(inputColor.rgb);\r
\r
	// Luma at the four direct neighbours of the current fragment.\r
	float lumaDown = luminance(texture2D(inputBuffer, vUvDown).rgb);\r
	float lumaUp = luminance(texture2D(inputBuffer, vUvUp).rgb);\r
	float lumaLeft = luminance(texture2D(inputBuffer, vUvLeft).rgb);\r
	float lumaRight = luminance(texture2D(inputBuffer, vUvRight).rgb);\r
\r
	// Find the maximum and minimum luma around the current fragment.\r
	float lumaMin = min(lumaCenter, min(min(lumaDown, lumaUp), min(lumaLeft, lumaRight)));\r
	float lumaMax = max(lumaCenter, max(max(lumaDown, lumaUp), max(lumaLeft, lumaRight)));\r
\r
	// Compute the delta.\r
	float lumaRange = lumaMax - lumaMin;\r
\r
	// Skip AA if the luma variation is lower than a threshold (low contrast or dark area).\r
	if(lumaRange < max(EDGE_THRESHOLD_MIN, lumaMax * EDGE_THRESHOLD_MAX)) {\r
\r
		return inputColor;\r
\r
	}\r
\r
	// Query the 4 remaining corners lumas.\r
	float lumaDownLeft = luminance(texture2D(inputBuffer, vUvDownLeft).rgb);\r
	float lumaUpRight = luminance(texture2D(inputBuffer, vUvUpRight).rgb);\r
	float lumaUpLeft = luminance(texture2D(inputBuffer, vUvUpLeft).rgb);\r
	float lumaDownRight = luminance(texture2D(inputBuffer, vUvDownRight).rgb);\r
\r
	// Combine the four edges lumas (using intermediary variables for future computations with the same values).\r
	float lumaDownUp = lumaDown + lumaUp;\r
	float lumaLeftRight = lumaLeft + lumaRight;\r
\r
	// Same for corners.\r
	float lumaLeftCorners = lumaDownLeft + lumaUpLeft;\r
	float lumaDownCorners = lumaDownLeft + lumaDownRight;\r
	float lumaRightCorners = lumaDownRight + lumaUpRight;\r
	float lumaUpCorners = lumaUpRight + lumaUpLeft;\r
\r
	// Compute an estimation of the gradient along the horizontal and vertical axis.\r
	float edgeHorizontal = (\r
		abs(-2.0 * lumaLeft + lumaLeftCorners) +\r
		abs(-2.0 * lumaCenter + lumaDownUp ) * 2.0 +\r
		abs(-2.0 * lumaRight + lumaRightCorners)\r
	);\r
\r
	float edgeVertical = (\r
		abs(-2.0 * lumaUp + lumaUpCorners) +\r
		abs(-2.0 * lumaCenter + lumaLeftRight) * 2.0 +\r
		abs(-2.0 * lumaDown + lumaDownCorners)\r
	);\r
\r
	// Check if the local edge is horizontal or vertical.\r
	bool isHorizontal = (edgeHorizontal >= edgeVertical);\r
\r
	// Choose the step size (one pixel) accordingly.\r
	float stepLength = isHorizontal ? texelSize.y : texelSize.x;\r
\r
	// Select the two neighboring texels' lumas in the opposite direction to the local edge.\r
	float luma1 = isHorizontal ? lumaDown : lumaLeft;\r
	float luma2 = isHorizontal ? lumaUp : lumaRight;\r
\r
	// Compute gradients in this direction.\r
	float gradient1 = abs(luma1 - lumaCenter);\r
	float gradient2 = abs(luma2 - lumaCenter);\r
\r
	// Check which direction is the steepest.\r
	bool is1Steepest = gradient1 >= gradient2;\r
\r
	// Gradient in the corresponding direction, normalized.\r
	float gradientScaled = 0.25 * max(gradient1, gradient2);\r
\r
	// Average luma in the correct direction.\r
	float lumaLocalAverage = 0.0;\r
\r
	if(is1Steepest) {\r
\r
		// Switch the direction.\r
		stepLength = -stepLength;\r
		lumaLocalAverage = 0.5 * (luma1 + lumaCenter);\r
\r
	} else {\r
\r
		lumaLocalAverage = 0.5 * (luma2 + lumaCenter);\r
\r
	}\r
\r
	// Shift UV in the correct direction by half a pixel.\r
	vec2 currentUv = uv;\r
\r
	if(isHorizontal) {\r
\r
		currentUv.y += stepLength * 0.5;\r
\r
	} else {\r
\r
		currentUv.x += stepLength * 0.5;\r
\r
	}\r
\r
	// Compute offset (for each iteration step) in the right direction.\r
	vec2 offset = isHorizontal ? vec2(texelSize.x, 0.0) : vec2(0.0, texelSize.y);\r
\r
	// Compute UVs to explore on each side of the edge, orthogonally. The QUALITY allows us to step faster.\r
	vec2 uv1 = currentUv - offset * QUALITY(0);\r
	vec2 uv2 = currentUv + offset * QUALITY(0);\r
\r
	// Read lumas at both extremities of the exploration segment, and compute the delta w.r.t. the local average luma.\r
	float lumaEnd1 = luminance(texture2D(inputBuffer, uv1).rgb);\r
	float lumaEnd2 = luminance(texture2D(inputBuffer, uv2).rgb);\r
	lumaEnd1 -= lumaLocalAverage;\r
	lumaEnd2 -= lumaLocalAverage;\r
\r
	// If the deltas at the current extremities are larger than the local gradient, the side of the edge has been reached.\r
	bool reached1 = abs(lumaEnd1) >= gradientScaled;\r
	bool reached2 = abs(lumaEnd2) >= gradientScaled;\r
	bool reachedBoth = reached1 && reached2;\r
\r
	// If the side has not been reached, continue to explore in this direction.\r
	if(!reached1) {\r
\r
		uv1 -= offset * QUALITY(1);\r
\r
	}\r
\r
	if(!reached2) {\r
\r
		uv2 += offset * QUALITY(1);\r
\r
	}\r
\r
	// If both sides have not been reached, continue to explore.\r
	if(!reachedBoth) {\r
\r
		for(int i = 2; i < SAMPLES; ++i) {\r
\r
			// If needed, read luma in 1st direction, compute delta.\r
			if(!reached1) {\r
\r
				lumaEnd1 = luminance(texture2D(inputBuffer, uv1).rgb);\r
				lumaEnd1 = lumaEnd1 - lumaLocalAverage;\r
\r
			}\r
\r
			// If needed, read luma in opposite direction, compute delta.\r
			if(!reached2) {\r
\r
				lumaEnd2 = luminance(texture2D(inputBuffer, uv2).rgb);\r
				lumaEnd2 = lumaEnd2 - lumaLocalAverage;\r
\r
			}\r
\r
			// If the deltas are larger than the local gradient, the side of the edge has been reached.\r
			reached1 = abs(lumaEnd1) >= gradientScaled;\r
			reached2 = abs(lumaEnd2) >= gradientScaled;\r
			reachedBoth = reached1 && reached2;\r
			\r
			// If the side has not been reached, continue to explore in this direction, with dynamic quality.\r
			if(!reached1) {\r
\r
				uv1 -= offset * QUALITY(i);\r
\r
			}\r
\r
			if(!reached2) {\r
\r
				uv2 += offset * QUALITY(i);\r
\r
			}\r
\r
			// If both sides have been reached, stop the exploration.\r
			if(reachedBoth) {\r
\r
				break;\r
\r
			}\r
\r
		}\r
\r
	}\r
\r
	// Compute the distances to each side edge of the edge (!).\r
	float distance1 = isHorizontal ? (uv.x - uv1.x) : (uv.y - uv1.y);\r
	float distance2 = isHorizontal ? (uv2.x - uv.x) : (uv2.y - uv.y);\r
\r
	// Check in which direction the side of the edge is closer.\r
	bool isDirection1 = distance1 < distance2;\r
	float distanceFinal = min(distance1, distance2);\r
\r
	// Thickness of the edge.\r
	float edgeThickness = (distance1 + distance2);\r
\r
	// Check if the luma at the center is smaller than the local average.\r
	bool isLumaCenterSmaller = lumaCenter < lumaLocalAverage;\r
\r
	// If the luma is smaller than at its neighbour, the delta luma at each end should be positive (same variation).\r
	bool correctVariation1 = (lumaEnd1 < 0.0) != isLumaCenterSmaller;\r
	bool correctVariation2 = (lumaEnd2 < 0.0) != isLumaCenterSmaller;\r
\r
	// Only keep the result in the direction of the closer side of the edge.\r
	bool correctVariation = isDirection1 ? correctVariation1 : correctVariation2;\r
\r
	// UV offset: read in the direction of the closest side of the edge.\r
	float pixelOffset = -distanceFinal / edgeThickness + 0.5;\r
\r
	// If the luma variation is incorrect, do not offset.\r
	float finalOffset = correctVariation ? pixelOffset : 0.0;\r
\r
	// Sub-Pixel Shifting\r
	// Full weighted average of the luma over the 3x3 neighborhood.\r
	float lumaAverage = ONE_OVER_TWELVE * (2.0 * (lumaDownUp + lumaLeftRight) + lumaLeftCorners + lumaRightCorners);\r
	// Ratio of the delta between the global average and the center luma, over the luma range in the 3x3 neighborhood.\r
	float subPixelOffset1 = clamp(abs(lumaAverage - lumaCenter) / lumaRange, 0.0, 1.0);\r
	float subPixelOffset2 = (-2.0 * subPixelOffset1 + 3.0) * subPixelOffset1 * subPixelOffset1;\r
	// Compute a sub-pixel offset based on this delta.\r
	float subPixelOffsetFinal = subPixelOffset2 * subPixelOffset2 * SUBPIXEL_QUALITY;\r
\r
	// Pick the biggest of the two offsets.\r
	finalOffset = max(finalOffset, subPixelOffsetFinal);\r
\r
	// Compute the final UV coordinates.\r
	vec2 finalUv = uv;\r
\r
	if(isHorizontal) {\r
\r
		finalUv.y += finalOffset * stepLength;\r
\r
	} else {\r
\r
		finalUv.x += finalOffset * stepLength;\r
\r
	}\r
\r
	return texture2D(inputBuffer, finalUv);\r
\r
}\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	outputColor = fxaa(inputColor, uv);\r
\r
}\r
`, io = `varying vec2 vUvDown;\r
varying vec2 vUvUp;\r
varying vec2 vUvLeft;\r
varying vec2 vUvRight;\r
\r
varying vec2 vUvDownLeft;\r
varying vec2 vUvUpRight;\r
varying vec2 vUvUpLeft;\r
varying vec2 vUvDownRight;\r
\r
void mainSupport(const in vec2 uv) {\r
\r
	vUvDown = uv + vec2(0.0, -1.0) * texelSize;\r
	vUvUp = uv + vec2(0.0, 1.0) * texelSize;\r
	vUvRight = uv + vec2(1.0, 0.0) * texelSize;\r
	vUvLeft = uv + vec2(-1.0, 0.0) * texelSize;\r
\r
	vUvDownLeft = uv + vec2(-1.0, -1.0) * texelSize;\r
	vUvUpRight = uv + vec2(1.0, 1.0) * texelSize;\r
	vUvUpLeft = uv + vec2(-1.0, 1.0) * texelSize;\r
	vUvDownRight = uv + vec2(1.0, -1.0) * texelSize;\r
\r
}\r
`;
class Ch extends I {
  /**
   * Constructs a new FXAA effect.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.SRC] - The blend function of this effect.
   */
  constructor({ blendFunction: e = E.SRC } = {}) {
    super("FXAAEffect", no, {
      vertexShader: io,
      blendFunction: e,
      defines: /* @__PURE__ */ new Map([
        ["EDGE_THRESHOLD_MIN", "0.0312"],
        ["EDGE_THRESHOLD_MAX", "0.125"],
        ["SUBPIXEL_QUALITY", "0.75"],
        ["SAMPLES", "12"]
      ])
    });
  }
  /**
   * The minimum edge detection threshold. Range is [0.0, 1.0].
   *
   * @type {Number}
   */
  get minEdgeThreshold() {
    return Number(this.defines.get("EDGE_THRESHOLD_MIN"));
  }
  set minEdgeThreshold(e) {
    this.defines.set("EDGE_THRESHOLD_MIN", e.toFixed(12)), this.setChanged();
  }
  /**
   * The maximum edge detection threshold. Range is [0.0, 1.0].
   *
   * @type {Number}
   */
  get maxEdgeThreshold() {
    return Number(this.defines.get("EDGE_THRESHOLD_MAX"));
  }
  set maxEdgeThreshold(e) {
    this.defines.set("EDGE_THRESHOLD_MAX", e.toFixed(12)), this.setChanged();
  }
  /**
   * The subpixel blend quality. Range is [0.0, 1.0].
   *
   * @type {Number}
   */
  get subpixelQuality() {
    return Number(this.defines.get("SUBPIXEL_QUALITY"));
  }
  set subpixelQuality(e) {
    this.defines.set("SUBPIXEL_QUALITY", e.toFixed(12)), this.setChanged();
  }
  /**
   * The maximum amount of edge detection samples.
   *
   * @type {Number}
   */
  get samples() {
    return Number(this.defines.get("SAMPLES"));
  }
  set samples(e) {
    this.defines.set("SAMPLES", e.toFixed(0)), this.setChanged();
  }
}
const so = `uniform float gamma;\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	outputColor = LinearToGamma(max(inputColor, 0.0), gamma);\r
\r
}\r
`;
class Ah extends I {
  /**
   * Constructs a new gamma correction effect.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.SRC] - The blend function of this effect.
   * @param {Number} [options.gamma=2.0] - The gamma factor.
   */
  constructor({ blendFunction: e = E.SRC, gamma: t = 2 } = {}) {
    super("GammaCorrectionEffect", so, {
      blendFunction: e,
      uniforms: /* @__PURE__ */ new Map([
        ["gamma", new a(t)]
      ])
    });
  }
}
const bt = {
  DISABLED: 0,
  SPORADIC: 1,
  CONSTANT_MILD: 2,
  CONSTANT_WILD: 3
};
function ao(c, e, t) {
  const r = /* @__PURE__ */ new Map([
    [yi, 1],
    [Bt, 1],
    [ps, 2],
    [ne, 4]
  ]);
  let n;
  if (r.has(e) || console.error("Invalid noise texture format"), t === se) {
    n = new Uint8Array(c * r.get(e));
    for (let i = 0, s = n.length; i < s; ++i)
      n[i] = Math.random() * 255 + 0.5;
  } else {
    n = new Float32Array(c * r.get(e));
    for (let i = 0, s = n.length; i < s; ++i)
      n[i] = Math.random();
  }
  return n;
}
class lr extends le {
  /**
   * Constructs a new noise texture.
   *
   * The texture format can be either `LuminanceFormat` or `RGBAFormat`. Additionally, the formats `RedFormat` and
   * `RGFormat` can be used in a WebGL 2 context.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   * @param {Number} [format=LuminanceFormat] - The texture format.
   * @param {Number} [type=UnsignedByteType] - The texture type.
   */
  constructor(e, t, r = yi, n = se) {
    super(ao(e * t, r, n), e, t, r, n), this.needsUpdate = !0;
  }
}
const oo = `uniform lowp sampler2D perturbationMap;\r
\r
uniform bool active;\r
uniform float columns;\r
uniform float random;\r
uniform vec2 seeds;\r
uniform vec2 distortion;\r
\r
void mainUv(inout vec2 uv) {\r
\r
	if(active) {\r
\r
		if(uv.y < distortion.x + columns && uv.y > distortion.x - columns * random) {\r
\r
			float sx = clamp(ceil(seeds.x), 0.0, 1.0);\r
			uv.y = sx * (1.0 - (uv.y + distortion.y)) + (1.0 - sx) * distortion.y;\r
\r
		}\r
\r
		if(uv.x < distortion.y + columns && uv.x > distortion.y - columns * random) {\r
\r
			float sy = clamp(ceil(seeds.y), 0.0, 1.0);\r
			uv.x = sy * distortion.x + (1.0 - sy) * (1.0 - (uv.x + distortion.x));\r
\r
		}\r
\r
		vec2 normal = texture2D(perturbationMap, uv * random * random).rg;\r
		uv += normal * seeds * (random * 0.2);\r
\r
	}\r
\r
}\r
`, Yt = "Glitch.Generated";
function xe(c, e) {
  return c + Math.random() * (e - c);
}
class Eh extends I {
  /**
   * Constructs a new glitch effect.
   *
   * TODO Change ratio to 0.15.
   * @param {Object} [options] - The options.
   * @param {Vector2} [options.chromaticAberrationOffset] - A chromatic aberration offset. If provided, the glitch effect will influence this offset.
   * @param {Vector2} [options.delay] - The minimum and maximum delay between glitch activations in seconds.
   * @param {Vector2} [options.duration] - The minimum and maximum duration of a glitch in seconds.
   * @param {Vector2} [options.strength] - The strength of weak and strong glitches.
   * @param {Texture} [options.perturbationMap] - A perturbation map. If none is provided, a noise texture will be created.
   * @param {Number} [options.dtSize=64] - The size of the generated noise map. Will be ignored if a perturbation map is provided.
   * @param {Number} [options.columns=0.05] - The scale of the blocky glitch columns.
   * @param {Number} [options.ratio=0.85] - The threshold for strong glitches.
   */
  constructor({
    chromaticAberrationOffset: e = null,
    delay: t = new g(1.5, 3.5),
    duration: r = new g(0.6, 1),
    strength: n = new g(0.3, 1),
    columns: i = 0.05,
    ratio: s = 0.85,
    perturbationMap: o = null,
    dtSize: l = 64
  } = {}) {
    if (super("GlitchEffect", oo, {
      uniforms: /* @__PURE__ */ new Map([
        ["perturbationMap", new a(null)],
        ["columns", new a(i)],
        ["active", new a(!1)],
        ["random", new a(1)],
        ["seeds", new a(new g())],
        ["distortion", new a(new g())]
      ])
    }), o === null) {
      const u = new lr(l, l, ne);
      u.name = Yt, this.perturbationMap = u;
    } else
      this.perturbationMap = o;
    this.time = 0, this.distortion = this.uniforms.get("distortion").value, this.delay = t, this.duration = r, this.breakPoint = new g(
      xe(this.delay.x, this.delay.y),
      xe(this.duration.x, this.duration.y)
    ), this.strength = n, this.mode = bt.SPORADIC, this.ratio = s, this.chromaticAberrationOffset = e;
  }
  /**
   * Random number seeds.
   *
   * @type {Vector2}
   * @private
   */
  get seeds() {
    return this.uniforms.get("seeds").value;
  }
  /**
   * Indicates whether the glitch effect is currently active.
   *
   * @type {Boolean}
   */
  get active() {
    return this.uniforms.get("active").value;
  }
  /**
   * Indicates whether the glitch effect is currently active.
   *
   * @deprecated Use active instead.
   * @return {Boolean} Whether the glitch effect is active.
   */
  isActive() {
    return this.active;
  }
  /**
   * The minimum delay between glitch activations.
   *
   * @type {Number}
   */
  get minDelay() {
    return this.delay.x;
  }
  set minDelay(e) {
    this.delay.x = e;
  }
  /**
   * Returns the minimum delay between glitch activations.
   *
   * @deprecated Use minDelay instead.
   * @return {Number} The minimum delay in seconds.
   */
  getMinDelay() {
    return this.delay.x;
  }
  /**
   * Sets the minimum delay between glitch activations.
   *
   * @deprecated Use minDelay instead.
   * @param {Number} value - The minimum delay in seconds.
   */
  setMinDelay(e) {
    this.delay.x = e;
  }
  /**
   * The maximum delay between glitch activations.
   *
   * @type {Number}
   */
  get maxDelay() {
    return this.delay.y;
  }
  set maxDelay(e) {
    this.delay.y = e;
  }
  /**
   * Returns the maximum delay between glitch activations.
   *
   * @deprecated Use maxDelay instead.
   * @return {Number} The maximum delay in seconds.
   */
  getMaxDelay() {
    return this.delay.y;
  }
  /**
   * Sets the maximum delay between glitch activations.
   *
   * @deprecated Use maxDelay instead.
   * @param {Number} value - The maximum delay in seconds.
   */
  setMaxDelay(e) {
    this.delay.y = e;
  }
  /**
   * The minimum duration of sporadic glitches.
   *
   * @type {Number}
   */
  get minDuration() {
    return this.duration.x;
  }
  set minDuration(e) {
    this.duration.x = e;
  }
  /**
   * Returns the minimum duration of sporadic glitches.
   *
   * @deprecated Use minDuration instead.
   * @return {Number} The minimum duration in seconds.
   */
  getMinDuration() {
    return this.duration.x;
  }
  /**
   * Sets the minimum duration of sporadic glitches.
   *
   * @deprecated Use minDuration instead.
   * @param {Number} value - The minimum duration in seconds.
   */
  setMinDuration(e) {
    this.duration.x = e;
  }
  /**
   * The maximum duration of sporadic glitches.
   *
   * @type {Number}
   */
  get maxDuration() {
    return this.duration.y;
  }
  set maxDuration(e) {
    this.duration.y = e;
  }
  /**
   * Returns the maximum duration of sporadic glitches.
   *
   * @deprecated Use maxDuration instead.
   * @return {Number} The maximum duration in seconds.
   */
  getMaxDuration() {
    return this.duration.y;
  }
  /**
   * Sets the maximum duration of sporadic glitches.
   *
   * @deprecated Use maxDuration instead.
   * @param {Number} value - The maximum duration in seconds.
   */
  setMaxDuration(e) {
    this.duration.y = e;
  }
  /**
   * The strength of weak glitches.
   *
   * @type {Number}
   */
  get minStrength() {
    return this.strength.x;
  }
  set minStrength(e) {
    this.strength.x = e;
  }
  /**
   * Returns the strength of weak glitches.
   *
   * @deprecated Use minStrength instead.
   * @return {Number} The strength.
   */
  getMinStrength() {
    return this.strength.x;
  }
  /**
   * Sets the strength of weak glitches.
   *
   * @deprecated Use minStrength instead.
   * @param {Number} value - The strength.
   */
  setMinStrength(e) {
    this.strength.x = e;
  }
  /**
   * The strength of strong glitches.
   *
   * @type {Number}
   */
  get maxStrength() {
    return this.strength.y;
  }
  set maxStrength(e) {
    this.strength.y = e;
  }
  /**
   * Returns the strength of strong glitches.
   *
   * @deprecated Use maxStrength instead.
   * @return {Number} The strength.
   */
  getMaxStrength() {
    return this.strength.y;
  }
  /**
   * Sets the strength of strong glitches.
   *
   * @deprecated Use maxStrength instead.
   * @param {Number} value - The strength.
   */
  setMaxStrength(e) {
    this.strength.y = e;
  }
  /**
   * Returns the current glitch mode.
   *
   * @deprecated Use mode instead.
   * @return {GlitchMode} The mode.
   */
  getMode() {
    return this.mode;
  }
  /**
   * Sets the current glitch mode.
   *
   * @deprecated Use mode instead.
   * @param {GlitchMode} value - The mode.
   */
  setMode(e) {
    this.mode = e;
  }
  /**
   * Returns the glitch ratio.
   *
   * @deprecated Use ratio instead.
   * @return {Number} The ratio.
   */
  getGlitchRatio() {
    return 1 - this.ratio;
  }
  /**
   * Sets the ratio of weak (0.0) and strong (1.0) glitches.
   *
   * @deprecated Use ratio instead.
   * @param {Number} value - The ratio. Range is [0.0, 1.0].
   */
  setGlitchRatio(e) {
    this.ratio = Math.min(Math.max(1 - e, 0), 1);
  }
  /**
   * The glitch column size.
   *
   * @type {Number}
   */
  get columns() {
    return this.uniforms.get("columns").value;
  }
  set columns(e) {
    this.uniforms.get("columns").value = e;
  }
  /**
   * Returns the glitch column size.
   *
   * @deprecated Use columns instead.
   * @return {Number} The glitch column size.
   */
  getGlitchColumns() {
    return this.columns;
  }
  /**
   * Sets the glitch column size.
   *
   * @deprecated Use columns instead.
   * @param {Number} value - The glitch column size.
   */
  setGlitchColumns(e) {
    this.columns = e;
  }
  /**
   * Returns the chromatic aberration offset.
   *
   * @deprecated Use chromaticAberrationOffset instead.
   * @return {Vector2} The offset.
   */
  getChromaticAberrationOffset() {
    return this.chromaticAberrationOffset;
  }
  /**
   * Sets the chromatic aberration offset.
   *
   * @deprecated Use chromaticAberrationOffset instead.
   * @param {Vector2} value - The offset.
   */
  setChromaticAberrationOffset(e) {
    this.chromaticAberrationOffset = e;
  }
  /**
   * The perturbation map.
   *
   * @type {Texture}
   */
  get perturbationMap() {
    return this.uniforms.get("perturbationMap").value;
  }
  set perturbationMap(e) {
    const t = this.perturbationMap;
    t !== null && t.name === Yt && t.dispose(), e.minFilter = e.magFilter = b, e.wrapS = e.wrapT = G, e.generateMipmaps = !1, this.uniforms.get("perturbationMap").value = e;
  }
  /**
   * Returns the current perturbation map.
   *
   * @deprecated Use perturbationMap instead.
   * @return {Texture} The current perturbation map.
   */
  getPerturbationMap() {
    return this.perturbationMap;
  }
  /**
   * Replaces the current perturbation map with the given one.
   *
   * The current map will be disposed if it was generated by this effect.
   *
   * @deprecated Use perturbationMap instead.
   * @param {Texture} value - The new perturbation map.
   */
  setPerturbationMap(e) {
    this.perturbationMap = e;
  }
  /**
   * Generates a perturbation map.
   *
   * @deprecated Use NoiseTexture instead.
   * @param {Number} [value=64] - The texture size.
   * @return {DataTexture} The perturbation map.
   */
  generatePerturbationMap(e = 64) {
    const t = new lr(e, e, ne);
    return t.name = Yt, t;
  }
  /**
   * Updates this effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   */
  update(e, t, r) {
    const n = this.mode, i = this.breakPoint, s = this.chromaticAberrationOffset, o = this.strength;
    let l = this.time, u = !1, f = 0, h = 0, m;
    n !== bt.DISABLED && (n === bt.SPORADIC && (l += r, m = l > i.x, l >= i.x + i.y && (i.set(
      xe(this.delay.x, this.delay.y),
      xe(this.duration.x, this.duration.y)
    ), l = 0)), f = Math.random(), this.uniforms.get("random").value = f, m && f > this.ratio || n === bt.CONSTANT_WILD ? (u = !0, f *= o.y * 0.03, h = xe(-Math.PI, Math.PI), this.seeds.set(xe(-o.y, o.y), xe(-o.y, o.y)), this.distortion.set(xe(0, 1), xe(0, 1))) : (m || n === bt.CONSTANT_MILD) && (u = !0, f *= o.x * 0.03, h = xe(-Math.PI, Math.PI), this.seeds.set(xe(-o.x, o.x), xe(-o.x, o.x)), this.distortion.set(xe(0, 1), xe(0, 1))), this.time = l), s !== null && (u ? s.set(Math.cos(h), Math.sin(h)).multiplyScalar(f) : s.set(0, 0)), this.uniforms.get("active").value = u;
  }
  /**
   * Deletes generated resources.
   */
  dispose() {
    const e = this.perturbationMap;
    e !== null && e.name === Yt && e.dispose();
  }
}
const lo = `#include <common>\r
#include <dithering_pars_fragment>\r
\r
#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D inputBuffer;\r
\r
#else\r
\r
	uniform lowp sampler2D inputBuffer;\r
\r
#endif\r
\r
uniform vec2 lightPosition;\r
uniform float exposure;\r
uniform float decay;\r
uniform float density;\r
uniform float weight;\r
uniform float clampMax;\r
\r
varying vec2 vUv;\r
\r
void main() {\r
\r
	vec2 coord = vUv;\r
\r
	// Calculate the vector from this pixel to the light position in screen space.\r
	vec2 delta = lightPosition - coord;\r
	delta *= 1.0 / SAMPLES_FLOAT * density;\r
\r
	// A decreasing illumination factor.\r
	float illuminationDecay = 1.0;\r
	vec4 color = vec4(0.0);\r
\r
	// Estimate the probability of occlusion at each pixel by summing samples along a ray to the light position.\r
	for(int i = 0; i < SAMPLES_INT; ++i) {\r
\r
		coord += delta;\r
		vec4 texel = texture2D(inputBuffer, coord);\r
\r
		// Apply the sample attenuation scale/decay factors.\r
		texel *= illuminationDecay * weight;\r
		color += texel;\r
\r
		// Update the exponential decay factor.\r
		illuminationDecay *= decay;\r
\r
	}\r
\r
	gl_FragColor = clamp(color * exposure, 0.0, clampMax);\r
\r
	#include <dithering_fragment>\r
\r
}\r
`;
class co extends N {
  /**
   * Constructs a new god rays material.
   *
   * TODO Remove lightPosition param.
   * @param {Vector2} lightPosition - Deprecated.
   */
  constructor(e) {
    super({
      name: "GodRaysMaterial",
      defines: {
        SAMPLES_INT: "60",
        SAMPLES_FLOAT: "60.0"
      },
      uniforms: {
        inputBuffer: new a(null),
        lightPosition: new a(e),
        density: new a(1),
        decay: new a(1),
        weight: new a(1),
        exposure: new a(1),
        clampMax: new a(1)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: lo,
      vertexShader: Be
    });
  }
  /**
   * The input buffer.
   *
   * @type {Texture}
   */
  set inputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * Sets the input buffer.
   *
   * @deprecated Use inputBuffer instead.
   * @param {Texture} value - The input buffer.
   */
  setInputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * The screen space position of the light source.
   *
   * @type {Vector2}
   */
  get lightPosition() {
    return this.uniforms.lightPosition.value;
  }
  /**
   * Returns the screen space position of the light source.
   *
   * @deprecated Use lightPosition instead.
   * @return {Vector2} The position.
   */
  getLightPosition() {
    return this.uniforms.lightPosition.value;
  }
  /**
   * Sets the screen space position of the light source.
   *
   * @deprecated Use lightPosition instead.
   * @param {Vector2} value - The position.
   */
  setLightPosition(e) {
    this.uniforms.lightPosition.value = e;
  }
  /**
   * The density.
   *
   * @type {Number}
   */
  get density() {
    return this.uniforms.density.value;
  }
  set density(e) {
    this.uniforms.density.value = e;
  }
  /**
   * Returns the density.
   *
   * @deprecated Use density instead.
   * @return {Number} The density.
   */
  getDensity() {
    return this.uniforms.density.value;
  }
  /**
   * Sets the density.
   *
   * @deprecated Use density instead.
   * @param {Number} value - The density.
   */
  setDensity(e) {
    this.uniforms.density.value = e;
  }
  /**
   * The decay.
   *
   * @type {Number}
   */
  get decay() {
    return this.uniforms.decay.value;
  }
  set decay(e) {
    this.uniforms.decay.value = e;
  }
  /**
   * Returns the decay.
   *
   * @deprecated Use decay instead.
   * @return {Number} The decay.
   */
  getDecay() {
    return this.uniforms.decay.value;
  }
  /**
   * Sets the decay.
   *
   * @deprecated Use decay instead.
   * @param {Number} value - The decay.
   */
  setDecay(e) {
    this.uniforms.decay.value = e;
  }
  /**
   * The weight.
   *
   * @type {Number}
   */
  get weight() {
    return this.uniforms.weight.value;
  }
  set weight(e) {
    this.uniforms.weight.value = e;
  }
  /**
   * Returns the weight.
   *
   * @deprecated Use weight instead.
   * @return {Number} The weight.
   */
  getWeight() {
    return this.uniforms.weight.value;
  }
  /**
   * Sets the weight.
   *
   * @deprecated Use weight instead.
   * @param {Number} value - The weight.
   */
  setWeight(e) {
    this.uniforms.weight.value = e;
  }
  /**
   * The exposure.
   *
   * @type {Number}
   */
  get exposure() {
    return this.uniforms.exposure.value;
  }
  set exposure(e) {
    this.uniforms.exposure.value = e;
  }
  /**
   * Returns the exposure.
   *
   * @deprecated Use exposure instead.
   * @return {Number} The exposure.
   */
  getExposure() {
    return this.uniforms.exposure.value;
  }
  /**
   * Sets the exposure.
   *
   * @deprecated Use exposure instead.
   * @param {Number} value - The exposure.
   */
  setExposure(e) {
    this.uniforms.exposure.value = e;
  }
  /**
   * The maximum light intensity.
   *
   * @type {Number}
   */
  get maxIntensity() {
    return this.uniforms.clampMax.value;
  }
  set maxIntensity(e) {
    this.uniforms.clampMax.value = e;
  }
  /**
   * Returns the maximum light intensity.
   *
   * @deprecated Use maxIntensity instead.
   * @return {Number} The maximum light intensity.
   */
  getMaxIntensity() {
    return this.uniforms.clampMax.value;
  }
  /**
   * Sets the maximum light intensity.
   *
   * @deprecated Use maxIntensity instead.
   * @param {Number} value - The maximum light intensity.
   */
  setMaxIntensity(e) {
    this.uniforms.clampMax.value = e;
  }
  /**
   * The amount of samples per pixel.
   *
   * @type {Number}
   */
  get samples() {
    return Number(this.defines.SAMPLES_INT);
  }
  set samples(e) {
    const t = Math.floor(e);
    this.defines.SAMPLES_INT = t.toFixed(0), this.defines.SAMPLES_FLOAT = t.toFixed(1), this.needsUpdate = !0;
  }
  /**
   * Returns the amount of samples per pixel.
   *
   * @deprecated Use samples instead.
   * @return {Number} The sample count.
   */
  getSamples() {
    return this.samples;
  }
  /**
   * Sets the amount of samples per pixel.
   *
   * @deprecated Use samples instead.
   * @param {Number} value - The sample count.
   */
  setSamples(e) {
    this.samples = e;
  }
}
const uo = `#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D map;\r
\r
#else\r
\r
	uniform lowp sampler2D map;\r
\r
#endif\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	outputColor = texture2D(map, uv);\r
\r
}\r
`, br = /* @__PURE__ */ new T(), bn = /* @__PURE__ */ new C();
class Rh extends I {
  /**
   * Constructs a new god rays effect.
   *
   * @param {Camera} [camera] - The main camera.
   * @param {Mesh|Points} [lightSource] - The light source. Must not write depth and has to be flagged as transparent.
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.SCREEN] - The blend function of this effect.
   * @param {Number} [options.samples=60.0] - The number of samples per pixel.
   * @param {Number} [options.density=0.96] - The density of the light rays.
   * @param {Number} [options.decay=0.9] - An illumination decay factor.
   * @param {Number} [options.weight=0.4] - A light ray weight factor.
   * @param {Number} [options.exposure=0.6] - A constant attenuation coefficient.
   * @param {Number} [options.clampMax=1.0] - An upper bound for the saturation of the overall effect.
   * @param {Number} [options.resolutionScale=0.5] - The resolution scale.
   * @param {Number} [options.resolutionX=Resolution.AUTO_SIZE] - The horizontal resolution.
   * @param {Number} [options.resolutionY=Resolution.AUTO_SIZE] - The vertical resolution.
   * @param {Number} [options.width=Resolution.AUTO_SIZE] - Deprecated. Use resolutionX instead.
   * @param {Number} [options.height=Resolution.AUTO_SIZE] - Deprecated. Use resolutionY instead.
   * @param {KernelSize} [options.kernelSize=KernelSize.SMALL] - The blur kernel size. Has no effect if blur is disabled.
   * @param {Boolean} [options.blur=true] - Whether the god rays should be blurred to reduce artifacts.
   */
  constructor(e, t, {
    blendFunction: r = E.SCREEN,
    samples: n = 60,
    density: i = 0.96,
    decay: s = 0.9,
    weight: o = 0.4,
    exposure: l = 0.6,
    clampMax: u = 1,
    blur: f = !0,
    kernelSize: h = Le.SMALL,
    resolutionScale: m = 0.5,
    width: p = W.AUTO_SIZE,
    height: d = W.AUTO_SIZE,
    resolutionX: v = p,
    resolutionY: x = d
  } = {}) {
    super("GodRaysEffect", uo, {
      blendFunction: r,
      attributes: Q.DEPTH,
      uniforms: /* @__PURE__ */ new Map([
        ["map", new a(null)]
      ])
    }), this.camera = e, this._lightSource = t, this.lightSource = t, this.lightScene = new zt(), this.screenPosition = new g(), this.renderTargetA = new A(1, 1, { depthBuffer: !1 }), this.renderTargetA.texture.name = "GodRays.Target.A", this.renderTargetB = this.renderTargetA.clone(), this.renderTargetB.texture.name = "GodRays.Target.B", this.uniforms.get("map").value = this.renderTargetB.texture, this.renderTargetLight = new A(1, 1), this.renderTargetLight.texture.name = "GodRays.Light", this.renderTargetLight.depthTexture = new Oe(), this.renderPassLight = new pe(this.lightScene, e), this.renderPassLight.clearPass.overrideClearColor = new y(0), this.clearPass = new De(!0, !1, !1), this.clearPass.overrideClearColor = new y(0), this.blurPass = new at({ kernelSize: h }), this.blurPass.enabled = f, this.depthMaskPass = new ee(new _e());
    const w = this.depthMaskMaterial;
    w.depthBuffer1 = this.renderTargetLight.depthTexture, w.copyCameraSettings(e), this.godRaysPass = new ee(new co(this.screenPosition));
    const M = this.godRaysMaterial;
    M.density = i, M.decay = s, M.weight = o, M.exposure = l, M.maxIntensity = u, M.samples = n;
    const D = this.resolution = new W(this, v, x, m);
    D.addEventListener("change", (P) => this.setSize(D.baseWidth, D.baseHeight));
  }
  set mainCamera(e) {
    this.camera = e, this.renderPassLight.mainCamera = e, this.depthMaskMaterial.copyCameraSettings(e);
  }
  /**
   * Sets the light source.
   *
   * @type {Mesh|Points}
   */
  get lightSource() {
    return this._lightSource;
  }
  set lightSource(e) {
    this._lightSource = e, e !== null && (e.material.depthWrite = !1, e.material.transparent = !0);
  }
  /**
   * Returns the blur pass that reduces aliasing artifacts and makes the light softer.
   *
   * @deprecated Use blurPass instead.
   * @return {KawaseBlurPass} The blur pass.
   */
  getBlurPass() {
    return this.blurPass;
  }
  /**
   * A texture that contains the intermediate result of this effect.
   *
   * @type {Texture}
   */
  get texture() {
    return this.renderTargetB.texture;
  }
  /**
   * Returns the god rays texture.
   *
   * @deprecated Use texture instead.
   * @return {Texture} The texture.
   */
  getTexture() {
    return this.texture;
  }
  /**
   * The depth mask material.
   *
   * @type {DepthMaskMaterial}
   * @private
   */
  get depthMaskMaterial() {
    return this.depthMaskPass.fullscreenMaterial;
  }
  /**
   * The internal god rays material.
   *
   * @type {GodRaysMaterial}
   */
  get godRaysMaterial() {
    return this.godRaysPass.fullscreenMaterial;
  }
  /**
   * Returns the god rays material.
   *
   * @deprecated Use godRaysMaterial instead.
   * @return {GodRaysMaterial} The material.
   */
  getGodRaysMaterial() {
    return this.godRaysMaterial;
  }
  /**
   * Returns the resolution of this effect.
   *
   * @deprecated Use resolution instead.
   * @return {GodRaysMaterial} The material.
   */
  getResolution() {
    return this.resolution;
  }
  /**
   * The current width of the internal render targets.
   *
   * @type {Number}
   * @deprecated Use resolution.width instead.
   */
  get width() {
    return this.resolution.width;
  }
  set width(e) {
    this.resolution.preferredWidth = e;
  }
  /**
   * The current height of the internal render targets.
   *
   * @type {Number}
   * @deprecated Use resolution.height instead.
   */
  get height() {
    return this.resolution.height;
  }
  set height(e) {
    this.resolution.preferredHeight = e;
  }
  /**
   * Indicates whether dithering is enabled.
   *
   * @type {Boolean}
   * @deprecated
   */
  get dithering() {
    return this.godRaysMaterial.dithering;
  }
  set dithering(e) {
    const t = this.godRaysMaterial;
    t.dithering = e, t.needsUpdate = !0;
  }
  /**
   * Indicates whether the god rays should be blurred to reduce artifacts.
   *
   * @type {Boolean}
   * @deprecated Use blurPass.enabled instead.
   */
  get blur() {
    return this.blurPass.enabled;
  }
  set blur(e) {
    this.blurPass.enabled = e;
  }
  /**
   * The blur kernel size.
   *
   * @type {KernelSize}
   * @deprecated Use blurPass.kernelSize instead.
   */
  get kernelSize() {
    return this.blurPass.kernelSize;
  }
  set kernelSize(e) {
    this.blurPass.kernelSize = e;
  }
  /**
   * Returns the current resolution scale.
   *
   * @return {Number} The resolution scale.
   * @deprecated Use resolution instead.
   */
  getResolutionScale() {
    return this.resolution.scale;
  }
  /**
   * Sets the resolution scale.
   *
   * @param {Number} scale - The new resolution scale.
   * @deprecated Use resolution instead.
   */
  setResolutionScale(e) {
    this.resolution.scale = e;
  }
  /**
   * The number of samples per pixel.
   *
   * @type {Number}
   * @deprecated Use godRaysMaterial.samples instead.
   */
  get samples() {
    return this.godRaysMaterial.samples;
  }
  /**
   * A higher sample count improves quality at the cost of performance.
   *
   * @type {Number}
   * @deprecated Use godRaysMaterial.samples instead.
   */
  set samples(e) {
    this.godRaysMaterial.samples = e;
  }
  /**
   * Sets the depth texture.
   *
   * @param {Texture} depthTexture - A depth texture.
   * @param {Number} [depthPacking=BasicDepthPacking] - The depth packing.
   */
  setDepthTexture(e, t = $) {
    this.depthMaskPass.fullscreenMaterial.depthBuffer0 = e, this.depthMaskPass.fullscreenMaterial.depthPacking0 = t;
  }
  /**
   * Updates this effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   */
  update(e, t, r) {
    const n = this.lightSource, i = n.parent, s = n.matrixAutoUpdate, o = this.renderTargetA, l = this.renderTargetLight;
    n.material.depthWrite = !0, n.matrixAutoUpdate = !1, n.updateWorldMatrix(!0, !1), i !== null && (s || bn.copy(n.matrix), n.matrix.copy(n.matrixWorld)), this.lightScene.add(n), this.renderPassLight.render(e, l), this.clearPass.render(e, o), this.depthMaskPass.render(e, l, o), n.material.depthWrite = !1, n.matrixAutoUpdate = s, i !== null && (s || n.matrix.copy(bn), i.add(n)), br.setFromMatrixPosition(n.matrixWorld).project(this.camera), this.screenPosition.set(
      Math.min(Math.max((br.x + 1) * 0.5, -1), 2),
      Math.min(Math.max((br.y + 1) * 0.5, -1), 2)
    ), this.blurPass.enabled && this.blurPass.render(e, o, o), this.godRaysPass.render(e, o, this.renderTargetB);
  }
  /**
   * Updates the size of internal render targets.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.resolution;
    r.setBaseSize(e, t);
    const n = r.width, i = r.height;
    this.renderTargetA.setSize(n, i), this.renderTargetB.setSize(n, i), this.renderTargetLight.setSize(n, i), this.blurPass.resolution.copy(r);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    this.blurPass.initialize(e, t, r), this.renderPassLight.initialize(e, t, r), this.depthMaskPass.initialize(e, t, r), this.godRaysPass.initialize(e, t, r), r !== void 0 && (this.renderTargetA.texture.type = r, this.renderTargetB.texture.type = r, this.renderTargetLight.texture.type = r, e !== null && e.outputColorSpace === V && (this.renderTargetA.texture.colorSpace = V, this.renderTargetB.texture.colorSpace = V, this.renderTargetLight.texture.colorSpace = V));
  }
}
const fo = `uniform vec2 scale;\r
uniform float lineWidth;\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	float grid = 0.5 - max(abs(mod(uv.x * scale.x, 1.0) - 0.5), abs(mod(uv.y * scale.y, 1.0) - 0.5));\r
	outputColor = vec4(vec3(smoothstep(0.0, lineWidth, grid)), inputColor.a);\r
\r
}\r
`;
class Nh extends I {
  /**
   * Constructs a new grid effect.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.OVERLAY] - The blend function of this effect.
   * @param {Number} [options.scale=1.0] - The scale of the grid pattern.
   * @param {Number} [options.lineWidth=0.0] - The line width of the grid pattern.
   */
  constructor({ blendFunction: e = E.OVERLAY, scale: t = 1, lineWidth: r = 0 } = {}) {
    super("GridEffect", fo, {
      blendFunction: e,
      uniforms: /* @__PURE__ */ new Map([
        ["scale", new a(new g())],
        ["lineWidth", new a(r)]
      ])
    }), this.resolution = new g(), this.s = 0, this.scale = t, this.l = 0, this.lineWidth = r;
  }
  /**
   * The scale.
   *
   * @type {Number}
   */
  get scale() {
    return this.s;
  }
  set scale(e) {
    this.s = Math.max(e, 1e-6), this.setSize(this.resolution.width, this.resolution.height);
  }
  /**
   * Returns the current grid scale.
   *
   * @deprecated Use scale instead.
   * @return {Number} The grid scale.
   */
  getScale() {
    return this.scale;
  }
  /**
   * Sets the grid scale.
   *
   * @deprecated Use scale instead.
   * @param {Number} value - The new grid scale.
   */
  setScale(e) {
    this.scale = e;
  }
  /**
   * The line width.
   *
   * @type {Number}
   */
  get lineWidth() {
    return this.l;
  }
  set lineWidth(e) {
    this.l = e, this.setSize(this.resolution.width, this.resolution.height);
  }
  /**
   * Returns the current grid line width.
   *
   * @deprecated Use lineWidth instead.
   * @return {Number} The grid line width.
   */
  getLineWidth() {
    return this.lineWidth;
  }
  /**
   * Sets the grid line width.
   *
   * @deprecated Use lineWidth instead.
   * @param {Number} value - The new grid line width.
   */
  setLineWidth(e) {
    this.lineWidth = e;
  }
  /**
   * Updates the size of this pass.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.resolution.set(e, t);
    const r = e / t, n = this.scale * (t * 0.125);
    this.uniforms.get("scale").value.set(r * n, n), this.uniforms.get("lineWidth").value = n / t + this.lineWidth;
  }
}
const ho = `uniform vec3 hue;\r
uniform float saturation;\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	// Hue.\r
	vec3 color = vec3(\r
		dot(inputColor.rgb, hue.xyz),\r
		dot(inputColor.rgb, hue.zxy),\r
		dot(inputColor.rgb, hue.yzx)\r
	);\r
\r
	// Saturation.\r
	float average = (color.r + color.g + color.b) / 3.0;\r
	vec3 diff = average - color;\r
\r
	if(saturation > 0.0) {\r
\r
		color += diff * (1.0 - 1.0 / (1.001 - saturation));\r
\r
	} else {\r
\r
		color += diff * -saturation;\r
\r
	}\r
\r
	outputColor = vec4(min(color, 1.0), inputColor.a);\r
\r
}\r
`;
class zh extends I {
  /**
   * Constructs a new hue/saturation effect.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.SRC] - The blend function of this effect.
   * @param {Number} [options.hue=0.0] - The hue in radians.
   * @param {Number} [options.saturation=0.0] - The saturation factor, ranging from -1 to 1, where 0 means no change.
   */
  constructor({ blendFunction: e = E.SRC, hue: t = 0, saturation: r = 0 } = {}) {
    super("HueSaturationEffect", ho, {
      blendFunction: e,
      uniforms: /* @__PURE__ */ new Map([
        ["hue", new a(new T())],
        ["saturation", new a(r)]
      ])
    }), this.hue = t;
  }
  /**
   * The saturation.
   *
   * @type {Number}
   */
  get saturation() {
    return this.uniforms.get("saturation").value;
  }
  set saturation(e) {
    this.uniforms.get("saturation").value = e;
  }
  /**
   * Returns the saturation.
   *
   * @deprecated Use saturation instead.
   * @return {Number} The saturation.
   */
  getSaturation() {
    return this.saturation;
  }
  /**
   * Sets the saturation.
   *
   * @deprecated Use saturation instead.
   * @param {Number} value - The saturation.
   */
  setSaturation(e) {
    this.saturation = e;
  }
  /**
   * The hue.
   *
   * @type {Number}
   */
  get hue() {
    const e = this.uniforms.get("hue").value;
    return Math.acos((e.x * 3 - 1) / 2);
  }
  set hue(e) {
    const t = Math.sin(e), r = Math.cos(e);
    this.uniforms.get("hue").value.set(
      (2 * r + 1) / 3,
      (-Math.sqrt(3) * t - r + 1) / 3,
      (Math.sqrt(3) * t - r + 1) / 3
    );
  }
  /**
   * Returns the hue.
   *
   * @deprecated Use hue instead.
   * @return {Number} The hue in radians.
   */
  getHue() {
    return this.hue;
  }
  /**
   * Sets the hue.
   *
   * @deprecated Use hue instead.
   * @param {Number} value - The hue in radians.
   */
  setHue(e) {
    this.hue = e;
  }
}
const mo = `#ifdef LUT_PRECISION_HIGH\r
\r
	#ifdef GL_FRAGMENT_PRECISION_HIGH\r
\r
		uniform highp sampler2D lut;\r
\r
	#else\r
\r
		uniform mediump sampler2D lut;\r
\r
	#endif\r
\r
#else\r
\r
	uniform lowp sampler2D lut;\r
\r
#endif\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	outputColor = vec4(\r
		texture2D(lut, vec2(inputColor.r, 0.5)).r,\r
		texture2D(lut, vec2(inputColor.g, 0.5)).r,\r
		texture2D(lut, vec2(inputColor.b, 0.5)).r,\r
		inputColor.a\r
	);\r
\r
}\r
`;
class Fh extends I {
  /**
   * Constructs a new color grading effect.
   *
   * @param {Texture} lut - The lookup texture.
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.SRC] - The blend function of this effect.
   */
  constructor(e, { blendFunction: t = E.SRC } = {}) {
    super("LUT1DEffect", mo, {
      blendFunction: t,
      uniforms: /* @__PURE__ */ new Map([["lut", new a(null)]])
    }), this.lut = e;
  }
  /**
   * The LUT.
   *
   * @type {Texture}
   */
  get lut() {
    return this.uniforms.get("lut").value;
  }
  set lut(e) {
    this.uniforms.get("lut").value = e, e !== null && (e.type === Y || e.type === K) && this.defines.set("LUT_PRECISION_HIGH", "1");
  }
}
const po = `uniform vec3 scale;\r
uniform vec3 offset;\r
\r
#ifdef CUSTOM_INPUT_DOMAIN\r
\r
	uniform vec3 domainMin;\r
	uniform vec3 domainMax;\r
\r
#endif\r
\r
#ifdef LUT_3D\r
\r
	#ifdef LUT_PRECISION_HIGH\r
\r
		#ifdef GL_FRAGMENT_PRECISION_HIGH\r
\r
			uniform highp sampler3D lut;\r
\r
		#else\r
\r
			uniform mediump sampler3D lut;\r
\r
		#endif\r
\r
	#else\r
\r
		uniform lowp sampler3D lut;\r
\r
	#endif\r
\r
	vec4 applyLUT(const in vec3 rgb) {\r
\r
		#ifdef TETRAHEDRAL_INTERPOLATION\r
\r
			/* Strategy: Fetch the four corners (v1, v2, v3, v4) of the tetrahedron that corresponds to the input coordinates,\r
			calculate the barycentric weights and interpolate the nearest color samples. */\r
\r
			vec3 p = floor(rgb);\r
			vec3 f = rgb - p;\r
\r
			vec3 v1 = (p + 0.5) * LUT_TEXEL_WIDTH;\r
			vec3 v4 = (p + 1.5) * LUT_TEXEL_WIDTH;\r
			vec3 v2, v3; // Must be identified.\r
			vec3 frac;\r
\r
			if(f.r >= f.g) {\r
\r
				if(f.g > f.b) {\r
\r
					// T4: R >= G > B\r
					frac = f.rgb;\r
					v2 = vec3(v4.x, v1.y, v1.z);\r
					v3 = vec3(v4.x, v4.y, v1.z);\r
\r
				} else if(f.r >= f.b) {\r
\r
					// T6: R >= B >= G\r
					frac = f.rbg;\r
					v2 = vec3(v4.x, v1.y, v1.z);\r
					v3 = vec3(v4.x, v1.y, v4.z);\r
\r
				} else {\r
\r
					// T2: B > R >= G\r
					frac = f.brg;\r
					v2 = vec3(v1.x, v1.y, v4.z);\r
					v3 = vec3(v4.x, v1.y, v4.z);\r
\r
				}\r
\r
			} else {\r
\r
				if(f.b > f.g) {\r
\r
					// T3: B > G > R\r
					frac = f.bgr;\r
					v2 = vec3(v1.x, v1.y, v4.z);\r
					v3 = vec3(v1.x, v4.y, v4.z);\r
\r
				} else if(f.r >= f.b) {\r
\r
					// T5: G > R >= B\r
					frac = f.grb;\r
					v2 = vec3(v1.x, v4.y, v1.z);\r
					v3 = vec3(v4.x, v4.y, v1.z);\r
\r
				} else {\r
\r
					// T1: G >= B > R\r
					frac = f.gbr;\r
					v2 = vec3(v1.x, v4.y, v1.z);\r
					v3 = vec3(v1.x, v4.y, v4.z);\r
\r
				}\r
\r
			}\r
\r
			// Interpolate manually to avoid 8-bit quantization of fractions.\r
			vec4 n1 = texture(lut, v1);\r
			vec4 n2 = texture(lut, v2);\r
			vec4 n3 = texture(lut, v3);\r
			vec4 n4 = texture(lut, v4);\r
\r
			vec4 weights = vec4(\r
				1.0 - frac.x,\r
				frac.x - frac.y,\r
				frac.y - frac.z,\r
				frac.z\r
			);\r
\r
			// weights.x * n1 + weights.y * n2 + weights.z * n3 + weights.w * n4\r
			vec4 result = weights * mat4(\r
				vec4(n1.r, n2.r, n3.r, n4.r),\r
				vec4(n1.g, n2.g, n3.g, n4.g),\r
				vec4(n1.b, n2.b, n3.b, n4.b),\r
				vec4(1.0)\r
			);\r
\r
			return vec4(result.rgb, 1.0);\r
\r
		#else\r
\r
			/* Built-in trilinear interpolation. Note that the fractional components are quantized to 8 bits on common\r
			hardware, which introduces significant error with small grid sizes. */\r
			return texture(lut, rgb);\r
\r
		#endif\r
\r
	}\r
\r
#else\r
\r
	#ifdef LUT_PRECISION_HIGH\r
\r
		#ifdef GL_FRAGMENT_PRECISION_HIGH\r
\r
			uniform highp sampler2D lut;\r
\r
		#else\r
\r
			uniform mediump sampler2D lut;\r
\r
		#endif\r
\r
	#else\r
\r
		uniform lowp sampler2D lut;\r
\r
	#endif\r
\r
	vec4 applyLUT(const in vec3 rgb) {\r
\r
		// Get the slices on either side of the sample.\r
		float slice = rgb.b * LUT_SIZE;\r
		float slice0 = floor(slice);\r
		float interp = slice - slice0;\r
		float centeredInterp = interp - 0.5;\r
		float slice1 = slice0 + sign(centeredInterp);\r
\r
		#ifdef LUT_STRIP_HORIZONTAL\r
\r
			// Pull X in by half a texel in each direction to avoid slice bleeding.\r
			float xOffset = clamp(\r
				rgb.r * LUT_TEXEL_HEIGHT,\r
				LUT_TEXEL_WIDTH * 0.5,\r
				LUT_TEXEL_HEIGHT - LUT_TEXEL_WIDTH * 0.5\r
			);\r
\r
			vec2 uv0 = vec2(slice0 * LUT_TEXEL_HEIGHT + xOffset, rgb.g);\r
			vec2 uv1 = vec2(slice1 * LUT_TEXEL_HEIGHT + xOffset, rgb.g);\r
\r
		#else\r
\r
			// Pull Y in by half a texel in each direction to avoid slice bleeding.\r
			float yOffset = clamp(\r
				rgb.g * LUT_TEXEL_WIDTH,\r
				LUT_TEXEL_HEIGHT * 0.5,\r
				LUT_TEXEL_WIDTH - LUT_TEXEL_HEIGHT * 0.5\r
			);\r
\r
			vec2 uv0 = vec2(rgb.r, slice0 * LUT_TEXEL_WIDTH + yOffset);\r
			vec2 uv1 = vec2(rgb.r, slice1 * LUT_TEXEL_WIDTH + yOffset);\r
\r
		#endif\r
\r
		// Manual trilinear interpolation (subject to quantization errors).\r
		vec4 sample0 = texture2D(lut, uv0);\r
		vec4 sample1 = texture2D(lut, uv1);\r
\r
		return mix(sample0, sample1, abs(centeredInterp));\r
\r
	}\r
\r
#endif\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	vec3 c = inputColor.rgb;\r
\r
	#ifdef CUSTOM_INPUT_DOMAIN\r
\r
		if(c.r >= domainMin.r && c.g >= domainMin.g && c.b >= domainMin.b &&\r
			c.r <= domainMax.r && c.g <= domainMax.g && c.b <= domainMax.b) {\r
\r
			c = applyLUT(scale * c + offset).rgb;\r
\r
		} else {\r
\r
			c = inputColor.rgb;\r
\r
		}\r
\r
	#else\r
\r
		#if !defined(LUT_3D) || defined(TETRAHEDRAL_INTERPOLATION)\r
\r
			c = clamp(c, 0.0, 1.0);\r
\r
		#endif\r
\r
		c = applyLUT(scale * c + offset).rgb;\r
\r
	#endif\r
\r
	outputColor = vec4(c, inputColor.a);\r
\r
}\r
`;
class Ih extends I {
  /**
   * Constructs a new color grading effect.
   *
   * @param {Texture} lut - The lookup texture.
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.SRC] - The blend function of this effect.
   * @param {Boolean} [options.tetrahedralInterpolation=false] - Enables or disables tetrahedral interpolation.
   * @param {ColorSpace} [options.inputColorSpace=SRGBColorSpace] - The input color space.
   */
  constructor(e, {
    blendFunction: t = E.SRC,
    tetrahedralInterpolation: r = !1,
    inputColorSpace: n = V
  } = {}) {
    super("LUT3DEffect", po, {
      blendFunction: t,
      uniforms: /* @__PURE__ */ new Map([
        ["lut", new a(null)],
        ["scale", new a(new T())],
        ["offset", new a(new T())],
        ["domainMin", new a(null)],
        ["domainMax", new a(null)]
      ])
    }), this.tetrahedralInterpolation = r, this.inputColorSpace = n, this.lut = e;
  }
  /**
   * The LUT.
   *
   * @type {Texture}
   */
  get lut() {
    return this.uniforms.get("lut").value;
  }
  set lut(e) {
    const t = this.defines, r = this.uniforms;
    if (this.lut !== e && (r.get("lut").value = e, e !== null)) {
      const n = e.image, i = this.tetrahedralInterpolation;
      if (t.clear(), t.set("LUT_SIZE", Math.min(n.width, n.height).toFixed(16)), t.set("LUT_TEXEL_WIDTH", (1 / n.width).toFixed(16)), t.set("LUT_TEXEL_HEIGHT", (1 / n.height).toFixed(16)), r.get("domainMin").value = null, r.get("domainMax").value = null, (e.type === Y || e.type === K) && t.set("LUT_PRECISION_HIGH", "1"), n.width > n.height ? t.set("LUT_STRIP_HORIZONTAL", "1") : e instanceof _t && t.set("LUT_3D", "1"), e instanceof rt) {
        const s = e.domainMin, o = e.domainMax;
        (s.x !== 0 || s.y !== 0 || s.z !== 0 || o.x !== 1 || o.y !== 1 || o.z !== 1) && (t.set("CUSTOM_INPUT_DOMAIN", "1"), r.get("domainMin").value = s.clone(), r.get("domainMax").value = o.clone());
      }
      this.tetrahedralInterpolation = i;
    }
  }
  /**
   * Returns the current LUT.
   *
   * @deprecated Use lut instead.
   * @return {Texture} The LUT.
   */
  getLUT() {
    return this.lut;
  }
  /**
   * Sets the LUT.
   *
   * @deprecated Use lut instead.
   * @param {Texture} value - The LUT.
   */
  setLUT(e) {
    this.lut = e;
  }
  /**
   * Updates the scale and offset for the LUT sampling coordinates.
   *
   * @private
   */
  updateScaleOffset() {
    const e = this.lut;
    if (e !== null) {
      const t = Math.min(e.image.width, e.image.height), r = this.uniforms.get("scale").value, n = this.uniforms.get("offset").value;
      if (this.tetrahedralInterpolation && e instanceof _t)
        if (this.defines.has("CUSTOM_INPUT_DOMAIN")) {
          const i = e.domainMax.clone().sub(e.domainMin);
          r.setScalar(t - 1).divide(i), n.copy(e.domainMin).negate().multiply(r);
        } else
          r.setScalar(t - 1), n.setScalar(0);
      else if (this.defines.has("CUSTOM_INPUT_DOMAIN")) {
        const i = e.domainMax.clone().sub(e.domainMin).multiplyScalar(t);
        r.setScalar(t - 1).divide(i), n.copy(e.domainMin).negate().multiply(r).addScalar(1 / (2 * t));
      } else
        r.setScalar((t - 1) / t), n.setScalar(1 / (2 * t));
    }
  }
  /**
   * Configures parameters for tetrahedral interpolation.
   *
   * @private
   */
  configureTetrahedralInterpolation() {
    const e = this.lut;
    e !== null && (e.minFilter = F, e.magFilter = F, this.tetrahedralInterpolation && (e instanceof _t ? (e.minFilter = b, e.magFilter = b) : console.warn("Tetrahedral interpolation requires a 3D texture")), e.needsUpdate = !0);
  }
  /**
   * Indicates whether tetrahedral interpolation is enabled. Requires a 3D LUT, disabled by default.
   *
   * Tetrahedral interpolation produces highly accurate results but is slower than hardware interpolation.
   *
   * @type {Boolean}
   */
  get tetrahedralInterpolation() {
    return this.defines.has("TETRAHEDRAL_INTERPOLATION");
  }
  set tetrahedralInterpolation(e) {
    e ? this.defines.set("TETRAHEDRAL_INTERPOLATION", "1") : this.defines.delete("TETRAHEDRAL_INTERPOLATION"), this.configureTetrahedralInterpolation(), this.updateScaleOffset(), this.setChanged();
  }
  /**
   * Enables or disables tetrahedral interpolation.
   *
   * @deprecated Use tetrahedralInterpolation instead.
   * @param {Boolean} value - Whether tetrahedral interpolation should be enabled.
   */
  setTetrahedralInterpolationEnabled(e) {
    this.tetrahedralInterpolation = e;
  }
}
const Rt = {
  FULL: 0,
  SINGLE: 1
}, zi = {
  DEPTH: 0,
  LUMA: 1,
  COLOR: 2
}, vo = {
  DISABLED: 0,
  DEPTH: 1,
  CUSTOM: 2
}, Dt = {
  LOW: 0,
  MEDIUM: 1,
  HIGH: 2,
  ULTRA: 3
}, ye = {
  LINEAR: 0,
  REINHARD: 1,
  REINHARD2: 2,
  REINHARD2_ADAPTIVE: 3,
  UNCHARTED2: 4,
  OPTIMIZED_CINEON: 5,
  CINEON: 5,
  ACES_FILMIC: 6,
  AGX: 7,
  NEUTRAL: 8
}, Pt = {
  DEFAULT: 0,
  ESKIL: 1
}, Bh = {
  DERIVATIVES: "derivatives",
  FRAG_DEPTH: "fragDepth",
  DRAW_BUFFERS: "drawBuffers",
  SHADER_TEXTURE_LOD: "shaderTextureLOD"
}, go = `void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	vec3 noise = vec3(rand(uv * (1.0 + time)));\r
\r
	#ifdef PREMULTIPLY\r
\r
		outputColor = vec4(min(inputColor.rgb * noise, vec3(1.0)), inputColor.a);\r
\r
	#else\r
\r
		outputColor = vec4(noise, inputColor.a);\r
\r
	#endif\r
\r
}\r
`;
class Lh extends I {
  /**
   * Constructs a new noise effect.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.SCREEN] - The blend function of this effect.
   * @param {Boolean} [options.premultiply=false] - Whether the noise should be multiplied with the input colors prior to blending.
   */
  constructor({ blendFunction: e = E.SCREEN, premultiply: t = !1 } = {}) {
    super("NoiseEffect", go, { blendFunction: e }), this.premultiply = t;
  }
  /**
   * Indicates whether noise will be multiplied with the input colors prior to blending.
   *
   * @type {Boolean}
   */
  get premultiply() {
    return this.defines.has("PREMULTIPLY");
  }
  set premultiply(e) {
    this.premultiply !== e && (e ? this.defines.set("PREMULTIPLY", "1") : this.defines.delete("PREMULTIPLY"), this.setChanged());
  }
  /**
   * Indicates whether noise will be multiplied with the input colors prior to blending.
   *
   * @deprecated Use premultiply instead.
   * @return {Boolean} Whether noise is premultiplied.
   */
  isPremultiplied() {
    return this.premultiply;
  }
  /**
   * Controls whether noise should be multiplied with the input colors prior to blending.
   *
   * @deprecated Use premultiply instead.
   * @param {Boolean} value - Whether noise should be premultiplied.
   */
  setPremultiplied(e) {
    this.premultiply = e;
  }
}
const xo = `#include <packing>\r
#include <clipping_planes_pars_fragment>\r
\r
#ifdef GL_FRAGMENT_PRECISION_HIGH\r
\r
	uniform highp sampler2D depthBuffer;\r
\r
#else\r
\r
	uniform mediump sampler2D depthBuffer;\r
\r
#endif\r
\r
uniform float cameraNear;\r
uniform float cameraFar;\r
\r
centroid varying float vViewZ;\r
centroid varying vec4 vProjTexCoord;\r
\r
void main() {\r
\r
	#include <clipping_planes_fragment>\r
\r
	// Transform into Cartesian coordinates (not mirrored).\r
	vec2 projTexCoord = (vProjTexCoord.xy / vProjTexCoord.w) * 0.5 + 0.5;\r
	projTexCoord = clamp(projTexCoord, 0.002, 0.998);\r
\r
	#if DEPTH_PACKING == 3201\r
\r
		float fragCoordZ = unpackRGBAToDepth(texture2D(depthBuffer, projTexCoord));\r
\r
	#else\r
\r
		float fragCoordZ = texture2D(depthBuffer, projTexCoord).r;\r
\r
	#endif\r
\r
	#ifdef PERSPECTIVE_CAMERA\r
\r
		float viewZ = perspectiveDepthToViewZ(fragCoordZ, cameraNear, cameraFar);\r
\r
	#else\r
\r
		float viewZ = orthographicDepthToViewZ(fragCoordZ, cameraNear, cameraFar);\r
\r
	#endif\r
\r
	float depthTest = (-vViewZ > -viewZ) ? 1.0 : 0.0;\r
\r
	gl_FragColor.rg = vec2(0.0, depthTest);\r
\r
}\r
`, So = `#include <common>\r
#include <morphtarget_pars_vertex>\r
#include <skinning_pars_vertex>\r
#include <clipping_planes_pars_vertex>\r
\r
varying float vViewZ;\r
varying vec4 vProjTexCoord;\r
\r
void main() {\r
\r
	#include <skinbase_vertex>\r
\r
	#include <begin_vertex>\r
	#include <morphtarget_vertex>\r
	#include <skinning_vertex>\r
	#include <project_vertex>\r
\r
	vViewZ = mvPosition.z;\r
	vProjTexCoord = gl_Position;\r
\r
	#include <clipping_planes_vertex>\r
\r
}\r
`;
class qr extends N {
  /**
   * Constructs a new depth comparison material.
   *
   * @param {Texture} [depthTexture=null] - A depth texture.
   * @param {PerspectiveCamera} [camera] - A camera.
   */
  constructor(e = null, t) {
    super({
      name: "DepthComparisonMaterial",
      defines: {
        DEPTH_PACKING: "0"
      },
      uniforms: {
        depthBuffer: new a(null),
        cameraNear: new a(0.3),
        cameraFar: new a(1e3)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: xo,
      vertexShader: So
    }), this.depthBuffer = e, this.depthPacking = ce, this.copyCameraSettings(t);
  }
  /**
   * The depth buffer.
   *
   * @type {Texture}
   */
  set depthBuffer(e) {
    this.uniforms.depthBuffer.value = e;
  }
  /**
   * The depth packing strategy.
   *
   * @type {DepthPackingStrategies}
   */
  set depthPacking(e) {
    this.defines.DEPTH_PACKING = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Sets the depth buffer.
   *
   * @deprecated Use depthBuffer and depthPacking instead.
   * @param {Texture} buffer - The depth texture.
   * @param {DepthPackingStrategies} [depthPacking=RGBADepthPacking] - The depth packing strategy.
   */
  setDepthBuffer(e, t = ce) {
    this.depthBuffer = e, this.depthPacking = t;
  }
  /**
   * Copies the settings of the given camera.
   *
   * @deprecated Use copyCameraSettings instead.
   * @param {Camera} camera - A camera.
   */
  adoptCameraSettings(e) {
    this.copyCameraSettings(e);
  }
  /**
   * Copies the settings of the given camera.
   *
   * @param {Camera} camera - A camera.
   */
  copyCameraSettings(e) {
    e && (this.uniforms.cameraNear.value = e.near, this.uniforms.cameraFar.value = e.far, e instanceof ke ? this.defines.PERSPECTIVE_CAMERA = "1" : delete this.defines.PERSPECTIVE_CAMERA, this.needsUpdate = !0);
  }
}
const yo = `uniform lowp sampler2D inputBuffer;\r
\r
varying vec2 vUv0;\r
varying vec2 vUv1;\r
varying vec2 vUv2;\r
varying vec2 vUv3;\r
\r
void main() {\r
\r
	vec2 c0 = texture2D(inputBuffer, vUv0).rg;\r
	vec2 c1 = texture2D(inputBuffer, vUv1).rg;\r
	vec2 c2 = texture2D(inputBuffer, vUv2).rg;\r
	vec2 c3 = texture2D(inputBuffer, vUv3).rg;\r
\r
	float d0 = (c0.x - c1.x) * 0.5;\r
	float d1 = (c2.x - c3.x) * 0.5;\r
	float d = length(vec2(d0, d1));\r
\r
	float a0 = min(c0.y, c1.y);\r
	float a1 = min(c2.y, c3.y);\r
	float visibilityFactor = min(a0, a1);\r
\r
	gl_FragColor.rg = (1.0 - visibilityFactor > 0.001) ? vec2(d, 0.0) : vec2(0.0, d);\r
\r
}\r
`, Mo = `uniform vec2 texelSize;\r
\r
varying vec2 vUv0;\r
varying vec2 vUv1;\r
varying vec2 vUv2;\r
varying vec2 vUv3;\r
\r
void main() {\r
\r
	vec2 uv = position.xy * 0.5 + 0.5;\r
\r
	vUv0 = vec2(uv.x + texelSize.x, uv.y);\r
	vUv1 = vec2(uv.x - texelSize.x, uv.y);\r
	vUv2 = vec2(uv.x, uv.y + texelSize.y);\r
	vUv3 = vec2(uv.x, uv.y - texelSize.y);\r
\r
	gl_Position = vec4(position.xy, 1.0, 1.0);\r
\r
}\r
`;
class Jr extends N {
  /**
   * Constructs a new outline material.
   *
   * TODO Remove texelSize param.
   * @param {Vector2} [texelSize] - The screen texel size.
   */
  constructor(e = new g()) {
    super({
      name: "OutlineMaterial",
      uniforms: {
        inputBuffer: new a(null),
        texelSize: new a(new g())
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: yo,
      vertexShader: Mo
    }), this.uniforms.texelSize.value.set(e.x, e.y), this.uniforms.maskTexture = this.uniforms.inputBuffer;
  }
  /**
   * The input buffer.
   *
   * @type {Texture}
   */
  set inputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * Sets the input buffer.
   *
   * @deprecated Use inputBuffer instead.
   * @param {Texture} value - The input buffer.
   */
  setInputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * Sets the texel size.
   *
   * @deprecated Use setSize() instead.
   * @param {Number} x - The texel width.
   * @param {Number} y - The texel height.
   */
  setTexelSize(e, t) {
    this.uniforms.texelSize.value.set(e, t);
  }
  /**
   * Sets the size of this object.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.uniforms.texelSize.value.set(1 / e, 1 / t);
  }
}
const Fi = `uniform lowp sampler2D edgeTexture;\r
uniform lowp sampler2D maskTexture;\r
\r
uniform vec3 visibleEdgeColor;\r
uniform vec3 hiddenEdgeColor;\r
uniform float pulse;\r
uniform float edgeStrength;\r
\r
#ifdef USE_PATTERN\r
\r
	uniform lowp sampler2D patternTexture;\r
	varying vec2 vUvPattern;\r
\r
#endif\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	vec2 edge = texture2D(edgeTexture, uv).rg;\r
	vec2 mask = texture2D(maskTexture, uv).rg;\r
\r
	#ifndef X_RAY\r
\r
		edge.y = 0.0;\r
\r
	#endif\r
\r
	edge *= (edgeStrength * mask.x * pulse);\r
	vec3 color = edge.x * visibleEdgeColor + edge.y * hiddenEdgeColor;\r
\r
	float visibilityFactor = 0.0;\r
\r
	#ifdef USE_PATTERN\r
\r
		vec4 patternColor = texture2D(patternTexture, vUvPattern);\r
\r
		#ifdef X_RAY\r
\r
			float hiddenFactor = 0.5;\r
\r
		#else\r
\r
			float hiddenFactor = 0.0;\r
\r
		#endif\r
\r
		visibilityFactor = (1.0 - mask.y > 0.0) ? 1.0 : hiddenFactor;\r
		visibilityFactor *= (1.0 - mask.x) * patternColor.a;\r
		color += visibilityFactor * patternColor.rgb;\r
\r
	#endif\r
\r
	float alpha = max(max(edge.x, edge.y), visibilityFactor);\r
\r
	#ifdef ALPHA\r
\r
		// Alpha blending already accounts for input alpha.\r
		outputColor = vec4(color, alpha);\r
\r
	#else\r
\r
		// Preserve input alpha.\r
		outputColor = vec4(color, max(alpha, inputColor.a));\r
\r
	#endif\r
\r
}\r
`, Ii = `uniform float patternScale;\r
\r
varying vec2 vUvPattern;\r
\r
void mainSupport(const in vec2 uv) {\r
\r
	vUvPattern = uv * vec2(aspect, 1.0) * patternScale;\r
\r
}\r
`;
class Uh extends I {
  /**
   * Constructs a new outline effect.
   *
   * @param {Scene} scene - The main scene.
   * @param {Camera} camera - The main camera.
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.SCREEN] - The blend function. Use `BlendFunction.ALPHA` for dark outlines.
   * @param {Texture} [options.patternTexture=null] - A pattern texture.
   * @param {Number} [options.patternScale=1.0] - The pattern scale.
   * @param {Number} [options.edgeStrength=1.0] - The edge strength.
   * @param {Number} [options.pulseSpeed=0.0] - The pulse speed. A value of zero disables the pulse effect.
   * @param {Number} [options.visibleEdgeColor=0xffffff] - The color of visible edges.
   * @param {Number} [options.hiddenEdgeColor=0x22090a] - The color of hidden edges.
   * @param {KernelSize} [options.kernelSize=KernelSize.VERY_SMALL] - The blur kernel size.
   * @param {Boolean} [options.blur=false] - Whether the outline should be blurred.
   * @param {Boolean} [options.xRay=true] - Whether occluded parts of selected objects should be visible.
   * @param {Number} [options.multisampling=0] - The number of samples used for multisample antialiasing. Requires WebGL 2.
   * @param {Number} [options.resolutionScale=0.5] - The resolution scale.
   * @param {Number} [options.resolutionX=Resolution.AUTO_SIZE] - The horizontal resolution.
   * @param {Number} [options.resolutionY=Resolution.AUTO_SIZE] - The vertical resolution.
   * @param {Number} [options.width=Resolution.AUTO_SIZE] - Deprecated. Use resolutionX instead.
   * @param {Number} [options.height=Resolution.AUTO_SIZE] - Deprecated. Use resolutionY instead.
   */
  constructor(e, t, {
    blendFunction: r = E.SCREEN,
    patternTexture: n = null,
    patternScale: i = 1,
    edgeStrength: s = 1,
    pulseSpeed: o = 0,
    visibleEdgeColor: l = 16777215,
    hiddenEdgeColor: u = 2230538,
    kernelSize: f = Le.VERY_SMALL,
    blur: h = !1,
    xRay: m = !0,
    multisampling: p = 0,
    resolutionScale: d = 0.5,
    width: v = W.AUTO_SIZE,
    height: x = W.AUTO_SIZE,
    resolutionX: w = v,
    resolutionY: M = x
  } = {}) {
    super("OutlineEffect", Fi, {
      uniforms: /* @__PURE__ */ new Map([
        ["maskTexture", new a(null)],
        ["edgeTexture", new a(null)],
        ["edgeStrength", new a(s)],
        ["visibleEdgeColor", new a(new y(l))],
        ["hiddenEdgeColor", new a(new y(u))],
        ["pulse", new a(1)],
        ["patternScale", new a(i)],
        ["patternTexture", new a(null)]
      ])
    }), this.blendMode.addEventListener("change", (S) => {
      this.blendMode.blendFunction === E.ALPHA ? this.defines.set("ALPHA", "1") : this.defines.delete("ALPHA"), this.setChanged();
    }), this.blendMode.blendFunction = r, this.patternTexture = n, this.xRay = m, this.scene = e, this.camera = t, this.renderTargetMask = new A(1, 1), this.renderTargetMask.samples = p, this.renderTargetMask.texture.name = "Outline.Mask", this.uniforms.get("maskTexture").value = this.renderTargetMask.texture, this.renderTargetOutline = new A(1, 1, { depthBuffer: !1 }), this.renderTargetOutline.texture.name = "Outline.Edges", this.uniforms.get("edgeTexture").value = this.renderTargetOutline.texture, this.clearPass = new De(), this.clearPass.overrideClearColor = new y(0), this.clearPass.overrideClearAlpha = 1, this.depthPass = new Me(e, t), this.maskPass = new pe(e, t, new qr(this.depthPass.texture, t));
    const D = this.maskPass.clearPass;
    D.overrideClearColor = new y(16777215), D.overrideClearAlpha = 1, this.blurPass = new at({ resolutionScale: d, resolutionX: w, resolutionY: M, kernelSize: f }), this.blurPass.enabled = h;
    const P = this.blurPass.resolution;
    P.addEventListener("change", (S) => this.setSize(P.baseWidth, P.baseHeight)), this.outlinePass = new ee(new Jr());
    const O = this.outlinePass.fullscreenMaterial;
    O.inputBuffer = this.renderTargetMask.texture, this.time = 0, this.forceUpdate = !0, this.selection = new Se(), this.pulseSpeed = o;
  }
  set mainScene(e) {
    this.scene = e, this.depthPass.mainScene = e, this.maskPass.mainScene = e;
  }
  set mainCamera(e) {
    this.camera = e, this.depthPass.mainCamera = e, this.maskPass.mainCamera = e, this.maskPass.overrideMaterial.copyCameraSettings(e);
  }
  /**
   * The resolution of this effect.
   *
   * @type {Resolution}
   */
  get resolution() {
    return this.blurPass.resolution;
  }
  /**
   * Returns the resolution.
   *
   * @return {Resizer} The resolution.
   */
  getResolution() {
    return this.blurPass.getResolution();
  }
  /**
   * The amount of MSAA samples.
   *
   * Requires WebGL 2. Set to zero to disable multisampling.
   *
   * @experimental Requires three >= r138.
   * @type {Number}
   */
  get multisampling() {
    return this.renderTargetMask.samples;
  }
  set multisampling(e) {
    this.renderTargetMask.samples = e, this.renderTargetMask.dispose();
  }
  /**
   * The pattern scale.
   *
   * @type {Number}
   */
  get patternScale() {
    return this.uniforms.get("patternScale").value;
  }
  set patternScale(e) {
    this.uniforms.get("patternScale").value = e;
  }
  /**
   * The edge strength.
   *
   * @type {Number}
   */
  get edgeStrength() {
    return this.uniforms.get("edgeStrength").value;
  }
  set edgeStrength(e) {
    this.uniforms.get("edgeStrength").value = e;
  }
  /**
   * The visible edge color.
   *
   * @type {Color}
   */
  get visibleEdgeColor() {
    return this.uniforms.get("visibleEdgeColor").value;
  }
  set visibleEdgeColor(e) {
    this.uniforms.get("visibleEdgeColor").value = e;
  }
  /**
   * The hidden edge color.
   *
   * @type {Color}
   */
  get hiddenEdgeColor() {
    return this.uniforms.get("hiddenEdgeColor").value;
  }
  set hiddenEdgeColor(e) {
    this.uniforms.get("hiddenEdgeColor").value = e;
  }
  /**
   * Returns the blur pass.
   *
   * @deprecated Use blurPass instead.
   * @return {KawaseBlurPass} The blur pass.
   */
  getBlurPass() {
    return this.blurPass;
  }
  /**
   * Returns the selection.
   *
   * @deprecated Use selection instead.
   * @return {Selection} The selection.
   */
  getSelection() {
    return this.selection;
  }
  /**
   * Returns the pulse speed.
   *
   * @deprecated Use pulseSpeed instead.
   * @return {Number} The speed.
   */
  getPulseSpeed() {
    return this.pulseSpeed;
  }
  /**
   * Sets the pulse speed. Set to zero to disable.
   *
   * @deprecated Use pulseSpeed instead.
   * @param {Number} value - The speed.
   */
  setPulseSpeed(e) {
    this.pulseSpeed = e;
  }
  /**
   * The current width of the internal render targets.
   *
   * @type {Number}
   * @deprecated Use resolution.width instead.
   */
  get width() {
    return this.resolution.width;
  }
  set width(e) {
    this.resolution.preferredWidth = e;
  }
  /**
   * The current height of the internal render targets.
   *
   * @type {Number}
   * @deprecated Use resolution.height instead.
   */
  get height() {
    return this.resolution.height;
  }
  set height(e) {
    this.resolution.preferredHeight = e;
  }
  /**
   * The selection layer.
   *
   * @type {Number}
   * @deprecated Use selection.layer instead.
   */
  get selectionLayer() {
    return this.selection.layer;
  }
  set selectionLayer(e) {
    this.selection.layer = e;
  }
  /**
   * Indicates whether dithering is enabled.
   *
   * @type {Boolean}
   * @deprecated
   */
  get dithering() {
    return this.blurPass.dithering;
  }
  set dithering(e) {
    this.blurPass.dithering = e;
  }
  /**
   * The blur kernel size.
   *
   * @type {KernelSize}
   * @deprecated Use blurPass.kernelSize instead.
   */
  get kernelSize() {
    return this.blurPass.kernelSize;
  }
  set kernelSize(e) {
    this.blurPass.kernelSize = e;
  }
  /**
   * Indicates whether the outlines should be blurred.
   *
   * @type {Boolean}
   * @deprecated Use blurPass.enabled instead.
   */
  get blur() {
    return this.blurPass.enabled;
  }
  set blur(e) {
    this.blurPass.enabled = e;
  }
  /**
   * Indicates whether X-ray mode is enabled.
   *
   * @type {Boolean}
   */
  get xRay() {
    return this.defines.has("X_RAY");
  }
  set xRay(e) {
    this.xRay !== e && (e ? this.defines.set("X_RAY", "1") : this.defines.delete("X_RAY"), this.setChanged());
  }
  /**
   * Indicates whether X-ray mode is enabled.
   *
   * @deprecated Use xRay instead.
   * @return {Boolean} Whether X-ray mode is enabled.
   */
  isXRayEnabled() {
    return this.xRay;
  }
  /**
   * Enables or disables X-ray outlines.
   *
   * @deprecated Use xRay instead.
   * @param {Boolean} value - Whether X-ray should be enabled.
   */
  setXRayEnabled(e) {
    this.xRay = e;
  }
  /**
   * The pattern texture. Set to `null` to disable.
   *
   * @type {Texture}
   */
  get patternTexture() {
    return this.uniforms.get("patternTexture").value;
  }
  set patternTexture(e) {
    e !== null ? (e.wrapS = e.wrapT = G, this.defines.set("USE_PATTERN", "1"), this.setVertexShader(Ii)) : (this.defines.delete("USE_PATTERN"), this.setVertexShader(null)), this.uniforms.get("patternTexture").value = e, this.setChanged();
  }
  /**
   * Sets the pattern texture.
   *
   * @deprecated Use patternTexture instead.
   * @param {Texture} value - The new texture.
   */
  setPatternTexture(e) {
    this.patternTexture = e;
  }
  /**
   * Returns the current resolution scale.
   *
   * @return {Number} The resolution scale.
   * @deprecated Use resolution instead.
   */
  getResolutionScale() {
    return this.resolution.scale;
  }
  /**
   * Sets the resolution scale.
   *
   * @param {Number} scale - The new resolution scale.
   * @deprecated Use resolution instead.
   */
  setResolutionScale(e) {
    this.resolution.scale = e;
  }
  /**
   * Clears the current selection and selects a list of objects.
   *
   * @param {Object3D[]} objects - The objects that should be outlined. This array will be copied.
   * @return {OutlinePass} This pass.
   * @deprecated Use selection.set() instead.
   */
  setSelection(e) {
    return this.selection.set(e), this;
  }
  /**
   * Clears the list of selected objects.
   *
   * @return {OutlinePass} This pass.
   * @deprecated Use selection.clear() instead.
   */
  clearSelection() {
    return this.selection.clear(), this;
  }
  /**
   * Selects an object.
   *
   * @param {Object3D} object - The object that should be outlined.
   * @return {OutlinePass} This pass.
   * @deprecated Use selection.add() instead.
   */
  selectObject(e) {
    return this.selection.add(e), this;
  }
  /**
   * Deselects an object.
   *
   * @param {Object3D} object - The object that should no longer be outlined.
   * @return {OutlinePass} This pass.
   * @deprecated Use selection.delete() instead.
   */
  deselectObject(e) {
    return this.selection.delete(e), this;
  }
  /**
   * Updates this effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   */
  update(e, t, r) {
    const n = this.scene, i = this.camera, s = this.selection, l = this.uniforms.get("pulse"), u = n.background, f = i.layers.mask;
    (this.forceUpdate || s.size > 0) && (n.background = null, l.value = 1, this.pulseSpeed > 0 && (l.value = Math.cos(this.time * this.pulseSpeed * 10) * 0.375 + 0.625), this.time += r, s.setVisible(!1), this.depthPass.render(e), s.setVisible(!0), i.layers.set(s.layer), this.maskPass.render(e, this.renderTargetMask), i.layers.mask = f, n.background = u, this.outlinePass.render(e, null, this.renderTargetOutline), this.blurPass.enabled && this.blurPass.render(e, this.renderTargetOutline, this.renderTargetOutline)), this.forceUpdate = s.size > 0;
  }
  /**
   * Updates the size of internal render targets.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.blurPass.setSize(e, t), this.renderTargetMask.setSize(e, t);
    const r = this.resolution;
    r.setBaseSize(e, t);
    const n = r.width, i = r.height;
    this.depthPass.setSize(n, i), this.renderTargetOutline.setSize(n, i), this.outlinePass.fullscreenMaterial.setSize(n, i);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    this.blurPass.initialize(e, t, se), r !== void 0 && (this.depthPass.initialize(e, t, r), this.maskPass.initialize(e, t, r), this.outlinePass.initialize(e, t, r));
  }
}
class $t {
  /**
      * Constructs a new outline manager.
      *
      * @param {Scene} scene - The main scene.
      * @param {Camera} camera - The main camera.
      * @param {Object} [options] - The options.
      * @param {Number} [options.resolutionScale=1.0] - The resolution scale.
      * @param {Number} [options.multisampling=0] - The number of samples for multisampling.
      */
  constructor(e, t, {
    resolutionScale: r = 1,
    multisampling: n = 0
  } = {}) {
    this.scene = e, this.camera = t, this.selectionsByLayer = /* @__PURE__ */ new Map(), this.selection = new Se(), this.currentLayer = null, this.selectionsByLayer.set(this.selection.layer, this.selection), this.renderTargetMask = new A(1, 1), this.renderTargetMask.samples = n, this.renderTargetMask.texture.name = "Outline.Mask", this.depthPass = new Me(e, t), this.maskPass = new pe(e, t, new qr(this.depthPass.texture, t));
    const i = this.maskPass.clearPass;
    i.overrideClearColor = new y(16777215), i.overrideClearAlpha = 1, this.resolutionScale = r, this.needsUpdate = !0;
  }
  /**
      * Get a selection for a specific layer, creating it if it doesn't exist.
      *
      * @param {Number} layer - The layer to get a selection for.
      * @return {Selection} The selection for the specified layer.
      */
  getSelectionForLayer(e) {
    if (!this.selectionsByLayer.has(e)) {
      const t = new Se([], e);
      this.selectionsByLayer.set(e, t);
    }
    return this.selectionsByLayer.get(e);
  }
  /**
      * Add an object to a specific layer's selection.
      *
      * @param {Object3D} object - The object to add.
      * @param {Number} layer - The layer to add the object to.
      */
  addToLayer(e, t) {
    this.getSelectionForLayer(t).add(e), this.needsUpdate = !0;
  }
  /**
      * Remove an object from a specific layer's selection.
      *
      * @param {Object3D} object - The object to remove.
      * @param {Number} layer - The layer to remove the object from.
      * @return {Boolean} Whether the object was removed.
      */
  removeFromLayer(e, t) {
    if (this.selectionsByLayer.has(t)) {
      const n = this.selectionsByLayer.get(t).delete(e);
      return n && (this.needsUpdate = !0), n;
    }
    return !1;
  }
  /**
      * Updates the shared resources.
      *
      * @param {WebGLRenderer} renderer - The renderer.
      * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
      * @return {Boolean} Whether an update was performed.
      */
  update(e, t) {
    const r = this.scene, n = this.camera;
    let i = !1;
    for (const f of this.selectionsByLayer.values())
      if (f.size > 0) {
        i = !0;
        break;
      }
    if (!this.needsUpdate && !i)
      return !1;
    const s = Date.now();
    if (!$t.lastDepthPassTime || s - $t.lastDepthPassTime > 16) {
      console.time("SharedOutline.update");
      const f = r.background;
      n.layers.mask, r.background = null, console.time("SharedOutline.depthPass");
      for (const h of this.selectionsByLayer.values())
        h.setVisible(!1);
      this.depthPass.render(e);
      for (const h of this.selectionsByLayer.values())
        h.setVisible(!0);
      console.timeEnd("SharedOutline.depthPass"), $t.lastDepthPassTime = s, r.background = f;
    }
    console.time("SharedOutline.maskPass");
    const o = n.layers.mask;
    let l = !1;
    const u = this.currentLayer;
    return u != null && this.selectionsByLayer.has(u) && this.selectionsByLayer.get(u).size > 0 && (n.layers.set(u), this.maskPass.render(e, this.renderTargetMask), l = !0), l || (n.layers.set(this.selection.layer), this.maskPass.render(e, this.renderTargetMask)), console.timeEnd("SharedOutline.maskPass"), n.layers.mask = o, this.needsUpdate = !1, console.timeEnd("SharedOutline.update"), !0;
  }
  /**
      * Clear this manager's selection but keep track of which objects are
      * still used by active outline effects.
      *
      * @param {Set<Object3D>} activeObjects - Objects that are still part of active effects.
      * @param {Number} [exceptLayer] - A layer to exclude from clearing.
      */
  clearUnused(e, t) {
    for (const [r, n] of this.selectionsByLayer.entries()) {
      if (t !== void 0 && r === t)
        continue;
      const i = [...n];
      for (const s of i)
        e.has(s) || (n.delete(s), this.needsUpdate = !0);
    }
  }
  /**
      * Updates the size of internal render targets.
      *
      * @param {Number} width - The width.
      * @param {Number} height - The height.
      */
  setSize(e, t) {
    const r = Math.floor(e * this.resolutionScale), n = Math.floor(t * this.resolutionScale);
    this.depthPass.setSize(r, n), this.renderTargetMask.setSize(r, n);
  }
  /**
      * Performs initialization tasks.
      *
      * @param {WebGLRenderer} renderer - The renderer.
      * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
      * @param {Number} frameBufferType - The type of the main frame buffers.
      */
  initialize(e, t, r) {
    this.depthPass.initialize(e, t, r), this.maskPass.initialize(e, t, r);
  }
  /**
      * Forces the shared resources to update in the next frame.
      */
  setNeedsUpdate() {
    this.needsUpdate = !0;
  }
}
const wo = `uniform lowp sampler2D edgeTexture;
uniform lowp sampler2D maskTexture;

uniform vec3 visibleEdgeColor;
uniform vec3 hiddenEdgeColor;
uniform float pulse;
uniform float edgeStrength;

#ifdef USE_PATTERN

	uniform lowp sampler2D patternTexture;
	varying vec2 vUvPattern;

#endif

void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {

	vec2 edge = texture2D(edgeTexture, uv).rg;
	vec2 mask = texture2D(maskTexture, uv).rg;

	#ifndef X_RAY

		edge.y = 0.0;

	#endif

	edge *= (edgeStrength * mask.x * pulse);
	
	vec3 color = vec3(0.0);
	if (edge.x > 0.01 || edge.y > 0.01) {
		if (edge.x > edge.y) {
			color = visibleEdgeColor * edge.x;
		} else {
			color = hiddenEdgeColor * edge.y;
		}
	}

	float visibilityFactor = 0.0;

	#ifdef USE_PATTERN

		vec4 patternColor = texture2D(patternTexture, vUvPattern);

		#ifdef X_RAY

			float hiddenFactor = 0.5;

		#else

			float hiddenFactor = 0.0;

		#endif

		visibilityFactor = (1.0 - mask.y > 0.0) ? 1.0 : hiddenFactor;
		visibilityFactor *= (1.0 - mask.x) * patternColor.a;
		color += visibilityFactor * patternColor.rgb;

	#endif

	float alpha = max(max(edge.x, edge.y), visibilityFactor);

	#ifdef ALPHA

		// Alpha blending already accounts for input alpha.
		outputColor = vec4(color, alpha);

	#else

		// Preserve input alpha.
		outputColor = vec4(color, max(alpha, inputColor.a));

	#endif

}
`, Dn = `uniform float patternScale;\r
\r
varying vec2 vUvPattern;\r
\r
void mainSupport(const in vec2 uv) {\r
\r
	vUvPattern = uv * vec2(aspect, 1.0) * patternScale;\r
\r
}\r
`;
class Oh extends I {
  /**
   * Constructs a new outline effect.
   *
   * @param {Scene} scene - The main scene.
   * @param {Camera} camera - The main camera.
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.SCREEN] - The blend function. Use `BlendFunction.ALPHA` for dark outlines.
   * @param {Texture} [options.patternTexture=null] - A pattern texture.
   * @param {Number} [options.patternScale=1.0] - The pattern scale.
   * @param {Number} [options.edgeStrength=1.0] - The edge strength.
   * @param {Number} [options.pulseSpeed=0.0] - The pulse speed. A value of zero disables the pulse effect.
   * @param {Number} [options.visibleEdgeColor=0xffffff] - The color of visible edges.
   * @param {Number} [options.hiddenEdgeColor=0x22090a] - The color of hidden edges.
   * @param {KernelSize} [options.kernelSize=KernelSize.VERY_SMALL] - The blur kernel size.
   * @param {Boolean} [options.blur=false] - Whether the outline should be blurred.
   * @param {Boolean} [options.xRay=true] - Whether occluded parts of selected objects should be visible.
   * @param {Number} [options.multisampling=0] - The number of samples used for multisample antialiasing. Requires WebGL 2.
   * @param {Number} [options.resolutionScale=0.5] - The resolution scale.
   * @param {Number} [options.resolutionX=Resolution.AUTO_SIZE] - The horizontal resolution.
   * @param {Number} [options.resolutionY=Resolution.AUTO_SIZE] - The vertical resolution.
   * @param {Number} [options.width=Resolution.AUTO_SIZE] - Deprecated. Use resolutionX instead.
   * @param {Number} [options.height=Resolution.AUTO_SIZE] - Deprecated. Use resolutionY instead.
   * @param {Number[]} [options.layers=[20, 21, 22]] - The layers to use for the different outline colors.
   */
  constructor(e, t, {
    blendFunction: r = E.SCREEN,
    patternTexture: n = null,
    patternScale: i = 1,
    edgeStrength: s = 1,
    pulseSpeed: o = 0,
    visibleEdgeColor: l = 16777215,
    hiddenEdgeColor: u = 2230538,
    kernelSize: f = Le.VERY_SMALL,
    blur: h = !1,
    xRay: m = !0,
    multisampling: p = 0,
    resolutionScale: d = 0.5,
    width: v = W.AUTO_SIZE,
    height: x = W.AUTO_SIZE,
    resolutionX: w = v,
    resolutionY: M = x,
    layers: D = [20, 21, 22]
  } = {}) {
    super("OutlineMultiEffect", wo, {
      uniforms: /* @__PURE__ */ new Map([
        ["maskTexture", new a(null)],
        ["edgeTexture", new a(null)],
        ["edgeStrength", new a(s)],
        ["visibleEdgeColor", new a(new y(l))],
        ["hiddenEdgeColor", new a(new y(u))],
        ["pulse", new a(1)],
        ["patternScale", new a(i)],
        ["patternTexture", new a(null)]
      ])
    }), this.blendMode.addEventListener("change", (L) => {
      this.blendMode.blendFunction === E.ALPHA ? this.defines.set("ALPHA", "1") : this.defines.delete("ALPHA"), this.setChanged();
    }), this.blendMode.blendFunction = r, this.patternTexture = n, this.xRay = m, this.scene = e, this.camera = t, this.renderTargetMask = new A(1, 1), this.renderTargetMask.samples = p, this.renderTargetMask.texture.name = "Outline.Mask", this.uniforms.get("maskTexture").value = this.renderTargetMask.texture, this.renderTargetOutline = new A(1, 1, { depthBuffer: !1 }), this.renderTargetOutline.texture.name = "Outline.Edges", this.uniforms.get("edgeTexture").value = this.renderTargetOutline.texture, this.clearPass = new De(), this.clearPass.overrideClearColor = new y(0), this.clearPass.overrideClearAlpha = 1, this.depthPass = new Me(e, t), this.maskPass = new pe(e, t, new qr(this.depthPass.texture, t));
    const P = this.maskPass.clearPass;
    P.overrideClearColor = new y(16777215), P.overrideClearAlpha = 1, this.maskPass.selection = null, this.blurPass = new at({ resolutionScale: d, resolutionX: w, resolutionY: M, kernelSize: f }), this.blurPass.enabled = h;
    const O = this.blurPass.resolution;
    O.addEventListener("change", (L) => this.setSize(O.baseWidth, O.baseHeight)), this.outlinePass = new ee(new Jr());
    const S = this.outlinePass.fullscreenMaterial;
    S.inputBuffer = this.renderTargetMask.texture, this.time = 0, this.forceUpdate = !0, this.pulseSpeed = o, this.layers = D, this.layerObjectsMap = /* @__PURE__ */ new Map();
    for (const L of this.layers)
      this.layerObjectsMap.set(L, /* @__PURE__ */ new Set());
    this.outlineColors = [
      { visible: new y(1, 0, 0), hidden: new y(0.7, 0, 0) },
      // 纯红色
      { visible: new y(0, 1, 0), hidden: new y(0, 0.7, 0) },
      // 纯绿色
      { visible: new y(0, 0, 1), hidden: new y(0, 0, 0.7) }
      // 纯蓝色
    ], this.setVertexShader(Dn);
  }
  set mainScene(e) {
    this.scene = e, this.depthPass.mainScene = e, this.maskPass.mainScene = e;
  }
  set mainCamera(e) {
    this.camera = e, this.depthPass.mainCamera = e, this.maskPass.mainCamera = e, this.maskPass.overrideMaterial.copyCameraSettings(e);
  }
  /**
   * The resolution of this effect.
   *
   * @type {Resolution}
   */
  get resolution() {
    return this.blurPass.resolution;
  }
  /**
   * Returns the resolution.
   *
   * @return {Resizer} The resolution.
   */
  getResolution() {
    return this.blurPass.getResolution();
  }
  /**
   * The amount of MSAA samples.
   *
   * Requires WebGL 2. Set to zero to disable multisampling.
   *
   * @experimental Requires three >= r138.
   * @type {Number}
   */
  get multisampling() {
    return this.renderTargetMask.samples;
  }
  set multisampling(e) {
    this.renderTargetMask.samples = e, this.renderTargetMask.dispose();
  }
  /**
   * The pattern scale.
   *
   * @type {Number}
   */
  get patternScale() {
    return this.uniforms.get("patternScale").value;
  }
  set patternScale(e) {
    this.uniforms.get("patternScale").value = e;
  }
  /**
   * The edge strength.
   *
   * @type {Number}
   */
  get edgeStrength() {
    return this.uniforms.get("edgeStrength").value;
  }
  set edgeStrength(e) {
    this.uniforms.get("edgeStrength").value = e;
  }
  /**
   * The visible edge color.
   *
   * @type {Color}
   */
  get visibleEdgeColor() {
    return this.uniforms.get("visibleEdgeColor").value;
  }
  set visibleEdgeColor(e) {
    this.uniforms.get("visibleEdgeColor").value = e;
  }
  /**
   * The hidden edge color.
   *
   * @type {Color}
   */
  get hiddenEdgeColor() {
    return this.uniforms.get("hiddenEdgeColor").value;
  }
  set hiddenEdgeColor(e) {
    this.uniforms.get("hiddenEdgeColor").value = e;
  }
  /**
   * Returns the blur pass.
   *
   * @deprecated Use blurPass instead.
   * @return {KawaseBlurPass} The blur pass.
   */
  getBlurPass() {
    return this.blurPass;
  }
  /**
   * Returns the selection.
   *
   * @deprecated Use selection instead.
   * @return {Selection} The selection.
   */
  getSelection() {
    return this.selection;
  }
  /**
   * Returns the pulse speed.
   *
   * @deprecated Use pulseSpeed instead.
   * @return {Number} The speed.
   */
  getPulseSpeed() {
    return this.pulseSpeed;
  }
  /**
   * Sets the pulse speed. Set to zero to disable.
   *
   * @deprecated Use pulseSpeed instead.
   * @param {Number} value - The speed.
   */
  setPulseSpeed(e) {
    this.pulseSpeed = e;
  }
  /**
   * The current width of the internal render targets.
   *
   * @type {Number}
   * @deprecated Use resolution.width instead.
   */
  get width() {
    return this.resolution.width;
  }
  set width(e) {
    this.resolution.preferredWidth = e;
  }
  /**
   * The current height of the internal render targets.
   *
   * @type {Number}
   * @deprecated Use resolution.height instead.
   */
  get height() {
    return this.resolution.height;
  }
  set height(e) {
    this.resolution.preferredHeight = e;
  }
  /**
   * The selection layer.
   *
   * @type {Number}
   * @deprecated Use selection.layer instead.
   */
  get selectionLayer() {
    return this.selection.layer;
  }
  set selectionLayer(e) {
    this.selection.layer = e;
  }
  /**
   * Indicates whether dithering is enabled.
   *
   * @type {Boolean}
   * @deprecated
   */
  get dithering() {
    return this.blurPass.dithering;
  }
  set dithering(e) {
    this.blurPass.dithering = e;
  }
  /**
   * The blur kernel size.
   *
   * @type {KernelSize}
   * @deprecated Use blurPass.kernelSize instead.
   */
  get kernelSize() {
    return this.blurPass.kernelSize;
  }
  set kernelSize(e) {
    this.blurPass.kernelSize = e;
  }
  /**
   * Indicates whether the outlines should be blurred.
   *
   * @type {Boolean}
   * @deprecated Use blurPass.enabled instead.
   */
  get blur() {
    return this.blurPass.enabled;
  }
  set blur(e) {
    this.blurPass.enabled = e;
  }
  /**
   * Indicates whether X-ray mode is enabled.
   *
   * @type {Boolean}
   */
  get xRay() {
    return this.defines.has("X_RAY");
  }
  set xRay(e) {
    this.xRay !== e && (e ? this.defines.set("X_RAY", "1") : this.defines.delete("X_RAY"), this.setChanged());
  }
  /**
   * The pattern texture. Set to `null` to disable.
   *
   * @type {Texture}
   */
  get patternTexture() {
    return this.uniforms.get("patternTexture").value;
  }
  set patternTexture(e) {
    e !== null ? (e.wrapS = e.wrapT = G, this.defines.set("USE_PATTERN", "1"), this.setVertexShader(Dn)) : (this.defines.delete("USE_PATTERN"), this.setVertexShader(null)), this.uniforms.get("patternTexture").value = e, this.setChanged();
  }
  /**
   * Returns the current resolution scale.
   *
   * @return {Number} The resolution scale.
   * @deprecated Use resolution instead.
   */
  getResolutionScale() {
    return this.resolution.scale;
  }
  /**
   * Sets the resolution scale.
   *
   * @param {Number} scale - The new resolution scale.
   * @deprecated Use resolution instead.
   */
  setResolutionScale(e) {
    this.resolution.scale = e;
  }
  /**
   * Updates this effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   */
  update(e, t, r) {
    const n = this.scene, i = this.camera, o = this.uniforms.get("pulse"), l = n.background, u = i.layers.mask;
    n.background = null, this.time += r, o.value = this.pulseSpeed > 0 ? 0.625 + Math.cos(this.time * this.pulseSpeed * 10) * 0.375 : 1;
    let f = !1;
    for (const v of this.layerObjectsMap.values())
      if (v.size > 0) {
        f = !0;
        break;
      }
    if (!f && !this.forceUpdate) {
      i.layers.mask = u, n.background = l;
      return;
    }
    const h = /* @__PURE__ */ new Map();
    i.layers.set(0), this.depthPass.render(e), this.clearPass.overrideClearColor = new y(0), this.clearPass.render(e, this.renderTargetOutline);
    const m = this.outlinePass.fullscreenMaterial;
    m.blending, m.blendSrc, m.blendDst, m.blendEquation;
    const p = e.autoClear, d = {
      blending: vs,
      blendEquation: Lt,
      blendSrc: xn,
      blendDst: xn
      // 使用OneFactor而不是OneMinusSrcAlphaFactor以实现加法叠加
    };
    this.clearPass.render(e, this.renderTargetOutline), e.autoClear = !1, this.layers.forEach((v, x) => {
      const w = this.layerObjectsMap.get(v);
      if (w.size === 0) {
        console.log(`跳过空层 ${v} - 没有对象`);
        return;
      }
      console.log(`处理层 ${v} 中的 ${w.size} 个对象`);
      const M = Math.min(x, this.outlineColors.length - 1), D = this.outlineColors[M];
      this.clearPass.overrideClearColor = new y(16777215), this.clearPass.render(e, this.renderTargetMask), w.forEach((O) => {
        h.has(O) || h.set(O, O.layers.mask), O.layers.set(v), console.log(`设置对象 "${O.name || "unnamed"}" 到层 ${v}`);
      }), i.layers.set(v), console.log(`相机设置为查看层 ${v}`), this.maskPass.render(e, this.renderTargetMask);
      const P = this.outlinePass.fullscreenMaterial;
      P.visibleEdgeColor.copy(D.visible), P.hiddenEdgeColor.copy(D.hidden), P.edgeStrength = this.edgeStrength, P.pulse = o.value, Object.assign(P, d), this.outlinePass.render(e, null, this.renderTargetOutline), console.log(`渲染层 ${v} 的轮廓, 颜色:`, D.visible), w.forEach((O) => {
        O.layers.mask = h.get(O);
      });
    }), this.blurPass.enabled && this.blurPass.render(e, this.renderTargetOutline, this.renderTargetOutline), e.autoClear = p, i.layers.mask = u, n.background = l, this.forceUpdate = f;
  }
  /**
   * Updates the size of internal render targets.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.blurPass.setSize(e, t), this.renderTargetMask.setSize(e, t);
    const r = this.resolution;
    r.setBaseSize(e, t);
    const n = r.width, i = r.height;
    this.depthPass.setSize(n, i), this.renderTargetOutline.setSize(n, i), this.outlinePass.fullscreenMaterial.setSize(n, i);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    this.blurPass.initialize(e, t, se), r !== void 0 && (this.depthPass.initialize(e, t, r), this.maskPass.initialize(e, t, r), this.outlinePass.initialize(e, t, r));
  }
  /**
   * 添加对象到指定的层
   * @param {Object3D} object - 要添加的对象
   * @param {Number} layer - 目标层编号
   * @private
   */
  _addObjectToLayer(e, t) {
    const r = this.layerObjectsMap.get(t);
    r && (r.add(e), e.layers.enable(0), e.layers.enable(t));
  }
  /**
   * 从所有层中移除对象
   * @param {Object3D} object - 要移除的对象
   * @private
   */
  _removeObjectFromAllLayers(e) {
    for (const [t, r] of this.layerObjectsMap.entries())
      r.delete(e), e.layers.disable(t);
  }
  /**
   * 设置对象的轮廓颜色层
   * @param {Object3D} object - 要设置颜色的对象
   * @param {Number} layerIndex - 层索引(0-2)，对应this.layers数组的索引
   * @returns {OutlineMultiEffect} - 返回this以支持链式调用
   */
  setOutlineColorLayer(e, t) {
    t = Math.max(0, Math.min(this.layers.length - 1, t)), this._removeObjectFromAllLayers(e);
    const r = this.layers[t];
    return this._addObjectToLayer(e, r), this.forceUpdate = !0, this;
  }
  /**
   * 设置为红色轮廓 (层索引0)
   * @param {Object3D} object - 要应用红色轮廓的对象
   * @returns {OutlineMultiEffect} - 返回this以支持链式调用
   */
  setRedOutline(e) {
    return this.setOutlineColorLayer(e, 0);
  }
  /**
   * 设置为绿色轮廓 (层索引1)
   * @param {Object3D} object - 要应用绿色轮廓的对象
   * @returns {OutlineMultiEffect} - 返回this以支持链式调用
   */
  setGreenOutline(e) {
    return this.setOutlineColorLayer(e, 1);
  }
  /**
   * 设置为蓝色轮廓 (层索引2)
   * @param {Object3D} object - 要应用蓝色轮廓的对象
   * @returns {OutlineMultiEffect} - 返回this以支持链式调用
   */
  setBlueOutline(e) {
    return this.setOutlineColorLayer(e, 2);
  }
  /**
   * 移除对象的轮廓效果
   * @param {Object3D} object - 要移除轮廓的对象
   * @returns {OutlineMultiEffect} - 返回this以支持链式调用
   */
  removeOutline(e) {
    return this._removeObjectFromAllLayers(e), this.forceUpdate = !0, this;
  }
  /**
   * 自定义轮廓颜色
   * @param {Number} layerIndex - 层索引(0-2)
   * @param {Number|Color} visibleColor - 可见部分的轮廓颜色
   * @param {Number|Color} hiddenColor - 隐藏部分的轮廓颜色
   * @returns {OutlineMultiEffect} - 返回this以支持链式调用
   */
  setOutlineColor(e, t, r) {
    return e >= 0 && e < this.outlineColors.length && (t !== void 0 && (this.outlineColors[e].visible = t instanceof y ? t.clone() : new y(t)), r !== void 0 && (this.outlineColors[e].hidden = r instanceof y ? r.clone() : new y(r)), this.forceUpdate = !0), this;
  }
}
const To = `uniform bool active;\r
uniform vec4 d;\r
\r
void mainUv(inout vec2 uv) {\r
\r
	if(active) {\r
\r
		uv = d.xy * (floor(uv * d.zw) + 0.5);\r
\r
	}\r
\r
}\r
`;
class kh extends I {
  /**
   * Constructs a new pixelation effect.
   *
   * @param {Object} [granularity=30.0] - The pixel granularity.
   */
  constructor(e = 30) {
    super("PixelationEffect", To, {
      uniforms: /* @__PURE__ */ new Map([
        ["active", new a(!1)],
        ["d", new a(new ze())]
      ])
    }), this.resolution = new g(), this._granularity = 0, this.granularity = e;
  }
  /**
   * The pixel granularity.
   *
   * A higher value yields coarser visuals.
   *
   * @type {Number}
   */
  get granularity() {
    return this._granularity;
  }
  set granularity(e) {
    let t = Math.floor(e);
    t % 2 > 0 && (t += 1), this._granularity = t, this.uniforms.get("active").value = t > 0, this.setSize(this.resolution.width, this.resolution.height);
  }
  /**
   * Returns the pixel granularity.
   *
   * @deprecated Use granularity instead.
   * @return {Number} The granularity.
   */
  getGranularity() {
    return this.granularity;
  }
  /**
   * Sets the pixel granularity.
   *
   * @deprecated Use granularity instead.
   * @param {Number} value - The new granularity.
   */
  setGranularity(e) {
    this.granularity = e;
  }
  /**
   * Updates the granularity.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.resolution;
    r.set(e, t);
    const n = this.granularity, i = n / r.x, s = n / r.y;
    this.uniforms.get("d").value.set(i, s, 1 / i, 1 / s);
  }
}
const bo = `uniform float focus;\r
uniform float focalLength;\r
uniform float fStop;\r
uniform float maxBlur;\r
uniform float luminanceThreshold;\r
uniform float luminanceGain;\r
uniform float bias;\r
uniform float fringe;\r
\r
#ifdef MANUAL_DOF\r
\r
	uniform vec4 dof;\r
\r
#endif\r
\r
#ifdef PENTAGON\r
\r
	float pentagon(const in vec2 coords) {\r
\r
		const vec4 HS0 = vec4( 1.0,          0.0,         0.0, 1.0);\r
		const vec4 HS1 = vec4( 0.309016994,  0.951056516, 0.0, 1.0);\r
		const vec4 HS2 = vec4(-0.809016994,  0.587785252, 0.0, 1.0);\r
		const vec4 HS3 = vec4(-0.809016994, -0.587785252, 0.0, 1.0);\r
		const vec4 HS4 = vec4( 0.309016994, -0.951056516, 0.0, 1.0);\r
		const vec4 HS5 = vec4( 0.0,          0.0,         1.0, 1.0);\r
\r
		const vec4 ONE = vec4(1.0);\r
\r
		const float P_FEATHER = 0.4;\r
		const float N_FEATHER = -P_FEATHER;\r
\r
		float inOrOut = -4.0;\r
\r
		vec4 P = vec4(coords, vec2(RINGS_FLOAT - 1.3));\r
\r
		vec4 dist = vec4(\r
			dot(P, HS0),\r
			dot(P, HS1),\r
			dot(P, HS2),\r
			dot(P, HS3)\r
		);\r
\r
		dist = smoothstep(N_FEATHER, P_FEATHER, dist);\r
\r
		inOrOut += dot(dist, ONE);\r
\r
		dist.x = dot(P, HS4);\r
		dist.y = HS5.w - abs(P.z);\r
\r
		dist = smoothstep(N_FEATHER, P_FEATHER, dist);\r
		inOrOut += dist.x;\r
\r
		return clamp(inOrOut, 0.0, 1.0);\r
\r
	}\r
\r
#endif\r
\r
vec3 processTexel(const in vec2 coords, const in float blur) {\r
\r
	vec2 scale = texelSize * fringe * blur;\r
\r
	vec3 c = vec3(\r
		texture2D(inputBuffer, coords + vec2(0.0, 1.0) * scale).r,\r
		texture2D(inputBuffer, coords + vec2(-0.866, -0.5) * scale).g,\r
		texture2D(inputBuffer, coords + vec2(0.866, -0.5) * scale).b\r
	);\r
\r
	// Calculate the luminance of the constructed color.\r
	float luminance = linearToRelativeLuminance(c);\r
	float threshold = max((luminance - luminanceThreshold) * luminanceGain, 0.0);\r
\r
	return c + mix(vec3(0.0), c, threshold * blur);\r
\r
}\r
\r
float gather(const in float i, const in float j, const in float ringSamples,\r
	const in vec2 uv, const in vec2 blurFactor, const in float blur, inout vec3 color) {\r
\r
	float step = PI2 / ringSamples;\r
	vec2 wh = vec2(cos(j * step) * i, sin(j * step) * i);\r
\r
	#ifdef PENTAGON\r
\r
		float p = pentagon(wh);\r
\r
	#else\r
\r
		float p = 1.0;\r
\r
	#endif\r
\r
	color += processTexel(wh * blurFactor + uv, blur) * mix(1.0, i / RINGS_FLOAT, bias) * p;\r
\r
	return mix(1.0, i / RINGS_FLOAT, bias) * p;\r
\r
}\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, const in float depth, out vec4 outputColor) {\r
\r
	#ifdef PERSPECTIVE_CAMERA\r
\r
		float viewZ = perspectiveDepthToViewZ(depth, cameraNear, cameraFar);\r
		float linearDepth = viewZToOrthographicDepth(viewZ, cameraNear, cameraFar);\r
\r
	#else\r
\r
		float linearDepth = depth;\r
\r
	#endif\r
\r
	#ifdef MANUAL_DOF\r
\r
		float focalPlane = linearDepth - focus;\r
		float farDoF = (focalPlane - dof.z) / dof.w;\r
		float nearDoF = (-focalPlane - dof.x) / dof.y;\r
\r
		float blur = (focalPlane > 0.0) ? farDoF : nearDoF;\r
\r
	#else\r
\r
		const float CIRCLE_OF_CONFUSION = 0.03; // 35mm film = 0.03mm CoC.\r
\r
		float focalPlaneMM = focus * 1000.0;\r
		float depthMM = linearDepth * 1000.0;\r
\r
		float focalPlane = (depthMM * focalLength) / (depthMM - focalLength);\r
		float farDoF = (focalPlaneMM * focalLength) / (focalPlaneMM - focalLength);\r
		float nearDoF = (focalPlaneMM - focalLength) / (focalPlaneMM * fStop * CIRCLE_OF_CONFUSION);\r
\r
		float blur = abs(focalPlane - farDoF) * nearDoF;\r
\r
	#endif\r
\r
	const int MAX_RING_SAMPLES = RINGS_INT * SAMPLES_INT;\r
\r
	blur = clamp(blur, 0.0, 1.0);\r
	vec3 color = inputColor.rgb;\r
\r
	if(blur >= 0.05) {\r
\r
		vec2 blurFactor = blur * maxBlur * texelSize;\r
\r
		float s = 1.0;\r
		int ringSamples;\r
\r
		for(int i = 1; i <= RINGS_INT; i++) {\r
\r
			ringSamples = i * SAMPLES_INT;\r
\r
			for(int j = 0; j < MAX_RING_SAMPLES; j++) {\r
\r
				if(j >= ringSamples) {\r
\r
					break;\r
\r
				}\r
\r
				s += gather(float(i), float(j), float(ringSamples), uv, blurFactor, blur, color);\r
\r
			}\r
\r
		}\r
\r
		color /= s;\r
\r
	}\r
\r
	#ifdef SHOW_FOCUS\r
\r
		float edge = 0.002 * linearDepth;\r
		float m = clamp(smoothstep(0.0, edge, blur), 0.0, 1.0);\r
		float e = clamp(smoothstep(1.0 - edge, 1.0, blur), 0.0, 1.0);\r
\r
		color = mix(color, vec3(1.0, 0.5, 0.0), (1.0 - m) * 0.6);\r
		color = mix(color, vec3(0.0, 0.5, 1.0), ((1.0 - e) - (1.0 - m)) * 0.2);\r
\r
	#endif\r
\r
	outputColor = vec4(color, inputColor.a);\r
\r
}\r
`;
class Vh extends I {
  /**
   * Constructs a new bokeh effect.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction] - The blend function of this effect.
   * @param {Number} [options.focus=1.0] - The focus distance in world units.
   * @param {Number} [options.focalLength=24.0] - The focal length of the main camera.
   * @param {Number} [options.fStop=0.9] - The ratio of the lens focal length to the diameter of the entrance pupil (aperture).
   * @param {Number} [options.luminanceThreshold=0.5] - A luminance threshold.
   * @param {Number} [options.luminanceGain=2.0] - A luminance gain factor.
   * @param {Number} [options.bias=0.5] - A blur bias.
   * @param {Number} [options.fringe=0.7] - A blur offset.
   * @param {Number} [options.maxBlur=1.0] - The maximum blur strength.
   * @param {Boolean} [options.rings=3] - The number of blur iterations.
   * @param {Boolean} [options.samples=2] - The amount of samples taken per ring.
   * @param {Boolean} [options.showFocus=false] - Whether the focal point should be highlighted. Useful for debugging.
   * @param {Boolean} [options.manualDoF=false] - Enables manual control over the depth of field.
   * @param {Boolean} [options.pentagon=false] - Enables pentagonal blur shapes. Requires a high number of rings and samples.
   */
  constructor({
    blendFunction: e,
    focus: t = 1,
    focalLength: r = 24,
    fStop: n = 0.9,
    luminanceThreshold: i = 0.5,
    luminanceGain: s = 2,
    bias: o = 0.5,
    fringe: l = 0.7,
    maxBlur: u = 1,
    rings: f = 3,
    samples: h = 2,
    showFocus: m = !1,
    manualDoF: p = !1,
    pentagon: d = !1
  } = {}) {
    super("RealisticBokehEffect", bo, {
      blendFunction: e,
      attributes: Q.CONVOLUTION | Q.DEPTH,
      uniforms: /* @__PURE__ */ new Map([
        ["focus", new a(t)],
        ["focalLength", new a(r)],
        ["fStop", new a(n)],
        ["luminanceThreshold", new a(i)],
        ["luminanceGain", new a(s)],
        ["bias", new a(o)],
        ["fringe", new a(l)],
        ["maxBlur", new a(u)],
        ["dof", new a(null)]
      ])
    }), this.rings = f, this.samples = h, this.showFocus = m, this.manualDoF = p, this.pentagon = d;
  }
  /**
   * The amount of blur iterations.
   *
   * @type {Number}
   */
  get rings() {
    return Number.parseInt(this.defines.get("RINGS_INT"));
  }
  set rings(e) {
    const t = Math.floor(e);
    this.defines.set("RINGS_INT", t.toFixed(0)), this.defines.set("RINGS_FLOAT", t.toFixed(1)), this.setChanged();
  }
  /**
   * The amount of blur samples per ring.
   *
   * @type {Number}
   */
  get samples() {
    return Number.parseInt(this.defines.get("SAMPLES_INT"));
  }
  set samples(e) {
    const t = Math.floor(e);
    this.defines.set("SAMPLES_INT", t.toFixed(0)), this.defines.set("SAMPLES_FLOAT", t.toFixed(1)), this.setChanged();
  }
  /**
   * Indicates whether the focal point will be highlighted.
   *
   * @type {Boolean}
   */
  get showFocus() {
    return this.defines.has("SHOW_FOCUS");
  }
  set showFocus(e) {
    this.showFocus !== e && (e ? this.defines.set("SHOW_FOCUS", "1") : this.defines.delete("SHOW_FOCUS"), this.setChanged());
  }
  /**
   * Indicates whether the Depth of Field should be calculated manually.
   *
   * If enabled, the Depth of Field can be adjusted via the `dof` uniform.
   *
   * @type {Boolean}
   */
  get manualDoF() {
    return this.defines.has("MANUAL_DOF");
  }
  set manualDoF(e) {
    this.manualDoF !== e && (e ? (this.defines.set("MANUAL_DOF", "1"), this.uniforms.get("dof").value = new ze(0.2, 1, 0.2, 2)) : (this.defines.delete("MANUAL_DOF"), this.uniforms.get("dof").value = null), this.setChanged());
  }
  /**
   * Indicates whether the blur shape should be pentagonal.
   *
   * @type {Boolean}
   */
  get pentagon() {
    return this.defines.has("PENTAGON");
  }
  set pentagon(e) {
    this.pentagon !== e && (e ? this.defines.set("PENTAGON", "1") : this.defines.delete("PENTAGON"), this.setChanged());
  }
}
const Do = `uniform float count;\r
\r
#ifdef SCROLL\r
\r
	uniform float scrollSpeed;\r
\r
#endif\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	float y = uv.y;\r
\r
	#ifdef SCROLL\r
\r
		y += time * scrollSpeed;\r
\r
	#endif\r
\r
	vec2 sl = vec2(sin(y * count), cos(y * count));\r
	outputColor = vec4(sl.xyx, inputColor.a);\r
\r
}\r
`;
class Hh extends I {
  /**
   * Constructs a new scanline effect.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.OVERLAY] - The blend function of this effect.
   * @param {Number} [options.density=1.25] - The scanline density.
   * @param {Number} [options.scrollSpeed=0.0] - The scanline scroll speed.
   */
  constructor({ blendFunction: e = E.OVERLAY, density: t = 1.25, scrollSpeed: r = 0 } = {}) {
    super("ScanlineEffect", Do, {
      blendFunction: e,
      uniforms: /* @__PURE__ */ new Map([
        ["count", new a(0)],
        ["scrollSpeed", new a(0)]
      ])
    }), this.resolution = new g(), this.d = t, this.scrollSpeed = r;
  }
  /**
   * The scanline density.
   *
   * @type {Number}
   */
  get density() {
    return this.d;
  }
  set density(e) {
    this.d = e, this.setSize(this.resolution.width, this.resolution.height);
  }
  /**
   * Returns the current scanline density.
   *
   * @deprecated Use density instead.
   * @return {Number} The scanline density.
   */
  getDensity() {
    return this.density;
  }
  /**
   * Sets the scanline density.
   *
   * @deprecated Use density instead.
   * @param {Number} value - The new scanline density.
   */
  setDensity(e) {
    this.density = e;
  }
  /**
   * The scanline scroll speed. Default is 0 (disabled).
   *
   * @type {Number}
   */
  get scrollSpeed() {
    return this.uniforms.get("scrollSpeed").value;
  }
  set scrollSpeed(e) {
    this.uniforms.get("scrollSpeed").value = e, e === 0 ? this.defines.delete("SCROLL") && this.setChanged() : this.defines.has("SCROLL") || (this.defines.set("SCROLL", "1"), this.setChanged());
  }
  /**
   * Updates the size of this pass.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.resolution.set(e, t), this.uniforms.get("count").value = Math.round(t * this.density);
  }
}
const Po = `uniform bool active;\r
uniform vec2 center;\r
uniform float waveSize;\r
uniform float radius;\r
uniform float maxRadius;\r
uniform float amplitude;\r
\r
varying float vSize;\r
\r
void mainUv(inout vec2 uv) {\r
\r
	if(active) {\r
\r
		vec2 aspectCorrection = vec2(aspect, 1.0);\r
		vec2 difference = uv * aspectCorrection - center * aspectCorrection;\r
		float distance = sqrt(dot(difference, difference)) * vSize;\r
\r
		if(distance > radius) {\r
\r
			if(distance < radius + waveSize) {\r
\r
				float angle = (distance - radius) * PI2 / waveSize;\r
				float cosSin = (1.0 - cos(angle)) * 0.5;\r
\r
				float extent = maxRadius + waveSize;\r
				float decay = max(extent - distance * distance, 0.0) / extent;\r
\r
				uv -= ((cosSin * amplitude * difference) / distance) * decay;\r
\r
			}\r
\r
		}\r
\r
	}\r
\r
}\r
`, Co = `uniform float size;\r
uniform float cameraDistance;\r
\r
varying float vSize;\r
\r
void mainSupport() {\r
\r
	vSize = (0.1 * cameraDistance) / size;\r
\r
}\r
`, Ao = Math.PI * 0.5, Ct = /* @__PURE__ */ new T(), Pn = /* @__PURE__ */ new T();
class Gh extends I {
  /**
   * Constructs a new shock wave effect.
   *
   * @param {Camera} camera - The main camera.
   * @param {Vector3} [position] - The world position of the shock wave.
   * @param {Object} [options] - The options.
   * @param {Number} [options.speed=2.0] - The animation speed.
   * @param {Number} [options.maxRadius=1.0] - The extent of the shock wave.
   * @param {Number} [options.waveSize=0.2] - The wave size.
   * @param {Number} [options.amplitude=0.05] - The distortion amplitude.
   */
  constructor(e, t = new T(), {
    speed: r = 2,
    maxRadius: n = 1,
    waveSize: i = 0.2,
    amplitude: s = 0.05
  } = {}) {
    super("ShockWaveEffect", Po, {
      vertexShader: Co,
      uniforms: /* @__PURE__ */ new Map([
        ["active", new a(!1)],
        ["center", new a(new g(0.5, 0.5))],
        ["cameraDistance", new a(1)],
        ["size", new a(1)],
        ["radius", new a(-i)],
        ["maxRadius", new a(n)],
        ["waveSize", new a(i)],
        ["amplitude", new a(s)]
      ])
    }), this.position = t, this.speed = r, this.camera = e, this.screenPosition = this.uniforms.get("center").value, this.time = 0, this.active = !1;
  }
  set mainCamera(e) {
    this.camera = e;
  }
  /**
   * The amplitude.
   *
   * @type {Number}
   */
  get amplitude() {
    return this.uniforms.get("amplitude").value;
  }
  set amplitude(e) {
    this.uniforms.get("amplitude").value = e;
  }
  /**
   * The wave size.
   *
   * @type {Number}
   */
  get waveSize() {
    return this.uniforms.get("waveSize").value;
  }
  set waveSize(e) {
    this.uniforms.get("waveSize").value = e;
  }
  /**
   * The maximum radius.
   *
   * @type {Number}
   */
  get maxRadius() {
    return this.uniforms.get("maxRadius").value;
  }
  set maxRadius(e) {
    this.uniforms.get("maxRadius").value = e;
  }
  /**
   * The position of the shock wave.
   *
   * @type {Vector3}
   * @deprecated Use position instead.
   */
  get epicenter() {
    return this.position;
  }
  set epicenter(e) {
    this.position = e;
  }
  /**
   * Returns the position of the shock wave.
   *
   * @deprecated Use position instead.
   * @return {Vector3} The position.
   */
  getPosition() {
    return this.position;
  }
  /**
   * Sets the position of the shock wave.
   *
   * @deprecated Use position instead.
   * @param {Vector3} value - The position.
   */
  setPosition(e) {
    this.position = e;
  }
  /**
   * Returns the speed of the shock wave.
   *
   * @deprecated Use speed instead.
   * @return {Number} The speed.
   */
  getSpeed() {
    return this.speed;
  }
  /**
   * Sets the speed of the shock wave.
   *
   * @deprecated Use speed instead.
   * @param {Number} value - The speed.
   */
  setSpeed(e) {
    this.speed = e;
  }
  /**
   * Emits the shock wave.
   */
  explode() {
    this.time = 0, this.active = !0, this.uniforms.get("active").value = !0;
  }
  /**
   * Updates this effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {Number} [delta] - The time between the last frame and the current one in seconds.
   */
  update(e, t, r) {
    const n = this.position, i = this.camera, s = this.uniforms, o = s.get("active");
    if (this.active) {
      const l = s.get("waveSize").value;
      i.getWorldDirection(Ct), Pn.copy(i.position).sub(n), o.value = Ct.angleTo(Pn) > Ao, o.value && (s.get("cameraDistance").value = i.position.distanceTo(n), Ct.copy(n).project(i), this.screenPosition.set((Ct.x + 1) * 0.5, (Ct.y + 1) * 0.5)), this.time += r * this.speed;
      const u = this.time - l;
      s.get("radius").value = u, u >= (s.get("maxRadius").value + l) * 2 && (this.active = !1, o.value = !1);
    }
  }
}
class Wh extends za {
  /**
   * Constructs a new selective bloom effect.
   *
   * @param {Scene} scene - The main scene.
   * @param {Camera} camera - The main camera.
   * @param {Object} [options] - The options. See {@link BloomEffect} for details.
   * @param {Object} [options.gBufferTextures=null] - G-Buffer textures from RenderPass. If provided, will use gDepth for main scene depth.
   * @param {Texture} [options.gBufferTextures.gDepth] - G-Buffer depth texture.
   */
  constructor(e, t, r = {}) {
    super(r), this.setAttributes(this.getAttributes() | Q.DEPTH), this.scene = e, this.camera = t, this.gBufferTextures = r.gBufferTextures || null, this.depthPass = new Me(e, t), this.clearPass = new De(!0, !1, !1), this.clearPass.overrideClearColor = new y(0), this.depthMaskPass = new ee(new _e());
    const n = this.depthMaskMaterial;
    n.copyCameraSettings(t), n.depthBuffer1 = this.depthPass.texture, n.depthPacking1 = ce, n.depthMode = Ue, this.depthMaskMaterial.epsilon = 9e-6, this.gBufferTextures && this.gBufferTextures.gDepth && (n.depthBuffer0 = this.gBufferTextures.gDepth, n.depthPacking0 = $), this.renderTargetMasked = new A(1, 1, { depthBuffer: !1 }), this.renderTargetMasked.texture.name = "Bloom.Masked", this.selection = new Se(), this._inverted = !1, this._ignoreBackground = !1;
  }
  set mainScene(e) {
    this.depthPass.mainScene = e;
  }
  set mainCamera(e) {
    this.camera = e, this.depthPass.mainCamera = e, this.depthMaskMaterial.copyCameraSettings(e);
  }
  /**
   * Returns the selection.
   *
   * @deprecated Use selection instead.
   * @return {Selection} The selection.
   */
  getSelection() {
    return this.selection;
  }
  /**
   * The depth mask material.
   *
   * @type {DepthMaskMaterial}
   * @private
   */
  get depthMaskMaterial() {
    return this.depthMaskPass.fullscreenMaterial;
  }
  /**
   * Indicates whether the selection should be considered inverted.
   *
   * @type {Boolean}
   */
  get inverted() {
    return this._inverted;
  }
  set inverted(e) {
    this._inverted = e, this.depthMaskMaterial.depthMode = e ? Mt : Ue;
  }
  /**
   * Indicates whether the mask is inverted.
   *
   * @deprecated Use inverted instead.
   * @return {Boolean} Whether the mask is inverted.
   */
  isInverted() {
    return this.inverted;
  }
  /**
   * Enables or disable mask inversion.
   *
   * @deprecated Use inverted instead.
   * @param {Boolean} value - Whether the mask should be inverted.
   */
  setInverted(e) {
    this.inverted = e;
  }
  /**
   * Indicates whether the background colors will be ignored.
   *
   * @type {Boolean}
   */
  get ignoreBackground() {
    return this._ignoreBackground;
  }
  set ignoreBackground(e) {
    this._ignoreBackground = e, this.depthMaskMaterial.maxDepthStrategy = e ? we.DISCARD_MAX_DEPTH : we.KEEP_MAX_DEPTH;
  }
  /**
   * Indicates whether the background is disabled.
   *
   * @deprecated Use ignoreBackground instead.
   * @return {Boolean} Whether the background is disabled.
   */
  isBackgroundDisabled() {
    return this.ignoreBackground;
  }
  /**
   * Enables or disables the background.
   *
   * @deprecated Use ignoreBackground instead.
   * @param {Boolean} value - Whether the background should be disabled.
   */
  setBackgroundDisabled(e) {
    this.ignoreBackground = e;
  }
  /**
   * Sets the depth texture.
   *
   * @param {Texture} depthTexture - A depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing.
   */
  setDepthTexture(e, t = $) {
    this.depthMaskMaterial.depthBuffer0 = e, this.depthMaskMaterial.depthPacking0 = t;
  }
  /**
   * Sets the G-Buffer textures.
   *
   * @param {Object|null} gBufferTextures - The G-Buffer textures.
   * @param {Texture} [gBufferTextures.gDepth] - The depth texture from G-Buffer.
   */
  setGBufferTextures(e) {
    this.gBufferTextures = e, e && e.gDepth ? (console.log("🎯 SelectiveBloomEffect.setGBufferTextures: 启用 GBuffer 深度纹理"), console.log("  - gDepth:", !!e.gDepth), this.depthMaskMaterial.depthBuffer0 = e.gDepth, this.depthMaskMaterial.depthPacking0 = $) : console.log("🎯 SelectiveBloomEffect.setGBufferTextures: 禁用 GBuffer，需要手动设置深度纹理");
  }
  /**
   * Updates this effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {DepthPass} [depthPass] - An optional shared depth pass for optimized rendering.
   */
  update(e, t, r, n) {
    const i = this.camera, s = this.selection, o = this.inverted;
    let l = t;
    if (ve("SelectiveBloomEffect.update"), bi("SelectiveBloomEffect update called, selection size:", s.size), this.ignoreBackground || !o || s.size > 0) {
      {
        ve("SelectiveBloomEffect.update.depthPass");
        const u = i.layers.mask;
        i.layers.set(s.layer), this.depthPass.render(e, void 0, void 0, void 0, void 0, void 0, {
          projectObject: !0,
          updateMatrixWorld: !1,
          useProgramCache: !1
        }), i.layers.mask = u, ge("SelectiveBloomEffect.update.depthPass");
      }
      ve("SelectiveBloomEffect.update.maskRender"), l = this.renderTargetMasked, this.clearPass.render(e, l), this.depthMaskPass.render(e, t, l, void 0, void 0, {
        projectObject: !0,
        updateMatrixWorld: !1,
        useProgramCache: !1
      }), ge("SelectiveBloomEffect.update.maskRender");
    }
    ve("SelectiveBloomEffect.update.superUpdate"), super.update(e, l, r), ge("SelectiveBloomEffect.update.superUpdate"), ge("SelectiveBloomEffect.update");
  }
  /**
   * Updates the size of internal render targets.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    super.setSize(e, t), this.renderTargetMasked.setSize(e, t), this.depthPass.setSize(e, t);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    super.initialize(e, t, r), this.clearPass.initialize(e, t, r), this.depthPass.initialize(e, t, r), this.depthMaskPass.initialize(e, t, r), e !== null && e.capabilities.logarithmicDepthBuffer && (this.depthMaskPass.fullscreenMaterial.defines.LOG_DEPTH = "1"), r !== void 0 && (this.renderTargetMasked.texture.type = r, e !== null && e.outputColorSpace === V && (this.renderTargetMasked.texture.colorSpace = V));
  }
}
const Eo = `uniform vec3 weightsR;\r
uniform vec3 weightsG;\r
uniform vec3 weightsB;\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	vec3 color = vec3(\r
		dot(inputColor.rgb, weightsR),\r
		dot(inputColor.rgb, weightsG),\r
		dot(inputColor.rgb, weightsB)\r
	);\r
\r
	outputColor = vec4(color, inputColor.a);\r
\r
}\r
`;
class jh extends I {
  /**
   * Constructs a new sepia effect.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction] - The blend function of this effect.
   * @param {Number} [options.intensity=1.0] - The intensity of the effect.
   */
  constructor({ blendFunction: e, intensity: t = 1 } = {}) {
    super("SepiaEffect", Eo, {
      blendFunction: e,
      uniforms: /* @__PURE__ */ new Map([
        ["weightsR", new a(new T(0.393, 0.769, 0.189))],
        ["weightsG", new a(new T(0.349, 0.686, 0.168))],
        ["weightsB", new a(new T(0.272, 0.534, 0.131))]
      ])
    });
  }
  /**
   * The intensity.
   *
   * @deprecated Use blendMode.opacity instead.
   * @type {Number}
   */
  get intensity() {
    return this.blendMode.opacity.value;
  }
  set intensity(e) {
    this.blendMode.opacity.value = e;
  }
  /**
   * Returns the current sepia intensity.
   *
   * @deprecated Use blendMode.opacity instead.
   * @return {Number} The intensity.
   */
  getIntensity() {
    return this.intensity;
  }
  /**
   * Sets the sepia intensity.
   *
   * @deprecated Use blendMode.opacity instead.
   * @param {Number} value - The intensity.
   */
  setIntensity(e) {
    this.intensity = e;
  }
  /**
   * The weights for the red channel. Default is `(0.393, 0.769, 0.189)`.
   *
   * @type {Vector3}
   */
  get weightsR() {
    return this.uniforms.get("weightsR").value;
  }
  /**
   * The weights for the green channel. Default is `(0.349, 0.686, 0.168)`.
   *
   * @type {Vector3}
   */
  get weightsG() {
    return this.uniforms.get("weightsG").value;
  }
  /**
   * The weights for the blue channel. Default is `(0.272, 0.534, 0.131)`.
   *
   * @type {Vector3}
   */
  get weightsB() {
    return this.uniforms.get("weightsB").value;
  }
}
class de extends I {
  /**
      * Constructs a new shared outline effect.
      *
      * @param {OutlineManager} outlineManager - The outline manager with shared resources.
      * @param {Object} [options] - The options.
      * @param {BlendFunction} [options.blendFunction=BlendFunction.SCREEN] - The blend function. Use `BlendFunction.ALPHA` for dark outlines.
      * @param {Texture} [options.patternTexture=null] - A pattern texture.
      * @param {Number} [options.patternScale=1.0] - The pattern scale.
      * @param {Number} [options.edgeStrength=1.0] - The edge strength.
      * @param {Number} [options.pulseSpeed=0.0] - The pulse speed. A value of zero disables the pulse effect.
      * @param {Number} [options.visibleEdgeColor=0xffffff] - The color of visible edges.
      * @param {Number} [options.hiddenEdgeColor=0x22090a] - The color of hidden edges.
      * @param {KernelSize} [options.kernelSize=KernelSize.VERY_SMALL] - The blur kernel size.
      * @param {Boolean} [options.blur=false] - Whether the outline should be blurred.
      * @param {Boolean} [options.xRay=true] - Whether occluded parts of selected objects should be visible.
      * @param {Number} [options.resolutionScale=0.5] - The resolution scale.
      * @param {Number} [options.resolutionX=Resolution.AUTO_SIZE] - The horizontal resolution.
      * @param {Number} [options.resolutionY=Resolution.AUTO_SIZE] - The vertical resolution.
      */
  constructor(e, {
    blendFunction: t = E.SCREEN,
    patternTexture: r = null,
    patternScale: n = 1,
    edgeStrength: i = 1,
    pulseSpeed: s = 0,
    visibleEdgeColor: o = 16777215,
    hiddenEdgeColor: l = 2230538,
    kernelSize: u = Le.VERY_SMALL,
    blur: f = !1,
    xRay: h = !0,
    resolutionScale: m = 0.5,
    resolutionX: p = W.AUTO_SIZE,
    resolutionY: d = W.AUTO_SIZE
  } = {}) {
    super("SharedOutlineEffect", Fi, {
      uniforms: /* @__PURE__ */ new Map([
        ["maskTexture", new a(null)],
        ["edgeTexture", new a(null)],
        ["edgeStrength", new a(i)],
        ["visibleEdgeColor", new a(new y(o))],
        ["hiddenEdgeColor", new a(new y(l))],
        ["pulse", new a(1)],
        ["patternScale", new a(n)],
        ["patternTexture", new a(null)]
      ])
    }), this.blendMode.addEventListener("change", (x) => {
      this.blendMode.blendFunction === E.ALPHA ? this.defines.set("ALPHA", "1") : this.defines.delete("ALPHA"), this.setChanged();
    }), this.blendMode.blendFunction = t, this.patternTexture = r, this.xRay = h, this.manager = e, this.renderTargetOutline = new A(1, 1, { depthBuffer: !1 }), this.renderTargetOutline.texture.name = "Outline.Edges", this.uniforms.get("edgeTexture").value = this.renderTargetOutline.texture, this.uniforms.get("maskTexture").value = e.renderTargetMask.texture, this.blurPass = new at({
      resolutionScale: m,
      resolutionX: p,
      resolutionY: d,
      kernelSize: u
    }), this.blurPass.enabled = f, this.outlinePass = new ee(new Jr());
    const v = this.outlinePass.fullscreenMaterial;
    v.inputBuffer = e.renderTargetMask.texture, this.time = 0, this.selection = new Se(), this.pulseSpeed = s, this._selectionLayer = this.selection.layer, globalThis.SharedOutlineEffect === void 0 && (globalThis.SharedOutlineEffect = de);
  }
  /**
      * The resolution of this effect.
      *
      * @type {Resolution}
      */
  get resolution() {
    return this.blurPass.resolution;
  }
  /**
      * Returns the resolution.
      *
      * @return {Resolution} The resolution.
      */
  getResolution() {
    return this.blurPass.getResolution();
  }
  /**
      * The pattern scale.
      *
      * @type {Number}
      */
  get patternScale() {
    return this.uniforms.get("patternScale").value;
  }
  set patternScale(e) {
    this.uniforms.get("patternScale").value = e;
  }
  /**
      * The edge strength.
      *
      * @type {Number}
      */
  get edgeStrength() {
    return this.uniforms.get("edgeStrength").value;
  }
  set edgeStrength(e) {
    this.uniforms.get("edgeStrength").value = e;
  }
  /**
      * The visible edge color.
      *
      * @type {Color}
      */
  get visibleEdgeColor() {
    return this.uniforms.get("visibleEdgeColor").value;
  }
  set visibleEdgeColor(e) {
    this.uniforms.get("visibleEdgeColor").value = e;
  }
  /**
      * The hidden edge color.
      *
      * @type {Color}
      */
  get hiddenEdgeColor() {
    return this.uniforms.get("hiddenEdgeColor").value;
  }
  set hiddenEdgeColor(e) {
    this.uniforms.get("hiddenEdgeColor").value = e;
  }
  /**
      * Indicates whether X-ray mode is enabled.
      *
      * @type {Boolean}
      */
  get xRay() {
    return this.defines.has("X_RAY");
  }
  set xRay(e) {
    this.xRay !== e && (e ? this.defines.set("X_RAY", "1") : this.defines.delete("X_RAY"), this.setChanged());
  }
  /**
      * The pattern texture. Set to `null` to disable.
      *
      * @type {Texture}
      */
  get patternTexture() {
    return this.uniforms.get("patternTexture").value;
  }
  set patternTexture(e) {
    e !== null ? (e.wrapS = e.wrapT = G, this.defines.set("USE_PATTERN", "1"), this.setVertexShader(Ii)) : (this.defines.delete("USE_PATTERN"), this.setVertexShader(null)), this.uniforms.get("patternTexture").value = e, this.setChanged();
  }
  /**
      * Updates this effect.
      *
      * @param {WebGLRenderer} renderer - The renderer.
      * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
      * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
      */
  update(e, t, r) {
    console.time(`SharedOutlineEffect.update[layer=${this._selectionLayer}]`), console.log(`开始渲染轮廓效果 - 层: ${this._selectionLayer}, 选中对象数: ${this.selection.size}`), de.activeObjects === void 0 && (de.activeObjects = /* @__PURE__ */ new Set()), de.currentLayer === void 0 && (de.currentLayer = null), de.updateCounter === void 0 && (de.updateCounter = 0), de.updateCounter++;
    const n = de.updateCounter;
    if (console.log(`轮廓渲染计数: ${n} - 层: ${this._selectionLayer}`), this.manager.currentLayer = this._selectionLayer, de.currentLayer = this._selectionLayer, this.selection.size > 0)
      for (const o of this.selection)
        de.activeObjects.add(o);
    const s = this.uniforms.get("pulse");
    if (s.value = 1, this.pulseSpeed > 0 && (s.value = Math.cos(this.time * this.pulseSpeed * 10) * 0.375 + 0.625), this.time += r, this.selection.size > 0) {
      const o = this.manager.getSelectionForLayer(this._selectionLayer);
      for (const l of this.selection)
        o.has(l) || (o.add(l), this.manager.setNeedsUpdate());
      this.manager.update(e, r), console.time(`SharedOutlineEffect.outlinePass[layer=${this._selectionLayer}]`), this.outlinePass.render(e, null, this.renderTargetOutline), console.timeEnd(`SharedOutlineEffect.outlinePass[layer=${this._selectionLayer}]`), this.blurPass.enabled && (console.time(`SharedOutlineEffect.blurPass[layer=${this._selectionLayer}]`), this.blurPass.render(e, this.renderTargetOutline, this.renderTargetOutline), console.timeEnd(`SharedOutlineEffect.blurPass[layer=${this._selectionLayer}]`));
    }
    n === 2 && (this.manager.clearUnused(de.activeObjects, this._selectionLayer), de.activeObjects.clear(), de.updateCounter = 0, de.currentLayer = null), console.log(`完成渲染轮廓效果 - 层: ${this._selectionLayer}`), console.timeEnd(`SharedOutlineEffect.update[layer=${this._selectionLayer}]`);
  }
  /**
      * Updates the size of internal render targets.
      *
      * @param {Number} width - The width.
      * @param {Number} height - The height.
      */
  setSize(e, t) {
    this.blurPass.setSize(e, t);
    const r = this.resolution;
    r.setBaseSize(e, t);
    const n = r.width, i = r.height;
    this.renderTargetOutline.setSize(n, i), this.outlinePass.fullscreenMaterial.setSize(n, i);
  }
  /**
      * Performs initialization tasks.
      *
      * @param {WebGLRenderer} renderer - The renderer.
      * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
      * @param {Number} frameBufferType - The type of the main frame buffers.
      */
  initialize(e, t, r) {
    this.blurPass.initialize(e, t, r), this.outlinePass.initialize(e, t, r);
  }
  /**
      * The selection layer.
      *
      * @type {Number}
      */
  get selectionLayer() {
    return this._selectionLayer;
  }
  set selectionLayer(e) {
    this._selectionLayer, this._selectionLayer = e;
    const t = [...this.selection];
    this.selection.clear(), this.selection = new Se([], e);
    for (const r of t)
      this.selection.add(r);
    this.manager && this.manager.setNeedsUpdate();
  }
}
const Ro = `varying vec2 vUv;\r
varying vec2 vUv0;\r
varying vec2 vUv1;\r
\r
#if EDGE_DETECTION_MODE != 0\r
\r
	varying vec2 vUv2;\r
	varying vec2 vUv3;\r
	varying vec2 vUv4;\r
	varying vec2 vUv5;\r
\r
#endif\r
\r
#if EDGE_DETECTION_MODE == 1\r
\r
	#include <common>\r
\r
#endif\r
\r
#if EDGE_DETECTION_MODE == 0 || PREDICATION_MODE == 1\r
\r
	#ifdef GL_FRAGMENT_PRECISION_HIGH\r
\r
		uniform highp sampler2D depthBuffer;\r
\r
	#else\r
\r
		uniform mediump sampler2D depthBuffer;\r
\r
	#endif\r
\r
	float readDepth(const in vec2 uv) {\r
\r
		#if DEPTH_PACKING == 3201\r
\r
			return unpackRGBAToDepth(texture2D(depthBuffer, uv));\r
\r
		#else\r
\r
			return texture2D(depthBuffer, uv).r;\r
\r
		#endif\r
\r
	}\r
\r
	vec3 gatherNeighbors() {\r
\r
		float p = readDepth(vUv);\r
		float pLeft = readDepth(vUv0);\r
		float pTop = readDepth(vUv1);\r
\r
		return vec3(p, pLeft, pTop);\r
\r
	}\r
\r
#elif PREDICATION_MODE == 2\r
\r
	uniform sampler2D predicationBuffer;\r
\r
	vec3 gatherNeighbors() {\r
\r
		float p = texture2D(predicationBuffer, vUv).r;\r
		float pLeft = texture2D(predicationBuffer, vUv0).r;\r
		float pTop = texture2D(predicationBuffer, vUv1).r;\r
\r
		return vec3(p, pLeft, pTop);\r
\r
	}\r
\r
#endif\r
\r
#if PREDICATION_MODE != 0\r
\r
	vec2 calculatePredicatedThreshold() {\r
\r
		vec3 neighbours = gatherNeighbors();\r
		vec2 delta = abs(neighbours.xx - neighbours.yz);\r
		vec2 edges = step(PREDICATION_THRESHOLD, delta);\r
\r
		return PREDICATION_SCALE * EDGE_THRESHOLD * (1.0 - PREDICATION_STRENGTH * edges);\r
\r
	}\r
\r
#endif\r
\r
#if EDGE_DETECTION_MODE != 0\r
\r
	uniform sampler2D inputBuffer;\r
\r
#endif\r
\r
void main() {\r
\r
	#if EDGE_DETECTION_MODE == 0\r
\r
		const vec2 threshold = vec2(DEPTH_THRESHOLD);\r
\r
	#elif PREDICATION_MODE != 0\r
\r
		vec2 threshold = calculatePredicatedThreshold();\r
\r
	#else\r
\r
		const vec2 threshold = vec2(EDGE_THRESHOLD);\r
\r
	#endif\r
\r
	#if EDGE_DETECTION_MODE == 0\r
\r
		// Depth-based edge detection.\r
\r
		vec3 neighbors = gatherNeighbors();\r
		vec2 delta = abs(neighbors.xx - vec2(neighbors.y, neighbors.z));\r
		vec2 edges = step(threshold, delta);\r
\r
		if(dot(edges, vec2(1.0)) == 0.0) {\r
\r
			discard;\r
\r
		}\r
\r
		gl_FragColor = vec4(edges, 0.0, 1.0);\r
\r
	#elif EDGE_DETECTION_MODE == 1\r
\r
		// Luma-based edge detection.\r
\r
		float l = luminance(texture2D(inputBuffer, vUv).rgb);\r
		float lLeft = luminance(texture2D(inputBuffer, vUv0).rgb);\r
		float lTop  = luminance(texture2D(inputBuffer, vUv1).rgb);\r
\r
		vec4 delta;\r
		delta.xy = abs(l - vec2(lLeft, lTop));\r
\r
		vec2 edges = step(threshold, delta.xy);\r
\r
		if(dot(edges, vec2(1.0)) == 0.0) {\r
\r
			discard;\r
\r
		}\r
\r
		// Calculate right and bottom deltas.\r
		float lRight = luminance(texture2D(inputBuffer, vUv2).rgb);\r
		float lBottom  = luminance(texture2D(inputBuffer, vUv3).rgb);\r
		delta.zw = abs(l - vec2(lRight, lBottom));\r
\r
		// Calculate the maximum delta in the direct neighborhood.\r
		vec2 maxDelta = max(delta.xy, delta.zw);\r
\r
		// Calculate left-left and top-top deltas.\r
		float lLeftLeft = luminance(texture2D(inputBuffer, vUv4).rgb);\r
		float lTopTop = luminance(texture2D(inputBuffer, vUv5).rgb);\r
		delta.zw = abs(vec2(lLeft, lTop) - vec2(lLeftLeft, lTopTop));\r
\r
		// Calculate the final maximum delta.\r
		maxDelta = max(maxDelta.xy, delta.zw);\r
		float finalDelta = max(maxDelta.x, maxDelta.y);\r
\r
		// Local contrast adaptation.\r
		edges.xy *= step(finalDelta, LOCAL_CONTRAST_ADAPTATION_FACTOR * delta.xy);\r
\r
		gl_FragColor = vec4(edges, 0.0, 1.0);\r
\r
	#elif EDGE_DETECTION_MODE == 2\r
\r
		// Chroma-based edge detection.\r
\r
		vec4 delta;\r
		vec3 c = texture2D(inputBuffer, vUv).rgb;\r
\r
		vec3 cLeft = texture2D(inputBuffer, vUv0).rgb;\r
		vec3 t = abs(c - cLeft);\r
		delta.x = max(max(t.r, t.g), t.b);\r
\r
		vec3 cTop = texture2D(inputBuffer, vUv1).rgb;\r
		t = abs(c - cTop);\r
		delta.y = max(max(t.r, t.g), t.b);\r
\r
		vec2 edges = step(threshold, delta.xy);\r
\r
		if(dot(edges, vec2(1.0)) == 0.0) {\r
\r
			discard;\r
\r
		}\r
\r
		// Calculate right and bottom deltas.\r
		vec3 cRight = texture2D(inputBuffer, vUv2).rgb;\r
		t = abs(c - cRight);\r
		delta.z = max(max(t.r, t.g), t.b);\r
\r
		vec3 cBottom = texture2D(inputBuffer, vUv3).rgb;\r
		t = abs(c - cBottom);\r
		delta.w = max(max(t.r, t.g), t.b);\r
\r
		// Calculate the maximum delta in the direct neighborhood.\r
		vec2 maxDelta = max(delta.xy, delta.zw);\r
\r
		// Calculate left-left and top-top deltas.\r
		vec3 cLeftLeft = texture2D(inputBuffer, vUv4).rgb;\r
		t = abs(c - cLeftLeft);\r
		delta.z = max(max(t.r, t.g), t.b);\r
\r
		vec3 cTopTop = texture2D(inputBuffer, vUv5).rgb;\r
		t = abs(c - cTopTop);\r
		delta.w = max(max(t.r, t.g), t.b);\r
\r
		// Calculate the final maximum delta.\r
		maxDelta = max(maxDelta.xy, delta.zw);\r
		float finalDelta = max(maxDelta.x, maxDelta.y);\r
\r
		// Local contrast adaptation.\r
		edges *= step(finalDelta, LOCAL_CONTRAST_ADAPTATION_FACTOR * delta.xy);\r
\r
		gl_FragColor = vec4(edges, 0.0, 1.0);\r
\r
	#endif\r
\r
}\r
`, No = `uniform vec2 texelSize;\r
\r
varying vec2 vUv;\r
varying vec2 vUv0;\r
varying vec2 vUv1;\r
\r
#if EDGE_DETECTION_MODE != 0\r
\r
	varying vec2 vUv2;\r
	varying vec2 vUv3;\r
	varying vec2 vUv4;\r
	varying vec2 vUv5;\r
\r
#endif\r
\r
void main() {\r
\r
	vUv = position.xy * 0.5 + 0.5;\r
\r
	// Left and top texel coordinates.\r
	vUv0 = vUv + texelSize * vec2(-1.0, 0.0);\r
	vUv1 = vUv + texelSize * vec2(0.0, -1.0);\r
\r
	#if EDGE_DETECTION_MODE != 0\r
\r
		// Right and bottom texel coordinates.\r
		vUv2 = vUv + texelSize * vec2(1.0, 0.0);\r
		vUv3 = vUv + texelSize * vec2(0.0, 1.0);\r
\r
		// Left-left and top-top texel coordinates.\r
		vUv4 = vUv + texelSize * vec2(-2.0, 0.0);\r
		vUv5 = vUv + texelSize * vec2(0.0, -2.0);\r
\r
	#endif\r
\r
	gl_Position = vec4(position.xy, 1.0, 1.0);\r
\r
}\r
`;
class zo extends N {
  /**
   * Constructs a new edge detection material.
   *
   * TODO Remove parameters.
   * @param {Vector2} [texelSize] - The screen texel size.
   * @param {EdgeDetectionMode} [mode=EdgeDetectionMode.COLOR] - The edge detection mode.
   */
  constructor(e = new g(), t = zi.COLOR) {
    super({
      name: "EdgeDetectionMaterial",
      defines: {
        THREE_REVISION: Vt.replace(/\D+/g, ""),
        LOCAL_CONTRAST_ADAPTATION_FACTOR: "2.0",
        EDGE_THRESHOLD: "0.1",
        DEPTH_THRESHOLD: "0.01",
        PREDICATION_MODE: "0",
        PREDICATION_THRESHOLD: "0.01",
        PREDICATION_SCALE: "2.0",
        PREDICATION_STRENGTH: "1.0",
        DEPTH_PACKING: "0"
      },
      uniforms: {
        inputBuffer: new a(null),
        depthBuffer: new a(null),
        predicationBuffer: new a(null),
        texelSize: new a(e)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: Ro,
      vertexShader: No
    }), this.edgeDetectionMode = t;
  }
  /**
   * The depth buffer.
   *
   * @type {Texture}
   */
  set depthBuffer(e) {
    this.uniforms.depthBuffer.value = e;
  }
  /**
   * The depth packing strategy.
   *
   * @type {DepthPackingStrategies}
   */
  set depthPacking(e) {
    this.defines.DEPTH_PACKING = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Sets the depth buffer.
   *
   * @deprecated Use depthBuffer and depthPacking instead.
   * @param {Texture} buffer - The depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing strategy.
   */
  setDepthBuffer(e, t = $) {
    this.depthBuffer = e, this.depthPacking = t;
  }
  /**
   * The edge detection mode.
   *
   * @type {EdgeDetectionMode}
   */
  get edgeDetectionMode() {
    return Number(this.defines.EDGE_DETECTION_MODE);
  }
  set edgeDetectionMode(e) {
    this.defines.EDGE_DETECTION_MODE = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Returns the edge detection mode.
   *
   * @deprecated Use edgeDetectionMode instead.
   * @return {EdgeDetectionMode} The mode.
   */
  getEdgeDetectionMode() {
    return this.edgeDetectionMode;
  }
  /**
   * Sets the edge detection mode.
   *
   * @deprecated Use edgeDetectionMode instead.
   * @param {EdgeDetectionMode} value - The edge detection mode.
   */
  setEdgeDetectionMode(e) {
    this.edgeDetectionMode = e;
  }
  /**
   * The local contrast adaptation factor. Has no effect if the edge detection mode is set to DEPTH. Default is 2.0.
   *
   * If a neighbor edge has _factor_ times bigger contrast than the current edge, the edge will be discarded.
   *
   * This allows to eliminate spurious crossing edges and is based on the fact that if there is too much contrast in a
   * direction, the perceptual contrast in the other neighbors will be hidden.
   *
   * @type {Number}
   */
  get localContrastAdaptationFactor() {
    return Number(this.defines.LOCAL_CONTRAST_ADAPTATION_FACTOR);
  }
  set localContrastAdaptationFactor(e) {
    this.defines.LOCAL_CONTRAST_ADAPTATION_FACTOR = e.toFixed("6"), this.needsUpdate = !0;
  }
  /**
   * Returns the local contrast adaptation factor.
   *
   * @deprecated Use localContrastAdaptationFactor instead.
   * @return {Number} The factor.
   */
  getLocalContrastAdaptationFactor() {
    return this.localContrastAdaptationFactor;
  }
  /**
   * Sets the local contrast adaptation factor. Has no effect if the edge detection mode is set to DEPTH.
   *
   * @deprecated Use localContrastAdaptationFactor instead.
   * @param {Number} value - The local contrast adaptation factor. Default is 2.0.
   */
  setLocalContrastAdaptationFactor(e) {
    this.localContrastAdaptationFactor = e;
  }
  /**
   * The edge detection threshold. Range: [0.0, 0.5].
   *
   * A lower value results in more edges being detected at the expense of performance.
   *
   * For luma- and chroma-based edge detection, 0.1 is a reasonable value and allows to catch most visible edges. 0.05
   * is a rather overkill value that allows to catch 'em all. Darker scenes may require an even lower threshold.
   *
   * If depth-based edge detection is used, the threshold will depend on the scene depth.
   *
   * @type {Number}
   */
  get edgeDetectionThreshold() {
    return Number(this.defines.EDGE_THRESHOLD);
  }
  set edgeDetectionThreshold(e) {
    this.defines.EDGE_THRESHOLD = e.toFixed("6"), this.defines.DEPTH_THRESHOLD = (e * 0.1).toFixed("6"), this.needsUpdate = !0;
  }
  /**
   * Returns the edge detection threshold.
   *
   * @deprecated Use edgeDetectionThreshold instead.
   * @return {Number} The threshold.
   */
  getEdgeDetectionThreshold() {
    return this.edgeDetectionThreshold;
  }
  /**
   * Sets the edge detection threshold.
   *
   * @deprecated Use edgeDetectionThreshold instead.
   * @param {Number} value - The edge detection threshold. Range: [0.0, 0.5].
   */
  setEdgeDetectionThreshold(e) {
    this.edgeDetectionThreshold = e;
  }
  /**
   * The predication mode.
   *
   * Predicated thresholding allows to better preserve texture details and to improve edge detection using an additional
   * buffer such as a light accumulation or depth buffer.
   *
   * @type {PredicationMode}
   */
  get predicationMode() {
    return Number(this.defines.PREDICATION_MODE);
  }
  set predicationMode(e) {
    this.defines.PREDICATION_MODE = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Returns the predication mode.
   *
   * @deprecated Use predicationMode instead.
   * @return {PredicationMode} The mode.
   */
  getPredicationMode() {
    return this.predicationMode;
  }
  /**
   * Sets the predication mode.
   *
   * @deprecated Use predicationMode instead.
   * @param {PredicationMode} value - The predication mode.
   */
  setPredicationMode(e) {
    this.predicationMode = e;
  }
  /**
   * The predication buffer.
   *
   * @type {Texture}
   */
  set predicationBuffer(e) {
    this.uniforms.predicationBuffer.value = e;
  }
  /**
   * Sets a custom predication buffer.
   *
   * @deprecated Use predicationBuffer instead.
   * @param {Texture} value - The predication buffer.
   */
  setPredicationBuffer(e) {
    this.uniforms.predicationBuffer.value = e;
  }
  /**
   * The predication threshold.
   *
   * @type {Number}
   */
  get predicationThreshold() {
    return Number(this.defines.PREDICATION_THRESHOLD);
  }
  set predicationThreshold(e) {
    this.defines.PREDICATION_THRESHOLD = e.toFixed("6"), this.needsUpdate = !0;
  }
  /**
   * Returns the predication threshold.
   *
   * @deprecated Use predicationThreshold instead.
   * @return {Number} The threshold.
   */
  getPredicationThreshold() {
    return this.predicationThreshold;
  }
  /**
   * Sets the predication threshold.
   *
   * @deprecated Use predicationThreshold instead.
   * @param {Number} value - The threshold.
   */
  setPredicationThreshold(e) {
    this.predicationThreshold = e;
  }
  /**
   * The predication scale. Range: [1.0, 5.0].
   *
   * Determines how much the edge detection threshold should be scaled when using predication.
   *
   * @type {Boolean|Texture|Number}
   */
  get predicationScale() {
    return Number(this.defines.PREDICATION_SCALE);
  }
  set predicationScale(e) {
    this.defines.PREDICATION_SCALE = e.toFixed("6"), this.needsUpdate = !0;
  }
  /**
   * Returns the predication scale.
   *
   * @deprecated Use predicationScale instead.
   * @return {Number} The scale.
   */
  getPredicationScale() {
    return this.predicationScale;
  }
  /**
   * Sets the predication scale.
   *
   * @deprecated Use predicationScale instead.
   * @param {Number} value - The scale. Range: [1.0, 5.0].
   */
  setPredicationScale(e) {
    this.predicationScale = e;
  }
  /**
   * The predication strength. Range: [0.0, 1.0].
   *
   * Determines how much the edge detection threshold should be decreased locally when using predication.
   *
   * @type {Number}
   */
  get predicationStrength() {
    return Number(this.defines.PREDICATION_STRENGTH);
  }
  set predicationStrength(e) {
    this.defines.PREDICATION_STRENGTH = e.toFixed("6"), this.needsUpdate = !0;
  }
  /**
   * Returns the predication strength.
   *
   * @deprecated Use predicationStrength instead.
   * @return {Number} The strength.
   */
  getPredicationStrength() {
    return this.predicationStrength;
  }
  /**
   * Sets the predication strength.
   *
   * @deprecated Use predicationStrength instead.
   * @param {Number} value - The strength. Range: [0.0, 1.0].
   */
  setPredicationStrength(e) {
    this.predicationStrength = e;
  }
  /**
   * Sets the size of this object.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.uniforms.texelSize.value.set(1 / e, 1 / t);
  }
}
const Fo = `#define sampleLevelZeroOffset(t, coord, offset) texture2D(t, coord + offset * texelSize)\r
\r
#if __VERSION__ < 300\r
\r
	#define round(v) floor(v + 0.5)\r
\r
#endif\r
\r
#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D inputBuffer;\r
\r
#else\r
\r
	uniform lowp sampler2D inputBuffer;\r
\r
#endif\r
\r
uniform lowp sampler2D areaTexture;\r
uniform lowp sampler2D searchTexture;\r
\r
uniform vec2 texelSize;\r
uniform vec2 resolution;\r
\r
varying vec2 vUv;\r
varying vec4 vOffset[3];\r
varying vec2 vPixCoord;\r
\r
\r
/**\r
 * Moves values to a target vector based on a given conditional vector.\r
 */\r
\r
void movec(const in bvec2 c, inout vec2 variable, const in vec2 value) {\r
\r
	if(c.x) { variable.x = value.x; }\r
	if(c.y) { variable.y = value.y; }\r
\r
}\r
\r
void movec(const in bvec4 c, inout vec4 variable, const in vec4 value) {\r
\r
	movec(c.xy, variable.xy, value.xy);\r
	movec(c.zw, variable.zw, value.zw);\r
\r
}\r
\r
/**\r
 * Allows to decode two binary values from a bilinear-filtered access.\r
 *\r
 * Bilinear access for fetching 'e' have a 0.25 offset, and we are interested\r
 * in the R and G edges:\r
 *\r
 * +---G---+-------+\r
 * |   x o R   x   |\r
 * +-------+-------+\r
 *\r
 * Then, if one of these edge is enabled:\r
 *  Red: (0.75 * X + 0.25 * 1) => 0.25 or 1.0\r
 *  Green: (0.75 * 1 + 0.25 * X) => 0.75 or 1.0\r
 *\r
 * This function will unpack the values (mad + mul + round):\r
 * wolframalpha.com: round(x * abs(5 * x - 5 * 0.75)) plot 0 to 1\r
 */\r
\r
vec2 decodeDiagBilinearAccess(in vec2 e) {\r
\r
	e.r = e.r * abs(5.0 * e.r - 5.0 * 0.75);\r
\r
	return round(e);\r
\r
}\r
\r
vec4 decodeDiagBilinearAccess(in vec4 e) {\r
\r
	e.rb = e.rb * abs(5.0 * e.rb - 5.0 * 0.75);\r
\r
	return round(e);\r
\r
}\r
\r
/**\r
 * Diagonal pattern searches.\r
 */\r
\r
vec2 searchDiag1(const in vec2 texCoord, const in vec2 dir, out vec2 e) {\r
\r
	vec4 coord = vec4(texCoord, -1.0, 1.0);\r
	vec3 t = vec3(texelSize, 1.0);\r
\r
	for(int i = 0; i < MAX_SEARCH_STEPS_INT; ++i) {\r
\r
		if(!(coord.z < float(MAX_SEARCH_STEPS_DIAG_INT - 1) && coord.w > 0.9)) {\r
\r
			break;\r
\r
		}\r
\r
		coord.xyz = t * vec3(dir, 1.0) + coord.xyz;\r
		e = texture2D(inputBuffer, coord.xy).rg;\r
		coord.w = dot(e, vec2(0.5));\r
\r
	}\r
\r
	return coord.zw;\r
\r
}\r
\r
vec2 searchDiag2(const in vec2 texCoord, const in vec2 dir, out vec2 e) {\r
\r
	vec4 coord = vec4(texCoord, -1.0, 1.0);\r
	coord.x += 0.25 * texelSize.x; // See @SearchDiag2Optimization\r
	vec3 t = vec3(texelSize, 1.0);\r
\r
	for(int i = 0; i < MAX_SEARCH_STEPS_INT; ++i) {\r
\r
		if(!(coord.z < float(MAX_SEARCH_STEPS_DIAG_INT - 1) && coord.w > 0.9)) {\r
\r
			break;\r
\r
		}\r
\r
		coord.xyz = t * vec3(dir, 1.0) + coord.xyz;\r
\r
		// @SearchDiag2Optimization\r
		// Fetch both edges at once using bilinear filtering.\r
		e = texture2D(inputBuffer, coord.xy).rg;\r
		e = decodeDiagBilinearAccess(e);\r
\r
		// Non-optimized version:\r
		// e.g = texture2D(inputBuffer, coord.xy).g;\r
		// e.r = SMAASampleLevelZeroOffset(inputBuffer, coord.xy, vec2(1, 0)).r;\r
\r
		coord.w = dot(e, vec2(0.5));\r
\r
	}\r
\r
	return coord.zw;\r
\r
}\r
\r
/**\r
 * Calculates the area corresponding to a certain diagonal distance and crossing\r
 * edges 'e'.\r
 */\r
\r
vec2 areaDiag(const in vec2 dist, const in vec2 e, const in float offset) {\r
\r
	vec2 texCoord = vec2(AREATEX_MAX_DISTANCE_DIAG, AREATEX_MAX_DISTANCE_DIAG) * e + dist;\r
\r
	// Apply a scale and bias for mapping to texel space.\r
	texCoord = AREATEX_PIXEL_SIZE * texCoord + 0.5 * AREATEX_PIXEL_SIZE;\r
\r
	// Diagonal areas are on the second half of the texture.\r
	texCoord.x += 0.5;\r
\r
	// Move to the proper place, according to the subpixel offset.\r
	texCoord.y += AREATEX_SUBTEX_SIZE * offset;\r
\r
	return texture2D(areaTexture, texCoord).rg;\r
\r
}\r
\r
/**\r
 * Searches for diagonal patterns and returns the corresponding weights.\r
 */\r
\r
vec2 calculateDiagWeights(const in vec2 texCoord, const in vec2 e, const in vec4 subsampleIndices) {\r
\r
	vec2 weights = vec2(0.0);\r
\r
	// Search for the line ends.\r
	vec4 d;\r
	vec2 end;\r
\r
	if(e.r > 0.0) {\r
\r
		d.xz = searchDiag1(texCoord, vec2(-1.0,  1.0), end);\r
		d.x += float(end.y > 0.9);\r
\r
	} else {\r
\r
		d.xz = vec2(0.0);\r
\r
	}\r
\r
	d.yw = searchDiag1(texCoord, vec2(1.0, -1.0), end);\r
\r
	if(d.x + d.y > 2.0) { // d.x + d.y + 1 > 3\r
\r
		// Fetch the crossing edges.\r
		vec4 coords = vec4(-d.x + 0.25, d.x, d.y, -d.y - 0.25) * texelSize.xyxy + texCoord.xyxy;\r
		vec4 c;\r
		c.xy = sampleLevelZeroOffset(inputBuffer, coords.xy, vec2(-1, 0)).rg;\r
		c.zw = sampleLevelZeroOffset(inputBuffer, coords.zw, vec2(1, 0)).rg;\r
		c.yxwz = decodeDiagBilinearAccess(c.xyzw);\r
\r
		// Non-optimized version:\r
		// vec4 coords = vec4(-d.x, d.x, d.y, -d.y) * texelSize.xyxy + texCoord.xyxy;\r
		// vec4 c;\r
		// c.x = sampleLevelZeroOffset(inputBuffer, coords.xy, vec2(-1, 0)).g;\r
		// c.y = sampleLevelZeroOffset(inputBuffer, coords.xy, vec2(0, 0)).r;\r
		// c.z = sampleLevelZeroOffset(inputBuffer, coords.zw, vec2(1, 0)).g;\r
		// c.w = sampleLevelZeroOffset(inputBuffer, coords.zw, vec2(1, -1)).r;\r
\r
		// Merge crossing edges at each side into a single value.\r
		vec2 cc = vec2(2.0) * c.xz + c.yw;\r
\r
		// Remove the crossing edge if no end of the line could be found.\r
		movec(bvec2(step(0.9, d.zw)), cc, vec2(0.0));\r
\r
		// Fetch the areas for this line.\r
		weights += areaDiag(d.xy, cc, subsampleIndices.z);\r
\r
	}\r
\r
	// Search for the line ends.\r
	d.xz = searchDiag2(texCoord, vec2(-1.0, -1.0), end);\r
\r
	if(sampleLevelZeroOffset(inputBuffer, texCoord, vec2(1, 0)).r > 0.0) {\r
\r
		d.yw = searchDiag2(texCoord, vec2(1.0), end);\r
		d.y += float(end.y > 0.9);\r
\r
	} else {\r
\r
		d.yw = vec2(0.0);\r
\r
	}\r
\r
	if(d.x + d.y > 2.0) { // d.x + d.y + 1 > 3\r
\r
		// Fetch the crossing edges.\r
		vec4 coords = vec4(-d.x, -d.x, d.y, d.y) * texelSize.xyxy + texCoord.xyxy;\r
		vec4 c;\r
		c.x = sampleLevelZeroOffset(inputBuffer, coords.xy, vec2(-1, 0)).g;\r
		c.y = sampleLevelZeroOffset(inputBuffer, coords.xy, vec2(0, -1)).r;\r
		c.zw = sampleLevelZeroOffset(inputBuffer, coords.zw, vec2(1, 0)).gr;\r
		vec2 cc = vec2(2.0) * c.xz + c.yw;\r
\r
		// Remove the crossing edge if no end of the line could be found.\r
		movec(bvec2(step(0.9, d.zw)), cc, vec2(0.0));\r
\r
		// Fetch the areas for this line.\r
		weights += areaDiag(d.xy, cc, subsampleIndices.w).gr;\r
\r
	}\r
\r
	return weights;\r
\r
}\r
\r
/**\r
 * Determines how much length should be added in the last step of the searches.\r
 *\r
 * Takes the bilinearly interpolated edge (see @PSEUDO_GATHER4), and adds 0, 1\r
 * or 2 depending on which edges and crossing edges are active.\r
 */\r
\r
float searchLength(const in vec2 e, const in float offset) {\r
\r
	/* The texture is flipped vertically, with left and right cases taking half\r
	of the space horizontally. */\r
	vec2 scale = SEARCHTEX_SIZE * vec2(0.5, -1.0);\r
	vec2 bias = SEARCHTEX_SIZE * vec2(offset, 1.0);\r
\r
	// Scale and bias to access texel centers.\r
	scale += vec2(-1.0, 1.0);\r
	bias += vec2(0.5, -0.5);\r
\r
	// Convert from pixel coordinates to texcoords.\r
	scale *= 1.0 / SEARCHTEX_PACKED_SIZE;\r
	bias *= 1.0 / SEARCHTEX_PACKED_SIZE;\r
\r
	return texture2D(searchTexture, scale * e + bias).r;\r
\r
}\r
\r
/**\r
 * Horizontal search for the second pass.\r
 */\r
\r
float searchXLeft(in vec2 texCoord, const in float end) {\r
\r
	/* @PSEUDO_GATHER4\r
	This texCoord has been offset by (-0.25, -0.125) in the vertex shader to\r
	sample between edges, thus fetching four edges in a row.\r
	Sampling with different offsets in each direction allows to disambiguate\r
	which edges are active from the four fetched ones. */\r
\r
	vec2 e = vec2(0.0, 1.0);\r
\r
	for(int i = 0; i < MAX_SEARCH_STEPS_INT; ++i) {\r
\r
		if(!(texCoord.x > end && e.g > 0.8281 && e.r == 0.0)) {\r
\r
			break;\r
\r
		}\r
\r
		e = texture2D(inputBuffer, texCoord).rg;\r
		texCoord = vec2(-2.0, 0.0) * texelSize + texCoord;\r
\r
	}\r
\r
	float offset = -(255.0 / 127.0) * searchLength(e, 0.0) + 3.25;\r
\r
	return texelSize.x * offset + texCoord.x;\r
\r
	// Non-optimized version:\r
	// Correct the previous (-0.25, -0.125) offset.\r
	// texCoord.x += 0.25 * texelSize.x;\r
	// The searches are biased by 1, so adjust the coords accordingly.\r
	// texCoord.x += texelSize.x;\r
	// Disambiguate the length added by the last step.\r
	// texCoord.x += 2.0 * texelSize.x; // Undo last step.\r
	// texCoord.x -= texelSize.x * (255.0 / 127.0) * searchLength(e, 0.0);\r
	// return texelSize.x * offset + texCoord.x);\r
\r
}\r
\r
float searchXRight(vec2 texCoord, const in float end) {\r
\r
	vec2 e = vec2(0.0, 1.0);\r
\r
	for(int i = 0; i < MAX_SEARCH_STEPS_INT; ++i) {\r
\r
		if(!(texCoord.x < end && e.g > 0.8281 && e.r == 0.0)) {\r
\r
			break;\r
\r
		}\r
\r
		e = texture2D(inputBuffer, texCoord).rg;\r
		texCoord = vec2(2.0, 0.0) * texelSize.xy + texCoord;\r
\r
	}\r
\r
	float offset = -(255.0 / 127.0) * searchLength(e, 0.5) + 3.25;\r
\r
	return -texelSize.x * offset + texCoord.x;\r
\r
}\r
\r
/**\r
 * Vertical search for the second pass.\r
 */\r
\r
float searchYUp(vec2 texCoord, const in float end) {\r
\r
	vec2 e = vec2(1.0, 0.0);\r
\r
	for(int i = 0; i < MAX_SEARCH_STEPS_INT; ++i) {\r
\r
		if(!(texCoord.y > end && e.r > 0.8281 && e.g == 0.0)) {\r
\r
			break;\r
\r
		}\r
\r
		e = texture2D(inputBuffer, texCoord).rg;\r
		texCoord = -vec2(0.0, 2.0) * texelSize.xy + texCoord;\r
\r
	}\r
\r
	float offset = -(255.0 / 127.0) * searchLength(e.gr, 0.0) + 3.25;\r
\r
	return texelSize.y * offset + texCoord.y;\r
\r
}\r
\r
float searchYDown(vec2 texCoord, const in float end) {\r
\r
	vec2 e = vec2(1.0, 0.0);\r
\r
	for(int i = 0; i < MAX_SEARCH_STEPS_INT; i++) {\r
\r
		if(!(texCoord.y < end && e.r > 0.8281 && e.g == 0.0)) {\r
\r
			break;\r
\r
		}\r
\r
		e = texture2D(inputBuffer, texCoord).rg;\r
		texCoord = vec2(0.0, 2.0) * texelSize.xy + texCoord;\r
\r
	}\r
\r
	float offset = -(255.0 / 127.0) * searchLength(e.gr, 0.5) + 3.25;\r
\r
	return -texelSize.y * offset + texCoord.y;\r
\r
}\r
\r
/**\r
 * Determines the areas at each side of the current edge.\r
 */\r
\r
vec2 area(const in vec2 dist, const in float e1, const in float e2, const in float offset) {\r
\r
	// Rounding prevents precision errors of bilinear filtering.\r
	vec2 texCoord = vec2(AREATEX_MAX_DISTANCE) * round(4.0 * vec2(e1, e2)) + dist;\r
\r
	// Apply a scale and bias for mapping to texel space.\r
	texCoord = AREATEX_PIXEL_SIZE * texCoord + 0.5 * AREATEX_PIXEL_SIZE;\r
\r
	// Move to the proper place, according to the subpixel offset.\r
	texCoord.y = AREATEX_SUBTEX_SIZE * offset + texCoord.y;\r
\r
	return texture2D(areaTexture, texCoord).rg;\r
\r
}\r
\r
/**\r
 * Corner detection.\r
 */\r
\r
void detectHorizontalCornerPattern(inout vec2 weights, const in vec4 texCoord, const in vec2 d) {\r
\r
	#if !defined(DISABLE_CORNER_DETECTION)\r
\r
		vec2 leftRight = step(d.xy, d.yx);\r
		vec2 rounding = (1.0 - CORNER_ROUNDING_NORM) * leftRight;\r
\r
		// Reduce blending for pixels in the center of a line.\r
		rounding /= leftRight.x + leftRight.y;\r
\r
		vec2 factor = vec2(1.0);\r
		factor.x -= rounding.x * sampleLevelZeroOffset(inputBuffer, texCoord.xy, vec2(0, 1)).r;\r
		factor.x -= rounding.y * sampleLevelZeroOffset(inputBuffer, texCoord.zw, vec2(1, 1)).r;\r
		factor.y -= rounding.x * sampleLevelZeroOffset(inputBuffer, texCoord.xy, vec2(0, -2)).r;\r
		factor.y -= rounding.y * sampleLevelZeroOffset(inputBuffer, texCoord.zw, vec2(1, -2)).r;\r
\r
		weights *= clamp(factor, 0.0, 1.0);\r
\r
	#endif\r
\r
}\r
\r
void detectVerticalCornerPattern(inout vec2 weights, const in vec4 texCoord, const in vec2 d) {\r
\r
	#if !defined(DISABLE_CORNER_DETECTION)\r
\r
		vec2 leftRight = step(d.xy, d.yx);\r
		vec2 rounding = (1.0 - CORNER_ROUNDING_NORM) * leftRight;\r
\r
		rounding /= leftRight.x + leftRight.y;\r
\r
		vec2 factor = vec2(1.0);\r
		factor.x -= rounding.x * sampleLevelZeroOffset(inputBuffer, texCoord.xy, vec2(1, 0)).g;\r
		factor.x -= rounding.y * sampleLevelZeroOffset(inputBuffer, texCoord.zw, vec2(1, 1)).g;\r
		factor.y -= rounding.x * sampleLevelZeroOffset(inputBuffer, texCoord.xy, vec2(-2, 0)).g;\r
		factor.y -= rounding.y * sampleLevelZeroOffset(inputBuffer, texCoord.zw, vec2(-2, 1)).g;\r
\r
		weights *= clamp(factor, 0.0, 1.0);\r
\r
	#endif\r
\r
}\r
\r
void main() {\r
\r
	vec4 weights = vec4(0.0);\r
	vec4 subsampleIndices = vec4(0.0);\r
	vec2 e = texture2D(inputBuffer, vUv).rg;\r
\r
	if(e.g > 0.0) {\r
\r
		// Edge at north.\r
\r
		#if !defined(DISABLE_DIAG_DETECTION)\r
\r
			/* Diagonals have both north and west edges, so searching for them in one of\r
			the boundaries is enough. */\r
			weights.rg = calculateDiagWeights(vUv, e, subsampleIndices);\r
\r
			// Skip horizontal/vertical processing if there is a diagonal.\r
			if(weights.r == -weights.g) { // weights.r + weights.g == 0.0\r
\r
		#endif\r
\r
		vec2 d;\r
\r
		// Find the distance to the left.\r
		vec3 coords;\r
		coords.x = searchXLeft(vOffset[0].xy, vOffset[2].x);\r
		coords.y = vOffset[1].y; // vOffset[1].y = vUv.y - 0.25 * texelSize.y (@CROSSING_OFFSET)\r
		d.x = coords.x;\r
\r
		/* Now fetch the left crossing edges, two at a time using bilinear\r
		filtering. Sampling at -0.25 (see @CROSSING_OFFSET) enables to discern what\r
		value each edge has. */\r
		float e1 = texture2D(inputBuffer, coords.xy).r;\r
\r
		// Find the distance to the right.\r
		coords.z = searchXRight(vOffset[0].zw, vOffset[2].y);\r
		d.y = coords.z;\r
\r
		/* Translate distances to pixel units for better interleave arithmetic and\r
		memory accesses. */\r
		d = round(resolution.xx * d + -vPixCoord.xx);\r
\r
		// The area texture is compressed quadratically.\r
		vec2 sqrtD = sqrt(abs(d));\r
\r
		// Fetch the right crossing edges.\r
		float e2 = sampleLevelZeroOffset(inputBuffer, coords.zy, vec2(1, 0)).r;\r
\r
		// Pattern recognized, now get the actual area.\r
		weights.rg = area(sqrtD, e1, e2, subsampleIndices.y);\r
\r
		// Fix corners.\r
		coords.y = vUv.y;\r
		detectHorizontalCornerPattern(weights.rg, coords.xyzy, d);\r
\r
		#if !defined(DISABLE_DIAG_DETECTION)\r
\r
			} else {\r
\r
				// Skip vertical processing.\r
				e.r = 0.0;\r
\r
			}\r
\r
		#endif\r
\r
	}\r
\r
	if(e.r > 0.0) {\r
\r
		// Edge at west.\r
\r
		vec2 d;\r
\r
		// Find the distance to the top.\r
		vec3 coords;\r
		coords.y = searchYUp(vOffset[1].xy, vOffset[2].z);\r
		coords.x = vOffset[0].x; // vOffset[1].x = vUv.x - 0.25 * texelSize.x;\r
		d.x = coords.y;\r
\r
		// Fetch the top crossing edges.\r
		float e1 = texture2D(inputBuffer, coords.xy).g;\r
\r
		// Find the distance to the bottom.\r
		coords.z = searchYDown(vOffset[1].zw, vOffset[2].w);\r
		d.y = coords.z;\r
\r
		// Translate distances into pixel units.\r
		d = round(resolution.yy * d - vPixCoord.yy);\r
\r
		// The area texture is compressed quadratically.\r
		vec2 sqrtD = sqrt(abs(d));\r
\r
		// Fetch the bottom crossing edges.\r
		float e2 = sampleLevelZeroOffset(inputBuffer, coords.xz, vec2(0, 1)).g;\r
\r
		// Get the area for this direction.\r
		weights.ba = area(sqrtD, e1, e2, subsampleIndices.x);\r
\r
		// Fix corners.\r
		coords.x = vUv.x;\r
		detectVerticalCornerPattern(weights.ba, coords.xyxz, d);\r
\r
	}\r
\r
	gl_FragColor = weights;\r
\r
}\r
`, Io = `uniform vec2 texelSize;\r
uniform vec2 resolution;\r
\r
varying vec2 vUv;\r
varying vec4 vOffset[3];\r
varying vec2 vPixCoord;\r
\r
void main() {\r
\r
	vUv = position.xy * 0.5 + 0.5;\r
	vPixCoord = vUv * resolution;\r
\r
	// Offsets for the searches (see @PSEUDO_GATHER4).\r
	vOffset[0] = vUv.xyxy + texelSize.xyxy * vec4(-0.25, -0.125, 1.25, -0.125);\r
	vOffset[1] = vUv.xyxy + texelSize.xyxy * vec4(-0.125, -0.25, -0.125, 1.25);\r
\r
	// This indicates the ends of the loops.\r
	vOffset[2] = vec4(vOffset[0].xz, vOffset[1].yw) +\r
		vec4(-2.0, 2.0, -2.0, 2.0) * texelSize.xxyy * MAX_SEARCH_STEPS_FLOAT;\r
\r
	gl_Position = vec4(position.xy, 1.0, 1.0);\r
\r
}\r
`;
class Bo extends N {
  /**
   * Constructs a new SMAA weights material.
   *
   * @param {Vector2} [texelSize] - The absolute screen texel size.
   * @param {Vector2} [resolution] - The resolution.
   */
  constructor(e = new g(), t = new g()) {
    super({
      name: "SMAAWeightsMaterial",
      defines: {
        // Configurable settings:
        MAX_SEARCH_STEPS_INT: "16",
        MAX_SEARCH_STEPS_FLOAT: "16.0",
        MAX_SEARCH_STEPS_DIAG_INT: "8",
        MAX_SEARCH_STEPS_DIAG_FLOAT: "8.0",
        CORNER_ROUNDING: "25",
        CORNER_ROUNDING_NORM: "0.25",
        // Non-configurable settings:
        AREATEX_MAX_DISTANCE: "16.0",
        AREATEX_MAX_DISTANCE_DIAG: "20.0",
        AREATEX_PIXEL_SIZE: "(1.0 / vec2(160.0, 560.0))",
        AREATEX_SUBTEX_SIZE: "(1.0 / 7.0)",
        SEARCHTEX_SIZE: "vec2(66.0, 33.0)",
        SEARCHTEX_PACKED_SIZE: "vec2(64.0, 16.0)"
      },
      uniforms: {
        inputBuffer: new a(null),
        searchTexture: new a(null),
        areaTexture: new a(null),
        resolution: new a(t),
        texelSize: new a(e)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: Fo,
      vertexShader: Io
    });
  }
  /**
   * The input buffer.
   *
   * @type {Texture}
   */
  set inputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * Sets the input buffer.
   *
   * @deprecated Use inputBuffer instead.
   * @param {Texture} value - The input buffer.
   */
  setInputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * The search lookup texture.
   *
   * @type {Texture}
   */
  get searchTexture() {
    return this.uniforms.searchTexture.value;
  }
  set searchTexture(e) {
    this.uniforms.searchTexture.value = e;
  }
  /**
   * The area lookup texture.
   *
   * @type {Texture}
   */
  get areaTexture() {
    return this.uniforms.areaTexture.value;
  }
  set areaTexture(e) {
    this.uniforms.areaTexture.value = e;
  }
  /**
   * Sets the search and area lookup textures.
   *
   * @deprecated Use searchTexture and areaTexture instead.
   * @param {Texture} search - The search lookup texture.
   * @param {Texture} area - The area lookup texture.
   */
  setLookupTextures(e, t) {
    this.searchTexture = e, this.areaTexture = t;
  }
  /**
   * The maximum amount of steps performed in the horizontal/vertical pattern searches, at each side of the pixel.
   * Range: [0, 112].
   *
   * In number of pixels, it's actually the double. So the maximum line length perfectly handled by, for example 16, is
   * 64 (perfectly means that longer lines won't look as good, but are still antialiased).
   *
   * @type {Number}
   */
  get orthogonalSearchSteps() {
    return Number(this.defines.MAX_SEARCH_STEPS_INT);
  }
  set orthogonalSearchSteps(e) {
    const t = Math.min(Math.max(e, 0), 112);
    this.defines.MAX_SEARCH_STEPS_INT = t.toFixed("0"), this.defines.MAX_SEARCH_STEPS_FLOAT = t.toFixed("1"), this.needsUpdate = !0;
  }
  /**
   * Sets the maximum amount of steps performed in the horizontal/vertical pattern searches, at each side of the pixel.
   *
   * @deprecated Use orthogonalSearchSteps instead.
   * @param {Number} value - The search steps. Range: [0, 112].
   */
  setOrthogonalSearchSteps(e) {
    this.orthogonalSearchSteps = e;
  }
  /**
   * The maximum steps performed in the diagonal pattern searches, at each side of the pixel. This search
   * jumps one pixel at a time. Range: [0, 20].
   *
   * On high-end machines this search is cheap (between 0.8x and 0.9x slower for 16 steps), but it can have a
   * significant impact on older machines.
   *
   * @type {Number}
   */
  get diagonalSearchSteps() {
    return Number(this.defines.MAX_SEARCH_STEPS_DIAG_INT);
  }
  set diagonalSearchSteps(e) {
    const t = Math.min(Math.max(e, 0), 20);
    this.defines.MAX_SEARCH_STEPS_DIAG_INT = t.toFixed("0"), this.defines.MAX_SEARCH_STEPS_DIAG_FLOAT = t.toFixed("1"), this.needsUpdate = !0;
  }
  /**
   * Specifies the maximum steps performed in the diagonal pattern searches, at each side of the pixel.
   *
   * @deprecated Use diagonalSearchSteps instead.
   * @param {Number} value - The search steps. Range: [0, 20].
   */
  setDiagonalSearchSteps(e) {
    this.diagonalSearchSteps = e;
  }
  /**
   * Indicates whether diagonal pattern detection is enabled.
   *
   * @type {Boolean}
   */
  get diagonalDetection() {
    return this.defines.DISABLE_DIAG_DETECTION === void 0;
  }
  set diagonalDetection(e) {
    e ? delete this.defines.DISABLE_DIAG_DETECTION : this.defines.DISABLE_DIAG_DETECTION = "1", this.needsUpdate = !0;
  }
  /**
   * Indicates whether diagonal pattern detection is enabled.
   *
   * @deprecated Use diagonalDetection instead.
   * @return {Boolean} Whether diagonal pattern detection is enabled.
   */
  isDiagonalDetectionEnabled() {
    return this.diagonalDetection;
  }
  /**
   * Enables or disables diagonal pattern detection.
   *
   * @deprecated Use diagonalDetection instead.
   * @param {Boolean} value - Whether diagonal pattern detection should be enabled.
   */
  setDiagonalDetectionEnabled(e) {
    this.diagonalDetection = e;
  }
  /**
   * Specifies how much sharp corners will be rounded. Range: [0, 100].
   *
   * @type {Number}
   */
  get cornerRounding() {
    return Number(this.defines.CORNER_ROUNDING);
  }
  set cornerRounding(e) {
    const t = Math.min(Math.max(e, 0), 100);
    this.defines.CORNER_ROUNDING = t.toFixed("4"), this.defines.CORNER_ROUNDING_NORM = (t / 100).toFixed("4"), this.needsUpdate = !0;
  }
  /**
   * Specifies how much sharp corners will be rounded.
   *
   * @deprecated Use cornerRounding instead.
   * @param {Number} value - The corner rounding amount. Range: [0, 100].
   */
  setCornerRounding(e) {
    this.cornerRounding = e;
  }
  /**
   * Indicates whether corner detection is enabled.
   *
   * @type {Number}
   */
  get cornerDetection() {
    return this.defines.DISABLE_CORNER_DETECTION === void 0;
  }
  set cornerDetection(e) {
    e ? delete this.defines.DISABLE_CORNER_DETECTION : this.defines.DISABLE_CORNER_DETECTION = "1", this.needsUpdate = !0;
  }
  /**
   * Indicates whether corner rounding is enabled.
   *
   * @deprecated Use cornerDetection instead.
   * @return {Boolean} Whether corner rounding is enabled.
   */
  isCornerRoundingEnabled() {
    return this.cornerDetection;
  }
  /**
   * Enables or disables corner rounding.
   *
   * @deprecated Use cornerDetection instead.
   * @param {Boolean} value - Whether corner rounding should be enabled.
   */
  setCornerRoundingEnabled(e) {
    this.cornerDetection = e;
  }
  /**
   * Sets the size of this object.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.uniforms;
    r.texelSize.value.set(1 / e, 1 / t), r.resolution.value.set(e, t);
  }
}
const Lo = `uniform sampler2D weightMap;\r
\r
varying vec2 vOffset0;\r
varying vec2 vOffset1;\r
\r
/**\r
 * Moves values to a target vector based on a given conditional vector.\r
 */\r
\r
void movec(const in bvec2 c, inout vec2 variable, const in vec2 value) {\r
\r
	if(c.x) { variable.x = value.x; }\r
	if(c.y) { variable.y = value.y; }\r
\r
}\r
\r
void movec(const in bvec4 c, inout vec4 variable, const in vec4 value) {\r
\r
	movec(c.xy, variable.xy, value.xy);\r
	movec(c.zw, variable.zw, value.zw);\r
\r
}\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	// Fetch the blending weights for the current pixel.\r
	vec4 a;\r
	a.x = texture2D(weightMap, vOffset0).a;\r
	a.y = texture2D(weightMap, vOffset1).g;\r
	a.wz = texture2D(weightMap, uv).rb;\r
\r
	vec4 color = inputColor;\r
\r
	// Ignore tiny blending weights.\r
	if(dot(a, vec4(1.0)) >= 1e-5) {\r
\r
		// max(horizontal) > max(vertical)\r
		bool h = max(a.x, a.z) > max(a.y, a.w);\r
\r
		// Calculate the blending offsets.\r
		vec4 blendingOffset = vec4(0.0, a.y, 0.0, a.w);\r
		vec2 blendingWeight = a.yw;\r
		movec(bvec4(h), blendingOffset, vec4(a.x, 0.0, a.z, 0.0));\r
		movec(bvec2(h), blendingWeight, a.xz);\r
		blendingWeight /= dot(blendingWeight, vec2(1.0));\r
\r
		// Calculate the texture coordinates.\r
		vec4 blendingCoord = blendingOffset * vec4(texelSize, -texelSize) + uv.xyxy;\r
\r
		// Rely on bilinear filtering to mix the current pixel with the neighbor.\r
		color = blendingWeight.x * texture2D(inputBuffer, blendingCoord.xy);\r
		color += blendingWeight.y * texture2D(inputBuffer, blendingCoord.zw);\r
\r
	}\r
\r
	outputColor = color;\r
\r
}\r
`, Uo = `varying vec2 vOffset0;\r
varying vec2 vOffset1;\r
\r
void mainSupport(const in vec2 uv) {\r
\r
	vOffset0 = uv + texelSize * vec2(1.0, 0.0);\r
	vOffset1 = uv + texelSize * vec2(0.0, 1.0);\r
\r
}\r
`;
class Xh extends I {
  /**
   * Constructs a new SMAA effect.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.SRC] - The blend function of this effect.
   * @param {SMAAPreset} [options.preset=SMAAPreset.MEDIUM] - The quality preset.
   * @param {EdgeDetectionMode} [options.edgeDetectionMode=EdgeDetectionMode.COLOR] - The edge detection mode.
   * @param {PredicationMode} [options.predicationMode=PredicationMode.DISABLED] - The predication mode.
   */
  constructor({
    blendFunction: e = E.SRC,
    preset: t = Dt.MEDIUM,
    edgeDetectionMode: r = zi.COLOR,
    predicationMode: n = vo.DISABLED
  } = {}) {
    super("SMAAEffect", Lo, {
      vertexShader: Uo,
      blendFunction: e,
      attributes: Q.CONVOLUTION | Q.DEPTH,
      uniforms: /* @__PURE__ */ new Map([
        ["weightMap", new a(null)]
      ])
    });
    let i, s;
    arguments.length > 1 && (i = arguments[0], s = arguments[1], arguments.length > 2 && (t = arguments[2]), arguments.length > 3 && (r = arguments[3])), this.renderTargetEdges = new A(1, 1, { depthBuffer: !1 }), this.renderTargetEdges.texture.name = "SMAA.Edges", this.renderTargetWeights = this.renderTargetEdges.clone(), this.renderTargetWeights.texture.name = "SMAA.Weights", this.uniforms.get("weightMap").value = this.renderTargetWeights.texture, this.clearPass = new De(!0, !1, !1), this.clearPass.overrideClearColor = new y(0), this.clearPass.overrideClearAlpha = 1, this.edgeDetectionPass = new ee(new zo()), this.edgeDetectionMaterial.edgeDetectionMode = r, this.edgeDetectionMaterial.predicationMode = n, this.weightsPass = new ee(new Bo());
    const o = new kt();
    o.onLoad = () => {
      const l = new It(i);
      l.name = "SMAA.Search", l.magFilter = b, l.minFilter = b, l.generateMipmaps = !1, l.needsUpdate = !0, l.flipY = !0, this.weightsMaterial.searchTexture = l;
      const u = new It(s);
      u.name = "SMAA.Area", u.magFilter = F, u.minFilter = F, u.generateMipmaps = !1, u.needsUpdate = !0, u.flipY = !1, this.weightsMaterial.areaTexture = u, this.dispatchEvent({ type: "load" });
    }, o.itemStart("search"), o.itemStart("area"), i !== void 0 && s !== void 0 ? (o.itemEnd("search"), o.itemEnd("area")) : typeof Image < "u" && (i = new Image(), s = new Image(), i.addEventListener("load", () => o.itemEnd("search")), s.addEventListener("load", () => o.itemEnd("area")), i.src = Fr, s.src = Ir), this.applyPreset(t);
  }
  /**
   * The edges texture.
   *
   * @type {Texture}
   */
  get edgesTexture() {
    return this.renderTargetEdges.texture;
  }
  /**
   * Returns the edges texture.
   *
   * @deprecated Use edgesTexture instead.
   * @return {Texture} The texture.
   */
  getEdgesTexture() {
    return this.edgesTexture;
  }
  /**
   * The edge weights texture.
   *
   * @type {Texture}
   */
  get weightsTexture() {
    return this.renderTargetWeights.texture;
  }
  /**
   * Returns the edge weights texture.
   *
   * @deprecated Use weightsTexture instead.
   * @return {Texture} The texture.
   */
  getWeightsTexture() {
    return this.weightsTexture;
  }
  /**
   * The edge detection material.
   *
   * @type {EdgeDetectionMaterial}
   */
  get edgeDetectionMaterial() {
    return this.edgeDetectionPass.fullscreenMaterial;
  }
  /**
   * The edge detection material.
   *
   * @type {EdgeDetectionMaterial}
   * @deprecated Use edgeDetectionMaterial instead.
   */
  get colorEdgesMaterial() {
    return this.edgeDetectionMaterial;
  }
  /**
   * Returns the edge detection material.
   *
   * @deprecated Use edgeDetectionMaterial instead.
   * @return {EdgeDetectionMaterial} The material.
   */
  getEdgeDetectionMaterial() {
    return this.edgeDetectionMaterial;
  }
  /**
   * The edge weights material.
   *
   * @type {SMAAWeightsMaterial}
   */
  get weightsMaterial() {
    return this.weightsPass.fullscreenMaterial;
  }
  /**
   * Returns the edge weights material.
   *
   * @deprecated Use weightsMaterial instead.
   * @return {SMAAWeightsMaterial} The material.
   */
  getWeightsMaterial() {
    return this.weightsMaterial;
  }
  /**
   * Sets the edge detection sensitivity.
   *
   * See {@link EdgeDetectionMaterial#setEdgeDetectionThreshold} for more details.
   *
   * @deprecated Use edgeDetectionMaterial instead.
   * @param {Number} threshold - The edge detection sensitivity. Range: [0.05, 0.5].
   */
  setEdgeDetectionThreshold(e) {
    this.edgeDetectionMaterial.edgeDetectionThreshold = e;
  }
  /**
   * Sets the maximum amount of horizontal/vertical search steps.
   *
   * See {@link SMAAWeightsMaterial#setOrthogonalSearchSteps} for more details.
   *
   * @deprecated Use weightsMaterial instead.
   * @param {Number} steps - The search steps. Range: [0, 112].
   */
  setOrthogonalSearchSteps(e) {
    this.weightsMaterial.orthogonalSearchSteps = e;
  }
  /**
   * Applies the given quality preset.
   *
   * @param {SMAAPreset} preset - The preset.
   */
  applyPreset(e) {
    const t = this.edgeDetectionMaterial, r = this.weightsMaterial;
    switch (e) {
      case Dt.LOW:
        t.edgeDetectionThreshold = 0.15, r.orthogonalSearchSteps = 4, r.diagonalDetection = !1, r.cornerDetection = !1;
        break;
      case Dt.MEDIUM:
        t.edgeDetectionThreshold = 0.1, r.orthogonalSearchSteps = 8, r.diagonalDetection = !1, r.cornerDetection = !1;
        break;
      case Dt.HIGH:
        t.edgeDetectionThreshold = 0.1, r.orthogonalSearchSteps = 16, r.diagonalSearchSteps = 8, r.cornerRounding = 25, r.diagonalDetection = !0, r.cornerDetection = !0;
        break;
      case Dt.ULTRA:
        t.edgeDetectionThreshold = 0.05, r.orthogonalSearchSteps = 32, r.diagonalSearchSteps = 16, r.cornerRounding = 25, r.diagonalDetection = !0, r.cornerDetection = !0;
        break;
    }
  }
  /**
   * Sets the depth texture.
   *
   * @param {Texture} depthTexture - A depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing.
   */
  setDepthTexture(e, t = $) {
    this.edgeDetectionMaterial.depthBuffer = e, this.edgeDetectionMaterial.depthPacking = t;
  }
  /**
   * Updates this effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   */
  update(e, t, r) {
    this.clearPass.render(e, this.renderTargetEdges), this.edgeDetectionPass.render(e, t, this.renderTargetEdges), this.weightsPass.render(e, this.renderTargetEdges, this.renderTargetWeights);
  }
  /**
   * Updates the size of internal render targets.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.edgeDetectionMaterial.setSize(e, t), this.weightsMaterial.setSize(e, t), this.renderTargetEdges.setSize(e, t), this.renderTargetWeights.setSize(e, t);
  }
  /**
   * Deletes internal render targets and textures.
   */
  dispose() {
    const { searchTexture: e, areaTexture: t } = this.weightsMaterial;
    e !== null && t !== null && (e.dispose(), t.dispose()), super.dispose();
  }
  /**
   * The SMAA search image, encoded as a base64 data URL.
   *
   * @type {String}
   * @deprecated
   */
  static get searchImageDataURL() {
    return Fr;
  }
  /**
   * The SMAA area image, encoded as a base64 data URL.
   *
   * @type {String}
   * @deprecated
   */
  static get areaImageDataURL() {
    return Ir;
  }
}
const Oo = `uniform sampler2D depthBuffer;\r
uniform sampler2D normalBuffer;\r
uniform sampler2D noiseTexture;\r
\r
uniform vec2 texelSize;\r
uniform float cameraNear;\r
uniform float cameraFar;\r
uniform float snowHeight;\r
uniform float snowAmount;\r
uniform float snowBrightness;\r
uniform bool additiveBlending;\r
uniform vec3 snowColor;\r
uniform float alphaTest;\r
uniform vec3 cameraPosition;\r
uniform float slopeMinAngle;\r
uniform float slopeMaxAngle;\r
uniform float normalThreshold;\r
uniform float viewStability;\r
uniform float snowNoisiness; // 雪的不均匀程度\r
uniform float snowAccumulation; // 雪的堆积程度\r
\r
// 添加矩阵用于世界位置重建\r
uniform mat4 cameraMatrixWorld;\r
uniform mat4 projectionMatrixInverse;\r
\r
float readDepth(in vec2 uv) {\r
    return texture2D(depthBuffer, uv).r;\r
}\r
\r
// 将深度值转换回线性空间\r
float linearizeDepth(in float depth) {\r
    return cameraNear * cameraFar / (cameraNear + depth * (cameraFar - cameraNear));\r
}\r
\r
// 从深度重建世界坐标\r
vec3 reconstructWorldPosition(vec2 uv, float depth) {\r
    // 构建截锥体空间坐标 (-1到1范围)\r
    vec4 clipPos = vec4(\r
        (uv.x * 2.0 - 1.0),\r
        (uv.y * 2.0 - 1.0),\r
        (depth * 2.0 - 1.0),\r
        1.0\r
    );\r
    \r
    // 转换到相机空间\r
    vec4 viewPos = projectionMatrixInverse * clipPos;\r
    viewPos /= viewPos.w;\r
    \r
    // 转换到世界空间\r
    vec4 worldPos = cameraMatrixWorld * viewPos;\r
    \r
    return worldPos.xyz;\r
}\r
\r
// 计算相机视角稳定因子 - 防止极端视角下雪消失\r
float calculateViewStabilityFactor(vec3 normal, vec3 worldPos) {\r
    // 计算从相机到表面点的向量\r
    vec3 viewDir = normalize(worldPos - cameraPosition);\r
    \r
    // 计算视线与法线的夹角 (点积)\r
    float viewDotNormal = abs(dot(viewDir, normal));\r
    \r
    // 当视线几乎垂直于表面时(接近0)，提供额外的稳定因子\r
    // 使用viewStability参数控制稳定性强度\r
    return mix(0.0, smoothstep(0.0, 0.3, viewDotNormal), 1.0 - viewStability);\r
}\r
\r
// 计算雪滑落效果 - 大角度表面会有更少的积雪\r
float calculateSnowSliding(vec3 normal, vec3 worldPos, float noise) {\r
    // 基础滑落计算 - 基于法线Y分量（垂直度）\r
    float slope = normal.y; // 1.0表示完全水平, 0.0表示完全垂直\r
    \r
    // 添加噪声到滑落计算，使得边缘更自然\r
    float noisySlope = mix(slope, slope * (0.8 + noise * 0.4), snowNoisiness);\r
    \r
    // 计算滑落系数 - 陡峭的表面滑落更多，缓和的表面积累更多\r
    float slideThreshold = 0.7; // 开始滑落的阈值角度\r
    float slideFactor = smoothstep(slideThreshold - 0.3, slideThreshold + 0.2, noisySlope);\r
    \r
    // 模拟雪的堆积 - 在坡度变化的地方添加更多雪\r
    float localHeight = dot(worldPos, vec3(0.8, 1.0, 0.8)) * 0.1;\r
    float heightVariation = fract(localHeight) * 2.0 - 1.0;\r
    float accumulationFactor = smoothstep(0.3, 0.7, abs(heightVariation)) * snowAccumulation;\r
    \r
    // 在坡度适中的地方增加雪的堆积\r
    float accumulationSlope = smoothstep(0.4, 0.9, noisySlope) * smoothstep(1.0, 0.4, noisySlope);\r
    float finalAccumulation = mix(0.0, accumulationFactor, accumulationSlope);\r
    \r
    // 合并滑落和堆积效果\r
    return slideFactor + finalAccumulation * 0.5;\r
}\r
\r
// 生成复杂噪声 - 用于不均匀雪分布\r
float generateComplexNoise(vec2 uv) {\r
    float noise1 = texture2D(noiseTexture, uv * 10.0).r;\r
    float noise2 = texture2D(noiseTexture, uv * 5.0 + vec2(0.4, 0.6)).r;\r
    float noise3 = texture2D(noiseTexture, uv * 20.0 - vec2(0.1, 0.9)).r;\r
    \r
    // 混合不同尺度的噪声，创造自然变化\r
    return noise1 * 0.5 + noise2 * 0.3 + noise3 * 0.2;\r
}\r
\r
// 计算积雪稳定因子 - 不会随着相机角度变化\r
float calculateSnowStableFactor(vec3 normal, float linearDepth, vec3 worldPos) {\r
    // 使用世界坐标Y值作为基本判断 - 高度越高积雪越多\r
    float worldHeightFactor = smoothstep(-5.0, 5.0, worldPos.y);\r
    \r
    // 计算基于深度的积雪因子 - 距离相机越近积雪越多\r
    float depthFactor = 1.0 - linearDepth / snowHeight;\r
    \r
    // 法线朝上的因子 - 垂直面（法线Y接近0）应该没有雪\r
    float normalFactor = pow(max(0.0, normal.y), 2.0);\r
    \r
    // 硬性阈值 - 如果法线Y分量小于normalThreshold，就不应该有雪\r
    float normalThresholdFactor = step(normalThreshold, normal.y);\r
    \r
    // 获取视角稳定因子\r
    float viewStabilityFactor = calculateViewStabilityFactor(normal, worldPos);\r
    \r
    // 将各个因子混合 - 世界高度和法线共同决定\r
    float combinedFactor = mix(worldHeightFactor, depthFactor, 0.3);\r
    \r
    // 提高法线因子在接近垂直视角下的稳定性\r
    float stabilizedNormalFactor = mix(normalFactor, max(normalFactor, 0.2), 1.0 - viewStabilityFactor);\r
    \r
    // 法线因子权重增加到0.5，并应用硬性阈值\r
    // 应用视角稳定性修正，在极端视角下保持积雪效果\r
    return mix(combinedFactor, stabilizedNormalFactor, 0.5) * normalThresholdFactor;\r
}\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
    // 获取法线和深度数据\r
    vec4 normalData = texture2D(normalBuffer, uv);\r
    float depth = readDepth(uv);\r
    \r
    // 检查alpha值，如果低于阈值则不应用积雪\r
    if(inputColor.a < alphaTest) {\r
        outputColor = inputColor;\r
        return;\r
    }\r
    \r
    // 将深度转换为线性空间\r
    float linearDepth = linearizeDepth(depth);\r
    \r
    // 重建世界坐标 - 用于更稳定的积雪计算\r
    vec3 worldPos = reconstructWorldPosition(uv, depth);\r
    \r
    // 获取法线向量\r
    vec3 normal = normalize(normalData.xyz * 2.0 - 1.0);\r
    \r
    // 生成复杂噪声 - 用于不均匀雪分布\r
    float complexNoise = generateComplexNoise(uv);\r
    \r
    // 使用稳定的积雪计算 - 减少相机角度依赖\r
    float stableFactor = calculateSnowStableFactor(normal, linearDepth, worldPos);\r
    \r
    // 计算雪滑落效果\r
    float slidingFactor = calculateSnowSliding(normal, worldPos, complexNoise);\r
    \r
    // 增加不均匀性 - 使用噪声调整雪分布\r
    float variationFactor = mix(1.0, complexNoise * 0.7 + 0.5, snowNoisiness);\r
    \r
    // 计算积雪因子 - 综合所有效果\r
    float snowFactor = clamp(stableFactor * slidingFactor * variationFactor * snowAmount, 0.0, 1.0);\r
    \r
    // 平滑过渡到雪的颜色\r
    vec3 finalColor;\r
    if (additiveBlending) {\r
        // 加性混合模式\r
        finalColor = mix(inputColor.rgb, inputColor.rgb + snowColor * snowBrightness, snowFactor);\r
    } else {\r
        // 普通混合模式\r
        finalColor = mix(inputColor.rgb, snowColor, snowFactor);\r
    }\r
    \r
    // 输出最终颜色\r
    outputColor = vec4(finalColor, inputColor.a);\r
} `, wt = "data:image/png;base64,";
class Yh extends I {
  /**
   * 构造一个新的积雪覆盖效果
   *
   * @param {Camera} camera - 相机对象，用于深度计算
   * @param {Texture} normalBuffer - 法线信息缓冲区
   * @param {Texture} depthBuffer - 深度信息缓冲区
   * @param {Object} [options] - 效果选项
   * @param {BlendFunction} [options.blendFunction=BlendFunction.NORMAL] - 混合模式
   * @param {Number} [options.snowAmount=0.5] - 积雪量，0-1
   * @param {Number} [options.snowHeight=20.0] - 雪线高度，越高积雪越少
   * @param {Number} [options.snowBrightness=1.5] - 雪的亮度
   * @param {Boolean} [options.additiveBlending=false] - 是否使用加性混合
   * @param {Color|String|Number} [options.snowColor=0xffffff] - 雪的颜色
   * @param {Number} [options.alphaTest=0.1] - 透明度测试阈值
   * @param {Number} [options.slopeMinAngle=0.1] - 最小积雪角度（约6度）
   * @param {Number} [options.slopeMaxAngle=0.5] - 最大积雪角度（约30度）
   * @param {Number} [options.normalThreshold=0.3] - 法线阈值，决定多垂直的表面可以积雪
   * @param {Number} [options.viewStability=0.5] - 视角稳定性，值越高在极端视角下积雪效果越稳定
   * @param {Number} [options.snowNoisiness=0.5] - 积雪不均匀度，值越高积雪分布越不均匀
   * @param {Number} [options.snowAccumulation=0.5] - 积雪堆积强度，值越高在坡度变化处积雪堆积越明显
   */
  composer;
  constructor(e, t, r, {
    blendFunction: n = E.NORMAL,
    snowAmount: i = 0.5,
    snowHeight: s = 20,
    snowBrightness: o = 1.5,
    additiveBlending: l = !1,
    snowColor: u = 16777215,
    alphaTest: f = 0.1,
    slopeMinAngle: h = 0.1,
    slopeMaxAngle: m = 0.5,
    normalThreshold: p = 0.3,
    viewStability: d = 0.5,
    snowNoisiness: v = 0.5,
    snowAccumulation: x = 0.5
  } = {}) {
    super("SnowOverlayEffect", Oo, {
      blendFunction: n,
      uniforms: /* @__PURE__ */ new Map([
        ["depthBuffer", new a(r.createDepthTexture())],
        ["normalBuffer", new a(t)],
        ["cameraNear", new a(e.near)],
        ["cameraFar", new a(e.far)],
        ["cameraPosition", new a(new T().copy(e.position))],
        ["cameraMatrixWorld", new a(new C().copy(e.matrixWorld))],
        ["projectionMatrixInverse", new a(new C().copy(e.projectionMatrix).invert())],
        ["texelSize", new a(new g())],
        ["snowHeight", new a(s)],
        ["snowAmount", new a(i)],
        ["snowBrightness", new a(o)],
        ["additiveBlending", new a(l)],
        ["snowColor", new a(new y(u))],
        ["noiseTexture", new a(null)],
        ["alphaTest", new a(f)],
        ["slopeMinAngle", new a(h)],
        ["slopeMaxAngle", new a(m)],
        ["normalThreshold", new a(p)],
        ["viewStability", new a(d)],
        ["snowNoisiness", new a(v)],
        ["snowAccumulation", new a(x)]
      ])
    }), this.composer = r, this.camera = e, this.uniforms.get("depthBuffer").value = this.composer.createDepthTexture(), this.depthBuffer = r.createDepthTexture(), this.normalBuffer = t, this._createNoiseTexture();
  }
  /**
   * 创建噪声纹理，用于雪花的随机变化
   *
   * @private
   */
  _createNoiseTexture() {
    const e = new Ie().load(wt);
    e.wrapS = G, e.wrapT = G, e.needsUpdate = !0, this.uniforms.get("noiseTexture").value = e;
  }
  /**
   * 积雪量
   *
   * @type {Number}
   */
  get snowAmount() {
    return this.uniforms.get("snowAmount").value;
  }
  set snowAmount(e) {
    this.uniforms.get("snowAmount").value = e;
  }
  /**
   * 雪线高度
   *
   * @type {Number}
   */
  get snowHeight() {
    return this.uniforms.get("snowHeight").value;
  }
  set snowHeight(e) {
    this.uniforms.get("snowHeight").value = e;
  }
  /**
   * 雪的亮度
   *
   * @type {Number}
   */
  get snowBrightness() {
    return this.uniforms.get("snowBrightness").value;
  }
  set snowBrightness(e) {
    this.uniforms.get("snowBrightness").value = e;
  }
  /**
   * 是否使用加性混合
   *
   * @type {Boolean}
   */
  get additiveBlending() {
    return this.uniforms.get("additiveBlending").value;
  }
  set additiveBlending(e) {
    this.uniforms.get("additiveBlending").value = e;
  }
  /**
   * 雪的颜色
   *
   * @type {Color}
   */
  get snowColor() {
    return this.uniforms.get("snowColor").value;
  }
  set snowColor(e) {
    this.uniforms.get("snowColor").value.set(e);
  }
  /**
   * 透明度测试阈值，低于此值的片段将不会应用积雪
   *
   * @type {Number}
   */
  get alphaTest() {
    return this.uniforms.get("alphaTest").value;
  }
  set alphaTest(e) {
    this.uniforms.get("alphaTest").value = e;
  }
  /**
   * 坡度最小角度，低于此值的表面将不会积雪
   *
   * @type {Number}
   */
  get slopeMinAngle() {
    return this.uniforms.get("slopeMinAngle").value;
  }
  set slopeMinAngle(e) {
    this.uniforms.get("slopeMinAngle").value = e;
  }
  /**
   * 坡度最大角度，高于此值的表面将完全积雪
   *
   * @type {Number}
   */
  get slopeMaxAngle() {
    return this.uniforms.get("slopeMaxAngle").value;
  }
  set slopeMaxAngle(e) {
    this.uniforms.get("slopeMaxAngle").value = e;
  }
  /**
   * 法线阈值，决定多垂直的表面可以积雪
   * 值越小，越垂直的表面也会积雪
   *
   * @type {Number}
   */
  get normalThreshold() {
    return this.uniforms.get("normalThreshold").value;
  }
  set normalThreshold(e) {
    this.uniforms.get("normalThreshold").value = e;
  }
  /**
   * 视角稳定性，值越高在极端视角下积雪效果越稳定
   *
   * @type {Number}
   */
  get viewStability() {
    return this.uniforms.get("viewStability").value;
  }
  set viewStability(e) {
    this.uniforms.get("viewStability").value = e;
  }
  /**
   * 积雪不均匀度，值越高积雪分布越不均匀
   *
   * @type {Number}
   */
  get snowNoisiness() {
    return this.uniforms.get("snowNoisiness").value;
  }
  set snowNoisiness(e) {
    this.uniforms.get("snowNoisiness").value = e;
  }
  /**
   * 积雪堆积强度，值越高在坡度变化处积雪堆积越明显
   *
   * @type {Number}
   */
  get snowAccumulation() {
    return this.uniforms.get("snowAccumulation").value;
  }
  set snowAccumulation(e) {
    this.uniforms.get("snowAccumulation").value = e;
  }
  /**
   * 更新效果
   *
   * @param {WebGLRenderer} renderer - 渲染器
   * @param {WebGLRenderTarget} inputBuffer - 输入缓冲区
   * @param {Number} [deltaTime] - 渲染帧间隔时间
   */
  update(e, t, r) {
    this.uniforms.get("depthBuffer").value = this.composer.depthTexture, this.camera && (this.uniforms.get("cameraNear").value = this.camera.near, this.uniforms.get("cameraFar").value = this.camera.far, this.uniforms.get("cameraPosition").value.copy(this.camera.position), this.uniforms.get("cameraMatrixWorld").value.copy(this.camera.matrixWorld), this.uniforms.get("projectionMatrixInverse").value.copy(this.camera.projectionMatrix).invert());
  }
  /**
   * 当渲染尺寸改变时更新纹素大小
   *
   * @param {Number} width - 宽度
   * @param {Number} height - 高度
   */
  setSize(e, t) {
    this.uniforms.get("texelSize").value.set(1 / e, 1 / t);
  }
}
const ko = `uniform sampler2D noiseTexture;\r
uniform vec2 texelSize;\r
uniform float time;\r
uniform float density;\r
uniform float snowSpeed;\r
uniform float snowSize;\r
uniform vec2 windDirection;\r
uniform vec3 snowColor;\r
uniform float alphaTest;\r
uniform int debugMode; // 0=正常模式, 1=显示UV坐标, 2=显示雪花层1, 3=显示雪花层2, 等等\r
uniform int activeLayer; // 当前活跃的雪花层(0-4)，用于单独调试\r
\r
varying vec2 vUv;\r
varying vec2 vUvPattern;\r
\r
// 雪花函数 - 生成一片雪花\r
float snowflake(vec2 uv, float scale) {\r
    vec2 pos = vec2(uv * scale);\r
    \r
    // 使用噪声纹理创建随机位置\r
    vec2 seed = floor(pos);\r
    // 修改噪声采样坐标计算方式，确保更均匀的分布\r
    vec3 noise = texture2D(noiseTexture, seed / vec2(scale) * 0.03).rgb;\r
    \r
    // 计算雪花位置\r
    vec2 center = seed + noise.xy;\r
    \r
    // 动画 - 下落和飘动\r
    float t = time * snowSpeed;\r
    \r
    // 雪花下落 - 添加变化以使不同位置的雪花速度不同\r
    center.y -= t + noise.z * 8.0;\r
    \r
    // 雪花水平飘动 - 使用风向\r
    center.x += sin(t * (0.1 + noise.z * 0.1)) * windDirection.x;\r
    center.y += cos(t * (0.1 + noise.z * 0.1)) * windDirection.y;\r
    \r
    // 循环雪花位置\r
    center = fract(center);\r
    \r
    // 计算当前点到雪花中心的距离\r
    float dist = length(pos - center - 0.5 + noise.xy * 0.2) * 2.0;\r
    \r
    // 获取额外的噪声值用于雪花形状变化\r
    float shapeNoise = noise.z;\r
    \r
    // 创建雪花形状 - 根据噪声随机选择不同的形状\r
    float snowShape;\r
    \r
    if (shapeNoise < 0.3) {\r
        // 标准圆形雪花\r
        snowShape = 1.0 - smoothstep(0.0, snowSize * (0.3 + noise.z * 0.7), dist);\r
    } else if (shapeNoise < 0.6) {\r
        // 六边形形状的雪花\r
        vec2 hexCoord = pos - center - 0.5;\r
        float hexDist = max(abs(hexCoord.x), abs(hexCoord.y) * 0.866);\r
        snowShape = 1.0 - smoothstep(0.0, snowSize * 0.4, hexDist);\r
    } else {\r
        // 星形雪花\r
        vec2 starCoord = pos - center - 0.5;\r
        float angle = atan(starCoord.y, starCoord.x);\r
        float starFactor = abs(sin(angle * 4.0)) * 0.5 + 0.5;\r
        float starDist = dist * (1.0 - starFactor * 0.2);\r
        snowShape = 1.0 - smoothstep(0.0, snowSize * 0.5, starDist);\r
    }\r
    \r
    return snowShape;\r
}\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
    // 调试模式 - 显示UV坐标\r
    if (debugMode == 1) {\r
        outputColor = vec4(vUv.x, vUv.y, 0.0, 1.0);\r
        return;\r
    } else if (debugMode == 2) {\r
        outputColor = vec4(vUvPattern.x, vUvPattern.y, 0.0, 1.0);\r
        return;\r
    }\r
    \r
    // 应用透明度测试 - 如果原始颜色的透明度低于阈值，不添加雪花效果\r
    if(inputColor.a < alphaTest) {\r
        outputColor = inputColor;\r
        return;\r
    }\r
    \r
    // 创建多个不同大小的雪花层，使用不同的UV坐标和缩放系数\r
    float snow = 0.0;\r
    float layer1 = 0.0, layer2 = 0.0, layer3 = 0.0, layer4 = 0.0, layer5 = 0.0;\r
    \r
    // 雪花层1 - 使用基本UV坐标，大尺度\r
    layer1 = snowflake(vUv, 40.0) * 0.25;\r
    \r
    // 雪花层2 - 使用基本UV坐标，中尺度 \r
    layer2 = snowflake(vUv * 1.2, 25.0) * 0.25;\r
    \r
    // 雪花层3 - 使用旋转UV坐标，中小尺度\r
    layer3 = snowflake(vUvPattern, 15.0) * 0.2;\r
    \r
    // 雪花层4 - 使用旋转UV坐标，小尺度\r
    layer4 = snowflake(vUvPattern * 1.1, 12.0) * 0.15;\r
    \r
    // 雪花层5 - 使用混合UV坐标，超小尺度\r
    layer5 = snowflake(mix(vUv, vUvPattern, 0.5), 8.0) * 0.15;\r
    \r
    // 调试模式 - 单独显示某一层雪花\r
    if (debugMode >= 3 && debugMode <= 7) {\r
        int layerIndex = debugMode - 3;\r
        if (layerIndex == 0) snow = layer1 * 4.0;\r
        else if (layerIndex == 1) snow = layer2 * 4.0;\r
        else if (layerIndex == 2) snow = layer3 * 5.0;\r
        else if (layerIndex == 3) snow = layer4 * 6.0;\r
        else if (layerIndex == 4) snow = layer5 * 6.0;\r
    }\r
    // 正常模式 - 根据activeLayer设置显示全部或指定层\r
    else {\r
        if (activeLayer == -1 || activeLayer == 0) snow += layer1;\r
        if (activeLayer == -1 || activeLayer == 1) snow += layer2;\r
        if (activeLayer == -1 || activeLayer == 2) snow += layer3;\r
        if (activeLayer == -1 || activeLayer == 3) snow += layer4;\r
        if (activeLayer == -1 || activeLayer == 4) snow += layer5;\r
    }\r
    \r
    // 应用雪花密度，增加基础可见度\r
    snow *= density * 1.5;\r
    \r
    // 混合输入颜色和雪花颜色\r
    vec3 finalColor = mix(inputColor.rgb, snowColor, snow);\r
    \r
    // 调试模式 - 显示全屏填充的雪花颜色\r
    if (debugMode == 8) {\r
        finalColor = snowColor;\r
    }\r
    \r
    outputColor = vec4(finalColor, inputColor.a);\r
} `, Vo = `uniform vec2 texelSize;\r
\r
varying vec2 vUv;\r
varying vec2 vUvPattern;\r
\r
void mainSupport(const in vec2 uv) {\r
    // 基本UV坐标\r
    vUv = uv;\r
    \r
    // 用于雪花图案的稍微旋转的UV坐标\r
    float angle = 0.15; // 轻微旋转\r
    mat2 rotation = mat2(\r
        cos(angle), -sin(angle),\r
        sin(angle), cos(angle)\r
    );\r
    \r
    // 旋转并稍微缩放UV以获得更多变化\r
    vUvPattern = (rotation * (uv - 0.5) * 1.1) + 0.5;\r
}\r
\r
void main() {\r
    // 根据正规化设备坐标计算UV\r
    vec2 uv = position.xy * 0.5 + 0.5;\r
    \r
    // 调用辅助函数处理UV\r
    mainSupport(uv);\r
    \r
    // 设置顶点位置\r
    gl_Position = vec4(position.xy, 1.0, 1.0);\r
} `;
class Ho extends I {
  /**
      * 构造一个新的雪花飘落效果
      *
      * @param {Object} [options] - 效果选项
      * @param {BlendFunction} [options.blendFunction=BlendFunction.SCREEN] - 混合模式
      * @param {Number} [options.density=0.6] - 雪花密度，0-1
      * @param {Number} [options.snowSpeed=0.2] - 雪花下落速度
      * @param {Number} [options.snowSize=0.2] - 雪花大小
      * @param {Vector2} [options.windDirection=new Vector2(0.1, 0.0)] - 风向
      * @param {Color|String|Number} [options.snowColor=0xffffff] - 雪花颜色
      * @param {Number} [options.alphaTest=0.1] - 透明度测试阈值，低于此值的像素不会显示雪花
      * @param {Number} [options.debugMode=0] - 调试模式: 0=正常, 1=显示UV, 2=显示UV旋转, 3-7=显示不同雪花层, 8=纯色
      * @param {Number} [options.activeLayer=-1] - 活跃的雪花层: -1=全部, 0-4=单独某层
      */
  constructor({
    blendFunction: e = E.SCREEN,
    density: t = 0.6,
    snowSpeed: r = 0.2,
    snowSize: n = 0.2,
    windDirection: i = new g(0.1, 0),
    snowColor: s = 16777215,
    alphaTest: o = 0.1,
    debugMode: l = 0,
    activeLayer: u = -1
  } = {}) {
    super("SnowfallEffect", ko, {
      blendFunction: e,
      vertexShader: Vo,
      uniforms: /* @__PURE__ */ new Map([
        ["texelSize", new a(new g())],
        ["time", new a(0)],
        ["density", new a(t)],
        ["snowSpeed", new a(r)],
        ["snowSize", new a(n)],
        ["windDirection", new a(new g().copy(i))],
        ["snowColor", new a(new y(s))],
        ["noiseTexture", new a(null)],
        ["alphaTest", new a(o)],
        ["debugMode", new a(l)],
        ["activeLayer", new a(u)]
      ])
    }), this.time = 0, this._createNoiseTexture();
  }
  /**
      * 创建噪声纹理，用于雪花的随机变化
      *
      * @private
      */
  _createNoiseTexture() {
    try {
      const e = new Ie().load(wt);
      e.wrapS = G, e.wrapT = G, e.minFilter = F, e.magFilter = F, e.needsUpdate = !0, this.uniforms.get("noiseTexture").value = e;
    } catch (e) {
      console.error("无法加载雪花纹理:", e);
      const t = 128, r = new Uint8Array(t * t * 4);
      for (let i = 0; i < t; i++)
        for (let s = 0; s < t; s++) {
          const o = (i * t + s) * 4;
          r[o] = Math.floor(Math.random() * 255), r[o + 1] = Math.floor(Math.random() * 255), r[o + 2] = Math.floor(Math.random() * 255), r[o + 3] = 255;
        }
      const n = new le(r, t, t, ne, se);
      n.wrapS = G, n.wrapT = G, n.minFilter = F, n.magFilter = F, n.needsUpdate = !0, this.uniforms.get("noiseTexture").value = n, console.log("使用备用噪声纹理");
    }
  }
  /**
      * 调试模式
      * 0=正常, 1=显示UV, 2=显示UV旋转, 3-7=显示不同雪花层, 8=纯色
      * @type {Number}
      */
  get debugMode() {
    return this.uniforms.get("debugMode").value;
  }
  set debugMode(e) {
    this.uniforms.get("debugMode").value = e;
  }
  /**
      * 活跃的雪花层
      * -1=全部, 0-4=单独某层
      * @type {Number}
      */
  get activeLayer() {
    return this.uniforms.get("activeLayer").value;
  }
  set activeLayer(e) {
    this.uniforms.get("activeLayer").value = e;
  }
  /**
      * 透明度测试阈值
      *
      * @type {Number}
      */
  get alphaTest() {
    return this.uniforms.get("alphaTest").value;
  }
  set alphaTest(e) {
    this.uniforms.get("alphaTest").value = e;
  }
  /**
      * 雪花密度
      *
      * @type {Number}
      */
  get density() {
    return this.uniforms.get("density").value;
  }
  set density(e) {
    this.uniforms.get("density").value = e;
  }
  /**
      * 雪花下落速度
      *
      * @type {Number}
      */
  get snowSpeed() {
    return this.uniforms.get("snowSpeed").value;
  }
  set snowSpeed(e) {
    this.uniforms.get("snowSpeed").value = e;
  }
  /**
      * 雪花大小
      *
      * @type {Number}
      */
  get snowSize() {
    return this.uniforms.get("snowSize").value;
  }
  set snowSize(e) {
    this.uniforms.get("snowSize").value = e;
  }
  /**
      * 风向
      *
      * @type {Vector2}
      */
  get windDirection() {
    return this.uniforms.get("windDirection").value;
  }
  set windDirection(e) {
    this.uniforms.get("windDirection").value.copy(e);
  }
  /**
      * 雪花颜色
      *
      * @type {Color}
      */
  get snowColor() {
    return this.uniforms.get("snowColor").value;
  }
  set snowColor(e) {
    this.uniforms.get("snowColor").value.set(e);
  }
  /**
      * 更新效果
      *
      * @param {WebGLRenderer} renderer - 渲染器
      * @param {WebGLRenderTarget} inputBuffer - 输入缓冲区
      * @param {Number} [deltaTime] - 渲染帧间隔时间
      */
  update(e, t, r) {
    this.time += r || 0.016, this.uniforms.get("time").value = this.time;
  }
  /**
      * 当渲染尺寸改变时更新纹素大小
      *
      * @param {Number} width - 宽度
      * @param {Number} height - 高度
      */
  setSize(e, t) {
    this.uniforms.get("texelSize").value.set(1 / e, 1 / t);
  }
}
const Go = `#include <common>\r
#include <packing>\r
\r
#ifdef NORMAL_DEPTH\r
\r
	#ifdef GL_FRAGMENT_PRECISION_HIGH\r
\r
		uniform highp sampler2D normalDepthBuffer;\r
\r
	#else\r
\r
		uniform mediump sampler2D normalDepthBuffer;\r
\r
	#endif\r
\r
	float readDepth(const in vec2 uv) {\r
\r
		return texture2D(normalDepthBuffer, uv).a;\r
\r
	}\r
\r
#else\r
\r
	uniform lowp sampler2D normalBuffer;\r
\r
	#if DEPTH_PACKING == 3201\r
\r
		uniform lowp sampler2D depthBuffer;\r
\r
	#elif defined(GL_FRAGMENT_PRECISION_HIGH)\r
\r
		uniform highp sampler2D depthBuffer;\r
\r
	#else\r
\r
		uniform mediump sampler2D depthBuffer;\r
\r
	#endif\r
\r
	float readDepth(const in vec2 uv) {\r
\r
		#if DEPTH_PACKING == 3201\r
\r
			return unpackRGBAToDepth(texture2D(depthBuffer, uv));\r
\r
		#else\r
\r
			return texture2D(depthBuffer, uv).r;\r
\r
		#endif\r
\r
	}\r
\r
#endif\r
\r
uniform lowp sampler2D noiseTexture;\r
\r
uniform mat4 inverseProjectionMatrix;\r
uniform mat4 projectionMatrix;\r
uniform vec2 texelSize;\r
uniform vec2 cameraNearFar;\r
\r
uniform float intensity;\r
uniform float minRadiusScale;\r
uniform float fade;\r
uniform float bias;\r
\r
uniform vec2 distanceCutoff;\r
uniform vec2 proximityCutoff;\r
\r
varying vec2 vUv;\r
varying vec2 vUv2;\r
\r
float getViewZ(const in float depth) {\r
\r
	#ifdef PERSPECTIVE_CAMERA\r
\r
		return perspectiveDepthToViewZ(depth, cameraNearFar.x, cameraNearFar.y);\r
\r
	#else\r
\r
		return orthographicDepthToViewZ(depth, cameraNearFar.x, cameraNearFar.y);\r
\r
	#endif\r
\r
}\r
\r
vec3 getViewPosition(const in vec2 screenPosition, const in float depth, const in float viewZ) {\r
\r
	vec4 clipPosition = vec4(vec3(screenPosition, depth) * 2.0 - 1.0, 1.0);\r
\r
	// Unoptimized version:\r
	// vec4 viewPosition = inverseProjectionMatrix * clipPosition;\r
	// viewPosition /= viewPosition.w; // Unproject.\r
	// return viewPosition.xyz;\r
\r
	float clipW = projectionMatrix[2][3] * viewZ + projectionMatrix[3][3];\r
	clipPosition *= clipW; // Unproject.\r
\r
	return (inverseProjectionMatrix * clipPosition).xyz;\r
\r
}\r
\r
float getAmbientOcclusion(const in vec3 p, const in vec3 n, const in float depth, const in vec2 uv) {\r
\r
	// Distance scaling\r
	float radiusScale = 1.0 - smoothstep(0.0, distanceCutoff.y, depth);\r
	radiusScale = radiusScale * (1.0 - minRadiusScale) + minRadiusScale;\r
	float radius = RADIUS * radiusScale;\r
\r
	// Use a random starting angle.\r
	float noise = texture2D(noiseTexture, vUv2).r;\r
	float baseAngle = noise * PI2;\r
	float rings = SPIRAL_TURNS * PI2;\r
\r
	float occlusion = 0.0;\r
	int taps = 0;\r
\r
	for(int i = 0; i < SAMPLES_INT; ++i) {\r
\r
		float alpha = (float(i) + 0.5) * INV_SAMPLES_FLOAT;\r
		float angle = alpha * rings + baseAngle;\r
		vec2 rotation = vec2(cos(angle), sin(angle));\r
		vec2 coords = alpha * radius * rotation * texelSize + uv;\r
\r
		if(coords.s < 0.0 || coords.s > 1.0 || coords.t < 0.0 || coords.t > 1.0) {\r
\r
			// Skip samples outside the screen.\r
			continue;\r
\r
		}\r
\r
		float sampleDepth = readDepth(coords);\r
		float viewZ = getViewZ(sampleDepth);\r
\r
		#ifdef PERSPECTIVE_CAMERA\r
\r
			float linearSampleDepth = viewZToOrthographicDepth(viewZ, cameraNearFar.x, cameraNearFar.y);\r
\r
		#else\r
\r
			float linearSampleDepth = sampleDepth;\r
\r
		#endif\r
\r
		float proximity = abs(depth - linearSampleDepth);\r
\r
		if(proximity < proximityCutoff.y) {\r
\r
			float falloff = 1.0 - smoothstep(proximityCutoff.x, proximityCutoff.y, proximity);\r
\r
			vec3 Q = getViewPosition(coords, sampleDepth, viewZ);\r
			vec3 v = Q - p;\r
\r
			float vv = dot(v, v);\r
			float vn = dot(v, n) - bias;\r
\r
			float f = max(RADIUS_SQ - vv, 0.0) / RADIUS_SQ;\r
			occlusion += (f * f * f * max(vn / (fade + vv), 0.0)) * falloff;\r
\r
		}\r
\r
		++taps;\r
\r
	}\r
\r
	return occlusion / (4.0 * max(float(taps), 1.0));\r
\r
}\r
\r
void main() {\r
\r
	#ifdef NORMAL_DEPTH\r
\r
		vec4 normalDepth = texture2D(normalDepthBuffer, vUv);\r
\r
	#else\r
\r
		vec4 normalDepth = vec4(texture2D(normalBuffer, vUv).xyz, readDepth(vUv));\r
\r
	#endif\r
\r
	float ao = 0.0;\r
	float depth = normalDepth.a;\r
	float viewZ = getViewZ(depth);\r
\r
	#ifdef PERSPECTIVE_CAMERA\r
\r
		float linearDepth = viewZToOrthographicDepth(viewZ, cameraNearFar.x, cameraNearFar.y);\r
\r
	#else\r
\r
		float linearDepth = depth;\r
\r
	#endif\r
\r
	// Skip fragments that are too far away.\r
	if(linearDepth < distanceCutoff.y) {\r
\r
		vec3 viewPosition = getViewPosition(vUv, depth, viewZ);\r
		vec3 viewNormal = unpackRGBToNormal(normalDepth.rgb);\r
		ao += getAmbientOcclusion(viewPosition, viewNormal, linearDepth, vUv);\r
\r
		// Fade AO based on depth.\r
		float d = smoothstep(distanceCutoff.x, distanceCutoff.y, linearDepth);\r
		ao = mix(ao, 0.0, d);\r
\r
		#ifdef LEGACY_INTENSITY\r
\r
			ao = clamp(1.0 - pow(1.0 - ao, abs(intensity)), 0.0, 1.0);\r
\r
		#endif\r
\r
	}\r
\r
	gl_FragColor.r = ao;\r
\r
}\r
`, Wo = `uniform vec2 noiseScale;\r
\r
varying vec2 vUv;\r
varying vec2 vUv2;\r
\r
void main() {\r
\r
	vUv = position.xy * 0.5 + 0.5;\r
	vUv2 = vUv * noiseScale;\r
\r
	gl_Position = vec4(position.xy, 1.0, 1.0);\r
\r
}\r
`;
class Bi extends N {
  /**
   * Constructs a new SSAO material.
   *
   * @param {Camera} camera - A camera.
   */
  constructor(e) {
    super({
      name: "SSAOMaterial",
      defines: {
        SAMPLES_INT: "0",
        INV_SAMPLES_FLOAT: "0.0",
        SPIRAL_TURNS: "0.0",
        RADIUS: "1.0",
        RADIUS_SQ: "1.0",
        DISTANCE_SCALING: "1",
        DEPTH_PACKING: "0"
      },
      uniforms: {
        depthBuffer: new a(null),
        normalBuffer: new a(null),
        normalDepthBuffer: new a(null),
        noiseTexture: new a(null),
        inverseProjectionMatrix: new a(new C()),
        projectionMatrix: new a(new C()),
        texelSize: new a(new g()),
        cameraNearFar: new a(new g()),
        distanceCutoff: new a(new g()),
        proximityCutoff: new a(new g()),
        noiseScale: new a(new g()),
        minRadiusScale: new a(0.33),
        intensity: new a(1),
        fade: new a(0.01),
        bias: new a(0)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: Go,
      vertexShader: Wo
    }), this.copyCameraSettings(e), this.resolution = new g(), this.r = 1;
  }
  /**
   * The current near plane setting.
   *
   * @type {Number}
   * @private
   */
  get near() {
    return this.uniforms.cameraNearFar.value.x;
  }
  /**
   * The current far plane setting.
   *
   * @type {Number}
   * @private
   */
  get far() {
    return this.uniforms.cameraNearFar.value.y;
  }
  /**
   * A combined normal-depth buffer.
   *
   * @type {Texture}
   */
  set normalDepthBuffer(e) {
    this.uniforms.normalDepthBuffer.value = e, e !== null ? this.defines.NORMAL_DEPTH = "1" : delete this.defines.NORMAL_DEPTH, this.needsUpdate = !0;
  }
  /**
   * Sets the combined normal-depth buffer.
   *
   * @deprecated Use normalDepthBuffer instead.
   * @param {Number} value - The buffer.
   */
  setNormalDepthBuffer(e) {
    this.normalDepthBuffer = e;
  }
  /**
   * The normal buffer.
   *
   * @type {Texture}
   */
  set normalBuffer(e) {
    this.uniforms.normalBuffer.value = e;
  }
  /**
   * Sets the normal buffer.
   *
   * @deprecated Use normalBuffer instead.
   * @param {Number} value - The buffer.
   */
  setNormalBuffer(e) {
    this.uniforms.normalBuffer.value = e;
  }
  /**
   * The depth buffer.
   *
   * @type {Texture}
   */
  set depthBuffer(e) {
    this.uniforms.depthBuffer.value = e;
  }
  /**
   * The depth packing strategy.
   *
   * @type {DepthPackingStrategies}
   */
  set depthPacking(e) {
    this.defines.DEPTH_PACKING = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Sets the depth buffer.
   *
   * @deprecated Use depthBuffer and depthPacking instead.
   * @param {Texture} buffer - The depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing strategy.
   */
  setDepthBuffer(e, t = $) {
    this.depthBuffer = e, this.depthPacking = t;
  }
  /**
   * The noise texture.
   *
   * @type {Texture}
   */
  set noiseTexture(e) {
    this.uniforms.noiseTexture.value = e;
  }
  /**
   * Sets the noise texture.
   *
   * @deprecated Use noiseTexture instead.
   * @param {Number} value - The texture.
   */
  setNoiseTexture(e) {
    this.uniforms.noiseTexture.value = e;
  }
  /**
   * The sample count.
   *
   * @type {Number}
   */
  get samples() {
    return Number(this.defines.SAMPLES_INT);
  }
  set samples(e) {
    this.defines.SAMPLES_INT = e.toFixed(0), this.defines.INV_SAMPLES_FLOAT = (1 / e).toFixed(9), this.needsUpdate = !0;
  }
  /**
   * Returns the amount of occlusion samples per pixel.
   *
   * @deprecated Use samples instead.
   * @return {Number} The sample count.
   */
  getSamples() {
    return this.samples;
  }
  /**
   * Sets the amount of occlusion samples per pixel.
   *
   * @deprecated Use samples instead.
   * @param {Number} value - The sample count.
   */
  setSamples(e) {
    this.samples = e;
  }
  /**
   * The sampling spiral ring count.
   *
   * @type {Number}
   */
  get rings() {
    return Number(this.defines.SPIRAL_TURNS);
  }
  set rings(e) {
    this.defines.SPIRAL_TURNS = e.toFixed(1), this.needsUpdate = !0;
  }
  /**
   * Returns the amount of spiral turns in the occlusion sampling pattern.
   *
   * @deprecated Use rings instead.
   * @return {Number} The radius.
   */
  getRings() {
    return this.rings;
  }
  /**
   * Sets the amount of spiral turns in the occlusion sampling pattern.
   *
   * @deprecated Use rings instead.
   * @param {Number} value - The radius.
   */
  setRings(e) {
    this.rings = e;
  }
  /**
   * The intensity.
   *
   * @type {Number}
   * @deprecated Use SSAOEffect.intensity instead.
   */
  get intensity() {
    return this.uniforms.intensity.value;
  }
  set intensity(e) {
    this.uniforms.intensity.value = e, this.defines.LEGACY_INTENSITY === void 0 && (this.defines.LEGACY_INTENSITY = "1", this.needsUpdate = !0);
  }
  /**
   * Returns the intensity.
   *
   * @deprecated Use SSAOEffect.intensity instead.
   * @return {Number} The intensity.
   */
  getIntensity() {
    return this.uniforms.intensity.value;
  }
  /**
   * Sets the intensity.
   *
   * @deprecated Use SSAOEffect.intensity instead.
   * @param {Number} value - The intensity.
   */
  setIntensity(e) {
    this.uniforms.intensity.value = e;
  }
  /**
   * The depth fade factor.
   *
   * @type {Number}
   */
  get fade() {
    return this.uniforms.fade.value;
  }
  set fade(e) {
    this.uniforms.fade.value = e;
  }
  /**
   * Returns the depth fade factor.
   *
   * @deprecated Use fade instead.
   * @return {Number} The fade factor.
   */
  getFade() {
    return this.uniforms.fade.value;
  }
  /**
   * Sets the depth fade factor.
   *
   * @deprecated Use fade instead.
   * @param {Number} value - The fade factor.
   */
  setFade(e) {
    this.uniforms.fade.value = e;
  }
  /**
   * The depth bias. Range: [0.0, 1.0].
   *
   * @type {Number}
   */
  get bias() {
    return this.uniforms.bias.value;
  }
  set bias(e) {
    this.uniforms.bias.value = e;
  }
  /**
   * Returns the depth bias.
   *
   * @deprecated Use bias instead.
   * @return {Number} The bias.
   */
  getBias() {
    return this.uniforms.bias.value;
  }
  /**
   * Sets the depth bias.
   *
   * @deprecated Use bias instead.
   * @param {Number} value - The bias.
   */
  setBias(e) {
    this.uniforms.bias.value = e;
  }
  /**
   * The minimum radius scale for distance scaling. Range: [0.0, 1.0].
   *
   * @type {Number}
   */
  get minRadiusScale() {
    return this.uniforms.minRadiusScale.value;
  }
  set minRadiusScale(e) {
    this.uniforms.minRadiusScale.value = e;
  }
  /**
   * Returns the minimum radius scale for distance scaling.
   *
   * @deprecated Use minRadiusScale instead.
   * @return {Number} The minimum radius scale.
   */
  getMinRadiusScale() {
    return this.uniforms.minRadiusScale.value;
  }
  /**
   * Sets the minimum radius scale for distance scaling.
   *
   * @deprecated Use minRadiusScale instead.
   * @param {Number} value - The minimum radius scale.
   */
  setMinRadiusScale(e) {
    this.uniforms.minRadiusScale.value = e;
  }
  /**
   * Updates the absolute radius.
   *
   * @private
   */
  updateRadius() {
    const e = this.r * this.resolution.height;
    this.defines.RADIUS = e.toFixed(11), this.defines.RADIUS_SQ = (e * e).toFixed(11), this.needsUpdate = !0;
  }
  /**
   * The occlusion sampling radius. Range: [0.0, 1.0].
   *
   * @type {Number}
   */
  get radius() {
    return this.r;
  }
  set radius(e) {
    this.r = Math.min(Math.max(e, 1e-6), 1), this.updateRadius();
  }
  /**
   * Returns the occlusion sampling radius.
   *
   * @deprecated Use radius instead.
   * @return {Number} The radius.
   */
  getRadius() {
    return this.radius;
  }
  /**
   * Sets the occlusion sampling radius.
   *
   * @deprecated Use radius instead.
   * @param {Number} value - The radius. Range [1e-6, 1.0].
   */
  setRadius(e) {
    this.radius = e;
  }
  /**
   * Indicates whether distance-based radius scaling is enabled.
   *
   * @type {Boolean}
   * @deprecated
   */
  get distanceScaling() {
    return !0;
  }
  set distanceScaling(e) {
  }
  /**
   * Indicates whether distance-based radius scaling is enabled.
   *
   * @deprecated
   * @return {Boolean} Whether distance scaling is enabled.
   */
  isDistanceScalingEnabled() {
    return this.distanceScaling;
  }
  /**
   * Enables or disables distance-based radius scaling.
   *
   * @deprecated
   * @param {Boolean} value - Whether distance scaling should be enabled.
   */
  setDistanceScalingEnabled(e) {
    this.distanceScaling = e;
  }
  /**
   * The occlusion distance threshold. Range: [0.0, 1.0].
   *
   * @type {Number}
   */
  get distanceThreshold() {
    return this.uniforms.distanceCutoff.value.x;
  }
  set distanceThreshold(e) {
    this.uniforms.distanceCutoff.value.set(
      Math.min(Math.max(e, 0), 1),
      Math.min(Math.max(e + this.distanceFalloff, 0), 1)
    );
  }
  /**
   * The occlusion distance threshold in world units.
   *
   * @type {Number}
   */
  get worldDistanceThreshold() {
    return -$e(this.distanceThreshold, this.near, this.far);
  }
  set worldDistanceThreshold(e) {
    this.distanceThreshold = Qe(-e, this.near, this.far);
  }
  /**
   * The occlusion distance falloff. Range: [0.0, 1.0].
   *
   * @type {Number}
   */
  get distanceFalloff() {
    return this.uniforms.distanceCutoff.value.y - this.distanceThreshold;
  }
  set distanceFalloff(e) {
    this.uniforms.distanceCutoff.value.y = Math.min(Math.max(this.distanceThreshold + e, 0), 1);
  }
  /**
   * The occlusion distance falloff in world units.
   *
   * @type {Number}
   */
  get worldDistanceFalloff() {
    return -$e(this.distanceFalloff, this.near, this.far);
  }
  set worldDistanceFalloff(e) {
    this.distanceFalloff = Qe(-e, this.near, this.far);
  }
  /**
   * Sets the occlusion distance cutoff.
   *
   * @deprecated Use distanceThreshold and distanceFalloff instead.
   * @param {Number} threshold - The distance threshold. Range [0.0, 1.0].
   * @param {Number} falloff - The falloff. Range [0.0, 1.0].
   */
  setDistanceCutoff(e, t) {
    this.uniforms.distanceCutoff.value.set(
      Math.min(Math.max(e, 0), 1),
      Math.min(Math.max(e + t, 0), 1)
    );
  }
  /**
   * The occlusion proximity threshold. Range: [0.0, 1.0].
   *
   * @type {Number}
   */
  get proximityThreshold() {
    return this.uniforms.proximityCutoff.value.x;
  }
  set proximityThreshold(e) {
    this.uniforms.proximityCutoff.value.set(
      Math.min(Math.max(e, 0), 1),
      Math.min(Math.max(e + this.proximityFalloff, 0), 1)
    );
  }
  /**
   * The occlusion proximity threshold in world units.
   *
   * @type {Number}
   */
  get worldProximityThreshold() {
    return -$e(this.proximityThreshold, this.near, this.far);
  }
  set worldProximityThreshold(e) {
    this.proximityThreshold = Qe(-e, this.near, this.far);
  }
  /**
   * The occlusion proximity falloff. Range: [0.0, 1.0].
   *
   * @type {Number}
   */
  get proximityFalloff() {
    return this.uniforms.proximityCutoff.value.y - this.proximityThreshold;
  }
  set proximityFalloff(e) {
    this.uniforms.proximityCutoff.value.y = Math.min(Math.max(this.proximityThreshold + e, 0), 1);
  }
  /**
   * The occlusion proximity falloff in world units.
   *
   * @type {Number}
   */
  get worldProximityFalloff() {
    return -$e(this.proximityFalloff, this.near, this.far);
  }
  set worldProximityFalloff(e) {
    this.proximityFalloff = Qe(-e, this.near, this.far);
  }
  /**
   * Sets the occlusion proximity cutoff.
   *
   * @deprecated Use proximityThreshold and proximityFalloff instead.
   * @param {Number} threshold - The range threshold. Range [0.0, 1.0].
   * @param {Number} falloff - The falloff. Range [0.0, 1.0].
   */
  setProximityCutoff(e, t) {
    this.uniforms.proximityCutoff.value.set(
      Math.min(Math.max(e, 0), 1),
      Math.min(Math.max(e + t, 0), 1)
    );
  }
  /**
   * Sets the texel size.
   *
   * @deprecated Use setSize() instead.
   * @param {Number} x - The texel width.
   * @param {Number} y - The texel height.
   */
  setTexelSize(e, t) {
    this.uniforms.texelSize.value.set(e, t);
  }
  /**
   * Copies the settings of the given camera.
   *
   * @deprecated Use copyCameraSettings instead.
   * @param {Camera} camera - A camera.
   */
  adoptCameraSettings(e) {
    this.copyCameraSettings(e);
  }
  /**
   * Copies the settings of the given camera.
   *
   * @param {Camera} camera - A camera.
   */
  copyCameraSettings(e) {
    e && (this.uniforms.cameraNearFar.value.set(e.near, e.far), this.uniforms.projectionMatrix.value.copy(e.projectionMatrix), this.uniforms.inverseProjectionMatrix.value.copy(e.projectionMatrix).invert(), e instanceof ke ? this.defines.PERSPECTIVE_CAMERA = "1" : delete this.defines.PERSPECTIVE_CAMERA, this.needsUpdate = !0);
  }
  /**
   * Sets the size of this object.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.uniforms, n = r.noiseTexture.value;
    n !== null && r.noiseScale.value.set(
      e / n.image.width,
      t / n.image.height
    ), r.texelSize.value.set(1 / e, 1 / t), this.resolution.set(e, t), this.updateRadius();
  }
}
const jo = `#include <packing>\r
\r
#ifdef GL_FRAGMENT_PRECISION_HIGH\r
\r
	uniform highp sampler2D depthBuffer;\r
\r
#else\r
\r
	uniform mediump sampler2D depthBuffer;\r
\r
#endif\r
\r
#ifdef DOWNSAMPLE_NORMALS\r
\r
	uniform lowp sampler2D normalBuffer;\r
\r
#endif\r
\r
varying vec2 vUv0;\r
varying vec2 vUv1;\r
varying vec2 vUv2;\r
varying vec2 vUv3;\r
\r
float readDepth(const in vec2 uv) {\r
\r
	#if DEPTH_PACKING == 3201\r
\r
		return unpackRGBAToDepth(texture2D(depthBuffer, uv));\r
\r
	#else\r
\r
		return texture2D(depthBuffer, uv).r;\r
\r
	#endif\r
\r
}\r
\r
/**\r
 * Returns the index of the most representative depth in the 2x2 neighborhood.\r
 */\r
\r
int findBestDepth(const in float samples[4]) {\r
\r
	// Calculate the centroid.\r
	float c = (samples[0] + samples[1] + samples[2] + samples[3]) * 0.25;\r
\r
	float distances[4];\r
	distances[0] = abs(c - samples[0]);\r
	distances[1] = abs(c - samples[1]);\r
	distances[2] = abs(c - samples[2]);\r
	distances[3] = abs(c - samples[3]);\r
\r
	float maxDistance = max(\r
		max(distances[0], distances[1]),\r
		max(distances[2], distances[3])\r
	);\r
\r
	int remaining[3];\r
	int rejected[3];\r
\r
	int i, j, k;\r
\r
	for(i = 0, j = 0, k = 0; i < 4; ++i) {\r
\r
		if(distances[i] < maxDistance) {\r
\r
			// Keep the most representative samples.\r
			remaining[j++] = i;\r
\r
		} else {\r
\r
			// Discard max distance samples.\r
			rejected[k++] = i;\r
\r
		}\r
\r
	}\r
\r
	// Fill up the array in case there were two or more max distance samples.\r
	for(; j < 3; ++j) {\r
\r
		remaining[j] = rejected[--k];\r
\r
	}\r
\r
	// Final candidates.\r
	vec3 s = vec3(\r
		samples[remaining[0]],\r
		samples[remaining[1]],\r
		samples[remaining[2]]\r
	);\r
\r
	// Recalculate the controid.\r
	c = (s.x + s.y + s.z) / 3.0;\r
\r
	distances[0] = abs(c - s.x);\r
	distances[1] = abs(c - s.y);\r
	distances[2] = abs(c - s.z);\r
\r
	float minDistance = min(distances[0], min(distances[1], distances[2]));\r
\r
	// Determine the index of the min distance sample.\r
	for(i = 0; i < 3; ++i) {\r
\r
		if(distances[i] == minDistance) {\r
\r
			break;\r
\r
		}\r
\r
	}\r
\r
	return remaining[i];\r
\r
}\r
\r
void main() {\r
\r
	// Gather depth samples in a 2x2 neighborhood.\r
	float d[4];\r
	d[0] = readDepth(vUv0);\r
	d[1] = readDepth(vUv1);\r
	d[2] = readDepth(vUv2);\r
	d[3] = readDepth(vUv3);\r
\r
	int index = findBestDepth(d);\r
\r
	#ifdef DOWNSAMPLE_NORMALS\r
\r
		// Gather all corresponding normals to avoid dependent texel fetches.\r
		vec3 n[4];\r
		n[0] = texture2D(normalBuffer, vUv0).rgb;\r
		n[1] = texture2D(normalBuffer, vUv1).rgb;\r
		n[2] = texture2D(normalBuffer, vUv2).rgb;\r
		n[3] = texture2D(normalBuffer, vUv3).rgb;\r
\r
	#else\r
\r
		vec3 n[4];\r
		n[0] = vec3(0.0);\r
		n[1] = vec3(0.0);\r
		n[2] = vec3(0.0);\r
		n[3] = vec3(0.0);\r
\r
	#endif\r
\r
	gl_FragColor = vec4(n[index], d[index]);\r
\r
}\r
`, Xo = `uniform vec2 texelSize;\r
\r
varying vec2 vUv0;\r
varying vec2 vUv1;\r
varying vec2 vUv2;\r
varying vec2 vUv3;\r
\r
void main() {\r
\r
	vec2 uv = position.xy * 0.5 + 0.5;\r
\r
	vUv0 = uv;\r
	vUv1 = vec2(uv.x, uv.y + texelSize.y);\r
	vUv2 = vec2(uv.x + texelSize.x, uv.y);\r
	vUv3 = uv + texelSize;\r
\r
	gl_Position = vec4(position.xy, 1.0, 1.0);\r
\r
}\r
`;
class Yo extends N {
  /**
   * Constructs a new depth downsampling material.
   */
  constructor() {
    super({
      name: "DepthDownsamplingMaterial",
      defines: {
        DEPTH_PACKING: "0"
      },
      uniforms: {
        depthBuffer: new a(null),
        normalBuffer: new a(null),
        texelSize: new a(new g())
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: jo,
      vertexShader: Xo
    });
  }
  /**
   * The depth buffer.
   *
   * @type {Texture}
   */
  set depthBuffer(e) {
    this.uniforms.depthBuffer.value = e;
  }
  /**
   * The depth packing strategy.
   *
   * @type {DepthPackingStrategies}
   */
  set depthPacking(e) {
    this.defines.DEPTH_PACKING = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Sets the depth buffer.
   *
   * @deprecated Use depthBuffer and depthPacking instead.
   * @param {Texture} buffer - The depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing strategy.
   */
  setDepthBuffer(e, t = $) {
    this.depthBuffer = e, this.depthPacking = t;
  }
  /**
   * The normal buffer.
   *
   * @type {Texture}
   */
  set normalBuffer(e) {
    this.uniforms.normalBuffer.value = e, e !== null ? this.defines.DOWNSAMPLE_NORMALS = "1" : delete this.defines.DOWNSAMPLE_NORMALS, this.needsUpdate = !0;
  }
  /**
   * Sets the normal buffer.
   *
   * @deprecated Use normalBuffer instead.
   * @param {Texture} value - The normal buffer.
   */
  setNormalBuffer(e) {
    this.normalBuffer = e;
  }
  /**
   * Sets the texel size.
   *
   * @deprecated Use setSize() instead.
   * @param {Number} x - The texel width.
   * @param {Number} y - The texel height.
   */
  setTexelSize(e, t) {
    this.uniforms.texelSize.value.set(e, t);
  }
  /**
   * Sets the size of this object.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.uniforms.texelSize.value.set(1 / e, 1 / t);
  }
}
class Li extends X {
  /**
   * Constructs a new depth downsampling pass.
   *
   * @param {Object} [options] - The options.
   * @param {Texture} [options.normalBuffer=null] - A texture that contains view space normals. See {@link NormalPass}.
   * @param {Number} [options.resolutionScale=0.5] - The resolution scale.
   * @param {Number} [options.resolutionX=Resolution.AUTO_SIZE] - The horizontal resolution.
   * @param {Number} [options.resolutionY=Resolution.AUTO_SIZE] - The vertical resolution.
   * @param {Number} [options.width=Resolution.AUTO_SIZE] - Deprecated. Use resolutionX instead.
   * @param {Number} [options.height=Resolution.AUTO_SIZE] - Deprecated. Use resolutionY instead.
   */
  constructor({
    normalBuffer: e = null,
    resolutionScale: t = 0.5,
    width: r = W.AUTO_SIZE,
    height: n = W.AUTO_SIZE,
    resolutionX: i = r,
    resolutionY: s = n
  } = {}) {
    super("DepthDownsamplingPass");
    const o = new Yo();
    o.normalBuffer = e, this.fullscreenMaterial = o, this.needsDepthTexture = !0, this.needsSwap = !1, this.renderTarget = new A(1, 1, {
      minFilter: b,
      magFilter: b,
      depthBuffer: !1,
      type: Y
    }), this.renderTarget.texture.name = "DepthDownsamplingPass.Target", this.renderTarget.texture.generateMipmaps = !1;
    const l = this.resolution = new W(this, i, s, t);
    l.addEventListener("change", (u) => this.setSize(l.baseWidth, l.baseHeight));
  }
  /**
   * The normal(RGB) + depth(A) texture.
   *
   * @type {Texture}
   */
  get texture() {
    return this.renderTarget.texture;
  }
  /**
   * Returns the normal(RGB) + depth(A) texture.
   *
   * @deprecated Use texture instead.
   * @return {Texture} The texture.
   */
  getTexture() {
    return this.renderTarget.texture;
  }
  /**
   * Returns the resolution settings.
   *
   * @deprecated Use resolution instead.
   * @return {Resolution} The resolution.
   */
  getResolution() {
    return this.resolution;
  }
  /**
   * Sets the depth texture.
   *
   * @param {Texture} depthTexture - A depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing strategy.
   */
  setDepthTexture(e, t = $) {
    this.fullscreenMaterial.depthBuffer = e, this.fullscreenMaterial.depthPacking = t;
  }
  /**
   * Downsamples depth and scene normals.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i) {
    e.setRenderTarget(this.renderToScreen ? null : this.renderTarget), e.render(this.scene, this.camera);
  }
  /**
   * Updates the size of this pass.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.resolution;
    r.setBaseSize(e, t), this.renderTarget.setSize(r.width, r.height), this.fullscreenMaterial.setSize(e, t);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    const n = e.getContext();
    if (!(n.getExtension("EXT_color_buffer_float") || n.getExtension("EXT_color_buffer_half_float")))
      throw new Error("Rendering to float texture is not supported.");
  }
}
const Ui = `uniform lowp sampler2D aoBuffer;\r
uniform float luminanceInfluence;\r
uniform float intensity;\r
\r
#if defined(DEPTH_AWARE_UPSAMPLING) && defined(NORMAL_DEPTH)\r
\r
	#ifdef GL_FRAGMENT_PRECISION_HIGH\r
\r
		uniform highp sampler2D normalDepthBuffer;\r
\r
	#else\r
\r
		uniform mediump sampler2D normalDepthBuffer;\r
\r
	#endif\r
\r
#endif\r
\r
#ifdef COLORIZE\r
\r
	uniform vec3 color;\r
\r
#endif\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, const in float depth, out vec4 outputColor) {\r
\r
	float aoLinear = texture2D(aoBuffer, uv).r;\r
\r
	#if defined(DEPTH_AWARE_UPSAMPLING) && defined(NORMAL_DEPTH) && __VERSION__ == 300\r
\r
		// Gather normals and depth in a 2x2 neighborhood.\r
		vec4 normalDepth[4];\r
		normalDepth[0] = textureOffset(normalDepthBuffer, uv, ivec2(0, 0));\r
		normalDepth[1] = textureOffset(normalDepthBuffer, uv, ivec2(0, 1));\r
		normalDepth[2] = textureOffset(normalDepthBuffer, uv, ivec2(1, 0));\r
		normalDepth[3] = textureOffset(normalDepthBuffer, uv, ivec2(1, 1));\r
\r
		// Determine the smoothness of the surface around this fragment.\r
		float dot01 = dot(normalDepth[0].rgb, normalDepth[1].rgb);\r
		float dot02 = dot(normalDepth[0].rgb, normalDepth[2].rgb);\r
		float dot03 = dot(normalDepth[0].rgb, normalDepth[3].rgb);\r
\r
		float minDot = min(dot01, min(dot02, dot03));\r
		float s = step(THRESHOLD, minDot);\r
\r
		// Find the best AO based on depth.\r
		float smallestDistance = 1.0;\r
		int index;\r
\r
		for(int i = 0; i < 4; ++i) {\r
\r
			float distance = abs(depth - normalDepth[i].a);\r
\r
			if(distance < smallestDistance) {\r
\r
				smallestDistance = distance;\r
				index = i;\r
\r
			}\r
\r
		}\r
 \r
		// Fetch the exact AO texel that corresponds to the best depth.\r
		ivec2 offsets[4];\r
		offsets[0] = ivec2(0, 0); offsets[1] = ivec2(0, 1);\r
		offsets[2] = ivec2(1, 0); offsets[3] = ivec2(1, 1);\r
\r
		ivec2 coord = ivec2(uv * vec2(textureSize(aoBuffer, 0))) + offsets[index];\r
		float aoNearest = texelFetch(aoBuffer, coord, 0).r;\r
\r
		// Smooth surfaces benefit more from linear filtering.\r
		float ao = mix(aoNearest, aoLinear, s);\r
\r
	#else\r
\r
		float ao = aoLinear;\r
\r
	#endif\r
\r
	// Fade AO based on luminance.\r
	float l = luminance(inputColor.rgb);\r
	ao = mix(ao, 0.0, l * luminanceInfluence);\r
	ao = clamp(ao * intensity, 0.0, 1.0);\r
\r
	#ifdef COLORIZE\r
\r
		outputColor = vec4(1.0 - ao * (1.0 - color), inputColor.a);\r
\r
	#else\r
\r
		outputColor = vec4(vec3(1.0 - ao), inputColor.a);\r
\r
	#endif\r
\r
}\r
`, Cn = 64;
let Zh = class extends I {
  /**
   * Constructs a new SSAO effect.
   *
   * @todo Move normalBuffer to options.
   * @param {Camera} [camera] - The main camera.
   * @param {Texture} [normalBuffer] - A texture that contains the scene normals.
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.MULTIPLY] - The blend function of this effect.
   * @param {Boolean} [options.distanceScaling=true] - Deprecated.
   * @param {Boolean} [options.depthAwareUpsampling=true] - Enables or disables depth-aware upsampling. Has no effect if WebGL 2 is not supported.
   * @param {Texture} [options.normalDepthBuffer=null] - Deprecated.
   * @param {Object} [options.gBufferTextures=null] - G-Buffer textures from RenderPass. If provided, will use these instead of generating own buffers.
   * @param {Texture} [options.gBufferTextures.gNormal] - G-Buffer normal texture.
   * @param {Texture} [options.gBufferTextures.gDepth] - G-Buffer depth texture.
   * @param {Texture} [options.gBufferTextures.gPosition] - G-Buffer position texture (optional).
   * @param {Number} [options.samples=9] - The amount of samples per pixel. Should not be a multiple of the ring count.
   * @param {Number} [options.rings=7] - The amount of spiral turns in the occlusion sampling pattern. Should be a prime number.
   * @param {Number} [options.worldDistanceThreshold] - The world distance threshold at which the occlusion effect starts to fade out.
   * @param {Number} [options.worldDistanceFalloff] - The world distance falloff. Influences the smoothness of the occlusion cutoff.
   * @param {Number} [options.worldProximityThreshold] - The world proximity threshold at which the occlusion starts to fade out.
   * @param {Number} [options.worldProximityFalloff] - The world proximity falloff. Influences the smoothness of the proximity cutoff.
   * @param {Number} [options.distanceThreshold=0.97] - Deprecated.
   * @param {Number} [options.distanceFalloff=0.03] - Deprecated.
   * @param {Number} [options.rangeThreshold=0.0005] - Deprecated.
   * @param {Number} [options.rangeFalloff=0.001] - Deprecated.
   * @param {Number} [options.minRadiusScale=0.1] - The minimum radius scale.
   * @param {Number} [options.luminanceInfluence=0.7] - Determines how much the luminance of the scene influences the ambient occlusion.
   * @param {Number} [options.radius=0.1825] - The occlusion sampling radius, expressed as a scale relative to the resolution. Range [1e-6, 1.0].
   * @param {Number} [options.intensity=1.0] - The intensity of the ambient occlusion.
   * @param {Number} [options.bias=0.025] - An occlusion bias. Eliminates artifacts caused by depth discontinuities.
   * @param {Number} [options.fade=0.01] - Influences the smoothness of the shadows. A lower value results in higher contrast.
   * @param {Color} [options.color=null] - The color of the ambient occlusion.
   * @param {Number} [options.resolutionScale=1.0] - The resolution scale.
   * @param {Number} [options.resolutionX=Resolution.AUTO_SIZE] - The horizontal resolution.
   * @param {Number} [options.resolutionY=Resolution.AUTO_SIZE] - The vertical resolution.
   * @param {Number} [options.width=Resolution.AUTO_SIZE] - Deprecated. Use resolutionX instead.
   * @param {Number} [options.height=Resolution.AUTO_SIZE] - Deprecated. Use resolutionY instead.
   */
  constructor(e, t, {
    blendFunction: r = E.MULTIPLY,
    samples: n = 9,
    rings: i = 7,
    normalDepthBuffer: s = null,
    gBufferTextures: o = null,
    depthAwareUpsampling: l = !0,
    worldDistanceThreshold: u,
    worldDistanceFalloff: f,
    worldProximityThreshold: h,
    worldProximityFalloff: m,
    distanceThreshold: p = 0.97,
    distanceFalloff: d = 0.03,
    rangeThreshold: v = 5e-4,
    rangeFalloff: x = 1e-3,
    minRadiusScale: w = 0.1,
    luminanceInfluence: M = 0.7,
    radius: D = 0.1825,
    intensity: P = 1,
    bias: O = 0.025,
    fade: S = 0.01,
    color: L = null,
    resolutionScale: H = 1,
    width: Z = W.AUTO_SIZE,
    height: z = W.AUTO_SIZE,
    resolutionX: k = Z,
    resolutionY: R = z
  } = {}) {
    super("SSAOEffect", Ui, {
      blendFunction: r,
      attributes: Q.DEPTH,
      defines: /* @__PURE__ */ new Map([
        ["THRESHOLD", "0.997"]
      ]),
      uniforms: /* @__PURE__ */ new Map([
        ["aoBuffer", new a(null)],
        ["normalDepthBuffer", new a(s)],
        ["luminanceInfluence", new a(M)],
        ["color", new a(null)],
        ["intensity", new a(P)],
        ["scale", new a(0)]
        // Unused.
      ])
    }), this.renderTarget = new A(1, 1, { depthBuffer: !1 }), this.renderTarget.texture.name = "AO.Target", this.uniforms.get("aoBuffer").value = this.renderTarget.texture;
    const U = this.resolution = new W(this, k, R, H);
    U.addEventListener("change", (Pe) => this.setSize(U.baseWidth, U.baseHeight)), this.camera = e, this.gBufferTextures = o;
    const q = o && o.gNormal && o.gDepth;
    q && (console.log("🎯 SSAOEffect: 使用 GBuffer 纹理"), console.log("  - gNormal:", !!o.gNormal), console.log("  - gDepth:", !!o.gDepth), console.log("  - gPosition:", !!o.gPosition)), this.depthDownsamplingPass = new Li({
      normalBuffer: q ? o.gNormal : t,
      resolutionScale: H
    }), this.depthDownsamplingPass.enabled = !q && s === null, this.ssaoPass = new ee(new Bi(e));
    const ie = new lr(Cn, Cn, ne);
    ie.wrapS = ie.wrapT = G;
    const J = this.ssaoMaterial;
    J.normalBuffer = q ? o.gNormal : t, J.noiseTexture = ie, J.minRadiusScale = w, J.samples = n, J.radius = D, J.rings = i, J.fade = S, J.bias = O, J.distanceThreshold = p, J.distanceFalloff = d, J.proximityThreshold = v, J.proximityFalloff = x, u !== void 0 && (J.worldDistanceThreshold = u), f !== void 0 && (J.worldDistanceFalloff = f), h !== void 0 && (J.worldProximityThreshold = h), m !== void 0 && (J.worldProximityFalloff = m), s !== null ? (this.ssaoMaterial.normalDepthBuffer = s, this.defines.set("NORMAL_DEPTH", "1")) : q && console.log("🎯 SSAOEffect: 使用 GBuffer 作为 normalDepthBuffer 来源"), this.depthAwareUpsampling = l, this.color = L;
  }
  set mainCamera(e) {
    this.camera = e, this.ssaoMaterial.copyCameraSettings(e);
  }
  /**
   * Sets the normal buffer.
   *
   * @type {Texture}
   */
  get normalBuffer() {
    return this.ssaoMaterial.normalBuffer;
  }
  set normalBuffer(e) {
    this.ssaoMaterial.normalBuffer = e, this.depthDownsamplingPass.fullscreenMaterial.normalBuffer = e;
  }
  /**
   * Returns the resolution settings.
   *
   * @deprecated Use resolution instead.
   * @return {Resolution} The resolution.
   */
  getResolution() {
    return this.resolution;
  }
  /**
   * The SSAO material.
   *
   * @type {SSAOMaterial}
   */
  get ssaoMaterial() {
    return this.ssaoPass.fullscreenMaterial;
  }
  /**
   * Returns the SSAO material.
   *
   * @deprecated Use ssaoMaterial instead.
   * @return {SSAOMaterial} The material.
   */
  getSSAOMaterial() {
    return this.ssaoMaterial;
  }
  /**
   * The amount of occlusion samples per pixel.
   *
   * @type {Number}
   * @deprecated Use ssaoMaterial.samples instead.
   */
  get samples() {
    return this.ssaoMaterial.samples;
  }
  set samples(e) {
    this.ssaoMaterial.samples = e;
  }
  /**
   * The amount of spiral turns in the occlusion sampling pattern.
   *
   * @type {Number}
   * @deprecated Use ssaoMaterial.rings instead.
   */
  get rings() {
    return this.ssaoMaterial.rings;
  }
  set rings(e) {
    this.ssaoMaterial.rings = e;
  }
  /**
   * The occlusion sampling radius.
   *
   * @type {Number}
   * @deprecated Use ssaoMaterial.radius instead.
   */
  get radius() {
    return this.ssaoMaterial.radius;
  }
  set radius(e) {
    this.ssaoMaterial.radius = e;
  }
  /**
   * Indicates whether depth-aware upsampling is enabled.
   *
   * @type {Boolean}
   */
  get depthAwareUpsampling() {
    return this.defines.has("DEPTH_AWARE_UPSAMPLING");
  }
  set depthAwareUpsampling(e) {
    this.depthAwareUpsampling !== e && (e ? this.defines.set("DEPTH_AWARE_UPSAMPLING", "1") : this.defines.delete("DEPTH_AWARE_UPSAMPLING"), this.setChanged());
  }
  /**
   * Indicates whether depth-aware upsampling is enabled.
   *
   * @deprecated Use depthAwareUpsampling instead.
   * @return {Boolean} Whether depth-aware upsampling is enabled.
   */
  isDepthAwareUpsamplingEnabled() {
    return this.depthAwareUpsampling;
  }
  /**
   * Enables or disables depth-aware upsampling.
   *
   * @deprecated Use depthAwareUpsampling instead.
   * @param {Boolean} value - Whether depth-aware upsampling should be enabled.
   */
  setDepthAwareUpsamplingEnabled(e) {
    this.depthAwareUpsampling = e;
  }
  /**
   * Indicates whether distance-based radius scaling is enabled.
   *
   * @type {Boolean}
   * @deprecated
   */
  get distanceScaling() {
    return !0;
  }
  set distanceScaling(e) {
  }
  /**
   * The color of the ambient occlusion. Set to `null` to disable.
   *
   * @type {Color}
   */
  get color() {
    return this.uniforms.get("color").value;
  }
  set color(e) {
    const t = this.uniforms, r = this.defines;
    e !== null ? r.has("COLORIZE") ? t.get("color").value.set(e) : (r.set("COLORIZE", "1"), t.get("color").value = new y(e), this.setChanged()) : r.has("COLORIZE") && (r.delete("COLORIZE"), t.get("color").value = null, this.setChanged());
  }
  /**
   * The luminance influence factor. Range: [0.0, 1.0].
   *
   * @type {Boolean}
   */
  get luminanceInfluence() {
    return this.uniforms.get("luminanceInfluence").value;
  }
  set luminanceInfluence(e) {
    this.uniforms.get("luminanceInfluence").value = e;
  }
  /**
   * The intensity.
   *
   * @type {Number}
   */
  get intensity() {
    return this.uniforms.get("intensity").value;
  }
  set intensity(e) {
    this.uniforms.get("intensity").value = e;
  }
  /**
   * Returns the color of the ambient occlusion.
   *
   * @deprecated Use color instead.
   * @return {Color} The color.
   */
  getColor() {
    return this.color;
  }
  /**
   * Sets the color of the ambient occlusion. Set to `null` to disable colorization.
   *
   * @deprecated Use color instead.
   * @param {Color} value - The color.
   */
  setColor(e) {
    this.color = e;
  }
  /**
   * Sets the occlusion distance cutoff.
   *
   * @deprecated Use ssaoMaterial instead.
   * @param {Number} threshold - The distance threshold. Range [0.0, 1.0].
   * @param {Number} falloff - The falloff. Range [0.0, 1.0].
   */
  setDistanceCutoff(e, t) {
    this.ssaoMaterial.distanceThreshold = e, this.ssaoMaterial.distanceFalloff = t;
  }
  /**
   * Sets the occlusion proximity cutoff.
   *
   * @deprecated Use ssaoMaterial instead.
   * @param {Number} threshold - The proximity threshold. Range [0.0, 1.0].
   * @param {Number} falloff - The falloff. Range [0.0, 1.0].
   */
  setProximityCutoff(e, t) {
    this.ssaoMaterial.proximityThreshold = e, this.ssaoMaterial.proximityFalloff = t;
  }
  /**
   * Sets the depth texture.
   *
   * @param {Texture} depthTexture - A depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing.
   */
  setDepthTexture(e, t = $) {
    this.depthDownsamplingPass.setDepthTexture(e, t), this.ssaoMaterial.depthBuffer = e, this.ssaoMaterial.depthPacking = t;
  }
  /**
   * Updates this effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   */
  update(e, t, r) {
    const n = this.renderTarget;
    this.depthDownsamplingPass.enabled && this.depthDownsamplingPass.render(e), this.ssaoPass.render(e, null, n);
  }
  /**
   * Sets the size.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.resolution;
    r.setBaseSize(e, t);
    const n = r.width, i = r.height;
    this.ssaoMaterial.copyCameraSettings(this.camera), this.ssaoMaterial.setSize(n, i), this.renderTarget.setSize(n, i), this.depthDownsamplingPass.resolution.scale = r.scale, this.depthDownsamplingPass.setSize(e, t);
  }
  /**
   * Sets the G-Buffer textures.
   *
   * @param {Object|null} gBufferTextures - The G-Buffer textures.
   * @param {Texture} [gBufferTextures.gNormal] - The normal texture.
   * @param {Texture} [gBufferTextures.gDepth] - The depth texture.
   * @param {Texture} [gBufferTextures.gPosition] - The position texture (optional).
   */
  setGBufferTextures(e) {
    this.gBufferTextures = e, e && e.gNormal && e.gDepth ? (console.log("🎯 SSAOEffect.setGBufferTextures: 启用 GBuffer 纹理"), console.log("  - gNormal:", !!e.gNormal), console.log("  - gDepth:", !!e.gDepth), console.log("  - gPosition:", !!e.gPosition), this.ssaoMaterial.normalBuffer = e.gNormal, this.depthDownsamplingPass.enabled = !1, this.defines.has("NORMAL_DEPTH") || (this.defines.set("NORMAL_DEPTH", "1"), this.setChanged())) : (console.log("🎯 SSAOEffect.setGBufferTextures: 禁用 GBuffer，恢复传统模式"), this.depthDownsamplingPass.enabled = !0);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    try {
      let n = this.uniforms.get("normalDepthBuffer").value;
      n === null && (this.gBufferTextures && this.gBufferTextures.gNormal && this.gBufferTextures.gDepth ? (console.log("🎯 SSAOEffect.initialize: 跳过 DepthDownsamplingPass，使用 GBuffer"), this.defines.set("NORMAL_DEPTH", "1")) : (this.depthDownsamplingPass.initialize(e, t, r), n = this.depthDownsamplingPass.texture, this.uniforms.get("normalDepthBuffer").value = n, this.ssaoMaterial.normalDepthBuffer = n, this.defines.set("NORMAL_DEPTH", "1")));
    } catch {
      this.depthDownsamplingPass.enabled = !1;
    }
  }
};
const An = 64;
class Qh extends I {
  /**
   * Constructs a new enhanced SSAO effect.
   *
   * @param {Camera} camera - The main camera.
   * @param {Texture} [normalBuffer] - A texture that contains the scene normals.
   * @param {Object} [options] - The options.
   * @param {Object} [options.gBufferTextures] - G-Buffer textures if available.
   * @param {Texture} [options.gBufferTextures.gNormal] - G-Buffer normal texture.
   * @param {Texture} [options.gBufferTextures.gDepth] - G-Buffer depth texture.
   * @param {Texture} [options.gBufferTextures.gPosition] - G-Buffer position texture.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.MULTIPLY] - The blend function.
   * @param {Number} [options.samples=9] - The amount of samples per pixel.
   * @param {Number} [options.rings=7] - The amount of spiral turns in the occlusion sampling pattern.
   * @param {Number} [options.radius=0.1825] - The occlusion sampling radius.
   * @param {Number} [options.intensity=1.0] - The intensity of the ambient occlusion.
   * @param {Number} [options.bias=0.025] - An occlusion bias.
   * @param {Number} [options.fade=0.01] - Influences the smoothness of the shadows.
   * @param {Color} [options.color=null] - The color of the ambient occlusion.
   * @param {Number} [options.resolutionScale=1.0] - The resolution scale.
   * @param {Number} [options.resolutionX=Resolution.AUTO_SIZE] - The horizontal resolution.
   * @param {Number} [options.resolutionY=Resolution.AUTO_SIZE] - The vertical resolution.
   */
  constructor(e, t, {
    gBufferTextures: r = null,
    blendFunction: n = E.MULTIPLY,
    samples: i = 9,
    rings: s = 7,
    normalDepthBuffer: o = null,
    depthAwareUpsampling: l = !0,
    worldDistanceThreshold: u,
    worldDistanceFalloff: f,
    worldProximityThreshold: h,
    worldProximityFalloff: m,
    distanceThreshold: p = 0.97,
    distanceFalloff: d = 0.03,
    rangeThreshold: v = 5e-4,
    rangeFalloff: x = 1e-3,
    minRadiusScale: w = 0.1,
    luminanceInfluence: M = 0.7,
    radius: D = 0.1825,
    intensity: P = 1,
    bias: O = 0.025,
    fade: S = 0.01,
    color: L = null,
    resolutionScale: H = 1,
    resolutionX: Z = W.AUTO_SIZE,
    resolutionY: z = W.AUTO_SIZE
  } = {}) {
    super("SSAOEffectGBufferEnhanced", Ui, {
      blendFunction: n,
      attributes: Q.DEPTH,
      defines: /* @__PURE__ */ new Map([
        ["THRESHOLD", "0.997"]
      ]),
      uniforms: /* @__PURE__ */ new Map([
        ["aoBuffer", new a(null)],
        ["normalDepthBuffer", new a(o)],
        ["luminanceInfluence", new a(M)],
        ["color", new a(null)],
        ["intensity", new a(P)],
        ["scale", new a(0)]
      ])
    }), this.gBufferTextures = r, this.usingGBuffer = this.gBufferTextures && this.gBufferTextures.gNormal, this.camera = e, this.renderTarget = new A(1, 1, { depthBuffer: !1 }), this.renderTarget.texture.name = "AO.Target", this.uniforms.get("aoBuffer").value = this.renderTarget.texture;
    const k = this.resolution = new W(this, Z, z, H);
    k.addEventListener("change", (q) => this.setSize(k.baseWidth, k.baseHeight)), this.depthDownsamplingPass = null, this.usingGBuffer || (this.depthDownsamplingPass = new Li({
      normalBuffer: t || this.gBufferTextures?.gNormal,
      resolutionScale: H
    }), this.depthDownsamplingPass.enabled = o === null), this.ssaoPass = new ee(new Bi(e));
    const R = new lr(An, An, ne);
    R.wrapS = R.wrapT = G;
    const U = this.ssaoMaterial;
    this.usingGBuffer ? (U.normalBuffer = this.gBufferTextures.gNormal, console.log("✅ SSAO: 使用G-Buffer法线数据")) : (U.normalBuffer = t, console.log("⚠️ SSAO: 使用传统法线缓冲区")), U.noiseTexture = R, U.minRadiusScale = w, U.samples = i, U.radius = D, U.rings = s, U.fade = S, U.bias = O, U.distanceThreshold = p, U.distanceFalloff = d, U.proximityThreshold = v, U.proximityFalloff = x, u !== void 0 && (U.worldDistanceThreshold = u), f !== void 0 && (U.worldDistanceFalloff = f), h !== void 0 && (U.worldProximityThreshold = h), m !== void 0 && (U.worldProximityFalloff = m), this.usingGBuffer ? this.defines.set("GBUFFER_NORMALS", "1") : o !== null && (this.ssaoMaterial.normalDepthBuffer = o, this.defines.set("NORMAL_DEPTH", "1")), this.depthAwareUpsampling = l, this.color = L, this.logConfiguration();
  }
  /**
   * Logs the current configuration for debugging.
   *
   * @private
   */
  logConfiguration() {
    console.log("🔧 SSAO Configuration:"), console.log(`  - Using G-Buffer: ${this.usingGBuffer ? "✅" : "❌"}`), console.log(`  - Depth Downsampling: ${this.depthDownsamplingPass?.enabled ? "✅" : "❌"}`), console.log(`  - Normal Buffer: ${this.ssaoMaterial.normalBuffer ? "✅" : "❌"}`), console.log(`  - Samples: ${this.ssaoMaterial.samples}`), console.log(`  - Radius: ${this.ssaoMaterial.radius}`), console.log(`  - Intensity: ${this.uniforms.get("intensity").value}`);
  }
  /**
   * Updates G-Buffer textures (can be called at runtime).
   *
   * @param {Object} gBufferTextures - New G-Buffer textures.
   */
  updateGBufferTextures(e) {
    if (!e || !e.gNormal) {
      console.warn("SSAO: Invalid G-Buffer textures provided");
      return;
    }
    const t = this.usingGBuffer;
    this.gBufferTextures = e, this.usingGBuffer = !0, this.ssaoMaterial.normalBuffer = e.gNormal, !t && this.depthDownsamplingPass && (this.depthDownsamplingPass.enabled = !1, console.log("✅ SSAO: 切换到G-Buffer模式，禁用深度下采样")), this.defines.set("GBUFFER_NORMALS", "1"), this.defines.delete("NORMAL_DEPTH"), this.setChanged(), console.log("✅ SSAO: G-Buffer纹理已更新");
  }
  /**
   * The SSAO material.
   *
   * @type {SSAOMaterial}
   */
  get ssaoMaterial() {
    return this.ssaoPass.fullscreenMaterial;
  }
  /**
   * Sets the main camera.
   *
   * @type {Camera}
   */
  set mainCamera(e) {
    this.camera = e, this.ssaoMaterial.copyCameraSettings(e);
  }
  /**
   * Sets the normal buffer.
   *
   * @type {Texture}
   */
  get normalBuffer() {
    return this.ssaoMaterial.normalBuffer;
  }
  set normalBuffer(e) {
    this.ssaoMaterial.normalBuffer = e, this.depthDownsamplingPass && (this.depthDownsamplingPass.fullscreenMaterial.normalBuffer = e);
  }
  /**
   * The intensity.
   *
   * @type {Number}
   */
  get intensity() {
    return this.uniforms.get("intensity").value;
  }
  set intensity(e) {
    this.uniforms.get("intensity").value = e;
  }
  /**
   * The color of the ambient occlusion.
   *
   * @type {Color}
   */
  get color() {
    return this.uniforms.get("color").value;
  }
  set color(e) {
    const t = this.uniforms, r = this.defines;
    e !== null ? r.has("COLORIZE") ? t.get("color").value.set(e) : (r.set("COLORIZE", "1"), t.get("color").value = new y(e), this.setChanged()) : r.has("COLORIZE") && (r.delete("COLORIZE"), t.get("color").value = null, this.setChanged());
  }
  /**
   * Updates this effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   */
  update(e, t, r) {
    const n = this.renderTarget;
    !this.usingGBuffer && this.depthDownsamplingPass?.enabled && this.depthDownsamplingPass.render(e), this.ssaoPass.render(e, null, n);
  }
  /**
   * Sets the depth texture.
   *
   * @param {Texture} depthTexture - A depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing.
   */
  setDepthTexture(e, t = $) {
    this.usingGBuffer && this.gBufferTextures.gDepth ? (this.ssaoMaterial.depthBuffer = this.gBufferTextures.gDepth, this.ssaoMaterial.depthPacking = $, console.log("✅ SSAO: 使用G-Buffer深度数据")) : (this.depthDownsamplingPass && this.depthDownsamplingPass.setDepthTexture(e, t), this.ssaoMaterial.depthBuffer = e, this.ssaoMaterial.depthPacking = t);
  }
  /**
   * Sets the size.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.resolution;
    r.setBaseSize(e, t);
    const n = r.width, i = r.height;
    this.ssaoMaterial.copyCameraSettings(this.camera), this.ssaoMaterial.setSize(n, i), this.renderTarget.setSize(n, i), this.depthDownsamplingPass && (this.depthDownsamplingPass.resolution.scale = r.scale, this.depthDownsamplingPass.setSize(e, t));
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    try {
      if (this.usingGBuffer) {
        console.log("✅ SSAO: 使用G-Buffer数据，跳过传统初始化");
        return;
      }
      let n = this.uniforms.get("normalDepthBuffer").value;
      n === null && this.depthDownsamplingPass && (this.depthDownsamplingPass.initialize(e, t, r), n = this.depthDownsamplingPass.texture, this.uniforms.get("normalDepthBuffer").value = n, this.ssaoMaterial.normalDepthBuffer = n, this.defines.set("NORMAL_DEPTH", "1"));
    } catch (n) {
      this.depthDownsamplingPass && (this.depthDownsamplingPass.enabled = !1), console.warn("SSAO initialization failed:", n);
    }
  }
  /**
   * Returns performance metrics.
   *
   * @returns {Object} Performance information.
   */
  getPerformanceInfo() {
    return {
      usingGBuffer: this.usingGBuffer,
      depthDownsamplingEnabled: this.depthDownsamplingPass?.enabled || !1,
      samples: this.ssaoMaterial.samples,
      radius: this.ssaoMaterial.radius,
      resolution: `${this.resolution.width}x${this.resolution.height}`,
      resolutionScale: this.resolution.scale
    };
  }
}
const Zo = `#ifdef TEXTURE_PRECISION_HIGH\r
\r
	uniform mediump sampler2D map;\r
\r
#else\r
\r
	uniform lowp sampler2D map;\r
\r
#endif\r
\r
varying vec2 vUv2;\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	#ifdef UV_TRANSFORM\r
\r
		vec4 texel = texture2D(map, vUv2);\r
\r
	#else\r
\r
		vec4 texel = texture2D(map, uv);\r
\r
	#endif\r
\r
	outputColor = TEXEL;\r
\r
}\r
`, Ko = `#ifdef ASPECT_CORRECTION\r
\r
	uniform float scale;\r
\r
#else\r
\r
	uniform mat3 uvTransform;\r
\r
#endif\r
\r
varying vec2 vUv2;\r
\r
void mainSupport(const in vec2 uv) {\r
\r
	#ifdef ASPECT_CORRECTION\r
\r
		vUv2 = uv * vec2(aspect, 1.0) * scale;\r
\r
	#else\r
\r
		vUv2 = (uvTransform * vec3(uv, 1.0)).xy;\r
\r
	#endif\r
\r
}\r
`;
class qh extends I {
  /**
   * Constructs a new texture effect.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction] - The blend function of this effect.
   * @param {Texture} [options.texture] - A texture.
   * @param {Boolean} [options.aspectCorrection=false] - Deprecated. Adjust the texture's offset, repeat and center instead.
   */
  constructor({ blendFunction: e, texture: t = null, aspectCorrection: r = !1 } = {}) {
    super("TextureEffect", Zo, {
      blendFunction: e,
      defines: /* @__PURE__ */ new Map([
        ["TEXEL", "texel"]
      ]),
      uniforms: /* @__PURE__ */ new Map([
        ["map", new a(null)],
        ["scale", new a(1)],
        ["uvTransform", new a(null)]
      ])
    }), this.texture = t, this.aspectCorrection = r;
  }
  /**
   * The texture.
   *
   * @type {Texture}
   */
  get texture() {
    return this.uniforms.get("map").value;
  }
  set texture(e) {
    const t = this.texture, r = this.uniforms, n = this.defines;
    t !== e && (r.get("map").value = e, r.get("uvTransform").value = e.matrix, n.delete("TEXTURE_PRECISION_HIGH"), e !== null && (e.matrixAutoUpdate ? (n.set("UV_TRANSFORM", "1"), this.setVertexShader(Ko)) : (n.delete("UV_TRANSFORM"), this.setVertexShader(null)), e.type !== se && n.set("TEXTURE_PRECISION_HIGH", "1"), (t === null || t.type !== e.type || t.encoding !== e.encoding) && this.setChanged()));
  }
  /**
   * Returns the texture.
   *
   * @deprecated Use texture instead.
   * @return {Texture} The texture.
   */
  getTexture() {
    return this.texture;
  }
  /**
   * Sets the texture.
   *
   * @deprecated Use texture instead.
   * @param {Texture} value - The texture.
   */
  setTexture(e) {
    this.texture = e;
  }
  /**
   * Indicates whether aspect correction is enabled.
   *
   * @type {Number}
   * @deprecated Adjust the texture's offset, repeat, rotation and center instead.
   */
  get aspectCorrection() {
    return this.defines.has("ASPECT_CORRECTION");
  }
  set aspectCorrection(e) {
    this.aspectCorrection !== e && (e ? this.defines.set("ASPECT_CORRECTION", "1") : this.defines.delete("ASPECT_CORRECTION"), this.setChanged());
  }
  /**
   * Indicates whether the texture UV coordinates will be transformed using the transformation matrix of the texture.
   *
   * @type {Boolean}
   * @deprecated Use texture.matrixAutoUpdate instead.
   */
  get uvTransform() {
    const e = this.texture;
    return e !== null && e.matrixAutoUpdate;
  }
  set uvTransform(e) {
    const t = this.texture;
    t !== null && (t.matrixAutoUpdate = e);
  }
  /**
   * Sets the swizzles that will be applied to the components of a texel before it is written to the output color.
   *
   * @param {ColorChannel} r - The swizzle for the `r` component.
   * @param {ColorChannel} [g=r] - The swizzle for the `g` component.
   * @param {ColorChannel} [b=r] - The swizzle for the `b` component.
   * @param {ColorChannel} [a=r] - The swizzle for the `a` component.
   */
  setTextureSwizzleRGBA(e, t = e, r = e, n = e) {
    const i = "rgba";
    let s = "";
    (e !== mt.RED || t !== mt.GREEN || r !== mt.BLUE || n !== mt.ALPHA) && (s = [".", i[e], i[t], i[r], i[n]].join("")), this.defines.set("TEXEL", "texel" + s), this.setChanged();
  }
  /**
   * Updates this effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   */
  update(e, t, r) {
    this.texture.matrixAutoUpdate && this.texture.updateMatrix();
  }
}
const Qo = `#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D inputBuffer;\r
\r
#else\r
\r
	uniform lowp sampler2D inputBuffer;\r
\r
#endif\r
\r
uniform vec4 maskParams;\r
\r
varying vec2 vUv;\r
varying vec2 vUv2;\r
varying vec2 vOffset;\r
\r
float linearGradientMask(const in float x) {\r
\r
	return smoothstep(maskParams.x, maskParams.y, x) -\r
		smoothstep(maskParams.w, maskParams.z, x);\r
\r
}\r
\r
void main() {\r
\r
	vec2 dUv = vOffset * (1.0 - linearGradientMask(vUv2.y));\r
	vec4 sum = texture2D(inputBuffer, vec2(vUv.x - dUv.x, vUv.y + dUv.y)); // Top left\r
	sum += texture2D(inputBuffer, vec2(vUv.x + dUv.x, vUv.y + dUv.y)); // Top right\r
	sum += texture2D(inputBuffer, vec2(vUv.x + dUv.x, vUv.y - dUv.y)); // Bottom right\r
	sum += texture2D(inputBuffer, vec2(vUv.x - dUv.x, vUv.y - dUv.y)); // Bottom left\r
	gl_FragColor = sum * 0.25; // Compute the average\r
\r
	#include <colorspace_fragment>\r
\r
}\r
`, qo = `uniform vec4 texelSize; // XY = texel size, ZW = half texel size\r
uniform float kernel;\r
uniform float scale;\r
uniform float aspect;\r
uniform vec2 rotation;\r
\r
varying vec2 vUv;\r
varying vec2 vUv2;\r
varying vec2 vOffset;\r
\r
void main() {\r
\r
	vec2 uv = position.xy * 0.5 + 0.5;\r
\r
	vUv = uv;\r
	vUv2 = (uv - 0.5) * 2.0 * vec2(aspect, 1.0);\r
	vUv2 = vec2(dot(rotation, vUv2), dot(rotation, vec2(vUv2.y, -vUv2.x)));\r
	vOffset = (texelSize.xy * vec2(kernel) + texelSize.zw) * scale;\r
\r
	gl_Position = vec4(position.xy, 1.0, 1.0);\r
\r
}\r
`;
class Jo extends Di {
  /**
   * Constructs a new tilt shift blur material.
   *
   * @param {Object} [options] - The options.
   * @param {Number} [options.offset=0.0] - The relative offset of the focus area.
   * @param {Number} [options.rotation=0.0] - The rotation of the focus area in radians.
   * @param {Number} [options.focusArea=0.4] - The relative size of the focus area.
   * @param {Number} [options.feather=0.3] - The softness of the focus area edges.
   */
  constructor({
    kernelSize: e = Le.MEDIUM,
    offset: t = 0,
    rotation: r = 0,
    focusArea: n = 0.4,
    feather: i = 0.3
  } = {}) {
    super(), this.fragmentShader = Qo, this.vertexShader = qo, this.kernelSize = e, this.uniforms.aspect = new a(1), this.uniforms.rotation = new a(new g()), this.uniforms.maskParams = new a(new ze()), this._offset = t, this._focusArea = n, this._feather = i, this.rotation = r, this.updateParams();
  }
  /**
   * The relative offset of the focus area.
   *
   * @private
   */
  updateParams() {
    const e = this.uniforms.maskParams.value, t = Math.max(this.focusArea, 0), r = Math.max(t - this.feather, 0);
    e.set(
      this.offset - t,
      this.offset - r,
      this.offset + t,
      this.offset + r
    );
  }
  /**
   * The rotation of the focus area in radians.
   *
   * @type {Number}
   */
  get rotation() {
    return Math.acos(this.uniforms.rotation.value.x);
  }
  set rotation(e) {
    this.uniforms.rotation.value.set(Math.cos(e), Math.sin(e));
  }
  /**
   * The relative offset of the focus area.
   *
   * @type {Number}
   */
  get offset() {
    return this._offset;
  }
  set offset(e) {
    this._offset = e, this.updateParams();
  }
  /**
   * The relative size of the focus area.
   *
   * @type {Number}
   */
  get focusArea() {
    return this._focusArea;
  }
  set focusArea(e) {
    this._focusArea = e, this.updateParams();
  }
  /**
   * The softness of the focus area edges.
   *
   * @type {Number}
   */
  get feather() {
    return this._feather;
  }
  set feather(e) {
    this._feather = e, this.updateParams();
  }
  /**
   * Sets the size of this object.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    super.setSize(e, t), this.uniforms.aspect.value = e / t;
  }
}
class _o extends at {
  /**
   * Constructs a new Kawase blur pass.
   *
   * @param {Object} [options] - The options.
   * @param {Number} [options.offset=0.0] - The relative offset of the focus area.
   * @param {Number} [options.rotation=0.0] - The rotation of the focus area in radians.
   * @param {Number} [options.focusArea=0.4] - The relative size of the focus area.
   * @param {Number} [options.feather=0.3] - The softness of the focus area edges.
   * @param {KernelSize} [options.kernelSize=KernelSize.MEDIUM] - The blur kernel size.
   * @param {Number} [options.resolutionScale=0.5] - The resolution scale.
   * @param {Number} [options.resolutionX=Resolution.AUTO_SIZE] - The horizontal resolution.
   * @param {Number} [options.resolutionY=Resolution.AUTO_SIZE] - The vertical resolution.
   */
  constructor({
    offset: e = 0,
    rotation: t = 0,
    focusArea: r = 0.4,
    feather: n = 0.3,
    kernelSize: i = Le.MEDIUM,
    resolutionScale: s = 0.5,
    resolutionX: o = W.AUTO_SIZE,
    resolutionY: l = W.AUTO_SIZE
  } = {}) {
    super({ kernelSize: i, resolutionScale: s, resolutionX: o, resolutionY: l }), this.blurMaterial = new Jo({ kernelSize: i, offset: e, rotation: t, focusArea: r, feather: n });
  }
}
const $o = `#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D map;\r
\r
#else\r
\r
	uniform lowp sampler2D map;\r
\r
#endif\r
\r
uniform vec2 maskParams;\r
varying vec2 vUv2;\r
\r
float linearGradientMask(const in float x) {\r
\r
	return step(maskParams.x, x) - step(maskParams.y, x);\r
\r
}\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	float mask = linearGradientMask(vUv2.y);\r
	vec4 texel = texture2D(map, uv);\r
	outputColor = mix(texel, inputColor, mask);\r
\r
}\r
`, el = `uniform vec2 rotation;\r
varying vec2 vUv2;\r
\r
void mainSupport(const in vec2 uv) {\r
\r
	vUv2 = (uv - 0.5) * 2.0 * vec2(aspect, 1.0);\r
	vUv2 = vec2(dot(rotation, vUv2), dot(rotation, vec2(vUv2.y, -vUv2.x)));\r
\r
}\r
`;
class Jh extends I {
  /**
   * Constructs a new tilt shift Effect
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction] - The blend function of this effect.
   * @param {Number} [options.offset=0.0] - The relative offset of the focus area.
   * @param {Number} [options.rotation=0.0] - The rotation of the focus area in radians.
   * @param {Number} [options.focusArea=0.4] - The relative size of the focus area.
   * @param {Number} [options.feather=0.3] - The softness of the focus area edges.
   * @param {Number} [options.bias=0.06] - Deprecated.
   * @param {KernelSize} [options.kernelSize=KernelSize.MEDIUM] - The blur kernel size.
   * @param {Number} [options.resolutionScale=0.5] - The resolution scale.
   * @param {Number} [options.resolutionX=Resolution.AUTO_SIZE] - The horizontal resolution.
   * @param {Number} [options.resolutionY=Resolution.AUTO_SIZE] - The vertical resolution.
   */
  constructor({
    blendFunction: e,
    offset: t = 0,
    rotation: r = 0,
    focusArea: n = 0.4,
    feather: i = 0.3,
    kernelSize: s = Le.MEDIUM,
    resolutionScale: o = 0.5,
    resolutionX: l = W.AUTO_SIZE,
    resolutionY: u = W.AUTO_SIZE
  } = {}) {
    super("TiltShiftEffect", $o, {
      vertexShader: el,
      blendFunction: e,
      uniforms: /* @__PURE__ */ new Map([
        ["rotation", new a(new g())],
        ["maskParams", new a(new g())],
        ["map", new a(null)]
      ])
    }), this._offset = t, this._focusArea = n, this._feather = i, this.renderTarget = new A(1, 1, { depthBuffer: !1 }), this.renderTarget.texture.name = "TiltShift.Target", this.uniforms.get("map").value = this.renderTarget.texture, this.blurPass = new _o({
      kernelSize: s,
      resolutionScale: o,
      resolutionX: l,
      resolutionY: u,
      offset: t,
      rotation: r,
      focusArea: n,
      feather: i
    });
    const f = this.resolution = new W(this, l, u, o);
    f.addEventListener("change", (h) => this.setSize(f.baseWidth, f.baseHeight)), this.rotation = r, this.updateParams();
  }
  /**
   * Updates the mask params.
   *
   * @private
   */
  updateParams() {
    const e = this.uniforms.get("maskParams").value, t = Math.max(this.focusArea - this.feather, 0);
    e.set(this.offset - t, this.offset + t);
  }
  /**
   * The rotation of the focus area in radians.
   *
   * @type {Number}
   */
  get rotation() {
    return Math.acos(this.uniforms.get("rotation").value.x);
  }
  set rotation(e) {
    this.uniforms.get("rotation").value.set(Math.cos(e), Math.sin(e)), this.blurPass.blurMaterial.rotation = e;
  }
  /**
   * The relative offset of the focus area.
   *
   * @type {Number}
   */
  get offset() {
    return this._offset;
  }
  set offset(e) {
    this._offset = e, this.blurPass.blurMaterial.offset = e, this.updateParams();
  }
  /**
   * The relative size of the focus area.
   *
   * @type {Number}
   */
  get focusArea() {
    return this._focusArea;
  }
  set focusArea(e) {
    this._focusArea = e, this.blurPass.blurMaterial.focusArea = e, this.updateParams();
  }
  /**
   * The softness of the focus area edges.
   *
   * @type {Number}
   */
  get feather() {
    return this._feather;
  }
  set feather(e) {
    this._feather = e, this.blurPass.blurMaterial.feather = e, this.updateParams();
  }
  /**
   * A blend bias.
   *
   * @type {Number}
   * @deprecated
   */
  get bias() {
    return 0;
  }
  set bias(e) {
  }
  /**
   * Updates this effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   */
  update(e, t, r) {
    this.blurPass.render(e, t, this.renderTarget);
  }
  /**
   * Updates the size of internal render targets.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.resolution;
    r.setBaseSize(e, t), this.renderTarget.setSize(r.width, r.height), this.blurPass.resolution.copy(r);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    this.blurPass.initialize(e, t, r), r !== void 0 && (this.renderTarget.texture.type = r, e !== null && e.outputColorSpace === V && (this.renderTarget.texture.colorSpace = V));
  }
}
const tl = `#include <packing>\r
\r
#define packFloatToRGBA(v) packDepthToRGBA(v)\r
#define unpackRGBAToFloat(v) unpackRGBAToDepth(v)\r
\r
uniform lowp sampler2D luminanceBuffer0;\r
uniform lowp sampler2D luminanceBuffer1;\r
\r
uniform float minLuminance;\r
uniform float deltaTime;\r
uniform float tau;\r
\r
varying vec2 vUv;\r
\r
void main() {\r
\r
	// This 1x1 buffer contains the previous luminance.\r
	float l0 = unpackRGBAToFloat(texture2D(luminanceBuffer0, vUv));\r
\r
	// Get the current average scene luminance.\r
	#if __VERSION__ < 300\r
\r
		float l1 = texture2DLodEXT(luminanceBuffer1, vUv, MIP_LEVEL_1X1).r;\r
\r
	#else\r
\r
		float l1 = textureLod(luminanceBuffer1, vUv, MIP_LEVEL_1X1).r;\r
\r
	#endif\r
\r
	l0 = max(minLuminance, l0);\r
	l1 = max(minLuminance, l1);\r
\r
	// Adapt the luminance using Pattanaik's technique.\r
	float adaptedLum = l0 + (l1 - l0) * (1.0 - exp(-deltaTime * tau));\r
\r
	gl_FragColor = (adaptedLum == 1.0) ? vec4(1.0) : packFloatToRGBA(adaptedLum);\r
\r
}\r
`;
class rl extends N {
  /**
   * Constructs a new adaptive luminance material.
   */
  constructor() {
    super({
      name: "AdaptiveLuminanceMaterial",
      defines: {
        MIP_LEVEL_1X1: "0.0"
      },
      uniforms: {
        luminanceBuffer0: new a(null),
        luminanceBuffer1: new a(null),
        minLuminance: new a(0.01),
        deltaTime: new a(0),
        tau: new a(1)
      },
      extensions: {
        shaderTextureLOD: !0
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: tl,
      vertexShader: Be
    });
  }
  /**
   * The primary luminance buffer that contains the downsampled average luminance.
   *
   * @type {Texture}
   */
  set luminanceBuffer0(e) {
    this.uniforms.luminanceBuffer0.value = e;
  }
  /**
   * Sets the primary luminance buffer that contains the downsampled average luminance.
   *
   * @deprecated Use luminanceBuffer0 instead.
   * @param {Texture} value - The buffer.
   */
  setLuminanceBuffer0(e) {
    this.uniforms.luminanceBuffer0.value = e;
  }
  /**
   * The secondary luminance buffer.
   *
   * @type {Texture}
   */
  set luminanceBuffer1(e) {
    this.uniforms.luminanceBuffer1.value = e;
  }
  /**
   * Sets the secondary luminance buffer.
   *
   * @deprecated Use luminanceBuffer1 instead.
   * @param {Texture} value - The buffer.
   */
  setLuminanceBuffer1(e) {
    this.uniforms.luminanceBuffer1.value = e;
  }
  /**
   * The 1x1 mipmap level.
   *
   * This level is used to identify the smallest mipmap of the primary luminance buffer.
   *
   * @type {Number}
   */
  set mipLevel1x1(e) {
    this.defines.MIP_LEVEL_1X1 = e.toFixed(1), this.needsUpdate = !0;
  }
  /**
   * Sets the 1x1 mipmap level.
   *
   * @deprecated Use mipLevel1x1 instead.
   * @param {Number} value - The level.
   */
  setMipLevel1x1(e) {
    this.mipLevel1x1 = e;
  }
  /**
   * The delta time.
   *
   * @type {Number}
   */
  set deltaTime(e) {
    this.uniforms.deltaTime.value = e;
  }
  /**
   * Sets the delta time.
   *
   * @deprecated Use deltaTime instead.
   * @param {Number} value - The delta time.
   */
  setDeltaTime(e) {
    this.uniforms.deltaTime.value = e;
  }
  /**
   * The lowest possible luminance value.
   *
   * @type {Number}
   */
  get minLuminance() {
    return this.uniforms.minLuminance.value;
  }
  set minLuminance(e) {
    this.uniforms.minLuminance.value = e;
  }
  /**
   * Returns the lowest possible luminance value.
   *
   * @deprecated Use minLuminance instead.
   * @return {Number} The minimum luminance.
   */
  getMinLuminance() {
    return this.uniforms.minLuminance.value;
  }
  /**
   * Sets the minimum luminance.
   *
   * @deprecated Use minLuminance instead.
   * @param {Number} value - The minimum luminance.
   */
  setMinLuminance(e) {
    this.uniforms.minLuminance.value = e;
  }
  /**
   * The luminance adaptation rate.
   *
   * @type {Number}
   */
  get adaptationRate() {
    return this.uniforms.tau.value;
  }
  set adaptationRate(e) {
    this.uniforms.tau.value = e;
  }
  /**
   * Returns the luminance adaptation rate.
   *
   * @deprecated Use adaptationRate instead.
   * @return {Number} The adaptation rate.
   */
  getAdaptationRate() {
    return this.uniforms.tau.value;
  }
  /**
   * Sets the luminance adaptation rate.
   *
   * @deprecated Use adaptationRate instead.
   * @param {Number} value - The adaptation rate.
   */
  setAdaptationRate(e) {
    this.uniforms.tau.value = e;
  }
}
class nl extends X {
  /**
   * Constructs a new adaptive luminance pass.
   *
   * @param {Texture} luminanceBuffer - A buffer that contains the current scene luminance.
   * @param {Object} [options] - The options.
   * @param {Number} [options.minLuminance=0.01] - The minimum luminance.
   * @param {Number} [options.adaptationRate=1.0] - The luminance adaptation rate.
   */
  constructor(e, { minLuminance: t = 0.01, adaptationRate: r = 1 } = {}) {
    super("AdaptiveLuminancePass"), this.fullscreenMaterial = new rl(), this.needsSwap = !1, this.renderTargetPrevious = new A(1, 1, {
      minFilter: b,
      magFilter: b,
      depthBuffer: !1
    }), this.renderTargetPrevious.texture.name = "Luminance.Previous";
    const n = this.fullscreenMaterial;
    n.luminanceBuffer0 = this.renderTargetPrevious.texture, n.luminanceBuffer1 = e, n.minLuminance = t, n.adaptationRate = r, this.renderTargetAdapted = this.renderTargetPrevious.clone(), this.renderTargetAdapted.texture.name = "Luminance.Adapted", this.copyPass = new Ti(this.renderTargetPrevious, !1);
  }
  /**
   * The adaptive luminance texture.
   *
   * @type {Texture}
   */
  get texture() {
    return this.renderTargetAdapted.texture;
  }
  /**
   * Returns the adaptive 1x1 luminance texture.
   *
   * @deprecated Use texture instead.
   * @return {Texture} The texture.
   */
  getTexture() {
    return this.renderTargetAdapted.texture;
  }
  /**
   * Sets the 1x1 mipmap level.
   *
   * This level is used to identify the smallest mipmap of the main luminance texture which contains the downsampled
   * average scene luminance.
   *
   * @type {Number}
   * @deprecated Use fullscreenMaterial.mipLevel1x1 instead.
   */
  set mipLevel1x1(e) {
    this.fullscreenMaterial.mipLevel1x1 = e;
  }
  /**
   * The luminance adaptation rate.
   *
   * @type {Number}
   * @deprecated Use fullscreenMaterial.adaptationRate instead.
   */
  get adaptationRate() {
    return this.fullscreenMaterial.adaptationRate;
  }
  /**
   * @type {Number}
   * @deprecated Use fullscreenMaterial.adaptationRate instead.
   */
  set adaptationRate(e) {
    this.fullscreenMaterial.adaptationRate = e;
  }
  /**
   * Renders the scene normals.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i) {
    this.fullscreenMaterial.deltaTime = n, e.setRenderTarget(this.renderToScreen ? null : this.renderTargetAdapted), e.render(this.scene, this.camera), this.copyPass.render(e, this.renderTargetAdapted);
  }
}
const il = `#include <tonemapping_pars_fragment>\r
\r
uniform float whitePoint;\r
\r
#if TONE_MAPPING_MODE == 2 || TONE_MAPPING_MODE == 3\r
\r
	// Reinhard 2\r
\r
	uniform float middleGrey;\r
\r
	#if TONE_MAPPING_MODE == 3\r
\r
		uniform lowp sampler2D luminanceBuffer;\r
\r
	#else\r
\r
		uniform float averageLuminance;\r
\r
	#endif\r
\r
	vec3 Reinhard2ToneMapping(vec3 color) {\r
\r
		color *= toneMappingExposure;\r
\r
		// Calculate the luminance of the current pixel.\r
		float l = luminance(color);\r
\r
		#if TONE_MAPPING_MODE == 3\r
\r
			// Get the average luminance from the adaptive 1x1 buffer.\r
			float lumAvg = unpackRGBAToFloat(texture2D(luminanceBuffer, vec2(0.5)));\r
\r
		#else\r
\r
			float lumAvg = averageLuminance;\r
\r
		#endif\r
\r
		float lumScaled = (l * middleGrey) / max(lumAvg, 1e-6);\r
		float lumCompressed = lumScaled * (1.0 + lumScaled / (whitePoint * whitePoint));\r
		lumCompressed /= (1.0 + lumScaled);\r
\r
		return clamp(lumCompressed * color, 0.0, 1.0);\r
\r
	}\r
\r
#elif TONE_MAPPING_MODE == 4\r
\r
	// Uncharted 2: http://filmicworlds.com/blog/filmic-tonemapping-operators\r
\r
	#define A 0.15\r
	#define B 0.50\r
	#define C 0.10\r
	#define D 0.20\r
	#define E 0.02\r
	#define F 0.30\r
\r
	vec3 Uncharted2Helper(const in vec3 x) {\r
\r
		return ((x * (A * x + C * B) + D * E) / (x * (A * x + B) + D * F)) - E / F;\r
\r
	}\r
\r
	vec3 Uncharted2ToneMapping(vec3 color) {\r
\r
		color *= toneMappingExposure;\r
		return clamp(Uncharted2Helper(color) / Uncharted2Helper(vec3(whitePoint)), 0.0, 1.0);\r
\r
	}\r
\r
#endif\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	#if TONE_MAPPING_MODE == 2 || TONE_MAPPING_MODE == 3\r
\r
		outputColor = vec4(Reinhard2ToneMapping(inputColor.rgb), inputColor.a);\r
\r
	#elif TONE_MAPPING_MODE == 4\r
\r
		outputColor = vec4(Uncharted2ToneMapping(inputColor.rgb), inputColor.a);\r
\r
	#else\r
\r
		outputColor = vec4(toneMapping(inputColor.rgb), inputColor.a);\r
\r
	#endif\r
\r
}\r
`;
class _h extends I {
  /**
   * Constructs a new tone mapping effect.
   *
   * The additional parameters only affect the Reinhard2 operator.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction=BlendFunction.SRC] - The blend function of this effect.
   * @param {Boolean} [options.adaptive=false] - Deprecated. Use mode instead.
   * @param {ToneMappingMode} [options.mode=ToneMappingMode.AGX] - The tone mapping mode.
   * @param {Number} [options.resolution=256] - The resolution of the luminance texture. Must be a power of two.
   * @param {Number} [options.maxLuminance=4.0] - Deprecated. Same as whitePoint.
   * @param {Number} [options.whitePoint=4.0] - The white point.
   * @param {Number} [options.middleGrey=0.6] - The middle grey factor.
   * @param {Number} [options.minLuminance=0.01] - The minimum luminance. Prevents very high exposure in dark scenes.
   * @param {Number} [options.averageLuminance=1.0] - The average luminance. Used for the non-adaptive Reinhard operator.
   * @param {Number} [options.adaptationRate=1.0] - The luminance adaptation rate.
   */
  constructor({
    blendFunction: e = E.SRC,
    adaptive: t = !1,
    mode: r = t ? ye.REINHARD2_ADAPTIVE : ye.AGX,
    resolution: n = 256,
    maxLuminance: i = 4,
    whitePoint: s = i,
    middleGrey: o = 0.6,
    minLuminance: l = 0.01,
    averageLuminance: u = 1,
    adaptationRate: f = 1
  } = {}) {
    super("ToneMappingEffect", il, {
      blendFunction: e,
      uniforms: /* @__PURE__ */ new Map([
        ["luminanceBuffer", new a(null)],
        ["maxLuminance", new a(i)],
        // Unused
        ["whitePoint", new a(s)],
        ["middleGrey", new a(o)],
        ["averageLuminance", new a(u)]
      ])
    }), this.renderTargetLuminance = new A(1, 1, {
      minFilter: gs,
      depthBuffer: !1
    }), this.renderTargetLuminance.texture.generateMipmaps = !0, this.renderTargetLuminance.texture.name = "Luminance", this.luminancePass = new Pi({
      renderTarget: this.renderTargetLuminance
    }), this.adaptiveLuminancePass = new nl(this.luminancePass.texture, {
      minLuminance: l,
      adaptationRate: f
    }), this.uniforms.get("luminanceBuffer").value = this.adaptiveLuminancePass.texture, this.resolution = n, this.mode = r;
  }
  /**
   * The tone mapping mode.
   *
   * @type {ToneMappingMode}
   */
  get mode() {
    return Number(this.defines.get("TONE_MAPPING_MODE"));
  }
  set mode(e) {
    if (this.mode === e)
      return;
    const r = Vt.replace(/\D+/g, "") >= 168 ? "CineonToneMapping(texel)" : "OptimizedCineonToneMapping(texel)";
    switch (this.defines.clear(), this.defines.set("TONE_MAPPING_MODE", e.toFixed(0)), e) {
      case ye.LINEAR:
        this.defines.set("toneMapping(texel)", "LinearToneMapping(texel)");
        break;
      case ye.REINHARD:
        this.defines.set("toneMapping(texel)", "ReinhardToneMapping(texel)");
        break;
      case ye.CINEON:
      case ye.OPTIMIZED_CINEON:
        this.defines.set("toneMapping(texel)", r);
        break;
      case ye.ACES_FILMIC:
        this.defines.set("toneMapping(texel)", "ACESFilmicToneMapping(texel)");
        break;
      case ye.AGX:
        this.defines.set("toneMapping(texel)", "AgXToneMapping(texel)");
        break;
      case ye.NEUTRAL:
        this.defines.set("toneMapping(texel)", "NeutralToneMapping(texel)");
        break;
      default:
        this.defines.set("toneMapping(texel)", "texel");
        break;
    }
    this.adaptiveLuminancePass.enabled = e === ye.REINHARD2_ADAPTIVE, this.setChanged();
  }
  /**
   * Returns the current tone mapping mode.
   *
   * @deprecated Use mode instead.
   * @return {ToneMappingMode} The tone mapping mode.
   */
  getMode() {
    return this.mode;
  }
  /**
   * Sets the tone mapping mode.
   *
   * @deprecated Use mode instead.
   * @param {ToneMappingMode} value - The tone mapping mode.
   */
  setMode(e) {
    this.mode = e;
  }
  /**
   * The white point. Default is `4.0`.
   *
   * Only applies to Reinhard2 (Modified & Adaptive).
   *
   * @type {Number}
   */
  get whitePoint() {
    return this.uniforms.get("whitePoint").value;
  }
  set whitePoint(e) {
    this.uniforms.get("whitePoint").value = e;
  }
  /**
   * The middle grey factor. Default is `0.6`.
   *
   * Only applies to Reinhard2 (Modified & Adaptive).
   *
   * @type {Number}
   */
  get middleGrey() {
    return this.uniforms.get("middleGrey").value;
  }
  set middleGrey(e) {
    this.uniforms.get("middleGrey").value = e;
  }
  /**
   * The average luminance.
   *
   * Only applies to Reinhard2 (Modified).
   *
   * @type {Number}
   */
  get averageLuminance() {
    return this.uniforms.get("averageLuminance").value;
  }
  set averageLuminance(e) {
    this.uniforms.get("averageLuminance").value = e;
  }
  /**
   * The adaptive luminance material.
   *
   * @type {AdaptiveLuminanceMaterial}
   */
  get adaptiveLuminanceMaterial() {
    return this.adaptiveLuminancePass.fullscreenMaterial;
  }
  /**
   * Returns the adaptive luminance material.
   *
   * @deprecated Use adaptiveLuminanceMaterial instead.
   * @return {AdaptiveLuminanceMaterial} The material.
   */
  getAdaptiveLuminanceMaterial() {
    return this.adaptiveLuminanceMaterial;
  }
  /**
   * The resolution of the luminance texture. Must be a power of two.
   *
   * @type {Number}
   */
  get resolution() {
    return this.luminancePass.resolution.width;
  }
  set resolution(e) {
    const t = Math.max(0, Math.ceil(Math.log2(e))), r = Math.pow(2, t);
    this.luminancePass.resolution.setPreferredSize(r, r), this.adaptiveLuminanceMaterial.mipLevel1x1 = t;
  }
  /**
   * Returns the resolution of the luminance texture.
   *
   * @deprecated Use resolution instead.
   * @return {Number} The resolution.
   */
  getResolution() {
    return this.resolution;
  }
  /**
   * Sets the resolution of the luminance texture. Must be a power of two.
   *
   * @deprecated Use resolution instead.
   * @param {Number} value - The resolution.
   */
  setResolution(e) {
    this.resolution = e;
  }
  /**
   * Indicates whether this pass uses adaptive luminance.
   *
   * @type {Boolean}
   * @deprecated Use mode instead.
   */
  get adaptive() {
    return this.mode === ye.REINHARD2_ADAPTIVE;
  }
  set adaptive(e) {
    this.mode = e ? ye.REINHARD2_ADAPTIVE : ye.REINHARD2;
  }
  /**
   * The luminance adaptation rate.
   *
   * @type {Number}
   * @deprecated Use adaptiveLuminanceMaterial.adaptationRate instead.
   */
  get adaptationRate() {
    return this.adaptiveLuminanceMaterial.adaptationRate;
  }
  set adaptationRate(e) {
    this.adaptiveLuminanceMaterial.adaptationRate = e;
  }
  /**
   * @type {Number}
   * @deprecated
   */
  get distinction() {
    return console.warn(this.name, "distinction was removed."), 1;
  }
  set distinction(e) {
    console.warn(this.name, "distinction was removed.");
  }
  /**
   * Updates this effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   */
  update(e, t, r) {
    this.adaptiveLuminancePass.enabled && (this.luminancePass.render(e, t), this.adaptiveLuminancePass.render(e, null, null, r));
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    this.adaptiveLuminancePass.initialize(e, t, r);
  }
}
const sl = `uniform float offset;\r
uniform float darkness;\r
uniform vec3 color;\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
\r
	const vec2 center = vec2(0.5);\r
	vec3 rgb = inputColor.rgb;\r
\r
	#if VIGNETTE_TECHNIQUE == 0\r
\r
		float d = distance(uv, center);\r
		float factor = smoothstep(0.8, offset * 0.799, d * (darkness + offset));\r
		rgb = mix(color, rgb, factor);\r
\r
	#else\r
\r
		vec2 coord = (uv - center) * vec2(offset);\r
		float factor = dot(coord, coord);\r
		rgb = mix(rgb, color, darkness * factor);\r
\r
	#endif\r
\r
	outputColor = vec4(rgb, inputColor.a);\r
\r
}\r
`;
class $h extends I {
  /**
   * Constructs a new Vignette effect.
   *
   * @param {Object} [options] - The options.
   * @param {BlendFunction} [options.blendFunction] - The blend function of this effect.
   * @param {VignetteTechnique} [options.technique=VignetteTechnique.DEFAULT] - The Vignette technique.
   * @param {Boolean} [options.eskil=false] - Deprecated. Use technique instead.
   * @param {Number} [options.offset=0.5] - The Vignette offset.
   * @param {Number} [options.darkness=0.5] - The Vignette darkness.
   * @param {Color|String|Number} [options.color=0x000000] - The Vignette color.
   */
  constructor({
    blendFunction: e,
    eskil: t = !1,
    technique: r = t ? Pt.ESKIL : Pt.DEFAULT,
    offset: n = 0.5,
    darkness: i = 0.5,
    color: s = 0
  } = {}) {
    super("VignetteEffect", sl, {
      blendFunction: e,
      defines: /* @__PURE__ */ new Map([
        ["VIGNETTE_TECHNIQUE", r.toFixed(0)]
      ]),
      uniforms: /* @__PURE__ */ new Map([
        ["offset", new a(n)],
        ["darkness", new a(i)],
        ["color", new a(new y(s))]
      ])
    });
  }
  /**
   * The Vignette technique.
   *
   * @type {VignetteTechnique}
   */
  get technique() {
    return Number(this.defines.get("VIGNETTE_TECHNIQUE"));
  }
  set technique(e) {
    this.technique !== e && (this.defines.set("VIGNETTE_TECHNIQUE", e.toFixed(0)), this.setChanged());
  }
  /**
   * Indicates whether Eskil's Vignette technique is enabled.
   *
   * @type {Boolean}
   * @deprecated Use technique instead.
   */
  get eskil() {
    return this.technique === Pt.ESKIL;
  }
  /**
   * Indicates whether Eskil's Vignette technique is enabled.
   *
   * @type {Boolean}
   * @deprecated Use technique instead.
   */
  set eskil(e) {
    this.technique = e ? Pt.ESKIL : Pt.DEFAULT;
  }
  /**
   * Returns the Vignette technique.
   *
   * @deprecated Use technique instead.
   * @return {VignetteTechnique} The technique.
   */
  getTechnique() {
    return this.technique;
  }
  /**
   * Sets the Vignette technique.
   *
   * @deprecated Use technique instead.
   * @param {VignetteTechnique} value - The technique.
   */
  setTechnique(e) {
    this.technique = e;
  }
  /**
   * The Vignette offset.
   *
   * @type {Number}
   */
  get offset() {
    return this.uniforms.get("offset").value;
  }
  set offset(e) {
    this.uniforms.get("offset").value = e;
  }
  /**
   * Returns the Vignette offset.
   *
   * @deprecated Use offset instead.
   * @return {Number} The offset.
   */
  getOffset() {
    return this.offset;
  }
  /**
   * Sets the Vignette offset.
   *
   * @deprecated Use offset instead.
   * @param {Number} value - The offset.
   */
  setOffset(e) {
    this.offset = e;
  }
  /**
   * The Vignette darkness.
   *
   * @type {Number}
   */
  get darkness() {
    return this.uniforms.get("darkness").value;
  }
  set darkness(e) {
    this.uniforms.get("darkness").value = e;
  }
  /**
   * Returns the Vignette darkness.
   *
   * @deprecated Use darkness instead.
   * @return {Number} The darkness.
   */
  getDarkness() {
    return this.darkness;
  }
  /**
   * Sets the Vignette darkness.
   *
   * @deprecated Use darkness instead.
   * @param {Number} value - The darkness.
   */
  setDarkness(e) {
    this.darkness = e;
  }
  /**
   * The Vignette color.
   *
   * @type {Color}
   */
  get color() {
    return this.uniforms.get("color").value;
  }
  set color(e) {
    this.uniforms.get("color").value.set(e);
  }
  /**
   * Returns the Vignette color.
   *
   * @return {Color} The color.
   */
  getColor() {
    return this.color;
  }
  /**
   * Sets the Vignette color.
   *
   * @param {Color|String|Number} value - The color.
   */
  setColor(e) {
    this.color.set(e);
  }
}
const We = `varying vec2 vUv;\r
\r
void main() {\r
    vUv = position.xy * 0.5 + 0.5;\r
    gl_Position = vec4(position.xy, 1.0, 1.0);\r
}`;
let al = class extends X {
  constructor() {
    super("CubeToEquirectEnvPass"), this.renderTarget = new A(1, 1, { depthBuffer: !1, type: Y }), this.fullscreenMaterial = new N({
      fragmentShader: (
        /* glsl */
        `
            varying vec2 vUv;
			uniform samplerCube cubeMap;

			#define M_PI 3.1415926535897932384626433832795
			
			// source: https://github.com/spite/CubemapToEquirectangular/blob/master/src/CubemapToEquirectangular.js
            void main() {
				float longitude = vUv.x * 2. * M_PI - M_PI + M_PI / 2.;
				float latitude = vUv.y * M_PI;

				vec3 dir = vec3(
					- sin( longitude ) * sin( latitude ),
					cos( latitude ),
					- cos( longitude ) * sin( latitude )
				);

				dir.y = -dir.y;

				gl_FragColor = textureCube( cubeMap, dir );
            }
            `
      ),
      vertexShader: We,
      uniforms: {
        cubeMap: { value: null }
      },
      blending: B,
      depthWrite: !1,
      depthTest: !1,
      toneMapped: !1
    });
  }
  dispose() {
    this.renderTarget.dispose();
  }
  generateEquirectEnvMap(e, t, r = null, n = null, i = 4096) {
    if (r === null && n === null) {
      const u = t.source.data[0].width, f = 2 ** Math.ceil(Math.log2(2 * u * 3 ** 0.5)), h = 2 ** Math.ceil(Math.log2(u * 3 ** 0.5));
      r = f, n = h;
    }
    r > i && (r = i, n = i / 2), this.renderTarget.setSize(r, n), this.fullscreenMaterial.uniforms.cubeMap.value = t;
    const { renderTarget: s } = this;
    e.setRenderTarget(s), e.render(this.scene, this.camera);
    const o = new Float32Array(r * n * 4);
    e.readRenderTargetPixels(s, 0, 0, r, n, o);
    const l = new le(o, r, n, ne, Y);
    return l.wrapS = tt, l.wrapT = tt, l.minFilter = nt, l.magFilter = nt, l.needsUpdate = !0, l.mapping = Mi, l;
  }
};
const ol = (c) => c.material.fragmentShader?.includes(
  "float intersection2 = diskIntersectWithBackFaceCulling( camPos, p, h, vec3( 0.0, 1.0, 0.0 ), radius );"
), _r = (c, e = c.material) => e.visible && e.depthWrite && e.depthTest && (!e.transparent || e.opacity > 0) && !ol(c), Oi = (c, e, t) => c.position.distanceToSquared(e) > 1e-6 || c.quaternion.angleTo(t) > 1e-3, ki = (c) => {
  const e = [c], t = [];
  for (; e.length !== 0; ) {
    const r = e.shift();
    r.material && t.push(r);
    for (const n of r.children)
      n.visible && e.push(n);
  }
  return t;
}, ot = `uniform highp sampler2D gBufferTexture;\r
\r
struct Material {\r
  highp vec4 diffuse;\r
  highp vec3 normal;\r
  highp float roughness;\r
  highp float metalness;\r
  highp vec3 emissive;\r
};\r
\r
#define ONE_SAFE 0.999999\r
#define NON_ZERO_OFFSET 0.0001\r
\r
const highp float c_precision = 256.0;\r
const highp float c_precisionp1 = c_precision + 1.0;\r
\r
highp float color2float(in highp vec3 color) {\r
  color = min(color + NON_ZERO_OFFSET, vec3(ONE_SAFE));\r
\r
  return floor(color.r * c_precision + 0.5) + floor(color.b * c_precision + 0.5) * c_precisionp1 +\r
         floor(color.g * c_precision + 0.5) * c_precisionp1 * c_precisionp1;\r
}\r
\r
highp vec3 float2color(in highp float value) {\r
  highp vec3 color;\r
  color.r = mod(value, c_precisionp1) / c_precision;\r
  color.b = mod(floor(value / c_precisionp1), c_precisionp1) / c_precision;\r
  color.g = floor(value / (c_precisionp1 * c_precisionp1)) / c_precision;\r
\r
  color -= NON_ZERO_OFFSET;\r
  color = max(color, vec3(0.0));\r
\r
  return color;\r
}\r
\r
highp vec2 OctWrap(highp vec2 v) {\r
  highp vec2 w = 1.0 - abs(v.yx);\r
  if (v.x < 0.0)\r
    w.x = -w.x;\r
  if (v.y < 0.0)\r
    w.y = -w.y;\r
  return w;\r
}\r
\r
highp vec2 encodeOctWrap(highp vec3 n) {\r
  n /= (abs(n.x) + abs(n.y) + abs(n.z));\r
  n.xy = n.z > 0.0 ? n.xy : OctWrap(n.xy);\r
  n.xy = n.xy * 0.5 + 0.5;\r
  return n.xy;\r
}\r
\r
highp vec3 decodeOctWrap(highp vec2 f) {\r
  f = f * 2.0 - 1.0;\r
  highp vec3 n = vec3(f.x, f.y, 1.0 - abs(f.x) - abs(f.y));\r
  highp float t = max(-n.z, 0.0);\r
  n.x += n.x >= 0.0 ? -t : t;\r
  n.y += n.y >= 0.0 ? -t : t;\r
  return normalize(n);\r
}\r
\r
highp float packNormal(highp vec3 normal) { return uintBitsToFloat(packHalf2x16(encodeOctWrap(normal))); }\r
\r
highp vec3 unpackNormal(highp float packedNormal) { return decodeOctWrap(unpackHalf2x16(floatBitsToUint(packedNormal))); }\r
\r
highp vec4 packTwoVec4(highp vec4 v1, highp vec4 v2) {\r
  // note: we get artifacts on some back-ends such as v2 = vec3(1., 0., 0.) being decoded as black (only applies for v2 and red channel)\r
  highp vec4 encoded = vec4(0.0);\r
\r
  v1 += NON_ZERO_OFFSET;\r
  v2 += NON_ZERO_OFFSET;\r
\r
  highp uint v1r = packHalf2x16(v1.rg);\r
  highp uint v1g = packHalf2x16(v1.ba);\r
  highp uint v2r = packHalf2x16(v2.rg);\r
  highp uint v2g = packHalf2x16(v2.ba);\r
\r
  encoded.r = uintBitsToFloat(v1r);\r
  encoded.g = uintBitsToFloat(v1g);\r
  encoded.b = uintBitsToFloat(v2r);\r
  encoded.a = uintBitsToFloat(v2g);\r
\r
  return encoded;\r
}\r
\r
void unpackTwoVec4(highp vec4 encoded, out highp vec4 v1, out highp vec4 v2) {\r
  highp uint r = floatBitsToUint(encoded.r);\r
  highp uint g = floatBitsToUint(encoded.g);\r
  highp uint b = floatBitsToUint(encoded.b);\r
  highp uint a = floatBitsToUint(encoded.a);\r
\r
  v1.rg = unpackHalf2x16(r);\r
  v1.ba = unpackHalf2x16(g);\r
  v2.rg = unpackHalf2x16(b);\r
  v2.ba = unpackHalf2x16(a);\r
\r
  v1 -= NON_ZERO_OFFSET;\r
  v2 -= NON_ZERO_OFFSET;\r
}\r
\r
vec4 unpackTwoVec4(highp vec4 encoded, const int index) {\r
  highp uint r = floatBitsToUint(index == 0 ? encoded.r : encoded.b);\r
  highp uint g = floatBitsToUint(index == 0 ? encoded.g : encoded.a);\r
\r
  vec4 v;\r
\r
  v.rg = unpackHalf2x16(r);\r
  v.ba = unpackHalf2x16(g);\r
\r
  v -= NON_ZERO_OFFSET;\r
\r
  return v;\r
}\r
\r
// highp float packVec2(highp vec2 value) {\r
//   value = min(value + NON_ZERO_OFFSET, vec2(ONE_SAFE));\r
\r
//   return uintBitsToFloat(packUnorm2x16(value));\r
// }\r
\r
// highp vec2 unpackVec2(highp float packedValue) {\r
//   vec2 v = unpackUnorm2x16(floatBitsToUint(packedValue));\r
//   v = max(v - NON_ZERO_OFFSET, vec2(0.0));\r
\r
//   return v;\r
// }\r
\r
highp vec4 encodeRGBE8(highp vec3 rgb) {\r
  highp vec4 vEncoded;\r
  highp float maxComponent = max(max(rgb.r, rgb.g), rgb.b);\r
  highp float fExp = ceil(log2(maxComponent));\r
  vEncoded.rgb = rgb / exp2(fExp);\r
  vEncoded.a = (fExp + 128.0) / 255.0;\r
  return vEncoded;\r
}\r
\r
highp vec3 decodeRGBE8(highp vec4 rgbe) {\r
  highp vec3 vDecoded;\r
  highp float fExp = rgbe.a * 255.0 - 128.0;\r
  vDecoded = rgbe.rgb * exp2(fExp);\r
  return vDecoded;\r
}\r
\r
highp float vec4ToFloat(highp vec4 vec) {\r
  vec = min(vec + NON_ZERO_OFFSET, vec4(ONE_SAFE));\r
\r
  highp uvec4 v = uvec4(vec * 255.0);\r
  highp uint value = (v.a << 24u) | (v.b << 16u) | (v.g << 8u) | (v.r);\r
  return uintBitsToFloat(value);\r
}\r
\r
highp vec4 floatToVec4(highp float f) {\r
  highp uint value = floatBitsToUint(f);\r
\r
  highp vec4 v;\r
  v.r = float(value & 0xFFu) / 255.0;\r
  v.g = float((value >> 8u) & 0xFFu) / 255.0;\r
  v.b = float((value >> 16u) & 0xFFu) / 255.0;\r
  v.a = float((value >> 24u) & 0xFFu) / 255.0;\r
\r
  v -= NON_ZERO_OFFSET;\r
  v = max(v, vec4(0.0));\r
\r
  return v;\r
}\r
\r
highp vec4 packGBuffer(highp vec4 diffuse, highp vec3 normal, highp float roughness, highp float metalness, highp vec3 emissive) {\r
  highp vec4 gBuffer;\r
\r
  gBuffer.r = vec4ToFloat(diffuse);\r
  gBuffer.g = packNormal(normal);\r
\r
  // unfortunately packVec2 results in severe precision loss and artifacts for\r
  // the first on Metal backends thus we use color2float instead\r
  gBuffer.b = color2float(vec3(roughness, metalness, 0.));\r
  gBuffer.a = vec4ToFloat(encodeRGBE8(emissive));\r
\r
  return gBuffer;\r
}\r
\r
// loading a material from a packed g-buffer\r
Material getMaterial(highp sampler2D gBufferTexture, highp vec2 uv) {\r
  highp vec4 gBuffer = textureLod(gBufferTexture, uv, 0.0);\r
\r
  highp vec4 diffuse = floatToVec4(gBuffer.r);\r
  highp vec3 normal = unpackNormal(gBuffer.g);\r
\r
  // using float2color instead of unpackVec2 as the latter results in severe\r
  // precision loss and artifacts on Metal backends\r
  highp vec3 roughnessMetalness = float2color(gBuffer.b);\r
  highp float roughness = roughnessMetalness.r;\r
  highp float metalness = roughnessMetalness.g;\r
\r
  highp vec3 emissive = decodeRGBE8(floatToVec4(gBuffer.a));\r
\r
  return Material(diffuse, normal, roughness, metalness, emissive);\r
}\r
\r
Material getMaterial(highp vec2 uv) { return getMaterial(gBufferTexture, uv); }\r
\r
highp vec3 getNormal(highp sampler2D gBufferTexture, highp vec2 uv) { return unpackNormal(textureLod(gBufferTexture, uv, 0.0).g); }\r
`, ll = `uniform sampler2D blueNoiseTexture;\r
uniform vec2 blueNoiseSize;\r
uniform int blueNoiseIndex;\r
\r
// internal RNG state\r
uvec4 s1;\r
ivec2 pixel;\r
\r
void rng_initialize(vec2 p, int index) {\r
  pixel = ivec2(p);\r
\r
  // blue noise seed\r
  s1 = uvec4(index, index * 15843, index * 31 + 4566, index * 2345 + 58585);\r
}\r
\r
// https://www.pcg-random.org/\r
void pcg4d(inout uvec4 v) {\r
  v = v * 1664525u + 1013904223u;\r
  v.x += v.y * v.w;\r
  v.y += v.z * v.x;\r
  v.z += v.x * v.y;\r
  v.w += v.y * v.z;\r
  v = v ^ (v >> 16u);\r
  v.x += v.y * v.w;\r
  v.y += v.z * v.x;\r
  v.z += v.x * v.y;\r
  v.w += v.y * v.z;\r
}\r
\r
// random blue noise sampling pos\r
ivec2 shift2(ivec2 size) {\r
  pcg4d(s1);\r
  return (pixel + ivec2(s1.xy % 0x0fffffffu)) % size;\r
}\r
\r
// needs a uniform called "resolution" with the size of the render target\r
vec4 blueNoise(vec2 uv, int index) {\r
  if (index == 0)\r
    return textureLod(blueNoiseTexture, uv * resolution / blueNoiseSize, 0.0);\r
\r
  rng_initialize(vUv * resolution, index);\r
  vec4 blueNoise = texelFetch(blueNoiseTexture, shift2(ivec2(blueNoiseSize)), 0);\r
\r
  return blueNoise;\r
}\r
\r
vec4 blueNoise() { return blueNoise(vUv, int(blueNoiseIndex)); }\r
vec4 blueNoise(vec2 uv) { return blueNoise(uv, int(blueNoiseIndex)); }`, Br = 128, En = 2147483647;
function cl() {
  const c = Br, e = new Uint8Array(c * c * 4);
  for (let r = 0; r < c * c * 4; r += 4) {
    const n = Math.floor(Math.random() * 255);
    e[r] = n, e[r + 1] = n, e[r + 2] = n, e[r + 3] = 255;
  }
  const t = new le(e, c, c, ne);
  return t.minFilter = b, t.magFilter = b, t.wrapS = G, t.wrapT = G, t.colorSpace = Fe, t.needsUpdate = !0, t;
}
let er = null;
const ul = new Promise((c) => {
  er = cl(), new Ie().load(
    wt,
    // 成功加载
    (e) => {
      e.minFilter = b, e.magFilter = b, e.wrapS = G, e.wrapT = G, e.colorSpace = Fe, er = e, c(e);
    },
    // 加载进度
    void 0,
    // 加载失败
    (e) => {
      console.warn("加载蓝噪声纹理失败，使用内联纹理:", e), c(er);
    }
  );
});
let Vi = !1, Lr = [];
ul.then(() => {
  Vi = !0, Lr.forEach((c) => {
    Hi(c);
  }), Lr = [];
});
const $r = (c) => {
  let e = 0;
  const t = Math.floor(Math.random() * En), r = {
    blueNoiseTexture: { value: er },
    blueNoiseSize: { value: new g(Br, Br) },
    blueNoiseIndex: {
      get value() {
        return e = (t + e + 1) % En, e;
      },
      set value(n) {
        e = n;
      }
    }
  };
  return c = c.replace("uniform vec2 resolution;", `uniform vec2 resolution;
` + ll), { uniforms: r, fragmentShader: c };
};
function Hi(c) {
  const { fragmentShader: e, uniforms: t } = $r(c.fragmentShader);
  c.fragmentShader = e, c.uniforms = { ...c.uniforms, ...t }, c.needsUpdate = !0;
}
const en = (c) => {
  Vi ? Hi(c) : Lr.push(c);
};
class fl extends xs {
  onBeforeCompile(e) {
    this.uniforms = e.uniforms, e.uniforms.resolution = { value: new g(1, 1) }, e.uniforms.cameraNotMovedFrames = { value: 0 }, e.vertexShader = e.vertexShader.replace(/#include <lights_.*>/g, ""), e.fragmentShader = e.fragmentShader.replace(/#include <lights_.*>/g, ""), e.vertexShader = e.vertexShader.replace(/#include <alpha.*>/g, ""), e.fragmentShader = e.fragmentShader.replace(/#include <alpha.*>/g, ""), e.vertexShader = e.vertexShader.replace(/#include <aomap.*>/g, ""), e.fragmentShader = e.fragmentShader.replace(/#include <aomap.*>/g, ""), e.vertexShader = e.vertexShader.replace(/#include <lightmap.*>/g, ""), e.fragmentShader = e.fragmentShader.replace(/#include <lightmap.*>/g, ""), e.vertexShader = e.vertexShader.replace(/#include <alphahash.*>/g, ""), e.fragmentShader = e.fragmentShader.replace(/#include <alphahash.*>/g, ""), e.vertexShader = e.vertexShader.replace(/#include <alphatest.*>/g, ""), e.fragmentShader = e.fragmentShader.replace(/#include <alphatest.*>/g, ""), e.fragmentShader = e.fragmentShader.replace("#include <opaque_fragment>", ""), e.fragmentShader = e.fragmentShader.replace("#include <colorspace_fragment>", ""), e.fragmentShader = e.fragmentShader.replace("#include <fog_fragment>", ""), e.fragmentShader = e.fragmentShader.replace(
      "void main() {",
      /* glsl */
      `
			#define vUv gl_FragCoord.xy
            uniform vec2 resolution;
            uniform float cameraNotMovedFrames;

            ${ot}

            void main() {
					float a = opacity;

					#ifdef USE_ALPHAMAP
						a *= texture2D( alphaMap, vAlphaMapUv ).g;
					#endif

					if (cameraNotMovedFrames == 0.) {
						if(a < 0.5) {
							discard;
							return;
						}

						a = 1.;
					} else if (a != 1.) {
						float aStep = a > 0.5 ? 1. : 0.;
						a = mix(a, aStep, (1. / (cameraNotMovedFrames * 0.1 + 1.)));

						vec4 noise = blueNoise();
						if (noise.x > a) {
							discard;
							return;
						}
					}
        `
    ).replace(
      "#include <dithering_fragment>",
      /* glsl */
      `
            #include <dithering_fragment>

            vec3 worldNormal = normalize((vec4(normal, 1.) * viewMatrix).xyz);

            vec4 gBuffer = packGBuffer(diffuseColor, worldNormal, roughnessFactor, metalnessFactor, totalEmissiveRadiance);

            gl_FragColor = gBuffer;`
    );
    const { uniforms: t, fragmentShader: r } = $r(e.fragmentShader);
    e.uniforms = { ...e.uniforms, ...t }, e.fragmentShader = r;
  }
}
const cr = new fl();
function hl(c) {
  const e = cr.clone();
  return dl(c, e), e;
}
let ur = Object.keys(cr);
ur = ur.filter(
  (c) => !c.startsWith("_") && !c.startsWith("is") && c !== "uuid" && c !== "type" && c !== "transparent"
);
function dl(c, e) {
  for (const t of ur)
    c[t] !== void 0 && (e[t] = c[t]);
}
const ml = ur.filter(
  (c) => typeof cr[c] == "string" || typeof cr[c] == "number"
);
function pl(c, e) {
  for (const t of ml)
    e[t] = c[t];
}
const vl = new y(0);
let tn = class extends X {
  frame = 21483;
  cachedMaterials = /* @__PURE__ */ new WeakMap();
  visibleMeshes = [];
  lastCameraPosition = new T();
  lastCameraQuaternion = new Yr();
  constructor(e, t) {
    super("GBufferPass"), this._scene = e, this._camera = t, this.initGBufferRenderTarget();
  }
  get texture() {
    return this.renderTarget.texture;
  }
  get depthTexture() {
    return this.renderTarget.depthTexture;
  }
  initGBufferRenderTarget() {
    this.renderTarget = new A(1, 1, {
      type: Y,
      minFilter: b,
      magFilter: b,
      depthBuffer: !0
    }), this.renderTarget.texture.name = "GBufferPass.Texture", this.renderTarget.depthTexture = new Oe(1, 1), this.renderTarget.depthTexture.type = Y, this.renderTarget.depthTexture.name = "GBufferPass.DepthTexture";
  }
  setSize(e, t) {
    this.renderTarget.setSize(e, t);
  }
  dispose() {
    super.dispose(), this.renderTarget.dispose();
  }
  setGBufferMaterialInScene() {
    this.visibleMeshes = ki(this._scene);
    const e = Oi(this._camera, this.lastCameraPosition, this.lastCameraQuaternion);
    for (const t of this.visibleMeshes) {
      const r = t.material;
      let [n, i] = this.cachedMaterials.get(t) || [];
      r !== n && (i && i.dispose(), i = hl(r), this.cachedMaterials.set(t, [r, i])), i.uniforms && (i.uniforms.cameraNotMovedFrames.value = e ? 0 : (i.uniforms.cameraNotMovedFrames.value + 1) % 65535), t.visible = _r(t, r), pl(r, i), t.material = i;
    }
  }
  unsetGBufferMaterialInScene() {
    for (const e of this.visibleMeshes) {
      const [t] = this.cachedMaterials.get(e);
      e.material = t;
    }
  }
  render(e) {
    this.frame = (this.frame + 1) % 4096;
    const { background: t } = this._scene;
    this._scene.background = vl, this.setGBufferMaterialInScene(), e.setRenderTarget(this.renderTarget), e.render(this._scene, this._camera), this.unsetGBufferMaterialInScene(), this.lastCameraPosition.copy(this._camera.position), this.lastCameraQuaternion.copy(this._camera.quaternion), this._scene.background = t;
  }
};
const gl = `varying vec2 vUv;\r
\r
uniform sampler2D accumulatedTexture;\r
uniform highp sampler2D depthTexture;\r
uniform highp sampler2D velocityTexture;\r
uniform sampler2D directLightTexture;\r
uniform vec3 backgroundColor;\r
\r
uniform mat4 projectionMatrix;\r
uniform mat4 projectionMatrixInverse;\r
uniform mat4 cameraMatrixWorld;\r
\r
uniform float maxEnvMapMipLevel;\r
\r
uniform float rayDistance;\r
uniform float thickness;\r
uniform float envBlur;\r
\r
uniform vec2 resolution;\r
\r
uniform float cameraNear;\r
uniform float cameraFar;\r
uniform float nearMinusFar;\r
uniform float nearMulFar;\r
uniform float farMinusNear;\r
\r
struct EquirectHdrInfo {\r
  sampler2D marginalWeights;\r
  sampler2D conditionalWeights;\r
  sampler2D map;\r
  vec2 size;\r
  float totalSumWhole;\r
  float totalSumDecimal;\r
};\r
\r
uniform EquirectHdrInfo envMapInfo;\r
\r
#define INVALID_RAY_COORDS vec2(-1.0);\r
#define EPSILON 0.00001\r
#define ONE_MINUS_EPSILON 1.0 - EPSILON\r
\r
vec2 invTexSize;\r
\r
#define MODE_SSGI 0\r
#define MODE_SSR 1\r
\r
#include <packing>\r
\r
// helper functions\r
#include <gbuffer_packing>\r
#include <ssgi_utils>\r
\r
vec2 RayMarch(inout vec3 dir, inout vec3 hitPos, vec4 random);\r
vec2 BinarySearch(inout vec3 dir, inout vec3 hitPos);\r
\r
struct RayTracingInfo {\r
  float NoV;            // dot(n, v)\r
  float NoL;            // dot(n, l)\r
  float NoH;            // dot(n, h)\r
  float LoH;            // dot(l, h)\r
  float VoH;            // dot(v, h)\r
  bool isDiffuseSample; // whether the sample is diffuse or specular\r
  bool isEnvSample;     // whether the sample is importance sampled from the env\r
                        // map\r
};\r
\r
struct RayTracingResult {\r
  vec3 gi;          // computed global illumination for a sample\r
  vec3 l;           // sample world space direction\r
  vec3 hitPos;      // hit position\r
  bool isMissedRay; // whether the ray missed the scene\r
  vec3 brdf;        // brdf value\r
  float pdf;        // pdf value\r
};\r
\r
struct EnvMisSample {\r
  float pdf;\r
  float probability;\r
  bool isEnvSample;\r
};\r
\r
// !todo: refactor functions\r
// RayTracingResult doSample(const vec3 viewPos, const vec3 viewDir, const vec3\r
// viewNormal, const vec3 worldPos, const vec4 random, Material mat,\r
// RayTracingInfo info);\r
\r
vec3 worldNormal;\r
\r
vec3 doSample(const vec3 viewPos, const vec3 viewDir, const vec3 viewNormal, const vec3 worldPos, const float metalness, const float roughness,\r
              const bool isDiffuseSample, const bool isEnvSample, const float NoV, const float NoL, const float NoH, const float LoH, const float VoH,\r
              const vec4 random, inout vec3 l, inout vec3 hitPos, out bool isMissedRay, out vec3 brdf, out float pdf);\r
\r
void calculateAngles(inout vec3 h, inout vec3 l, inout vec3 v, inout vec3 n, inout float NoL, inout float NoH, inout float LoH, inout float VoH) {\r
  h = normalize(v + l); // half vector\r
\r
  NoL = clamp(dot(n, l), EPSILON, ONE_MINUS_EPSILON);\r
  NoH = clamp(dot(n, h), EPSILON, ONE_MINUS_EPSILON);\r
  LoH = clamp(dot(l, h), EPSILON, ONE_MINUS_EPSILON);\r
  VoH = clamp(dot(v, h), EPSILON, ONE_MINUS_EPSILON);\r
}\r
\r
vec3 worldPos;\r
Material mat;\r
\r
void main() {\r
  float unpackedDepth = textureLod(depthTexture, vUv, 0.0).r;\r
\r
  // filter out background\r
  if (unpackedDepth == 1.0) {\r
    vec4 directLight = textureLod(directLightTexture, vUv, 0.0);\r
    gl_FragColor = packTwoVec4(directLight, directLight);\r
    return;\r
  }\r
\r
  mat = getMaterial(gBufferTexture, vUv);\r
  float roughnessSq = clamp(mat.roughness * mat.roughness, 0.000001, 1.0);\r
\r
  invTexSize = 1. / resolution;\r
\r
  // view-space depth\r
  float viewZ = getViewZ(unpackedDepth);\r
\r
  // view-space position of the current texel\r
  vec3 viewPos = getViewPosition(viewZ);\r
\r
  vec3 viewDir = normalize(viewPos);\r
  worldNormal = mat.normal;\r
  vec3 viewNormal = normalize((vec4(worldNormal, 0.) * cameraMatrixWorld).xyz);\r
  worldPos = (cameraMatrixWorld * vec4(viewPos, 1.)).xyz;\r
\r
  vec3 n = viewNormal; // view-space normal\r
  vec3 v = -viewDir;   // incoming vector\r
  float NoV = max(EPSILON, dot(n, v));\r
\r
  // convert view dir to world-space\r
  vec3 V = (vec4(v, 0.) * viewMatrix).xyz;\r
  vec3 N = worldNormal;\r
\r
  vec4 random;\r
  vec3 H, l, h, F, T, B, envMisDir, gi;\r
  vec3 diffuseGI, specularGI, brdf, hitPos, specularHitPos;\r
\r
  Onb(N, T, B);\r
\r
  V = ToLocal(T, B, N, V);\r
\r
  // fresnel f0\r
  vec3 f0 = mix(vec3(0.04), mat.diffuse.rgb, mat.metalness);\r
\r
  float NoL, NoH, LoH, VoH, diffW, specW, invW, pdf, envPdf, diffuseSamples, specularSamples;\r
  bool isDiffuseSample, isEnvSample, isMissedRay;\r
\r
  random = blueNoise();\r
  // Disney BRDF and sampling source: https://www.shadertoy.com/view/cll3R4\r
  // calculate GGX reflection ray\r
  H = SampleGGXVNDF(V, roughnessSq, roughnessSq, random.r, random.g);\r
  if (H.z < 0.0)\r
    H = -H;\r
\r
  l = normalize(reflect(-V, H));\r
  l = ToWorld(T, B, N, l);\r
\r
  // convert reflected vector back to view-space\r
  l = (vec4(l, 0.) * cameraMatrixWorld).xyz;\r
  l = normalize(l);\r
\r
  calculateAngles(h, l, v, n, NoL, NoH, LoH, VoH);\r
\r
#if mode == MODE_SSGI\r
  // fresnel\r
  F = F_Schlick(f0, VoH);\r
\r
  // diffuse and specular weight\r
  diffW = (1. - mat.metalness) * luminance(mat.diffuse.rgb);\r
  specW = luminance(F);\r
\r
  diffW = max(diffW, EPSILON);\r
  specW = max(specW, EPSILON);\r
\r
  invW = 1. / (diffW + specW);\r
\r
  // relative weights used for choosing either a diffuse or specular ray\r
  diffW *= invW;\r
\r
  // if diffuse lighting should be sampled\r
  isDiffuseSample = random.b < diffW;\r
#else\r
  isDiffuseSample = false;\r
#endif\r
\r
  EnvMisSample ems;\r
  ems.pdf = 1.;\r
\r
  envMisDir = vec3(0.0);\r
  envPdf = 1.;\r
\r
#ifdef importanceSampling\r
  ems.pdf = sampleEquirectProbability(envMapInfo, random.rg, envMisDir);\r
  envMisDir = normalize((vec4(envMisDir, 0.) * cameraMatrixWorld).xyz);\r
\r
  ems.probability = dot(envMisDir, viewNormal);\r
  ems.probability *= mat.roughness;\r
  ems.probability = min(ONE_MINUS_EPSILON, ems.probability);\r
\r
  ems.isEnvSample = random.a < ems.probability;\r
\r
  if (ems.isEnvSample) {\r
    ems.pdf /= 1. - ems.probability;\r
\r
    l = envMisDir;\r
    calculateAngles(h, l, v, n, NoL, NoH, LoH, VoH);\r
  } else {\r
    ems.pdf = 1. - ems.probability;\r
  }\r
#endif\r
\r
  vec3 diffuseRay = ems.isEnvSample ? envMisDir : cosineSampleHemisphere(viewNormal, random.rg);\r
  vec3 specularRay = ems.isEnvSample ? envMisDir : l;\r
\r
// optional diffuse ray\r
#if mode == MODE_SSGI\r
  if (isDiffuseSample) {\r
    l = diffuseRay;\r
\r
    calculateAngles(h, l, v, n, NoL, NoH, LoH, VoH);\r
\r
    gi = doSample(viewPos, viewDir, viewNormal, worldPos, mat.metalness, roughnessSq, isDiffuseSample, ems.isEnvSample, NoV, NoL, NoH, LoH, VoH,\r
                  random, l, hitPos, isMissedRay, brdf, pdf);\r
\r
    gi *= brdf;\r
\r
    if (ems.isEnvSample) {\r
      gi *= misHeuristic(ems.pdf, pdf);\r
    } else {\r
      gi /= pdf;\r
    }\r
    gi /= ems.pdf;\r
\r
    diffuseSamples++;\r
\r
    diffuseGI = mix(diffuseGI, gi, 1. / diffuseSamples);\r
  }\r
#endif\r
\r
  // specular ray (traced every frame)\r
  l = specularRay;\r
  calculateAngles(h, l, v, n, NoL, NoH, LoH, VoH);\r
\r
  gi = doSample(viewPos, viewDir, viewNormal, worldPos, mat.metalness, roughnessSq, isDiffuseSample, ems.isEnvSample, NoV, NoL, NoH, LoH, VoH, random,\r
                l, hitPos, isMissedRay, brdf, pdf);\r
\r
  gi *= brdf;\r
\r
  if (ems.isEnvSample) {\r
    gi *= misHeuristic(ems.pdf, pdf);\r
  } else {\r
    gi /= pdf;\r
  }\r
  gi /= ems.pdf;\r
\r
  specularHitPos = hitPos;\r
\r
  specularSamples++;\r
\r
  specularGI = mix(specularGI, gi, 1. / specularSamples);\r
\r
#ifdef useDirectLight\r
  vec3 directLight = textureLod(directLightTexture, vUv, 0.).rgb;\r
\r
  diffuseGI += directLight;\r
  specularGI += directLight;\r
#endif\r
\r
  highp vec4 gDiffuse, gSpecular;\r
\r
#if mode == MODE_SSGI\r
  if (diffuseSamples == 0.0)\r
    diffuseGI = vec3(-1.0);\r
  gDiffuse = vec4(diffuseGI, mat.roughness);\r
#endif\r
\r
  // calculate world-space ray length used for reprojecting hit points instead\r
  // of screen-space pixels in the temporal reproject pass\r
  highp float rayLength = 0.0;\r
  vec4 hitPosWS;\r
  vec3 cameraPosWS = cameraMatrixWorld[3].xyz;\r
\r
  isMissedRay = hitPos.x > 10.0e8;\r
\r
  if (!isMissedRay) {\r
    // convert hitPos from view- to world-space\r
    hitPosWS = cameraMatrixWorld * vec4(specularHitPos, 1.0);\r
\r
    // get the camera position in world-space from the camera matrix\r
    rayLength = distance(cameraPosWS, hitPosWS.xyz);\r
  }\r
\r
  // encode both roughness and rayLength into the alpha channel\r
  highp uint packedRoughnessRayLength = packHalf2x16(vec2(rayLength, mat.roughness));\r
  highp float a = uintBitsToFloat(packedRoughnessRayLength);\r
\r
#if mode == MODE_SSGI\r
  gSpecular = vec4(specularGI, rayLength);\r
  gl_FragColor = packTwoVec4(gDiffuse, gSpecular);\r
#else\r
  gSpecular = vec4(specularGI, a);\r
  gl_FragColor = gSpecular;\r
#endif\r
}\r
\r
vec3 getEnvColor(vec3 l, vec3 worldPos, float roughness, bool isDiffuseSample, bool isEnvSample) {\r
  vec3 envMapSample;\r
#ifdef USE_ENVMAP\r
  // world-space reflected ray\r
  vec3 reflectedWS = normalize((vec4(l, 0.) * viewMatrix).xyz);\r
\r
#ifdef BOX_PROJECTED_ENV_MAP\r
  reflectedWS = parallaxCorrectNormal(reflectedWS.xyz, envMapSize, envMapPosition, worldPos);\r
  reflectedWS = normalize(reflectedWS.xyz);\r
#endif\r
\r
  float mip = envBlur * maxEnvMapMipLevel;\r
\r
  if (!isDiffuseSample && roughness < 0.15)\r
    mip *= roughness / 0.15;\r
\r
  envMapSample = sampleEquirectEnvMapColor(reflectedWS, envMapInfo.map, mip);\r
\r
  float maxEnvLum = isEnvSample ? 100.0 : 25.0;\r
\r
  if (maxEnvLum != 0.0) {\r
    // we won't deal with calculating direct sun light from the env map as it\r
    // is too noisy\r
    float envLum = luminance(envMapSample);\r
\r
    if (envLum > maxEnvLum) {\r
      envMapSample *= maxEnvLum / envLum;\r
    }\r
  }\r
\r
  return envMapSample;\r
#else\r
  // if we don't have an environment map, just return black\r
  return vec3(0.0);\r
#endif\r
}\r
\r
float getSaturation(vec3 c) {\r
  float maxComponent = max(max(c.r, c.g), c.b);\r
  float minComponent = min(min(c.r, c.g), c.b);\r
\r
  float delta = maxComponent - minComponent;\r
\r
  // If the maximum and minimum components are equal, the color is achromatic (gray)\r
  if (maxComponent == minComponent) {\r
    return 0.0;\r
  } else {\r
    return delta / maxComponent;\r
  }\r
}\r
\r
vec3 doSample(const vec3 viewPos, const vec3 viewDir, const vec3 viewNormal, const vec3 worldPos, const float metalness, const float roughness,\r
              const bool isDiffuseSample, const bool isEnvSample, const float NoV, const float NoL, const float NoH, const float LoH, const float VoH,\r
              const vec4 random, inout vec3 l, inout vec3 hitPos, out bool isMissedRay, out vec3 brdf, out float pdf) {\r
  float cosTheta = max(0.0, dot(viewNormal, l));\r
\r
  if (isDiffuseSample) {\r
    vec3 diffuseBrdf = evalDisneyDiffuse(NoL, NoV, LoH, roughness, metalness);\r
    pdf = NoL / M_PI;\r
\r
    brdf = diffuseBrdf;\r
  } else {\r
    vec3 specularBrdf = evalDisneySpecular(roughness, NoH, NoV, NoL);\r
    pdf = GGXVNDFPdf(NoH, NoV, roughness);\r
\r
    brdf = specularBrdf;\r
  }\r
\r
  brdf *= cosTheta;\r
  pdf = max(EPSILON, pdf);\r
\r
  hitPos = viewPos;\r
\r
  vec2 coords = RayMarch(l, hitPos, random);\r
\r
  bool allowMissedRays = false;\r
#ifdef missedRays\r
  allowMissedRays = true;\r
#endif\r
\r
  isMissedRay = hitPos.x == 10.0e9;\r
\r
  vec3 envMapSample = vec3(0.);\r
\r
  // inisEnvSample ray, use environment lighting as fallback\r
  if (isMissedRay && !allowMissedRays)\r
    return getEnvColor(l, worldPos, roughness, isDiffuseSample, isEnvSample);\r
\r
  // reproject the coords from the last frame\r
  vec4 velocity = textureLod(velocityTexture, coords.xy, 0.0);\r
\r
  vec2 reprojectedUv = coords.xy - velocity.xy;\r
\r
  vec3 SSGI;\r
  vec3 envColor = getEnvColor(l, worldPos, roughness, isDiffuseSample, isEnvSample);\r
\r
  // check if the reprojected coordinates are within the screen\r
  if (reprojectedUv.x >= 0.0 && reprojectedUv.x <= 1.0 && reprojectedUv.y >= 0.0 && reprojectedUv.y <= 1.0) {\r
    vec4 reprojectedGI = textureLod(accumulatedTexture, reprojectedUv, 0.);\r
\r
    float saturation = getSaturation(mat.diffuse.rgb);\r
\r
    // saturate reprojected GI by the saturation value\r
    reprojectedGI.rgb = mix(reprojectedGI.rgb, vec3(luminance(reprojectedGI.rgb)), (1. - roughness) * saturation * 0.4);\r
\r
    SSGI = reprojectedGI.rgb;\r
\r
    float aspect = resolution.x / resolution.y;\r
\r
    float border = 0.15;\r
    float borderFactor = smoothstep(0.0, border, coords.x) * smoothstep(1.0, 1.0 - border, coords.x) * smoothstep(0.0, border, coords.y) *\r
                         smoothstep(1.0, 1.0 - border, coords.y);\r
\r
    borderFactor = sqrt(borderFactor);\r
    SSGI = mix(envColor, SSGI, borderFactor);\r
  } else {\r
    return envColor;\r
  }\r
\r
  if (allowMissedRays) {\r
    float ssgiLum = luminance(SSGI);\r
    float envLum = luminance(envMapSample);\r
\r
    if (envLum > ssgiLum)\r
      SSGI = envMapSample;\r
  }\r
\r
  return SSGI;\r
}\r
\r
vec2 RayMarch(inout vec3 dir, inout vec3 hitPos, vec4 random) {\r
  float rayHitDepthDifference;\r
\r
  // todo: investigate offset (different value?)\r
  // hitPos += dir * 0.01;\r
\r
  dir *= rayDistance / float(steps);\r
\r
  vec2 uv;\r
\r
  for (int i = 1; i < steps; i++) {\r
    // use slower increments for the first few steps to sharpen contact shadows: https://www.desmos.com/calculator/8cvzy4kdeb\r
    float cs = 1. - exp(-0.25 * pow(float(i) + random.b - 0.5, 2.));\r
    hitPos += dir * cs;\r
\r
    uv = viewSpaceToScreenSpace(hitPos);\r
\r
    float unpackedDepth = textureLod(depthTexture, uv, 0.0).r;\r
    float z = getViewZ(unpackedDepth);\r
\r
    rayHitDepthDifference = z - hitPos.z;\r
\r
    if (rayHitDepthDifference >= 0.0 && rayHitDepthDifference < thickness) {\r
      if (refineSteps == 0) {\r
        return uv;\r
      } else {\r
        return BinarySearch(dir, hitPos);\r
      }\r
    }\r
  }\r
\r
  hitPos.xyz = vec3(10.0e9);\r
\r
  return uv;\r
}\r
\r
vec2 BinarySearch(inout vec3 dir, inout vec3 hitPos) {\r
  float rayHitDepthDifference;\r
  vec2 uv;\r
\r
  dir *= 0.5;\r
  hitPos -= dir;\r
\r
  for (int i = 0; i < refineSteps; i++) {\r
    uv = viewSpaceToScreenSpace(hitPos);\r
\r
    float unpackedDepth = textureLod(depthTexture, uv, 0.0).r;\r
    float z = getViewZ(unpackedDepth);\r
\r
    rayHitDepthDifference = z - hitPos.z;\r
\r
    dir *= 0.5;\r
    if (rayHitDepthDifference >= 0.0) {\r
      hitPos -= dir;\r
    } else {\r
      hitPos += dir;\r
    }\r
  }\r
\r
  uv = viewSpaceToScreenSpace(hitPos);\r
\r
  return uv;\r
}`, xl = `#define PI M_PI\r
\r
#define luminance(a) dot(vec3(0.2125, 0.7154, 0.0721), a)\r
\r
// source:\r
// https://github.com/mrdoob/three.js/blob/342946c8392639028da439b6dc0597e58209c696/examples/js/shaders/SAOShader.js#L123\r
float getViewZ(const in float depth) {\r
#ifdef PERSPECTIVE_CAMERA\r
  return nearMulFar / (farMinusNear * depth - cameraFar);\r
#else\r
  return depth * nearMinusFar - cameraNear;\r
#endif\r
}\r
\r
// source:\r
// https://github.com/mrdoob/three.js/blob/dev/examples/js/shaders/SSAOShader.js\r
vec3 getViewPosition(float viewZ) {\r
  float clipW = projectionMatrix[2][3] * viewZ + projectionMatrix[3][3];\r
  vec4 clipPosition = vec4((vec3(vUv, viewZ) - 0.5) * 2.0, 1.0);\r
  clipPosition *= clipW;\r
  vec3 p = (projectionMatrixInverse * clipPosition).xyz;\r
  p.z = viewZ;\r
  return p;\r
}\r
\r
vec2 viewSpaceToScreenSpace(const vec3 position) {\r
  vec4 projectedCoord = projectionMatrix * vec4(position, 1.0);\r
  projectedCoord.xy /= projectedCoord.w;\r
  // [-1, 1] --> [0, 1] (NDC to screen position)\r
  projectedCoord.xy = projectedCoord.xy * 0.5 + 0.5;\r
\r
  return projectedCoord.xy;\r
}\r
\r
vec3 worldSpaceToViewSpace(vec3 worldPosition) {\r
  vec4 viewPosition = viewMatrix * vec4(worldPosition, 1.0);\r
  return viewPosition.xyz / viewPosition.w;\r
}\r
\r
#ifdef BOX_PROJECTED_ENV_MAP\r
uniform vec3 envMapSize;\r
uniform vec3 envMapPosition;\r
\r
vec3 parallaxCorrectNormal(const vec3 v, const vec3 cubeSize, const vec3 cubePos, const vec3 worldPosition) {\r
  vec3 nDir = normalize(v);\r
  vec3 rbmax = (.5 * cubeSize + cubePos - worldPosition) / nDir;\r
  vec3 rbmin = (-.5 * cubeSize + cubePos - worldPosition) / nDir;\r
  vec3 rbminmax;\r
  rbminmax.x = (nDir.x > 0.) ? rbmax.x : rbmin.x;\r
  rbminmax.y = (nDir.y > 0.) ? rbmax.y : rbmin.y;\r
  rbminmax.z = (nDir.z > 0.) ? rbmax.z : rbmin.z;\r
  float correction = min(min(rbminmax.x, rbminmax.y), rbminmax.z);\r
  vec3 boxIntersection = worldPosition + nDir * correction;\r
\r
  return boxIntersection - cubePos;\r
}\r
#endif\r
\r
#define M_PI 3.1415926535897932384626433832795\r
\r
// source:\r
// https://github.com/gkjohnson/three-gpu-pathtracer/blob/4de53ebc08dffdb21dbb14beb5c9953b600978cc/src/shader/shaderUtils.js#L215\r
// ray sampling x and z are swapped to align with expected background view\r
vec2 equirectDirectionToUv(const vec3 direction) {\r
  // from Spherical.setFromCartesianCoords\r
  vec2 uv = vec2(atan(direction.z, direction.x), acos(direction.y));\r
  uv /= vec2(2.0 * M_PI, M_PI);\r
  // apply adjustments to get values in range [0, 1] and y right side up\r
\r
  uv.x += 0.5;\r
  uv.y = 1.0 - uv.y;\r
\r
  return uv;\r
}\r
\r
// source: https://github.com/gkjohnson/three-gpu-pathtracer\r
vec3 equirectUvToDirection(vec2 uv) {\r
  // undo above adjustments\r
  uv.x -= 0.5;\r
  uv.y = 1.0 - uv.y;\r
  // from Vector3.setFromSphericalCoords\r
  float theta = uv.x * 2.0 * PI;\r
  float phi = uv.y * PI;\r
  float sinPhi = sin(phi);\r
  return vec3(sinPhi * cos(theta), cos(phi), sinPhi * sin(theta));\r
}\r
\r
// source:\r
// https://github.com/gkjohnson/three-gpu-pathtracer/blob/3340cc19c796a01abe0ec121930154ec3301e4f2/src/shader/shaderEnvMapSampling.js#L3\r
vec3 sampleEquirectEnvMapColor(const vec3 direction, const sampler2D map, const float lod) {\r
  return textureLod(map, equirectDirectionToUv(direction), lod).rgb;\r
}\r
\r
// source of the following functions: https://www.shadertoy.com/view/cll3R4\r
\r
mat3 getBasisFromNormal(const vec3 normal) {\r
  vec3 other;\r
  if (abs(normal.x) > 0.5) {\r
    other = vec3(0.0, 1.0, 0.0);\r
  } else {\r
    other = vec3(1.0, 0.0, 0.0);\r
  }\r
  vec3 ortho = normalize(cross(normal, other));\r
  vec3 ortho2 = normalize(cross(normal, ortho));\r
  return mat3(ortho2, ortho, normal);\r
}\r
\r
vec3 F_Schlick(const vec3 f0, const float theta) { return f0 + (1. - f0) * pow(1.0 - theta, 5.); }\r
\r
float F_Schlick(const float f0, const float f90, const float theta) { return f0 + (f90 - f0) * pow(1.0 - theta, 5.0); }\r
\r
float D_GTR(const float roughness, const float NoH, const float k) {\r
  float a2 = pow(roughness, 2.);\r
  return a2 / (PI * pow((NoH * NoH) * (a2 * a2 - 1.) + 1., k));\r
}\r
\r
float SmithG(const float NDotV, const float alphaG) {\r
  float a = alphaG * alphaG;\r
  float b = NDotV * NDotV;\r
  return (2.0 * NDotV) / (NDotV + sqrt(a + b - a * b));\r
}\r
\r
float GGXVNDFPdf(const float NoH, const float NoV, const float roughness) {\r
  float D = D_GTR(roughness, NoH, 2.);\r
  float G1 = SmithG(NoV, roughness * roughness);\r
  return (D * G1) / max(0.00001, 4.0 * NoV);\r
}\r
\r
float GeometryTerm(const float NoL, const float NoV, const float roughness) {\r
  float a2 = roughness * roughness;\r
  float G1 = SmithG(NoV, a2);\r
  float G2 = SmithG(NoL, a2);\r
  return G1 * G2;\r
}\r
\r
vec3 evalDisneyDiffuse(const float NoL, const float NoV, const float LoH, const float roughness, const float metalness) {\r
  float FD90 = 0.5 + 2. * roughness * pow(LoH, 2.);\r
  float a = F_Schlick(1., FD90, NoL);\r
  float b = F_Schlick(1., FD90, NoV);\r
\r
  return vec3((a * b / PI) * (1. - metalness));\r
}\r
\r
vec3 evalDisneySpecular(const float roughness, const float NoH, const float NoV, const float NoL) {\r
  float D = D_GTR(roughness, NoH, 2.);\r
  float G = GeometryTerm(NoL, NoV, pow(0.5 + roughness * .5, 2.));\r
\r
  vec3 spec = vec3(D * G / (4. * NoL * NoV));\r
\r
  return spec;\r
}\r
\r
vec3 SampleGGXVNDF(const vec3 V, const float ax, const float ay, const float r1, const float r2) {\r
  vec3 Vh = normalize(vec3(ax * V.x, ay * V.y, V.z));\r
\r
  float lensq = Vh.x * Vh.x + Vh.y * Vh.y;\r
  vec3 T1 = lensq > 0. ? vec3(-Vh.y, Vh.x, 0.) * inversesqrt(lensq) : vec3(1., 0., 0.);\r
  vec3 T2 = cross(Vh, T1);\r
\r
  float r = sqrt(r1);\r
  float phi = 2.0 * PI * r2;\r
  float t1 = r * cos(phi);\r
  float t2 = r * sin(phi);\r
  float s = 0.5 * (1.0 + Vh.z);\r
  t2 = (1.0 - s) * sqrt(1.0 - t1 * t1) + s * t2;\r
\r
  vec3 Nh = t1 * T1 + t2 * T2 + sqrt(max(0.0, 1.0 - t1 * t1 - t2 * t2)) * Vh;\r
\r
  return normalize(vec3(ax * Nh.x, ay * Nh.y, max(0.0, Nh.z)));\r
}\r
\r
void Onb(const vec3 N, inout vec3 T, inout vec3 B) {\r
  vec3 up = abs(N.z) < 0.9999999 ? vec3(0, 0, 1) : vec3(1, 0, 0);\r
  T = normalize(cross(up, N));\r
  B = cross(N, T);\r
}\r
\r
vec3 ToLocal(const vec3 X, const vec3 Y, const vec3 Z, const vec3 V) { return vec3(dot(V, X), dot(V, Y), dot(V, Z)); }\r
\r
vec3 ToWorld(const vec3 X, const vec3 Y, const vec3 Z, const vec3 V) { return V.x * X + V.y * Y + V.z * Z; }\r
\r
// source: https://www.shadertoy.com/view/cll3R4\r
vec3 cosineSampleHemisphere(const vec3 n, const vec2 u) {\r
  float r = sqrt(u.x);\r
  float theta = 2.0 * PI * u.y;\r
\r
  vec3 b = normalize(cross(n, vec3(0.0, 1.0, 1.0)));\r
  vec3 t = cross(b, n);\r
\r
  return normalize(r * sin(theta) * b + sqrt(1.0 - u.x) * n + r * cos(theta) * t);\r
}\r
\r
// end: functions\r
\r
// source: https://github.com/gkjohnson/three-gpu-pathtracer\r
float equirectDirectionPdf(vec3 direction) {\r
  vec2 uv = equirectDirectionToUv(direction);\r
  float theta = uv.y * PI;\r
  float sinTheta = sin(theta);\r
  if (sinTheta == 0.0) {\r
    return 0.0;\r
  }\r
\r
  return 1.0 / (2.0 * PI * PI * sinTheta);\r
}\r
\r
// for whatever reason, using names like "random" or "noise" for the blueNoise\r
// param results in shader errors, e.g. "_urandom is not defined"\r
// source: https://github.com/gkjohnson/three-gpu-pathtracer\r
float sampleEquirectProbability(EquirectHdrInfo info, vec2 blueNoise, out vec3 direction) {\r
  // sample env map cdf\r
  float v = textureLod(info.marginalWeights, vec2(blueNoise.x, 0.0), 0.).x;\r
  float u = textureLod(info.conditionalWeights, vec2(blueNoise.y, v), 0.).x;\r
  vec2 uv = vec2(u, v);\r
\r
  vec3 derivedDirection = equirectUvToDirection(uv);\r
  direction = derivedDirection;\r
  vec3 color = texture(info.map, uv).rgb;\r
\r
  float totalSum = info.totalSumWhole + info.totalSumDecimal;\r
  float lum = luminance(color);\r
  float pdf = lum / totalSum;\r
\r
  return info.size.x * info.size.y * pdf;\r
}\r
\r
float misHeuristic(float a, float b) {\r
  float aa = a * a;\r
  float bb = b * b;\r
  return aa / (aa + bb);\r
}\r
\r
// this function takes a normal and a direction as input and returns a new\r
// direction that is aligned to the normal\r
vec3 alignToNormal(const vec3 normal, const vec3 direction) {\r
  vec3 tangent;\r
  vec3 bitangent;\r
  Onb(normal, tangent, bitangent);\r
\r
  vec3 localDir = ToLocal(tangent, bitangent, normal, direction);\r
  vec3 localDirAligned = vec3(localDir.x, localDir.y, abs(localDir.z));\r
  vec3 alignedDir = ToWorld(tangent, bitangent, normal, localDirAligned);\r
\r
  return alignedDir;\r
}\r
\r
// source:\r
// http://rodolphe-vaillant.fr/entry/118/curvature-of-a-distance-field-implicit-surface\r
float getFlatness(vec3 g, vec3 rp) {\r
  vec3 gw = fwidth(g);\r
  vec3 pw = fwidth(rp);\r
\r
  float wfcurvature = length(gw) / length(pw);\r
  wfcurvature = smoothstep(0.0, 30., wfcurvature);\r
\r
  return clamp(wfcurvature, 0., 1.);\r
}`, Sl = ({ data: { width: c, height: e, isFloatType: t, flipY: r, data: n } }) => {
  const i = /* @__PURE__ */ s();
  function s() {
    const d = new ArrayBuffer(4), v = new Float32Array(d), x = new Uint32Array(d), w = new Uint32Array(512), M = new Uint32Array(512);
    for (let S = 0; S < 256; ++S) {
      const L = S - 127;
      L < -27 ? (w[S] = 0, w[S | 256] = 32768, M[S] = 24, M[S | 256] = 24) : L < -14 ? (w[S] = 1024 >> -L - 14, w[S | 256] = 1024 >> -L - 14 | 32768, M[S] = -L - 1, M[S | 256] = -L - 1) : L <= 15 ? (w[S] = L + 15 << 10, w[S | 256] = L + 15 << 10 | 32768, M[S] = 13, M[S | 256] = 13) : L < 128 ? (w[S] = 31744, w[S | 256] = 64512, M[S] = 24, M[S | 256] = 24) : (w[S] = 31744, w[S | 256] = 64512, M[S] = 13, M[S | 256] = 13);
    }
    const D = new Uint32Array(2048), P = new Uint32Array(64), O = new Uint32Array(64);
    for (let S = 1; S < 1024; ++S) {
      let L = S << 13, H = 0;
      for (; (L & 8388608) === 0; )
        L <<= 1, H -= 8388608;
      L &= -8388609, H += 947912704, D[S] = L | H;
    }
    for (let S = 1024; S < 2048; ++S)
      D[S] = 939524096 + (S - 1024 << 13);
    for (let S = 1; S < 31; ++S)
      P[S] = S << 23;
    P[31] = 1199570944, P[32] = 2147483648;
    for (let S = 33; S < 63; ++S)
      P[S] = 2147483648 + (S - 32 << 23);
    P[63] = 3347054592;
    for (let S = 1; S < 64; ++S)
      S !== 32 && (O[S] = 1024);
    return {
      floatView: v,
      uint32View: x,
      baseTable: w,
      shiftTable: M,
      mantissaTable: D,
      exponentTable: P,
      offsetTable: O
    };
  }
  function o(d) {
    const v = d >> 10;
    return i.uint32View[0] = i.mantissaTable[i.offsetTable[v] + (d & 1023)] + i.exponentTable[v], i.floatView[0];
  }
  function l(d, v, x) {
    return 0.2126 * d + 0.7152 * v + 0.0722 * x;
  }
  const u = (d, v, x = 0, w = d.length) => {
    let M = x, D = x + w - 1;
    for (; M < D; ) {
      const P = M + D >> 1;
      d[P] < v ? M = P + 1 : D = P;
    }
    return M - x;
  }, f = (d, v, x, w, M, D) => {
    if (w)
      for (let z = 0, k = x - 1; z <= k; z++)
        for (let R = 0, U = v * 4; R < U; R += 4) {
          const q = k - z, ie = z * U + R, J = q * U + R;
          d[J] = d[ie], d[J + 1] = d[ie + 1], d[J + 2] = d[ie + 2], d[J + 3] = d[ie + 3];
        }
    const P = new Float32Array(v * x), O = new Float32Array(v * x), S = new Float32Array(x), L = new Float32Array(x);
    let H = 0, Z = 0;
    for (let z = 0; z < x; z++) {
      let k = 0;
      for (let R = 0; R < v; R++) {
        const U = z * v + R, q = d[4 * U + 0], ie = d[4 * U + 1], J = d[4 * U + 2], Pe = l(q, ie, J);
        k += Pe, H += Pe, P[U] = Pe, O[U] = k;
      }
      if (k !== 0)
        for (let R = z * v, U = z * v + v; R < U; R++)
          P[R] /= k, O[R] /= k;
      Z += k, S[z] = k, L[z] = Z;
    }
    if (Z !== 0)
      for (let z = 0, k = S.length; z < k; z++)
        S[z] /= Z, L[z] /= Z;
    for (let z = 0; z < x; z++) {
      const k = (z + 1) / x, R = u(L, k);
      M[z] = (R + 0.5) / x;
    }
    for (let z = 0; z < x; z++)
      for (let k = 0; k < v; k++) {
        const R = z * v + k, U = (k + 1) / v, q = u(O, U, z * v, v);
        D[R] = (q + 0.5) / v;
      }
    return H;
  };
  if (!t) {
    const d = new Float32Array(n.length);
    for (let v = 0; v < n.length; v++)
      d[v] = o(n[v]);
    n = d;
  }
  const h = new Float32Array(e), m = new Float32Array(c * e), p = f(n, c, e, r, h, m);
  postMessage({ totalSumValue: p, marginalDataArray: h, conditionalDataArray: m });
}, yl = new Blob(["onmessage = " + Sl], { type: "application/javascript" }), Ml = URL.createObjectURL(yl);
let wl = class {
  constructor() {
    const e = new le(new Float32Array([1, 1, 1, 1]), 1, 1);
    e.type = Y, e.format = ne, e.minFilter = b, e.magFilter = b, e.wrapS = G, e.wrapT = G, e.generateMipmaps = !1, e.needsUpdate = !0;
    const t = new le(new Float32Array([0, 1]), 1, 2);
    t.type = Y, t.format = Bt, t.minFilter = b, t.magFilter = b, t.generateMipmaps = !1, t.needsUpdate = !0;
    const r = new le(new Float32Array([0, 0, 1, 1]), 2, 2);
    r.type = Y, r.format = Bt, r.minFilter = b, r.magFilter = b, r.generateMipmaps = !1, r.needsUpdate = !0, this.map = e, this.marginalWeights = t, this.conditionalWeights = r, this.totalSumWhole = 1, this.totalSumDecimal = 0, this.size = new g();
  }
  dispose() {
    this.marginalWeights.dispose(), this.conditionalWeights.dispose(), this.map.dispose();
  }
  updateFrom(e) {
    e = e.clone();
    const { width: t, height: r, data: n } = e.image, { type: i } = e;
    return this.size.set(t, r), new Promise((s) => {
      this.worker?.terminate(), this.worker = new Worker(Ml), this.worker.postMessage({ width: t, height: r, isFloatType: i === Y, flipY: e.flipY, data: n }), this.worker.onmessage = ({ data: { totalSumValue: o, marginalDataArray: l, conditionalDataArray: u } }) => {
        this.dispose();
        const { marginalWeights: f, conditionalWeights: h } = this;
        f.image = { width: r, height: 1, data: l }, f.needsUpdate = !0, h.image = { width: t, height: r, data: u }, h.needsUpdate = !0;
        const m = ~~o, p = o - m;
        this.totalSumWhole = m, this.totalSumDecimal = p, this.map = e, this.worker = null, s(e);
      };
    });
  }
}, Tl = class extends N {
  constructor() {
    super({
      type: "SSGIMaterial",
      uniforms: {
        accumulatedTexture: new a(null),
        gBufferTexture: new a(null),
        depthTexture: new a(null),
        velocityTexture: new a(null),
        directLightTexture: new a(null),
        blueNoiseTexture: new a(null),
        projectionMatrix: new a(new C()),
        projectionMatrixInverse: new a(new C()),
        cameraMatrixWorld: new a(new C()),
        viewMatrix: new a(new C()),
        cameraNear: new a(0),
        cameraFar: new a(0),
        nearMulFar: new a(0),
        nearMinusFar: new a(0),
        farMinusNear: new a(0),
        rayDistance: new a(0),
        thickness: new a(0),
        frame: new a(0),
        envBlur: new a(0),
        maxEnvMapMipLevel: new a(0),
        envMapInfo: { value: new wl() },
        envMapPosition: new a(new T()),
        envMapSize: new a(new T()),
        backgroundColor: new a(new y()),
        resolution: new a(new g()),
        blueNoiseRepeat: new a(new g())
      },
      defines: {
        steps: 20,
        refineSteps: 5,
        CUBEUV_TEXEL_WIDTH: 0,
        CUBEUV_TEXEL_HEIGHT: 0,
        CUBEUV_MAX_MIP: 0,
        vWorldPosition: "worldPos"
      },
      fragmentShader: gl.replace("#include <ssgi_utils>", xl).replace("#include <gbuffer_packing>", ot),
      vertexShader: We,
      blending: B,
      depthWrite: !1,
      depthTest: !1,
      toneMapped: !1
    }), en(this);
  }
};
const bl = new y(0);
let Dl = class extends X {
  defaultFragmentShader = "";
  frame = 21483;
  constructor(e, t) {
    super("SSGIPass"), this.ssgiEffect = e, this._scene = e._scene, this._camera = e._camera, this.fullscreenMaterial = new Tl(), this.defaultFragmentShader = this.fullscreenMaterial.fragmentShader, this.renderTarget = new A(1, 1, {
      type: Y,
      minFilter: b,
      magFilter: b,
      depthBuffer: !1
    }), this.renderTarget.texture.name = "SSGIPass.Texture", this.fullscreenMaterial.uniforms.cameraMatrixWorld.value = this._camera.matrixWorld, this.fullscreenMaterial.uniforms.viewMatrix.value = this._camera.matrixWorldInverse, this.fullscreenMaterial.uniforms.projectionMatrix.value = this._camera.projectionMatrix, this.fullscreenMaterial.uniforms.projectionMatrixInverse.value = this._camera.projectionMatrixInverse, e._camera.isPerspectiveCamera && (this.fullscreenMaterial.defines.PERSPECTIVE_CAMERA = ""), this.fullscreenMaterial.defines.mode = ["ssgi", "ssr"].indexOf(t.mode), this.gBufferPass = new tn(this._scene, this._camera), this.fullscreenMaterial.uniforms.gBufferTexture.value = this.gBufferPass.texture, this.fullscreenMaterial.uniforms.depthTexture.value = this.gBufferPass.depthTexture;
  }
  get texture() {
    return this.renderTarget.texture;
  }
  setSize(e, t) {
    this.renderTarget.setSize(e * this.ssgiEffect.resolutionScale, t * this.ssgiEffect.resolutionScale), this.gBufferPass.setSize(e, t), this.fullscreenMaterial.uniforms.resolution.value.set(this.renderTarget.width, this.renderTarget.height);
  }
  dispose() {
    super.dispose(), this.renderTarget.dispose(), this.renderTarget.dispose(), this.fullscreenMaterial.dispose();
  }
  render(e) {
    this.frame = (this.frame + 1) % 4096;
    const { mask: t } = this._camera.layers, r = this.ssgiEffect.selection.size > 0;
    this._camera.layers.set(r ? this.ssgiEffect.selection.layer : 0), this.gBufferPass.render(e), this._camera.layers.mask = t, this.fullscreenMaterial.uniforms.frame.value = this.frame, this.fullscreenMaterial.uniforms.cameraNear.value = this._camera.near, this.fullscreenMaterial.uniforms.cameraFar.value = this._camera.far, this.fullscreenMaterial.uniforms.nearMinusFar.value = this._camera.near - this._camera.far, this.fullscreenMaterial.uniforms.farMinusNear.value = this._camera.far - this._camera.near, this.fullscreenMaterial.uniforms.nearMulFar.value = this._camera.near * this._camera.far, this.fullscreenMaterial.uniforms.accumulatedTexture.value = this.ssgiEffect.denoiser.texture, this.fullscreenMaterial.uniforms.velocityTexture.value = this.ssgiEffect.velocityTexture;
    const n = this._scene.background instanceof y ? this._scene.background : bl;
    this.fullscreenMaterial.uniforms.backgroundColor.value.copy(n), e.setRenderTarget(this.renderTarget), e.render(this.scene, this.camera);
  }
};
const Ur = 1.324717957244746, Pl = 1 / Ur, Cl = 1 / (Ur * Ur), Rn = 1.1127756842787055, Al = (c) => {
  const e = [];
  for (let t = 0; t < c; t++)
    e.push([(Rn + Pl * t) % 1, (Rn + Cl * t) % 1]);
  return e;
}, Nn = Al(256).map(([c, e]) => [c - 0.5, e - 0.5]);
function El(c, e, t, r, n = 1) {
  const [i, s] = Nn[r % Nn.length];
  t.setViewOffset && t.setViewOffset(c, e, i * n, s * n, c, e);
}
const Rl = `varying vec2 vUv;\r
\r
uniform highp sampler2D inputTexture;\r
uniform highp sampler2D velocityTexture;\r
\r
uniform highp sampler2D depthTexture;\r
uniform highp sampler2D lastVelocityTexture;\r
\r
uniform float maxBlend;\r
uniform float neighborhoodClampIntensity;\r
uniform bool fullAccumulate;\r
uniform vec2 invTexSize;\r
uniform float cameraNear;\r
uniform float cameraFar;\r
\r
uniform mat4 projectionMatrix;\r
uniform mat4 projectionMatrixInverse;\r
uniform mat4 cameraMatrixWorld;\r
uniform vec3 cameraPos;\r
uniform vec3 prevCameraPos;\r
uniform mat4 prevViewMatrix;\r
uniform mat4 prevCameraMatrixWorld;\r
uniform mat4 prevProjectionMatrix;\r
uniform mat4 prevProjectionMatrixInverse;\r
\r
uniform float keepData;\r
\r
#define EPSILON 0.00001\r
\r
#define DIFFUSE_SPECULAR 0\r
#define DIFFUSE 1\r
#define SPECULAR 2\r
\r
#include <gbuffer_packing>\r
#include <packing>\r
#include <reproject>\r
\r
vec3 reprojectedUvDiffuse = vec3(-1.0), reprojectedUvSpecular = vec3(-1.0);\r
\r
// this function does the final accumulation of the input texture and the accumulated texture\r
// it computes a final blend value, taking into account factors such as movement and confidence\r
void accumulate(inout vec4 outputColor, inout vec4 inp, inout vec4 acc, inout float roughness, inout float moveFactor, bool doReprojectSpecular) {\r
  vec3 reprojectedUvConfidence = doReprojectSpecular ? reprojectedUvSpecular : reprojectedUvDiffuse;\r
\r
  vec2 reprojectedUv = reprojectedUvConfidence.xy;\r
  float confidence = reprojectedUvConfidence.z;\r
  confidence = pow(confidence, confidencePower);\r
\r
  float accumBlend = 1. - 1. / (acc.a + 1.0);\r
  accumBlend = mix(0., accumBlend, confidence);\r
\r
  float maxValue = (fullAccumulate ? 1. : maxBlend) * keepData; // keepData is a flag that is either 1 or 0 when we call reset()\r
  // maxValue *= 0.;\r
\r
#if inputType != DIFFUSE\r
  const float roughnessMaximum = 0.1;\r
\r
  if (doReprojectSpecular && roughness >= 0.0 && roughness < roughnessMaximum) {\r
    float maxRoughnessValue = mix(0., maxValue, roughness / roughnessMaximum);\r
    maxValue = mix(maxValue, maxRoughnessValue, min(100. * moveFactor, 1.));\r
  }\r
#endif\r
\r
  float temporalReprojectMix = min(accumBlend, maxValue);\r
\r
  // calculate the alpha from temporalReprojectMix\r
  acc.a = 1. / (1. - temporalReprojectMix) - 1.;\r
  acc.a = min(65536., acc.a);\r
  // acc.a = 10.;\r
\r
  outputColor.rgb = mix(inp.rgb, acc.rgb, temporalReprojectMix);\r
  outputColor.a = acc.a;\r
  undoColorTransform(outputColor.rgb);\r
\r
  // outputColor.rgb = vec3(confidence);\r
  // if (length(fwidth(worldNormal)) < 0.02) {\r
  //   outputColor.rgb = vec3(0., 1., 0.);\r
  // }\r
}\r
\r
// this function reprojects the input texture to the current frame\r
// it calculates a confidence value for the reprojection by which the input texture is blended with the accumulated texture\r
void reproject(inout vec4 inp, inout vec4 acc, sampler2D accumulatedTexture, inout bool wasSampled, bool doNeighborhoodClamp,\r
               bool doReprojectSpecular) {\r
  // Get the reprojected UV coordinate.\r
  vec3 uvc = doReprojectSpecular ? reprojectedUvSpecular : reprojectedUvDiffuse;\r
  vec2 uv = uvc.xy;\r
\r
  // Sample the accumulated texture.\r
  acc = sampleReprojectedTexture(accumulatedTexture, uv);\r
  transformColor(acc.rgb);\r
\r
  // If we haven't sampled before, simply use the sample.\r
  if (!wasSampled) {\r
    inp.rgb = acc.rgb;\r
    return;\r
  }\r
\r
  // Add one more frame.\r
  acc.a++;\r
\r
  // Apply neighborhood clamping, if enabled.\r
  vec3 clampedColor = acc.rgb;\r
\r
  int clampRadius = doReprojectSpecular && roughness < 0.25 ? 1 : 2;\r
  clampNeighborhood(inputTexture, clampedColor, inp.rgb, clampRadius, doReprojectSpecular);\r
  float r = doReprojectSpecular ? roughness : 1.0;\r
\r
  float clampAggressiveness = min(1., uvc.z * r);\r
\r
  float clampIntensity = mix(0., min(1., moveFactor * 50. + neighborhoodClampIntensity), clampAggressiveness);\r
\r
  vec3 newColor = mix(acc.rgb, clampedColor, clampIntensity);\r
\r
  // check how much the color has changed\r
  float colorDiff = min(length(newColor - acc.rgb), 1.);\r
  // moveFactor = colorDiff;\r
\r
  acc.a *= 1. - colorDiff;\r
\r
  acc.rgb = newColor;\r
}\r
\r
void preprocessInput(inout highp vec4 texel, inout bool sampledThisFrame) {\r
  sampledThisFrame = texel.r >= 0.;\r
  texel.rgb = max(texel.rgb, vec3(0.));\r
  transformColor(texel.rgb);\r
}\r
\r
void getTexels(inout highp vec4 inputTexel[textureCount], inout bool sampledThisFrame[textureCount]) {\r
#if inputType == DIFFUSE_SPECULAR\r
  // not defining the sampled texture as a variable but passing it to the function directly causes platform-specific errors\r
  // on Samsung Galaxy S21, for example the textures seems to be sampled as HalfFloats instead of Floats (it gives the same errorneous results as\r
  // packing the 2 textures in a single HalfFloat RGBA texture). This is probably a bug in the driver. The diffuse texture appears blueish in that\r
  // case\r
  highp vec4 tex = textureLod(inputTexture, vUv, 0.);\r
  unpackTwoVec4(tex, inputTexel[0], inputTexel[1]);\r
\r
  preprocessInput(inputTexel[0], sampledThisFrame[0]);\r
  preprocessInput(inputTexel[1], sampledThisFrame[1]);\r
#else\r
  inputTexel[0] = textureLod(inputTexture, vUv, 0.0);\r
  preprocessInput(inputTexel[0], sampledThisFrame[0]);\r
#endif\r
}\r
\r
void computeGVariables(vec2 dilatedUv, float depth) {\r
  worldPos = screenSpaceToWorldSpace(dilatedUv, depth, cameraMatrixWorld, projectionMatrixInverse);\r
  vec3 viewPos = (viewMatrix * vec4(worldPos, 1.0)).xyz;\r
  viewDir = normalize(viewPos);\r
  vec3 viewNormal = (vec4(worldNormal, 0.0) * viewMatrix).xyz;\r
  viewAngle = dot(-viewDir, viewNormal);\r
}\r
\r
void computeReprojectedUv(float depth, vec3 worldPos, vec3 worldNormal) {\r
  reprojectedUvDiffuse = getReprojectedUV(false, depth, worldPos, worldNormal);\r
\r
#if inputType == DIFFUSE_SPECULAR || inputType == SPECULAR\r
  reprojectedUvSpecular = getReprojectedUV(true, depth, worldPos, worldNormal);\r
\r
  if (reprojectedUvSpecular.x == -1.0) {\r
    reprojectedUvSpecular = reprojectedUvDiffuse;\r
  }\r
#endif\r
}\r
\r
void getRoughnessRayLength(inout highp vec4 inputTexel[textureCount]) {\r
#if inputType == DIFFUSE_SPECULAR\r
  rayLength = inputTexel[1].a;\r
  roughness = clamp(inputTexel[0].a, 0., 1.);\r
#elif inputType == SPECULAR\r
  vec2 data = unpackHalf2x16(floatBitsToUint(inputTexel[0].a));\r
  rayLength = data.r;\r
  roughness = clamp(data.g, 0., 1.);\r
#endif\r
}\r
\r
void main() {\r
  vec2 dilatedUv = vUv;\r
  getVelocityNormalDepth(dilatedUv, velocity, worldNormal, depth);\r
\r
  highp vec4 inputTexel[textureCount], accumulatedTexel[textureCount];\r
  bool textureSampledThisFrame[textureCount];\r
\r
  getTexels(inputTexel, textureSampledThisFrame);\r
\r
// ! todo: find better solution\r
#if inputType != DIFFUSE\r
  if (depth == 1.0 && fwidth(depth) == 0.0) {\r
    discard;\r
    return;\r
  }\r
#endif\r
\r
  curvature = getCurvature(worldNormal);\r
\r
  computeGVariables(dilatedUv, depth);\r
  getRoughnessRayLength(inputTexel);\r
  computeReprojectedUv(depth, worldPos, worldNormal);\r
\r
  moveFactor = min(dot(velocity, velocity) * 10000., 1.);\r
\r
#pragma unroll_loop_start\r
  for (int i = 0; i < textureCount; i++) {\r
    reproject(inputTexel[i], accumulatedTexel[i], accumulatedTexture[i], textureSampledThisFrame[i], neighborhoodClamp[i], reprojectSpecular[i]);\r
    accumulate(gOutput[i], inputTexel[i], accumulatedTexel[i], roughness, moveFactor, reprojectSpecular[i]);\r
  }\r
#pragma unroll_loop_end\r
}`, Nl = `// #define VISUALIZE_DISOCCLUSIONS\r
\r
vec2 dilatedUv, velocity;\r
vec3 worldNormal, worldPos, viewDir;\r
float depth, curvature, viewAngle, rayLength, angleMix;\r
float roughness = 1.;\r
float moveFactor = 0.;\r
\r
#define luminance(a) dot(vec3(0.2125, 0.7154, 0.0721), a)\r
\r
// source:\r
// https://github.com/mrdoob/three.js/blob/342946c8392639028da439b6dc0597e58209c696/examples/js/shaders/SAOShader.js#L123\r
float getViewZ(const in float depth) {\r
#ifdef PERSPECTIVE_CAMERA\r
  return perspectiveDepthToViewZ(depth, cameraNear, cameraFar);\r
#else\r
  return orthographicDepthToViewZ(depth, cameraNear, cameraFar);\r
#endif\r
}\r
\r
vec3 screenSpaceToWorldSpace(const vec2 uv, const float depth, mat4 curMatrixWorld, const mat4 projMatrixInverse) {\r
  vec4 ndc = vec4((uv.x - 0.5) * 2.0, (uv.y - 0.5) * 2.0, (depth - 0.5) * 2.0, 1.0);\r
\r
  vec4 clip = projMatrixInverse * ndc;\r
  vec4 view = curMatrixWorld * (clip / clip.w);\r
\r
  return view.xyz;\r
}\r
\r
vec2 viewSpaceToScreenSpace(const vec3 position, const mat4 projMatrix) {\r
  vec4 projectedCoord = projMatrix * vec4(position, 1.0);\r
  projectedCoord.xy /= projectedCoord.w;\r
  // [-1, 1] --> [0, 1] (NDC to screen position)\r
  projectedCoord.xy = projectedCoord.xy * 0.5 + 0.5;\r
\r
  return projectedCoord.xy;\r
}\r
\r
#ifdef logTransform\r
// idea from:\r
// https://www.elopezr.com/temporal-aa-and-the-quest-for-the-holy-trail/\r
void transformColor(inout vec3 color) { color = log(color + 1.); }\r
void undoColorTransform(inout vec3 color) { color = exp(color) - 1.; }\r
#else\r
#define transformColor\r
#define undoColorTransform\r
#endif\r
\r
// Extract the minimum and maximum color values from the neighborhood of the current pixel.\r
// The neighborhood is defined by the clampRadius.\r
// The neighborhood is in a square centered around the current pixel.\r
// The neighborhood does not include the current pixel.\r
void getNeighborhoodAABB(const sampler2D tex, const int clampRadius, inout vec3 minNeighborColor, inout vec3 maxNeighborColor,\r
                         const bool isSpecular) {\r
\r
  // For each pixel in the neighborhood of the current pixel...\r
  for (int x = -clampRadius; x <= clampRadius; x++) {\r
    for (int y = -clampRadius; y <= clampRadius; y++) {\r
      // Calculate the offset of the current pixel relative to the current pixel.\r
      vec2 offset = vec2(x, y) * invTexSize;\r
      // Calculate the texture coordinate of the current pixel.\r
      vec2 neighborUv = vUv + offset;\r
\r
      // Get the current pixel's color.\r
#if inputType == DIFFUSE_SPECULAR\r
      vec4 t1, t2;\r
      vec4 packedNeighborTexel = textureLod(inputTexture, neighborUv, 0.0);\r
      unpackTwoVec4(packedNeighborTexel, t1, t2);\r
      vec4 neighborTexel = isSpecular ? t2 : t1;\r
#else\r
      vec4 neighborTexel = textureLod(inputTexture, neighborUv, 0.0);\r
#endif\r
\r
      if (neighborTexel.r >= 0.) {\r
        // Update the minimum and maximum color values for the current pixel's neighborhood.\r
        minNeighborColor = min(neighborTexel.rgb, minNeighborColor);\r
        maxNeighborColor = max(neighborTexel.rgb, maxNeighborColor);\r
      }\r
    }\r
  }\r
}\r
\r
void clampNeighborhood(const sampler2D tex, inout vec3 color, vec3 inputColor, const int clampRadius, const bool isSpecular) {\r
  undoColorTransform(inputColor);\r
  vec3 minNeighborColor = inputColor;\r
  vec3 maxNeighborColor = inputColor;\r
\r
  // Find the AABB of the neighborhood.\r
  getNeighborhoodAABB(tex, clampRadius, minNeighborColor, maxNeighborColor, isSpecular);\r
\r
  transformColor(minNeighborColor);\r
  transformColor(maxNeighborColor);\r
\r
  color = clamp(color, minNeighborColor, maxNeighborColor);\r
}\r
\r
void getVelocityNormalDepth(inout vec2 dilatedUv, out vec2 vel, out vec3 normal, out float depth) {\r
  vec2 centerUv = dilatedUv;\r
\r
  vec4 velocityTexel = textureLod(velocityTexture, centerUv, 0.0);\r
\r
  vel = velocityTexel.rg;\r
  normal = unpackNormal(velocityTexel.b);\r
  depth = velocityTexel.a;\r
}\r
\r
#define PLANE_DISTANCE 20.\r
#define WORLD_DISTANCE 10.\r
#define NORMAL_DISTANCE 1.\r
\r
float planeDistanceDisocclusionCheck(const vec3 worldPos, const vec3 lastWorldPos, const vec3 worldNormal, const float distFactor) {\r
  // Calculate distance to plane\r
  vec3 toCurrent = worldPos - lastWorldPos;\r
  float distToPlane = abs(dot(toCurrent, worldNormal));\r
\r
  // Return the distance to the plane scaled by the distFactor\r
  return distToPlane / PLANE_DISTANCE * distFactor;\r
}\r
\r
float worldDistanceDisocclusionCheck(const vec3 worldPos, const vec3 lastWorldPos, const float distFactor) {\r
  // Calculate the distance between the current and last world position\r
  return length(worldPos - lastWorldPos) / WORLD_DISTANCE * distFactor;\r
}\r
\r
float normalDisocclusionCheck(const vec3 worldNormal, const vec3 lastWorldNormal, const float distFactor) {\r
  // Calculate the distance between the current and last world position\r
  return min(1. - dot(worldNormal, lastWorldNormal), 1.) / NORMAL_DISTANCE * distFactor;\r
}\r
\r
float validateReprojectedUV(const vec2 reprojectedUv, const vec3 worldPos, const vec3 worldNormal, const bool isHitPoint) {\r
  if (reprojectedUv.x > 1.0 || reprojectedUv.x < 0.0 || reprojectedUv.y > 1.0 || reprojectedUv.y < 0.0)\r
    return 0.;\r
\r
  vec2 dilatedReprojectedUv = reprojectedUv;\r
  vec2 lastVelocity = vec2(0.0);\r
  vec3 lastWorldNormal = vec3(0.0);\r
  float lastDepth = 0.0;\r
\r
  getVelocityNormalDepth(dilatedReprojectedUv, lastVelocity, lastWorldNormal, lastDepth);\r
  vec3 lastWorldPos = screenSpaceToWorldSpace(dilatedReprojectedUv, lastDepth, prevCameraMatrixWorld, prevProjectionMatrixInverse);\r
\r
  vec3 lastViewPos = (prevViewMatrix * vec4(lastWorldPos, 1.0)).xyz;\r
\r
  vec3 lastViewDir = normalize(lastViewPos);\r
  vec3 lastViewNormal = (vec4(lastWorldNormal, 0.0) * prevViewMatrix).xyz;\r
\r
  // get the angle between the view direction and the normal\r
  float lastViewAngle = dot(-lastViewDir, lastViewNormal);\r
  angleMix = abs(lastViewAngle - viewAngle);\r
\r
  float viewZ = abs(getViewZ(depth));\r
  float distFactor = 1. + 1. / (viewZ + 1.0);\r
\r
  float disoccl = 0.;\r
\r
  disoccl += worldDistanceDisocclusionCheck(worldPos, lastWorldPos, distFactor);\r
  disoccl += planeDistanceDisocclusionCheck(worldPos, lastWorldPos, worldNormal, distFactor);\r
  disoccl += normalDisocclusionCheck(worldNormal, lastWorldNormal, distFactor);\r
\r
  float confidence = 1. - min(disoccl, 1.);\r
  // confidence *= 1. - angleMix * 0.25;\r
  confidence = max(confidence, 0.);\r
\r
  confidence = pow(confidence, confidencePower);\r
\r
  return confidence;\r
}\r
\r
vec2 reprojectHitPoint(const vec3 rayOrig, const float rayLength) {\r
  if (curvature > 0.05 || rayLength < 0.01) {\r
    return vec2(-1.);\r
  }\r
\r
  // todo: don't use hit point reprojection if the surface is too curvy\r
\r
  // Find the direction of the ray\r
  vec3 cameraRay = normalize(rayOrig - cameraPos);\r
\r
  // Find the position of the hit point\r
  vec3 parallaxHitPoint = cameraPos + cameraRay * rayLength;\r
\r
  // Reproject the hit point into the previous frame\r
  vec4 reprojectedHitPoint = prevProjectionMatrix * prevViewMatrix * vec4(parallaxHitPoint, 1.0);\r
\r
  // Convert to screen space coordinates\r
  reprojectedHitPoint.xyz /= reprojectedHitPoint.w;\r
  reprojectedHitPoint.xy = reprojectedHitPoint.xy * 0.5 + 0.5;\r
\r
  vec2 diffuseUv = vUv - velocity.xy;\r
  float m = min(max(0., roughness - 0.25) / 0.25, 1.);\r
\r
  return reprojectedHitPoint.xy;\r
}\r
\r
vec3 getReprojectedUV(const bool doReprojectSpecular, const float depth, const vec3 worldPos, const vec3 worldNormal) {\r
  if (doReprojectSpecular) {\r
    // hit point reprojection\r
    vec2 reprojectedUv = reprojectHitPoint(worldPos, rayLength);\r
\r
    float confidence = validateReprojectedUV(reprojectedUv, worldPos, worldNormal, true);\r
    return vec3(reprojectedUv, confidence);\r
  } else {\r
    // reprojection using motion vectors\r
    vec2 reprojectedUv = vUv - velocity;\r
\r
    float confidence = validateReprojectedUV(reprojectedUv, worldPos, worldNormal, false);\r
    return vec3(reprojectedUv, confidence);\r
  }\r
}\r
\r
// source: https://www.shadertoy.com/view/styXDh\r
vec4 BiCubicCatmullRom5Tap(sampler2D tex, vec2 P) {\r
  vec2 Weight[3];\r
  vec2 Sample[3];\r
\r
  vec2 UV = P / invTexSize;\r
  vec2 tc = floor(UV - 0.5) + 0.5;\r
  vec2 f = UV - tc;\r
  vec2 f2 = f * f;\r
  vec2 f3 = f2 * f;\r
\r
  vec2 w0 = f2 - 0.5 * (f3 + f);\r
  vec2 w1 = 1.5 * f3 - 2.5 * f2 + vec2(1.);\r
  vec2 w3 = 0.5 * (f3 - f2);\r
  vec2 w2 = vec2(1.) - w0 - w1 - w3;\r
\r
  Weight[0] = w0;\r
  Weight[1] = w1 + w2;\r
  Weight[2] = w3;\r
\r
  Sample[0] = tc - vec2(1.);\r
  Sample[1] = tc + w2 / Weight[1];\r
  Sample[2] = tc + vec2(2.);\r
\r
  Sample[0] *= invTexSize;\r
  Sample[1] *= invTexSize;\r
  Sample[2] *= invTexSize;\r
\r
  float sampleWeight[5];\r
  sampleWeight[0] = Weight[1].x * Weight[0].y;\r
  sampleWeight[1] = Weight[0].x * Weight[1].y;\r
  sampleWeight[2] = Weight[1].x * Weight[1].y;\r
  sampleWeight[3] = Weight[2].x * Weight[1].y;\r
  sampleWeight[4] = Weight[1].x * Weight[2].y;\r
\r
  vec4 Ct = textureLod(tex, vec2(Sample[1].x, Sample[0].y), 0.) * sampleWeight[0];\r
  vec4 Cl = textureLod(tex, vec2(Sample[0].x, Sample[1].y), 0.) * sampleWeight[1];\r
  vec4 Cc = textureLod(tex, vec2(Sample[1].x, Sample[1].y), 0.) * sampleWeight[2];\r
  vec4 Cr = textureLod(tex, vec2(Sample[2].x, Sample[1].y), 0.) * sampleWeight[3];\r
  vec4 Cb = textureLod(tex, vec2(Sample[1].x, Sample[2].y), 0.) * sampleWeight[4];\r
\r
  float WeightMultiplier = 1. / (sampleWeight[0] + sampleWeight[1] + sampleWeight[2] + sampleWeight[3] + sampleWeight[4]);\r
\r
  return max((Ct + Cl + Cc + Cr + Cb) * WeightMultiplier, vec4(0.));\r
}\r
\r
vec4 sampleReprojectedTexture(const sampler2D tex, const vec2 reprojectedUv) {\r
  // this causes severe reprojection artifacts on iPhone devices\r
  // todo: figure out why and find different solution\r
  vec4 catmull = BiCubicCatmullRom5Tap(tex, reprojectedUv);\r
  // vec4 bilinear = max(textureLod(tex, reprojectedUv, 0.0), vec4(0.));\r
  return catmull;\r
}\r
\r
float getCurvature(vec3 n) {\r
  float curvature = length(fwidth(n));\r
\r
  return curvature;\r
}`, zl = (c) => {
  const { width: e, height: t } = c.image;
  return Math.floor(Math.log2(Math.max(e, t))) + 1;
}, Gi = () => {
  if (ue.envmap_physical_pars_fragment.includes("iblRadianceDisabled") || (ue.envmap_physical_pars_fragment = ue.envmap_physical_pars_fragment.replace(
    "vec3 getIBLRadiance( const in vec3 viewDir, const in vec3 normal, const in float roughness ) {",
    /* glsl */
    `
		uniform bool iblRadianceDisabled;
	
		vec3 getIBLRadiance( const in vec3 viewDir, const in vec3 normal, const in float roughness ) {
		 if(iblRadianceDisabled) return vec3(0.);
		`
  )), "iblRadianceDisabled" in He.physical.uniforms)
    return He.physical.uniforms.iblRadianceDisabled;
  const c = {
    value: !1
  };
  He.physical.uniforms.iblRadianceDisabled = c;
  const { clone: e } = oe;
  return oe.clone = (t) => {
    const r = e(t);
    return "iblRadianceDisabled" in t && (r.iblRadianceDisabled = c), r;
  }, c;
}, Fl = /#pragma unroll_loop_start\s+for\s*\(\s*int\s+i\s*=\s*(\d+)\s*;\s*i\s*<\s*(\d+)\s*;\s*i\s*\+\+\s*\)\s*{([\s\S]+?)}\s+#pragma unroll_loop_end/g;
function rn(c) {
  return c.replace(Fl, Il);
}
function Il(c, e, t, r) {
  let n = "";
  for (let i = parseInt(e); i < parseInt(t); i++)
    n += r.replace(/\[\s*i\s*\]/g, "[ " + i + " ]").replace(/UNROLLED_LOOP_INDEX/g, i);
  return n;
}
let Bl = class extends N {
  constructor(e = 1) {
    let t = Rl.replace("#include <reproject>", Nl).replace("#include <gbuffer_packing>", ot), r = "";
    for (let s = 0; s < e; s++)
      r += /* glsl */
      `
				uniform sampler2D accumulatedTexture${s};

				layout(location = ${s}) out vec4 gOutput${s};
			`;
    t = r + t.replaceAll("textureCount", e), t = rn(t);
    const n = t.matchAll(/accumulatedTexture\[\s*[0-9]+\s*]/g);
    for (const [s] of n) {
      const o = s.replace(/[^0-9]/g, "");
      t = t.replace(s, "accumulatedTexture" + o);
    }
    const i = t.matchAll(/gOutput\[\s*[0-9]+\s*]/g);
    for (const [s] of i) {
      const o = s.replace(/[^0-9]/g, "");
      t = t.replace(s, "gOutput" + o);
    }
    super({
      type: "TemporalReprojectMaterial",
      uniforms: {
        inputTexture: new a(null),
        velocityTexture: new a(null),
        depthTexture: new a(null),
        lastVelocityTexture: new a(null),
        neighborhoodClampIntensity: new a(0),
        fullAccumulate: new a(!1),
        keepData: new a(1),
        delta: new a(0),
        invTexSize: new a(new g()),
        projectionMatrix: new a(new C()),
        projectionMatrixInverse: new a(new C()),
        cameraMatrixWorld: new a(new C()),
        viewMatrix: new a(new C()),
        prevViewMatrix: new a(new C()),
        prevCameraMatrixWorld: new a(new C()),
        prevProjectionMatrix: new a(new C()),
        prevProjectionMatrixInverse: new a(new C()),
        cameraPos: new a(new T()),
        prevCameraPos: new a(new T()),
        cameraNear: new a(0),
        cameraFar: new a(0),
        maxBlend: new a(0)
      },
      vertexShader: We,
      fragmentShader: t,
      blending: B,
      depthWrite: !1,
      depthTest: !1,
      toneMapped: !1,
      glslVersion: Te
    });
    for (let s = 0; s < e; s++)
      this.uniforms["inputTexture" + s] = new a(null), this.uniforms["accumulatedTexture" + s] = new a(null);
  }
};
const Ut = {
  dilation: !1,
  fullAccumulate: !1,
  neighborhoodClamp: !1,
  neighborhoodClampRadius: 1,
  neighborhoodClampIntensity: 1,
  maxBlend: 1,
  logTransform: !1,
  depthDistance: 2,
  worldDistance: 4,
  reprojectSpecular: !1,
  renderTarget: null,
  copyTextures: !0,
  confidencePower: 0.75,
  inputType: "diffuse"
}, zn = new C(), Fn = new C(), Ll = new g();
let nn = class extends X {
  needsSwap = !1;
  overrideAccumulatedTextures = [];
  clock = new wi();
  r2Sequence = [];
  frame = 0;
  lastCameraTransform = {
    position: new T(),
    quaternion: new Yr()
  };
  constructor(e, t, r, n, i, s = Ut) {
    super("TemporalReprojectPass"), this._scene = e, this._camera = t, this.textureCount = i, s = { ...Ut, ...s }, this.renderTarget = new be(1, 1, i, {
      minFilter: b,
      magFilter: b,
      type: n.type,
      depthBuffer: !1
    }), this.renderTarget.texture.forEach(
      (o, l) => o.name = "TemporalReprojectPass.accumulatedTexture" + l
    ), this.fullscreenMaterial = new Bl(i), this.fullscreenMaterial.defines.textureCount = i, s.dilation && (this.fullscreenMaterial.defines.dilation = ""), s.neighborhoodClamp && (this.fullscreenMaterial.defines.neighborhoodClamp = ""), s.logTransform && (this.fullscreenMaterial.defines.logTransform = ""), t.isPerspectiveCamera && (this.fullscreenMaterial.defines.PERSPECTIVE_CAMERA = ""), this.fullscreenMaterial.defines.neighborhoodClampRadius = parseInt(s.neighborhoodClampRadius), this.fullscreenMaterial.defines.depthDistance = s.depthDistance.toPrecision(5), this.fullscreenMaterial.defines.worldDistance = s.worldDistance.toPrecision(5), this.fullscreenMaterial.uniforms.fullAccumulate.value = s.fullAccumulate, this.fullscreenMaterial.uniforms.neighborhoodClampIntensity.value = s.neighborhoodClampIntensity, this.fullscreenMaterial.uniforms.maxBlend.value = s.maxBlend, this.fullscreenMaterial.uniforms.projectionMatrix.value = t.projectionMatrix.clone(), this.fullscreenMaterial.uniforms.projectionMatrixInverse.value = t.projectionMatrixInverse.clone(), this.fullscreenMaterial.uniforms.cameraMatrixWorld.value = t.matrixWorld, this.fullscreenMaterial.uniforms.viewMatrix.value = t.matrixWorldInverse, this.fullscreenMaterial.uniforms.cameraPos.value = t.position, this.fullscreenMaterial.uniforms.prevViewMatrix.value = t.matrixWorldInverse.clone(), this.fullscreenMaterial.uniforms.prevCameraMatrixWorld.value = t.matrixWorld.clone(), this.fullscreenMaterial.uniforms.prevProjectionMatrix.value = t.projectionMatrix.clone(), this.fullscreenMaterial.uniforms.prevProjectionMatrixInverse.value = t.projectionMatrixInverse.clone(), this.fullscreenMaterial.uniforms.velocityTexture.value = r.renderTarget.texture, this.fullscreenMaterial.uniforms.depthTexture.value = r.depthTexture, this.fullscreenMaterial.defines.inputType = ["diffuseSpecular", "diffuse", "specular"].indexOf(s.inputType) ?? 1;
    for (const o of ["reprojectSpecular", "neighborhoodClamp"]) {
      let l = s[o];
      typeof l != "array" && (l = Array(i).fill(l)), this.fullscreenMaterial.defines[o] = /* glsl */
      `bool[](${l.join(", ")})`;
    }
    this.fullscreenMaterial.defines.confidencePower = s.confidencePower.toPrecision(5), this.options = s, this.velocityDepthNormalPass = r, this.fullscreenMaterial.uniforms.inputTexture.value = n;
  }
  dispose() {
    super.dispose(), this.renderTarget.dispose(), this.fullscreenMaterial.dispose();
  }
  setSize(e, t) {
    this.renderTarget.setSize(e, t), this.fullscreenMaterial.uniforms.invTexSize.value.set(1 / e, 1 / t), this.framebufferTexture?.dispose();
    const r = this.fullscreenMaterial.uniforms.inputTexture.value;
    this.framebufferTexture = new Zr(e, t, r.format), this.framebufferTexture.type = r.type, this.framebufferTexture.minFilter = F, this.framebufferTexture.magFilter = F, this.framebufferTexture.needsUpdate = !0;
    for (let n = 0; n < this.textureCount; n++) {
      const i = this.overrideAccumulatedTextures[n] ?? this.framebufferTexture;
      this.fullscreenMaterial.uniforms["accumulatedTexture" + n].value = i;
    }
  }
  get texture() {
    return this.renderTarget.texture[0];
  }
  reset() {
    this.fullscreenMaterial.uniforms.keepData.value = 0;
  }
  render(e) {
    this.frame = (this.frame + 1) % 4096;
    const t = Math.min(1 / 10, this.clock.getDelta());
    this.fullscreenMaterial.uniforms.delta.value = t, zn.copy(this._camera.projectionMatrix), Fn.copy(this._camera.projectionMatrixInverse), this._camera.view && (this._camera.view.enabled = !1), this._camera.updateProjectionMatrix(), this.fullscreenMaterial.uniforms.projectionMatrix.value.copy(this._camera.projectionMatrix), this.fullscreenMaterial.uniforms.projectionMatrixInverse.value.copy(this._camera.projectionMatrixInverse), this.fullscreenMaterial.uniforms.lastVelocityTexture.value = this.velocityDepthNormalPass.lastVelocityTexture, this.fullscreenMaterial.uniforms.fullAccumulate.value = this.options.fullAccumulate && !Oi(this._camera, this.lastCameraTransform.position, this.lastCameraTransform.quaternion), this.lastCameraTransform.position.copy(this._camera.position), this.lastCameraTransform.quaternion.copy(this._camera.quaternion), this._camera.view && (this._camera.view.enabled = !0), this._camera.projectionMatrix.copy(zn), this._camera.projectionMatrixInverse.copy(Fn), this.fullscreenMaterial.uniforms.cameraNear.value = this._camera.near, this.fullscreenMaterial.uniforms.cameraFar.value = this._camera.far, e.setRenderTarget(this.renderTarget), e.render(this.scene, this.camera), this.fullscreenMaterial.uniforms.keepData.value = 1, this.overrideAccumulatedTextures.length === 0 && (this.framebufferTexture.needsUpdate = !0, e.copyFramebufferToTexture(Ll, this.framebufferTexture)), this.fullscreenMaterial.uniforms.prevCameraMatrixWorld.value.copy(this._camera.matrixWorld), this.fullscreenMaterial.uniforms.prevViewMatrix.value.copy(this._camera.matrixWorldInverse), this.fullscreenMaterial.uniforms.prevProjectionMatrix.value.copy(
      this.fullscreenMaterial.uniforms.projectionMatrix.value
    ), this.fullscreenMaterial.uniforms.prevProjectionMatrixInverse.value.copy(
      this.fullscreenMaterial.uniforms.projectionMatrixInverse.value
    ), this.fullscreenMaterial.uniforms.prevCameraPos.value.copy(this._camera.position);
  }
  jitter(e = 1) {
    this.unjitter(), El(this.renderTarget.width, this.renderTarget.height, this._camera, this.frame, e);
  }
  unjitter() {
    this._camera.clearViewOffset && this._camera.clearViewOffset();
  }
};
const Ul = (
  /* glsl */
  `
		#ifdef USE_SKINNING
		#ifdef BONE_TEXTURE
			uniform sampler2D prevBoneTexture;
			uniform int boneTextureSize;
			mat4 getPrevBoneMatrix( const in float i ) {
				float j = i * 4.0;
				float x = mod( j, float( boneTextureSize ) );
				float y = floor( j / float( boneTextureSize ) );
				float dx = 1.0 / float( boneTextureSize );
				float dy = 1.0 / float( boneTextureSize );
				y = dy * ( y + 0.5 );
				vec4 v1 = textureLod( prevBoneTexture, vec2( dx * ( x + 0.5 ), y ), 0. );
				vec4 v2 = textureLod( prevBoneTexture, vec2( dx * ( x + 1.5 ), y ), 0. );
				vec4 v3 = textureLod( prevBoneTexture, vec2( dx * ( x + 2.5 ), y ), 0. );
				vec4 v4 = textureLod( prevBoneTexture, vec2( dx * ( x + 3.5 ), y ), 0. );
				mat4 bone = mat4( v1, v2, v3, v4 );
				return bone;
			}
		#else
			uniform mat4 prevBoneMatrices[ MAX_BONES ];
			mat4 getPrevBoneMatrix( const in float i ) {
				mat4 bone = prevBoneMatrices[ int(i) ];
				return bone;
			}
		#endif
		#endif
`
), Ol = (
  /* glsl */
  `
#define MAX_BONES 64
                    
${ue.skinning_pars_vertex}
${Ul}

uniform mat4 velocityMatrix;
uniform mat4 prevVelocityMatrix;
varying vec4 prevPosition;
varying vec4 newPosition;

varying vec2 vHighPrecisionZW;
`
), kl = (
  /* glsl */
  `
// Get the current vertex position
transformed = vec3( position );
${ue.skinning_vertex}
newPosition = velocityMatrix * vec4( transformed, 1.0 );

// Get the previous vertex position
transformed = vec3( position );
${ue.skinbase_vertex.replace(/mat4 /g, "").replace(/getBoneMatrix/g, "getPrevBoneMatrix")}
${ue.skinning_vertex.replace(/vec4 /g, "")}
prevPosition = prevVelocityMatrix * vec4( transformed, 1.0 );

gl_Position = newPosition;

vHighPrecisionZW = gl_Position.zw;
`
), Vl = (
  /* glsl */
  `
varying vec4 prevPosition;
varying vec4 newPosition;

varying vec2 vHighPrecisionZW;
`
), Hl = (
  /* glsl */
  `
vec2 pos0 = (prevPosition.xy / prevPosition.w) * 0.5 + 0.5;
vec2 pos1 = (newPosition.xy / newPosition.w) * 0.5 + 0.5;

vec2 vel = pos1 - pos0;

float fragCoordZ = 0.5 * vHighPrecisionZW[0] / vHighPrecisionZW[1] + 0.5;

gl_FragColor = vec4(vel.x, vel.y, 0., 0.);
`
), Gl = {
  prevVelocityMatrix: { value: new C() },
  velocityMatrix: { value: new C() },
  prevBoneTexture: { value: null },
  boneTexture: { value: null },
  boneTextureSize: { value: 0 },
  normalMap: { value: null },
  normalScale: { value: new g(1, 1) },
  uvTransform: { value: new Ot() }
};
let Wl = class extends N {
  constructor(e) {
    super({
      uniforms: {
        ...oe.clone(Gl),
        cameraMatrixWorld: { value: e.matrixWorld }
      },
      vertexShader: (
        /* glsl */
        `
					#include <common>
					#include <uv_pars_vertex>
					#include <displacementmap_pars_vertex>
					#include <normal_pars_vertex>
					#include <morphtarget_pars_vertex>
					#include <logdepthbuf_pars_vertex>
					#include <clipping_planes_pars_vertex>

					varying vec2 vUv;

					varying vec3 vViewPosition;
					
                    ${Ol}
        
                    void main() {
						vec3 transformed;

						#include <uv_vertex>

						#include <skinbase_vertex>
						#include <beginnormal_vertex>
						#include <skinnormal_vertex>
						#include <defaultnormal_vertex>

						#include <morphnormal_vertex>
						#include <normal_vertex>
						#include <morphtarget_vertex>
						#include <displacementmap_vertex>
						#include <project_vertex>
						#include <logdepthbuf_vertex>
						#include <clipping_planes_vertex>

						${kl}

						vViewPosition = - mvPosition.xyz;

						vUv = uv;

                    }`
      ),
      fragmentShader: (
        /* glsl */
        `
					precision highp float;
					uniform mat4 cameraMatrixWorld;

					varying vec3 vViewPosition;

					${Vl}
					#include <packing>

					#include <uv_pars_fragment>
					#include <normal_pars_fragment>
					#include <normalmap_pars_fragment>

					varying vec2 vUv;

					// source: https://knarkowicz.wordpress.com/2014/04/16/octahedron-normal-vector-encoding/
					vec2 OctWrap( vec2 v ) {
						vec2 w = 1.0 - abs( v.yx );
						if (v.x < 0.0) w.x = -w.x;
						if (v.y < 0.0) w.y = -w.y;
						return w;
					}

					vec2 encodeOctWrap(vec3 n) {
						n /= (abs(n.x) + abs(n.y) + abs(n.z));
						n.xy = n.z > 0.0 ? n.xy : OctWrap(n.xy);
						n.xy = n.xy * 0.5 + 0.5;
						return n.xy;
					}

					float packNormal(vec3 normal) {
						return uintBitsToFloat(packHalf2x16(encodeOctWrap(normal)));
					}

                    void main() {
						#define vNormalMapUv vUv

						#include <normal_fragment_begin>
                    	#include <normal_fragment_maps>

						${Hl}
						vec3 worldNormal = normalize((cameraMatrixWorld * vec4(normal, 0.)).xyz);
						gl_FragColor.b = packNormal(worldNormal);
						gl_FragColor.a = fragCoordZ;
                    }`
      )
    });
  }
};
const jl = [
  "vertexTangent",
  "vertexColors",
  "vertexAlphas",
  "vertexUvs",
  "uvsVertexOnly",
  "supportsVertexTextures",
  "instancing",
  "instancingColor",
  "side",
  "flatShading",
  "skinning",
  "doubleSided",
  "flipSided"
], Xl = (c, e) => {
  for (const t of jl)
    e[t] = c[t];
}, Yl = (c, e, t, r, n) => {
  e[t] !== c[t] && (c[t] = e[t], c.uniforms[t].value = e[t], e[t] ? c.defines[r] = "" : delete c.defines[r], c.needsUpdate = !0);
}, Zl = (c) => {
  const e = [c], t = [];
  for (; e.length !== 0; ) {
    const r = e.shift();
    r.material && t.push(r);
    for (const n of r.children)
      n.visible && e.push(n);
  }
  return t;
}, Kl = new y(0), Ql = new g(), In = new C(), Bn = new C(), Wi = (c) => {
  let e = c.material.uniforms.prevBoneTexture.value;
  if (e && e.image.width === c.skeleton.boneTexture.width)
    e = c.material.uniforms.prevBoneTexture.value, e.image.data.set(c.skeleton.boneTexture.image.data);
  else {
    e?.dispose();
    const t = c.skeleton.boneTexture.image.data.slice(), r = c.skeleton.boneTexture.image.width;
    e = new le(t, r, r, ne, Y), c.material.uniforms.prevBoneTexture.value = e, e.needsUpdate = !0;
  }
}, ql = (c, e) => {
  c.skeleton?.boneTexture && (c.material.uniforms.boneTexture.value = c.skeleton.boneTexture, c.material.uniforms.boneTextureSize.value = c.skeleton.boneTexture.image.width, "USE_SKINNING" in c.material.defines || (c.material.defines.USE_SKINNING = "", c.material.defines.BONE_TEXTURE = "", c.material.needsUpdate = !0)), c.modelViewMatrix.multiplyMatrices(e.matrixWorldInverse, c.matrixWorld), c.material.uniforms.velocityMatrix.value.multiplyMatrices(e.projectionMatrix, c.modelViewMatrix);
}, Jl = (c, e) => {
  c.material.uniforms.prevVelocityMatrix.value.multiplyMatrices(e.projectionMatrix, c.modelViewMatrix), c.skeleton?.boneTexture && Wi(c);
};
let sn = class extends X {
  cachedMaterials = /* @__PURE__ */ new WeakMap();
  visibleMeshes = [];
  needsSwap = !1;
  constructor(e, t) {
    super("VelocityDepthNormalPass"), this._scene = e, this._camera = t, this.renderTarget = new A(1, 1, {
      type: Y,
      minFilter: b,
      magFilter: b
    }), this.renderTarget.texture.name = "VelocityDepthNormalPass.Texture", this.renderTarget.depthTexture = new Oe(1, 1), this.renderTarget.depthTexture.type = Y;
  }
  get texture() {
    return this.renderTarget.texture;
  }
  setVelocityDepthNormalMaterialInScene() {
    this.visibleMeshes = ki(this._scene);
    for (const e of this.visibleMeshes) {
      const t = e.material;
      let [r, n] = this.cachedMaterials.get(e) || [];
      t !== r && (n = new Wl(this._camera), Xl(t, n), e.material = n, e.skeleton?.boneTexture && Wi(e), this.cachedMaterials.set(e, [t, n])), e.material = n, e.visible = _r(e, t), Yl(
        n,
        t,
        "normalMap",
        "USE_NORMALMAP_TANGENTSPACE"
      ), n.uniforms.normalMap.value = t.normalMap;
      const i = t.map || t.normalMap || t.roughnessMap || t.metalnessMap;
      i && (n.uniforms.uvTransform.value = i.matrix), ql(e, this._camera);
    }
  }
  unsetVelocityDepthNormalMaterialInScene() {
    for (const e of this.visibleMeshes) {
      e.visible = !0, Jl(e, this._camera);
      const t = this.cachedMaterials.get(e);
      t && t[0] && (e.material = t[0], e.material.needsUpdate !== void 0 && (e.material.needsUpdate = !0));
    }
    this.visibleMeshes = [];
  }
  setSize(e, t) {
    this.renderTarget.setSize(e, t), this.lastVelocityTexture?.dispose(), this.lastVelocityTexture = new Zr(e, t, ne), this.lastVelocityTexture.type = Y, this.lastVelocityTexture.minFilter = b, this.lastVelocityTexture.magFilter = b;
  }
  dispose() {
    super.dispose(), this.renderTarget.dispose();
  }
  render(e) {
    In.copy(this._camera.projectionMatrix), Bn.copy(this._camera.projectionMatrixInverse), this._camera.view && (this._camera.view.enabled = !1), this._camera.updateProjectionMatrix();
    const t = e.shadowMap.autoUpdate;
    e.shadowMap.autoUpdate = !1, this._camera.updateMatrixWorld(), this.setVelocityDepthNormalMaterialInScene();
    const { background: r } = this._scene;
    this._scene.background = Kl, e.setRenderTarget(this.renderTarget), e.copyFramebufferToTexture(this.lastVelocityTexture, Ql), e.render(this._scene, this._camera), this._scene.background = r, this.unsetVelocityDepthNormalMaterialInScene(), this._camera.view && (this._camera.view.enabled = !0), this._camera.projectionMatrix.copy(In), this._camera.projectionMatrixInverse.copy(Bn), e.shadowMap.autoUpdate = t;
  }
};
const _l = `// source:\r
// https://github.com/mrdoob/three.js/blob/342946c8392639028da439b6dc0597e58209c696/examples/js/shaders/SAOShader.js#L123\r
float getViewZ(const in float depth) {\r
#ifdef PERSPECTIVE_CAMERA\r
  return perspectiveDepthToViewZ(depth, cameraNear, cameraFar);\r
#else\r
  return orthographicDepthToViewZ(depth, cameraNear, cameraFar);\r
#endif\r
}\r
\r
// source:\r
// https://github.com/mrdoob/three.js/blob/dev/examples/js/shaders/SSAOShader.js\r
vec3 getViewPosition(float viewZ) {\r
  float clipW = projectionMatrix[2][3] * viewZ + projectionMatrix[3][3];\r
  vec4 clipPosition = vec4((vec3(vUv, viewZ) - 0.5) * 2.0, 1.0);\r
  clipPosition *= clipW;\r
  vec3 p = (projectionMatrixInverse * clipPosition).xyz;\r
  p.z = -viewZ;\r
  return p;\r
}\r
\r
vec3 F_Schlick(const vec3 f0, const float theta) { return f0 + (1. - f0) * pow(1.0 - theta, 5.); }\r
\r
vec3 SampleGGXVNDF(const vec3 V, const float ax, const float ay, const float r1, const float r2) {\r
  vec3 Vh = normalize(vec3(ax * V.x, ay * V.y, V.z));\r
\r
  float lensq = Vh.x * Vh.x + Vh.y * Vh.y;\r
  vec3 T1 = lensq > 0. ? vec3(-Vh.y, Vh.x, 0.) * inversesqrt(lensq) : vec3(1., 0., 0.);\r
  vec3 T2 = cross(Vh, T1);\r
\r
  float r = sqrt(r1);\r
  float phi = 2.0 * PI * r2;\r
  float t1 = r * cos(phi);\r
  float t2 = r * sin(phi);\r
  float s = 0.5 * (1.0 + Vh.z);\r
  t2 = (1.0 - s) * sqrt(1.0 - t1 * t1) + s * t2;\r
\r
  vec3 Nh = t1 * T1 + t2 * T2 + sqrt(max(0.0, 1.0 - t1 * t1 - t2 * t2)) * Vh;\r
\r
  return normalize(vec3(ax * Nh.x, ay * Nh.y, max(0.0, Nh.z)));\r
}\r
\r
void Onb(const vec3 N, inout vec3 T, inout vec3 B) {\r
  vec3 up = abs(N.z) < 0.9999999 ? vec3(0, 0, 1) : vec3(1, 0, 0);\r
  T = normalize(cross(up, N));\r
  B = cross(N, T);\r
}\r
\r
vec3 ToLocal(const vec3 X, const vec3 Y, const vec3 Z, const vec3 V) { return vec3(dot(V, X), dot(V, Y), dot(V, Z)); }\r
\r
vec3 ToWorld(const vec3 X, const vec3 Y, const vec3 Z, const vec3 V) { return V.x * X + V.y * Y + V.z * Z; }\r
\r
vec3 constructGlobalIllumination(vec3 diffuseGi, vec3 specularGi, vec3 cameraRay, vec3 viewNormal, vec3 diffuse, vec3 emissive, float roughness,\r
                                 float metalness) {\r
  roughness *= roughness;\r
\r
  // convert the viewNormal to world-space\r
  vec3 normal = (vec4(viewNormal, 0.) * viewMatrix).xyz;\r
\r
  vec3 T, B;\r
\r
  vec3 v = -cameraRay; // incoming vector\r
\r
  // convert view dir and view normal to world-space\r
  vec3 V = (vec4(v, 0.) * viewMatrix).xyz; // invert view dir\r
  vec3 N = normal;\r
\r
  Onb(N, T, B);\r
\r
  V = ToLocal(T, B, N, V);\r
\r
  // seems to approximate Fresnel very well\r
  vec3 H = SampleGGXVNDF(V, roughness, roughness, 0.25, 0.25);\r
  if (H.z < 0.0)\r
    H = -H;\r
\r
  vec3 l = normalize(reflect(-V, H));\r
  l = ToWorld(T, B, N, l);\r
\r
  // convert reflected vector back to view-space\r
  l = (vec4(l, 1.) * cameraMatrixWorld).xyz;\r
  l = normalize(l);\r
\r
  if (dot(viewNormal, l) < 0.)\r
    l = -l;\r
\r
  vec3 h = normalize(v + l); // half vector\r
\r
  // try to approximate the fresnel term we get when accumulating over multiple\r
  // frames\r
  float VoH = max(EPSILON, dot(v, h));\r
\r
  // fresnel\r
  vec3 f0 = mix(vec3(0.04), diffuse, metalness);\r
  vec3 F = F_Schlick(f0, VoH);\r
\r
#if inputType != TYPE_SPECULAR\r
  vec3 diffuseComponent = diffuse * (1. - metalness) * (1. - F) * diffuseGi;\r
#else\r
  vec3 diffuseComponent = textureLod(sceneTexture, vUv, 0.).rgb;\r
#endif\r
\r
  vec3 specularComponent = specularGi * F;\r
\r
  vec3 globalIllumination = diffuseComponent + specularComponent + emissive;\r
\r
  return globalIllumination;\r
}`;
class ji extends X {
  constructor(e, t, r, n, i = {}) {
    super("DenoiserComposePass"), this._camera = e, this.renderTarget = new A(1, 1, {
      depthBuffer: !1,
      type: Y,
      minFilter: b,
      magFilter: b
    }), this.renderTarget.texture.name = "DenoiserComposePass.Texture";
    let s, o;
    this.originalTextures = t, this.options = i, this._updateCount = 0, i.inputType === "diffuseSpecular" ? (s = t[0], o = t[1]) : i.inputType === "diffuse" ? s = t[0] : i.inputType === "specular" && (o = t[0]), this.fullscreenMaterial = new N({
      fragmentShader: (
        /* glsl */
        `
            varying vec2 vUv;
            uniform sampler2D sceneTexture;
            uniform highp sampler2D depthTexture;
            uniform sampler2D diffuseGiTexture;
            uniform sampler2D specularGiTexture;
            uniform mat4 cameraMatrixWorld;
            uniform mat4 projectionMatrix;
            uniform mat4 projectionMatrixInverse;
			uniform float cameraNear;
			uniform float cameraFar;

            #include <common>
            #include <packing>

			#define TYPE_DIFFUSE_SPECULAR 0
			#define TYPE_DIFFUSE 1
			#define TYPE_SPECULAR 2

            ${ot}
            ${_l}

            void main() {
                float depth = textureLod(depthTexture, vUv, 0.).r;

				if(depth == 1. && fwidth(depth) == 0.){
					discard;
					return;
				}

				// 防止天空或者无效像素

                Material mat = getMaterial(gBufferTexture, vUv);

                vec3 viewNormal = (vec4(mat.normal, 0.) * cameraMatrixWorld).xyz;

				float viewZ = -getViewZ(depth);

                // view-space position of the current texel
				vec3 viewPos = getViewPosition(viewZ);
                vec3 viewDir = normalize(viewPos);

                // 添加纹理有效性检查
                vec4 diffuseGi = vec4(0.0);
                vec4 specularGi = vec4(0.0);
                
                // 尝试读取纹理，如果纹理无效则使用默认值
                bool hasDiffuse = false;
                bool hasSpecular = false;
                
                #if inputType == TYPE_DIFFUSE_SPECULAR || inputType == TYPE_DIFFUSE
                    diffuseGi = textureLod(diffuseGiTexture, vUv, 0.);
                    hasDiffuse = true;
                #endif
                
                #if inputType == TYPE_DIFFUSE_SPECULAR || inputType == TYPE_SPECULAR
                    specularGi = textureLod(specularGiTexture, vUv, 0.);
                    hasSpecular = true;
                #endif
                
                // 如果纹理读取为NaN或INF，则使用安全的默认值
                if(hasDiffuse && (any(isnan(diffuseGi.rgb)) || any(isinf(diffuseGi.rgb)))) {
                    diffuseGi = vec4(0.0, 0.0, 0.0, 1.0);
                }
                
                if(hasSpecular && (any(isnan(specularGi.rgb)) || any(isinf(specularGi.rgb)))) {
                    specularGi = vec4(0.0, 0.0, 0.0, 1.0);
                }
                
                // 限制输入值范围，防止极端值
                diffuseGi.rgb = clamp(diffuseGi.rgb, vec3(0.0), vec3(10.0));
                specularGi.rgb = clamp(specularGi.rgb, vec3(0.0), vec3(10.0));

                vec3 gi = constructGlobalIllumination(diffuseGi.rgb, specularGi.rgb, viewDir, viewNormal, mat.diffuse.rgb, mat.emissive, mat.roughness, mat.metalness);

				gl_FragColor = vec4(gi, 1.);
            }
            `
      ),
      vertexShader: We,
      uniforms: {
        sceneTexture: { value: null },
        viewMatrix: { value: e.matrixWorldInverse },
        cameraMatrixWorld: { value: e.matrixWorld },
        projectionMatrix: { value: e.projectionMatrix },
        projectionMatrixInverse: { value: e.projectionMatrixInverse },
        cameraNear: { value: e.near },
        cameraFar: { value: e.far },
        gBufferTexture: { value: r },
        depthTexture: { value: n },
        diffuseGiTexture: { value: s },
        specularGiTexture: { value: o }
      },
      defines: {
        inputType: ["diffuseSpecular", "diffuse", "specular"].indexOf(i.inputType) ?? 0
      },
      blending: B,
      depthWrite: !1,
      depthTest: !1,
      toneMapped: !1
    }), e.isPerspectiveCamera && (this.fullscreenMaterial.defines.PERSPECTIVE_CAMERA = "");
  }
  // 更新纹理引用
  updateTextures(e) {
    e && (this.originalTextures = e, this.options.inputType === "diffuseSpecular" ? (this.fullscreenMaterial.uniforms.diffuseGiTexture.value = e[0], this.fullscreenMaterial.uniforms.specularGiTexture.value = e[1]) : this.options.inputType === "diffuse" ? this.fullscreenMaterial.uniforms.diffuseGiTexture.value = e[0] : this.options.inputType === "specular" && (this.fullscreenMaterial.uniforms.specularGiTexture.value = e[0]), this._updateCount++, (this._updateCount === 1 || this._updateCount % 10 === 0) && console.log("DenoiserComposePass: 更新纹理引用 (次数: " + this._updateCount + ")"));
  }
  get texture() {
    return this.renderTarget.texture;
  }
  dispose() {
    this.renderTarget.dispose();
  }
  setSize(e, t) {
    this.renderTarget.setSize(e, t);
  }
  setSceneTexture(e) {
    this.fullscreenMaterial.uniforms.sceneTexture.value = e;
  }
  render(e) {
    this.fullscreenMaterial.uniforms.cameraNear.value = this._camera.near, this.fullscreenMaterial.uniforms.cameraFar.value = this._camera.far, e.setRenderTarget(this.renderTarget), e.render(this.scene, this.camera);
  }
}
const $l = `varying vec2 vUv;\r
\r
uniform sampler2D inputTexture;\r
uniform highp sampler2D depthTexture;\r
uniform sampler2D normalTexture; // optional, in case no gBufferTexture is used\r
uniform mat4 projectionMatrix;\r
uniform mat4 projectionMatrixInverse;\r
uniform mat4 cameraMatrixWorld;\r
uniform float radius;\r
uniform float phi;\r
uniform float lumaPhi;\r
uniform float depthPhi;\r
uniform float normalPhi;\r
uniform float roughnessPhi;\r
uniform float specularPhi;\r
uniform vec2 resolution;\r
\r
layout(location = 0) out vec4 gOutput0;\r
\r
#if textureCount == 2\r
uniform sampler2D inputTexture2;\r
layout(location = 1) out vec4 gOutput1;\r
#endif\r
\r
#include <common>\r
#include <gbuffer_packing>\r
\r
#define luminance(a) pow(dot(vec3(0.2125, 0.7154, 0.0721), a), 0.125)\r
\r
#if textureCount == 1\r
#define inputTexture2 inputTexture\r
#endif\r
\r
Material mat;\r
vec3 normal;\r
float depth;\r
float glossiness;\r
float specularFactor;\r
\r
struct InputTexel {\r
  vec3 rgb;\r
  float a;\r
  float luminance;\r
  float w;\r
  float totalWeight;\r
  bool isSpecular;\r
};\r
\r
void toDenoiseSpace(inout vec3 color) { color = log(color + 1.); }\r
void toLinearSpace(inout vec3 color) { color = exp(color) - 1.; }\r
\r
float getBasicNeighborWeight(inout vec2 neighborUv) {\r
#ifdef GBUFFER_TEXTURE\r
  Material neighborMat = getMaterial(gBufferTexture, neighborUv);\r
  vec3 neighborNormal = neighborMat.normal;\r
  float neighborDepth = textureLod(depthTexture, neighborUv, 0.0).r;\r
#else\r
  vec4 neighborDepthVelocityTexel = textureLod(normalTexture, neighborUv, 0.);\r
  vec3 neighborNormal = unpackNormal(neighborDepthVelocityTexel.b);\r
  float neighborDepth = neighborDepthVelocityTexel.a;\r
#endif\r
  if (neighborDepth == 1.0)\r
    return 0.;\r
  float normalDiff = 1. - max(dot(normal, neighborNormal), 0.);\r
  float depthDiff = 10000. * abs(depth - neighborDepth);\r
\r
#ifdef GBUFFER_TEXTURE\r
  float roughnessDiff = abs(mat.roughness - neighborMat.roughness);\r
\r
  float wBasic = exp(-normalDiff * normalPhi - depthDiff * depthPhi - roughnessDiff * roughnessPhi);\r
#else\r
  float wBasic = exp(-normalDiff * normalPhi - depthDiff * depthPhi);\r
#endif\r
\r
  return wBasic;\r
}\r
\r
vec3 getNormal(Material mat) {\r
#ifdef GBUFFER_TEXTURE\r
  return mat.normal;\r
#else\r
  vec3 depthVelocityTexel = textureLod(normalTexture, vUv, 0.).xyz;\r
  return unpackNormal(depthVelocityTexel.b);\r
#endif\r
}\r
\r
#define SQRT_2 1.41421356237\r
\r
vec2 POISSON[8] = vec2[](vec2(-1.0, 0.0), vec2(0.0, -1.0), vec2(1.0, 0.0), vec2(0.0, 1.0), vec2(-0.25 * SQRT_2, -0.25 * SQRT_2),\r
                         vec2(0.25 * SQRT_2, -0.25 * SQRT_2), vec2(0.25 * SQRT_2, 0.25 * SQRT_2), vec2(-0.25 * SQRT_2, 0.25 * SQRT_2));\r
\r
void outputTexel(inout vec4 outputFrag, InputTexel inp) {\r
  inp.rgb /= inp.totalWeight;\r
\r
  outputFrag.rgb = inp.rgb;\r
  toLinearSpace(outputFrag.rgb);\r
  outputFrag.a = inp.a;\r
}\r
\r
void applyWeight(inout InputTexel inp, vec2 neighborUv, float wBasic) {\r
  float w = wBasic;\r
\r
  vec4 t;\r
  if (inp.isSpecular) {\r
    t = textureLod(inputTexture2, neighborUv, 0.);\r
    w *= specularFactor;\r
  } else {\r
    t = textureLod(inputTexture, neighborUv, 0.);\r
  }\r
  toDenoiseSpace(t.rgb);\r
\r
  float disocclW = pow(w, 0.1);\r
  float lumaDiff = abs(inp.luminance - luminance(t.rgb));\r
  lumaDiff = min(lumaDiff, 0.5);\r
  float lumaFactor = exp(-lumaDiff * lumaPhi);\r
  w = mix(w * lumaFactor, disocclW, inp.w) * inp.w;\r
\r
  w *= step(0.0001, w);\r
\r
  inp.rgb += w * t.rgb;\r
  inp.totalWeight += w;\r
}\r
\r
void main() {\r
  depth = textureLod(depthTexture, vUv, 0.).r;\r
\r
  if (depth == 1.0 && fwidth(depth) == 0.) {\r
    discard;\r
    return;\r
  }\r
\r
  InputTexel[textureCount] inputs;\r
\r
  float maxAlpha = 0.;\r
#pragma unroll_loop_start\r
  for (int i = 0; i < textureCount; i++) {\r
    {\r
      vec4 t;\r
\r
      if (isTextureSpecular[i]) {\r
        t = textureLod(inputTexture2, vUv, 0.);\r
      } else {\r
        t = textureLod(inputTexture, vUv, 0.);\r
      }\r
\r
      // check: https://www.desmos.com/calculator/isdut5hmdm for graphs\r
      float age = 1. / pow(t.a + 1., 1.2 * phi);\r
      // age = exp(-pow(log(t.a + 1.), phi));\r
      // float l = log2(t.a + 2.);\r
      // l *= l;\r
      // age = exp(-phi * l);\r
\r
      // the color becomes darker over time possibly due to precision issues, so we brighten it a bit\r
      t.rgb *= 1.0003;\r
\r
      toDenoiseSpace(t.rgb);\r
      InputTexel inp = InputTexel(t.rgb, t.a, luminance(t.rgb), age, 1., isTextureSpecular[i]);\r
      maxAlpha = max(maxAlpha, inp.a);\r
\r
      inputs[i] = inp;\r
    }\r
  }\r
#pragma unroll_loop_end\r
\r
  mat = getMaterial(gBufferTexture, vUv);\r
  normal = getNormal(mat);\r
  glossiness = max(0., 4. * (1. - mat.roughness / 0.25));\r
  specularFactor = exp(-glossiness * specularPhi);\r
\r
  float flatness = 1. - min(length(fwidth(normal)), 1.);\r
  flatness = pow(flatness, 2.) * 0.75 + 0.25;\r
\r
  // float roughnessRadius = mix(sqrt(mat.roughness), 1., 0.5 * (1. - mat.metalness));\r
\r
  vec4 random = blueNoise();\r
  float r = radius;\r
\r
  // rotate the poisson disk\r
  float angle = random.r * 2. * PI;\r
  float s = sin(angle), c = cos(angle);\r
  mat2 rm = r * flatness * mat2(c, -s, s, c);\r
\r
  for (int i = 0; i < 8; i++) {\r
    {\r
      vec2 offset = POISSON[i];\r
\r
      vec2 neighborUv = vUv + rm * (offset / resolution);\r
\r
      float wBasic = getBasicNeighborWeight(neighborUv);\r
\r
      applyWeight(inputs[0], neighborUv, wBasic);\r
\r
#if textureCount == 2\r
      applyWeight(inputs[1], neighborUv, wBasic);\r
#endif\r
    }\r
  }\r
\r
  // inputs[0].rgb = vec3(1.);\r
  // inputs[0].totalWeight = 1.;\r
\r
  outputTexel(gOutput0, inputs[0]);\r
\r
#if textureCount == 2\r
  outputTexel(gOutput1, inputs[1]);\r
#endif\r
}`, ec = $l.replace("#include <gbuffer_packing>", ot), Ne = {
  iterations: 1,
  radius: 3,
  phi: 0.5,
  lumaPhi: 5,
  depthPhi: 2,
  normalPhi: 3.25,
  inputType: "diffuseSpecular"
  // can be "diffuseSpecular", "diffuse" or "specular"
};
class xt extends X {
  iterations = Ne.iterations;
  index = 0;
  constructor(e, t, r = Ne) {
    super("PoissonBlurPass"), r = { ...Ne, ...r }, this.textures = t;
    let n = [!1, !0];
    r.inputType === "diffuse" && (n = [!1, !1]), r.inputType === "specular" && (n = [!0, !0]);
    const i = r.inputType === "diffuseSpecular" ? 2 : 1, s = rn(ec.replaceAll("textureCount", i));
    this.fullscreenMaterial = new N({
      fragmentShader: s,
      vertexShader: We,
      uniforms: {
        depthTexture: { value: null },
        inputTexture: { value: t[0] },
        inputTexture2: { value: t[1] },
        gBufferTexture: { value: null },
        normalTexture: { value: null },
        projectionMatrix: { value: e.projectionMatrix },
        projectionMatrixInverse: { value: e.projectionMatrixInverse },
        cameraMatrixWorld: { value: e.matrixWorld },
        viewMatrix: { value: e.matrixWorldInverse },
        radius: { value: Ne.radius },
        phi: { value: Ne.phi },
        lumaPhi: { value: Ne.lumaPhi },
        depthPhi: { value: Ne.depthPhi },
        normalPhi: { value: Ne.normalPhi },
        roughnessPhi: { value: Ne.roughnessPhi },
        specularPhi: { value: Ne.specularPhi },
        resolution: { value: new g() }
      },
      defines: {
        isTextureSpecular: "bool[2](" + n.join(",") + ")"
      },
      glslVersion: Te
    }), en(this.fullscreenMaterial);
    const o = {
      type: K,
      // using HalfFloatType as FloatType with bilinear filtering isn't supported on some Apple devices
      depthBuffer: !1
    };
    this.renderTargetA = new be(1, 1, i, o), this.renderTargetB = new be(1, 1, i, o), this.renderTargetB.texture[0].name = "PoissonDenoisePass." + (n[0] ? "specular" : "diffuse"), i > 1 && (this.renderTargetB.texture[1].name = "PoissonDenoisePass." + (n[1] ? "specular" : "diffuse"));
    const { uniforms: l } = this.fullscreenMaterial;
    l.depthPhi.value = r.depthPhi, l.normalPhi.value = r.normalPhi;
  }
  setSize(e, t) {
    this.renderTargetA.setSize(e, t), this.renderTargetB.setSize(e, t), this.fullscreenMaterial.uniforms.resolution.value.set(e, t);
  }
  get texture() {
    return this.renderTargetB.texture;
  }
  // can either be a GBufferPass or a VelocityDepthNormalPass
  setGBufferPass(e) {
    e instanceof tn ? (this.fullscreenMaterial.uniforms.gBufferTexture.value = e.texture, this.fullscreenMaterial.defines.GBUFFER_TEXTURE = "") : this.fullscreenMaterial.uniforms.normalTexture.value = e.texture, this.fullscreenMaterial.uniforms.depthTexture.value = e.renderTarget.depthTexture;
  }
  setnNormalTexture(e) {
    this.fullscreenMaterial.uniforms.normalTexture.value = e;
  }
  setDepthTexture(e) {
    this.fullscreenMaterial.uniforms.depthTexture.value = e;
  }
  dispose() {
    super.dispose(), this.renderTargetA.dispose(), this.renderTargetB.dispose(), this.fullscreenMaterial.dispose();
  }
  render(e) {
    for (let t = 0; t < 2 * this.iterations; t++) {
      const r = t % 2 === 0, n = r ? this.renderTargetB : this.renderTargetA;
      this.fullscreenMaterial.uniforms.inputTexture.value = t === 0 ? this.textures[0] : n.texture[0], this.fullscreenMaterial.uniforms.inputTexture2.value = t === 0 ? this.textures[1] : n.texture[1];
      const i = r ? this.renderTargetA : this.renderTargetB;
      e.setRenderTarget(i), e.render(this.scene, this.camera);
    }
  }
}
xt.DefaultOptions = Ne;
const tc = `varying vec2 vUv;\r
\r
uniform sampler2D inputTexture;\r
uniform highp sampler2D depthTexture;\r
uniform sampler2D normalTexture; // optional, in case no gBufferTexture is used\r
uniform mat4 projectionMatrix;\r
uniform mat4 projectionMatrixInverse;\r
uniform mat4 cameraMatrixWorld;\r
uniform float radius;\r
uniform float phi;\r
uniform float lumaPhi;\r
uniform float depthPhi;\r
uniform float normalPhi;\r
uniform float roughnessPhi;\r
uniform float specularPhi;\r
uniform vec2 resolution;\r
uniform float sigmaSpace;  // 高斯空间标准差\r
uniform float sigmaRange;  // 高斯范围标准差\r
\r
layout(location = 0) out vec4 gOutput0;\r
\r
#if textureCount == 2\r
uniform sampler2D inputTexture2;\r
layout(location = 1) out vec4 gOutput1;\r
#endif\r
\r
#include <common>\r
#include <gbuffer_packing>\r
\r
#define luminance(a) dot(vec3(0.2125, 0.7154, 0.0721), a)\r
\r
#if textureCount == 1\r
#define inputTexture2 inputTexture\r
#endif\r
\r
Material mat;\r
vec3 normal;\r
float depth;\r
\r
struct InputTexel {\r
  vec3 rgb;\r
  float a;\r
  float totalWeight;\r
  bool isSpecular;\r
};\r
\r
// 定义高斯核 - 计算像素间空间距离的权重\r
float gaussianWeight(float distanceSquared, float sigma) {\r
  return exp(-distanceSquared / (2.0 * sigma * sigma));\r
}\r
\r
// 计算两个颜色的平方距离\r
float colorDistanceSquared(vec3 a, vec3 b) {\r
  vec3 diff = a - b;\r
  return dot(diff, diff);\r
}\r
\r
// 进行真正的高斯双边滤波\r
void main() {\r
  // 获取深度和法线信息\r
  depth = textureLod(depthTexture, vUv, 0.0).r;\r
  \r
  // 天空不处理\r
  if (depth == 1.0) {\r
    discard;\r
    return;\r
  }\r
\r
  // 获取法线信息\r
  #ifdef GBUFFER_TEXTURE\r
    mat = getMaterial(gBufferTexture, vUv);\r
    normal = mat.normal;\r
  #else\r
    vec3 normalTexel = textureLod(normalTexture, vUv, 0.0).xyz;\r
    normal = unpackNormal(normalTexel.b);\r
  #endif\r
  \r
  // 读取原始输入纹理 (通道1)\r
  vec4 origColor = textureLod(inputTexture, vUv, 0.0);\r
  \r
  // 高斯双边滤波\r
  vec2 texelSize = 1.0 / resolution;\r
  vec3 result = vec3(0.0);\r
  float totalWeight = 0.0;\r
  \r
  // 使用较大的采样半径（但不会太大，减少漏光）\r
  int kernelRadius = int(radius);\r
  \r
  // 限制最大半径为5，防止过度模糊\r
  kernelRadius = min(kernelRadius, 5);\r
  \r
  // 防止数值过小导致的无效滤波\r
  float effectiveSigmaSpace = max(sigmaSpace, 0.5);\r
  float effectiveSigmaRange = max(sigmaRange, 0.01);\r
  \r
  for (int x = -kernelRadius; x <= kernelRadius; x++) {\r
    for (int y = -kernelRadius; y <= kernelRadius; y++) {\r
      vec2 offset = vec2(float(x), float(y));\r
      vec2 uv = vUv + offset * texelSize;\r
      \r
      // 确保在纹理边界内\r
      if (uv.x < 0.0 || uv.x > 1.0 || uv.y < 0.0 || uv.y > 1.0) {\r
        continue;\r
      }\r
      \r
      // 读取相邻像素\r
      vec4 neighborColor = textureLod(inputTexture, uv, 0.0);\r
      float neighborDepth = textureLod(depthTexture, uv, 0.0).r;\r
      \r
      // 获取邻居法线\r
      vec3 neighborNormal;\r
      #ifdef GBUFFER_TEXTURE\r
        Material neighborMat = getMaterial(gBufferTexture, uv);\r
        neighborNormal = neighborMat.normal;\r
      #else\r
        vec3 neighborNormalTexel = textureLod(normalTexture, uv, 0.0).xyz;\r
        neighborNormal = unpackNormal(neighborNormalTexel.b);\r
      #endif\r
      \r
      // 1. 空间权重（高斯）\r
      float distSquared = dot(offset, offset);\r
      float spatialWeight = gaussianWeight(distSquared, effectiveSigmaSpace);\r
      \r
      // 2. 范围权重（颜色差异）\r
      float colorDist = colorDistanceSquared(origColor.rgb, neighborColor.rgb);\r
      float rangeWeight = gaussianWeight(colorDist, effectiveSigmaRange);\r
      \r
      // 3. 深度权重\r
      float depthDiff = abs(depth - neighborDepth);\r
      float depthWeight = depthDiff < 0.1 ? 1.0 : 0.0; // 硬截断，防止跨边界模糊\r
      \r
      // 4. 法线权重\r
      float normalDiff = 1.0 - max(0.0, dot(normal, neighborNormal));\r
      float normalWeight = normalDiff < 0.3 ? 1.0 : 0.0; // 硬截断，保持边缘\r
      \r
      // 合并所有权重\r
      float weight = spatialWeight * rangeWeight * depthWeight * normalWeight;\r
      \r
      // 丢弃过小的权重\r
      if (weight < 0.001) continue;\r
      \r
      result += neighborColor.rgb * weight;\r
      totalWeight += weight;\r
    }\r
  }\r
  \r
  // 归一化\r
  if (totalWeight > 0.0) {\r
    result /= totalWeight;\r
  } else {\r
    // 如果没有有效的样本（可能是孤立的边缘点），保留原始颜色\r
    result = origColor.rgb;\r
  }\r
  \r
  // 锐化处理 - 提高边缘细节\r
  vec3 sharpened = origColor.rgb + (origColor.rgb - result) * 0.3;\r
  \r
  // 混合锐化和模糊结果 - 在平面区域使用模糊，边缘使用锐化\r
  float edgeFactor = length(fwidth(normal)) * 10.0; // 检测边缘\r
  vec3 finalColor = mix(result, sharpened, clamp(edgeFactor, 0.0, 0.5));\r
  \r
  // 对结果进行HDR权重调整，防止过亮区域产生噪点\r
  float lum = luminance(finalColor);\r
  if (lum > 1.0) {\r
    finalColor *= 1.0 / (1.0 + lum * 0.5);\r
  }\r
  \r
  // 输出结果，保留原始alpha\r
  gOutput0 = vec4(finalColor, origColor.a);\r
  \r
  // 处理第二个通道（如果有）- 通常是镜面反射\r
  #if textureCount == 2\r
  // 读取原始输入纹理 (通道2)\r
  vec4 origColor2 = textureLod(inputTexture2, vUv, 0.0);\r
  vec3 result2 = vec3(0.0);\r
  totalWeight = 0.0;\r
  \r
  for (int x = -kernelRadius; x <= kernelRadius; x++) {\r
    for (int y = -kernelRadius; y <= kernelRadius; y++) {\r
      vec2 offset = vec2(float(x), float(y));\r
      vec2 uv = vUv + offset * texelSize;\r
      \r
      // 确保在纹理边界内\r
      if (uv.x < 0.0 || uv.x > 1.0 || uv.y < 0.0 || uv.y > 1.0) {\r
        continue;\r
      }\r
      \r
      // 读取相邻像素\r
      vec4 neighborColor = textureLod(inputTexture2, uv, 0.0);\r
      float neighborDepth = textureLod(depthTexture, uv, 0.0).r;\r
      \r
      // 获取邻居法线\r
      vec3 neighborNormal;\r
      #ifdef GBUFFER_TEXTURE\r
        Material neighborMat = getMaterial(gBufferTexture, uv);\r
        neighborNormal = neighborMat.normal;\r
      #else\r
        vec3 neighborNormalTexel = textureLod(normalTexture, uv, 0.0).xyz;\r
        neighborNormal = unpackNormal(neighborNormalTexel.b);\r
      #endif\r
      \r
      // 1. 空间权重（高斯）\r
      float distSquared = dot(offset, offset);\r
      float spatialWeight = gaussianWeight(distSquared, effectiveSigmaSpace);\r
      \r
      // 2. 范围权重（颜色差异）- 对镜面反射使用更严格的范围权重\r
      float colorDist = colorDistanceSquared(origColor2.rgb, neighborColor.rgb);\r
      float rangeWeight = gaussianWeight(colorDist, effectiveSigmaRange * 0.5); // 更严格的范围\r
      \r
      // 3. 深度权重\r
      float depthDiff = abs(depth - neighborDepth);\r
      float depthWeight = depthDiff < 0.05 ? 1.0 : 0.0; // 更严格的深度限制\r
      \r
      // 4. 法线权重\r
      float normalDiff = 1.0 - max(0.0, dot(normal, neighborNormal));\r
      float normalWeight = normalDiff < 0.2 ? 1.0 : 0.0; // 更严格的法线限制\r
      \r
      // 合并所有权重\r
      float weight = spatialWeight * rangeWeight * depthWeight * normalWeight;\r
      \r
      // 丢弃过小的权重\r
      if (weight < 0.001) continue;\r
      \r
      result2 += neighborColor.rgb * weight;\r
      totalWeight += weight;\r
    }\r
  }\r
  \r
  // 归一化\r
  if (totalWeight > 0.0) {\r
    result2 /= totalWeight;\r
  } else {\r
    // 如果没有有效的样本（可能是孤立的边缘点），保留原始颜色\r
    result2 = origColor2.rgb;\r
  }\r
  \r
  // 镜面反射通常需要保留锐利的边缘，所以使用更强的锐化\r
  vec3 sharpened2 = origColor2.rgb + (origColor2.rgb - result2) * 0.5;\r
  \r
  // 混合锐化和模糊结果 - 在平面区域使用模糊，边缘使用锐化\r
  float edgeFactor2 = length(fwidth(normal)) * 15.0; // 检测边缘，镜面反射需要更敏感\r
  vec3 finalColor2 = mix(result2, sharpened2, clamp(edgeFactor2, 0.0, 0.7));\r
  \r
  // 对结果进行HDR权重调整，防止过亮区域产生噪点\r
  float lum2 = luminance(finalColor2);\r
  if (lum2 > 1.0) {\r
    finalColor2 *= 1.0 / (1.0 + lum2 * 0.7); // 镜面反射需要更强的调整\r
  }\r
  \r
  gOutput1 = vec4(finalColor2, origColor2.a);\r
  #endif\r
} `, rc = tc.replace("#include <gbuffer_packing>", ot), Dr = {
  iterations: 2,
  // 允许更多迭代
  radius: 3,
  // 增加默认半径
  phi: 0.5,
  lumaPhi: 5,
  depthPhi: 2,
  normalPhi: 3.25,
  roughnessPhi: 1,
  sigmaSpace: 2,
  // 更大的空间标准差
  sigmaRange: 0.05,
  // 合理的范围标准差
  inputType: "diffuseSpecular"
  // can be "diffuseSpecular", "diffuse" or "specular"
}, nc = `
varying vec2 vUv;
uniform sampler2D inputTexture;

void main() {
  // 直接拷贝输入纹理到输出
  gl_FragColor = texture2D(inputTexture, vUv);
}
`;
class Ln extends X {
  iterations = Dr.iterations;
  index = 0;
  // 跟踪渲染状态
  _hasInitialRender = !1;
  _lastRenderTime = 0;
  constructor(e, t, r = Dr) {
    super("GaussianBilateralDenoisePass"), r = { ...Dr, ...r }, this.options = r, this.textures = t;
    let n = [!1, !0];
    r.inputType === "diffuse" && (n = [!1, !1]), r.inputType === "specular" && (n = [!0, !0]);
    const i = r.inputType === "diffuseSpecular" ? 2 : 1, s = rn(rc.replaceAll("textureCount", i));
    this.fullscreenMaterial = new N({
      fragmentShader: s,
      vertexShader: We,
      uniforms: {
        depthTexture: { value: null },
        inputTexture: { value: t[0] },
        inputTexture2: { value: t[1] },
        gBufferTexture: { value: null },
        normalTexture: { value: null },
        projectionMatrix: { value: e.projectionMatrix },
        projectionMatrixInverse: { value: e.projectionMatrixInverse },
        cameraMatrixWorld: { value: e.matrixWorld },
        viewMatrix: { value: e.matrixWorldInverse },
        radius: { value: r.radius },
        phi: { value: r.phi },
        lumaPhi: { value: r.lumaPhi },
        depthPhi: { value: r.depthPhi },
        normalPhi: { value: r.normalPhi },
        roughnessPhi: { value: r.roughnessPhi },
        specularPhi: { value: r.specularPhi },
        sigmaSpace: { value: r.sigmaSpace },
        sigmaRange: { value: r.sigmaRange },
        resolution: { value: new g() }
      },
      defines: {
        isTextureSpecular: "bool[2](" + n.join(",") + ")"
      },
      glslVersion: Te
    }), this.copyMaterial = new N({
      fragmentShader: nc,
      vertexShader: We,
      uniforms: {
        inputTexture: { value: null }
      },
      blending: B,
      depthWrite: !1,
      depthTest: !1
    });
    const o = {
      type: K,
      depthBuffer: !1
    };
    this.renderTargetA = new be(1, 1, i, o), this.renderTargetB = new be(1, 1, i, o), this.renderTargetB.texture[0].name = "GaussianBilateralDenoisePass." + (n[0] ? "specular" : "diffuse"), i > 1 && (this.renderTargetB.texture[1].name = "GaussianBilateralDenoisePass." + (n[1] ? "specular" : "diffuse"));
    const { uniforms: l } = this.fullscreenMaterial;
    Object.keys(r).forEach((u) => {
      l[u] && (u === "radius" ? l[u].value = Math.max(1, Math.min(5, r[u])) : u === "sigmaSpace" ? l[u].value = Math.max(0.5, Math.min(5, r[u])) : u === "sigmaRange" ? l[u].value = Math.max(0.01, Math.min(0.2, r[u])) : u.includes("Phi") ? l[u].value = Math.max(1e-3, Math.min(10, r[u])) : l[u].value = r[u]);
    }), this.iterations = Math.max(1, Math.min(r.iterations || 2, 3)), console.log("创建高斯双边滤波器成功，参数:", {
      radius: l.radius.value,
      sigmaSpace: l.sigmaSpace.value,
      sigmaRange: l.sigmaRange.value,
      iterations: this.iterations
    }), this.isCopyMode = !1, this.noiseCounter = 0, this.adaptiveSigmaSpace = l.sigmaSpace.value;
  }
  get texture() {
    return this.isCopyMode ? this.textures : this.renderTargetB.texture;
  }
  // 设置GBuffer
  setGBufferPass(e) {
    e instanceof tn ? (this.fullscreenMaterial.uniforms.gBufferTexture.value = e.texture, this.fullscreenMaterial.defines.GBUFFER_TEXTURE = "") : this.fullscreenMaterial.uniforms.normalTexture.value = e.texture, this.fullscreenMaterial.uniforms.depthTexture.value = e.renderTarget.depthTexture;
  }
  setSize(e, t) {
    this.renderTargetA.setSize(e, t), this.renderTargetB.setSize(e, t), this.fullscreenMaterial.uniforms.resolution.value.set(e, t);
  }
  dispose() {
    this.renderTargetA.dispose(), this.renderTargetB.dispose(), this.fullscreenMaterial.dispose(), this.copyMaterial.dispose();
  }
  render(e) {
    try {
      const t = performance.now(), r = t - this._lastRenderTime;
      this._lastRenderTime = t;
      const n = r < 16;
      if (!this._hasInitialRender) {
        this._hasInitialRender = !0, this.fullscreenMaterial.uniforms.inputTexture.value = this.textures[0], this.textures[1] && (this.fullscreenMaterial.uniforms.inputTexture2.value = this.textures[1]), this.fullscreenMaterial.uniforms.sigmaSpace.value = Math.min(5, this.fullscreenMaterial.uniforms.sigmaSpace.value * 1.5), e.setRenderTarget(this.renderTargetB), e.render(this.scene, this.camera), this.fullscreenMaterial.uniforms.sigmaSpace.value = this.adaptiveSigmaSpace, this.isCopyMode = !1;
        return;
      }
      let i = this.renderTargetB, s = this.renderTargetA;
      this.fullscreenMaterial.uniforms.inputTexture.value = this.textures[0], this.textures[1] && (this.fullscreenMaterial.uniforms.inputTexture2.value = this.textures[1]), e.setRenderTarget(this.renderTargetA), e.render(this.scene, this.camera);
      const o = n ? this.iterations : Math.max(1, this.iterations - 1);
      for (let l = 1; l < o; l++) {
        const u = i;
        i = s, s = u;
        const f = 1 - l / o * 0.5;
        this.fullscreenMaterial.uniforms.sigmaSpace.value = this.adaptiveSigmaSpace * f, this.fullscreenMaterial.uniforms.inputTexture.value = i.texture[0], i.texture.length > 1 && (this.fullscreenMaterial.uniforms.inputTexture2.value = i.texture[1]), e.setRenderTarget(s), e.render(this.scene, this.camera);
      }
      s !== this.renderTargetB && (this.fullscreenMaterial.uniforms.inputTexture.value = s.texture[0], s.texture.length > 1 && (this.fullscreenMaterial.uniforms.inputTexture2.value = s.texture[1]), e.setRenderTarget(this.renderTargetB), e.render(this.scene, this.camera)), this.isCopyMode = !1, this._hasLoggedSuccess || (console.log("高斯双边滤波器渲染成功"), this._hasLoggedSuccess = !0);
    } catch (t) {
      console.error("高斯双边滤波器渲染失败:", t);
      try {
        this.isCopyMode = !0;
        for (let r = 0; r < this.textures.length && r < this.renderTargetB.texture.length; r++)
          this.copyMaterial.uniforms.inputTexture.value = this.textures[r], e.setRenderTarget(this.renderTargetB, r), e.render(this.scene, this.camera);
        console.log("使用简单复制着色器进行备份");
      } catch (r) {
        console.error("复制着色器渲染也失败:", r), this.isCopyMode = !0;
      }
    }
  }
}
const Un = {
  denoiseMode: "full",
  // can be "full" | "full_temporal" | "denoised" | "temporal"
  inputType: "diffuseSpecular",
  // can be "diffuseSpecular" | "diffuse" | "specular"
  gBufferPass: null,
  velocityDepthNormalPass: null,
  denoiseAlgorithm: "poisson",
  // 可以是 "poisson" 或 "gaussian-bilateral"
  // 为高斯双边滤波器提供默认参数
  sigmaSpace: 2,
  sigmaRange: 0.05,
  // 允许双边滤波进行更多迭代
  denoiseIterations: 2
};
class ic {
  constructor(e, t, r, n = Un) {
    n = { ...Un, ...n }, this.options = n, this.velocityDepthNormalPass = n.velocityDepthNormalPass ?? new sn(e, t), this.isOwnVelocityDepthNormalPass = !n.velocityDepthNormalPass;
    const i = n.inputType === "diffuseSpecular" ? 2 : 1;
    this.temporalReprojectPass = new nn(
      e,
      t,
      this.velocityDepthNormalPass,
      r,
      i,
      {
        fullAccumulate: !0,
        logTransform: !0,
        copyTextures: !n.denoise,
        reprojectSpecular: [!1, !0],
        neighborhoodClamp: [!0, !0],
        neighborhoodClampRadius: 2,
        neighborhoodClampIntensity: 0.5,
        ...n
      }
    );
    const s = this.temporalReprojectPass.renderTarget.texture.slice(0, i);
    if (this.options.denoiseMode === "full" || this.options.denoiseMode === "denoised") {
      if (n.denoiseAlgorithm !== "poisson" && n.denoiseAlgorithm !== "gaussian-bilateral" && (console.warn(`未知的降噪算法: ${n.denoiseAlgorithm}，将使用默认的泊松降噪`), n.denoiseAlgorithm = "poisson"), n.denoiseAlgorithm === "gaussian-bilateral") {
        console.log("使用高斯双边滤波器进行降噪");
        const l = {
          ...n,
          // 确保高斯双边滤波器参数存在且有合理的值
          sigmaSpace: n.sigmaSpace ?? 2,
          sigmaRange: n.sigmaRange ?? 0.05,
          radius: n.radius ?? 3,
          // 允许多次迭代，但限制最大值
          iterations: Math.min(n.denoiseIterations ?? 2, 3)
        };
        this.denoisePass = new Ln(t, s, l);
      } else
        this.denoisePass = new xt(t, s, n);
      n.gBufferPass ? this.denoisePass.setGBufferPass(n.gBufferPass) : this.velocityDepthNormalPass && this.denoisePass.setGBufferPass(this.velocityDepthNormalPass), this.temporalReprojectPass && this.temporalReprojectPass.renderTarget && this.temporalReprojectPass.renderTarget.texture && this.denoisePass && this.denoisePass.renderTargetB && this.denoisePass.renderTargetB.texture && (this.temporalReprojectPass.overrideAccumulatedTextures = this.denoisePass.renderTargetB.texture);
    }
    const o = this.denoisePass?.texture ?? s;
    n.denoiseMode.startsWith("full") && (this.denoiserComposePass = new ji(
      t,
      o,
      n.gBufferPass.texture,
      n.gBufferPass.renderTarget.depthTexture,
      n
    )), this._scene = e, this._camera = t, this._texture = r, this._currentTextures = o, this._texturesNeedUpdate = !1;
  }
  /**
   * 获取使用的降噪算法名称
   * @returns {string} "poisson" 或 "gaussian-bilateral"
   */
  get denoiseAlgorithm() {
    return this.options.denoiseAlgorithm;
  }
  /**
   * 设置降噪算法
   * @param {string} algorithm - "poisson" 或 "gaussian-bilateral"
   */
  set denoiseAlgorithm(e) {
    if (e !== "poisson" && e !== "gaussian-bilateral" && (console.warn(`不支持的降噪算法: ${e}，将使用默认的泊松降噪`), e = "poisson"), this.options.denoiseAlgorithm !== e) {
      if (this.options.denoiseAlgorithm = e, this.denoisePass) {
        const t = this.denoisePass, r = this.temporalReprojectPass.renderTarget.texture.slice(
          0,
          this.options.inputType === "diffuseSpecular" ? 2 : 1
        ), n = this.denoisePass.renderTargetA.width, i = this.denoisePass.renderTargetA.height;
        if (e === "gaussian-bilateral") {
          console.log("切换到高斯双边滤波器");
          const s = {
            ...this.options,
            // 确保高斯双边滤波器参数存在且有合理的值
            sigmaSpace: this.options.sigmaSpace ?? 2,
            sigmaRange: this.options.sigmaRange ?? 0.05,
            radius: this.options.radius ?? 3,
            // 允许更多迭代
            iterations: Math.min(this.options.denoiseIterations ?? 2, 3)
          };
          this.denoisePass = new Ln(
            this._camera,
            r,
            s
          );
        } else
          console.log("切换到泊松降噪器"), this.denoisePass = new xt(
            this._camera,
            r,
            this.options
          );
        if (this.options.gBufferPass ? this.denoisePass.setGBufferPass(this.options.gBufferPass) : this.velocityDepthNormalPass && this.denoisePass.setGBufferPass(this.velocityDepthNormalPass), this.denoisePass.setSize(n, i), this.temporalReprojectPass && (this.temporalReprojectPass.overrideAccumulatedTextures = this.denoisePass.renderTargetB.texture), this.denoiserComposePass)
          try {
            const s = this.denoisePass.texture;
            if (this._currentTextures = s, this._texturesNeedUpdate = !0, typeof this.denoiserComposePass.updateTextures == "function")
              this.denoiserComposePass.updateTextures(s), this._texturesNeedUpdate = !1;
            else {
              const o = this.denoiserComposePass.fullscreenMaterial.uniforms;
              this.options.inputType === "diffuseSpecular" && Array.isArray(s) ? (o.diffuseGiTexture && (o.diffuseGiTexture.value = s[0]), o.specularGiTexture && (o.specularGiTexture.value = s[1])) : this.options.inputType === "diffuse" ? o.diffuseGiTexture && (o.diffuseGiTexture.value = Array.isArray(s) ? s[0] : s) : this.options.inputType === "specular" && o.specularGiTexture && (o.specularGiTexture.value = Array.isArray(s) ? s[0] : s), this._texturesNeedUpdate = !1;
            }
            console.log("已更新DenoiserComposePass的输入纹理");
          } catch (s) {
            console.error("更新DenoiserComposePass的输入纹理时出错:", s);
          }
        t.dispose();
      }
      this.reset();
    }
  }
  get texture() {
    return this.options.denoiseMode === "denoised" ? this.denoisePass.texture : this.options.denoiseMode === "temporal" ? this.temporalReprojectPass.texture : this.options.denoiseMode.startsWith("full") ? this.denoiserComposePass.renderTarget.texture : this.temporalReprojectPass.texture;
  }
  reset() {
    if (this.temporalReprojectPass.reset(), this.denoiserComposePass && this.denoisePass)
      try {
        const e = this.denoisePass.texture;
        this.hasTexturesChanged(e) && (this._currentTextures = e, this._texturesNeedUpdate = !0, typeof this.denoiserComposePass.updateTextures == "function" && (this.denoiserComposePass.updateTextures(e), this._texturesNeedUpdate = !1));
      } catch (e) {
        console.error("在reset中更新DenoiserComposePass纹理时出错:", e);
      }
  }
  // 检查纹理是否发生变化
  hasTexturesChanged(e) {
    if (!this._currentTextures)
      return !0;
    if (this._currentTextures === e)
      return !1;
    if (Array.isArray(this._currentTextures) && Array.isArray(e)) {
      if (this._currentTextures.length !== e.length)
        return !0;
      for (let t = 0; t < this._currentTextures.length; t++)
        if (this._currentTextures[t] !== e[t])
          return !0;
      return !1;
    }
    return !0;
  }
  setSize(e, t) {
    this.velocityDepthNormalPass.setSize(e, t), this.temporalReprojectPass.setSize(e, t), this.denoisePass?.setSize(e, t), this.denoiserComposePass?.setSize(e, t);
  }
  dispose() {
    this.velocityDepthNormalPass.dispose(), this.temporalReprojectPass.dispose(), this.denoisePass?.dispose(), this.denoiserComposePass?.dispose();
  }
  render(e, t = null) {
    this.isOwnVelocityDepthNormalPass && this.velocityDepthNormalPass.render(e), this.temporalReprojectPass.render(e), this.denoisePass?.render(e), this.denoiserComposePass?.render(e);
  }
}
class sc extends X {
  constructor(e) {
    super("GBufferDebugPass"), this.renderTarget = new A(1, 1, {
      depthBuffer: !1,
      type: Y,
      minFilter: b,
      magFilter: b
    }), this.renderTarget.texture.name = "GBufferDebugPass.Texture", this.fullscreenMaterial = new N({
      fragmentShader: (
        /* glsl */
        `
            varying vec2 vUv;
            uniform highp sampler2D depthTexture;
			uniform int mode;

            #include <common>
            #include <packing>

            ${ot}

            void main() {
                highp float depth = textureLod(depthTexture, vUv, 0.).r;

				if(depth == 0.){
					gl_FragColor = vec4(0.);
					return;
				}

                Material mat = getMaterial(gBufferTexture, vUv);

                if (mode == 0) {
                    gl_FragColor = vec4(mat.diffuse.rgb, 1.);
                } else if (mode == 1) {
                    gl_FragColor = vec4(mat.diffuse.aaa, 1.);
                } else if (mode == 2) {
                    gl_FragColor = vec4(mat.normal, 1.);
                } else if (mode == 3) {
                    gl_FragColor = vec4(vec3(mat.roughness), 1.);
                } else if (mode == 4) {
                    gl_FragColor = vec4(vec3(mat.metalness), 1.);
                } else {
                    gl_FragColor = vec4(mat.emissive, 1.);
                }
            }
            `
      ),
      vertexShader: We,
      uniforms: {
        gBufferTexture: { value: e },
        mode: { value: 0 }
      },
      blending: B,
      depthWrite: !1,
      depthTest: !1,
      toneMapped: !1
    });
  }
  get texture() {
    return this.renderTarget.texture;
  }
  dispose() {
    this.renderTarget.dispose();
  }
  setSize(e, t) {
    this.renderTarget.setSize(e, t);
  }
  render(e) {
    e.setRenderTarget(this.renderTarget), e.render(this.scene, this.camera);
  }
}
const Xi = {
  mode: "ssgi",
  distance: 10,
  thickness: 10,
  autoThickness: !1,
  maxRoughness: 1,
  blend: 0.9,
  denoiseIterations: 1,
  denoiseKernel: 2,
  denoiseDiffuse: 10,
  denoiseSpecular: 10,
  depthPhi: 2,
  normalPhi: 50,
  roughnessPhi: 1,
  specularPhi: 0.95,
  diffuseOnly: !1,
  specularOnly: !1,
  resolutionScale: 1,
  missedRays: !1,
  rendererSize: 0,
  // temporal reproject
  maxAge: 3,
  reprojectSpecular: [!1, !0],
  temporalResolve: !0,
  neighborhoodClamp: [!0, !0],
  neighborhoodClampRadius: 2,
  neighborhoodClampIntensity: 0.5,
  blend: 0.8,
  steps: 20,
  refineSteps: 5,
  spp: 1,
  importanceSampling: !0,
  radius: 3,
  fade: 0,
  enableJitter: !0,
  enableReflections: !0,
  denoiseAlgorithm: "poisson",
  // 高斯双边滤波器特殊参数
  sigmaSpace: 3,
  // 空间高斯函数标准差
  sigmaRange: 0.1,
  // 范围高斯函数标准差
  outputTexture: null
}, ac = `uniform sampler2D inputTexture;\r
uniform sampler2D sceneTexture;\r
uniform highp sampler2D depthTexture;\r
uniform bool isDebug;\r
\r
uniform float cameraNear;\r
uniform float cameraFar;\r
\r
#include <fog_pars_fragment>\r
\r
// source: https://github.com/mrdoob/three.js/blob/79ea10830dfc97b6c0a7e29d217c7ff04c081095/examples/jsm/shaders/BokehShader.js#L66\r
float getViewZ(const in float depth) {\r
#if PERSPECTIVE_CAMERA == 1\r
  return perspectiveDepthToViewZ(depth, cameraNear, cameraFar);\r
#else\r
  return orthographicDepthToViewZ(depth, cameraNear, cameraFar);\r
#endif\r
}\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
  if (isDebug) {\r
    outputColor = textureLod(inputTexture, uv, 0.);\r
    return;\r
  }\r
\r
  float depth = textureLod(depthTexture, uv, 0.).r;\r
  vec3 ssgiClr;\r
\r
  if (depth == 1.0) {\r
    ssgiClr = textureLod(sceneTexture, uv, 0.).rgb;\r
  } else {\r
    ssgiClr = textureLod(inputTexture, uv, 0.).rgb;\r
\r
#ifdef USE_FOG\r
    float viewZ = getViewZ(depth) * 0.4; // todo: find why 0.4 is needed to somewhat match three.js's result\r
    vFogDepth = -viewZ;\r
\r
#include <fog_fragment>\r
\r
    ssgiClr = mix(ssgiClr, fogColor, fogFactor);\r
#endif\r
  }\r
\r
  outputColor = vec4(ssgiClr, 1.0);\r
}`, { render: On } = pe.prototype, oc = Gi();
let an = class extends I {
  selection = new Se();
  isUsingRenderPass = !0;
  id = "123";
  constructor(e, t, r, n) {
    n = { ...Xi, ...n };
    let i = ac.replace(
      "#include <fog_pars_fragment>",
      ue.fog_pars_fragment.replace("varying", "")
    );
    i = i.replace(
      "#include <fog_fragment>",
      ue.fog_fragment.replace(/.*gl_FragColor.*/g, "")
    );
    const s = /* @__PURE__ */ new Map();
    if (t.fog && s.set("USE_FOG", ""), t.fog?.isFogExp2 && s.set("FOG_EXP2", ""), super("SSGIEffect1", i, {
      type: "FinalSSGIMaterial",
      uniforms: /* @__PURE__ */ new Map([
        ["inputTexture", new a(null)],
        ["sceneTexture", new a(null)],
        ["depthTexture", new a(null)],
        ["isDebug", new a(!1)],
        ["fogColor", new a(new y())],
        ["fogNear", new a(0)],
        ["fogFar", new a(0)],
        ["fogDensity", new a(0)],
        ["cameraNear", new a(0)],
        ["cameraFar", new a(0)]
      ]),
      defines: new Map([["PERSPECTIVE_CAMERA", r.isPerspectiveCamera ? "1" : "0"], ...s])
    }), this._scene = t, this._camera = r, this.composer = e, n.mode === "ssr" ? (n.reprojectSpecular = !0, n.neighborhoodClamp = !0, n.inputType = "specular") : n.mode === "ssgi" && (n.reprojectSpecular = [!1, !0], n.neighborhoodClamp = [!1, !0]), typeof n.preset == "string")
      switch (n.preset) {
        case "low":
          n.steps = 10, n.refineSteps = 2, n.denoiseMode = "full_temporal";
          break;
        case "medium":
          n.steps = 20, n.refineSteps = 4, n.denoiseMode = "full";
          break;
        case "medium":
          n.steps = 40, n.refineSteps = 4, n.denoiseMode = "full";
          break;
      }
    this.ssgiPass = new Dl(this, n), this.denoiser = new ic(t, r, this.ssgiPass.texture, {
      gBufferPass: this.ssgiPass.gBufferPass,
      velocityDepthNormalPass: n.velocityDepthNormalPass,
      ...n
    }), this.lastSize = {
      width: n.width,
      height: n.height,
      resolutionScale: n.resolutionScale
    }, this.sceneRenderTarget = new A(1, 1, {
      colorSpace: V
    }), this.renderPass = new pe(this._scene, this._camera), this.renderPass.renderToScreen = !1, this.setSize(n.width, n.height);
    const o = this, l = this.renderPass;
    pe.prototype.render = function(...u) {
      if (this !== l) {
        const f = o.isUsingRenderPass;
        o.isUsingRenderPass = !0, f != o.isUsingRenderPass && o.updateUsingRenderPass();
      }
      On.call(this, ...u);
    }, this.makeOptionsReactive(n), this.outputTexture = this.denoiser.texture;
  }
  updateUsingRenderPass() {
    this.isUsingRenderPass ? this.ssgiPass.fullscreenMaterial.defines.useDirectLight = "" : delete this.ssgiPass.fullscreenMaterial.defines.useDirectLight, this.ssgiPass.fullscreenMaterial.needsUpdate = !0;
  }
  reset() {
    this.denoiser.reset();
  }
  makeOptionsReactive(e) {
    let t = !1;
    const r = this.ssgiPass.fullscreenMaterial.uniforms, n = Object.keys(r);
    for (const i of Object.keys(e))
      Object.defineProperty(this, i, {
        get() {
          return e[i];
        },
        set(s) {
          if (!(e[i] === s && t))
            switch (e[i] = s, i) {
              // denoiser
              case "denoiseIterations":
                this.denoiser.denoisePass && (this.denoiser.denoisePass.iterations = s);
                break;
              case "denoiseAlgorithm":
                if (this.denoiser)
                  try {
                    console.log("切换降噪算法到:", s);
                    const o = e.denoiseAlgorithm;
                    s === "gaussian-bilateral" && (e.sigmaSpace = e.sigmaSpace || 2, e.sigmaRange = e.sigmaRange || 0.05, e.denoiseIterations = Math.min(e.denoiseIterations || 2, 1), console.log("配置高斯双边滤波器参数:", {
                      sigmaSpace: e.sigmaSpace,
                      sigmaRange: e.sigmaRange,
                      iterations: e.denoiseIterations
                    }));
                    const l = this.lastSize.width, u = this.lastSize.height, f = e.resolutionScale;
                    this.denoiser.denoiseAlgorithm = s, this.outputTexture = this.denoiser.texture, this.setSize(l, u), this.reset(), console.log("降噪算法切换成功");
                  } catch (o) {
                    console.error("切换降噪算法失败:", o), e.denoiseAlgorithm = "poisson";
                    try {
                      this.denoiser.denoiseAlgorithm = "poisson", this.reset(), alert("切换降噪算法失败，已回退到泊松降噪");
                    } catch (l) {
                      console.error("回退到泊松降噪也失败:", l);
                    }
                  }
                break;
              case "radius":
              case "phi":
              case "lumaPhi":
              case "depthPhi":
              case "normalPhi":
              case "roughnessPhi":
              case "specularPhi":
              case "sigmaSpace":
              // 添加高斯双边滤波器参数
              case "sigmaRange":
                this.denoiser.denoisePass?.fullscreenMaterial.uniforms[i] && (i === "sigmaSpace" ? this.denoiser.denoisePass.fullscreenMaterial.uniforms[i].value = Math.max(1, Math.min(5, s)) : i === "sigmaRange" ? this.denoiser.denoisePass.fullscreenMaterial.uniforms[i].value = Math.max(0.01, Math.min(0.2, s)) : this.denoiser.denoisePass.fullscreenMaterial.uniforms[i].value = s, this.reset());
                break;
              case "denoiseIterations":
              case "radius":
                this.denoiser.denoisePass && (this.denoiser.denoisePass[i] = s);
                break;
              // SSGI
              case "resolutionScale":
                this.setSize(this.lastSize.width, this.lastSize.height), this.reset();
                break;
              case "steps":
              case "refineSteps":
                this.ssgiPass.fullscreenMaterial.defines[i] = parseInt(s), this.ssgiPass.fullscreenMaterial.needsUpdate = t, this.reset();
                break;
              case "importanceSampling":
              case "missedRays":
                s ? this.ssgiPass.fullscreenMaterial.defines[i] = "" : delete this.ssgiPass.fullscreenMaterial.defines[i], this.ssgiPass.fullscreenMaterial.needsUpdate = t, this.reset();
                break;
              case "distance":
                r.rayDistance.value = s, this.reset();
                break;
              case "outputTexture":
                if (!this.outputTexture)
                  return;
                if (typeof s == "string") {
                  this.gBufferDebugPass === void 0 && (this.gBufferDebugPass = new sc(this.ssgiPass.gBufferPass.texture), this.gBufferDebugPass.setSize(this.lastSize.width, this.lastSize.height));
                  const l = ["diffuse", "alpha", "normal", "roughness", "metalness", "emissive"].indexOf(s);
                  this.gBufferDebugPass.fullscreenMaterial.uniforms.mode.value = l, this.outputTexture = this.gBufferDebugPass.texture;
                } else this.gBufferDebugPass !== void 0 && this.outputTexture !== this.gBufferDebugPass.texture && (this.gBufferDebugPass.dispose(), delete this.gBufferDebugPass);
                this.uniforms.get("isDebug").value = this.outputTexture !== this.denoiser.texture;
                break;
              // must be a uniform
              default:
                n.includes(i) && (r[i].value = s, this.reset());
            }
        }
      }), this[i] = e[i];
    t = !0;
  }
  initialize(e, ...t) {
    super.initialize(e, ...t), this.ssgiPass.initialize(e, ...t);
  }
  setSize(e, t, r = !1) {
    e === void 0 && t === void 0 || !r && e === this.lastSize.width && t === this.lastSize.height && this.resolutionScale === this.lastSize.resolutionScale || (this.ssgiPass.setSize(e, t), this.denoiser.setSize(e, t), this.gBufferDebugPass?.setSize(e, t), this.sceneRenderTarget.setSize(e, t), this.cubeToEquirectEnvPass?.setSize(e, t), this.lastSize = {
      width: e,
      height: t,
      resolutionScale: this.resolutionScale
    });
  }
  dispose() {
    super.dispose(), this.ssgiPass.dispose(), this.denoiser.dispose(), this.cubeToEquirectEnvPass?.dispose(), pe.prototype.render = On;
  }
  keepEnvMapUpdated(e) {
    const t = this.ssgiPass.fullscreenMaterial;
    let r = this._scene.environment;
    if (r) {
      if (t.uniforms.envMapInfo.value.mapUuid !== r.uuid) {
        r.isCubeTexture && (this.cubeToEquirectEnvPass || (this.cubeToEquirectEnvPass = new al()), r = this.cubeToEquirectEnvPass.generateEquirectEnvMap(e, r), r.uuid = this._scene.environment.uuid), r.generateMipmaps || (r.generateMipmaps = !0, r.minFilter = nt, r.magFilter = F, r.needsUpdate = !0), r.type === Y && console.warn(
          "SSGI: Environment map is FloatType, this causes the environment map to be black in the SSGI pass for many modern Apple devices. Please use HalfFloatType instead."
        ), t.uniforms.envMapInfo.value.mapUuid = r.uuid;
        const n = zl(r);
        t.uniforms.maxEnvMapMipLevel.value = n, t.uniforms.envMapInfo.value.map = r, t.defines.USE_ENVMAP = "", delete t.defines.importanceSampling, this.importanceSampling ? t.uniforms.envMapInfo.value.updateFrom(r, e).then(() => {
          t.defines.importanceSampling = "", t.needsUpdate = !0;
        }) : t.uniforms.envMapInfo.value.map = r, this.reset(), t.needsUpdate = !0;
      }
    } else "USE_ENVMAP" in t.defines && (delete t.defines.USE_ENVMAP, delete t.defines.importanceSampling, t.needsUpdate = !0);
  }
  get depthTexture() {
    return this.ssgiPass.gBufferPass.depthTexture;
  }
  update(e, t) {
    this.keepEnvMapUpdated(e);
    const r = this.isUsingRenderPass ? t : this.sceneRenderTarget, n = [];
    this.ssgiPass.fullscreenMaterial.uniforms.directLightTexture.value = r.texture, this.ssgiPass.render(e), this.gBufferDebugPass?.render(e), this.denoiser.render(e, t), this.uniforms.get("inputTexture").value = this.outputTexture[0] ?? this.outputTexture, this.uniforms.get("sceneTexture").value = r.texture, this.uniforms.get("depthTexture").value = this.ssgiPass.gBufferPass.depthTexture, this._scene.fog && (this.uniforms.get("fogColor").value = this._scene.fog.color, this.uniforms.get("fogNear").value = this._scene.fog.near, this.uniforms.get("fogFar").value = this._scene.fog.far, this.uniforms.get("fogDensity").value = this._scene.fog.density, this.uniforms.get("cameraNear").value = this._camera.near, this.uniforms.get("cameraFar").value = this._camera.far);
    for (const i of n)
      i.visible = !0;
    oc.value = !0, cancelAnimationFrame(this.usingRenderPassRAF), this.isUsingRenderPass = !1;
  }
};
an.DefaultOptions = Xi;
const kn = {
  denoiseMode: "full",
  // can be "full" | "full_temporal" | "denoised" | "temporal"
  inputType: "diffuseSpecular",
  // can be "diffuseSpecular" | "diffuse" | "specular"
  gBufferPass: null,
  velocityDepthNormalPass: null,
  brightnessCompensation: 1.5
  // 增加亮度补偿选项
};
class Vn {
  constructor(e, t, r, n = kn) {
    n = { ...kn, ...n }, this.options = n, this._scene = e, this._camera = t, this.velocityDepthNormalPass = n.velocityDepthNormalPass ?? new sn(e, t), this.isOwnVelocityDepthNormalPass = !n.velocityDepthNormalPass;
    const i = n.inputType === "diffuseSpecular" ? 2 : 1;
    this.temporalReprojectPass = new nn(
      e,
      t,
      this.velocityDepthNormalPass,
      r,
      i,
      {
        fullAccumulate: !0,
        logTransform: !0,
        copyTextures: !n.denoise,
        reprojectSpecular: [!1, !0],
        neighborhoodClamp: [!0, !0],
        neighborhoodClampRadius: 1,
        // 减小半径，减少黑点产生
        neighborhoodClampIntensity: 0.3,
        // 降低强度，减少黑点
        ...n
      }
    );
    const s = this.temporalReprojectPass.renderTarget.texture.slice(0, i);
    if (this.options.denoiseMode === "full" || this.options.denoiseMode === "denoised") {
      const l = {
        ...n,
        phi: n.phi * 0.7,
        // 减小phi值
        lumaPhi: n.lumaPhi * 0.8,
        // 减小亮度phi值
        depthPhi: n.depthPhi * 0.9,
        // 调整深度phi值
        normalPhi: n.normalPhi * 1.2
        // 增大法线phi值以保留更多细节
      };
      this.denoisePass = new xt(t, s, l), this.denoisePass.setGBufferPass(n.gBufferPass ?? this.velocityDepthNormalPass), this.temporalReprojectPass.overrideAccumulatedTextures = this.denoisePass.renderTargetB.texture;
    }
    const o = this.denoisePass?.texture ?? s;
    n.denoiseMode.startsWith("full") && (this.denoiserComposePass = new ji(
      t,
      o,
      n.gBufferPass.texture,
      n.gBufferPass.renderTarget.depthTexture,
      {
        ...n,
        brightnessCompensation: n.brightnessCompensation || 1.5
        // 应用亮度补偿
      }
    )), console.log("创建了自定义降噪器，降噪参数已调整以避免黑点");
  }
  /**
      * 重置降噪器状态
      */
  reset() {
    this.temporalReprojectPass?.reset();
  }
  /**
      * 调整亮度补偿系数
      * @param {number} value - 新的亮度补偿系数
      */
  setBrightnessCompensation(e) {
    this.options.brightnessCompensation = e;
  }
  /**
      * 渲染降噪效果
      * @param {WebGLRenderer} renderer - WebGL渲染器
      * @param {WebGLRenderTarget} inputBuffer - 输入缓冲区
      */
  render(e, t) {
    this.temporalReprojectPass.render(e), this.denoisePass && this.denoisePass.render(e), this.denoiserComposePass && this.denoiserComposePass.render(e);
  }
  /**
      * 释放资源
      */
  dispose() {
    this.denoisePass?.dispose(), this.temporalReprojectPass?.dispose(), this.denoiserComposePass?.dispose(), this.isOwnVelocityDepthNormalPass && this.velocityDepthNormalPass.dispose();
  }
  /**
      * 获取输出纹理
      */
  get texture() {
    return this.denoiserComposePass?.renderTarget.texture ?? this.denoisePass?.renderTarget.texture[0] ?? this.temporalReprojectPass.renderTarget.texture[0];
  }
  /**
      * 设置渲染尺寸
      * @param {number} width - 宽度
      * @param {number} height - 高度
      */
  setSize(e, t) {
    this.temporalReprojectPass?.setSize(e, t), this.denoisePass?.setSize(e, t), this.denoiserComposePass?.setSize(e, t);
  }
}
const Hn = Gi();
class ud extends an {
  /**
      * 构造函数
      * @param {import("postprocessing").EffectComposer} composer - 效果合成器
      * @param {THREE.Scene} scene - 场景
      * @param {THREE.Camera} camera - 相机
      * @param {Object} options - SSGI选项
      */
  constructor(e, t, r, n = {}) {
    n = {
      ...n,
      // 调整一些默认参数以减少黑点产生
      normalPhi: n.normalPhi || 30,
      // 降低法线敏感度
      roughnessPhi: n.roughnessPhi || 30,
      // 降低粗糙度敏感度
      steps: n.steps || 20,
      refineSteps: n.refineSteps || 4
    }, super(e, t, r, n), this.brightnessCompensation = n.brightnessCompensation || 1.5;
    const i = this.denoiser;
    this.denoiser = new Vn(t, r, this.ssgiPass.texture, {
      gBufferPass: this.ssgiPass.gBufferPass,
      velocityDepthNormalPass: n.velocityDepthNormalPass,
      brightnessCompensation: this.brightnessCompensation,
      ...n
    }), this.outputTexture = this.denoiser.texture, i && i.dispose(), console.log("CustomSSGIEffect 已创建，使用自定义降噪器以避免黑点");
  }
  /**
      * 重写更新方法
      * @param {THREE.WebGLRenderer} renderer - 渲染器
      * @param {THREE.WebGLRenderTarget} inputBuffer - 输入缓冲区
      */
  update(e, t) {
    this.keepEnvMapUpdated(e);
    const r = this.isUsingRenderPass ? t : this.sceneRenderTarget, n = [];
    if (!this.isUsingRenderPass) {
      const i = [];
      for (const s of Zl(this._scene)) {
        if (s.isScene)
          return;
        s.visible = !_r(s), s.visible ? n.push(s) : i.push(s);
      }
      this.renderPass.render(e, this.sceneRenderTarget);
      for (const s of i)
        s.visible = !0;
      for (const s of n)
        s.visible = !1;
    }
    this.ssgiPass.fullscreenMaterial.uniforms.directLightTexture.value = r.texture, this.ssgiPass.render(e), this.denoiser.render(e, t), this.uniforms.get("inputTexture").value = this.denoiser.texture, this.uniforms.get("sceneTexture").value = r.texture, this.uniforms.get("depthTexture").value = this.ssgiPass.gBufferPass.depthTexture, this._scene.fog && (this.uniforms.get("fogColor").value = this._scene.fog.color, this.uniforms.get("fogNear").value = this._scene.fog.near, this.uniforms.get("fogFar").value = this._scene.fog.far, this.uniforms.get("fogDensity").value = this._scene.fog.density, this.uniforms.get("cameraNear").value = this._camera.near, this.uniforms.get("cameraFar").value = this._camera.far);
    for (const i of n)
      i.visible = !0;
    Hn.value = !0, cancelAnimationFrame(this.rAF2), cancelAnimationFrame(this.rAF), cancelAnimationFrame(this.usingRenderPassRAF), this.rAF = requestAnimationFrame(() => {
      this.rAF2 = requestAnimationFrame(() => {
        Hn.value = !1;
      });
    }), this.usingRenderPassRAF = requestAnimationFrame(() => {
      const i = this.isUsingRenderPass;
      this.isUsingRenderPass = !1, i != this.isUsingRenderPass && this.updateUsingRenderPass();
    });
  }
  /**
      * 重写dispose方法以确保清理自定义降噪器
      */
  dispose() {
    super.dispose(), this.denoiser && this.denoiser instanceof Vn && this.denoiser.dispose();
  }
}
const lc = `// inputBuffer和vUv由Effect基类已定义，这里不再重复定义
// uniform sampler2D inputBuffer;
uniform sampler2D fog_depthBuffer;
uniform vec2 fog_resolution;
uniform vec3 fog_color;
uniform float fog_density;
uniform float fog_heightFalloff;
uniform float fog_baseHeight;
uniform vec3 fog_lightPosition;
uniform vec3 fog_lightColor;
uniform float fog_lightIntensity;
uniform float fog_scatteringCoefficient;
uniform float fog_extinction;
uniform mat4 fog_projectionMatrix;
uniform float fog_cameraNear;
uniform float fog_cameraFar;
uniform float fog_stepSize;
uniform float fog_time;
uniform float debug_mode; // 调试模式 0=正常, 1=深度纹理, 2=线性深度

// 不再重复定义vUv（由基类已定义）
// varying vec2 vUv;
varying vec3 vRayOrigin;
varying vec3 vRayDir;

// 深度值转换为线性深度
float getLinearDepth(float depth) {
    // 检查深度值是否有效
    if (depth == 1.0) return fog_cameraFar; // 如果是背景，返回远平面距离
    
    float z = depth * 2.0 - 1.0;
    return (2.0 * fog_cameraNear * fog_cameraFar) / (fog_cameraFar + fog_cameraNear - z * (fog_cameraFar - fog_cameraNear));
}

// 基于高度的雾密度
float getDensity(vec3 position) {
    float height = position.y;
    float heightFactor = exp(-fog_heightFalloff * max(0.0, height - fog_baseHeight));
    return fog_density * heightFactor;
}

// 简单噪声函数
float hash(vec3 p) {
    p = fract(p * vec3(443.8975, 397.2973, 491.1871));
    p += dot(p, p.zxy + 19.19);
    return fract(p.x * p.y * p.z);
}

// 光线步进主函数
vec4 raymarch(vec3 rayOrigin, vec3 rayDir, float tMax) {
    // 减少最大步数以提高性能和兼容性
    const int MAX_STEPS = 16;
    
    vec3 totalScattering = vec3(0.0);
    float transmittance = 1.0;
    
    // 添加时间偏移以增加动态效果
    float timeOffset = fog_time * 0.05;
    
    // 调整步长，确保采样足够
    float stepSize = min(fog_stepSize, tMax / float(MAX_STEPS));
    
    // 简化的光线步进循环
    for (int i = 0; i < MAX_STEPS; i++) {
        // 计算当前采样点位置
        float t = float(i) * stepSize;
        
        // 跳过超出深度范围的采样
        if (t >= tMax) break;
        
        vec3 position = rayOrigin + rayDir * t;
        
        // 计算当前点的密度
        float density = getDensity(position);
        
        // 添加简单的噪声以使雾不均匀
        float noise = hash(position * 0.1 + timeOffset);
        density *= (0.7 + 0.3 * noise);
        
        if (density > 0.0) {
            // 计算到光源的方向和距离
            vec3 lightDir = normalize(fog_lightPosition - position);
            float lightDist = length(fog_lightPosition - position);
            
            // 简化光照计算，但保留基本的光源距离衰减
            float lightAttenuation = 1.0 / (1.0 + lightDist * 0.1);
            
            // 计算散射贡献 - 增加系数使效果更明显
            vec3 scattering = fog_lightColor * fog_lightIntensity * fog_scatteringCoefficient 
                            * density * lightAttenuation;
            
            // 增加散射强度，使效果更加明显
            scattering *= 8.0; 
            
            // 添加到累积散射中
            totalScattering += transmittance * scattering * stepSize;
            
            // 更新透射率 - 使用简化的衰减计算
            transmittance *= exp(-fog_extinction * density * stepSize);
            
            // 优化：如果透射率已经很低，提前退出
            if (transmittance < 0.01) break;
        }
    }
    
    // 确保散射不为零，并调整不透明度上限
    totalScattering = max(totalScattering, vec3(0.01));
    float fogOpacity = min(1.0 - transmittance, 0.7); // 降低最大不透明度以保持场景可见
    
    return vec4(totalScattering, fogOpacity);
}

void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
    // 初始默认输出，避免未赋值的情况
    outputColor = inputColor;
    
    // 获取场景深度
    float depth = texture2D(fog_depthBuffer, uv).r;
    
    // 检查深度纹理是否有效
    if (depth <= 0.0 || depth >= 1.0) {
        // 深度无效，保持原始颜色并退出
        return;
    }
    
    // 调试模式 - 深度纹理可视化
    // 使用浮点数比较，提高WebGL兼容性
    if (debug_mode > 0.5 && debug_mode < 1.5) {
        outputColor = vec4(vec3(depth), 1.0);
        return;
    }
    
    // 计算线性深度
    float linearDepth = getLinearDepth(depth);
    
    // 调试模式 - 线性深度可视化
    if (debug_mode > 1.5) {
        float normalizedDepth = linearDepth / fog_cameraFar;
        outputColor = vec4(vec3(normalizedDepth), 1.0);
        return;
    }
    
    // 计算光线长度（从相机到场景表面）
    float tMax = linearDepth;
    
    // 执行光线步进
    vec4 fogResult = raymarch(vRayOrigin, vRayDir, tMax);
    
    // 确保雾气不会完全替换场景
    float fogStrength = min(fogResult.a, 0.75);  // 允许更高的雾气强度
    
    // 混合原始场景颜色和雾效果 - 增加混合强度
    outputColor = mix(inputColor, vec4(fog_color * 0.5 + fogResult.rgb, 1.0), fogStrength * 0.5);
    
    // 添加散射光贡献 - 增加因子使效果更明显
    outputColor.rgb += fogResult.rgb * 3.0;
    
    // 确保输出颜色有有效的Alpha值
    outputColor.a = inputColor.a;
} `, cc = `// 移除重复的vUv定义，仅保留特定于体积雾的变量
// varying vec2 vUv; - 已由基类定义，所以移除此行
varying vec3 vRayOrigin;
varying vec3 vRayDir;

uniform mat4 cameraMatrixWorld;
uniform mat4 projectionMatrixInv;

// 不定义main函数，而是使用基类提供的mainSupport函数
void mainSupport(const in vec2 uv) {
    // 计算世界空间中的光线原点和方向
    vec4 ndcRay = vec4(
        (uv.x - 0.5) * 2.0,
        (uv.y - 0.5) * 2.0,
        1.0,
        1.0
    );
    
    vec4 rayOrigin = cameraMatrixWorld * vec4(0.0, 0.0, 0.0, 1.0);
    vec4 rayDir = projectionMatrixInv * ndcRay;
    rayDir = cameraMatrixWorld * vec4(normalize(rayDir.xyz), 0.0);
    
    vRayOrigin = rayOrigin.xyz;
    vRayDir = rayDir.xyz;
} `;
class Gn extends I {
  /**
      * 构造一个新的体积雾效果
      *
      * @param {Object} [options] - 效果选项
      * @param {BlendFunction} [options.blendFunction=BlendFunction.NORMAL] - 混合模式
      * @param {Number} [options.density=0.05] - 雾气密度
      * @param {Number} [options.heightFalloff=0.1] - 高度衰减系数
      * @param {Number} [options.baseHeight=0] - 基准高度
      * @param {Color|Number} [options.color=0xffffff] - 雾气颜色
      * @param {Number} [options.scattering=0.2] - 散射系数
      * @param {Number} [options.extinction=0.1] - 衰减系数
      * @param {Number} [options.samples=64] - 采样次数
      */
  constructor({
    blendFunction: e = E.NORMAL,
    density: t = 0.1,
    heightFalloff: r = 0.1,
    baseHeight: n = 0,
    color: i = 13421823,
    scattering: s = 0.3,
    extinction: o = 0.05,
    samples: l = 32
  } = {}) {
    super("VolumetricFogEffect", lc, {
      blendFunction: e,
      vertexShader: cc,
      attributes: Q.DEPTH,
      uniforms: /* @__PURE__ */ new Map([
        ["fog_depthBuffer", new a(null)],
        ["fog_resolution", new a(new g())],
        ["fog_color", new a(new y(i))],
        ["fog_density", new a(t)],
        ["fog_heightFalloff", new a(r)],
        ["fog_baseHeight", new a(n)],
        ["fog_lightPosition", new a(new T(10, 40, 10))],
        ["fog_lightColor", new a(new y(16777164))],
        ["fog_lightIntensity", new a(2)],
        ["fog_scatteringCoefficient", new a(s)],
        ["fog_extinction", new a(o)],
        ["cameraMatrixWorld", new a(new C())],
        ["projectionMatrixInv", new a(new C())],
        ["fog_cameraNear", new a(0.1)],
        ["fog_cameraFar", new a(100)],
        ["fog_stepSize", new a(1 / l)],
        ["fog_time", new a(0)],
        ["debug_mode", new a(0)]
      ])
    }), this.samples = l, this.depthPacking = 0;
  }
  /**
      * 设置渲染深度通道
      * @param {Boolean} value - 是否需要深度通道
      */
  get needsDepthTexture() {
    return !0;
  }
  /**
      * 体积雾密度
      * @type {Number}
      */
  get density() {
    return this.uniforms.get("fog_density").value;
  }
  set density(e) {
    this.uniforms.get("fog_density").value = e;
  }
  /**
      * 高度衰减系数
      * @type {Number}
      */
  get heightFalloff() {
    return this.uniforms.get("fog_heightFalloff").value;
  }
  set heightFalloff(e) {
    this.uniforms.get("fog_heightFalloff").value = e;
  }
  /**
      * 基准高度
      * @type {Number}
      */
  get baseHeight() {
    return this.uniforms.get("fog_baseHeight").value;
  }
  set baseHeight(e) {
    this.uniforms.get("fog_baseHeight").value = e;
  }
  /**
      * 雾气颜色
      * @type {Color}
      */
  get color() {
    return this.uniforms.get("fog_color").value;
  }
  set color(e) {
    this.uniforms.get("fog_color").value.set(e);
  }
  /**
      * 散射系数
      * @type {Number}
      */
  get scattering() {
    return this.uniforms.get("fog_scatteringCoefficient").value;
  }
  set scattering(e) {
    this.uniforms.get("fog_scatteringCoefficient").value = e;
  }
  /**
      * 衰减系数
      * @type {Number}
      */
  get extinction() {
    return this.uniforms.get("fog_extinction").value;
  }
  set extinction(e) {
    this.uniforms.get("fog_extinction").value = e;
  }
  /**
      * 设置主光源位置和颜色（将用于照亮雾气）
      * @param {Vector3} position - 光源位置
      * @param {Color|Number} color - 光源颜色
      * @param {Number} intensity - 光源强度
      */
  setLight(e, t, r = 1) {
    const n = this.uniforms.get("fog_lightPosition").value, i = this.uniforms.get("fog_lightColor").value;
    n.copy(e), t !== void 0 && (t instanceof y ? i.copy(t) : i.set(t)), r !== void 0 && (this.uniforms.get("fog_lightIntensity").value = r);
  }
  /**
      * 设置深度纹理
      * @param {Texture} depthTexture - 深度纹理
      * @param {Number} [depthPacking=0] - 深度打包格式
      */
  setDepthTexture(e, t = 0) {
    if (!e) {
      console.warn("VolumetricFogEffect: 尝试设置空的深度纹理");
      return;
    }
    try {
      if (!e.isTexture && typeof e.addEventListener != "function") {
        console.warn("VolumetricFogEffect: 提供的深度纹理不是有效的纹理对象"), console.log("深度纹理类型:", typeof e);
        return;
      }
      e.minFilter !== void 0 && (e.minFilter = e.magFilter = 1003, e.compareFunction = 0, e.generateMipmaps = !1), this.uniforms.get("fog_depthBuffer").value = e, this.depthPacking = t, console.log(
        "VolumetricFogEffect: 深度纹理已设置，纹理ID:",
        e.id,
        "格式:",
        e.format,
        "类型:",
        e.type
      );
    } catch (r) {
      console.error("VolumetricFogEffect: 设置深度纹理时出错:", r);
    }
  }
  /**
      * 更新效果
      * @param {WebGLRenderer} renderer - 渲染器
      * @param {WebGLRenderTarget} inputBuffer - 输入缓冲
      * @param {Number} deltaTime - 时间增量
      */
  update(e, t, r) {
    const n = this.uniforms.get("camera")?.value, i = this.uniforms.get("cameraMatrixWorld").value, s = this.uniforms.get("projectionMatrixInv").value;
    n ? (i.copy(n.matrixWorld), s.copy(n.projectionMatrix).invert(), this.uniforms.get("fog_cameraNear").value = n.near, this.uniforms.get("fog_cameraFar").value = n.far, this.debug && (console.log("相机位置:", n.position), console.log("相机近平面:", n.near, "远平面:", n.far), console.log("相机矩阵:", i.elements), this.debug = !1)) : console.warn("VolumetricFogEffect: 未设置相机对象，使用默认值");
    const o = this.uniforms.get("fog_time");
    o.value += r;
  }
  /**
      * 设置渲染尺寸
      * @param {Number} width - 宽度
      * @param {Number} height - 高度
      */
  setSize(e, t) {
    this.uniforms.get("fog_resolution").value.set(e, t);
  }
}
const uc = `uniform sampler2D noiseTexture;
uniform vec2 resolution;
uniform float time;
uniform int animateClouds;
uniform vec3 sunPosition;
uniform float intensity;
uniform float sunBrightness;
uniform float rayleighCoefficient;
uniform float mieCoefficient;
uniform float mieDirectionalG;
uniform float cloudiness;
uniform mat4 viewMatrix;
uniform float fov;  // 添加FOV参数
uniform float skyBlueness; // 添加天空蓝度参数
uniform float cloudAmount; // 添加云层数量参数
uniform float cloudScale; // 添加云层尺度参数
uniform float cloudThreshold; // 添加云层阈值参数
// 添加采样步数uniform变量
uniform int volumetricCloudStepsUniform;
uniform int volumetricLightStepsUniform;
uniform int cloudShadowingStepsUniform;
uniform int volumetricLightShadowStepsUniform;

// 添加夜空相关参数
uniform int enableStars;
uniform float starIntensity;
uniform float starDensity;
uniform float starSize; // 新增：星星大小参数
uniform int enableMoon;
uniform vec3 moonPosition;
uniform float moonSize;
uniform float moonIntensity;
uniform int enableAurora;
uniform float auroraIntensity;
uniform float auroraDensity; // 新增：极光密度参数
uniform vec3 auroraColor;
uniform float nightIntensity;

// 颜色叠加参数
uniform vec3 colorOverlay;
uniform float colorOverlayStrength;
uniform int colorOverlayAffectsClouds;

// 太阳颜色参数
uniform vec3 sunColor;

// 配置选项
#define VOLUMETRIC_LIGHT
//#define SPHERICAL_PROJECTION

// 云配置参数
#define cloudSpeed 0.02
#define cloudHeight 1600.0
#define cloudThickness 500.0

// 雾配置参数
#define fogDensity 0.00003

// 步进参数 - 使用默认值，但实际在运行时会使用uniform变量值
#define volumetricCloudSteps (volumetricCloudStepsUniform > 0 ? volumetricCloudStepsUniform : 16)
#define volumetricLightSteps (volumetricLightStepsUniform > 0 ? volumetricLightStepsUniform : 8)
#define cloudShadowingSteps (cloudShadowingStepsUniform > 0 ? cloudShadowingStepsUniform : 12)
#define volumetricLightShadowSteps (volumetricLightShadowStepsUniform > 0 ? volumetricLightShadowStepsUniform : 4)

// 星空配置参数
#define STAR_BRIGHTNESS 1.5
#define STAR_FLICKER_SPEED 0.75

// 极光配置参数
#define AURORA_STEPS 50
#define AURORA_SPEED 0.02      // 降低动画速度
#define AURORA_HEIGHT 0.03    // 大幅降低高度移动速度
#define AURORA_OFFSET 0.006

// 散射参数
#define earthRadius 6371000.0
const float pi = 3.1415926535897932384626433832795;
const float rPi = 1.0 / pi;
const float hPi = pi * 0.5;
const float tau = pi * 2.0;
const float rLOG2 = 1.0 / log(2.0);

//////////////////////////////////////////////////////////////////

// Bayer抖动函数
float bayer2(vec2 a){
    a = floor(a);
    return fract(dot(a, vec2(.5, a.y * .75)));
}

#define bayer4(a)   (bayer2(.5*(a))*.25+bayer2(a))
#define bayer8(a)   (bayer4(.5*(a))*.25+bayer2(a))
#define bayer16(a)  (bayer8(.5*(a))*.25+bayer2(a))

// 射线球体相交
vec2 rsi(vec3 position, vec3 direction, float radius) {
    float PoD = dot(position, direction);
    float radiusSquared = radius * radius;

    float delta = PoD * PoD + radiusSquared - dot(position, position);
    if (delta < 0.0) return vec2(-1.0);
    delta = sqrt(delta);

    return -PoD + vec2(-delta, delta);
}

// 旋转矩阵
mat3 rotationMatrix(vec3 axis, float angle) {
    axis = normalize(axis);
    float s = sin(angle);
    float c = cos(angle);
    float oc = 1.0 - c;
    
    float xx = axis.x * axis.x;
    float yy = axis.y * axis.y;
    float zz = axis.z * axis.z;
    
    float xy = axis.x * axis.y;
    float xz = axis.x * axis.z;
    float zy = axis.z * axis.y;
    
    return mat3(oc * xx + c, oc * xy - axis.z * s, oc * xz + axis.y * s,
                oc * xy + axis.z * s, oc * yy + c, oc * zy - axis.x * s, 
                oc * xz - axis.y * s, oc * zy + axis.x * s, oc * zz + c);
}

// 位置结构体
struct positionStruct {
    vec2 texcoord;
    vec3 worldPosition;
    vec3 worldVector;
    vec3 sunVector;
};

vec3 sphereToCart(vec3 sphere) {
    vec2 c = cos(sphere.xy);
    vec2 s = sin(sphere.xy);
    
    return sphere.z * vec3(c.x * c.y, s.y, s.x * c.y);
}

vec3 calculateWorldSpacePosition(vec2 p) {
    p = p * 2.0 - 1.0;
    
    vec3 worldSpacePosition = vec3(p.x, p.y, 1.0);
    
    #ifdef SPHERICAL_PROJECTION
        worldSpacePosition = sphereToCart(worldSpacePosition * vec3(pi, hPi, 1.0));
    #endif
    
    return worldSpacePosition;
}

void gatherPositions(inout positionStruct pos, vec2 fragCoord, vec2 screenResolution) {
    pos.texcoord = fragCoord / screenResolution;
    
    // 设置世界位置和方向
    // 使用fov参数调整视图方向
    float fovFactor = tan(radians(fov * 0.5));
    vec2 screenPos = pos.texcoord * 2.0 - 1.0;
    vec3 viewDir = normalize(vec3(screenPos.x * resolution.x / resolution.y * fovFactor, screenPos.y * fovFactor, -1.0));
    
    vec4 worldDir = viewMatrix * vec4(viewDir, 0.0);
    pos.worldPosition = worldDir.xyz;
    pos.worldVector = normalize(pos.worldPosition);
    
    // 设置太阳向量
    pos.sunVector = normalize(sunPosition);
}

///////////////////////////////////////////////////////////////////////////////////

// 散射相关计算
#define d0(x) (abs(x) + 1e-8)
#define d02(x) (abs(x) + 1e-3)

vec3 mieCoeffVector;
vec3 rayleighCoeffVector;
vec3 totalCoeff;

void initScattering() {
    rayleighCoeffVector = vec3(0.27, 0.5, 1.0 + skyBlueness) * rayleighCoefficient * 1e-5;
    mieCoeffVector = vec3(0.5) * mieCoefficient * 1e-6;
    totalCoeff = rayleighCoeffVector + mieCoeffVector;
}

vec3 scatter(vec3 coeff, float depth){
    return coeff * depth;
}

vec3 absorb(vec3 coeff, float depth){
    return exp2(scatter(coeff, -depth));
}

float calcParticleThickness(float depth){
    depth = depth * 2.0;
    depth = max(depth + 0.01, 0.01);
    depth = 1.0 / depth;
    
    return 100000.0 * depth;   
}

float calcParticleThicknessConst(const float depth){
    return 100000.0 / max(depth * 2.0 - 0.01, 0.01);   
}

float rayleighPhase(float x){
    return 0.375 * (1.0 + x*x);
}

float hgPhase(float x, float g) {
    float g2 = g*g;
    return 0.25 * ((1.0 - g2) * pow(1.0 + g2 - 2.0*g*x, -1.5));
}

float miePhaseSky(float x, float depth) {
    return hgPhase(x, exp2(-0.000003 * depth));
}

float powder(float od) {
    return 1.0 - exp2(-od * 2.0);
}

float calculateScatterIntergral(float opticalDepth, float coeff){
    float a = -coeff * rLOG2;
    float b = -1.0 / coeff;
    float c = 1.0 / coeff;

    return exp2(a * opticalDepth) * b + c;
}

vec3 calculateScatterIntergral(float opticalDepth, vec3 coeff){
    vec3 a = -coeff * rLOG2;
    vec3 b = -1.0 / coeff;
    vec3 c = 1.0 / coeff;

    return exp2(a * opticalDepth) * b + c;
}

// 大气散射计算
vec3 calcAtmosphericScatter(positionStruct pos, out vec3 absorbLight){
    const float ln2 = log(2.0);
    
    float lDotW = dot(pos.sunVector, pos.worldVector);
    float lDotU = dot(pos.sunVector, vec3(0.0, 1.0, 0.0));
    float uDotW = dot(vec3(0.0, 1.0, 0.0), pos.worldVector);
    
    float opticalDepth = calcParticleThickness(uDotW);
    float opticalDepthLight = calcParticleThickness(lDotU);
    
    vec3 scatterView = scatter(totalCoeff, opticalDepth);
    vec3 absorbView = absorb(totalCoeff, opticalDepth);
    
    vec3 scatterLight = scatter(totalCoeff, opticalDepthLight);
    absorbLight = absorb(totalCoeff, opticalDepthLight);
        
    vec3 absorbSun = abs(absorbLight - absorbView) / d0((scatterLight - scatterView) * ln2);
    
    vec3 mieScatter = scatter(mieCoeffVector, opticalDepth) * miePhaseSky(lDotW, opticalDepth);
    vec3 rayleighScatter = scatter(rayleighCoeffVector, opticalDepth) * rayleighPhase(lDotW);
    
    vec3 scatterSun = mieScatter + rayleighScatter;
    
    vec3 sunSpot = smoothstep(0.9999, 0.99993, lDotW) * absorbView * sunBrightness;
    
    // 使用太阳高度调整散射强度，当太阳位于地平线以下时，减弱散射
    float dayFactor = smoothstep(-0.15, 0.15, lDotU);
    vec3 finalScatter = (scatterSun * absorbSun + sunSpot) * sunBrightness * intensity;
    
    // 应用太阳颜色到散射结果
    finalScatter *= sunColor;
    
    // 当太阳在地平线下，根据nightIntensity调整夜空亮度
    float nightFactor = (1.0 - dayFactor) * nightIntensity;
    finalScatter = mix(finalScatter * dayFactor, vec3(nightFactor * 0.02), 1.0 - dayFactor);
    
    return finalScatter;
}

vec3 calcAtmosphericScatterTop(positionStruct pos){
    const float ln2 = log(2.0);
    
    float lDotU = dot(pos.sunVector, vec3(0.0, 1.0, 0.0));
    
    float opticalDepth = calcParticleThicknessConst(1.0);
    float opticalDepthLight = calcParticleThickness(lDotU);
    
    vec3 scatterView = scatter(totalCoeff, opticalDepth);
    vec3 absorbView = absorb(totalCoeff, opticalDepth);
    
    vec3 scatterLight = scatter(totalCoeff, opticalDepthLight);
    vec3 absorbLight = absorb(totalCoeff, opticalDepthLight);
    
    vec3 absorbSun = d02(absorbLight - absorbView) / d02((scatterLight - scatterView) * ln2);
    
    vec3 mieScatter = scatter(mieCoeffVector, opticalDepth) * 0.25;
    vec3 rayleighScatter = scatter(rayleighCoeffVector, opticalDepth) * 0.375;
    
    vec3 scatterSun = mieScatter + rayleighScatter;
    
    // 使用太阳高度调整散射强度
    float dayFactor = smoothstep(-0.15, 0.15, lDotU);
    vec3 finalScatter = (scatterSun * absorbSun) * sunBrightness * intensity;
    
    // 应用太阳颜色到散射结果
    finalScatter *= sunColor;
    
    // 当太阳在地平线下，根据nightIntensity调整夜空亮度
    float nightFactor = (1.0 - dayFactor) * nightIntensity;
    finalScatter = mix(finalScatter * dayFactor, vec3(nightFactor * 0.02), 1.0 - dayFactor);
    
    return finalScatter;
}

// 3D噪声函数
float Get3DNoise(vec3 pos) {
    float p = floor(pos.z);
    float f = pos.z - p;
    
    const float invNoiseRes = 1.0 / 64.0;
    
    float zStretch = 17.0 * invNoiseRes;
    
    vec2 coord = pos.xy * invNoiseRes + (p * zStretch);
    
    vec2 noise = vec2(texture2D(noiseTexture, coord).x,
                      texture2D(noiseTexture, coord + zStretch).x);
    
    return mix(noise.x, noise.y, f);
}

// 云计算
float getClouds(vec3 p) {
    p = vec3(p.x, length(p + vec3(0.0, earthRadius, 0.0)) - earthRadius, p.z);
    
    if (p.y < cloudHeight || p.y > cloudHeight + cloudThickness)
        return 0.0;
    
    // 修复：使用包装的时间值避免精度问题
    float wrappedTime = fract(time * cloudSpeed * 0.01) * 100.0;
    float timeOffset = wrappedTime * float(animateClouds);
    vec3 movement = vec3(timeOffset, 0.0, timeOffset);
    
    vec3 cloudCoord = (p * 0.001 * cloudScale) + movement;
    
    float noise = Get3DNoise(cloudCoord) * 0.5;
    noise += Get3DNoise(cloudCoord * 2.0 + movement) * 0.25;
    noise += Get3DNoise(cloudCoord * 7.0 - movement) * 0.125;
    noise += Get3DNoise((cloudCoord + movement) * 16.0) * 0.0625;
    
    const float top = 0.004;
    const float bottom = 0.01;
    
    float horizonHeight = p.y - cloudHeight;
    float treshHold = (1.0 - exp2(-bottom * horizonHeight)) * exp2(-top * horizonHeight);
    
    float clouds = smoothstep(0.55 - cloudThreshold * 0.1, 0.6 + cloudThreshold * 0.1, noise);
    clouds *= treshHold;
    
    return clouds * cloudiness * 0.05 * cloudAmount;
}

// 云阴影
float getCloudShadow(vec3 p, positionStruct pos) {
    float rSteps = cloudThickness / float(volumetricLightShadowSteps) / abs(pos.sunVector.y);
    
    vec3 increment = pos.sunVector * rSteps;
    vec3 position = pos.sunVector * (cloudHeight - p.y) / pos.sunVector.y + p;
    
    float transmittance = 0.0;
    
    for (int i = 0; i < volumetricLightShadowSteps; i++, position += increment) {
        transmittance += getClouds(position);
    }
    
    return exp2(-transmittance * rSteps);
}

// 太阳可见性
float getSunVisibility(vec3 p, positionStruct pos) {
    float rSteps = cloudThickness / float(cloudShadowingSteps);
    
    vec3 increment = pos.sunVector * rSteps;
    vec3 position = increment * 0.5 + p;
    
    float transmittance = 0.0;
    
    for (int i = 0; i < cloudShadowingSteps; i++, position += increment) {
        transmittance += getClouds(position);
    }
    
    return exp2(-transmittance * rSteps);
}

// 相位函数
float phase2Lobes(float x) {
    float m = 0.6;
    float gm = mieDirectionalG;
    
    float lobe1 = hgPhase(x, 0.8 * gm);
    float lobe2 = hgPhase(x, -0.5 * gm);
    
    return mix(lobe2, lobe1, m);
}

// 体积云散射
vec3 getVolumetricCloudsScattering(float opticalDepth, float phase, vec3 p, vec3 sunColor, vec3 skyLight, positionStruct pos) {
    float intergal = calculateScatterIntergral(opticalDepth, 1.11);
    
    float beersPowder = powder(opticalDepth * log(2.0));
    
    vec3 sunlighting = (sunColor * getSunVisibility(p, pos) * beersPowder) * phase * hPi * sunBrightness;
    vec3 skylighting = skyLight * 0.25 * rPi;
    
    return (sunlighting + skylighting) * intergal * pi;
}

// 高度雾光学深度
float getHeightFogOD(float height) {
    const float falloff = 0.001;
    
    return exp2(-height * falloff) * fogDensity;
}

// 体积光散射
vec3 getVolumetricLightScattering(float opticalDepth, float phase, vec3 p, vec3 sunColor, vec3 skyLight, positionStruct pos) {
    float intergal = calculateScatterIntergral(opticalDepth, 1.11);
    
    vec3 sunlighting = sunColor * phase * hPi * sunBrightness;
    sunlighting *= getCloudShadow(p, pos);
    vec3 skylighting = skyLight * 0.25 * rPi;
    
    return (sunlighting + skylighting) * intergal * pi;
}

// 体积光计算
vec3 calculateVolumetricLight(positionStruct pos, vec3 color, float dither, vec3 sunColor) {
    #ifndef VOLUMETRIC_LIGHT
        return color;
    #endif
    
    float iSteps = 1.0 / float(volumetricLightSteps);
    
    vec3 increment = pos.worldVector * cloudHeight / clamp(pos.worldVector.y, 0.1, 1.0) * iSteps;
    vec3 rayPosition = increment * dither;
    
    float stepLength = length(increment);
    
    vec3 scattering = vec3(0.0);
    vec3 transmittance = vec3(1.0);
    
    float lDotW = dot(pos.sunVector, pos.worldVector);
    float phase = hgPhase(lDotW, 0.8);
    
    vec3 skyLight = calcAtmosphericScatterTop(pos);
    
    for (int i = 0; i < volumetricLightSteps; i++, rayPosition += increment) {
        float opticalDepth = getHeightFogOD(rayPosition.y) * stepLength;
        
        if (opticalDepth <= 0.0)
            continue;
        
        scattering += getVolumetricLightScattering(opticalDepth, phase, rayPosition, sunColor, skyLight, pos) * transmittance;
        transmittance *= exp2(-opticalDepth);
    }
    
    return color * transmittance + scattering;
}

// 体积云计算
vec3 calculateVolumetricClouds(positionStruct pos, vec3 color, float dither, vec3 sunColor) {
    if (pos.worldVector.y < 0.0)
        return color;
    
    float iSteps = 1.0 / float(volumetricCloudSteps);
    
    float bottomSphere = rsi(vec3(0.0, 1.0, 0.0) * earthRadius, pos.worldVector, earthRadius + cloudHeight).y;
    float topSphere = rsi(vec3(0.0, 1.0, 0.0) * earthRadius, pos.worldVector, earthRadius + cloudHeight + cloudThickness).y;
    
    if (bottomSphere < 0.0 || topSphere < 0.0)
        return color;
    
    vec3 startPosition = pos.worldVector * bottomSphere;
    vec3 endPosition = pos.worldVector * topSphere;
    
    vec3 increment = (endPosition - startPosition) * iSteps;
    vec3 cloudPosition = increment * dither + startPosition;
    
    float stepLength = length(increment);
    
    vec3 scattering = vec3(0.0);
    float transmittance = 1.0;
    
    float lDotW = dot(pos.sunVector, pos.worldVector);
    float phase = phase2Lobes(lDotW);
    
    vec3 skyLight = calcAtmosphericScatterTop(pos);
    
    for (int i = 0; i < volumetricCloudSteps; i++, cloudPosition += increment) {
        float opticalDepth = getClouds(cloudPosition) * stepLength;
        
        if (opticalDepth <= 0.0)
            continue;
        
        scattering += getVolumetricCloudsScattering(opticalDepth, phase, cloudPosition, sunColor, skyLight, pos) * transmittance;
        transmittance *= exp2(-opticalDepth);
    }
    
    return mix(color * transmittance + scattering, color, clamp(length(startPosition) * 0.00001, 0.0, 1.0));
}

// -------- Shadertoy Aurora & Stars 实现 --------
// 以下代码直接从shadertoy移植，保持原始实现以确保视觉效果一致

// 从shadertoy移植的矩阵函数
mat2 mm2(in float a){
    float c = cos(a), s = sin(a);
    return mat2(c,s,-s,c);
}
mat2 m2 = mat2(0.95534, 0.29552, -0.29552, 0.95534);

// 三角波函数
float tri(in float x){
    return clamp(abs(fract(x)-.5),0.01,0.49);
}

// 二维三角波
vec2 tri2(in vec2 p){
    return vec2(tri(p.x)+tri(p.y),tri(p.y+tri(p.x)));
}

// 三角噪声2D - 修复精度问题
float triNoise2d(in vec2 p, float spd)
{
    float z=1.8;
    float z2=2.5;
    float rz = 0.;
    p *= mm2(p.x*0.06);
    vec2 bp = p;
    
    // 修复：使用fract保持时间值在合理范围内，避免精度丢失
    float wrappedTime = fract(time * spd * 0.1) * 10.0;
    
    for (float i=0.; i<5.; i++ )
    {
        vec2 dg = tri2(bp*1.85)*.75;
        dg *= mm2(wrappedTime);
        p -= dg/z2;

        bp *= 1.3;
        z2 *= .45;
        z *= .42;
        p *= 1.21 + (rz-1.0)*.02;
        
        rz += tri(p.x+tri(p.y))*z;
        p*= -m2;
    }
    return clamp(1./pow(rz*29., 1.3),0.,.55);
}

// 更好的哈希函数 - 从shadertoy移植
float hash21(in vec2 n){ 
    return fract(sin(dot(n, vec2(12.9898, 4.1414))) * 43758.5453); 
}

vec3 nmzHash33(vec3 q)
{
    uvec3 p = uvec3(ivec3(q));
    p = p*uvec3(374761393U, 1103515245U, 668265263U) + p.zxy + p.yzx;
    p = p.yzx*(p.zxy^(p >> 3U));
    return vec3(p^(p >> 16U))*(1.0/vec3(0xffffffffU));
}

// 星星生成 - 修复精度问题
vec3 stars(in vec3 p)
{
    // 修复：使用包装的时间值避免精度问题
    float wrappedTime = fract(time * cloudSpeed * 0.002) * 500.0;
    float timeOffset = wrappedTime * 0.2 * float(animateClouds);
    vec3 starMovement = vec3(timeOffset, 0.0, timeOffset);
    
    vec3 c = vec3(0.);
    float res = resolution.x;
    
    for (float i=0.;i<4.;i++)
    {
        // 添加移动到星星位置
        vec3 adjustedP = p + starMovement * (i + 1.0) * 0.02;
        
        vec3 q = fract(adjustedP*(.15*res*starDensity))-0.5;
        vec3 id = floor(adjustedP*(.15*res*starDensity));
        vec2 rn = nmzHash33(id).xy;
        float c2 = 1.-smoothstep(0.,.6*starSize,length(q));
        c2 *= step(rn.x,.0005+i*i*0.001*starDensity);
        c += c2*(mix(vec3(1.0,0.49,0.1),vec3(0.75,0.9,1.),rn.y)*0.1+0.9)*starIntensity;
        p *= 1.3;
    }
    return c*c*.8;
}

// 天空背景 - 从shadertoy移植
vec3 bg(in vec3 rd)
{
    float sd = dot(normalize(vec3(-0.5, -0.6, 0.9)), rd)*0.5+0.5;
    sd = pow(sd, 5.);
    vec3 col = mix(vec3(0.05,0.1,0.2), vec3(0.1,0.05,0.2), sd);
    return col*.63*nightIntensity;
}

// 极光生成 - 从shadertoy直接移植，增加密度控制
vec4 aurora(vec3 ro, vec3 rd)
{
    // 修复：使用包装的时间值避免精度问题
    float wrappedTime = fract(time * cloudSpeed * 0.01) * 100.0;
    float timeOffset = wrappedTime * float(animateClouds);
    vec2 cloudMovement = vec2(timeOffset, timeOffset);
    
    vec4 col = vec4(0);
    vec4 avgCol = vec4(0);
    
    // 调整步进数量基于密度
    float stepCount = 50.0 * auroraDensity;
    
    for(float i=0.;i<stepCount;i++)
    {
        float of = 0.006*hash21(gl_FragCoord.xy)*smoothstep(0.,15., i);
        float pt = ((.8+pow(i,1.4)*.002)-ro.y)/(rd.y*2.+0.4);
        pt -= of;
        vec3 bpos = ro + pt*rd;
        
        // 添加云层一致的移动
        vec2 p = bpos.zx + cloudMovement;
        
        float rzt = triNoise2d(p, 0.06);
        vec4 col2 = vec4(0,0,0, rzt);
        col2.rgb = (sin(1.-auroraColor+i*0.043)*0.5+0.5)*rzt;
        avgCol =  mix(avgCol, col2, .5);
        col += avgCol*exp2(-i*0.065 - 2.5)*smoothstep(0.,5., i);
    }
    
    col *= (clamp(rd.y*15.+.4,0.,1.));
    
    // 应用密度调整和亮度控制，但不包括nightIntensity，我们会在应用极光时加入nightIntensity
    float densityFactor = 1.0 / max(auroraDensity, 0.1);
    col *= 1.8 * auroraIntensity * densityFactor;
    
    return col;
}

// 从shadertoy移植的月亮生成函数
vec3 generateMoon(positionStruct pos) {
    if (enableMoon == 0) return vec3(0.0);
    
    // 月亮方向
    vec3 moonDir = normalize(moonPosition);
    
    // 计算视线方向与月亮方向的夹角余弦值
    float cosAngle = dot(pos.worldVector, moonDir);
    
    // 月亮的大小和亮度
    float moonRadius = moonSize;
    float moonGlow = smoothstep(0.9995, 0.9999, cosAngle) * moonIntensity;
    
    // 月亮的主体
    float moonDisk = smoothstep(cos(moonRadius), cos(moonRadius * 0.98), cosAngle);
    
    // 月球表面细节
    vec3 moonColor = vec3(1.0, 0.98, 0.9);  // 略带黄色的月亮
    
    // 月晕效果
    float halo = 0.5 * pow(max(0.0, cosAngle), 64.0) * moonIntensity;
    vec3 haloColor = vec3(0.8, 0.9, 1.0);  // 蓝白色月晕
    
    return moonColor * moonDisk * moonIntensity + haloColor * halo;
}

// 色调映射
vec3 robobo1221Tonemap(vec3 color) {
    #define rTOperator(x) (x / sqrt(x*x+1.0))
    
    float l = length(color);
    
    color = mix(color, color * 0.5, l / (l+1.0));
    color = rTOperator(color);
    
    return color;
}

// 主函数
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
    positionStruct pos;
    gatherPositions(pos, uv * resolution, resolution);
    
    float dither = bayer16(uv * resolution);
    
    // 初始化散射参数
    initScattering();
    
    vec3 lightAbsorb;
    
    vec3 color = vec3(0.0);
    color = calcAtmosphericScatter(pos, lightAbsorb);
    
    // 根据开关决定是否在早期阶段对整体天空色调进行调整
    if (colorOverlayStrength > 0.0 && colorOverlayAffectsClouds == 1) {
        vec3 colorTint = colorOverlay;
        
        // 使用温和的色调调整，影响整个天空光照系统（包括云层）
        vec3 tintedColor = color * colorTint;
        float luminance = dot(color, vec3(0.299, 0.587, 0.114));
        vec3 tintedColor2 = normalize(colorTint) * luminance * length(colorTint);
        vec3 finalTint = mix(tintedColor, tintedColor2, 0.4);
        
        // 温和地调整整体天空颜色，强度降低避免过度
        color = mix(color, finalTint, colorOverlayStrength * 0.4);
    }
    
    // 检测是否应该渲染夜空场景
    float sunHeight = dot(pos.sunVector, vec3(0.0, 1.0, 0.0));
    float isNight = smoothstep(0.1, -0.1, sunHeight);
    
    // 计算云层遮挡效果（预计算）
    float cloudOcclusion = 1.0;
    if (pos.worldVector.y > 0.0) {
        float bottomSphere = rsi(vec3(0.0, 1.0, 0.0) * earthRadius, pos.worldVector, earthRadius + cloudHeight).y;
        float topSphere = rsi(vec3(0.0, 1.0, 0.0) * earthRadius, pos.worldVector, earthRadius + cloudHeight + cloudThickness).y;
        
        if (bottomSphere > 0.0 && topSphere > 0.0) {
            vec3 startPosition = pos.worldVector * bottomSphere;
            vec3 endPosition = pos.worldVector * topSphere;
            vec3 cloudPosition = startPosition;
            
            float stepLength = length(endPosition - startPosition) / 8.0; // 使用较少的步数检测云
            vec3 increment = pos.worldVector * stepLength;
            
            for (int i = 0; i < 8; i++, cloudPosition += increment) {
                float cloudDensity = getClouds(cloudPosition);
                if (cloudDensity > 0.01) {
                    cloudOcclusion = 0.0;
                    break;
                }
            }
        }
    }
    
    // 夜空渲染 - 完全按照原始Shadertoy实现
    if (isNight > 0.0) {
        // 准备光线参数 - 完全匹配Shadertoy
        vec3 ro = vec3(0.0, 0.0, -6.7); // 光线原点
        vec3 rd = pos.worldVector;       // 光线方向
        
        // 应用与Shadertoy相同的视角旋转
        vec2 mo = vec2(-0.1, 0.1);      // 模拟Shadertoy中的默认鼠标位置
        rd.yz *= mm2(mo.y);              // 旋转Y-Z平面
        rd.xz *= mm2(mo.x + sin(fract(time*0.005)*2.0*pi)*0.2); // 修复：使用包装时间避免精度问题
        
        vec3 col = vec3(0.0);
        vec3 brd = rd;
        float fade = smoothstep(0.0, 0.01, abs(brd.y))*0.1+0.9;
        
        // 计算背景
        col = bg(rd)*fade;
        
        // 移除水面反射，所有方向都使用相同的天空渲染
        // 极光效果仅当启用时计算
        if (enableAurora == 1) {
            // 应用极光，并将nightIntensity应用到极光亮度上
            vec4 aur = smoothstep(0.0, 1.5, aurora(ro, rd))*fade*nightIntensity;
            // 混合极光 - 注意这里使用的是aur.a
            col = col*(1.0-aur.a) + aur.rgb;
        }
        
        // 添加星星
        if (enableStars == 1) {
            col += stars(rd)*cloudOcclusion;
        }
        
        // 添加月亮
        if (enableMoon == 1) {
            col += generateMoon(pos)*cloudOcclusion;
        }
        
        // 混合夜空和白天天空
        color = mix(color, col, isNight);
    }
    
    // 保存原始天空颜色
    vec3 originalSkyColor = color;
    
    // 计算云层和体积光
    color = calculateVolumetricClouds(pos, color, dither, lightAbsorb);
    color = calculateVolumetricLight(pos, color, dither, lightAbsorb);
    
    // 优化：统一计算天空遮罩，避免重复
    if (colorOverlayStrength > 0.0) {
        // 统一计算云层影响度和天空遮罩
        vec3 colorChange = color - originalSkyColor;
        float cloudInfluence = length(colorChange) / (length(originalSkyColor) + 0.001);
        float skyMask = 1.0 - clamp(cloudInfluence * 3.0, 0.0, 1.0);
        
        // 预计算颜色叠加结果
        vec3 colorTint = colorOverlay;
        vec3 tintedColor = originalSkyColor * colorTint;
        float luminance = dot(originalSkyColor, vec3(0.299, 0.587, 0.114));
        vec3 tintedColor2 = normalize(colorTint) * luminance * length(colorTint);
        vec3 finalTint = mix(tintedColor, tintedColor2, 0.3);
        
        if (colorOverlayAffectsClouds == 0) {
            // 不影响云层：只对纯天空区域应用
            if (skyMask > 0.05) {
                vec3 tintedSky = mix(originalSkyColor, finalTint, colorOverlayStrength);
                vec3 skyTintDiff = tintedSky - originalSkyColor;
                color += skyTintDiff * skyMask;
            }
        } else {
            // 影响云层：对纯天空区域额外强化
            if (skyMask > 0.3) {
                vec3 tintedSky = mix(originalSkyColor, finalTint, colorOverlayStrength * 0.6);
                vec3 skyTintDiff = tintedSky - originalSkyColor;
                color += skyTintDiff * skyMask * skyMask;
            }
        }
    }
    
    color = robobo1221Tonemap(color * 0.5);
    color = pow(color, vec3(1.0 / 2.2)); // gamma校正
    
    // 使用深度缓冲决定是否绘制天空
    #ifdef USE_DEPTH
        // 读取深度值
        float depth = texture2D(depthBuffer, uv).r;
        // 在这里我们仅在深度值为1.0（远平面）的地方绘制天空
        float skyMask = step(0.9999, depth);
        // 使用透明度为 skyMask 的方式进行混合
        outputColor = mix(inputColor, vec4(color, 1.0), skyMask);
    #else
        // 如果没有深度信息，则按照固定透明度混合
        outputColor = mix(inputColor, vec4(color, 1.0), 0.9);
    #endif
} `;
class Pr {
  /**
      * 计算太阳位置
      * @param {Object} sunParams - 太阳高度角和方位角参数
      * @param {Number} sunParams.elevation - 高度角（度）
      * @param {Number} sunParams.azimuth - 方位角（度）
      * @returns {Vector3} 归一化的太阳位置向量
      */
  static calculateSunPosition(e) {
    const t = Ee.degToRad(90 - e.elevation), r = Ee.degToRad(e.azimuth), n = Math.sin(t) * Math.cos(r), i = Math.cos(t), s = Math.sin(t) * Math.sin(r);
    return new T(n, i, s).normalize();
  }
  /**
      * 计算月亮位置
      * @param {Object} moonParams - 月亮高度角和方位角参数
      * @param {Number} moonParams.elevation - 高度角（度）
      * @param {Number} moonParams.azimuth - 方位角（度）
      * @param {Vector3} [moonOffset=new Vector3(0,0,0)] - 月亮位置偏移量（弧度）
      * @returns {Vector3} 归一化的月亮位置向量
      */
  static calculateMoonPosition(e, t = new T(0, 0, 0)) {
    const r = Ee.degToRad(90 - e.elevation), n = Ee.degToRad(e.azimuth), i = Math.sin(r) * Math.cos(n), s = Math.cos(r), o = Math.sin(r) * Math.sin(n), l = new T(i, s, o);
    if (t && (t.x !== 0 || t.y !== 0 || t.z !== 0)) {
      const u = new C().makeRotationX(t.x), f = new C().makeRotationY(t.y), h = new C().makeRotationZ(t.z);
      l.applyMatrix4(u), l.applyMatrix4(f), l.applyMatrix4(h);
    }
    return l.normalize();
  }
  /**
      * 从太阳位置计算对应的月亮位置（反向）
      * @param {Vector3} sunPosition - 太阳位置向量
      * @param {Vector3} [moonOffset=new Vector3(0,0,0)] - 月亮位置偏移量（弧度）
      * @returns {Vector3} 月亮位置向量
      */
  static calculateMoonPositionFromSun(e, t = new T(0, 0, 0)) {
    const r = e.x, n = e.y, i = e.z, s = n < 0 ? Math.abs(n) : -n, o = new T(
      r,
      s,
      i
    );
    if (t && (t.x !== 0 || t.y !== 0 || t.z !== 0)) {
      const l = new C().makeRotationX(t.x), u = new C().makeRotationY(t.y), f = new C().makeRotationZ(t.z);
      o.applyMatrix4(l), o.applyMatrix4(u), o.applyMatrix4(f);
    }
    return o.normalize();
  }
  /**
      * 计算一年中的天数
      * @param {Number} year - 年
      * @param {Number} month - 月
      * @param {Number} day - 日
      * @return {Number} 一年中的天数（1-366）
      */
  static getDayOfYear(e, t, r) {
    const i = [0, 31, e % 4 === 0 && e % 100 !== 0 || e % 400 === 0 ? 29 : 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31];
    let s = r;
    for (let o = 1; o < t; o++)
      s += i[o];
    return s;
  }
  /**
      * 根据日期计算太阳位置
      * 使用简化的天文算法计算太阳位置
      * @param {Date} date - 日期时间对象
      * @param {Number} latitude - 纬度（度）
      * @param {Number} longitude - 经度（度）
      * @return {Vector3} 太阳位置方向向量
      */
  static calculateSunPositionFromDate(e, t, r) {
    const n = t * Math.PI / 180, i = e.getFullYear(), s = e.getMonth() + 1, o = e.getDate(), l = e.getHours(), u = e.getMinutes(), f = e.getSeconds(), h = this.getDayOfYear(i, s, o), m = l + u / 60 + f / 3600, p = 0.4093 * Math.sin(2 * Math.PI * (h - 81) / 365), d = (12 - m) * 15 * Math.PI / 180, v = Math.sin(n) * Math.sin(p) + Math.cos(n) * Math.cos(p) * Math.cos(d), x = Math.asin(v), w = Math.cos(p) * Math.sin(d) / Math.cos(x), M = (Math.sin(p) - Math.sin(n) * v) / (Math.cos(n) * Math.cos(x));
    let D = Math.atan2(w, M);
    D < 0 && (D += 2 * Math.PI);
    const P = Math.cos(x) * Math.sin(D), O = Math.sin(x), S = Math.cos(x) * Math.cos(D);
    return new T(P, O, S).normalize();
  }
  /**
      * 获取太阳位置的高度角和方位角
      * @param {Vector3} sunPosition - 太阳位置向量
      * @returns {Object} 包含elevation（高度角）和azimuth（方位角）的对象
      */
  static getSunAngles(e) {
    const t = e.clone().normalize();
    Math.asin(t.y);
    let r = Math.atan2(t.x, t.z);
    return r < 0 && (r += 2 * Math.PI), {
      elevation: 90 - Ee.radToDeg(Math.acos(t.y)),
      // 高度角
      azimuth: Ee.radToDeg(r)
      // 方位角
    };
  }
  /**
      * 根据太阳高度自动调整场景参数
      * @param {Object} effect - 天空大气效果实例
      * @param {Number} sunHeight - 太阳高度（-1到1之间）
      */
  static autoAdjustParameters(e, t) {
    t > 0.1 ? (e.enableStars = !1, e.enableMoon = !1, e.enableAurora = !1, t > 0.5 ? e.sunBrightness = 3 + (t - 0.5) * 2 : e.sunBrightness = 3) : t > -0.1 ? (e.enableStars = t < 0.05, e.enableMoon = t < 0.02, e.enableAurora = !1, e.sunBrightness = 4.5) : (e.enableStars = !0, e.enableMoon = !0, e.enableAurora = t < -0.3, e.sunBrightness = 2 + (t + 0.1) * 5);
  }
  /**
      * 应用白天预设
      * @param {Object} effect - 天空大气效果实例
      * @param {Object} sunParams - 太阳参数对象，包含elevation和azimuth属性
      */
  static applyDaytimePreset(e, t) {
    return t.elevation = 45, t.azimuth = 180, e.enableStars = !1, e.enableMoon = !1, e.enableAurora = !1, e.intensity = 12, e.skyBlueness = 0.5, e.sunBrightness = 3, this.calculateSunPosition(t);
  }
  /**
      * 应用日落预设
      * @param {Object} effect - 天空大气效果实例
      * @param {Object} sunParams - 太阳参数对象，包含elevation和azimuth属性
      */
  static applySunsetPreset(e, t) {
    return t.elevation = 2, t.azimuth = 260, e.enableStars = !1, e.enableMoon = !1, e.enableAurora = !1, e.intensity = 15, e.skyBlueness = 0.3, e.sunBrightness = 4.5, this.calculateSunPosition(t);
  }
  /**
      * 应用夜晚预设
      * @param {Object} effect - 天空大气效果实例
      * @param {Object} sunParams - 太阳参数对象，包含elevation和azimuth属性
      * @param {Object} moonParams - 月亮参数对象，包含elevation和azimuth属性
      */
  static applyNightPreset(e, t, r) {
    return t.elevation = -5, t.azimuth = 180, r.elevation = 30, r.azimuth = 45, e.enableStars = !0, e.enableMoon = !0, e.enableAurora = !1, e.intensity = 12, e.nightIntensity = 0.2, e.starIntensity = 1.5, e.moonIntensity = 1.2, e.sunBrightness = 2, {
      sunPosition: this.calculateSunPosition(t),
      moonPosition: this.calculateMoonPosition(r)
    };
  }
  /**
      * 应用极光预设
      * @param {Object} effect - 天空大气效果实例
      * @param {Object} sunParams - 太阳参数对象，包含elevation和azimuth属性
      * @param {Object} moonParams - 月亮参数对象，包含elevation和azimuth属性
      * @param {Object} auroraParams - 极光参数对象，包含red、green和blue属性
      */
  static applyAuroraPreset(e, t, r, n) {
    return t.elevation = -8, t.azimuth = 180, r.elevation = 20, r.azimuth = 35, n.red = 2.15, n.green = -1, n.blue = 1, e.auroraColor.set(n.red, n.green, n.blue), e.enableStars = !0, e.enableMoon = !0, e.enableAurora = !0, e.intensity = 12, e.nightIntensity = 0.15, e.starIntensity = 1.2, e.moonIntensity = 0.8, e.auroraIntensity = 1.5, e.auroraDensity = 1, e.sunBrightness = 1.5, {
      sunPosition: this.calculateSunPosition(t),
      moonPosition: this.calculateMoonPosition(r)
    };
  }
}
class fc extends I {
  /**
   * 构造函数
   *
   * @param {Object} [options] - 效果选项
   * @param {BlendFunction} [options.blendFunction=BlendFunction.SCREEN] - 混合模式
   * @param {Vector3} [options.sunPosition=new Vector3(0.1, 0.05, -1).normalize()] - 太阳位置（方向向量）
   * @param {Boolean} [options.animateClouds=true] - 是否启用云层动画
   * @param {Number} [options.intensity=12.0] - 散射强度
   * @param {Number} [options.sunBrightness=3.0] - 太阳亮度
   * @param {Number} [options.rayleighCoefficient=1.0] - 瑞利散射系数
   * @param {Number} [options.mieCoefficient=0.8] - 米氏散射系数
   * @param {Number} [options.mieDirectionalG=0.8] - 米氏散射方向性参数（0-1）
   * @param {Number} [options.cloudiness=1.0] - 云层密度
   * @param {Number} [options.fov=60.0] - 视场角（度）
   * @param {Number} [options.skyBlueness=0.5] - 天空蓝度增强
   * @param {Number} [options.cloudAmount=1.0] - 云层数量
   * @param {Number} [options.cloudScale=1.0] - 云层尺度
   * @param {Number} [options.cloudThreshold=0.0] - 云层阈值
   * @param {Number} [options.volumetricCloudSteps=16] - 体积云采样步数
   * @param {Number} [options.volumetricLightSteps=8] - 体积光采样步数
   * @param {Number} [options.cloudShadowingSteps=12] - 云阴影采样步数
   * @param {Number} [options.volumetricLightShadowSteps=4] - 体积光阴影采样步数
   * @param {Boolean} [options.enableStars=false] - 是否启用星空
   * @param {Number} [options.starIntensity=1.0] - 星星亮度
   * @param {Number} [options.starDensity=0.5] - 星星密度
   * @param {Number} [options.starSize=1.0] - 星星大小
   * @param {Boolean} [options.enableMoon=false] - 是否启用月亮
   * @param {Vector3} [options.moonPosition=new Vector3(-0.1, 0.05, -1).normalize()] - 月亮位置（方向向量）
   * @param {Number} [options.moonSize=0.02] - 月亮大小
   * @param {Number} [options.moonIntensity=1.0] - 月亮亮度
   * @param {Boolean} [options.enableAurora=false] - 是否启用极光
   * @param {Number} [options.auroraIntensity=1.5] - 极光强度
   * @param {Number} [options.auroraDensity=1.0] - 极光密度
   * @param {Vector3} [options.auroraColor=new Vector3(2.15, -1.0, 1.0)] - 极光颜色 (R,G,B)系数
   * @param {Number} [options.nightIntensity=0.2] - 夜晚强度（值越小，夜空越暗）
   * @param {Boolean} [options.autoUpdateMoon=true] - 是否根据太阳位置自动更新月亮位置
   * @param {Vector3} [options.moonOffset=new Vector3(0, 0, 0)] - 月亮位置偏移量（弧度）
   * @param {Number} [options.resolutionScale=1.0] - 分辨率缩放比例（0-1）
   * @param {Vector3} [options.colorOverlay=new Vector3(1.0, 1.0, 1.0)] - 颜色叠加（RGB系数，用于调整天空整体色调）
   * @param {Number} [options.colorOverlayStrength=0.0] - 颜色叠加强度（0-1，0表示无叠加）
   * @param {Boolean} [options.colorOverlayAffectsClouds=false] - 颜色叠加是否影响云层
   * @param {Vector3} [options.sunColor=new Vector3(1.0, 1.0, 1.0)] - 太阳基础颜色（RGB系数）
   */
  constructor({
    blendFunction: e = E.SCREEN,
    sunPosition: t = new T(0.1, 0.05, -1).normalize(),
    animateClouds: r = !0,
    intensity: n = 12,
    sunBrightness: i = 3,
    rayleighCoefficient: s = 1,
    mieCoefficient: o = 0.8,
    mieDirectionalG: l = 0.8,
    cloudiness: u = 1,
    fov: f = 60,
    skyBlueness: h = 0.5,
    cloudAmount: m = 1,
    cloudScale: p = 1,
    cloudThreshold: d = 0,
    volumetricCloudSteps: v = 12,
    volumetricLightSteps: x = 4,
    cloudShadowingSteps: w = 4,
    volumetricLightShadowSteps: M = 1,
    enableStars: D = !1,
    starIntensity: P = 1,
    starDensity: O = 0.5,
    starSize: S = 1,
    enableMoon: L = !1,
    moonPosition: H = new T(-0.1, 0.05, -1).normalize(),
    moonSize: Z = 0.02,
    moonIntensity: z = 1,
    enableAurora: k = !1,
    auroraIntensity: R = 1.5,
    auroraDensity: U = 1,
    auroraColor: q = new T(2.15, -1, 1),
    nightIntensity: ie = 0.2,
    autoUpdateMoon: J = !0,
    moonOffset: Pe = new T(0, 0, 0),
    resolutionScale: yr = 1,
    colorOverlay: Tt = new T(1, 1, 1),
    colorOverlayStrength: dn = 0,
    colorOverlayAffectsClouds: mn = !1,
    sunColor: pn = new T(1, 1, 1)
  } = {}) {
    super("SkyAtmosphereEffect", uc, {
      blendFunction: e,
      attributes: Q.DEPTH,
      defines: /* @__PURE__ */ new Map([
        ["USE_DEPTH", "1"]
      ]),
      uniforms: /* @__PURE__ */ new Map([
        ["sunPosition", new a(t)],
        ["intensity", new a(n)],
        ["sunBrightness", new a(i)],
        ["animateClouds", new a(r ? 1 : 0)],
        ["time", new a(0)],
        ["resolution", new a(new g(1, 1))],
        ["noiseTexture", new a(null)],
        ["rayleighCoefficient", new a(s)],
        ["mieCoefficient", new a(o)],
        ["mieDirectionalG", new a(l)],
        ["cloudiness", new a(u)],
        ["viewMatrix", new a(new C())],
        ["fov", new a(f)],
        ["skyBlueness", new a(h)],
        ["cloudAmount", new a(m)],
        ["cloudScale", new a(p)],
        ["cloudThreshold", new a(d)],
        ["volumetricCloudStepsUniform", new a(v)],
        ["volumetricLightStepsUniform", new a(x)],
        ["cloudShadowingStepsUniform", new a(w)],
        ["volumetricLightShadowStepsUniform", new a(M)],
        ["enableStars", new a(D ? 1 : 0)],
        ["starIntensity", new a(P)],
        ["starDensity", new a(O)],
        ["starSize", new a(S)],
        ["enableMoon", new a(L ? 1 : 0)],
        ["moonPosition", new a(H)],
        ["moonSize", new a(Z)],
        ["moonIntensity", new a(z)],
        ["enableAurora", new a(k ? 1 : 0)],
        ["auroraIntensity", new a(R)],
        ["auroraDensity", new a(U)],
        ["auroraColor", new a(q)],
        ["nightIntensity", new a(ie)],
        ["colorOverlay", new a(Tt)],
        ["colorOverlayStrength", new a(dn)],
        ["colorOverlayAffectsClouds", new a(mn ? 1 : 0)],
        ["sunColor", new a(pn)]
      ])
    }), this.sunPosition = t, this.animateClouds = r, this.intensity = n, this._sunBrightness = i, this.rayleighCoefficient = s, this.mieCoefficient = o, this.mieDirectionalG = l, this.cloudiness = u, this._fov = f, this._skyBlueness = h, this._cloudAmount = m, this._cloudScale = p, this._cloudThreshold = d, this._volumetricCloudSteps = v, this._volumetricLightSteps = x, this._cloudShadowingSteps = w, this._volumetricLightShadowSteps = M, this._camera = null, this._enableStars = D, this._starIntensity = P, this._starDensity = O, this._starSize = S, this._enableMoon = L, this.moonPosition = H, this._moonSize = Z, this._moonIntensity = z, this._enableAurora = k, this._auroraIntensity = R, this.auroraColor = q, this._nightIntensity = ie, this._auroraDensity = U, this._autoUpdateMoon = J, this.moonOffset = Pe.clone(), this._resolutionScale = Math.max(0.1, Math.min(1, yr)), this.colorOverlay = Tt.clone(), this._colorOverlayStrength = Math.max(0, Math.min(1, dn)), this._colorOverlayAffectsClouds = mn, this.sunColor = pn.clone(), this._autoUpdateMoon && this.calculateMoonPosition(), this.createDefaultNoiseTexture();
  }
  /**
   * 获取视场角
   * @return {Number} 视场角（度）
   */
  get fov() {
    return this._fov;
  }
  /**
   * 设置视场角
   * @param {Number} value - 视场角（度）
   */
  set fov(e) {
    this._fov = e, this.uniforms.get("fov").value = e;
  }
  /**
   * 获取天空蓝度增强值
   * @return {Number} 蓝度增强值
   */
  get skyBlueness() {
    return this._skyBlueness;
  }
  /**
   * 设置天空蓝度增强值
   * @param {Number} value - 蓝度增强值
   */
  set skyBlueness(e) {
    this._skyBlueness = e, this.uniforms.get("skyBlueness").value = e;
  }
  /**
   * 获取云层数量
   * @return {Number} 云层数量
   */
  get cloudAmount() {
    return this._cloudAmount;
  }
  /**
   * 设置云层数量
   * @param {Number} value - 云层数量
   */
  set cloudAmount(e) {
    this._cloudAmount = e, this.uniforms.get("cloudAmount").value = e;
  }
  /**
   * 获取云层尺度
   * @return {Number} 云层尺度
   */
  get cloudScale() {
    return this._cloudScale;
  }
  /**
   * 设置云层尺度
   * @param {Number} value - 云层尺度
   */
  set cloudScale(e) {
    this._cloudScale = e, this.uniforms.get("cloudScale").value = e;
  }
  /**
   * 获取云层阈值
   * @return {Number} 云层阈值
   */
  get cloudThreshold() {
    return this._cloudThreshold;
  }
  /**
   * 设置云层阈值
   * @param {Number} value - 云层阈值
   */
  set cloudThreshold(e) {
    this._cloudThreshold = e, this.uniforms.get("cloudThreshold").value = e;
  }
  /**
   * 获取体积云采样步数
   * @return {Number} 体积云采样步数
   */
  get volumetricCloudSteps() {
    return this._volumetricCloudSteps;
  }
  /**
   * 设置体积云采样步数
   * @param {Number} value - 体积云采样步数
   */
  set volumetricCloudSteps(e) {
    this._volumetricCloudSteps = Math.max(1, Math.floor(e)), this.uniforms.get("volumetricCloudStepsUniform").value = this._volumetricCloudSteps;
  }
  /**
   * 获取体积光采样步数
   * @return {Number} 体积光采样步数
   */
  get volumetricLightSteps() {
    return this._volumetricLightSteps;
  }
  /**
   * 设置体积光采样步数
   * @param {Number} value - 体积光采样步数
   */
  set volumetricLightSteps(e) {
    this._volumetricLightSteps = Math.max(1, Math.floor(e)), this.uniforms.get("volumetricLightStepsUniform").value = this._volumetricLightSteps;
  }
  /**
   * 获取云阴影采样步数
   * @return {Number} 云阴影采样步数
   */
  get cloudShadowingSteps() {
    return this._cloudShadowingSteps;
  }
  /**
   * 设置云阴影采样步数
   * @param {Number} value - 云阴影采样步数
   */
  set cloudShadowingSteps(e) {
    this._cloudShadowingSteps = Math.max(1, Math.floor(e)), this.uniforms.get("cloudShadowingStepsUniform").value = this._cloudShadowingSteps;
  }
  /**
   * 获取体积光阴影采样步数
   * @return {Number} 体积光阴影采样步数
   */
  get volumetricLightShadowSteps() {
    return this._volumetricLightShadowSteps;
  }
  /**
   * 设置体积光阴影采样步数
   * @param {Number} value - 体积光阴影采样步数
   */
  set volumetricLightShadowSteps(e) {
    this._volumetricLightShadowSteps = Math.max(1, Math.floor(e)), this.uniforms.get("volumetricLightShadowStepsUniform").value = this._volumetricLightShadowSteps;
  }
  /**
   * 获取星空启用状态
   * @return {Boolean} 是否启用星空
   */
  get enableStars() {
    return this._enableStars;
  }
  /**
   * 设置星空启用状态
   * @param {Boolean} value - 是否启用星空
   */
  set enableStars(e) {
    this._enableStars = e, this.uniforms.get("enableStars").value = e ? 1 : 0;
  }
  /**
   * 获取星星亮度
   * @return {Number} 星星亮度
   */
  get starIntensity() {
    return this._starIntensity;
  }
  /**
   * 设置星星亮度
   * @param {Number} value - 星星亮度
   */
  set starIntensity(e) {
    this._starIntensity = e, this.uniforms.get("starIntensity").value = e;
  }
  /**
   * 获取星星密度
   * @return {Number} 星星密度
   */
  get starDensity() {
    return this._starDensity;
  }
  /**
   * 设置星星密度
   * @param {Number} value - 星星密度
   */
  set starDensity(e) {
    this._starDensity = e, this.uniforms.get("starDensity").value = e;
  }
  /**
   * 获取星星大小
   * @return {Number} 星星大小
   */
  get starSize() {
    return this._starSize;
  }
  /**
   * 设置星星大小
   * @param {Number} value - 星星大小
   */
  set starSize(e) {
    this._starSize = e, this.uniforms.get("starSize").value = e;
  }
  /**
   * 获取月亮启用状态
   * @return {Boolean} 是否启用月亮
   */
  get enableMoon() {
    return this._enableMoon;
  }
  /**
   * 设置月亮启用状态
   * @param {Boolean} value - 是否启用月亮
   */
  set enableMoon(e) {
    this._enableMoon = e, this.uniforms.get("enableMoon").value = e ? 1 : 0;
  }
  /**
   * 获取月亮大小
   * @return {Number} 月亮大小
   */
  get moonSize() {
    return this._moonSize;
  }
  /**
   * 设置月亮大小
   * @param {Number} value - 月亮大小
   */
  set moonSize(e) {
    this._moonSize = e, this.uniforms.get("moonSize").value = e;
  }
  /**
   * 获取月亮亮度
   * @return {Number} 月亮亮度
   */
  get moonIntensity() {
    return this._moonIntensity;
  }
  /**
   * 设置月亮亮度
   * @param {Number} value - 月亮亮度
   */
  set moonIntensity(e) {
    this._moonIntensity = e, this.uniforms.get("moonIntensity").value = e;
  }
  /**
   * 获取极光启用状态
   * @return {Boolean} 是否启用极光
   */
  get enableAurora() {
    return this._enableAurora;
  }
  /**
   * 设置极光启用状态
   * @param {Boolean} value - 是否启用极光
   */
  set enableAurora(e) {
    this._enableAurora = e, this.uniforms.get("enableAurora").value = e ? 1 : 0;
  }
  /**
   * 获取极光强度
   * @return {Number} 极光强度
   */
  get auroraIntensity() {
    return this._auroraIntensity;
  }
  /**
   * 设置极光强度
   * @param {Number} value - 极光强度
   */
  set auroraIntensity(e) {
    this._auroraIntensity = e, this.uniforms.get("auroraIntensity").value = e;
  }
  /**
   * 获取夜晚强度
   * @return {Number} 夜晚强度
   */
  get nightIntensity() {
    return this._nightIntensity;
  }
  /**
   * 设置夜晚强度
   * @param {Number} value - 夜晚强度
   */
  set nightIntensity(e) {
    this._nightIntensity = e, this.uniforms.get("nightIntensity").value = e;
  }
  /**
   * 获取极光密度
   * @return {Number} 极光密度
   */
  get auroraDensity() {
    return this._auroraDensity;
  }
  /**
   * 设置极光密度
   * @param {Number} value - 极光密度
   */
  set auroraDensity(e) {
    this._auroraDensity = e, this.uniforms.get("auroraDensity").value = e;
  }
  /**
   * 获取太阳亮度
   * @return {Number} 太阳亮度
   */
  get sunBrightness() {
    return this._sunBrightness;
  }
  /**
   * 设置太阳亮度
   * @param {Number} value - 太阳亮度
   */
  set sunBrightness(e) {
    this._sunBrightness = e, this.uniforms.get("sunBrightness").value = e;
  }
  /**
   * 创建默认噪声纹理
   * 用于在没有设置纹理时提供基本云噪声
   * @private
   */
  createDefaultNoiseTexture() {
    const t = new Uint8Array(16384);
    for (let n = 0; n < 4096; n++)
      t[n * 4] = Math.random() * 255, t[n * 4 + 1] = Math.random() * 255, t[n * 4 + 2] = Math.random() * 255, t[n * 4 + 3] = 255;
    const r = new le(t, 64, 64);
    r.wrapS = r.wrapT = G, r.minFilter = F, r.magFilter = F, r.needsUpdate = !0, this.uniforms.get("noiseTexture").value = r;
  }
  /**
   * 设置相机
   *
   * @param {Camera} camera - 相机对象
   */
  setCamera(e) {
    this._camera = e, e && (this.fov = e.fov);
  }
  /**
   * 更新效果
   *
   * @param {WebGLRenderer} renderer - WebGL渲染器
   * @param {WebGLRenderTarget} inputBuffer - 输入缓冲区
   * @param {Number} [deltaTime] - 时间增量
   */
  update(e, t, r) {
    this.uniforms.get("time").value += r;
    const n = e.getSize(new g());
    this.uniforms.get("resolution").value.set(
      n.width * this._resolutionScale,
      n.height * this._resolutionScale
    ), this.uniforms.get("animateClouds").value = this.animateClouds ? 1 : 0, this.uniforms.get("intensity").value = this.intensity, this.uniforms.get("sunBrightness").value = this._sunBrightness, this.uniforms.get("rayleighCoefficient").value = this.rayleighCoefficient, this.uniforms.get("mieCoefficient").value = this.mieCoefficient, this.uniforms.get("mieDirectionalG").value = this.mieDirectionalG, this.uniforms.get("cloudiness").value = this.cloudiness, this.uniforms.get("skyBlueness").value = this._skyBlueness, this.uniforms.get("cloudAmount").value = this._cloudAmount, this.uniforms.get("cloudScale").value = this._cloudScale, this.uniforms.get("cloudThreshold").value = this._cloudThreshold, this.uniforms.get("volumetricCloudStepsUniform").value = this._volumetricCloudSteps, this.uniforms.get("volumetricLightStepsUniform").value = this._volumetricLightSteps, this.uniforms.get("cloudShadowingStepsUniform").value = this._cloudShadowingSteps, this.uniforms.get("volumetricLightShadowStepsUniform").value = this._volumetricLightShadowSteps, this.uniforms.get("enableStars").value = this._enableStars ? 1 : 0, this.uniforms.get("starIntensity").value = this._starIntensity, this.uniforms.get("starDensity").value = this._starDensity, this.uniforms.get("starSize").value = this._starSize, this.uniforms.get("enableMoon").value = this._enableMoon ? 1 : 0, this.uniforms.get("moonPosition").value.copy(this.moonPosition), this.uniforms.get("moonSize").value = this._moonSize, this.uniforms.get("moonIntensity").value = this._moonIntensity, this.uniforms.get("enableAurora").value = this._enableAurora ? 1 : 0, this.uniforms.get("auroraIntensity").value = this._auroraIntensity, this.uniforms.get("auroraDensity").value = this._auroraDensity, this.uniforms.get("auroraColor").value.copy(this.auroraColor), this.uniforms.get("nightIntensity").value = this._nightIntensity, this.uniforms.get("colorOverlay").value.copy(this.colorOverlay), this.uniforms.get("colorOverlayStrength").value = this._colorOverlayStrength, this.uniforms.get("colorOverlayAffectsClouds").value = this._colorOverlayAffectsClouds ? 1 : 0, this.uniforms.get("sunColor").value.copy(this.sunColor), this._camera && (this.uniforms.get("viewMatrix").value.copy(this._camera.matrixWorld), this._camera.fov !== this._fov && (this.fov = this._camera.fov)), this._autoUpdateMoon && this.uniforms.get("moonPosition").value.copy(this.moonPosition);
  }
  /**
   * 设置噪声纹理
   *
   * @param {Texture} texture - 噪声纹理
   */
  setNoiseTexture(e) {
    e !== null && (e.minFilter = F, e.magFilter = F, e.wrapS = e.wrapT = G), this.uniforms.get("noiseTexture").value = e;
  }
  /**
   * 设置物理尺寸参数
   *
   * @param {Object} parameters - 物理参数
   * @param {Number} [parameters.earthRadius=6360e3] - 地球半径（米）
   * @param {Number} [parameters.atmosphereRadius=6380e3] - 大气层半径（米）
   * @param {Number} [parameters.rayleighHeight=8e3] - 瑞利散射高度（米）
   * @param {Number} [parameters.mieHeight=1.2e3] - 米氏散射高度（米）
   */
  setPhysicalParameters({
    earthRadius: e = 636e4,
    atmosphereRadius: t = 638e4,
    rayleighHeight: r = 8e3,
    mieHeight: n = 1200
  } = {}) {
  }
  /**
   * 获取是否自动更新月亮位置
   * @return {Boolean} 是否自动更新
   */
  get autoUpdateMoon() {
    return this._autoUpdateMoon;
  }
  /**
   * 设置是否自动更新月亮位置
   * @param {Boolean} value - 是否自动更新
   */
  set autoUpdateMoon(e) {
    this._autoUpdateMoon = e, e && this.calculateMoonPosition();
  }
  /**
   * 根据太阳位置计算月亮位置
   * 月亮位置从太阳下山的地方升起，基于地平线
   */
  calculateMoonPosition() {
    this._autoUpdateMoon && (this.moonPosition.copy(Pr.calculateMoonPositionFromSun(this.sunPosition, this.moonOffset)), this.uniforms.get("moonPosition").value = this.moonPosition);
  }
  /**
   * 从日期时间更新太阳位置
   * 基于给定的日期时间自动设置太阳位置，同时更新月亮位置（如果启用）
   *
   * @param {Date} date - 日期时间对象，默认为当前时间
   * @param {Number} latitude - 纬度（度），默认为35°（东京附近）
   * @param {Number} longitude - 经度（度），默认为139°（东京附近）
   */
  updateSunPositionFromDate(e = /* @__PURE__ */ new Date(), t = 35, r = 139) {
    e = e || /* @__PURE__ */ new Date();
    const n = Pr.calculateSunPositionFromDate(e, t, r);
    this.sunPosition.copy(n), this.uniforms.get("sunPosition").value = this.sunPosition, this.autoAdjustParameters(n.y), this._autoUpdateMoon && this.calculateMoonPosition();
  }
  /**
   * 根据太阳高度自动调整场景参数
   *
   * @param {Number} sunHeight - 太阳高度（-1到1之间）
   * @private
   */
  autoAdjustParameters(e) {
    Pr.autoAdjustParameters(this, e);
  }
  /**
   * 获取分辨率缩放比例
   * @return {Number} 分辨率缩放比例（0-1）
   */
  get resolutionScale() {
    return this._resolutionScale;
  }
  /**
   * 设置分辨率缩放比例
   * @param {Number} value - 分辨率缩放比例（0-1）
   */
  set resolutionScale(e) {
    this._resolutionScale = Math.max(0.1, Math.min(1, e));
  }
  /**
   * 获取颜色叠加强度
   * @return {Number} 颜色叠加强度（0-1）
   */
  get colorOverlayStrength() {
    return this._colorOverlayStrength;
  }
  /**
   * 设置颜色叠加强度
   * @param {Number} value - 颜色叠加强度（0-1，0表示无叠加）
   */
  set colorOverlayStrength(e) {
    this._colorOverlayStrength = Math.max(0, Math.min(1, e)), this.uniforms.get("colorOverlayStrength").value = this._colorOverlayStrength;
  }
  /**
   * 获取颜色叠加是否影响云层
   * @return {Boolean} 是否影响云层
   */
  get colorOverlayAffectsClouds() {
    return this._colorOverlayAffectsClouds;
  }
  /**
   * 设置颜色叠加是否影响云层
   * @param {Boolean} value - 是否影响云层
   */
  set colorOverlayAffectsClouds(e) {
    this._colorOverlayAffectsClouds = e, this.uniforms.get("colorOverlayAffectsClouds").value = e ? 1 : 0;
  }
}
const hc = `// 天气系统着色器 - 直接基于StillTravelling的Shadertoy
// 原始版本: https://www.shadertoy.com/view/tdSXzD

uniform float time; // 替代iTime
uniform vec2 resolution; // 替代iResolution
uniform vec3 sunPosition; // 替代iMouse控制的太阳位置
uniform sampler2D noiseTexture; // 替代iChannel0
uniform float intensity; // 散射强度因子
uniform float cloudiness; // 云层密度
uniform int animateEffects; // 是否启用动画

// 原始Shadertoy参数
#define ORIG_CLOUD 0
#define ENABLE_RAIN 0
#define SIMPLE_SUN 0
#define NICE_HACK_SUN 1
#define SOFT_SUN 1
#define cloudy cloudiness // 使用cloudiness替代原来的cloudy
#define haze 0.01 * (cloudy*20.)
#define rainmulti 5.0 // 固定值，原始版本用这个值
#define rainy (10.0 - rainmulti)
#define t time // 替代原始的t
#define fov tan(radians(60.0)) // 使用原始fov值
#define xaxiscloud t*5e2 // 使用原始定义
#define yaxiscloud 0. // 使用原始定义
#define zaxiscloud t*6e2 // 使用原始定义

// 常量定义
#define S(x, y, z) smoothstep(x, y, z)
#define cameraheight 5e1 //50.
#define mincloudheight 5e3 //5e3
#define maxcloudheight 8e3 //8e3
#define cloudnoise 2e-4 //2e-4

// 性能相关常量
const int steps = 16; // 16 is fast, 128 or 256 is extreme high
const int stepss = 16; // 16 is fast, 16 or 32 is high 

// 环境常量
const float R0 = 6360e3; // 行星半径 //6360e3 actual 6371km
const float Ra = 6380e3; // 大气半径 //6380e3 troposphere 8 to 14.5km
const float I = 10.; // 太阳光强度，10.0 is normal
const float SI = 5.; // 太阳强度
const float g = 0.45; // 光聚集 .76 //.45 //.6  .45 is normal
const float g2 = g * g;

const float ts = (cameraheight / 2.5e5);

const float s = 0.999; // 光聚集 for sun
#if SOFT_SUN
const float s2 = s;
#else
const float s2 = s * s;
#endif
const float Hr = 8e3; // Rayleigh scattering top //8e3
const float Hm = 1.2e3; // Mie scattering top //1.3e3

vec3 bM = vec3(21e-6); // normal mie // vec3(21e-6)

// Rayleigh scattering (sky color, atmospheric up to 8km)
vec3 bR = vec3(5.8e-6, 13.5e-6, 33.1e-6); // normal earth

vec3 C = vec3(0., -R0, 0.); // 行星中心
vec3 Ds = normalize(vec3(0., 0., -1.)); // 默认太阳方向

float cloudyhigh = 0.05; // if cloud2 defined

#if ORIG_CLOUD
float cloudnear = 1.0; //9e3 12e3
float cloudfar = 1e3; //15e3 17e3
#else
float cloudnear = 1.0; //15e3 17e3
float cloudfar = 70e3; //160e3
#endif

// AURORA STUFF
mat2 mm2(in float a) {
    float c = cos(a);
    float s = sin(a);
    return mat2(c, s, -s, c);
}

mat2 m2 = mat2(0.95534, 0.29552, -0.29552, 0.95534);

float tri(in float x) {
    return clamp(abs(fract(x) - .5), 0.01, 0.49);
}

vec2 tri2(in vec2 p) {
    return vec2(tri(p.x) + tri(p.y), tri(p.y + tri(p.x)));
}

float triNoise2d(in vec2 p, float spd) {
    float z = 1.8;
    float z2 = 2.5;
    float rz = 0.;
    p *= mm2(p.x * 0.06);
    vec2 bp = p;
    for (float i = 0.; i < 5.; i++) {
        vec2 dg = tri2(bp * 1.85) * .75;
        dg *= mm2(t * spd * float(animateEffects));
        p -= dg / z2;

        bp *= 1.3;
        z2 *= 1.45;
        z *= .42;
        p *= 1.21 + (rz - 1.0) * .02;

        rz += tri(p.x + tri(p.y)) * z;
        p *= -m2;
    }
    return clamp(1. / pow(rz * 29., 1.3), 0., .55);
}

float hash21(in vec2 n) { return fract(sin(dot(n, vec2(12.9898, 4.1414))) * 43758.5453); }
vec4 aurora(vec3 ro, vec3 rd) {
    vec4 col = vec4(0);
    vec4 avgCol = vec4(0);
    ro *= 1e-5;
    float mt = 10.;
    for (float i = 0.; i < 5.; i++) {
        float of = 0.006 * hash21(gl_FragCoord.xy) * smoothstep(0., 15., i * mt);
        float pt = ((.8 + pow((i * mt), 1.2) * .001) - rd.y) / (rd.y * 2. + 0.4);
        pt -= of;
        vec3 bpos = (ro) + pt * rd;
        vec2 p = bpos.zx;
        float rzt = triNoise2d(p, 0.1);
        vec4 col2 = vec4(0, 0, 0, rzt);
        col2.rgb = (sin(1. - vec3(2.15, -.5, 1.2) + (i * mt) * 0.053) * (0.5 * mt)) * rzt;
        avgCol = mix(avgCol, col2, .5);
        col += avgCol * exp2((-i * mt) * 0.04 - 2.5) * smoothstep(0., 5., i * mt);
    }

    col *= (clamp(rd.y * 15. + .4, 0., 1.2));
    return col * 2.8;
}

float noise(in vec2 v) {
    return texture(noiseTexture, (v + .5) / 256., 0.).r;
}

// by iq
float Noise(in vec3 x) {
    vec3 p = floor(x);
    vec3 f = fract(x);
    f = f * f * (3.0 - 2.0 * f);

    vec2 uv = (p.xy + vec2(37.0, 17.0) * p.z) + f.xy;
    vec2 rg = texture(noiseTexture, (uv + 0.5) / 256.0, -100.0).yx;
    return mix(rg.x, rg.y, f.z);
}

float fnoise(vec3 p, in float t) {
    p *= .25;
    float f;

    f = 0.5000 * Noise(p);
    p = p * 3.02;
    p.y -= t * .1 * float(animateEffects);
    f += 0.2500 * Noise(p);
    p = p * 3.03;
    p.y += t * .06 * float(animateEffects);
    f += 0.1250 * Noise(p);
    p = p * 3.01;
    f += 0.0625 * Noise(p);
    p = p * 3.03;
    f += 0.03125 * Noise(p);
    p = p * 3.02;
    f += 0.015625 * Noise(p);
    return f;
}

float cloud(vec3 p, in float t) {
    float cld = fnoise(p * cloudnoise, t) + mix(0.05, 0.2, cloudiness);
    cld = smoothstep(0.35, 0.55, cld);
    cld *= cld * mix(25.0, 50.0, cloudiness);
    return cld + 0.1;
}

void densities(in vec3 pos, out float rayleigh, out float mie) {
    float h = length(pos - C) - R0;
    rayleigh = exp(-h / Hr);
    vec3 d = pos;
    d.y = 0.0;
    float dist = length(d);

    float cld = 0.;
    if (mincloudheight < h && h < maxcloudheight) {
        cld = cloud(pos + vec3(xaxiscloud * float(animateEffects), yaxiscloud, zaxiscloud * float(animateEffects)), t) * mix(0.5, 1.0, cloudiness);
        cld *= sin(3.1415 * (h - mincloudheight) / mincloudheight) * 0.5;
    }
    
    #if ORIG_CLOUD
    if (dist < cloudfar) {
        float factor = clamp(1.0 - ((cloudfar - dist) / (cloudfar - cloudnear)), 0.0, 1.0);
        cld *= factor;
    }
    #else
    if (dist > cloudfar) {
        float factor = clamp(1.0 - ((dist - cloudfar) / (cloudfar - cloudnear)), 0.0, 1.0);
        cld *= factor;
    }
    #endif

    mie = exp(-h / Hm) + cld + mix(0.1, 0.3, cloudiness);
}

float escape(in vec3 p, in vec3 d, in float R) {
    vec3 v = p - C;
    float b = dot(v, d);
    float c = dot(v, v) - R * R;
    float det2 = b * b - c;
    if (det2 < 0.) return -1.;
    float det = sqrt(det2);
    float t1 = -b - det, t2 = -b + det;
    return (t1 >= 0.) ? t1 : t2;
}

// 大气散射计算
void scatter(vec3 o, vec3 d, out vec3 col, out vec3 scat, in float t) {
    float L = escape(o, d, Ra);
    float mu = dot(d, Ds);
    float opmu2 = 1. + mu * mu;
    float phaseR = 0.0596831 * opmu2;
    float phaseM = 0.0951945 * opmu2 / (2.20250 * pow(1.20250 - 0.90000 * mu, 1.5));
    float phaseS = 0.00011936 * opmu2 / (2.99900 * pow(1.99900 - 1.99800 * mu, 1.5));

    float depthR = 0., depthM = 0.;
    vec3 R = vec3(0.), M = vec3(0.);

    float dl = L / float(steps);
    for (int i = 0; i < steps; ++i) {
        float l = float(i) * dl;
        vec3 p = (o + d * l);

        float dR, dM;
        densities(p, dR, dM);
        dR *= dl;
        dM *= dl;
        depthR += dR;
        depthM += dM;

        float Ls = escape(p, Ds, Ra);
        if (Ls > 0.) {
            float dls = Ls / float(stepss);
            float depthRs = 0., depthMs = 0.;
            for (int j = 0; j < stepss; ++j) {
                float ls = float(j) * dls;
                vec3 ps = (p + Ds * ls);
                float dRs, dMs;
                densities(ps, dRs, dMs);
                depthRs += dRs * dls;
                depthMs += dMs * dls;
            }

            vec3 A = exp(-(bR * (depthRs + depthR) + bM * (depthMs + depthM)));
            R += (A * dR);
            M += A * dM;
        }
    }

    col = (intensity * mix(1.0, 1.5, cloudiness)) * (M * bM * phaseM);
    #if NICE_HACK_SUN
    col += (SI) * (M * bM * phaseS);
    #endif
    col += (intensity * mix(1.0, 1.5, cloudiness)) * (R * bR * phaseR);
    scat = 0.1 * (bM * depthM);
}

vec3 hash33(vec3 p) {
    p = fract(p * vec3(443.8975, 397.2973, 491.1871));
    p += dot(p.zxy, p.yxz + 19.27);
    return fract(vec3(p.x * p.y, p.z * p.x, p.y * p.z));
}

vec3 stars(in vec3 p) {
    vec3 c = vec3(0.);
    float res = resolution.x * 2.5;

    for (float i = 0.; i < 4.; i++) {
        vec3 q = fract(p * (.15 * res)) - 0.5;
        vec3 id = floor(p * (.15 * res));
        vec2 rn = hash33(id).xy;
        float c2 = 1. - smoothstep(0., .6, length(q));
        c2 *= step(rn.x, .0005 + i * i * 0.001);
        c += c2 * (mix(vec3(1.0, 0.49, 0.1), vec3(0.75, 0.9, 1.), rn.y) * 0.1 + 0.9);
        p *= 1.3;
    }
    return c * c * .8;
}

// SIMPLE SUN STUFF
const float density = 0.5;
const float zenithOffset = 0.48;
const vec3 skyColor = vec3(0.37, 0.55, 1.0) * (1.0 + 0.0);

#define zenithDensity(x) density / pow(max(x - zenithOffset, 0.0035), 0.75)

float getSunPoint(vec2 p, vec2 lp) {
    return smoothstep(0.04 * (fov/2.0), 0.026 * (fov/2.0), distance(p, lp)) * 50.0;
}

float getMie(vec2 p, vec2 lp) {
    float mytest = lp.y < 0.5 ? (lp.y + 0.5) * pow(0.05, 20.0) : 0.05;
    float disk = clamp(1.0 - pow(distance(p, lp), mytest), 0.0, 1.0);
    return disk * disk * (3.0 - 2.0 * disk) * 0.25 * 3.14159265358979323846;
}

vec3 getSkyAbsorption(vec3 x, float y) {
    vec3 absorption = x * y;
    absorption = pow(absorption, 1.0 - (y + absorption) * 0.5) / x / y;
    return absorption;
}

vec3 jodieReinhardTonemap(vec3 c) {
    float l = dot(c, vec3(0.2126, 0.7152, 0.0722));
    vec3 tc = c / (c + 1.0);
    return mix(c / (l + 1.0), tc, tc);
}

vec3 getAtmosphericScattering(vec2 p, vec2 lp) {
    float zenithnew = zenithDensity(p.y);
    float sunPointDistMult = clamp(length(max(lp.y + 0.1 - zenithOffset, 0.0)), 0.0, 1.0);
    vec3 absorption = getSkyAbsorption(skyColor, zenithnew);
    vec3 sunAbsorption = getSkyAbsorption(skyColor, zenithDensity(lp.y + 0.1));
    vec3 sun3 = getSunPoint(p, lp) * absorption;
    vec3 totalSky = sun3;
    totalSky *= sunAbsorption * 0.5 + 0.5 * length(sunAbsorption);
    vec3 newSky = jodieReinhardTonemap(totalSky);
    return newSky;
}

// RAIN STUFF
vec3 N31(float p) {
    vec3 p3 = fract(vec3(p) * vec3(.1031, .11369, .13787));
    p3 += dot(p3, p3.yzx + 19.19);
    return fract(vec3((p3.x + p3.y) * p3.z, (p3.x + p3.z) * p3.y, (p3.y + p3.z) * p3.x));
}

float SawTooth(float t) {
    return cos(t + cos(t)) + sin(2. * t) * .2 + sin(4. * t) * .02;
}

float DeltaSawTooth(float t) {
    return 0.4 * cos(2. * t) + 0.08 * cos(4. * t) - (1. - sin(t)) * sin(t + cos(t));
}

vec2 GetDrops(vec2 uv, float seed, float m) {
    float t2 = time + m;
    vec2 o = vec2(0.);

    #ifndef DROP_DEBUG
    uv.y += t2 * .05 * float(animateEffects);
    #endif

    uv *= vec2(10., 2.5) * 2.;
    vec2 id = floor(uv);
    vec3 n = N31(id.x + (id.y + seed) * 546.3524);
    vec2 bd = fract(uv);

    vec2 uv2 = bd;

    bd -= 0.5;

    bd.y *= 4.;

    bd.x += (n.x - .5) * rainy;

    t2 += n.z * 6.28;
    float slide = SawTooth(t2);

    float ts = 1.5;
    vec2 trailPos = vec2(bd.x * ts, (fract(bd.y * ts * 2. - t2 * 2.) - .5) * .5);

    bd.y += slide * 2.;

    #ifdef HIGH_QUALITY
    float dropShape = bd.x * bd.x;
    dropShape *= DeltaSawTooth(t);
    bd.y += dropShape;
    #endif

    float d = length(bd);

    float trailMask = S(-.2, .2, bd.y);
    trailMask *= bd.y;
    float td = length(trailPos * max(.5, trailMask));

    float mainDrop = S(.2, .1, d);
    float dropTrail = S(.1, .02, td);

    dropTrail *= trailMask;
    o = mix(bd * mainDrop, trailPos, dropTrail);

    #ifdef DROP_DEBUG
    if (uv2.x < .02 || uv2.y < .01) o = vec2(1.);
    #endif

    return o;
}

void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
    float AR = resolution.x / resolution.y;
    float M = 1.0; // canvas.innerWidth/M //canvas.innerHeight/M --res

    vec2 uvMouse = vec2(sunPosition.x * 0.5 + 0.5, sunPosition.y * 0.5 + 0.5);
    uvMouse.x *= AR;

    vec2 uv0 = uv;
    uv0 *= M;

    vec2 fragUV = uv * 2.0 - 1.0;
    fragUV.x *= AR;

    if (uvMouse.y == 0.) uvMouse.y = (0.7 - (0.05 * fov)); // initial view
    if (uvMouse.x == 0.) uvMouse.x = (1.0 - (0.05 * fov)); // initial view

    Ds = normalize(vec3(uvMouse.x - ((0.5 * AR)), uvMouse.y - 0.5, (fov / -2.0)));

    vec3 O = vec3(0., cameraheight, 0.);
    vec3 D = normalize(vec3(fragUV, -(fov)));

    vec3 color = vec3(0.);
    vec3 scat = vec3(0.);

    float att = 1.;
    float staratt = 1.;
    float scatatt = 1.;
    vec3 star = vec3(0.);
    vec4 aur = vec4(0.);

    float fade = smoothstep(0., 0.01, abs(D.y)) * 0.5 + 0.9;

    staratt = 1. - min(1.0, (uvMouse.y * 2.0));
    scatatt = 1. - min(1.0, (uvMouse.y * 2.2));

    if (D.y < -ts) {
        float L = -O.y / D.y;
        O = O + D * L;
        D.y = -D.y;
        D = normalize(D + vec3(0, .003 * sin(time + 6.2831 * noise(O.xz + vec2(0., -time * 1e3 * float(animateEffects)))), 0.));
        att = .6;
        star = stars(D);
        uvMouse.y < 0.5 ? aur = smoothstep(0.0, 2.5, aurora(O, D)) : aur = aur;
    } else {
        float L1 = O.y / D.y;
        vec3 O1 = O + D * L1;

        vec3 D1 = vec3(1.);
        D1 = normalize(D + vec3(1., 0.0009 * sin(time + 6.2831 * noise(O1.xz + vec2(0., time * 0.8 * float(animateEffects)))), 0.));
        star = stars(D1);
        uvMouse.y < 0.5 ? aur = smoothstep(0., 1.5, aurora(O, D)) * fade : aur = aur;
    }

    star *= att;
    star *= staratt;

    scatter(O, D, color, scat, time);
    color *= att;
    scat *= att;
    scat *= scatatt;

    #if SIMPLE_SUN
    vec2 uv1 = uv;
    uv1 *= M;
    uv1.x *= AR;

    vec3 sun2 = getAtmosphericScattering(uv1, vec2(uvMouse.x, uvMouse.y));
    color += sun2;
    #endif

    color += scat;
    color += star;
    color += aur.rgb * scatatt;

    #if ENABLE_RAIN
    vec2 drops = vec2(0.);
    if (rainmulti > 1.0) {
        drops = GetDrops(fragUV / 2.0, 1., 1.);
        color += drops.x + drops.y;
    }
    #endif

    vec3 finalColor = pow(color * mix(1.0, 1.3, cloudiness), vec3(0.454545));

    float skyMix = smoothstep(-0.05, 0.05, D.y);
    finalColor = mix(inputColor.rgb, finalColor, skyMix);

    outputColor = vec4(finalColor, inputColor.a);
}
`;
class fd extends I {
  /**
      * 构造函数
      *
      * @param {Object} [options] - 效果选项
      * @param {BlendFunction} [options.blendFunction=BlendFunction.SCREEN] - 混合模式
      * @param {Vector3} [options.sunPosition=new Vector3(0.1, 0.05, -1).normalize()] - 太阳位置（方向向量）
      * @param {Boolean} [options.animateEffects=true] - 是否启用动画效果
      * @param {Number} [options.intensity=10.0] - 散射强度
      * @param {Number} [options.cloudiness=0.5] - 云层密度
      */
  constructor({
    blendFunction: e = E.SCREEN,
    sunPosition: t = new T(0.1, 0.05, -1).normalize(),
    animateEffects: r = !0,
    intensity: n = 10,
    cloudiness: i = 0.5
  } = {}) {
    super(
      "WeatherSystemEffect",
      hc,
      {
        blendFunction: e,
        attributes: Q.DEPTH,
        defines: /* @__PURE__ */ new Map([
          ["USE_DEPTH", "1"]
        ]),
        uniforms: /* @__PURE__ */ new Map([
          ["sunPosition", new a(t)],
          ["intensity", new a(n)],
          ["animateEffects", new a(r ? 1 : 0)],
          ["time", new a(0)],
          ["resolution", new a(new g(1, 1))],
          ["noiseTexture", new a(null)],
          ["cloudiness", new a(i)]
        ])
      }
    ), this.sunPosition = t, this.animateEffects = r, this.intensity = n, this.cloudiness = i, this.createDefaultNoiseTexture();
  }
  /**
      * 创建默认噪声纹理
      * 当没有提供噪声纹理时使用
      */
  createDefaultNoiseTexture() {
    const t = new Float32Array(262144);
    for (let n = 0; n < 256 * 256; n++)
      t[n * 4] = Math.random(), t[n * 4 + 1] = Math.random(), t[n * 4 + 2] = Math.random(), t[n * 4 + 3] = 1;
    const r = new le(t, 256, 256, ne, K);
    r.wrapS = r.wrapT = G, r.needsUpdate = !0, this.uniforms.get("noiseTexture").value = r;
  }
  /**
      * 更新效果
      *
      * @param {WebGLRenderer} renderer - WebGL渲染器
      * @param {WebGLRenderTarget} inputBuffer - 输入缓冲区
      * @param {Number} [deltaTime] - 时间增量
      */
  update(e, t, r) {
    this.uniforms.get("time").value += r;
    const n = e.getSize(new g());
    this.uniforms.get("resolution").value.set(n.width, n.height), this.uniforms.get("animateEffects").value = this.animateEffects ? 1 : 0, this.uniforms.get("intensity").value = this.intensity, this.uniforms.get("cloudiness").value = this.cloudiness;
  }
  /**
      * 设置噪声纹理
      *
      * @param {Texture} texture - 噪声纹理
      */
  setNoiseTexture(e) {
    e !== null && (e.wrapS = e.wrapT = G, e.minFilter = e.magFilter = F, this.uniforms.get("noiseTexture").value = e);
  }
  /**
      * 设置天气预设
      *
      * @param {String} preset - 预设名称，可选值："clear", "cloudy", "overcast", "storm"
      */
  setWeatherPreset(e) {
    switch (this.cloudiness = 0.5, this.intensity = 10, e.toLowerCase()) {
      case "clear":
        this.cloudiness = 0.3, this.intensity = 12;
        break;
      case "cloudy":
        this.cloudiness = 1.2, this.intensity = 9;
        break;
      case "overcast":
        this.cloudiness = 1.8, this.intensity = 8;
        break;
      case "storm":
        this.cloudiness = 2.5, this.intensity = 7;
        break;
    }
  }
}
const dc = `uniform sampler2D noiseTexture;\r
uniform vec2 texelSize;\r
uniform float time;\r
uniform float intensity;\r
uniform float rainSpeed;\r
uniform float rainSize;\r
uniform vec2 windDirection;\r
uniform float rainAngle;\r
uniform vec3 rainColor;\r
uniform float wetness;\r
uniform float puddleIntensity;\r
uniform float mistIntensity;\r
uniform int enableLighting;\r
uniform float lightningFrequency;\r
uniform vec3 lightningColor;\r
uniform float lightningTime;\r
uniform float lightningFlash;\r
\r
varying vec2 vUv;\r
varying vec2 vUvPattern;\r
\r
// 生成雨滴\r
float generateRaindrop(vec2 uv, float layer, float time) {\r
    // 为每层应用不同的缩放和偏移\r
    vec2 scaledUv = uv * (20.0 + layer * 10.0);\r
    vec2 cellId = floor(scaledUv);\r
    vec2 cellUv = fract(scaledUv);\r
    \r
    // 从噪声纹理获取随机值\r
    vec3 noise = texture2D(noiseTexture, cellId / (20.0 + layer * 10.0) * 0.1).rgb;\r
    \r
    // 雨滴位置偏移\r
    vec2 offset = noise.xy * 0.8;\r
    \r
    // 雨滴下落动画\r
    float fallTime = time * rainSpeed * (0.8 + noise.z * 0.4);\r
    offset.y = mod(offset.y - fallTime, 1.0);\r
    \r
    // 风向影响\r
    offset.x += sin(fallTime * 0.5 + noise.x * 6.28) * windDirection.x * 0.3;\r
    offset.x += windDirection.x * (offset.y - 0.5) * 0.2; // 倾斜效果\r
    \r
    // 计算到雨滴中心的距离\r
    vec2 dropCenter = cellUv - offset;\r
    \r
    // 雨滴形状：椭圆形模拟运动模糊\r
    float aspectRatio = 0.3 + noise.z * 0.2; // 雨滴的长宽比\r
    dropCenter.y *= aspectRatio; // 垂直压缩\r
    \r
    // 应用雨滴角度 - 手动矩阵乘法避免兼容性问题\r
    float c = cos(rainAngle);\r
    float s = sin(rainAngle);\r
    vec2 rotatedDropCenter;\r
    rotatedDropCenter.x = c * dropCenter.x - s * dropCenter.y;\r
    rotatedDropCenter.y = s * dropCenter.x + c * dropCenter.y;\r
    dropCenter = rotatedDropCenter;\r
    \r
    float dropDist = length(dropCenter);\r
    float dropSize = rainSize * (0.7 + noise.z * 0.6) * (1.0 - layer * 0.2);\r
    \r
    // 雨滴强度\r
    float drop = 1.0 - smoothstep(0.0, dropSize, dropDist);\r
    drop *= drop; // 增强中心亮度\r
    \r
    return drop;\r
}\r
\r
// 生成雨雾效果\r
float generateMist(vec2 uv, float time) {\r
    vec2 mistUv = uv * 3.0 + vec2(time * 0.1, time * 0.05);\r
    vec3 noise1 = texture2D(noiseTexture, mistUv * 0.5).rgb;\r
    vec3 noise2 = texture2D(noiseTexture, mistUv * 1.5 + 0.5).rgb;\r
    \r
    float mist = (noise1.r + noise2.g * 0.5) * 0.5;\r
    mist = smoothstep(0.3, 0.8, mist);\r
    \r
    return mist * mistIntensity;\r
}\r
\r
// 生成积水反射效果\r
vec3 generatePuddles(vec2 uv, vec3 inputColor) {\r
    vec2 puddleUv = uv * 8.0;\r
    vec3 puddleNoise = texture2D(noiseTexture, puddleUv * 0.2).rgb;\r
    \r
    // 积水区域\r
    float puddleShape = smoothstep(0.4, 0.6, puddleNoise.r) * puddleIntensity;\r
    \r
    // 水面波纹\r
    vec2 ripple = sin(uv * 50.0 + time * 3.0) * 0.02 * puddleShape;\r
    \r
    // 反射效果\r
    vec3 reflectionColor = inputColor * 0.6 + vec3(0.2, 0.3, 0.4) * 0.4;\r
    \r
    return mix(inputColor, reflectionColor, puddleShape * wetness);\r
}\r
\r
// 闪电效果\r
vec3 generateLightning(vec2 uv, vec3 color) {\r
    if (enableLighting == 0 || lightningFlash <= 0.0) {\r
        return color;\r
    }\r
    \r
    // 闪电分支形状\r
    float lightning = 0.0;\r
    for (int i = 0; i < 3; i++) {\r
        float branch = float(i) * 0.3;\r
        vec2 lightningUv = uv + vec2(branch, sin(uv.y * 10.0 + branch) * 0.05);\r
        \r
        float dist = abs(lightningUv.x - 0.5 - sin(lightningUv.y * 5.0 + time * 10.0) * 0.1);\r
        lightning += exp(-dist * 200.0) * (1.0 - float(i) * 0.3);\r
    }\r
    \r
    // 整体闪电亮度\r
    float flash = lightningFlash * (0.5 + 0.5 * sin(time * 50.0));\r
    lightning *= flash;\r
    \r
    // 环境照明\r
    float ambientFlash = lightningFlash * 0.3 * (1.0 + sin(time * 30.0) * 0.5);\r
    \r
    return color + lightningColor * lightning + color * lightningColor * ambientFlash;\r
}\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
    vec3 color = inputColor.rgb;\r
    \r
    if (intensity <= 0.0) {\r
        outputColor = inputColor;\r
        return;\r
    }\r
    \r
    // 生成多层雨滴\r
    float rain = 0.0;\r
    \r
    // 前景雨滴 - 大且清晰\r
    rain += generateRaindrop(vUv, 0.0, time) * 0.8;\r
    \r
    // 中景雨滴 - 中等大小\r
    rain += generateRaindrop(vUv * 1.3, 1.0, time * 1.1) * 0.6;\r
    \r
    // 背景雨滴 - 小且模糊\r
    rain += generateRaindrop(vUvPattern, 2.0, time * 0.9) * 0.4;\r
    \r
    // 远景雨滴 - 非常小\r
    rain += generateRaindrop(vUvPattern * 0.8, 3.0, time * 1.2) * 0.2;\r
    \r
    // 应用强度\r
    rain *= intensity;\r
    \r
    // 雨雾效果\r
    float mist = generateMist(uv, time);\r
    \r
    // 湿润和积水效果\r
    color = generatePuddles(uv, color);\r
    \r
    // 应用湿润效果（增强对比度和饱和度）\r
    if (wetness > 0.0) {\r
        // 增强反射\r
        color *= 1.0 + wetness * 0.2;\r
        // 轻微的蓝色调\r
        color = mix(color, color * vec3(0.9, 0.95, 1.1), wetness * 0.1);\r
    }\r
    \r
    // 添加雨雾\r
    color = mix(color, vec3(0.7, 0.8, 0.9), mist);\r
    \r
    // 混合雨滴\r
    vec3 dropColor = rainColor;\r
    color = mix(color, dropColor, rain);\r
    \r
    // 闪电效果\r
    color = generateLightning(uv, color);\r
    \r
    // 整体雨天色调调整\r
    if (intensity > 0.0) {\r
        // 降低亮度，增加灰色调\r
        color *= 1.0 - intensity * 0.2;\r
        color = mix(color, vec3(dot(color, vec3(0.299, 0.587, 0.114))), intensity * 0.1);\r
    }\r
    \r
    outputColor = vec4(color, inputColor.a);\r
}\r
`, mc = `uniform float time;\r
\r
varying vec2 vUv;\r
varying vec2 vUvPattern;\r
\r
void mainSupport(const in vec2 uv) {\r
    // 基本UV坐标\r
    vUv = uv;\r
    \r
    // 创建旋转的UV坐标用于不同的雨滴图案\r
    float rotation = time * 0.1;\r
    float cos_r = cos(rotation);\r
    float sin_r = sin(rotation);\r
    \r
    vec2 center = vec2(0.5, 0.5);\r
    vec2 rotatedUv = uv - center;\r
    \r
    // 手动执行矩阵乘法，避免mat2构造函数的兼容性问题\r
    vec2 rotatedResult;\r
    rotatedResult.x = cos_r * rotatedUv.x - sin_r * rotatedUv.y;\r
    rotatedResult.y = sin_r * rotatedUv.x + cos_r * rotatedUv.y;\r
    \r
    vUvPattern = rotatedResult + center;\r
}\r
\r
void main() {\r
    // 根据正规化设备坐标计算UV\r
    vec2 uv = position.xy * 0.5 + 0.5;\r
    \r
    // 调用辅助函数处理UV\r
    mainSupport(uv);\r
    \r
    // 设置顶点位置\r
    gl_Position = vec4(position.xy, 1.0, 1.0);\r
}\r
`;
class pc extends I {
  /**
   * 构造一个新的降雨效果
   *
   * @param {Object} [options] - 效果选项
   * @param {BlendFunction} [options.blendFunction=BlendFunction.SCREEN] - 混合模式
   * @param {Number} [options.intensity=0.8] - 降雨强度，0-1
   * @param {Number} [options.rainSpeed=1.2] - 雨滴下落速度
   * @param {Number} [options.rainSize=0.15] - 雨滴大小
   * @param {Vector2} [options.windDirection=new Vector2(0.2, 0.0)] - 风向影响
   * @param {Number} [options.rainAngle=15.0] - 雨滴倾斜角度（度）
   * @param {Color|String|Number} [options.rainColor=0x87ceeb] - 雨滴颜色
   * @param {Number} [options.wetness=0.5] - 湿润程度，影响表面反射
   * @param {Number} [options.puddleIntensity=0.3] - 积水强度
   * @param {Number} [options.mistIntensity=0.2] - 雨雾强度
   * @param {Boolean} [options.enableLighting=true] - 是否启用闪电效果
   * @param {Number} [options.lightningFrequency=0.1] - 闪电频率
   * @param {Vector3} [options.lightningColor=new Vector3(1.0, 0.9, 0.8)] - 闪电颜色
   */
  constructor({
    blendFunction: e = E.ALPHA,
    intensity: t = 0.8,
    rainSpeed: r = 1.2,
    rainSize: n = 0.15,
    windDirection: i = new g(0.2, 0),
    rainAngle: s = 15,
    rainColor: o = 8900331,
    wetness: l = 0.5,
    puddleIntensity: u = 0.3,
    mistIntensity: f = 0.2,
    enableLighting: h = !0,
    lightningFrequency: m = 0.1,
    lightningColor: p = new T(1, 0.9, 0.8)
  } = {}) {
    super("RainfallEffect", dc, {
      blendFunction: e,
      vertexShader: mc,
      uniforms: /* @__PURE__ */ new Map([
        ["texelSize", new a(new g())],
        ["time", new a(0)],
        ["intensity", new a(t)],
        ["rainSpeed", new a(r)],
        ["rainSize", new a(n)],
        ["windDirection", new a(new g().copy(i))],
        ["rainAngle", new a(s * Math.PI / 180)],
        // 转换为弧度
        ["rainColor", new a(new y(o))],
        ["wetness", new a(l)],
        ["puddleIntensity", new a(u)],
        ["mistIntensity", new a(f)],
        ["enableLighting", new a(h ? 1 : 0)],
        ["lightningFrequency", new a(m)],
        ["lightningColor", new a(p.clone())],
        ["noiseTexture", new a(null)],
        ["lightningTime", new a(0)],
        ["lightningFlash", new a(0)]
      ])
    }), this.time = 0, this._lightningTimer = 0, this._nextLightning = Math.random() * 10, this._createNoiseTexture();
  }
  /**
   * 创建噪声纹理，用于雨滴的随机分布和形态
   *
   * @private
   */
  _createNoiseTexture() {
    const t = new Uint8Array(262144);
    for (let n = 0; n < 256; n++)
      for (let i = 0; i < 256; i++) {
        const s = (n * 256 + i) * 4;
        t[s] = Math.floor(Math.random() * 255), t[s + 1] = Math.floor(Math.random() * 255);
        const o = n / 256, l = i / 256, u = Math.sin(o * 12 + Math.cos(l * 8)) * 0.5 + 0.5;
        t[s + 2] = Math.floor(u * 255), t[s + 3] = 255;
      }
    const r = new le(t, 256, 256, ne, se);
    r.wrapS = G, r.wrapT = G, r.minFilter = F, r.magFilter = F, r.needsUpdate = !0, this.uniforms.get("noiseTexture").value = r;
  }
  /**
   * 降雨强度
   * @type {Number}
   */
  get intensity() {
    return this.uniforms.get("intensity").value;
  }
  set intensity(e) {
    this.uniforms.get("intensity").value = Math.max(0, Math.min(1, e));
  }
  /**
   * 雨滴下落速度
   * @type {Number}
   */
  get rainSpeed() {
    return this.uniforms.get("rainSpeed").value;
  }
  set rainSpeed(e) {
    this.uniforms.get("rainSpeed").value = Math.max(0, e);
  }
  /**
   * 雨滴大小
   * @type {Number}
   */
  get rainSize() {
    return this.uniforms.get("rainSize").value;
  }
  set rainSize(e) {
    this.uniforms.get("rainSize").value = Math.max(0, e);
  }
  /**
   * 风向影响
   * @type {Vector2}
   */
  get windDirection() {
    return this.uniforms.get("windDirection").value;
  }
  set windDirection(e) {
    this.uniforms.get("windDirection").value.copy(e);
  }
  /**
   * 雨滴倾斜角度（弧度）
   * @type {Number}
   */
  get rainAngle() {
    return this.uniforms.get("rainAngle").value;
  }
  set rainAngle(e) {
    this.uniforms.get("rainAngle").value = e * Math.PI / 180;
  }
  /**
   * 雨滴颜色
   * @type {Color}
   */
  get rainColor() {
    return this.uniforms.get("rainColor").value;
  }
  set rainColor(e) {
    this.uniforms.get("rainColor").value.set(e);
  }
  /**
   * 湿润程度
   * @type {Number}
   */
  get wetness() {
    return this.uniforms.get("wetness").value;
  }
  set wetness(e) {
    this.uniforms.get("wetness").value = Math.max(0, Math.min(1, e));
  }
  /**
   * 积水强度
   * @type {Number}
   */
  get puddleIntensity() {
    return this.uniforms.get("puddleIntensity").value;
  }
  set puddleIntensity(e) {
    this.uniforms.get("puddleIntensity").value = Math.max(0, Math.min(1, e));
  }
  /**
   * 雨雾强度
   * @type {Number}
   */
  get mistIntensity() {
    return this.uniforms.get("mistIntensity").value;
  }
  set mistIntensity(e) {
    this.uniforms.get("mistIntensity").value = Math.max(0, Math.min(1, e));
  }
  /**
   * 是否启用闪电效果
   * @type {Boolean}
   */
  get enableLighting() {
    return !!this.uniforms.get("enableLighting").value;
  }
  set enableLighting(e) {
    this.uniforms.get("enableLighting").value = e ? 1 : 0;
  }
  /**
   * 闪电频率
   * @type {Number}
   */
  get lightningFrequency() {
    return this.uniforms.get("lightningFrequency").value;
  }
  set lightningFrequency(e) {
    this.uniforms.get("lightningFrequency").value = Math.max(0, e);
  }
  /**
   * 设置雨天预设
   * @param {String} preset - 预设名称：'light', 'moderate', 'heavy', 'storm'
   */
  setRainPreset(e) {
    switch (e.toLowerCase()) {
      case "light":
        this.intensity = 0.3, this.rainSpeed = 0.8, this.rainSize = 0.1, this.wetness = 0.2, this.puddleIntensity = 0.1, this.mistIntensity = 0.1, this.enableLighting = !1;
        break;
      case "moderate":
        this.intensity = 0.6, this.rainSpeed = 1, this.rainSize = 0.15, this.wetness = 0.4, this.puddleIntensity = 0.3, this.mistIntensity = 0.2, this.enableLighting = !1;
        break;
      case "heavy":
        this.intensity = 0.8, this.rainSpeed = 1.3, this.rainSize = 0.2, this.wetness = 0.6, this.puddleIntensity = 0.5, this.mistIntensity = 0.3, this.enableLighting = !0, this.lightningFrequency = 0.05;
        break;
      case "storm":
        this.intensity = 1, this.rainSpeed = 1.8, this.rainSize = 0.25, this.wetness = 0.8, this.puddleIntensity = 0.7, this.mistIntensity = 0.4, this.enableLighting = !0, this.lightningFrequency = 0.15;
        break;
    }
  }
  /**
   * 更新效果
   *
   * @param {WebGLRenderer} renderer - 渲染器
   * @param {WebGLRenderTarget} inputBuffer - 输入缓冲区
   * @param {Number} [deltaTime] - 渲染帧间隔时间
   */
  update(e, t, r) {
    if (this.time += r || 0.016, this.uniforms.get("time").value = this.time, this.enableLighting)
      if (this._lightningTimer += r || 0.016, this._lightningTimer >= this._nextLightning)
        this.uniforms.get("lightningFlash").value = 1, this.uniforms.get("lightningTime").value = this.time, this._nextLightning = (Math.random() * 5 + 2) / this.lightningFrequency, this._lightningTimer = 0;
      else {
        const n = this.uniforms.get("lightningFlash").value;
        this.uniforms.get("lightningFlash").value = Math.max(0, n - r * 3);
      }
  }
  /**
   * 当渲染尺寸改变时更新纹素大小
   *
   * @param {Number} width - 宽度
   * @param {Number} height - 高度
   */
  setSize(e, t) {
    this.uniforms.get("texelSize").value.set(1 / e, 1 / t);
  }
}
class hd {
  /**
   * 构造天气管理器
   * 
   * @param {Object} [options] - 配置选项
   * @param {Boolean} [options.enableTransitions=true] - 是否启用天气过渡动画
   * @param {Number} [options.transitionSpeed=1.0] - 天气过渡速度
   * @param {Object} [options.effectOptions] - 各种效果的默认配置
   */
  constructor({
    enableTransitions: e = !0,
    transitionSpeed: t = 1,
    effectOptions: r = {}
  } = {}) {
    this.enableTransitions = e, this.transitionSpeed = t, this.currentWeather = "clear", this.targetWeather = "clear", this.transitionProgress = 1, this.effects = {}, this.weatherPresets = {
      clear: {
        name: "晴天",
        sky: { cloudiness: 0.2, intensity: 12 },
        rain: { intensity: 0 },
        snow: { density: 0 },
        fog: { density: 0 }
      },
      cloudy: {
        name: "多云",
        sky: { cloudiness: 0.7, intensity: 9 },
        rain: { intensity: 0 },
        snow: { density: 0 },
        fog: { density: 0.1 }
      },
      overcast: {
        name: "阴天",
        sky: { cloudiness: 1.2, intensity: 6 },
        rain: { intensity: 0 },
        snow: { density: 0 },
        fog: { density: 0.2 }
      },
      lightRain: {
        name: "小雨",
        sky: { cloudiness: 1, intensity: 7 },
        rain: { intensity: 0.3, preset: "light" },
        snow: { density: 0 },
        fog: { density: 0.15 }
      },
      rain: {
        name: "中雨",
        sky: { cloudiness: 1.3, intensity: 5 },
        rain: { intensity: 0.6, preset: "moderate" },
        snow: { density: 0 },
        fog: { density: 0.25 }
      },
      heavyRain: {
        name: "大雨",
        sky: { cloudiness: 1.8, intensity: 4 },
        rain: { intensity: 0.8, preset: "heavy" },
        snow: { density: 0 },
        fog: { density: 0.3 }
      },
      storm: {
        name: "暴风雨",
        sky: { cloudiness: 2, intensity: 3 },
        rain: { intensity: 1, preset: "storm" },
        snow: { density: 0 },
        fog: { density: 0.4 }
      },
      lightSnow: {
        name: "小雪",
        sky: { cloudiness: 0.8, intensity: 6, colorOverlay: new T(0.9, 0.95, 1), colorOverlayStrength: 0.2 },
        rain: { intensity: 0 },
        snow: { density: 0.3, snowSpeed: 0.15, windDirection: new g(0.05, 0) },
        fog: { density: 0.1 }
      },
      snow: {
        name: "中雪",
        sky: { cloudiness: 1.2, intensity: 4, colorOverlay: new T(0.85, 0.9, 1), colorOverlayStrength: 0.3 },
        rain: { intensity: 0 },
        snow: { density: 0.6, snowSpeed: 0.2, windDirection: new g(0.1, 0) },
        fog: { density: 0.2 }
      },
      heavySnow: {
        name: "大雪",
        sky: { cloudiness: 1.8, intensity: 3, colorOverlay: new T(0.8, 0.85, 1), colorOverlayStrength: 0.4 },
        rain: { intensity: 0 },
        snow: { density: 0.9, snowSpeed: 0.25, windDirection: new g(0.15, 0) },
        fog: { density: 0.35 }
      },
      blizzard: {
        name: "暴雪",
        sky: { cloudiness: 2.2, intensity: 2, colorOverlay: new T(0.75, 0.8, 1), colorOverlayStrength: 0.5 },
        rain: { intensity: 0 },
        snow: { density: 1, snowSpeed: 0.35, windDirection: new g(0.3, 0) },
        fog: { density: 0.5 }
      },
      fog: {
        name: "大雾",
        sky: { cloudiness: 0.5, intensity: 8 },
        rain: { intensity: 0 },
        snow: { density: 0 },
        fog: { density: 0.8 }
      }
    }, this.initializeEffects(r);
  }
  /**
   * 初始化天气效果
   * 
   * @private
   */
  initializeEffects(e) {
    this.effects.sky = new fc(e.sky || {}), this.effects.rain = new pc(e.rain || {}), this.effects.snow = new Ho(e.snow || {}), Gn && (this.effects.fog = new Gn(e.fog || {}));
  }
  /**
   * 获取所有天气效果实例数组
   * 
   * @return {Array} 效果实例数组
   */
  getEffects() {
    const e = [];
    e.push(this.effects.sky);
    const t = this.weatherPresets[this.currentWeather];
    return t && (t.rain.intensity > 0 && e.push(this.effects.rain), t.snow.density > 0 && e.push(this.effects.snow), this.effects.fog && t.fog.density > 0 && e.push(this.effects.fog)), e;
  }
  /**
   * 设置天气状态
   * 
   * @param {String} weatherType - 天气类型
   * @param {Boolean} [immediate=false] - 是否立即切换，不使用过渡动画
   */
  setWeather(e, t = !1) {
    if (!this.weatherPresets[e]) {
      console.warn(`未知的天气类型: ${e}`);
      return;
    }
    this.targetWeather = e, t || !this.enableTransitions ? (this.currentWeather = e, this.transitionProgress = 1, this.applyWeatherSettings(e, 1)) : this.transitionProgress = 0;
  }
  /**
   * 获取当前天气类型
   * 
   * @return {String} 当前天气类型
   */
  getCurrentWeather() {
    return this.currentWeather;
  }
  /**
   * 获取天气预设信息
   * 
   * @param {String} weatherType - 天气类型
   * @return {Object} 天气预设信息
   */
  getWeatherInfo(e) {
    return this.weatherPresets[e];
  }
  /**
   * 获取所有可用的天气类型
   * 
   * @return {Array} 天气类型数组
   */
  getAvailableWeatherTypes() {
    return Object.keys(this.weatherPresets);
  }
  /**
   * 应用天气设置
   * 
   * @param {String} weatherType - 天气类型
   * @param {Number} strength - 应用强度 (0-1)
   * @private
   */
  applyWeatherSettings(e, t = 1) {
    const r = this.weatherPresets[e];
    r && (r.sky && this.applyEffectSettings(this.effects.sky, r.sky, t), r.rain && (this.applyEffectSettings(this.effects.rain, r.rain, t), r.rain.preset && this.effects.rain.setRainPreset(r.rain.preset)), r.snow && this.applyEffectSettings(this.effects.snow, r.snow, t), r.fog && this.effects.fog && this.applyEffectSettings(this.effects.fog, r.fog, t));
  }
  /**
   * 应用效果设置
   * 
   * @param {Effect} effect - 效果实例
   * @param {Object} settings - 设置对象
   * @param {Number} strength - 应用强度
   * @private
   */
  applyEffectSettings(e, t, r) {
    Object.entries(t).forEach(([n, i]) => {
      n !== "preset" && e[n] !== void 0 && (typeof i == "number" ? e[n] = i * r : e[n] = i);
    });
  }
  /**
   * 更新天气系统
   * 
   * @param {Number} deltaTime - 时间增量
   */
  update(e) {
    this.transitionProgress < 1 && this.currentWeather !== this.targetWeather && (this.transitionProgress += e * this.transitionSpeed, this.transitionProgress = Math.min(1, this.transitionProgress), this.transitionProgress >= 1 ? (this.currentWeather = this.targetWeather, this.applyWeatherSettings(this.currentWeather, 1)) : this.blendWeatherStates(this.currentWeather, this.targetWeather, this.transitionProgress)), Object.values(this.effects).forEach((t) => {
      t && t.update && t.update(null, null, e);
    });
  }
  /**
   * 混合两种天气状态
   * 
   * @param {String} fromWeather - 起始天气
   * @param {String} toWeather - 目标天气
   * @param {Number} progress - 过渡进度 (0-1)
   * @private
   */
  blendWeatherStates(e, t, r) {
    const n = this.weatherPresets[e], i = this.weatherPresets[t];
    !n || !i || (this.blendEffectSettings(this.effects.sky, n.sky, i.sky, r), this.blendEffectSettings(this.effects.rain, n.rain, i.rain, r), this.blendEffectSettings(this.effects.snow, n.snow, i.snow, r), this.effects.fog && this.blendEffectSettings(this.effects.fog, n.fog, i.fog, r));
  }
  /**
   * 混合效果设置
   * 
   * @param {Effect} effect - 效果实例
   * @param {Object} fromSettings - 起始设置
   * @param {Object} toSettings - 目标设置
   * @param {Number} progress - 进度
   * @private
   */
  blendEffectSettings(e, t, r, n) {
    if (!t || !r) return;
    (/* @__PURE__ */ new Set([...Object.keys(t), ...Object.keys(r)])).forEach((s) => {
      if (s === "preset") return;
      const o = t[s] || 0, l = r[s] || 0;
      e[s] !== void 0 && (typeof o == "number" && typeof l == "number" ? e[s] = o + (l - o) * n : n > 0.5 ? e[s] = l : e[s] = o);
    });
  }
  /**
   * 设置过渡速度
   * 
   * @param {Number} speed - 过渡速度
   */
  setTransitionSpeed(e) {
    this.transitionSpeed = Math.max(0.1, e);
  }
  /**
   * 获取过渡速度
   * 
   * @return {Number} 过渡速度
   */
  getTransitionSpeed() {
    return this.transitionSpeed;
  }
  /**
   * 启用或禁用天气过渡
   * 
   * @param {Boolean} enable - 是否启用
   */
  setTransitionsEnabled(e) {
    this.enableTransitions = e;
  }
  /**
   * 检查是否正在过渡
   * 
   * @return {Boolean} 是否正在过渡
   */
  isTransitioning() {
    return this.transitionProgress < 1 && this.currentWeather !== this.targetWeather;
  }
  /**
   * 获取过渡进度
   * 
   * @return {Number} 过渡进度 (0-1)
   */
  getTransitionProgress() {
    return this.transitionProgress;
  }
}
const vc = `// Sky and Fog Effect (修改自 Lakes and Mountains Effect)
// 仅保留雾气效果部分

uniform float time;           // 替代 iTime
uniform vec2 resolution;      // 替代 iResolution
uniform sampler2D noiseTexture; // 替代 iChannel0

// 相机参数 - 从Three.js传入
uniform vec3 cameraPosition;
uniform mat4 viewMatrix;      // 注意：这里实际存储的是相机的世界矩阵(matrixWorld)
uniform float cameraFov;

// 效果参数
uniform float fogDensity;     // 雾气密度
uniform vec3 fogColor;        // 雾气颜色
uniform float fogDecay;       // 雾气衰减速度
uniform float fogMinDist;     // 雾气最小距离
uniform float quality;        // 质量设置 (0-低, 1-中, 2-高)
uniform float occludeSky;     // 雾气遮挡天空的程度 (0-1)

// 高度雾参数
uniform bool useHeightFog;    // 是否使用高度雾
uniform bool combineFog;      // 是否同时启用普通雾和高度雾（叠加显示）
uniform float fogMaxHeight;   // 雾气最大高度
uniform float fogMinHeight;   // 雾气最小高度，低于此高度不会有雾
uniform float fogNoiseScale;  // 雾气噪声缩放
uniform float fogNoiseStrength; // 雾气噪声强度
uniform float heightFogStrength; // 高度雾强度
uniform float heightFogDecay;   // 高度雾衰减速度
uniform float heightFogMinDist; // 高度雾最小距离
uniform float heightFogMaxDist; // 高度雾最大距离
uniform float heightFogTransition; // 高度雾过渡距离，控制上下边缘的平滑过渡

// 噪声函数
float hash(float n) {
    return fract(sin(n)*43758.5453123);
}

float noise(in vec2 x) {
    vec2 p = floor(x);
    vec2 f = fract(x);
    f = f*f*(3.0-2.0*f);
    float n = p.x + p.y*57.0;
    float res = mix(mix(hash(n+  0.0), hash(n+  1.0), f.x),
                    mix(hash(n+ 57.0), hash(n+ 58.0), f.x), f.y);
    return res;
}

float noiseHigh(in vec3 x) {
    vec3 p = floor(x);
    vec3 f = fract(x);
    f = f*f*(3.0-2.0*f);
    float n = p.x + p.y*57.0 + 113.0*p.z;
    float res = mix(mix(mix(hash(n+  0.0), hash(n+  1.0), f.x),
                        mix(hash(n+ 57.0), hash(n+ 58.0), f.x), f.y),
                    mix(mix(hash(n+113.0), hash(n+114.0), f.x),
                        mix(hash(n+170.0), hash(n+171.0), f.x), f.y), f.z);
    return res;
}

// 重建世界坐标的函数 - 改进版本
vec3 reconstructWorldPos(vec2 uv, float depth) {
    // 计算NDC空间的坐标 (-1 到 1 的范围)
    float z = depth * 2.0 - 1.0;
    vec2 screenPos = uv * 2.0 - 1.0;
    
    // 在相机空间中的射线方向 - 使用更精确的计算
    float fovFactor = tan(radians(cameraFov * 0.5));
    vec3 viewDir = normalize(vec3(
        screenPos.x * resolution.x / resolution.y * fovFactor, 
        screenPos.y * fovFactor, 
        -1.0
    ));
    
    // 线性深度计算 - 更精确版本，尽量减少浮点精度问题
    float linearDepth = 2.0 * cameraNear * cameraFar / 
        (cameraFar + cameraNear - z * (cameraFar - cameraNear));
    
    // 计算相机空间的位置 - 使用标准化方向乘以线性深度
    vec3 cameraSpacePos = viewDir * linearDepth;
    
    // 转换为世界空间 - 确保使用正确的相机位置和方向
    return cameraPosition + (viewMatrix * vec4(cameraSpacePos, 0.0)).xyz;
}

// 计算雾因子的函数，应用自定义的雾参数
float calculateFogFactor(float linearDepth) {
    // 考虑最小距离
    float adjustedDepth = max(0.0, linearDepth - fogMinDist);
    
    // 使用指数函数计算雾量 - 更加平滑，减少角度依赖
    // 将fogDensity和fogDecay结合为单一系数，简化计算
    float fogAmount = 1.0 - exp(-adjustedDepth * fogDensity * fogDecay);
    
    // 确保雾限制在合理范围内
    return clamp(fogAmount, 0.0, 1.0);
}

// 计算高度雾因子 - 修改版本
float calculateHeightFogFactor(vec3 worldPos, float linearDepth) {
    // 修改距离判断逻辑 - 减少对高度雾的影响
    float distFactor = 1.0;
    if (linearDepth < heightFogMinDist) {
        distFactor = smoothstep(0.0, heightFogMinDist, linearDepth);
    } else if (linearDepth > heightFogMaxDist) {
        // 使用更缓慢的衰减，减少对大范围的敏感性
        distFactor = 1.0 - smoothstep(heightFogMaxDist * 0.9, heightFogMaxDist * 1.1, linearDepth);
    }
    
    // 获取噪声值，用于扰动高度
    float noiseValue = noise(worldPos.xz * fogNoiseScale) * fogNoiseStrength;
    
    // 计算实际高度，添加噪声扰动
    float adjustedHeight = worldPos.y + noiseValue;
    
    // 使用过渡距离参数计算平滑的高度因子
    float lowerTransitionStart = fogMinHeight;
    float lowerTransitionEnd = fogMinHeight + heightFogTransition;
    float upperTransitionStart = fogMaxHeight - heightFogTransition;
    float upperTransitionEnd = fogMaxHeight;
    
    float heightFactor = 0.0;
    
    // 下边缘过渡区域
    if (adjustedHeight >= lowerTransitionStart && adjustedHeight <= lowerTransitionEnd) {
        // 在下边缘平滑过渡区内，使用平滑步进
        heightFactor = smoothstep(lowerTransitionStart, lowerTransitionEnd, adjustedHeight);
    } 
    // 中间完全雾化区域
    else if (adjustedHeight > lowerTransitionEnd && adjustedHeight < upperTransitionStart) {
        // 在中间区域，保持完全雾化
        heightFactor = 1.0;
    } 
    // 上边缘过渡区域
    else if (adjustedHeight >= upperTransitionStart && adjustedHeight <= upperTransitionEnd) {
        // 在上边缘平滑过渡区内，使用平滑步进的反向版本
        heightFactor = 1.0 - smoothstep(upperTransitionStart, upperTransitionEnd, adjustedHeight);
    }
    
    // 使用新的角度无关的计算方式
    // 保持基于距离的衰减，但移除对视角的直接依赖
    float baseIntensity = heightFogStrength * heightFactor * distFactor;
    float fogAmount = 1.0 - exp(-baseIntensity * heightFogDecay);
    
    return clamp(fogAmount, 0.0, 1.0);
}

// PostProcessing框架的主函数
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
    // 根据深度混合场景
    if (USE_DEPTH == 1) {
        // 读取深度
        float depth = texture2D(depthBuffer, uv).r;
        
        // 将深度转换为线性深度 - 使用更稳定的计算方式
        // 这种方式更不容易受到浮点精度问题的影响
        float linearDepth;
        if (depth >= 0.9999) {
            // 对于远处(天空)使用最大值
            linearDepth = 1.0;
        } else {
            // 标准线性深度计算
            float z = depth * 2.0 - 1.0;
            linearDepth = 2.0 * cameraNear * cameraFar / 
                (cameraFar + cameraNear - z * (cameraFar - cameraNear));
            // 归一化，但使用更大的缩放因子来提高近处的精度
            linearDepth = linearDepth / (cameraFar * 2.0); 
            linearDepth = clamp(linearDepth, 0.0, 0.5);
            // 重新映射到0-1范围
            linearDepth = linearDepth * 2.0;
        }
        
        // 基于深度的混合
        float skyMask = step(0.9999, depth); // 天空部分（最远处）
        
        float fogFactor;
        
        // 根据设置决定使用哪种雾算法
        if (useHeightFog && depth < 0.9999) {
            // 使用高度雾 - 先重建世界坐标
            vec3 worldPos = reconstructWorldPos(uv, depth);
            
            // 使用修改后的计算方式获取高度雾因子
            float heightFogFactor = calculateHeightFogFactor(worldPos, linearDepth);
            
            // 确保天空区域不受高度雾影响，除非特别设置了天空遮挡
            if (skyMask > 0.5 && occludeSky <= 0.0) {
                heightFogFactor = 0.0;
            }
            
            // 判断是否同时使用普通雾和高度雾
            if (combineFog) {
                // 计算普通雾的雾因子
                float standardFogFactor = calculateFogFactor(linearDepth);
                
                // 混合两种雾因子 - 使用屏幕空间混合模式，使两种雾叠加而不过度强化
                // 公式: 1.0 - (1.0 - a) * (1.0 - b)，类似于Photoshop的"变亮"混合模式
                fogFactor = 1.0 - (1.0 - heightFogFactor) * (1.0 - standardFogFactor);
            } else {
                // 仅使用高度雾
                fogFactor = heightFogFactor;
            }
        } else {
            // 使用标准雾
            fogFactor = calculateFogFactor(linearDepth);
        }
        
        // 应用雾效果到输入颜色
        vec3 foggedScene = mix(inputColor.rgb, fogColor, fogFactor);
        outputColor = vec4(foggedScene, inputColor.a);
    } else {
        // 不使用深度时直接返回输入颜色
        outputColor = inputColor;
    }
} `;
class dd extends I {
  /**
      * 构建一个新的雾气效果
      *
      * @param {Object} [options] - 效果选项
      * @param {Number} [options.fogDensity=1.0] - 雾气密度
      * @param {Color|String|Number} [options.fogColor=0xaaaaaa] - 雾气颜色
      * @param {Number} [options.fogDecay=1.0] - 雾气衰减速度
      * @param {Number} [options.fogMinDist=0.0] - 雾气最小距离
      * @param {Number} [options.quality=0.5] - 质量设置 (0-1)
      * @param {Number} [options.occludeSky=0.0] - 雾气遮挡天空的程度 (0-1)
      * @param {Boolean} [options.useHeightFog=false] - 是否使用高度雾，根据高度和噪声生成雾效果
      * @param {Boolean} [options.combineFog=false] - 是否同时启用普通雾和高度雾（叠加显示）
      * @param {Number} [options.fogMaxHeight=50.0] - 雾气最大高度，超过此高度不会有雾
      * @param {Number} [options.fogMinHeight=-10.0] - 雾气最小高度，低于此高度不会有雾
      * @param {Number} [options.fogNoiseScale=0.01] - 雾气噪声缩放，影响噪声的密度
      * @param {Number} [options.fogNoiseStrength=15.0] - 雾气噪声强度，影响噪声对高度的扰动量
      * @param {Number} [options.heightFogStrength=2.5] - 高度雾强度，独立控制高度雾的强度
      * @param {Number} [options.heightFogDecay=0.5] - 高度雾衰减速度，独立控制高度雾的衰减
      * @param {Number} [options.heightFogMinDist=0.0] - 高度雾的最小距离，独立于标准雾的最小距离
      * @param {Number} [options.heightFogMaxDist=4000.0] - 高度雾的最大距离，超过此距离不会显示高度雾
      * @param {Number} [options.heightFogTransition=5.0] - 高度雾的过渡距离，控制上下边缘的平滑过渡
      * @param {Object} [options.camera] - 相机对象，用于获取视图矩阵和位置
      */
  constructor({
    fogDensity: e = 1,
    fogColor: t = 16777215,
    fogDecay: r = 1,
    fogMinDist: n = 0,
    quality: i = 0.5,
    occludeSky: s = 0.1,
    useHeightFog: o = !1,
    combineFog: l = !1,
    fogMaxHeight: u = 50,
    fogMinHeight: f = -10,
    fogNoiseScale: h = 0.01,
    fogNoiseStrength: m = 15,
    heightFogStrength: p = 2.5,
    heightFogDecay: d = 0.5,
    heightFogMinDist: v = 0,
    heightFogMaxDist: x = 4e3,
    heightFogTransition: w = 5,
    camera: M = null,
    noiseTexture: D
  } = {}) {
    const P = new y(t);
    super("LakesMountainsEffect", vc, {
      attributes: Q.DEPTH,
      defines: /* @__PURE__ */ new Map([
        ["USE_DEPTH", "1"]
      ]),
      uniforms: /* @__PURE__ */ new Map([
        ["resolution", new a(new g())],
        ["time", new a(0)],
        ["noiseTexture", new a(D)],
        ["fogDensity", new a(e)],
        ["fogColor", new a(new T(P.r, P.g, P.b))],
        ["fogDecay", new a(r)],
        ["fogMinDist", new a(n)],
        ["quality", new a(i)],
        ["occludeSky", new a(s)],
        ["useHeightFog", new a(o)],
        ["combineFog", new a(l)],
        ["fogMaxHeight", new a(u)],
        ["fogMinHeight", new a(f)],
        ["fogNoiseScale", new a(h)],
        ["fogNoiseStrength", new a(m)],
        ["heightFogStrength", new a(p)],
        ["heightFogDecay", new a(d)],
        ["heightFogMinDist", new a(v)],
        ["heightFogMaxDist", new a(x)],
        ["heightFogTransition", new a(w)],
        ["cameraPosition", new a(new T())],
        ["viewMatrix", new a(new C())],
        ["cameraFov", new a(45)]
      ])
    }), this.camera = M, this.useHeightFog = o, this.combineFog = l, this.fogMaxHeight = u, this.fogMinHeight = f, this.fogNoiseScale = h, this.fogNoiseStrength = m, this.heightFogStrength = p, this.heightFogDecay = d, this.heightFogMinDist = v, this.heightFogMaxDist = x, this.heightFogTransition = w;
  }
  /**
      * 更新效果
      *
      * @param {WebGLRenderer} renderer - WebGL渲染器
      * @param {WebGLRenderTarget} inputBuffer - 输入缓冲区
      * @param {Number} [deltaTime] - 自上一帧以来经过的时间（秒）
      */
  update(e, t, r) {
    this.uniforms.get("time").value += r, this.uniforms.get("resolution").value.set(
      t.width,
      t.height
    ), this.camera && (this.uniforms.get("cameraPosition").value.copy(this.camera.position), this.uniforms.get("viewMatrix").value.copy(this.camera.matrixWorld), this.uniforms.get("cameraFov").value = this.camera.fov);
  }
  /**
      * 相机对象
      */
  get camera() {
    return this._camera;
  }
  set camera(e) {
    this._camera = e, e && (this.uniforms.get("cameraPosition").value.copy(e.position), this.uniforms.get("viewMatrix").value.copy(e.matrixWorld), this.uniforms.get("cameraFov").value = e.fov);
  }
  /**
      * 雾气密度
      */
  get fogDensity() {
    return this.uniforms.get("fogDensity").value;
  }
  set fogDensity(e) {
    this.uniforms.get("fogDensity").value = e;
  }
  /**
      * 雾气颜色
      */
  get fogColor() {
    const e = this.uniforms.get("fogColor").value;
    return new y(e.x, e.y, e.z);
  }
  set fogColor(e) {
    const t = new y(e);
    this.uniforms.get("fogColor").value.set(t.r, t.g, t.b);
  }
  /**
      * 雾气衰减速度
      */
  get fogDecay() {
    return this.uniforms.get("fogDecay").value;
  }
  set fogDecay(e) {
    this.uniforms.get("fogDecay").value = e;
  }
  /**
      * 雾气最小距离
      */
  get fogMinDist() {
    return this.uniforms.get("fogMinDist").value;
  }
  set fogMinDist(e) {
    this.uniforms.get("fogMinDist").value = e;
  }
  /**
      * 质量设置
      */
  get quality() {
    return this.uniforms.get("quality").value;
  }
  set quality(e) {
    this.uniforms.get("quality").value = e;
  }
  /**
      * 雾气遮挡天空的程度 (0-1)
      */
  get occludeSky() {
    return this.uniforms.get("occludeSky").value;
  }
  set occludeSky(e) {
    this.uniforms.get("occludeSky").value = e;
  }
  /**
      * 是否使用高度雾
      */
  get useHeightFog() {
    return this.uniforms.get("useHeightFog").value;
  }
  set useHeightFog(e) {
    this.uniforms.get("useHeightFog").value = e;
  }
  /**
      * 是否同时启用普通雾和高度雾（叠加显示）
      */
  get combineFog() {
    return this.uniforms.get("combineFog").value;
  }
  set combineFog(e) {
    this.uniforms.get("combineFog").value = e;
  }
  /**
      * 雾气最大高度
      */
  get fogMaxHeight() {
    return this.uniforms.get("fogMaxHeight").value;
  }
  set fogMaxHeight(e) {
    this.uniforms.get("fogMaxHeight").value = e;
  }
  /**
      * 雾气最小高度
      */
  get fogMinHeight() {
    return this.uniforms.get("fogMinHeight").value;
  }
  set fogMinHeight(e) {
    this.uniforms.get("fogMinHeight").value = e;
  }
  /**
      * 雾气噪声缩放
      */
  get fogNoiseScale() {
    return this.uniforms.get("fogNoiseScale").value;
  }
  set fogNoiseScale(e) {
    this.uniforms.get("fogNoiseScale").value = e;
  }
  /**
      * 雾气噪声强度
      */
  get fogNoiseStrength() {
    return this.uniforms.get("fogNoiseStrength").value;
  }
  set fogNoiseStrength(e) {
    this.uniforms.get("fogNoiseStrength").value = e;
  }
  /**
      * 高度雾强度
      */
  get heightFogStrength() {
    return this.uniforms.get("heightFogStrength").value;
  }
  set heightFogStrength(e) {
    this.uniforms.get("heightFogStrength").value = e;
  }
  /**
      * 高度雾衰减速度
      */
  get heightFogDecay() {
    return this.uniforms.get("heightFogDecay").value;
  }
  set heightFogDecay(e) {
    this.uniforms.get("heightFogDecay").value = e;
  }
  /**
      * 高度雾最小距离
      */
  get heightFogMinDist() {
    return this.uniforms.get("heightFogMinDist").value;
  }
  set heightFogMinDist(e) {
    this.uniforms.get("heightFogMinDist").value = e;
  }
  /**
      * 高度雾最大距离
      */
  get heightFogMaxDist() {
    return this.uniforms.get("heightFogMaxDist").value;
  }
  set heightFogMaxDist(e) {
    this.uniforms.get("heightFogMaxDist").value = e;
  }
  /**
      * 高度雾过渡距离
      */
  get heightFogTransition() {
    return this.uniforms.get("heightFogTransition").value;
  }
  set heightFogTransition(e) {
    this.uniforms.get("heightFogTransition").value = e;
  }
}
const gc = `// 体积大气散射着色器\r
// 基于László Matuska的Shadertoy代码，专注于体积雾和大气散射效果\r
\r
uniform sampler2D noiseTexture; // 噪声纹理\r
uniform vec2 resolution; // 分辨率\r
uniform float time; // 时间\r
uniform vec3 sunPosition; // 太阳/光源位置\r
uniform int animateClouds; // 云/雾动画开关\r
uniform float fogDensity; // 雾密度（替代原cloudy参数）\r
uniform float height; // 观察者高度\r
uniform float hazeDensity; // 雾霾密度（替代原haze参数）\r
uniform float scatteringStrength; // 散射强度（新参数）\r
uniform float fogColor; // 雾颜色色调（新参数，0-1之间，0=蓝色，1=暖色）\r
uniform float fogDepthMask; // 雾的深度阈值\r
uniform mat4 viewMatrix; // 视图矩阵\r
uniform float fov; // 视场角\r
\r
// 常量定义\r
const float M_PI = 3.1415926535;\r
const float DEGRAD = M_PI / 180.0;\r
\r
// 渲染质量参数 - 可以提高步数以获得更好的质量\r
const int steps = 20; // 散射采样步数\r
const int stepss = 4; // 次级光线采样步数\r
\r
// 散射参数\r
const float I = 12.0; // 光源强度\r
const float g = 0.45; // 光线集中度参数（Mie相位函数参数）\r
const float g2 = g * g;\r
\r
// 瑞利散射和米氏散射基础系数\r
const vec3 bR = vec3(5.8e-6, 13.5e-6, 33.1e-6); // 瑞利散射系数（控制颜色）\r
const vec3 bM = vec3(21e-6); // 米氏散射系数（控制体积雾密度）\r
\r
// 大气参数\r
const float Hr = 8000.0; // 瑞利散射高度\r
const float Hm = 1200.0; // 米氏散射高度\r
const float R0 = 6360e3; // 星球半径\r
const float Ra = 6380e3; // 大气层半径\r
const vec3 C = vec3(0.0, -R0, 0.0); // 星球中心\r
\r
//--------------------------------------------------------------------------\r
// 噪声相关函数\r
\r
float Noise(in vec3 x) {\r
    vec3 p = floor(x);\r
    vec3 f = fract(x);\r
    f = f * f * (3.0 - 2.0 * f);\r
\r
    vec2 uv = (p.xy + vec2(37.0, 17.0) * p.z) + f.xy;\r
    vec2 rg = texture(noiseTexture, (uv + 0.5) / 256.0).yx;\r
    return mix(rg.x, rg.y, f.z);\r
}\r
\r
float fnoise(vec3 p, in float t) {\r
    p *= 0.25;\r
    float f;\r
\r
    f = 0.5000 * Noise(p); p = p * 3.02; p.y -= t * 0.2;\r
    f += 0.2500 * Noise(p); p = p * 3.03; p.y += t * 0.06;\r
    f += 0.1250 * Noise(p); p = p * 3.01;\r
    f += 0.0625 * Noise(p); p = p * 3.03;\r
    f += 0.03125 * Noise(p); p = p * 3.02;\r
    f += 0.015625 * Noise(p);\r
    return f;\r
}\r
\r
//--------------------------------------------------------------------------\r
// 云雾和散射函数\r
\r
float volumetricFog(vec3 p, in float t) {\r
    // 使用噪声创建非均匀雾效果\r
    float fog = fnoise(p * 2e-4, t) + fogDensity * 0.1;\r
    fog = smoothstep(0.4 + 0.04, 0.6 + 0.04, fog);\r
    fog *= 70.0; // 增加雾的强度\r
    return fog + hazeDensity; // 加上均匀雾霾\r
}\r
\r
// 密度计算函数，计算散射介质的密度\r
void densities(in vec3 pos, out float rayleigh, out float mie, in float t) {\r
    float h = length(pos - C) - R0; // 当前点到地表的高度\r
    rayleigh = exp(-h / Hr); // 瑞利密度随高度指数衰减\r
    \r
    vec3 d = pos;\r
    d.y = 0.0;\r
    float dist = length(d); // 水平距离\r
    \r
    // 体积雾处理\r
    float fog = 0.0;\r
    if (1e3 < h && h < 8e3) {\r
        fog = volumetricFog(pos + vec3(23175.7, 0.0, -t * 3e3), t);\r
        fog *= sin(3.1415 * (h - 1e3) / 7e3) * fogDensity;\r
    }\r
    \r
    // 高层雾处理\r
    float fog2 = 0.0;\r
    if (9e3 < h && h < 15e3) {\r
        fog2 = fnoise(pos * 3e-4, t) * volumetricFog(pos * 32.0 + vec3(27612.3, 0.0, -t * 15e3), t);\r
        fog2 *= sin(3.1413 * (h - 9e3) / 6e3) * fogDensity * 0.7;\r
        fog2 = clamp(fog2, 0.0, 1.0);\r
    }\r
\r
    // 距离衰减\r
    const float fogNear = 10.0;\r
    const float fogFar = 1000.0;\r
    if (dist < fogFar) {\r
        float factor = clamp(1.0 - ((fogFar - dist) / (fogFar - fogNear)), 0.0, 1.0);\r
        fog *= factor;\r
    }\r
\r
    mie = exp(-h / Hm) + fog + hazeDensity + fog2;\r
}\r
\r
// 求交函数，计算射线与球面相交\r
float escape(in vec3 p, in vec3 d, in float R) {\r
    vec3 v = p - C;\r
    float b = dot(v, d);\r
    float c = dot(v, v) - R * R;\r
    float det2 = b * b - c;\r
    if (det2 < 0.0) return -1.0;\r
    float det = sqrt(det2);\r
    float t1 = -b - det, t2 = -b + det;\r
    return (t1 >= 0.0) ? t1 : t2;\r
}\r
\r
// 大气散射主函数\r
void scatter(vec3 o, vec3 d, out vec3 col, out float scattering, in float t) {\r
    float L = escape(o, d, Ra); // 计算光线穿过大气层的长度\r
    \r
    // 计算光源方向\r
    vec3 Ds = normalize(sunPosition);\r
    float mu = dot(d, Ds); // 视线与光源方向的夹角余弦\r
    float opmu2 = 1.0 + mu * mu;\r
    \r
    // 相位函数计算\r
    float phaseR = 0.0596831 * opmu2; // 瑞利散射相位函数\r
    float phaseM = 0.1193662 * (1.0 - g2) * opmu2 / ((2.0 + g2) * pow(1.0 + g2 - 2.0 * g * mu, 1.5)); // 米氏散射相位函数\r
\r
    float depthR = 0.0, depthM = 0.0;\r
    vec3 R = vec3(0.0), M = vec3(0.0);\r
\r
    // 主射线步进\r
    float dl = L / float(steps);\r
    for (int i = 0; i < steps; ++i) {\r
        float l = float(i) * dl;\r
        vec3 p = o + d * l; // 当前采样点\r
\r
        float dR, dM;\r
        densities(p, dR, dM, t); // 获取当前点的散射介质密度\r
        dR *= dl; dM *= dl;\r
        depthR += dR;\r
        depthM += dM;\r
\r
        // 从当前点到光源的光线计算\r
        float Ls = escape(p, Ds, Ra);\r
        if (Ls > 0.0) {\r
            float dls = Ls / float(stepss);\r
            float depthRs = 0.0, depthMs = 0.0;\r
            \r
            // 次级光线步进\r
            for (int j = 0; j < stepss; ++j) {\r
                float ls = float(j) * dls;\r
                vec3 ps = p + Ds * ls;\r
                float dRs, dMs;\r
                densities(ps, dRs, dMs, t);\r
                depthRs += dRs * dls;\r
                depthMs += dMs * dls;\r
            }\r
\r
            // 散射计算\r
            vec3 A = exp(-(bR * (depthRs + depthR) + bM * (depthMs + depthM)));\r
            vec3 dA = (A - exp(-(bR * depthR + bM * depthM))) * I;\r
            R += dA * dR * phaseR;\r
            M += dA * dM * phaseM;\r
        }\r
    }\r
    \r
    // 调整颜色色调（根据fogColor参数）\r
    vec3 fogTint = mix(\r
        vec3(0.6, 0.8, 1.0), // 蓝色雾调\r
        vec3(1.0, 0.8, 0.6), // 暖色雾调\r
        fogColor\r
    );\r
    \r
    // 应用散射强度\r
    col = ((R * bR) + (M * bM * fogTint)) * scatteringStrength;\r
    scattering = (1.0 - exp(-length(bM) * depthM)) * scatteringStrength;\r
}\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
    // 读取深度缓冲区\r
    float depth = texture(depthBuffer, uv).r;\r
    \r
    // 计算UV方向\r
    vec2 q = uv * 2.0 - 1.0;\r
    q.x *= resolution.x / resolution.y;\r
    \r
    // 计算视图方向\r
    vec3 vuv = vec3(viewMatrix[0][1], viewMatrix[1][1], viewMatrix[2][1]);\r
    vec3 vrgt = vec3(viewMatrix[0][0], viewMatrix[1][0], viewMatrix[2][0]);\r
    vec3 vfwd = -vec3(viewMatrix[0][2], viewMatrix[1][2], viewMatrix[2][2]);\r
    \r
    // 根据FOV计算视线方向\r
    float rad = fov * DEGRAD / 2.0;\r
    float vtan = tan(rad);\r
    vec3 dir = normalize(vfwd + q.x * vrgt * vtan + q.y * vuv * vtan);\r
    \r
    // 计算观察者位置\r
    vec3 ro = vec3(0.0, height, 0.0);\r
    \r
    // 渲染大气散射\r
    vec3 atmosphere = vec3(0.0);\r
    float scattering = 0.0;\r
    float t = time * 0.04 * float(animateClouds); // 动画时间\r
    \r
    // 计算地平线以上的天空\r
    if (dir.y > -0.05) {\r
        // 大气散射计算\r
        scatter(ro, dir, atmosphere, scattering, t);\r
    } else {\r
        // 对于地平线以下，使用简单渐变\r
        atmosphere = mix(\r
            vec3(0.05, 0.1, 0.2) * hazeDensity, // 地面颜色\r
            vec3(0.1, 0.15, 0.25) * hazeDensity, // 地平线颜色\r
            smoothstep(-0.2, -0.05, dir.y)\r
        );\r
    }\r
    \r
    // 根据深度混合大气和原始图像 - 改进版，防止边缘锐利变化\r
    // 增加软边界混合和噪声扰动\r
    float fogMaskBase = smoothstep(fogDepthMask, fogDepthMask + 0.01, depth);\r
    \r
    // 使用水平位置添加一些随机变化，打破硬边界\r
    vec2 screenPos = gl_FragCoord.xy / resolution;\r
    float noiseOffset = fnoise(vec3(screenPos * 100.0, time * 0.5), t) * 0.005;\r
    \r
    // 更平滑的边缘\r
    float fogMask = smoothstep(0.0, 1.0, fogMaskBase + noiseOffset);\r
    \r
    // 确保在深度接近1.0时始终有雾效果（远处）\r
    float farFog = smoothstep(0.998, 0.9999, depth);\r
    fogMask = max(fogMask, farFog);\r
    \r
    // 输出结果：混合原始颜色和大气颜色\r
    outputColor = mix(inputColor, vec4(atmosphere, 1.0), fogMask);\r
}\r
\r
void main() {\r
    vec4 inputColor = texture(inputBuffer, vUv);\r
    vec4 outputColor = vec4(0.0);\r
    \r
    mainImage(inputColor, vUv, outputColor);\r
    \r
    gl_FragColor = outputColor;\r
} `;
class md extends I {
  /**
      * 构造函数
      *
      * @param {Object} [options] - 效果选项
      * @param {BlendFunction} [options.blendFunction=BlendFunction.NORMAL] - 混合模式
      * @param {Vector3} [options.sunPosition=new Vector3(0.1, 0.5, -1).normalize()] - 光源位置（方向向量）
      * @param {Boolean} [options.animateClouds=true] - 是否启用雾动画
      * @param {Number} [options.fogDensity=0.6] - 雾密度 (0-1)
      * @param {Number} [options.height=500.0] - 观察者高度
      * @param {Number} [options.hazeDensity=0.1] - 雾霾密度
      * @param {Number} [options.scatteringStrength=1.0] - 散射强度
      * @param {Number} [options.fogColor=0.3] - 雾颜色色调 (0-1，0=蓝色，1=暖色)
      * @param {Number} [options.fogDepthMask=0.998] - 雾深度阈值
      * @param {Number} [options.fov=60.0] - 视场角（度）
      */
  constructor({
    blendFunction: e = E.NORMAL,
    sunPosition: t = new T(0.1, 0.5, -1).normalize(),
    animateClouds: r = !0,
    fogDensity: n = 0.6,
    height: i = 500,
    hazeDensity: s = 0.1,
    scatteringStrength: o = 1,
    fogColor: l = 0.3,
    fogDepthMask: u = 0.998,
    fov: f = 60
  } = {}) {
    super("VolumetricAtmosphereEffect", gc, {
      blendFunction: e,
      attributes: Q.DEPTH,
      defines: /* @__PURE__ */ new Map([
        ["USE_DEPTH", "1"]
      ]),
      uniforms: /* @__PURE__ */ new Map([
        ["sunPosition", new a(t)],
        ["animateClouds", new a(r ? 1 : 0)],
        ["fogDensity", new a(n)],
        ["height", new a(i)],
        ["hazeDensity", new a(s)],
        ["scatteringStrength", new a(o)],
        ["fogColor", new a(l)],
        ["fogDepthMask", new a(u)],
        ["time", new a(0)],
        ["resolution", new a(new g(1, 1))],
        ["noiseTexture", new a(null)],
        ["viewMatrix", new a(new C())],
        ["fov", new a(f)]
      ])
    }), this.sunPosition = t, this.animateClouds = r, this._fogDensity = n, this._height = i, this._hazeDensity = s, this._scatteringStrength = o, this._fogColor = l, this._fogDepthMask = u, this._fov = f, this._camera = null, this.createDefaultNoiseTexture();
  }
  /**
      * 获取雾密度
      * @return {Number} 雾密度
      */
  get fogDensity() {
    return this._fogDensity;
  }
  /**
      * 设置雾密度
      * @param {Number} value - 雾密度
      */
  set fogDensity(e) {
    this._fogDensity = e, this.uniforms.get("fogDensity").value = e;
  }
  /**
      * 获取观察者高度
      * @return {Number} 观察者高度
      */
  get height() {
    return this._height;
  }
  /**
      * 设置观察者高度
      * @param {Number} value - 观察者高度
      */
  set height(e) {
    this._height = e, this.uniforms.get("height").value = e;
  }
  /**
      * 获取雾霾密度
      * @return {Number} 雾霾密度
      */
  get hazeDensity() {
    return this._hazeDensity;
  }
  /**
      * 设置雾霾密度
      * @param {Number} value - 雾霾密度
      */
  set hazeDensity(e) {
    this._hazeDensity = e, this.uniforms.get("hazeDensity").value = e;
  }
  /**
      * 获取散射强度
      * @return {Number} 散射强度
      */
  get scatteringStrength() {
    return this._scatteringStrength;
  }
  /**
      * 设置散射强度
      * @param {Number} value - 散射强度
      */
  set scatteringStrength(e) {
    this._scatteringStrength = e, this.uniforms.get("scatteringStrength").value = e;
  }
  /**
      * 获取雾颜色色调
      * @return {Number} 雾颜色色调
      */
  get fogColor() {
    return this._fogColor;
  }
  /**
      * 设置雾颜色色调
      * @param {Number} value - 雾颜色色调
      */
  set fogColor(e) {
    this._fogColor = e, this.uniforms.get("fogColor").value = e;
  }
  /**
      * 获取雾深度阈值
      * @return {Number} 雾深度阈值
      */
  get fogDepthMask() {
    return this._fogDepthMask;
  }
  /**
      * 设置雾深度阈值
      * @param {Number} value - 雾深度阈值
      */
  set fogDepthMask(e) {
    this._fogDepthMask = e, this.uniforms.get("fogDepthMask").value = e;
  }
  /**
      * 获取视场角
      * @return {Number} 视场角（度）
      */
  get fov() {
    return this._fov;
  }
  /**
      * 设置视场角
      * @param {Number} value - 视场角（度）
      */
  set fov(e) {
    this._fov = e, this.uniforms.get("fov").value = e;
  }
  /**
      * 创建默认噪声纹理
      * 用于在没有设置纹理时提供基本噪声
      * @private
      */
  createDefaultNoiseTexture() {
    const t = new Uint8Array(16384);
    for (let n = 0; n < 4096; n++)
      t[n * 4] = Math.random() * 255, t[n * 4 + 1] = Math.random() * 255, t[n * 4 + 2] = Math.random() * 255, t[n * 4 + 3] = 255;
    const r = new le(t, 64, 64);
    r.wrapS = r.wrapT = G, r.minFilter = F, r.magFilter = F, r.needsUpdate = !0, this.uniforms.get("noiseTexture").value = r;
  }
  /**
      * 设置相机
      *
      * @param {Camera} camera - 相机对象
      */
  setCamera(e) {
    this._camera = e, e && (this.fov = e.fov);
  }
  /**
      * 设置光源位置
      * 使用高度角和方位角设置光源位置
      *
      * @param {Number} elevation - 高度角（度）
      * @param {Number} azimuth - 方位角（度）
      */
  setLightPosition(e, t) {
    const r = Ee.degToRad(90 - e), n = Ee.degToRad(t), i = Math.sin(r) * Math.cos(n), s = Math.cos(r), o = Math.sin(r) * Math.sin(n);
    this.sunPosition.set(i, s, o).normalize(), this.uniforms.get("sunPosition").value.copy(this.sunPosition);
  }
  /**
      * 设置噪声纹理
      *
      * @param {Texture} texture - 噪声纹理
      */
  setNoiseTexture(e) {
    e !== null && (e.minFilter = F, e.magFilter = F, e.wrapS = e.wrapT = G), this.uniforms.get("noiseTexture").value = e;
  }
  /**
      * 更新效果
      *
      * @param {WebGLRenderer} renderer - WebGL渲染器
      * @param {WebGLRenderTarget} inputBuffer - 输入缓冲区
      * @param {Number} [deltaTime] - 时间增量
      */
  update(e, t, r) {
    this.uniforms.get("time").value += r;
    const n = e.getSize(new g());
    this.uniforms.get("resolution").value.set(n.width, n.height), this.uniforms.get("animateClouds").value = this.animateClouds ? 1 : 0, this.uniforms.get("fogDensity").value = this._fogDensity, this.uniforms.get("height").value = this._height, this.uniforms.get("hazeDensity").value = this._hazeDensity, this.uniforms.get("scatteringStrength").value = this._scatteringStrength, this.uniforms.get("fogColor").value = this._fogColor, this.uniforms.get("fogDepthMask").value = this._fogDepthMask, this._camera && (this.uniforms.get("viewMatrix").value.copy(this._camera.matrixWorld), this._camera.fov !== this._fov && (this.fov = this._camera.fov));
  }
}
const xc = `// Foggy Flyover by Kristian Sivonen (ruojake)
// 基于Shadertoy效果 - 适配到postprocessing框架
// CC BY-SA 4.0 (https://creativecommons.org/licenses/by-sa/4.0/)

// 统一变量声明 - 替代Shadertoy变量
uniform float time;         // 替代 iTime
uniform vec2 resolution;    // 替代 iResolution
uniform vec2 mouse;         // 替代 iMouse
uniform int frame;          // 替代 iFrame

// 相机参数 - 从Three.js传入
uniform vec3 cameraPosition;
uniform mat4 viewMatrix;
uniform float cameraFov;
uniform float cameraNear;
uniform float cameraFar;

// 效果参数
uniform float fogDensity;   // 控制雾气浓度
uniform float enableLowCameraOptimization; // 低位置相机优化标志

// Hash without sine by Dave Hoskins
// https://www.shadertoy.com/view/4djSRW
float hash12(vec2 p)
{
	vec3 p3  = fract(vec3(p.xyx) * .1031);
    p3 += dot(p3, p3.yzx + 33.33);
    return fract((p3.x + p3.y) * p3.z);
}

float hash13(vec3 p3)
{
	p3  = fract(p3 * .1031);
    p3 += dot(p3, p3.yzx + 33.33);
    return fract((p3.x + p3.y) * p3.z);
}
// --

const vec2 o = vec2(1., 0.);

float noise(vec3 p)
{
	vec3 pi = floor(p);
    vec3 pf = smoothstep(0., 1., p - pi);
    return mix(
        mix(
        	mix(hash13(pi), hash13(pi+o.xyy), pf.x),
            mix(hash13(pi+o.yyx), hash13(pi+o.xyx), pf.x),
            pf.z
        ),
        mix(
        	mix(hash13(pi+o.yxy), hash13(pi+o.xxy), pf.x),
            mix(hash13(pi+o.yxx), hash13(pi+1.), pf.x),
            pf.z
        ),
    pf.y);
}

float noise(vec2 p)
{
	vec2 pi = floor(p);
    vec2 pf = smoothstep(0., 1., p - pi);
    vec2 r = mix(vec2(hash12(pi), hash12(pi+o.yx)), vec2(hash12(pi+o), hash12(pi+1.)), pf.x);
    return mix(r.x, r.y, pf.y);
}

const mat2 ROT = mat2(.98, -.198, .198, .98);

#define sat(v) clamp(v,0.,1.)

float fbm(vec2 p, float o)
{
	float res = 0.;
   	for(float i = 1.; i < o; i += i)
    {
        res += noise(p*i) / i;
    	p = p * ROT;
        o -= ROT[0][0] * .001 * sign(time);
    }
    return res;
}

float scene(vec3 p, float o)
{
	float res = fbm(p.xz, o) * .5;
    res *= 2.5 - res * res;
    return .4 * (p.y + res - 1.5);
}

vec3 normal(vec3 p, float o)
{
	float d = fbm(p.xz, o);
    vec2 e = vec2(.001, .0);
    return normalize(d - vec3(
        fbm(p.xz - e, o),
        d - 0.0011,
        fbm(p.xz - e.yx, o)));
}

float shadow(vec3 ro, vec3 rd, float maxDist, float k)
{
    float res = 1.;
    float d = 0.;
    float t = .01;
    for(int i = 0; i < 30; ++i)
    {
        d = scene(ro + rd * t, 16.);
        res = min(res, k * d / t);
        t += d;
    	if(abs(d) < .001 || t >= maxDist)
            break;
        if (res < .001)
        {
        	res = 0.;
            break;
        }
    }
    return res;
}

vec3 ray(vec3 ro, vec3 lookAt, vec2 uv, float zoom)
{
	vec3 f = normalize(lookAt - ro);
    vec3 r = cross(vec3(0., 1., 0.), f);
    vec3 u = cross(f, r);

    return normalize(uv.x * r + uv.y * u + f * zoom);
}

float clouds(vec3 p)
{
	float res = noise(p * 4.) * 2.;
    p.y -= time * .02;
    res -= noise(p * 11.);

    return sat(res * 4. * (1. - res));
}

vec3 material(vec3 p, vec3 n, float l, float t)
{
	float noise0 = fbm(p.xz * 20., 32.);
    float noise1 = fbm(p.xz * 310., 4.);
    noise1 = 2. * noise1 - 1.;
    float y = p.y;
    t = sat(t * .03 - .1);
    vec3 sunCol = vec3(1., .97, .85);
    
    vec3 forest = vec3(.03, .08, .01) + noise1 * .02;
    forest += l * .1 * sunCol;
    vec3 rock = vec3(.025) + noise1 * .01;
    rock += l * sunCol * .3;
    vec3 snow = vec3(.6, .6, .7);
    snow += l * .4 * sunCol;
    
    vec3 res = mix(rock, forest, smoothstep(.8, .5, y + .2 * noise0) * n.y * (2. - n.y));
    res = mix(res, snow, smoothstep(1.2 - t, 1.3 + t, y + .4 * noise0));
    return mix(res, res * vec3(.5, .55, .9), smoothstep(.3, 0., l));
}

// Shadertoy原始mainImage函数
void flyoverEffect(out vec4 fragColor, in vec2 fragCoord)
{
    vec2 uv = (fragCoord - resolution.xy * .5)/resolution.y;
	vec2 m = clamp((mouse / resolution.xy) * 2. - 1., vec2(-1.), vec2(1.));
    if (m == vec2(-1.,-1.)) m = vec2(0.);
    
    // 如果提供了相机信息，则使用相机位置和方向
    vec3 tgt, ro;
    
    if(length(cameraPosition) > 0.0) {
        // 使用相机位置作为起点
        ro = cameraPosition;
        
        // 计算目标点 - 使用视图矩阵获取前向方向
        vec3 viewForward = -normalize(vec3(viewMatrix[0][2], viewMatrix[1][2], viewMatrix[2][2]));
        tgt = ro + viewForward * 10.0;
        
        // 鼠标只作为微调，影响减小
        tgt.x += m.x * 0.5;
        tgt.y += m.y * 0.3;
    } else {
        // 使用原始Shadertoy相机设置
        tgt = vec3(m.x * 2. + 10., m.y + 1.4, 2. + time * .3 - max(abs(m.x), abs(m.y) * .5) * 2.);
        ro = vec3(10.,1.5, time * .3);
    }
    
    // 计算视点方向向量
    vec3 rd = ray(ro, tgt, uv, .8);
    float t = 0.;
    vec3 p;
    
    // 修改光线追踪逻辑：移除对rd.y的限制，让所有方向都可以渲染地形
    // 但保留对较高角度的优化处理
    float rdyThreshold = 0.6; // 约35度以上的视角可以直接判定为天空

    // 如果相机高度过低且启用了低位置优化，进一步降低阈值
    if (ro.y < 2.0 && enableLowCameraOptimization > 0.5) {
        rdyThreshold = min(rdyThreshold + (2.0 - ro.y) * 0.2, 0.95);
    }

    if (rd.y < rdyThreshold) // 只有非常陡峭向上的视线才判定为纯天空
    {
        for(int i = 0; i < 90; i++)
        {
            p = ro + rd * t;
            float d = scene(p, 16.);
            t += d * (1.25 - pow(abs(rd.y), 4.) + sat(t * .1 - 1.));
            if (abs(d) < .001 * (1.+t) || t > 30.) break;
        }
        t = t >= 30. ? 1000. : t;
    }
    else
    {
        t = 1000.;
        p = ro + rd * t;
    }
    
    float theta = .5 * 3.1415;
    vec3 lDir = normalize(vec3(-sin(theta), .25, cos(theta)));
    float dither = fract(13.013 * dot(fragCoord + float((frame & 7) * 17), vec2(.104212, .672709)));
    
    float maxl = .8;
    vec3 cFog = vec3(.1,.15,.2);
    vec4 fog = vec4(0.);
    float ft = max(ro.y - maxl, 0.) / (-rd.y + .0001);
    ft += dither * (.025 * ft + .1) * (1. - sat(-rd.y));
    vec3 v = vec3(0.,time * -.02,0.);
	float h = 1. - abs(rd.y);
    vec3 shadowCol = cFog * vec3(.4, .5, .7);
    float l = 0.;
    vec3 skyCol = mix(vec3(.7, .8, 1.), vec3(.1, .1, .4), sat(rd.y));
    
    vec3 col = vec3(0.);
	if (t < 500.)
    {
    
        if(p.y <= maxl)
            for(int i = 0; i < 35; i++)
            {
                float dt = .01 + h * .1;
                ft += dt;

                if (ft >= t) break;

                vec3 fp = (ro + rd * ft);

                if (rd.y >= 0. && fp.y > maxl) break;

                float d = clouds(fp + v) * (maxl + h) * dt * 10.;
                float fade = min(sat(maxl - fp.y), min(ft * .5, maxl - ft * .05));
                fade *= fade;
                d *= fade;
                ft += min(sat(h - fade) * .03, t - ft);
                if (d > .01)
                {
                    l = 0.;
                    float s = shadow(fp, lDir, 15. - fp.y, 20.);
                    if (s > .001)
                    {
                        s *= 2. - s;
                        l = sat(s * (1. - clouds(fp + lDir * .1 + v) * 3. * fade));
                    }
                    float w = sat((.1 * l + .91) - fog.a);
                    fog.rgb += mix(shadowCol, cFog + l, l) * d * w;
                    fog.a += d * w;

                    if (fog.a > .95) 
                    {
                        fog.a = 1.;
                        break;
                    }
                }
            }

        fog.rgb = mix(fog.rgb, skyCol, sat(t * .05 - .15));
        fog.a = min(fog.a, 1.);


        vec3 n = t < 10. ? t < 6. ? normal(p, 256.) : normal(p, 64.) : normal(p, 8.);
        l = sat(dot(n, lDir) * .8 + .2);
        float s = shadow(p + n * .1 + vec3(0.,.03,0.), lDir, 15., 14.);
        float sun = smoothstep(.9995, 1., dot(rd,lDir));	

        // 修改地形到天空的混合比例，考虑相机高度影响
        float heightMix = sat(t * .05 - .15);
        // 低位置相机时使用更渐进的混合
        if (ro.y < 1.0) {
            heightMix *= (1.0 - clamp(rd.y * 0.5 + 0.5, 0.0, 1.0));
        }

        col = rd.y <= rdyThreshold ? mix(
            vec3(material(p, n, l * s, t)), 
            skyCol, 
            heightMix) : skyCol;
        col += sun * 2. * smoothstep(1.,.9999, 30. - t);

        col = mix(col, fog.rgb, fog.a * fog.a);
    }
    else col = skyCol;
    
    col *= 1. - smoothstep(.45, .7, length((uv * resolution.y / resolution.xy))) * .5;
    col += (dither * .03 - .015) * col;
    fragColor = vec4(pow(col,vec3(1./2.2)),1.0);
}

// 适配到postprocessing框架的mainImage函数
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
    vec2 fragCoord = uv * resolution.xy;
    vec4 flyoverColor;
    flyoverEffect(flyoverColor, fragCoord);
    
    // 读取深度值
    float depth = texture2D(depthBuffer, uv).r;
    
    // 将深度值转换为线性深度 (0-1)
    float linearDepth = 2.0 * cameraNear * cameraFar / (cameraFar + cameraNear - (2.0 * depth - 1.0) * (cameraFar - cameraNear));
    linearDepth = linearDepth / cameraFar; // 归一化到0-1
    
    // 基于深度的混合策略:
    // 1. 远处(深度接近1)：使用完整的飞越效果
    // 2. 近处：根据距离逐渐应用雾气效果
    float skyMask = step(0.9999, depth); // 纯天空部分（最远处）
    
    // 基于线性深度的雾气混合因子
    float fogFactor = clamp(linearDepth * fogDensity * 2.0, 0.0, 1.0);
    
    // 两步混合:
    // 1. 先将输入颜色与雾气颜色基于深度混合，得到带雾的前景
    vec4 foggedColor = mix(inputColor, vec4(flyoverColor.rgb, 1.0), fogFactor);
    
    // 2. 在天空区域（最远处）使用完整的天空+雾气效果
    outputColor = mix(foggedColor, flyoverColor, skyMask);
} `;
class pd extends I {
  /**
      * 构造一个新的雾霾飞越效果
      *
      * @param {Object} [options] - 效果选项
      * @param {BlendFunction} [options.blendFunction=BlendFunction.NORMAL] - 混合模式
      * @param {Number} [options.fogDensity=1.0] - 雾气密度
      * @param {Boolean} [options.enableLowCameraOptimization=true] - 是否启用低位置相机优化
      */
  constructor({
    blendFunction: e = E.NORMAL,
    fogDensity: t = 1,
    enableLowCameraOptimization: r = !0
  } = {}) {
    super("FoggyFlyoverEffect", xc, {
      blendFunction: e,
      attributes: Q.DEPTH,
      uniforms: /* @__PURE__ */ new Map([
        ["time", new a(0)],
        ["resolution", new a(new g(1, 1))],
        ["mouse", new a(new g(0, 0))],
        ["frame", new a(0)],
        ["cameraPosition", new a(new T(0, 0, 0))],
        ["viewMatrix", new a(new C())],
        ["cameraFov", new a(60)],
        ["cameraNear", new a(0.1)],
        ["cameraFar", new a(1e3)],
        ["fogDensity", new a(t)],
        ["enableLowCameraOptimization", new a(r ? 1 : 0)]
      ])
    }), this._camera = null, this._enableLowCameraOptimization = r;
  }
  /**
      * 更新效果
      *
      * @param {WebGLRenderer} renderer - 渲染器
      * @param {WebGLRenderTarget} inputBuffer - 输入缓冲区
      * @param {Number} deltaTime - 自上次更新后经过的时间（秒）
      */
  update(e, t, r) {
    this.uniforms.get("time").value += r, this.uniforms.get("frame").value++;
    const n = e.getSize(new g());
    this.uniforms.get("resolution").value.set(n.width, n.height), this._updateCameraParameters();
  }
  /**
      * 更新相机参数
      * @private
      */
  _updateCameraParameters() {
    if (!this._camera)
      return;
    this.uniforms.get("cameraPosition").value.copy(this._camera.position), this.uniforms.get("viewMatrix").value.copy(this._camera.matrixWorldInverse), this.uniforms.get("cameraFov").value = this._camera.fov, this.uniforms.get("cameraNear").value = this._camera.near, this.uniforms.get("cameraFar").value = this._camera.far;
  }
  /**
      * 设置相机
      * @param {Camera} camera - Three.js相机对象
      */
  setCamera(e) {
    this._camera = e, e && (this.uniforms.get("cameraNear").value = e.near, this.uniforms.get("cameraFar").value = e.far);
  }
  /**
      * 更新鼠标位置 - 现在作为次要控制方式
      *
      * @param {Number} x - 鼠标X坐标
      * @param {Number} y - 鼠标Y坐标
      */
  setMousePosition(e, t) {
    this.uniforms.get("mouse").value.set(e, t);
  }
  /**
      * 雾气密度
      */
  get fogDensity() {
    return this.uniforms.get("fogDensity").value;
  }
  set fogDensity(e) {
    this.uniforms.get("fogDensity").value = e;
  }
  /**
      * 低位置相机优化
      */
  get enableLowCameraOptimization() {
    return this._enableLowCameraOptimization;
  }
  set enableLowCameraOptimization(e) {
    this._enableLowCameraOptimization = e, this.uniforms.get("enableLowCameraOptimization").value = e ? 1 : 0;
  }
  /**
      * 设置主相机
      * @param {Camera} value - 主相机
      * @override
      */
  set mainCamera(e) {
    this.setCamera(e);
  }
}
const Sc = `// 沙尘暴效果（修改版）
// 基于Shadertoy效果移植，提供体积雾、光线散射效果
// 暂时隐藏飞船模型，仅保留沙尘暴部分

// 统一变量声明 - 不需要重复定义已经由库提供的变量
// uniform sampler2D inputBuffer;
// uniform sampler2D depthBuffer;
// uniform vec2 vUv;

// 其他需要的统一变量
uniform float time;       // 替代 iTime
uniform vec2 resolution;  // 替代 iResolution
uniform vec2 mouse;       // 替代 iMouse

// 相机参数 - 从Three.js传入
uniform vec3 cameraPosition;
uniform mat4 viewMatrix;
uniform float cameraFov;
uniform float cameraNear;
uniform float cameraFar;

// 效果参数
uniform float volumeDensity;       // 体积密度
uniform float volumeAbsorbtion;    // 体积吸收率
uniform vec3 lightColor;           // 光源颜色
uniform float shadowQuality;       // 阴影质量
uniform float numSteps;            // 采样步数
uniform float enableDithering;     // 是否启用抖动
uniform float enableVolumetricLighting; // 是否启用体积光照
uniform float fogDensity;          // 雾气浓度
uniform float fogDecay;            // 雾气衰减速度
uniform float fogMinDist;          // 最小雾气距离

// 常量定义 PI无需反复定义
//#define PI 3.141592

// ACES 色调映射
vec3 ACES(vec3 x) { 
    float a = 2.51;
    float b = 0.03;
    float c = 2.43;
    float d = 0.59;
    float e = 0.14;
    return (x*(a*x+b))/(x*(c*x+d)+e);
}

// 2D旋转函数
mat2 rot(float a) {
    float s = sin(a), c = cos(a);
    return mat2(c, -s, s, c);
}

// 3D旋转函数
mat3 rot(vec3 a) {
    float c = cos(a.x), s = sin(a.x);
    mat3 rx = mat3(1,0,0,0,c,-s,0,s,c);
    c = cos(a.y), s = sin(a.y);
    mat3 ry = mat3(c,0,-s,0,1,0,s,0,c);
    c = cos(a.z), s = sin(a.z);
    mat3 rz = mat3(c,-s,0,s,c,0,0,0,1);
    
    return rz * rx * ry;
}

// 浮点随机数
float hash(float n) {
    return fract(sin(n)*43758.5453123);
}

// 噪声函数 (by iq)
float noise(vec3 x) {
    vec3 p = floor(x);
    vec3 f = fract(x);
    f = f*f*(3.0-2.0*f);

    float n = p.x + p.y*157.0 + 113.0*p.z;

    return mix(mix(mix(hash(n+  0.0), hash(n+  1.0),f.x),
                   mix(hash(n+157.0), hash(n+158.0),f.x),f.y),
               mix(mix(hash(n+113.0), hash(n+114.0),f.x),
                   mix(hash(n+270.0), hash(n+271.0),f.x),f.y),f.z);
}

// 分形噪声
float fbm(vec3 p) {
    float f = 0.0;
    f += 0.5*noise(p);
    f += 0.25*noise(2.0*p);
    f += 0.125*noise(4.0*p);
    f += 0.0625*noise(8.0*p);
    return f;
}

// 场景SDF (分形)
float map(vec3 p) {
    mat3 r = rot(vec3(1.57)); // 旋转
   
    p *= 6.0;
    vec3 q = p;
    float m = 1.0;
    
    for (int i=0; i<3; i++) {
        p = clamp(p, -1.0, 1.0) * 2.0 - p;
        float h = clamp(0.25/dot(p, p), 0.25, 1.0);
        p *= h;
        m *= h;
        if(i<2) p *= r;
        p = p*9.0 + q;
        m = m*9.0+1.0;
    }
    q = abs(p);
    return (max(q.x, max(q.y, q.z))-3.0) / (m*6.0);
}

// 光线行进函数
float intersect(vec3 ro, vec3 rd) {
    float t = 0.0; // 行进距离
    for (int i=0; i<128 && t<8.0; i++) {
        vec3 p = ro + rd*t; // 当前点
        
        float h = map(p); // 到场景的距离
        if (h<0.001) break; // 命中表面
        
        t += h; // 前进
    }
    // 返回距离
    return t;
}

// 法线估计
vec3 calcNormal(vec3 p) {
    float h = map(p);
    const vec2 e = vec2(0.0001, 0.0); // 偏移量
    
    return normalize(h - vec3(map(p-e.xyy),
                              map(p-e.yxy),
                              map(p-e.yyx)));
}

// 阴影函数
float shadow(vec3 ro, vec3 rd, float tmax) {
    for (float t=0.0; t<tmax;) {
        vec3 p = ro + rd*t;
        float h = map(p)*shadowQuality;
        if (h<0.001) return 0.0;
        t += h;
    }
    return 1.0;
}

// 光线函数
// 返回光线方向和光线向量的长度
vec4 getLight(vec3 ce, vec3 p) {
    vec3 lig = ce - p; // 光线向量
    float l = length(lig); // 光线向量的长度
    lig = normalize(lig); // 归一化
    return vec4(lig, l);
}

// 体积渲染 - 仅渲染沙尘暴
vec4 renderVolume(vec3 ro, vec3 rd, float tmax) {
    vec4 sum = vec4(0.0, 0.0, 0.0, 0.0); // 颜色和不透明度
    
    float s = tmax / float(int(numSteps)); // 步进大小
    float t = 0.0; // 行进距离
    if (enableDithering > 0.5) {
        // 抖动
        t += s*hash(gl_FragCoord.x*8315.9213/resolution.x+gl_FragCoord.y*2942.5192/resolution.y);
    }
    
    // 计算光源位置 - 随时间变化
    vec3 lightPos = vec3(1.5)*rot(vec3(0.5*time));
    
    for (int i=0; i<128; i++) { // 光线步进循环
        if (i >= int(numSteps)) break; // 限制步进次数
        
        vec3 p = ro + rd*t; // 当前点
        
        // 应用最小雾气距离 - 在cameraNear和fogMinDist之间的区域不会有雾气
        float distFromCamera = distance(p, ro);
        
        if (distFromCamera > fogMinDist) {
            // 应用衰减速度 - 使雾气随距离增加而变浓
            // 使用更加平滑的过渡函数
            float distFactor = 1.0 - exp(-0.1 * (1.0 - fogDecay) * (distFromCamera - fogMinDist));
            
            // 计算雾的密度，考虑距离因素和雾气浓度设置
            float h = volumeDensity * fbm(4.0*p) * distFactor * fogDensity;
            
            // 计算光照
            vec4 lig = getLight(lightPos, p); // 光线方向 + 光线向量的长度
            
            // 简化的阴影计算
            float sha = 1.0;
            if (enableVolumetricLighting > 0.5) {
                // 使用简化的阴影计算，根据距离衰减
                sha = clamp(1.0 - 0.2 * lig.w * shadowQuality, 0.0, 1.0);
            }
                      
            // 着色
            vec3 col = lightColor * sha / (lig.w * lig.w); // 光照衰减平方反比
                
            sum.rgb += h * s * exp(sum.a) * col; // 将颜色添加到最终结果
            sum.a += -h * s * volumeAbsorbtion; // 比尔定律
        }
        
        t += s; // 前进
    }
    
    // 输出
    return sum;
}

// 环境光遮蔽函数
float calcAO(vec3 p, vec3 n) { // 点和法线
    float res = 1.0; // 结果
    for (int i=0; i<5; i++) { // 采样5次
        float h = 0.1*float(i)/5.0;
        res *= clamp(0.5+0.5*map(p + n*h)/h, 0.0, 1.0);
    }    
    return res;
}

// 渲染
vec3 render(vec3 ro, vec3 rd) {
    vec3 col = vec3(0.0); // 背景
    
    float t = intersect(ro, rd); // 距离
    if (t<8.0) { // 命中表面
        vec3 p = ro + rd*t; // 命中点
        vec3 n = calcNormal(p); // 表面法线
        
        // 光源位置        
        vec3 lightPos = vec3(1.5)*rot(vec3(0.5*time)); // 光源位置
        vec4 lig = getLight(lightPos, p); // 光线方向 + 光线向量的长度
                
        float dif = clamp(dot(n, lig.xyz), 0.0, 1.0); // 漫反射光
        float sha = shadow(p+n*0.002, lig.xyz, lig.w*0.5); // 阴影
        float bac = clamp(dot(n, -lig.xyz), 0.0, 1.0); // 背光/反弹光
        float occ = calcAO(p, n); // 环境光遮蔽
        
        // 光与表面交互
        float lin = 0.0;
        lin += dif*sha; // 直射光
        lin += 0.1*occ*(1.0+bac); // 环境光
        lin /= lig.w*lig.w; // 平方反比定律
        
        col = lightColor*lin;
    }
    
    // 雾
    vec4 res = renderVolume(ro, rd, t);
    col = col*exp(res.a) + res.rgb; // 将颜色与雾气颜色混合
    
    // 输出
    return col;
}

// 相机函数
mat3 setCamera(vec3 ro, vec3 ta) {
    vec3 w = normalize(ta - ro);
    vec3 u = normalize(cross(w, vec3(0.0, 1.0, 0.0)));
    vec3 v = cross(u, w);
    return mat3(u, v, w);
}

// 深度转线性深度
float linearizeDepth(float depth) {
    return 2.0 * cameraNear * cameraFar / (cameraFar + cameraNear - (2.0 * depth - 1.0) * (cameraFar - cameraNear));
}

// 主函数 - 适配为postprocessing库的接口
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
    vec2 fragCoord = uv * resolution.xy;
    
    // 获取深度值
    float depth = texture2D(depthBuffer, uv).r;
    
    // 将深度转换为线性深度 (0-1)
    float linearDepth = linearizeDepth(depth) / cameraFar;
    
    // 计算相机方向
    float fovFactor = tan(radians(cameraFov * 0.5));
    vec2 p = (2.0 * uv - 1.0) * vec2(resolution.x / resolution.y * fovFactor, fovFactor);
    
    // 转换为世界空间方向
    vec3 rayDir = normalize(vec3(p, -1.0));
    vec3 rd = normalize((inverse(viewMatrix) * vec4(rayDir, 0.0)).xyz);
    
    // 最大渲染距离
    float maxDist = 30.0;
    
    // 渲染沙尘暴效果
    vec4 volumeResult = renderVolume(cameraPosition, rd, maxDist);
    vec3 sandStormColor = volumeResult.rgb;
    
    // 后处理
    sandStormColor = ACES(sandStormColor); // 色调映射
    sandStormColor = pow(sandStormColor, vec3(0.4545)); // 伽马校正
    sandStormColor = sandStormColor*0.2+0.8*sandStormColor*sandStormColor*(3.0-2.0*sandStormColor); // 对比度
    
    // 边缘暗角
    vec2 q = fragCoord/resolution.xy;
    sandStormColor *= 0.5+0.5*pow(16.0 * q.x*q.y*(1.0-q.x)*(1.0-q.y), 0.1);
    
    // --------- 参考foggy-mountains.frag的混合逻辑 ---------
    
    // 天空掩码 - 标识最远处的天空部分
    float skyMask = step(0.9999, depth);
    
    // 基于线性深度的混合因子 - 控制沙尘暴效果的强度
    // 使用fogDensity、fogDecay和fogMinDist来修改混合计算
    float distFactor = max(0.0, linearDepth - fogMinDist/cameraFar);
    // 使用更加平滑的过渡函数
    float blendFactor = clamp(distFactor * fogDensity * 2.0 * (1.0 - exp(-0.5 * (1.0 - fogDecay) * distFactor * cameraFar)), 0.0, 1.0);
    
    // 两步混合:
    // 1. 先将输入颜色与沙尘暴颜色基于深度混合，得到带沙尘的前景
    vec3 blendedColor = mix(inputColor.rgb, sandStormColor, blendFactor);
    
    // 2. 在天空区域（最远处）使用完整的沙尘暴效果
    vec3 finalColor = mix(blendedColor, sandStormColor, skyMask);
    
    // 最终输出
    outputColor = vec4(finalColor, inputColor.a);
} `;
class vd extends I {
  /**
      * 构建一个新的沙尘暴中的宇宙飞船效果
      *
      * @param {Object} [options] - 效果选项
      * @param {Number} [options.volumeDensity=0.6] - 体积密度
      * @param {Number} [options.volumeAbsorbtion=1.0] - 体积吸收率
      * @param {Color|String|Number} [options.lightColor=0xffba59] - 光源颜色 (默认偏黄色)
      * @param {Number} [options.shadowQuality=1.5] - 阴影质量 (值越高质量越低但性能越好)
      * @param {Number} [options.numSteps=32] - 光线行进采样步数
      * @param {Boolean} [options.enableDithering=true] - 是否启用抖动
      * @param {Boolean} [options.enableVolumetricLighting=true] - 是否启用体积光照
      * @param {Number} [options.fogDensity=1.0] - 雾气浓度
      * @param {Number} [options.fogDecay=0.9] - 雾气衰减速度
      * @param {Number} [options.fogMinDist=0.0] - 雾气最小距离（小于此距离不会有雾气）
      * @param {Object} [options.camera] - 相机对象，用于获取视图矩阵和位置
      */
  constructor({
    volumeDensity: e = 0.6,
    volumeAbsorbtion: t = 1,
    lightColor: r = 16759385,
    shadowQuality: n = 1.5,
    numSteps: i = 32,
    enableDithering: s = !0,
    enableVolumetricLighting: o = !0,
    fogDensity: l = 1,
    fogDecay: u = 0.9,
    fogMinDist: f = 0,
    camera: h = null
  } = {}) {
    const m = new y(r);
    super("SandStormEffect", Sc, {
      attributes: Q.DEPTH,
      defines: /* @__PURE__ */ new Map([
        ["USE_DEPTH", "1"]
      ]),
      uniforms: /* @__PURE__ */ new Map([
        ["resolution", new a(new g())],
        ["time", new a(0)],
        ["mouse", new a(new g(0, 0))],
        ["volumeDensity", new a(e)],
        ["volumeAbsorbtion", new a(t)],
        ["lightColor", new a(new T(m.r, m.g, m.b))],
        ["shadowQuality", new a(n)],
        ["numSteps", new a(i)],
        ["enableDithering", new a(s ? 1 : 0)],
        ["enableVolumetricLighting", new a(o ? 1 : 0)],
        ["fogDensity", new a(l)],
        ["fogDecay", new a(u)],
        ["fogMinDist", new a(f)],
        ["cameraPosition", new a(new T())],
        ["viewMatrix", new a(new C())],
        ["cameraFov", new a(45)],
        ["cameraNear", new a(0.1)],
        ["cameraFar", new a(1e3)]
      ])
    }), this.camera = h;
  }
  /**
      * 更新效果
      *
      * @param {WebGLRenderer} renderer - WebGL渲染器
      * @param {WebGLRenderTarget} inputBuffer - 输入缓冲区
      * @param {Number} [deltaTime] - 自上一帧以来经过的时间（秒）
      */
  update(e, t, r) {
    this.uniforms.get("time").value += r, this.uniforms.get("resolution").value.set(
      t.width,
      t.height
    ), this.camera && (this.uniforms.get("cameraPosition").value.copy(this.camera.position), this.uniforms.get("viewMatrix").value.copy(this.camera.matrixWorld), this.uniforms.get("cameraFov").value = this.camera.fov, this.uniforms.get("cameraNear").value = this.camera.near, this.uniforms.get("cameraFar").value = this.camera.far);
  }
  /**
      * 相机对象
      */
  get camera() {
    return this._camera;
  }
  set camera(e) {
    this._camera = e, e && (this.uniforms.get("cameraPosition").value.copy(e.position), this.uniforms.get("viewMatrix").value.copy(e.matrixWorld), this.uniforms.get("cameraFov").value = e.fov, this.uniforms.get("cameraNear").value = e.near, this.uniforms.get("cameraFar").value = e.far);
  }
  /**
      * 体积密度
      */
  get volumeDensity() {
    return this.uniforms.get("volumeDensity").value;
  }
  set volumeDensity(e) {
    this.uniforms.get("volumeDensity").value = e;
  }
  /**
      * 体积吸收率
      */
  get volumeAbsorbtion() {
    return this.uniforms.get("volumeAbsorbtion").value;
  }
  set volumeAbsorbtion(e) {
    this.uniforms.get("volumeAbsorbtion").value = e;
  }
  /**
      * 光源颜色
      */
  get lightColor() {
    const e = this.uniforms.get("lightColor").value;
    return new y(e.x, e.y, e.z);
  }
  set lightColor(e) {
    const t = new y(e);
    this.uniforms.get("lightColor").value.set(t.r, t.g, t.b);
  }
  /**
      * 阴影质量
      */
  get shadowQuality() {
    return this.uniforms.get("shadowQuality").value;
  }
  set shadowQuality(e) {
    this.uniforms.get("shadowQuality").value = e;
  }
  /**
      * 采样步数
      */
  get numSteps() {
    return this.uniforms.get("numSteps").value;
  }
  set numSteps(e) {
    this.uniforms.get("numSteps").value = e;
  }
  /**
      * 是否启用抖动
      */
  get enableDithering() {
    return this.uniforms.get("enableDithering").value > 0.5;
  }
  set enableDithering(e) {
    this.uniforms.get("enableDithering").value = e ? 1 : 0;
  }
  /**
      * 是否启用体积光照
      */
  get enableVolumetricLighting() {
    return this.uniforms.get("enableVolumetricLighting").value > 0.5;
  }
  set enableVolumetricLighting(e) {
    this.uniforms.get("enableVolumetricLighting").value = e ? 1 : 0;
  }
  /**
      * 雾气浓度
      */
  get fogDensity() {
    return this.uniforms.get("fogDensity").value;
  }
  set fogDensity(e) {
    this.uniforms.get("fogDensity").value = e;
  }
  /**
      * 雾气衰减速度
      */
  get fogDecay() {
    return this.uniforms.get("fogDecay").value;
  }
  set fogDecay(e) {
    this.uniforms.get("fogDecay").value = e;
  }
  /**
      * 雾气最小距离
      */
  get fogMinDist() {
    return this.uniforms.get("fogMinDist").value;
  }
  set fogMinDist(e) {
    this.uniforms.get("fogMinDist").value = e;
  }
}
const yc = `// 迷雾地形效果
// 基于 Shadertoy: Foggy Terrain
// 直接从原始代码移植，保持渲染逻辑完全一致

uniform float time;
uniform vec2 resolution;
uniform float terrainHeight;
uniform float fogDensity;

// 新增参数
uniform float fogMinDistance; // 雾的最近距离
uniform float fogFalloff;     // 雾的衰减速度
uniform vec3 fogColor;        // 雾的颜色
uniform bool hideTerrainMesh; // 是否隐藏地形网格，只显示雾效果

// 相机参数
uniform vec3 cameraPosition;
uniform mat4 viewMatrix;
uniform float cameraNear;
uniform float cameraFar;

// IÃ±igo Quilez的噪声函数
float hash(float n)
{
  return fract(cos(n) * 41415.92653);
}
float noise( in vec3 x )
{
  vec3 p  = floor(x);
  vec3 f  = smoothstep(0.0, 1.0, fract(x));
  float n = p.x + p.y*57.0 + 113.0*p.z;

  return mix(mix(mix( hash(n+  0.0), hash(n+  1.0),f.x),
    mix( hash(n+ 57.0), hash(n+ 58.0),f.x),f.y),
    mix(mix( hash(n+113.0), hash(n+114.0),f.x),
    mix( hash(n+170.0), hash(n+171.0),f.x),f.y),f.z);
}
mat3 m = mat3( 0.00,  1.60,  1.20, 
			  -1.60,  0.72, -0.96, 
			  -1.20, -0.96,  2.28 );
float fbm( vec3 p ) // 实验和修改过的
{
  float f = 0.5000 * noise( p ); p = m * p * 0.72;
  f += 0.2500 * noise( p ); p = m * p * 0.73;
  f += 0.1250 * noise( p ); p = m * p * 0.74;
  f += 0.0625 * noise( p ); p = m * p * 0.75; 
  f += 0.03125 * noise( p ); p = m * p;
  return f;
}

// 场景定义
float objFloor(in vec3 p, in float height) // 地形只是一个带噪声的平面
{
	return p.y + height * fbm(vec3(p.xz, 10));
}
vec2 scene(in vec3 p)
{
	vec2 floorPlane = vec2(objFloor(p, terrainHeight),
						   1.0);
	return floorPlane;
}

// 光线追踪相关
vec3 calcNormal(in vec3 p)
{
	vec3 e = vec3(0.001, 0.0, 0.0);
	
	vec3 n;
	n.x = scene(p + e.xyy).x - scene(p - e.xyy).x;
	n.y = scene(p + e.yxy).x - scene(p - e.yxy).x;
	n.z = scene(p + e.yyx).x - scene(p - e.yyx).x;
	
	return normalize(n);
}

// 增加最大深度和步数，扩大渲染范围
#define MAX_STEPS 128
#define MAX_DEPTH 100.0

vec2 intersect(in vec3 origin, in vec3 direction)
{
	float rayLength = 0.0;
	vec2 hit = vec2(1.);
	for (int i = 0; i < MAX_STEPS; ++i)
	{
		if (hit.x < 0.001 || rayLength > MAX_DEPTH)
			break;
		
		hit = scene(origin + direction * rayLength);
		 
		// 使用略微减少的步长来减少波形伪影
		rayLength += hit.x * 0.6;
	}
		
	return vec2(rayLength, rayLength > MAX_DEPTH ? 
						   0. : hit.y);
}

// 计算基于距离的雾效果
float calculateFog(float distance) {
    // 应用最小距离，小于最小距离的部分没有雾
    float adjustedDistance = max(0.0, distance - fogMinDistance);
    
    // 使用指数衰减计算雾的浓度
    float fogAmount = 1.0 - exp(-adjustedDistance * fogFalloff * fogDensity);
    
    return clamp(fogAmount, 0.0, 1.0);
}

// 渲染迷雾地形
vec4 renderFoggyTerrain(in vec2 uv)
{
	// 设置空间
	vec2 p = (uv * 2.0 - 1.0); p.x *= resolution.x / resolution.y;
	
	// 获取相机位置和目标
	vec3 origin = vec3(3. * cos(time * 0.0),  // 停止自动旋转
					   1,
					   2. * sin(time * 0.0)); // 停止自动旋转
	
	// 如果提供了相机位置，使用相机位置
	if(cameraPosition != vec3(0.0)) {
		origin = cameraPosition;
	}
	
	vec3 target = vec3(0,0,0);
	
	// 摄像机方向 - 如果有视图矩阵，使用它
	vec3 direction;
	vec3 right;
	vec3 up;
	
	if(viewMatrix != mat4(0.0)) {
		// 从视图矩阵提取相机方向
		direction = -normalize(vec3(viewMatrix[0][2], viewMatrix[1][2], viewMatrix[2][2]));
		right = normalize(vec3(viewMatrix[0][0], viewMatrix[1][0], viewMatrix[2][0]));
		up = normalize(vec3(viewMatrix[0][1], viewMatrix[1][1], viewMatrix[2][1]));
	} else {
		// 使用默认相机方向
		direction = normalize(target - origin);
		right = normalize(cross(direction, vec3(0,1,0)));
		up = normalize(cross(right, direction));
	}
	
	// 光线方向
	vec3 rayDirection = normalize(p.x * right + p.y * up + 1.5 * direction);
	
	// 场景着色 - 使用自定义的雾颜色和天空的混合
	vec3 skyColor = mix(vec3(32./255.), // 背景
					vec3(225./255.),
					uv.y);
					
	// 应用雾的颜色
	vec3 color = skyColor;
	
	// x 是交点的距离，y 是材质
	vec2 result = intersect(origin, rayDirection);
	
	// 计算基于距离的雾
	float fogAmount = calculateFog(result.x);
	
	if (result.y > 0.5 && !hideTerrainMesh)
	{
		vec3 position = origin + rayDirection * result.x;
		vec3 normal = calcNormal(position);
		vec3 light = normalize(vec3(0., 3., -1.));
		
		// 使用噪声作为基础颜色
		vec3 terrainColor = vec3(fbm(vec3(position.xz, 10)));
		
		// 添加光照效果
		terrainColor *= max(0.3, dot(normal, light));
		
		// 应用雾气 - 使用自定义颜色
		color = mix(terrainColor, fogColor, fogAmount);
	} else {
	    // 如果没有地形交点或者地形被隐藏，只应用雾效果
	    // 修改：将 MAX_DEPTH * 0.5 改为更合理的值，确保足够的雾浓度
	    float skyFogAmount = calculateFog(MAX_DEPTH * 0.3);
	    
	    // 修改：增强雾气强度并确保使用正确的雾颜色
	    // 当 hideTerrainMesh 为 true 时，确保空气中也应用了正确的雾颜色
	    if (hideTerrainMesh) {
	        // 直接使用更强的雾气浓度和完全的雾颜色
	        color = mix(skyColor, fogColor, skyFogAmount * fogDensity);
	    } else {
	        // 使用原来的混合方式
	        color = mix(skyColor, fogColor, skyFogAmount * fogDensity * 0.5);
	    }
	}
	
	return vec4(color, 1.0);
}

void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor)
{
	// 渲染迷雾地形
	vec4 terrainColor = renderFoggyTerrain(uv);
	
	// 读取深度值
	#ifdef USE_DEPTH
	float depth = texture2D(depthBuffer, uv).r;
	
	// 将深度值转换为线性深度 (0-1)
	float linearDepth = 2.0 * cameraNear * cameraFar / (cameraFar + cameraNear - (2.0 * depth - 1.0) * (cameraFar - cameraNear));
	linearDepth = linearDepth / cameraFar; // 归一化到0-1
	
	// 改进的深度混合
	float skyMask = step(0.9999, depth); // 纯天空部分（最远处）
	
	// 基于最小距离的深度混合因子
	float adjustedLinearDepth = max(0.0, linearDepth - fogMinDistance / cameraFar);
	
	// 基于线性深度的雾气混合因子 - 使用自定义衰减
	float fogFactor = clamp(adjustedLinearDepth * fogDensity * fogFalloff * 5.0, 0.0, 1.0);
	
	// 混合策略改进:
	// 1. 先将输入颜色与自定义雾颜色混合，得到带雾的前景
	vec4 foggedColor;
	if(hideTerrainMesh) {
	    // 如果隐藏地形，直接混合输入颜色和雾颜色 - 确保使用正确的雾颜色
	    foggedColor = mix(inputColor, vec4(fogColor, 1.0), fogFactor);
	} else {
	    // 否则使用之前的混合策略
	    foggedColor = mix(inputColor, terrainColor, fogFactor);
	}
	
	// 2. 在天空区域（最远处）使用完整的效果
	outputColor = mix(foggedColor, terrainColor, skyMask);
	#else
	// 如果没有深度信息，直接输出效果
	outputColor = terrainColor;
	#endif
} `;
class gd extends I {
  /**
      * 构造函数。
      *
      * @param {Object} [options] - 效果选项。
      * @param {BlendFunction} [options.blendFunction=BlendFunction.NORMAL] - 混合函数。
      * @param {Number} [options.terrainHeight=4.0] - 地形高度。
      * @param {Number} [options.fogDensity=2.0] - 雾气浓度。
      * @param {Number} [options.fogMinDistance=0.0] - 雾的最近距离。
      * @param {Number} [options.fogFalloff=0.05] - 雾的衰减速度。
      * @param {Color|String|Number} [options.fogColor=0xffffff] - 雾的颜色。
      * @param {Boolean} [options.hideTerrainMesh=false] - 是否隐藏地形网格。
      * @param {PerspectiveCamera} [options.camera=null] - 相机对象。
      */
  constructor({
    blendFunction: e = E.NORMAL,
    terrainHeight: t = 4,
    fogDensity: r = 2,
    fogMinDistance: n = 0,
    fogFalloff: i = 0.05,
    fogColor: s = 16777215,
    hideTerrainMesh: o = !1,
    camera: l = null
  } = {}) {
    super("FoggyTerrainEffect", yc, {
      blendFunction: e,
      attributes: Q.DEPTH,
      defines: /* @__PURE__ */ new Map([
        ["USE_DEPTH", "1"]
      ]),
      uniforms: /* @__PURE__ */ new Map([
        ["time", new a(0)],
        ["resolution", new a(new g())],
        ["terrainHeight", new a(t)],
        ["fogDensity", new a(r)],
        ["fogMinDistance", new a(n)],
        ["fogFalloff", new a(i)],
        ["fogColor", new a(new y(s))],
        ["hideTerrainMesh", new a(o)],
        ["cameraPosition", new a(null)],
        ["viewMatrix", new a(null)],
        ["cameraNear", new a(0.1)],
        ["cameraFar", new a(2e3)]
      ])
    }), this.camera = l;
  }
  /**
      * 设置相机。
      *
      * @param {PerspectiveCamera} camera - 相机对象。
      */
  setCamera(e) {
    this.camera = e;
  }
  /**
      * 更新方法。
      *
      * @param {WebGLRenderer} renderer - 渲染器。
      * @param {WebGLRenderTarget} inputBuffer - 输入缓冲区。
      * @param {Number} [deltaTime] - 自上一帧以来的时间（秒）。
      */
  update(e, t, r) {
    const n = this.uniforms;
    n.get("time").value += r, n.get("resolution").value.set(
      t.width,
      t.height
    ), this.camera !== null && (n.get("cameraPosition").value = this.camera.position, n.get("viewMatrix").value = this.camera.matrixWorldInverse, n.get("cameraNear").value = this.camera.near, n.get("cameraFar").value = this.camera.far);
  }
  /**
      * 获取地形高度。
      *
      * @return {Number} 地形高度。
      */
  get terrainHeight() {
    return this.uniforms.get("terrainHeight").value;
  }
  /**
      * 设置地形高度。
      *
      * @param {Number} value - 地形高度。
      */
  set terrainHeight(e) {
    this.uniforms.get("terrainHeight").value = e;
  }
  /**
      * 获取雾气浓度。
      *
      * @return {Number} 雾气浓度。
      */
  get fogDensity() {
    return this.uniforms.get("fogDensity").value;
  }
  /**
      * 设置雾气浓度。
      *
      * @param {Number} value - 雾气浓度。
      */
  set fogDensity(e) {
    this.uniforms.get("fogDensity").value = e;
  }
  /**
      * 获取雾的最近距离。
      *
      * @return {Number} 雾的最近距离。
      */
  get fogMinDistance() {
    return this.uniforms.get("fogMinDistance").value;
  }
  /**
      * 设置雾的最近距离。
      *
      * @param {Number} value - 雾的最近距离。
      */
  set fogMinDistance(e) {
    this.uniforms.get("fogMinDistance").value = e;
  }
  /**
      * 获取雾的衰减速度。
      *
      * @return {Number} 雾的衰减速度。
      */
  get fogFalloff() {
    return this.uniforms.get("fogFalloff").value;
  }
  /**
      * 设置雾的衰减速度。
      *
      * @param {Number} value - 雾的衰减速度。
      */
  set fogFalloff(e) {
    this.uniforms.get("fogFalloff").value = e;
  }
  /**
      * 获取雾的颜色。
      *
      * @return {Color} 雾的颜色。
      */
  get fogColor() {
    return this.uniforms.get("fogColor").value;
  }
  /**
      * 设置雾的颜色。
      *
      * @param {Color|String|Number} value - 雾的颜色。
      */
  set fogColor(e) {
    this.uniforms.get("fogColor").value.set(e);
  }
  /**
      * 获取是否隐藏地形网格。
      *
      * @return {Boolean} 是否隐藏地形网格。
      */
  get hideTerrainMesh() {
    return this.uniforms.get("hideTerrainMesh").value;
  }
  /**
      * 设置是否隐藏地形网格。
      *
      * @param {Boolean} value - 是否隐藏地形网格。
      */
  set hideTerrainMesh(e) {
    this.uniforms.get("hideTerrainMesh").value = e;
  }
}
const Mc = `// 这些变量由Three.js框架自动定义，不需要再声明
// uniform sampler2D inputBuffer;
// uniform sampler2D depthBuffer;
uniform sampler2D ditherMap;
uniform sampler2D marchingTex;
uniform sampler2D shadowMap;
// uniform vec2 resolution;
uniform mat4 cameraMatrixWorld;
// uniform vec3 cameraPosition;
// uniform float cameraNear;
// uniform float cameraFar;
uniform vec4 offsets;

uniform int rayMarchingStep;
uniform float maxRayLength;
uniform float volumetricLightIntensity;
uniform float volumetricShadowIntensity;
uniform float lightScatteringFactor;
uniform float shadowAttenuation;
uniform float minShadow;
uniform float samplerScale;
uniform vec3 lightPosition; // 光源位置

// varying vec2 vUv;
varying vec4 vRay;

// 重新映射值
float remap(float x, float from1, float to1, float from2, float to2) {
    return (x - from1) / (to1 - from1) * (to2 - from2) + from2;
}

// 计算线性深度值
float getLinearDepth(float depth) {
    float z = depth * 2.0 - 1.0;
    return 2.0 * cameraNear * cameraFar / (cameraFar + cameraNear - z * (cameraFar - cameraNear));
}

// 从深度重建世界坐标
vec3 getWorldPos(vec2 uv, float depth) {
    float linearDepth = getLinearDepth(depth);
    vec3 worldDir = normalize(vRay.xyz);
    return cameraPosition + worldDir * linearDepth;
}

// 判断点是否在阴影中 - 简化版，实际实现需要更精确的阴影计算
float getShadow(vec3 worldPos) {
    // 简化版阴影计算，仅用于演示
    // 在实际实现中，应替换为更准确的阴影计算方法
    float shadowValue = 0.8; // 默认值
    
    // 计算光源方向
    vec3 dirToLight = normalize(lightPosition - worldPos);
    
    // 简单的方向性阴影（这只是演示用）
    float dotNL = dot(dirToLight, normalize(vRay.xyz)) * 0.5 + 0.5;
    shadowValue = mix(0.5, 1.0, dotNL);
    
    return shadowValue;
}

// 主函数，用于射线行进计算阴影
vec4 calcVolumetricShadow() {
    // 获取深度
    float depth = texture2D(depthBuffer, vUv).r;
    float linearDepth = getLinearDepth(depth);
    
    // 限制最大深度
    linearDepth = min(linearDepth, maxRayLength);
    
    // 计算世界坐标
    vec3 worldPos = cameraPosition + vRay.xyz * linearDepth;
    
    float vShadow = 1.0; // 初始阴影值
    float vLight = 0.0;  // 初始光照值
    
    // 射线起点
    vec3 rayOri = cameraPosition;
    vec3 rayDir = normalize(vRay.xyz);
    
    // 计算相机到世界坐标点的距离
    float disCam2World = length(worldPos - cameraPosition);
    
    // 使用抖动图扰动采样点，减少走样
    vec2 offsetUV = mod(floor(gl_FragCoord.xy), 4.0);
    float ditherValue = texture2D(ditherMap, offsetUV * 0.25).a;
    rayOri += ditherValue * rayDir;
    
    // 计算光线散射强度
    vec3 toLight = normalize(lightPosition - cameraPosition);
    float dotLightRayDir = dot(toLight, rayDir) * 0.5 + 0.5;
    float scatteringLight = smoothstep(0.5, 1.0, dotLightRayDir);
    
    // 计算射线行进步长
    float marchStep = disCam2World / float(rayMarchingStep);
    
    // 射线行进循环
    for (int j = 0; j < 128; j++) { // 使用常数最大值，但在内部限制实际步数
        if (j >= rayMarchingStep) break;
        
        // 计算当前采样点
        vec3 currentPos = rayOri + rayDir * marchStep * float(j);
        
        // 计算当前点到相机的距离
        float disCam2Current = length(currentPos - cameraPosition);
        
        // 检查是否超出深度范围
        float outOfRange = step(disCam2Current, disCam2World);
        
        // 计算阴影值
        float shadow = getShadow(currentPos);
        
        // 累积阴影和光照值
        float stepWeight = float(j + 3) / float(rayMarchingStep);
        vShadow -= (1.0 - shadow) * volumetricShadowIntensity / float(rayMarchingStep) * stepWeight * outOfRange;
        vLight += shadow * volumetricLightIntensity * scatteringLight / float(rayMarchingStep) * stepWeight * outOfRange;
    }
    
    // 限制阴影值范围
    vShadow = clamp(vShadow, minShadow, 1.0);
    
    // 应用散射因子到光照值
    vLight = pow(clamp(vLight, 0.0, 1.0), lightScatteringFactor);
    
    return vec4(vLight, vShadow, 0.0, 1.0);
}

// 模糊函数
vec4 blur(sampler2D tex) {
    vec4 color = vec4(0.0);
    vec2 texelSize = 1.0 / resolution;
    
    // 计算模糊采样权重
    color += 0.40 * texture2D(tex, vUv);
    color += 0.15 * texture2D(tex, vUv + offsets.xy * texelSize);
    color += 0.15 * texture2D(tex, vUv - offsets.xy * texelSize);
    color += 0.10 * texture2D(tex, vUv + 2.0 * offsets.xy * texelSize);
    color += 0.10 * texture2D(tex, vUv - 2.0 * offsets.xy * texelSize);
    color += 0.05 * texture2D(tex, vUv + 3.0 * offsets.xy * texelSize);
    color += 0.05 * texture2D(tex, vUv - 3.0 * offsets.xy * texelSize);
    
    return color;
}

// 合成函数
vec4 combine(vec4 originalColor, vec4 marchingColor) {
    vec4 finalColor = vec4(1.0);
    
    // 从marchingTex中获取阴影和光照信息
    float lightValue = marchingColor.r;
    float shadowValue = marchingColor.g;
    
    // 计算最终颜色：原始颜色 + 光照效果，并应用阴影
    finalColor.rgb = clamp(originalColor.rgb + lightValue * vec3(1.0), 0.0, 1.0) * shadowValue;
    finalColor.a = originalColor.a;
    
    return finalColor;
}

// 确定当前处理阶段
// 1. 射线行进阶段: offsets.xy 长度几乎为0
// 2. 模糊阶段: offsets.xy 长度大于微小值
// 3. 合成阶段: offsets.xy 有特定的微小值
int determineStage() {
    float offsetLength = length(offsets.xy);
    
    if (offsetLength < 0.00001) {
        return 1; // 射线行进阶段
    } else if (offsetLength > 0.001) {
        return 2; // 模糊阶段
    } else {
        return 3; // 合成阶段
    }
}

void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
    int stage = determineStage();
    
    if (stage == 1) {
        // 阶段1: 射线行进阶段 - 计算初始体积阴影
        outputColor = calcVolumetricShadow();
    }
    else if (stage == 2) {
        // 阶段2: 模糊阶段 - 对阴影贴图应用模糊
        outputColor = blur(inputBuffer);
    }
    else {
        // 阶段3: 合成阶段 - 将阴影和原始图像合成
        vec4 marchingColor = texture2D(marchingTex, uv);
        outputColor = combine(inputColor, marchingColor);
    }
} `, wc = `// 这些变量已经由Three.js自动定义，不需要再声明
// uniform mat4 projectionMatrix;
// uniform mat4 modelViewMatrix;
// uniform vec3 cameraPosition;

// attribute vec3 position;
// attribute vec2 uv;

// varying vec2 vUv;
varying vec4 vRay;

uniform vec3 frustumCorners[4];

// 不再定义main函数，改为define跟踪函数
// 创建一个计算函数，让框架来调用
void setupRay() {
    // 根据顶点位置索引选择对应的视锥体角向量
    int index = int(position.z + 0.5); // 使用z坐标作为索引
    
    // 确保索引在有效范围内
    index = clamp(index, 0, 3);
    
    // 设置光线方向
    vRay = vec4(frustumCorners[index], 1.0);
}

// 在vertexShader注入点插入setupRay函数调用
#define FRAGMENT_HEAD_INJECT setupRay(); `;
class xd extends I {
  /**
      * 构造函数
      * @param {Object} [options] - 可选配置项
      * @param {Number} [options.blendFunction] - 混合模式
      * @param {Number} [options.downSample=2] - 降采样级别，提高性能
      * @param {Number} [options.rayMarchingStep=16] - 射线行进采样步数
      * @param {Number} [options.maxRayLength=15.0] - 最大光线长度
      * @param {Number} [options.samplerScale=1.0] - 采样比例
      * @param {Number} [options.volumetricLightIntensity=0.05] - 体积光强度
      * @param {Number} [options.lightScatteringFactor=0.5] - 光散射因子
      * @param {Number} [options.volumetricShadowIntensity=0.0] - 体积阴影强度
      * @param {Number} [options.shadowAttenuation=0.08] - 阴影衰减
      * @param {Number} [options.minShadow=0.5] - 最小阴影值
      */
  constructor({
    blendFunction: e,
    downSample: t = 2,
    rayMarchingStep: r = 16,
    maxRayLength: n = 15,
    samplerScale: i = 1,
    volumetricLightIntensity: s = 0.05,
    lightScatteringFactor: o = 0.5,
    volumetricShadowIntensity: l = 0,
    shadowAttenuation: u = 0.08,
    minShadow: f = 0.5
  } = {}) {
    super("VolumetricShadowEffect", Mc, {
      vertexShader: wc,
      // 注意：这会合并到框架的顶点着色器中，而不是完全替换
      blendFunction: e,
      attributes: Q.DEPTH | Q.CONVOLUTION,
      defines: /* @__PURE__ */ new Map([
        ["USE_FRUSTUM_CORNERS", "1"]
      ]),
      uniforms: /* @__PURE__ */ new Map([
        ["rayMarchingStep", new a(r)],
        ["maxRayLength", new a(n)],
        ["samplerScale", new a(i)],
        ["volumetricLightIntensity", new a(s)],
        ["lightScatteringFactor", new a(o)],
        ["volumetricShadowIntensity", new a(l)],
        ["shadowAttenuation", new a(u)],
        ["minShadow", new a(f)],
        ["ditherMap", new a(null)],
        ["offsets", new a(new ze(0, 0, 0, 0))],
        ["frustumCorners", new a(null)],
        ["marchingTex", new a(null)],
        ["lightPosition", new a(new T(5, 10, 5))]
      ])
    }), this.downSample = t, this.samplerScale = i, this.createDitherMap(), this.frustumCorners = null, this.uniforms.get("frustumCorners").value = this.frustumCorners, this.marchingRT = null, this.tempRT = null;
  }
  /**
      * 创建抖动纹理图
      * 用于随机化采样点，减少走样
      */
  createDitherMap() {
    const t = new Float32Array(64), r = [
      0,
      8,
      2,
      10,
      12,
      4,
      14,
      6,
      3,
      11,
      1,
      9,
      15,
      7,
      13,
      5
    ];
    for (let i = 0; i < r.length; i++) {
      const s = r[i] / 16;
      t[i * 4] = s, t[i * 4 + 1] = s, t[i * 4 + 2] = s, t[i * 4 + 3] = s;
    }
    const n = new le(t, 4, 4, ne, Y);
    return n.minFilter = b, n.magFilter = b, n.needsUpdate = !0, this.uniforms.get("ditherMap").value = n, n;
  }
  /**
      * 更新视锥体角向量
      * @param {Camera} camera - 相机
      */
  updateFrustumCorners(e) {
    const t = e.near, r = e.aspect, n = t * Math.tan(e.fov * 0.5 * Math.PI / 180), i = n * r, s = new T();
    e.getWorldDirection(s);
    const o = s.clone().cross(e.up).normalize(), l = o.clone().cross(s).normalize(), u = s.clone().multiplyScalar(t).add(l.clone().multiplyScalar(n)).sub(o.clone().multiplyScalar(i)), f = s.clone().multiplyScalar(t).add(l.clone().multiplyScalar(n)).add(o.clone().multiplyScalar(i)), h = s.clone().multiplyScalar(t).sub(l.clone().multiplyScalar(n)).sub(o.clone().multiplyScalar(i)), m = s.clone().multiplyScalar(t).sub(l.clone().multiplyScalar(n)).add(o.clone().multiplyScalar(i)), p = u.length() / t;
    u.normalize().multiplyScalar(p), f.normalize().multiplyScalar(p), h.normalize().multiplyScalar(p), m.normalize().multiplyScalar(p), this.frustumCorners ? (this.frustumCorners[0].copy(h), this.frustumCorners[1].copy(m), this.frustumCorners[2].copy(u), this.frustumCorners[3].copy(f)) : this.frustumCorners = [
      h,
      m,
      u,
      f
    ], this.uniforms.get("frustumCorners").value = this.frustumCorners;
  }
  /**
      * 设置射线行进采样步数
      * @param {Number} value - 采样步数
      */
  set rayMarchingStep(e) {
    this.uniforms.get("rayMarchingStep").value = e;
  }
  get rayMarchingStep() {
    return this.uniforms.get("rayMarchingStep").value;
  }
  /**
      * 设置最大光线长度
      * @param {Number} value - 最大光线长度
      */
  set maxRayLength(e) {
    this.uniforms.get("maxRayLength").value = e;
  }
  get maxRayLength() {
    return this.uniforms.get("maxRayLength").value;
  }
  /**
      * 设置体积光强度
      * @param {Number} value - 体积光强度
      */
  set volumetricLightIntensity(e) {
    this.uniforms.get("volumetricLightIntensity").value = e;
  }
  get volumetricLightIntensity() {
    return this.uniforms.get("volumetricLightIntensity").value;
  }
  /**
      * 设置光散射因子
      * @param {Number} value - 光散射因子
      */
  set lightScatteringFactor(e) {
    this.uniforms.get("lightScatteringFactor").value = e;
  }
  get lightScatteringFactor() {
    return this.uniforms.get("lightScatteringFactor").value;
  }
  /**
      * 设置体积阴影强度
      * @param {Number} value - 体积阴影强度
      */
  set volumetricShadowIntensity(e) {
    this.uniforms.get("volumetricShadowIntensity").value = e;
  }
  get volumetricShadowIntensity() {
    return this.uniforms.get("volumetricShadowIntensity").value;
  }
  /**
      * 设置阴影衰减
      * @param {Number} value - 阴影衰减
      */
  set shadowAttenuation(e) {
    this.uniforms.get("shadowAttenuation").value = e;
  }
  get shadowAttenuation() {
    return this.uniforms.get("shadowAttenuation").value;
  }
  /**
      * 设置最小阴影值
      * @param {Number} value - 最小阴影值
      */
  set minShadow(e) {
    this.uniforms.get("minShadow").value = e;
  }
  get minShadow() {
    return this.uniforms.get("minShadow").value;
  }
  /**
      * 设置采样比例
      * @param {Number} value - 采样比例
      */
  set samplerScale(e) {
    this.uniforms.get("samplerScale").value = e;
  }
  get samplerScale() {
    return this.uniforms.get("samplerScale").value;
  }
  /**
      * 初始化渲染目标
      * @param {WebGLRenderer} renderer - 渲染器
      */
  initialize(e, t, r) {
    const n = t >> this.downSample, i = r >> this.downSample;
    this.marchingRT = new A(n, i), this.tempRT = new A(n, i), this.uniforms.get("marchingTex").value = this.marchingRT.texture;
  }
  /**
      * 更新效果
      * @param {WebGLRenderer} renderer - 渲染器
      * @param {WebGLRenderTarget} inputBuffer - 输入缓冲区
      * @param {Number} deltaTime - 时间增量
      */
  update(e, t, r, n, i, s) {
    if (!this.camera || !this.scene) {
      console.warn("VolumetricShadowEffect: 必须设置相机和场景!");
      return;
    }
    this.updateFrustumCorners(this.camera), (!this.marchingRT || !this.tempRT) && this.initialize(e, t.width, t.height), this._performRenderPasses(e, t, i);
  }
  /**
      * 执行全部渲染通道
      * @param {WebGLRenderer} renderer - 渲染器
      * @param {WebGLRenderTarget} inputBuffer - 输入缓冲
      * @param {WebGLRenderTarget} outputBuffer - 输出缓冲
      */
  _performRenderPasses(e, t, r) {
    this._updateLightPosition(), this._renderRayMarching(e), this._renderBlur(e), this._renderFinalComposition(e, t, r);
  }
  /**
      * 更新光源位置
      */
  _updateLightPosition() {
    const e = this.scene.children.filter(
      (t) => t.type === "DirectionalLight"
    );
    e.length > 0 && this.uniforms.get("lightPosition").value.copy(e[0].position);
  }
  /**
      * 渲染射线行进阶段
      * @param {WebGLRenderer} renderer - 渲染器
      */
  _renderRayMarching(e) {
    this.uniforms.get("offsets").value.set(0, 0, 0, 0);
    const t = e.getRenderTarget();
    e.setRenderTarget(this.marchingRT), e.clear(), this.uniforms.get("marchingTex").value, this.uniforms.get("marchingTex").value = null, e.render(this.scene, this.camera), this.uniforms.get("marchingTex").value = this.marchingRT.texture, e.setRenderTarget(t);
  }
  /**
      * 渲染模糊阶段
      * @param {WebGLRenderer} renderer - 渲染器
      */
  _renderBlur(e) {
    const t = this.uniforms.get("samplerScale").value, r = e.getRenderTarget();
    this.uniforms.get("offsets").value.set(t, 0, 0, 0), e.setRenderTarget(this.tempRT), e.clear(), e.render(this.scene, this.camera), this.uniforms.get("offsets").value.set(0, t, 0, 0), e.setRenderTarget(this.marchingRT), e.clear(), e.render(this.scene, this.camera), this.uniforms.get("offsets").value.set(t, 0, 0, 0), e.setRenderTarget(this.tempRT), e.clear(), e.render(this.scene, this.camera), this.uniforms.get("offsets").value.set(0, t, 0, 0), e.setRenderTarget(this.marchingRT), e.clear(), e.render(this.scene, this.camera), e.setRenderTarget(r);
  }
  /**
      * 渲染最终合成阶段
      * @param {WebGLRenderer} renderer - 渲染器
      * @param {WebGLRenderTarget} inputBuffer - 输入缓冲
      * @param {WebGLRenderTarget} outputBuffer - 输出缓冲
      */
  _renderFinalComposition(e, t, r) {
    this.uniforms.get("marchingTex").value = this.marchingRT.texture, this.uniforms.get("offsets").value.set(1e-4, 1e-4, 0, 0), e.setRenderTarget(r), e.render(this.scene, this.camera);
  }
  // 添加访问器方法，允许动态更改参数
  setVolumetricLightIntensity(e) {
    this.uniforms.get("volumetricLightIntensity").value = e;
  }
  setVolumetricShadowIntensity(e) {
    this.uniforms.get("volumetricShadowIntensity").value = e;
  }
  setLightScatteringFactor(e) {
    this.uniforms.get("lightScatteringFactor").value = e;
  }
  setShadowAttenuation(e) {
    this.uniforms.get("shadowAttenuation").value = e;
  }
  setMinShadow(e) {
    this.uniforms.get("minShadow").value = e;
  }
  setRayMarchingStep(e) {
    this.uniforms.get("rayMarchingStep").value = parseInt(e);
  }
  setMaxRayLength(e) {
    this.uniforms.get("maxRayLength").value = e;
  }
  setSamplerScale(e) {
    this.samplerScale = e, this.uniforms.get("samplerScale").value = e;
  }
  /**
      * 设置相机
      * @param {Camera} camera - 相机
      */
  setCamera(e) {
    this.camera = e;
  }
  /**
      * 设置场景
      * @param {Scene} scene - 场景
      */
  setScene(e) {
    this.scene = e;
  }
}
const Tc = `// 均匀雾效果
// 提供一种距离无关的均匀雾效果，雾的浓度在视距范围内保持相对均匀

uniform float time;           // 时间
uniform vec2 resolution;      // 分辨率

// 相机参数
uniform vec3 cameraPosition;
uniform mat4 viewMatrix;      // 相机世界矩阵
uniform float cameraFov;

// 效果参数
uniform float fogDensity;     // 雾气密度
uniform vec3 fogColor;        // 雾气颜色
uniform float fogMinDist;     // 雾气最小距离
uniform float quality;        // 质量设置 (0-1)
uniform float occludeSky;     // 雾气遮挡天空的程度 (0-1)
uniform float segments;       // 雾的分段数
uniform int fogSteps;         // 采样步数
uniform float fogScale;       // 雾团大小
uniform float fogStrength;    // 雾团强度
uniform float fogEdgeMin;     // 雾团边缘平滑起点
uniform float fogEdgeMax;     // 雾团边缘平滑终点
uniform float fogAtten;      // 雾的距离衰减系数
uniform float fogFadeWidth;   // 雾的近距离淡入宽度

// 重建世界坐标的函数
vec3 reconstructWorldPos(vec2 uv, float depth) {
    // 计算NDC空间的坐标 (-1 到 1 的范围)
    float z = depth * 2.0 - 1.0;
    vec2 screenPos = uv * 2.0 - 1.0;
    
    // 在相机空间中的射线方向
    float fovFactor = tan(radians(cameraFov * 0.5));
    vec3 viewDir = normalize(vec3(
        screenPos.x * resolution.x / resolution.y * fovFactor, 
        screenPos.y * fovFactor, 
        -1.0
    ));
    
    // 线性深度计算
    float linearDepth = 2.0 * cameraNear * cameraFar / 
        (cameraFar + cameraNear - z * (cameraFar - cameraNear));
    
    // 计算相机空间的位置
    vec3 cameraSpacePos = viewDir * linearDepth;
    
    // 转换为世界空间
    return cameraPosition + (viewMatrix * vec4(cameraSpacePos, 0.0)).xyz;
}

// 3D hash/噪声
float hash(vec3 p) {
    p = fract(p * 0.3183099 + vec3(0.71, 0.113, 0.419));
    p *= 17.0;
    return fract(p.x * p.y * p.z * (p.x + p.y + p.z));
}

float noise(vec3 p) {
    vec3 i = floor(p);
    vec3 f = fract(p);
    f = f * f * (3.0 - 2.0 * f);
    float n000 = hash(i + vec3(0, 0, 0));
    float n001 = hash(i + vec3(0, 0, 1));
    float n010 = hash(i + vec3(0, 1, 0));
    float n011 = hash(i + vec3(0, 1, 1));
    float n100 = hash(i + vec3(1, 0, 0));
    float n101 = hash(i + vec3(1, 0, 1));
    float n110 = hash(i + vec3(1, 1, 0));
    float n111 = hash(i + vec3(1, 1, 1));
    return mix(
        mix(mix(n000, n100, f.x), mix(n010, n110, f.x), f.y),
        mix(mix(n001, n101, f.x), mix(n011, n111, f.x), f.y),
        f.z);
}

// 体积雾团采样（用真实空间距离）
float volumeFogFactor(vec3 camPos, vec3 rayDir, float maxDist, float fogDensity, float minDist) {
    int steps = fogSteps;
    float tMin = minDist;
    float tMax = maxDist;
    float dt = (tMax - tMin) / float(steps);
    float accum = 0.0;
    for(int i = 0; i < 128; i++) {
        if(i >= steps) break;
        float t = tMin + dt * (float(i) + 0.5);
        vec3 p = camPos + rayDir * t;
        float d = noise(p * fogScale + time * 0.02);
        d = smoothstep(fogEdgeMin, fogEdgeMax, d);
        float attenuation = exp(-t * fogAtten);
        accum += d * dt * attenuation;
    }
    accum = clamp(accum * fogDensity * fogStrength, 0.0, 1.0);
    return accum;
}

// PostProcessing框架的主函数
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
    if (USE_DEPTH == 1) {
        float depth = texture2D(depthBuffer, uv).r;
        float linearDepth;
        if (depth >= 0.9999) {
            linearDepth = 1.0;
        } else {
            float z = depth * 2.0 - 1.0;
            linearDepth = 2.0 * cameraNear * cameraFar / 
                (cameraFar + cameraNear - z * (cameraFar - cameraNear));
        }
        vec3 worldPos = reconstructWorldPos(uv, depth);
        vec3 camPos = cameraPosition;
        vec3 rayDir = normalize(worldPos - camPos);
        float maxDist = length(worldPos - camPos);
        float nearMask = step(fogMinDist, maxDist);
        float nearFade = smoothstep(fogMinDist, fogMinDist + fogFadeWidth, maxDist);
        float fogFactor = volumeFogFactor(camPos, rayDir, maxDist, fogDensity, fogMinDist) * nearFade * nearMask;
        vec3 foggedScene = mix(inputColor.rgb, fogColor, fogFactor);
        outputColor = vec4(foggedScene, inputColor.a);
    } else {
        outputColor = inputColor;
    }
} `;
class Sd extends I {
  /**
      * 构建一个新的均匀雾效果
      *
      * @param {Object} [options] - 效果选项
      * @param {Number} [options.fogDensity=0.5] - 雾气密度
      * @param {Color|String|Number} [options.fogColor=0xaaaaaa] - 雾气颜色
      * @param {Number} [options.fogMinDist=0.0] - 雾气最小距离
      * @param {Number} [options.quality=0.5] - 质量设置 (0-1)
      * @param {Number} [options.occludeSky=0.0] - 雾气遮挡天空的程度 (0-1)
      * @param {Number} [options.segments=3] - 雾的分段数，用于分段均匀雾
      * @param {Object} [options.camera] - 相机对象，用于获取视图矩阵和位置
      */
  constructor({
    fogDensity: e = 0.015,
    fogColor: t = 11184810,
    fogMinDist: r = 100,
    quality: n = 0.5,
    occludeSky: i = 0,
    segments: s = 3,
    camera: o = null
  } = {}) {
    const l = new y(t);
    super("UniformFogEffect", Tc, {
      attributes: Q.DEPTH,
      defines: /* @__PURE__ */ new Map([
        ["USE_DEPTH", "1"]
      ]),
      uniforms: /* @__PURE__ */ new Map([
        ["resolution", new a(new g())],
        ["time", new a(0)],
        ["fogDensity", new a(e)],
        ["fogColor", new a(new T(l.r, l.g, l.b))],
        ["fogMinDist", new a(r)],
        ["quality", new a(n)],
        ["occludeSky", new a(i)],
        ["segments", new a(s)],
        ["cameraPosition", new a(new T())],
        ["viewMatrix", new a(new C())],
        ["cameraFov", new a(45)],
        ["fogSteps", new a(64)],
        ["fogScale", new a(0.03)],
        ["fogStrength", new a(3)],
        ["fogEdgeMin", new a(0.45)],
        ["fogEdgeMax", new a(0.7)],
        ["fogAtten", new a(9e-3)],
        ["fogFadeWidth", new a(2)]
      ])
    }), this.camera = o;
  }
  /**
      * 更新效果
      *
      * @param {WebGLRenderer} renderer - WebGL渲染器
      * @param {WebGLRenderTarget} inputBuffer - 输入缓冲区
      * @param {Number} [deltaTime] - 自上一帧以来经过的时间（秒）
      */
  update(e, t, r) {
    this.uniforms.get("time").value += r, this.uniforms.get("resolution").value.set(
      t.width,
      t.height
    ), this.camera && (this.uniforms.get("cameraPosition").value.copy(this.camera.position), this.uniforms.get("viewMatrix").value.copy(this.camera.matrixWorld), this.uniforms.get("cameraFov").value = this.camera.fov);
  }
  /**
      * 相机对象
      */
  get camera() {
    return this._camera;
  }
  set camera(e) {
    this._camera = e, e && (this.uniforms.get("cameraPosition").value.copy(e.position), this.uniforms.get("viewMatrix").value.copy(e.matrixWorld), this.uniforms.get("cameraFov").value = e.fov);
  }
  /**
      * 雾气密度
      */
  get fogDensity() {
    return this.uniforms.get("fogDensity").value;
  }
  set fogDensity(e) {
    this.uniforms.get("fogDensity").value = e;
  }
  /**
      * 雾气颜色
      */
  get fogColor() {
    const e = this.uniforms.get("fogColor").value;
    return new y(e.x, e.y, e.z);
  }
  set fogColor(e) {
    const t = new y(e);
    this.uniforms.get("fogColor").value.set(t.r, t.g, t.b);
  }
  /**
      * 雾气最小距离
      */
  get fogMinDist() {
    return this.uniforms.get("fogMinDist").value;
  }
  set fogMinDist(e) {
    this.uniforms.get("fogMinDist").value = e;
  }
  /**
      * 质量设置
      */
  get quality() {
    return this.uniforms.get("quality").value;
  }
  set quality(e) {
    this.uniforms.get("quality").value = e;
  }
  /**
      * 雾气遮挡天空的程度
      */
  get occludeSky() {
    return this.uniforms.get("occludeSky").value;
  }
  set occludeSky(e) {
    this.uniforms.get("occludeSky").value = e;
  }
  /**
      * 雾的分段数
      */
  get segments() {
    return this.uniforms.get("segments").value;
  }
  set segments(e) {
    this.uniforms.get("segments").value = e;
  }
  /**
      * 体积雾团采样步数
      */
  get fogSteps() {
    return this.uniforms.get("fogSteps").value;
  }
  set fogSteps(e) {
    this.uniforms.get("fogSteps").value = e;
  }
  /**
      * 雾团大小
      */
  get fogScale() {
    return this.uniforms.get("fogScale").value;
  }
  set fogScale(e) {
    this.uniforms.get("fogScale").value = e;
  }
  /**
      * 雾团强度
      */
  get fogStrength() {
    return this.uniforms.get("fogStrength").value;
  }
  set fogStrength(e) {
    this.uniforms.get("fogStrength").value = e;
  }
  /**
      * 雾团平滑起点
      */
  get fogEdgeMin() {
    return this.uniforms.get("fogEdgeMin").value;
  }
  set fogEdgeMin(e) {
    this.uniforms.get("fogEdgeMin").value = e;
  }
  /**
      * 雾团平滑终点
      */
  get fogEdgeMax() {
    return this.uniforms.get("fogEdgeMax").value;
  }
  set fogEdgeMax(e) {
    this.uniforms.get("fogEdgeMax").value = e;
  }
  /**
      * 雾的距离衰减系数
      */
  get fogAtten() {
    return this.uniforms.get("fogAtten").value;
  }
  set fogAtten(e) {
    this.uniforms.get("fogAtten").value = e;
  }
  /**
      * 雾的近距离淡入宽度
      */
  get fogFadeWidth() {
    return this.uniforms.get("fogFadeWidth").value;
  }
  set fogFadeWidth(e) {
    this.uniforms.get("fogFadeWidth").value = e;
  }
}
const bc = `#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D inputBuffer;\r
\r
#else\r
\r
	uniform lowp sampler2D inputBuffer;\r
\r
#endif\r
\r
#ifdef BILATERAL\r
\r
	#include <packing>\r
\r
	uniform vec2 cameraNearFar;\r
\r
	#ifdef NORMAL_DEPTH\r
\r
		#ifdef GL_FRAGMENT_PRECISION_HIGH\r
\r
			uniform highp sampler2D normalDepthBuffer;\r
\r
		#else\r
\r
			uniform mediump sampler2D normalDepthBuffer;\r
\r
		#endif\r
\r
		float readDepth(const in vec2 uv) {\r
\r
			return texture2D(normalDepthBuffer, uv).a;\r
\r
		}\r
\r
	#else\r
\r
		#if DEPTH_PACKING == 3201\r
\r
			uniform lowp sampler2D depthBuffer;\r
\r
		#elif defined(GL_FRAGMENT_PRECISION_HIGH)\r
\r
			uniform highp sampler2D depthBuffer;\r
\r
		#else\r
\r
			uniform mediump sampler2D depthBuffer;\r
\r
		#endif\r
\r
		float readDepth(const in vec2 uv) {\r
\r
			#if DEPTH_PACKING == 3201\r
\r
				return unpackRGBAToDepth(texture2D(depthBuffer, uv));\r
\r
			#else\r
\r
				return texture2D(depthBuffer, uv).r;\r
\r
			#endif\r
\r
		}\r
\r
	#endif\r
\r
	float getViewZ(const in float depth) {\r
\r
		#ifdef PERSPECTIVE_CAMERA\r
\r
			return perspectiveDepthToViewZ(depth, cameraNearFar.x, cameraNearFar.y);\r
\r
		#else\r
\r
			return orthographicDepthToViewZ(depth, cameraNearFar.x, cameraNearFar.y);\r
\r
		#endif\r
\r
	}\r
\r
	#ifdef PERSPECTIVE_CAMERA\r
\r
		#define linearDepth(v) viewZToOrthographicDepth(getViewZ(readDepth(v)), cameraNearFar.x, cameraNearFar.y)\r
\r
	#else\r
\r
		#define linearDepth(v) readDepth(v)\r
\r
	#endif\r
\r
#endif\r
\r
#define getTexel(v) texture2D(inputBuffer, v)\r
\r
#if KERNEL_SIZE == 3\r
\r
	// Optimized 3x3\r
	varying vec2 vUv00, vUv01, vUv02;\r
	varying vec2 vUv03, vUv04, vUv05;\r
	varying vec2 vUv06, vUv07, vUv08;\r
\r
#elif KERNEL_SIZE == 5 && MAX_VARYING_VECTORS >= 13\r
\r
	// Optimized 5x5\r
	varying vec2 vUv00, vUv01, vUv02, vUv03, vUv04;\r
	varying vec2 vUv05, vUv06, vUv07, vUv08, vUv09;\r
	varying vec2 vUv10, vUv11, vUv12, vUv13, vUv14;\r
	varying vec2 vUv15, vUv16, vUv17, vUv18, vUv19;\r
	varying vec2 vUv20, vUv21, vUv22, vUv23, vUv24;\r
\r
#else\r
\r
	// General case\r
	uniform vec2 texelSize;\r
	uniform float scale;\r
	varying vec2 vUv;\r
\r
#endif\r
\r
void main() {\r
\r
	#if KERNEL_SIZE == 3\r
\r
		// Optimized 3x3\r
		vec4 c[] = vec4[KERNEL_SIZE_SQ](\r
			getTexel(vUv00), getTexel(vUv01), getTexel(vUv02),\r
			getTexel(vUv03), getTexel(vUv04), getTexel(vUv05),\r
			getTexel(vUv06), getTexel(vUv07), getTexel(vUv08)\r
		);\r
\r
		#ifdef BILATERAL\r
\r
			float z[] = float[KERNEL_SIZE_SQ](\r
				linearDepth(vUv00), linearDepth(vUv01), linearDepth(vUv02),\r
				linearDepth(vUv03), linearDepth(vUv04), linearDepth(vUv05),\r
				linearDepth(vUv06), linearDepth(vUv07), linearDepth(vUv08)\r
			);\r
\r
		#endif\r
\r
	#elif KERNEL_SIZE == 5 && MAX_VARYING_VECTORS >= 13\r
\r
		// Optimized 5x5\r
		vec4 c[] = vec4[KERNEL_SIZE_SQ](\r
			getTexel(vUv00), getTexel(vUv01), getTexel(vUv02), getTexel(vUv03), getTexel(vUv04),\r
			getTexel(vUv05), getTexel(vUv06), getTexel(vUv07), getTexel(vUv08), getTexel(vUv09),\r
			getTexel(vUv10), getTexel(vUv11), getTexel(vUv12), getTexel(vUv13), getTexel(vUv14),\r
			getTexel(vUv15), getTexel(vUv16), getTexel(vUv17), getTexel(vUv18), getTexel(vUv19),\r
			getTexel(vUv20), getTexel(vUv21), getTexel(vUv22), getTexel(vUv23), getTexel(vUv24)\r
		);\r
\r
		#ifdef BILATERAL\r
\r
			float z[] = float[KERNEL_SIZE_SQ](\r
				linearDepth(vUv00), linearDepth(vUv01), linearDepth(vUv02), linearDepth(vUv03), linearDepth(vUv04),\r
				linearDepth(vUv05), linearDepth(vUv06), linearDepth(vUv07), linearDepth(vUv08), linearDepth(vUv09),\r
				linearDepth(vUv10), linearDepth(vUv11), linearDepth(vUv12), linearDepth(vUv13), linearDepth(vUv14),\r
				linearDepth(vUv15), linearDepth(vUv16), linearDepth(vUv17), linearDepth(vUv18), linearDepth(vUv19),\r
				linearDepth(vUv20), linearDepth(vUv21), linearDepth(vUv22), linearDepth(vUv23), linearDepth(vUv24)\r
			);\r
\r
		#endif\r
\r
	#endif\r
\r
	vec4 result = vec4(0.0);\r
\r
	#ifdef BILATERAL\r
\r
		float w = 0.0;\r
\r
		#if KERNEL_SIZE == 3 || (KERNEL_SIZE == 5 && MAX_VARYING_VECTORS >= 13)\r
\r
			// Optimized 3x3 or 5x5\r
			float centerDepth = z[KERNEL_SIZE_SQ_HALF];\r
\r
			for(int i = 0; i < KERNEL_SIZE_SQ; ++i) {\r
\r
				float d = step(abs(z[i] - centerDepth), DISTANCE_THRESHOLD);\r
				result += c[i] * d;\r
				w += d;\r
\r
			}\r
\r
		#else\r
\r
			// General case\r
			float centerDepth = linearDepth(vUv);\r
			vec2 s = texelSize * scale;\r
\r
			for(int x = -KERNEL_SIZE_HALF; x <= KERNEL_SIZE_HALF; ++x) {\r
\r
				for(int y = -KERNEL_SIZE_HALF; y <= KERNEL_SIZE_HALF; ++y) {\r
\r
					vec2 coords = vUv + vec2(x, y) * s;\r
					vec4 c = getTexel(coords);\r
					float z = (x == 0 && y == 0) ? centerDepth : linearDepth(coords);\r
\r
					float d = step(abs(z - centerDepth), DISTANCE_THRESHOLD);\r
					result += c * d;\r
					w += d;\r
\r
				}\r
\r
			}\r
\r
		#endif\r
\r
		gl_FragColor = result / max(w, 1.0);\r
\r
	#else\r
\r
		#if KERNEL_SIZE == 3 || (KERNEL_SIZE == 5 && MAX_VARYING_VECTORS >= 13)\r
\r
			// Optimized 3x3 or 5x5\r
			for(int i = 0; i < KERNEL_SIZE_SQ; ++i) {\r
\r
				result += c[i];\r
\r
			}\r
\r
		#else\r
\r
			// General case\r
			vec2 s = texelSize * scale;\r
\r
			for(int x = -KERNEL_SIZE_HALF; x <= KERNEL_SIZE_HALF; ++x) {\r
\r
				for(int y = -KERNEL_SIZE_HALF; y <= KERNEL_SIZE_HALF; ++y) {\r
\r
					result += getTexel(uv + vec2(x, y) * s);\r
\r
				}\r
\r
			}\r
\r
		#endif\r
\r
		gl_FragColor = result * INV_KERNEL_SIZE_SQ;\r
\r
	#endif\r
\r
}\r
`, Dc = `uniform vec2 texelSize;\r
uniform float scale;\r
\r
#if KERNEL_SIZE == 3\r
\r
	// Optimized 3x3\r
	varying vec2 vUv00, vUv01, vUv02;\r
	varying vec2 vUv03, vUv04, vUv05;\r
	varying vec2 vUv06, vUv07, vUv08;\r
\r
#elif KERNEL_SIZE == 5 && MAX_VARYING_VECTORS >= 13\r
\r
	// Optimized 5x5\r
	varying vec2 vUv00, vUv01, vUv02, vUv03, vUv04;\r
	varying vec2 vUv05, vUv06, vUv07, vUv08, vUv09;\r
	varying vec2 vUv10, vUv11, vUv12, vUv13, vUv14;\r
	varying vec2 vUv15, vUv16, vUv17, vUv18, vUv19;\r
	varying vec2 vUv20, vUv21, vUv22, vUv23, vUv24;\r
\r
#else\r
\r
	// General case\r
	varying vec2 vUv;\r
\r
#endif\r
\r
void main() {\r
\r
	vec2 uv = position.xy * 0.5 + 0.5;\r
\r
	#if KERNEL_SIZE == 3\r
\r
		vec2 s = texelSize * scale;\r
\r
		// Optimized 3x3\r
		vUv00 = uv + s * vec2(-1.0, -1.0);\r
		vUv01 = uv + s * vec2(0.0, -1.0);\r
		vUv02 = uv + s * vec2(1.0, -1.0);\r
\r
		vUv03 = uv + s * vec2(-1.0, 0.0);\r
		vUv04 = uv;\r
		vUv05 = uv + s * vec2(1.0, 0.0);\r
\r
		vUv06 = uv + s * vec2(-1.0, 1.0);\r
		vUv07 = uv + s * vec2(0.0, 1.0);\r
		vUv08 = uv + s * vec2(1.0, 1.0);\r
\r
	#elif KERNEL_SIZE == 5\r
\r
		vec2 s = texelSize * scale;\r
\r
		// Optimized 5x5\r
		vUv00 = uv + s * vec2(-2.0, -2.0);\r
		vUv01 = uv + s * vec2(-1.0, -2.0);\r
		vUv02 = uv + s * vec2(0.0, -2.0);\r
		vUv03 = uv + s * vec2(1.0, -2.0);\r
		vUv04 = uv + s * vec2(2.0, -2.0);\r
\r
		vUv05 = uv + s * vec2(-2.0, -1.0);\r
		vUv06 = uv + s * vec2(-1.0, -1.0);\r
		vUv07 = uv + s * vec2(0.0, -1.0);\r
		vUv08 = uv + s * vec2(1.0, -1.0);\r
		vUv09 = uv + s * vec2(2.0, -1.0);\r
\r
		vUv10 = uv + s * vec2(-2.0, 0.0);\r
		vUv11 = uv + s * vec2(-1.0, 0.0);\r
		vUv12 = uv;\r
		vUv13 = uv + s * vec2(1.0, 0.0);\r
		vUv14 = uv + s * vec2(2.0, 0.0);\r
\r
		vUv15 = uv + s * vec2(-2.0, 1.0);\r
		vUv16 = uv + s * vec2(-1.0, 1.0);\r
		vUv17 = uv + s * vec2(0.0, 1.0);\r
		vUv18 = uv + s * vec2(1.0, 1.0);\r
		vUv19 = uv + s * vec2(2.0, 1.0);\r
\r
		vUv20 = uv + s * vec2(-2.0, 2.0);\r
		vUv21 = uv + s * vec2(-1.0, 2.0);\r
		vUv22 = uv + s * vec2(0.0, 2.0);\r
		vUv23 = uv + s * vec2(1.0, 2.0);\r
		vUv24 = uv + s * vec2(2.0, 2.0);\r
\r
	#else\r
\r
		// General case\r
		vUv = uv;\r
\r
	#endif\r
\r
	gl_Position = vec4(position.xy, 1.0, 1.0);\r
\r
}\r
`;
class Pc extends N {
  /**
   * Constructs a new box blur material.
   *
   * @param {Object} [options] - The options.
   * @param {Number} [options.bilateral=false] - Enables or disables bilateral blurring.
   * @param {Number} [options.kernelSize=5] - The kernel size.
   */
  constructor({ bilateral: e = !1, kernelSize: t = 5 } = {}) {
    super({
      name: "BoxBlurMaterial",
      defines: {
        DEPTH_PACKING: "0",
        DISTANCE_THRESHOLD: "0.1"
      },
      uniforms: {
        inputBuffer: new a(null),
        depthBuffer: new a(null),
        normalDepthBuffer: new a(null),
        texelSize: new a(new g()),
        cameraNearFar: new a(new g()),
        scale: new a(1)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: bc,
      vertexShader: Dc
    }), this.bilateral = e, this.kernelSize = t, this.maxVaryingVectors = 8;
  }
  /**
   * The maximum amount of varying vectors.
   *
   * Should be synced with `renderer.capabilities.maxVaryings`. Default is 8.
   *
   * @type {Number}
   */
  set maxVaryingVectors(e) {
    this.defines.MAX_VARYING_VECTORS = e.toFixed(0);
  }
  /**
   * The kernel size.
   *
   * - Must be an odd number
   * - Kernel size 3 and 5 use optimized code paths
   * - Default is 5
   *
   * @type {Number}
   */
  get kernelSize() {
    return Number(this.defines.KERNEL_SIZE);
  }
  set kernelSize(e) {
    if (e % 2 === 0)
      throw new Error("The kernel size must be an odd number");
    this.defines.KERNEL_SIZE = e.toFixed(0), this.defines.KERNEL_SIZE_HALF = Math.floor(e / 2).toFixed(0), this.defines.KERNEL_SIZE_SQ = (e ** 2).toFixed(0), this.defines.KERNEL_SIZE_SQ_HALF = Math.floor(e ** 2 / 2).toFixed(0), this.defines.INV_KERNEL_SIZE_SQ = (1 / e ** 2).toFixed(6), this.needsUpdate = !0;
  }
  /**
   * The blur scale.
   *
   * @type {Number}
   */
  get scale() {
    return this.uniforms.scale.value;
  }
  set scale(e) {
    this.uniforms.scale.value = e;
  }
  /**
   * The current near plane setting.
   *
   * @type {Number}
   * @private
   */
  get near() {
    return this.uniforms.cameraNearFar.value.x;
  }
  /**
   * The current far plane setting.
   *
   * @type {Number}
   * @private
   */
  get far() {
    return this.uniforms.cameraNearFar.value.y;
  }
  /**
   * The input buffer.
   *
   * @type {Texture}
   */
  set inputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * The depth buffer.
   *
   * @type {Texture}
   */
  set depthBuffer(e) {
    this.uniforms.depthBuffer.value = e;
  }
  /**
   * A combined normal-depth buffer. Overrides {@link depthBuffer} if set.
   *
   * @type {Texture}
   */
  set normalDepthBuffer(e) {
    this.uniforms.normalDepthBuffer.value = e, e !== null ? this.defines.NORMAL_DEPTH = "1" : delete this.defines.NORMAL_DEPTH, this.needsUpdate = !0;
  }
  /**
   * The depth packing strategy.
   *
   * @type {DepthPackingStrategies}
   */
  set depthPacking(e) {
    this.defines.DEPTH_PACKING = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Indicates whether bilateral filtering is enabled.
   *
   * @type {Boolean}
   */
  get bilateral() {
    return this.defines.BILATERAL !== void 0;
  }
  set bilateral(e) {
    e !== null ? this.defines.BILATERAL = "1" : delete this.defines.BILATERAL, this.needsUpdate = !0;
  }
  /**
   * The bilateral filter distance threshold in world units.
   *
   * @type {Number}
   */
  get worldDistanceThreshold() {
    return -$e(Number(this.defines.DISTANCE_THRESHOLD), this.near, this.far);
  }
  set worldDistanceThreshold(e) {
    const t = Qe(-e, this.near, this.far);
    this.defines.DISTANCE_THRESHOLD = t.toFixed(12), this.needsUpdate = !0;
  }
  /**
   * Copies the settings of the given camera.
   *
   * @param {Camera} camera - A camera.
   */
  copyCameraSettings(e) {
    e && (this.uniforms.cameraNearFar.value.set(e.near, e.far), e instanceof ke ? this.defines.PERSPECTIVE_CAMERA = "1" : delete this.defines.PERSPECTIVE_CAMERA, this.needsUpdate = !0);
  }
  /**
   * Sets the size of this object.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.uniforms.texelSize.value.set(1 / e, 1 / t);
  }
}
class yd extends X {
  /**
   * Constructs a new box blur pass.
   *
   * @param {Object} [options] - The options.
   * @param {Number} [options.kernelSize=5] - Must be an odd number. The sizes 3 and 5 use optimized code paths.
   * @param {Number} [options.iterations=1] - The amount of times the blur should be applied.
   * @param {Number} [options.bilateral=false] - Enables or disables bilateral blurring.
   * @param {Number} [options.resolutionScale=1.0] - The resolution scale.
   * @param {Number} [options.resolutionX=Resolution.AUTO_SIZE] - The horizontal resolution.
   * @param {Number} [options.resolutionY=Resolution.AUTO_SIZE] - The vertical resolution.
   */
  constructor({
    kernelSize: e = 5,
    iterations: t = 1,
    bilateral: r = !1,
    resolutionScale: n = 1,
    resolutionX: i = W.AUTO_SIZE,
    resolutionY: s = W.AUTO_SIZE
  } = {}) {
    super("BoxBlurPass"), this.needsDepthTexture = r, this.renderTargetA = new A(1, 1, { depthBuffer: !1 }), this.renderTargetA.texture.name = "Blur.Target.A", this.renderTargetB = new A(1, 1, { depthBuffer: !1 }), this.renderTargetB.texture.name = "Blur.Target.B", this.blurMaterial = new Pc({ bilateral: r, kernelSize: e }), this.copyMaterial = new dr();
    const o = this.resolution = new W(this, i, s, n);
    o.addEventListener("change", (l) => this.setSize(o.baseWidth, o.baseHeight)), this.iterations = t;
  }
  set mainCamera(e) {
    this.blurMaterial.copyCameraSettings(e);
  }
  /**
   * Sets the depth texture.
   *
   * @param {Texture} depthTexture - A depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing strategy.
   */
  setDepthTexture(e, t = $) {
    this.blurMaterial.depthBuffer = e, this.blurMaterial.depthPacking = t;
  }
  /**
   * Renders the blur.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i) {
    const s = this.scene, o = this.camera, l = this.renderTargetA, u = this.renderTargetB, f = this.blurMaterial;
    this.fullscreenMaterial = f;
    let h = t;
    for (let m = 0, p = Math.max(this.iterations, 1); m < p; ++m) {
      const d = (m & 1) === 0 ? l : u;
      f.inputBuffer = h.texture, e.setRenderTarget(d), e.render(s, o), h = d;
    }
    this.copyMaterial.inputBuffer = h.texture, this.fullscreenMaterial = this.copyMaterial, e.setRenderTarget(this.renderToScreen ? null : r), e.render(s, o);
  }
  /**
   * Updates the size of this pass.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.resolution;
    r.setBaseSize(e, t);
    const n = r.width, i = r.height;
    this.renderTargetA.setSize(n, i), this.renderTargetB.setSize(n, i), this.blurMaterial.setSize(e, t);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    e !== null && (this.blurMaterial.maxVaryingVectors = e.capabilities.maxVaryings), r !== void 0 && (this.renderTargetA.texture.type = r, this.renderTargetB.texture.type = r, r !== se ? this.fullscreenMaterial.defines.FRAMEBUFFER_PRECISION_HIGH = "1" : e !== null && e.outputColorSpace === V && (this.renderTargetA.texture.colorSpace = V, this.renderTargetB.texture.colorSpace = V));
  }
}
const Cc = `#include <packing>\r
\r
varying vec2 vUv;\r
\r
#ifdef NORMAL_DEPTH\r
\r
	#ifdef GL_FRAGMENT_PRECISION_HIGH\r
\r
		uniform highp sampler2D normalDepthBuffer;\r
\r
	#else\r
\r
		uniform mediump sampler2D normalDepthBuffer;\r
\r
	#endif\r
\r
	float readDepth(const in vec2 uv) {\r
\r
		return texture2D(normalDepthBuffer, uv).a;\r
\r
	}\r
\r
#else\r
\r
	#if INPUT_DEPTH_PACKING == 3201\r
\r
		uniform lowp sampler2D depthBuffer;\r
\r
	#elif defined(GL_FRAGMENT_PRECISION_HIGH)\r
\r
		uniform highp sampler2D depthBuffer;\r
\r
	#else\r
\r
		uniform mediump sampler2D depthBuffer;\r
\r
	#endif\r
\r
	float readDepth(const in vec2 uv) {\r
\r
		#if INPUT_DEPTH_PACKING == 3201\r
\r
			return unpackRGBAToDepth(texture2D(depthBuffer, uv));\r
\r
		#else\r
\r
			return texture2D(depthBuffer, uv).r;\r
\r
		#endif\r
\r
	}\r
\r
#endif\r
\r
void main() {\r
\r
	#if INPUT_DEPTH_PACKING == OUTPUT_DEPTH_PACKING\r
\r
		gl_FragColor = texture2D(depthBuffer, vUv);\r
\r
	#else\r
\r
		float depth = readDepth(vUv);\r
\r
		#if OUTPUT_DEPTH_PACKING == 3201\r
\r
			gl_FragColor = (depth == 1.0) ? vec4(1.0) : packDepthToRGBA(depth);\r
\r
		#else\r
\r
			gl_FragColor = vec4(vec3(depth), 1.0);\r
\r
		#endif\r
\r
	#endif\r
\r
}\r
`, Ac = `varying vec2 vUv;\r
\r
#if DEPTH_COPY_MODE == 1\r
\r
	uniform vec2 texelPosition;\r
\r
#endif\r
\r
void main() {\r
\r
	#if DEPTH_COPY_MODE == 1\r
\r
		vUv = texelPosition;\r
\r
	#else\r
\r
		vUv = position.xy * 0.5 + 0.5;\r
\r
	#endif\r
\r
	gl_Position = vec4(position.xy, 1.0, 1.0);\r
\r
}\r
`;
class Ec extends N {
  /**
   * Constructs a new depth copy material.
   */
  constructor() {
    super({
      name: "DepthCopyMaterial",
      defines: {
        INPUT_DEPTH_PACKING: "0",
        OUTPUT_DEPTH_PACKING: "0",
        DEPTH_COPY_MODE: "0"
      },
      uniforms: {
        depthBuffer: new a(null),
        texelPosition: new a(new g())
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: Cc,
      vertexShader: Ac
    }), this.depthCopyMode = Rt.FULL;
  }
  /**
   * The input depth buffer.
   *
   * @type {Texture}
   */
  get depthBuffer() {
    return this.uniforms.depthBuffer.value;
  }
  set depthBuffer(e) {
    this.uniforms.depthBuffer.value = e;
  }
  /**
   * The input depth packing strategy.
   *
   * @type {DepthPackingStrategies}
   */
  set inputDepthPacking(e) {
    this.defines.INPUT_DEPTH_PACKING = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * The output depth packing strategy.
   *
   * @type {DepthPackingStrategies}
   */
  get outputDepthPacking() {
    return Number(this.defines.OUTPUT_DEPTH_PACKING);
  }
  set outputDepthPacking(e) {
    this.defines.OUTPUT_DEPTH_PACKING = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Sets the input depth buffer.
   *
   * @deprecated Use depthBuffer and inputDepthPacking instead.
   * @param {Texture} buffer - The depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing strategy.
   */
  setDepthBuffer(e, t = $) {
    this.depthBuffer = e, this.inputDepthPacking = t;
  }
  /**
   * Returns the current input depth packing strategy.
   *
   * @deprecated
   * @return {DepthPackingStrategies} The input depth packing strategy.
   */
  getInputDepthPacking() {
    return Number(this.defines.INPUT_DEPTH_PACKING);
  }
  /**
   * Sets the input depth packing strategy.
   *
   * @deprecated Use inputDepthPacking instead.
   * @param {DepthPackingStrategies} value - The new input depth packing strategy.
   */
  setInputDepthPacking(e) {
    this.defines.INPUT_DEPTH_PACKING = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Returns the current output depth packing strategy.
   *
   * @deprecated Use outputDepthPacking instead.
   * @return {DepthPackingStrategies} The output depth packing strategy.
   */
  getOutputDepthPacking() {
    return Number(this.defines.OUTPUT_DEPTH_PACKING);
  }
  /**
   * Sets the output depth packing strategy.
   *
   * @deprecated Use outputDepthPacking instead.
   * @param {DepthPackingStrategies} value - The new output depth packing strategy.
   */
  setOutputDepthPacking(e) {
    this.defines.OUTPUT_DEPTH_PACKING = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * The screen space position used for single-texel copy operations.
   *
   * @type {Vector2}
   */
  get texelPosition() {
    return this.uniforms.texelPosition.value;
  }
  /**
   * Returns the screen space position used for single-texel copy operations.
   *
   * @deprecated Use texelPosition instead.
   * @return {Vector2} The position.
   */
  getTexelPosition() {
    return this.uniforms.texelPosition.value;
  }
  /**
   * Sets the screen space position used for single-texel copy operations.
   *
   * @deprecated
   * @param {Vector2} value - The position.
   */
  setTexelPosition(e) {
    this.uniforms.texelPosition.value = e;
  }
  /**
   * The depth copy mode.
   *
   * @type {DepthCopyMode}
   */
  get mode() {
    return this.depthCopyMode;
  }
  set mode(e) {
    this.depthCopyMode = e, this.defines.DEPTH_COPY_MODE = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Returns the depth copy mode.
   *
   * @deprecated Use mode instead.
   * @return {DepthCopyMode} The depth copy mode.
   */
  getMode() {
    return this.mode;
  }
  /**
   * Sets the depth copy mode.
   *
   * @deprecated Use mode instead.
   * @param {DepthCopyMode} value - The new mode.
   */
  setMode(e) {
    this.mode = e;
  }
}
class Rc extends X {
  /**
   * Constructs a new depth save pass.
   *
   * @param {Object} [options] - The options.
   * @param {DepthPackingStrategies} [options.depthPacking=RGBADepthPacking] - The output depth packing.
   */
  constructor({ depthPacking: e = ce } = {}) {
    super("DepthCopyPass");
    const t = new Ec();
    t.outputDepthPacking = e, this.fullscreenMaterial = t, this.needsDepthTexture = !0, this.needsSwap = !1, this.renderTarget = new A(1, 1, {
      type: e === ce ? se : Y,
      minFilter: b,
      magFilter: b,
      depthBuffer: !1
    }), this.renderTarget.texture.name = "DepthCopyPass.Target";
  }
  /**
   * The output depth texture.
   *
   * @type {Texture}
   */
  get texture() {
    return this.renderTarget.texture;
  }
  /**
   * Returns the output depth texture.
   *
   * @deprecated Use texture instead.
   * @return {Texture} The texture.
   */
  getTexture() {
    return this.renderTarget.texture;
  }
  /**
   * The output depth packing.
   *
   * @type {DepthPackingStrategies}
   */
  get depthPacking() {
    return this.fullscreenMaterial.outputDepthPacking;
  }
  /**
   * Returns the output depth packing.
   *
   * @deprecated Use depthPacking instead.
   * @return {DepthPackingStrategies} The depth packing.
   */
  getDepthPacking() {
    return this.fullscreenMaterial.outputDepthPacking;
  }
  /**
   * Sets the depth texture.
   *
   * @param {Texture} depthTexture - A depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing.
   */
  setDepthTexture(e, t = $) {
    this.fullscreenMaterial.depthBuffer = e, this.fullscreenMaterial.inputDepthPacking = t;
  }
  /**
   * Copies depth from a depth texture.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i) {
    e.setRenderTarget(this.renderToScreen ? null : this.renderTarget), e.render(this.scene, this.camera);
  }
  /**
   * Updates the size of this pass.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.renderTarget.setSize(e, t);
  }
}
const Nc = Number(Vt.replace(/\D+/g, "")), et = 255 / 256, zc = new Float32Array([
  et / 256 ** 3,
  et / 256 ** 2,
  et / 256,
  et
]), Fc = new Float32Array([
  et,
  et / 256,
  et / 256 ** 2,
  1 / 256 ** 3
]);
function Ic(c) {
  const e = Nc >= 167 ? Fc : zc;
  return (c[0] * e[0] + c[1] * e[1] + c[2] * e[2] + c[3] * e[3]) / 255;
}
class Md extends Rc {
  /**
   * Constructs a new depth picking pass.
   *
   * @param {Object} [options] - The options.
   * @param {DepthPackingStrategies} [options.depthPacking=RGBADepthPacking] - The depth packing.
   * @param {Number} [options.mode=DepthCopyMode.SINGLE] - The depth copy mode.
   */
  constructor({ depthPacking: e = ce, mode: t = Rt.SINGLE } = {}) {
    if (e !== ce && e !== $)
      throw new Error(`Unsupported depth packing: ${e}`);
    super({ depthPacking: e }), this.name = "DepthPickingPass", this.fullscreenMaterial.mode = t, this.pixelBuffer = e === ce ? new Uint8Array(4) : new Float32Array(4), this.callback = null;
  }
  /**
   * Reads depth at a specific screen position.
   *
   * Only one depth value can be picked per frame. Calling this method multiple times per frame will overwrite the
   * picking coordinates. Unresolved promises will be abandoned.
   *
   * @example
   * const ndc = new Vector3();
   * const clientRect = myViewport.getBoundingClientRect();
   * const clientX = pointerEvent.clientX - clientRect.left;
   * const clientY = pointerEvent.clientY - clientRect.top;
   * ndc.x = (clientX / myViewport.clientWidth) * 2.0 - 1.0;
   * ndc.y = -(clientY / myViewport.clientHeight) * 2.0 + 1.0;
   * const depth = await depthPickingPass.readDepth(ndc);
   * ndc.z = depth * 2.0 - 1.0;
   *
   * const worldPosition = ndc.unproject(camera);
   *
   * @param {Vector2|Vector3} ndc - Normalized device coordinates. Only X and Y are relevant.
   * @return {Promise<Number>} A promise that returns the depth on the next frame.
   */
  readDepth(e) {
    return this.fullscreenMaterial.texelPosition.set(e.x * 0.5 + 0.5, e.y * 0.5 + 0.5), new Promise((t) => {
      this.callback = t;
    });
  }
  /**
   * Copies depth and resolves depth picking promises.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i) {
    const s = this.fullscreenMaterial, o = s.mode;
    if (o === Rt.FULL && super.render(e), this.callback !== null) {
      const l = this.renderTarget, u = this.pixelBuffer, f = l.texture.type !== Y;
      let h = 0, m = 0;
      if (o === Rt.SINGLE)
        super.render(e);
      else {
        const p = s.texelPosition;
        h = Math.round(p.x * l.width), m = Math.round(p.y * l.height);
      }
      e.readRenderTargetPixels(l, h, m, 1, 1, u), this.callback(f ? Ic(u) : u[0]), this.callback = null;
    }
  }
  /**
   * Updates the size of this pass.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.fullscreenMaterial.mode === Rt.FULL && super.setSize(e, t);
  }
}
const Bc = `#include <common>\r
#include <packing>\r
#include <dithering_pars_fragment>\r
\r
#define packFloatToRGBA(v) packDepthToRGBA(v)\r
#define unpackRGBAToFloat(v) unpackRGBAToDepth(v)\r
\r
#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D inputBuffer;\r
\r
#else\r
\r
	uniform lowp sampler2D inputBuffer;\r
\r
#endif\r
\r
#if DEPTH_PACKING == 3201\r
\r
	uniform lowp sampler2D depthBuffer;\r
\r
#elif defined(GL_FRAGMENT_PRECISION_HIGH)\r
\r
	uniform highp sampler2D depthBuffer;\r
\r
#else\r
\r
	uniform mediump sampler2D depthBuffer;\r
\r
#endif\r
\r
uniform vec2 resolution;\r
uniform vec2 texelSize;\r
\r
uniform float cameraNear;\r
uniform float cameraFar;\r
uniform float aspect;\r
uniform float time;\r
\r
varying vec2 vUv;\r
\r
vec4 sRGBToLinear(const in vec4 value) {\r
\r
	return vec4(mix(\r
		pow(value.rgb * 0.9478672986 + vec3(0.0521327014), vec3(2.4)),\r
		value.rgb * 0.0773993808,\r
		vec3(lessThanEqual(value.rgb, vec3(0.04045)))\r
	), value.a);\r
\r
}\r
\r
float readDepth(const in vec2 uv) {\r
\r
	#if DEPTH_PACKING == 3201\r
\r
		return unpackRGBAToDepth(texture2D(depthBuffer, uv));\r
\r
	#else\r
\r
		return texture2D(depthBuffer, uv).r;\r
\r
	#endif\r
\r
}\r
\r
float getViewZ(const in float depth) {\r
\r
	#ifdef PERSPECTIVE_CAMERA\r
\r
		return perspectiveDepthToViewZ(depth, cameraNear, cameraFar);\r
\r
	#else\r
\r
		return orthographicDepthToViewZ(depth, cameraNear, cameraFar);\r
\r
	#endif\r
\r
}\r
\r
/**\r
 * Based on work by Sam Hocevar, Emil Persson and Ian Taylor.\r
 * https://www.chilliant.com/rgb2hsv.html\r
 */\r
\r
vec3 RGBToHCV(const in vec3 RGB) {\r
\r
	vec4 P = mix(vec4(RGB.bg, -1.0, 2.0 / 3.0), vec4(RGB.gb, 0.0, -1.0 / 3.0), step(RGB.b, RGB.g));\r
	vec4 Q = mix(vec4(P.xyw, RGB.r), vec4(RGB.r, P.yzx), step(P.x, RGB.r));\r
	float C = Q.x - min(Q.w, Q.y);\r
	float H = abs((Q.w - Q.y) / (6.0 * C + EPSILON) + Q.z);\r
	return vec3(H, C, Q.x);\r
\r
}\r
\r
vec3 RGBToHSL(const in vec3 RGB) {\r
\r
	vec3 HCV = RGBToHCV(RGB);\r
	float L = HCV.z - HCV.y * 0.5;\r
	float S = HCV.y / (1.0 - abs(L * 2.0 - 1.0) + EPSILON);\r
	return vec3(HCV.x, S, L);\r
\r
}\r
\r
vec3 HueToRGB(const in float H) {\r
\r
	float R = abs(H * 6.0 - 3.0) - 1.0;\r
	float G = 2.0 - abs(H * 6.0 - 2.0);\r
	float B = 2.0 - abs(H * 6.0 - 4.0);\r
	return clamp(vec3(R, G, B), 0.0, 1.0);\r
\r
}\r
\r
vec3 HSLToRGB(const in vec3 HSL) {\r
\r
	vec3 RGB = HueToRGB(HSL.x);\r
	float C = (1.0 - abs(2.0 * HSL.z - 1.0)) * HSL.y;\r
	return (RGB - 0.5) * C + HSL.z;\r
\r
}\r
\r
FRAGMENT_HEAD\r
\r
void main() {\r
\r
	FRAGMENT_MAIN_UV\r
\r
	vec4 color0 = texture2D(inputBuffer, UV);\r
	vec4 color1 = vec4(0.0);\r
\r
	FRAGMENT_MAIN_IMAGE\r
\r
	color0.a = clamp(color0.a, 0.0, 1.0);\r
	gl_FragColor = color0;\r
\r
	#ifdef ENCODE_OUTPUT\r
\r
		#include <colorspace_fragment>\r
\r
	#endif\r
\r
	#include <dithering_fragment>\r
\r
}\r
`, Lc = `uniform vec2 resolution;\r
uniform vec2 texelSize;\r
\r
uniform float cameraNear;\r
uniform float cameraFar;\r
uniform float aspect;\r
uniform float time;\r
\r
varying vec2 vUv;\r
\r
VERTEX_HEAD\r
\r
void main() {\r
\r
	vUv = position.xy * 0.5 + 0.5;\r
\r
	VERTEX_MAIN_SUPPORT\r
\r
	gl_Position = vec4(position.xy, 1.0, 1.0);\r
\r
}\r
`;
class Uc extends N {
  /**
   * Constructs a new effect material.
   *
   * @param {Map<String, String>} [shaderParts] - Deprecated. Use setShaderData instead.
   * @param {Map<String, String>} [defines] - Deprecated. Use setShaderData instead.
   * @param {Map<String, Uniform>} [uniforms] - Deprecated. Use setShaderData instead.
   * @param {Camera} [camera] - A camera.
   * @param {Boolean} [dithering=false] - Deprecated.
   */
  constructor(e, t, r, n, i = !1) {
    super({
      name: "EffectMaterial",
      defines: {
        THREE_REVISION: Vt.replace(/\D+/g, ""),
        DEPTH_PACKING: "0",
        ENCODE_OUTPUT: "1"
      },
      uniforms: {
        inputBuffer: new a(null),
        depthBuffer: new a(null),
        resolution: new a(new g()),
        texelSize: new a(new g()),
        cameraNear: new a(0.3),
        cameraFar: new a(1e3),
        aspect: new a(1),
        time: new a(0)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      dithering: i
    }), e && this.setShaderParts(e), t && this.setDefines(t), r && this.setUniforms(r), this.copyCameraSettings(n);
  }
  /**
   * The input buffer.
   *
   * @type {Texture}
   */
  set inputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * Sets the input buffer.
   *
   * @deprecated Use inputBuffer instead.
   * @param {Texture} value - The input buffer.
   */
  setInputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * The depth buffer.
   *
   * @type {Texture}
   */
  get depthBuffer() {
    return this.uniforms.depthBuffer.value;
  }
  set depthBuffer(e) {
    this.uniforms.depthBuffer.value = e;
  }
  /**
   * The depth packing strategy.
   *
   * @type {DepthPackingStrategies}
   */
  get depthPacking() {
    return Number(this.defines.DEPTH_PACKING);
  }
  set depthPacking(e) {
    this.defines.DEPTH_PACKING = e.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Sets the depth buffer.
   *
   * @deprecated Use depthBuffer and depthPacking instead.
   * @param {Texture} buffer - The depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing strategy.
   */
  setDepthBuffer(e, t = $) {
    this.depthBuffer = e, this.depthPacking = t;
  }
  /**
   * Sets the shader data.
   *
   * @param {EffectShaderData} data - The shader data.
   * @return {EffectMaterial} This material.
   */
  setShaderData(e) {
    this.setShaderParts(e.shaderParts), this.setDefines(e.defines), this.setUniforms(e.uniforms), this.setExtensions(e.extensions);
  }
  /**
   * Sets the shader parts.
   *
   * @deprecated Use setShaderData instead.
   * @param {Map<String, String>} shaderParts - A collection of shader snippets. See {@link EffectShaderSection}.
   * @return {EffectMaterial} This material.
   */
  setShaderParts(e) {
    return this.fragmentShader = Bc.replace(_.FRAGMENT_HEAD, e.get(_.FRAGMENT_HEAD) || "").replace(_.FRAGMENT_MAIN_UV, e.get(_.FRAGMENT_MAIN_UV) || "").replace(_.FRAGMENT_MAIN_IMAGE, e.get(_.FRAGMENT_MAIN_IMAGE) || ""), this.vertexShader = Lc.replace(_.VERTEX_HEAD, e.get(_.VERTEX_HEAD) || "").replace(_.VERTEX_MAIN_SUPPORT, e.get(_.VERTEX_MAIN_SUPPORT) || ""), this.needsUpdate = !0, this;
  }
  /**
   * Sets the shader macros.
   *
   * @deprecated Use setShaderData instead.
   * @param {Map<String, String>} defines - A collection of preprocessor macro definitions.
   * @return {EffectMaterial} This material.
   */
  setDefines(e) {
    for (const t of e.entries())
      this.defines[t[0]] = t[1];
    return this.needsUpdate = !0, this;
  }
  /**
   * Sets the shader uniforms.
   *
   * @deprecated Use setShaderData instead.
   * @param {Map<String, Uniform>} uniforms - A collection of uniforms.
   * @return {EffectMaterial} This material.
   */
  setUniforms(e) {
    for (const t of e.entries())
      this.uniforms[t[0]] = t[1];
    return this;
  }
  /**
   * Sets the required shader extensions.
   *
   * @deprecated Use setShaderData instead.
   * @param {Set<WebGLExtension>} extensions - A collection of extensions.
   * @return {EffectMaterial} This material.
   */
  setExtensions(e) {
    this.extensions = {};
    for (const t of e)
      this.extensions[t] = !0;
    return this;
  }
  /**
   * Indicates whether output encoding is enabled.
   *
   * @type {Boolean}
   */
  get encodeOutput() {
    return this.defines.ENCODE_OUTPUT !== void 0;
  }
  set encodeOutput(e) {
    this.encodeOutput !== e && (e ? this.defines.ENCODE_OUTPUT = "1" : delete this.defines.ENCODE_OUTPUT, this.needsUpdate = !0);
  }
  /**
   * Indicates whether output encoding is enabled.
   *
   * @deprecated Use encodeOutput instead.
   * @return {Boolean} Whether output encoding is enabled.
   */
  isOutputEncodingEnabled(e) {
    return this.encodeOutput;
  }
  /**
   * Enables or disables output encoding.
   *
   * @deprecated Use encodeOutput instead.
   * @param {Boolean} value - Whether output encoding should be enabled.
   */
  setOutputEncodingEnabled(e) {
    this.encodeOutput = e;
  }
  /**
   * The time in seconds.
   *
   * @type {Number}
   */
  get time() {
    return this.uniforms.time.value;
  }
  set time(e) {
    this.uniforms.time.value = e;
  }
  /**
   * Sets the delta time.
   *
   * @deprecated Use time instead.
   * @param {Number} value - The delta time in seconds.
   */
  setDeltaTime(e) {
    this.uniforms.time.value += e;
  }
  /**
   * Copies the settings of the given camera.
   *
   * @deprecated Use copyCameraSettings instead.
   * @param {Camera} camera - A camera.
   */
  adoptCameraSettings(e) {
    this.copyCameraSettings(e);
  }
  /**
   * Copies the settings of the given camera.
   *
   * @param {Camera} camera - A camera.
   */
  copyCameraSettings(e) {
    e && (this.uniforms.cameraNear.value = e.near, this.uniforms.cameraFar.value = e.far, e instanceof ke ? this.defines.PERSPECTIVE_CAMERA = "1" : delete this.defines.PERSPECTIVE_CAMERA, this.needsUpdate = !0);
  }
  /**
   * Sets the resolution.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.uniforms;
    r.resolution.value.set(e, t), r.texelSize.value.set(1 / e, 1 / t), r.aspect.value = e / t;
  }
  /**
   * An enumeration of shader code placeholders.
   *
   * @deprecated Use EffectShaderSection instead.
   * @type {Object}
   */
  static get Section() {
    return _;
  }
}
function Wn(c, e, t) {
  for (const r of e) {
    const n = "$1" + c + r.charAt(0).toUpperCase() + r.slice(1), i = new RegExp("([^\\.])(\\b" + r + "\\b)", "g");
    for (const s of t.entries())
      s[1] !== null && t.set(s[0], s[1].replace(i, n));
  }
}
function Oc(c, e, t) {
  let r = e.getFragmentShader(), n = e.getVertexShader();
  const i = r !== void 0 && /mainImage/.test(r), s = r !== void 0 && /mainUv/.test(r);
  if (t.attributes |= e.getAttributes(), r === void 0)
    throw new Error(`Missing fragment shader (${e.name})`);
  if (s && (t.attributes & Q.CONVOLUTION) !== 0)
    throw new Error(`Effects that transform UVs are incompatible with convolution effects (${e.name})`);
  if (!i && !s)
    throw new Error(`Could not find mainImage or mainUv function (${e.name})`);
  {
    const o = /\w+\s+(\w+)\([\w\s,]*\)\s*{/g, l = t.shaderParts;
    let u = l.get(_.FRAGMENT_HEAD) || "", f = l.get(_.FRAGMENT_MAIN_UV) || "", h = l.get(_.FRAGMENT_MAIN_IMAGE) || "", m = l.get(_.VERTEX_HEAD) || "", p = l.get(_.VERTEX_MAIN_SUPPORT) || "";
    const d = /* @__PURE__ */ new Set(), v = /* @__PURE__ */ new Set();
    if (s && (f += `	${c}MainUv(UV);
`, t.uvTransformation = !0), n !== null && /mainSupport/.test(n)) {
      const M = /mainSupport *\([\w\s]*?uv\s*?\)/.test(n);
      p += `	${c}MainSupport(`, p += M ? `vUv);
` : `);
`;
      for (const D of n.matchAll(/(?:varying\s+\w+\s+([\S\s]*?);)/g))
        for (const P of D[1].split(/\s*,\s*/))
          t.varyings.add(P), d.add(P), v.add(P);
      for (const D of n.matchAll(o))
        v.add(D[1]);
    }
    for (const M of r.matchAll(o))
      v.add(M[1]);
    for (const M of e.defines.keys())
      v.add(M.replace(/\([\w\s,]*\)/g, ""));
    for (const M of e.uniforms.keys())
      v.add(M);
    v.delete("while"), v.delete("for"), v.delete("if"), e.uniforms.forEach((M, D) => t.uniforms.set(c + D.charAt(0).toUpperCase() + D.slice(1), M)), e.defines.forEach((M, D) => t.defines.set(c + D.charAt(0).toUpperCase() + D.slice(1), M));
    const x = /* @__PURE__ */ new Map([["fragment", r], ["vertex", n]]);
    Wn(c, v, t.defines), Wn(c, v, x), r = x.get("fragment"), n = x.get("vertex");
    const w = e.blendMode;
    if (t.blendModes.set(w.blendFunction, w), i) {
      e.inputColorSpace !== null && e.inputColorSpace !== t.colorSpace && (h += e.inputColorSpace === V ? `color0 = sRGBTransferOETF(color0);
	` : `color0 = sRGBToLinear(color0);
	`), e.outputColorSpace !== Fe ? t.colorSpace = e.outputColorSpace : e.inputColorSpace !== null && (t.colorSpace = e.inputColorSpace);
      const M = /MainImage *\([\w\s,]*?depth[\w\s,]*?\)/;
      h += `${c}MainImage(color0, UV, `, (t.attributes & Q.DEPTH) !== 0 && M.test(r) && (h += "depth, ", t.readDepth = !0), h += `color1);
	`;
      const D = c + "BlendOpacity";
      t.uniforms.set(D, w.opacity), h += `color0 = blend${w.blendFunction}(color0, color1, ${D});

	`, u += `uniform float ${D};

`;
    }
    if (u += r + `
`, n !== null && (m += n + `
`), l.set(_.FRAGMENT_HEAD, u), l.set(_.FRAGMENT_MAIN_UV, f), l.set(_.FRAGMENT_MAIN_IMAGE, h), l.set(_.VERTEX_HEAD, m), l.set(_.VERTEX_MAIN_SUPPORT, p), e.extensions !== null)
      for (const M of e.extensions)
        t.extensions.add(M);
  }
}
class kc extends X {
  /**
   * Constructs a new effect pass.
   *
   * @param {Camera} camera - The main camera.
   * @param {...Effect} effects - The effects that will be rendered by this pass.
   */
  constructor(e, ...t) {
    super("EffectPass"), this.fullscreenMaterial = new Uc(null, null, null, e), this.listener = (r) => this.handleEvent(r), this.effects = [], this.setEffects(t), this.skipRendering = !1, this.minTime = 1, this.maxTime = Number.POSITIVE_INFINITY, this.timeScale = 1;
  }
  set mainScene(e) {
    for (const t of this.effects)
      t.mainScene = e;
  }
  set mainCamera(e) {
    this.fullscreenMaterial.copyCameraSettings(e);
    for (const t of this.effects)
      t.mainCamera = e;
  }
  /**
   * Indicates whether this pass encodes its output when rendering to screen.
   *
   * @type {Boolean}
   * @deprecated Use fullscreenMaterial.encodeOutput instead.
   */
  get encodeOutput() {
    return this.fullscreenMaterial.encodeOutput;
  }
  set encodeOutput(e) {
    this.fullscreenMaterial.encodeOutput = e;
  }
  /**
   * Indicates whether dithering is enabled.
   *
   * @type {Boolean}
   */
  get dithering() {
    return this.fullscreenMaterial.dithering;
  }
  set dithering(e) {
    const t = this.fullscreenMaterial;
    t.dithering = e, t.needsUpdate = !0;
  }
  /**
   * Sets the effects.
   *
   * @param {Effect[]} effects - The effects.
   * @protected
   */
  setEffects(e) {
    for (const t of this.effects)
      t.removeEventListener("change", this.listener), t.removeEventListener("updateEffectPass", this.listener);
    this.effects = e.sort((t, r) => r.attributes - t.attributes);
    for (const t of this.effects)
      t.addEventListener("change", this.listener), t.addEventListener("updateEffectPass", this.listener);
  }
  /**
   * Updates the compound shader material.
   *
   * @protected
   */
  updateMaterial() {
    const e = new zs();
    let t = 0;
    for (const o of this.effects)
      if (o.blendMode.blendFunction === E.DST)
        e.attributes |= o.getAttributes() & Q.DEPTH;
      else {
        if ((e.attributes & o.getAttributes() & Q.CONVOLUTION) !== 0)
          throw new Error(`Convolution effects cannot be merged (${o.name})`);
        Oc("e" + t++, o, e);
      }
    let r = e.shaderParts.get(_.FRAGMENT_HEAD), n = e.shaderParts.get(_.FRAGMENT_MAIN_IMAGE), i = e.shaderParts.get(_.FRAGMENT_MAIN_UV);
    const s = /\bblend\b/g;
    for (const o of e.blendModes.values())
      r += o.getShaderCode().replace(s, `blend${o.blendFunction}`) + `
`;
    (e.attributes & Q.DEPTH) !== 0 ? (e.readDepth && (n = `float depth = readDepth(UV);

	` + n), this.needsDepthTexture = this.getDepthTexture() === null) : this.needsDepthTexture = !1, e.colorSpace === V && (n += `color0 = sRGBToLinear(color0);
	`), e.uvTransformation ? (i = `vec2 transformedUv = vUv;
` + i, e.defines.set("UV", "transformedUv")) : e.defines.set("UV", "vUv"), e.shaderParts.set(_.FRAGMENT_HEAD, r), e.shaderParts.set(_.FRAGMENT_MAIN_IMAGE, n), e.shaderParts.set(_.FRAGMENT_MAIN_UV, i);
    for (const [o, l] of e.shaderParts)
      l !== null && e.shaderParts.set(o, l.trim().replace(/^#/, `
#`));
    this.skipRendering = t === 0, this.needsSwap = !this.skipRendering, this.fullscreenMaterial.setShaderData(e);
  }
  /**
   * Rebuilds the shader material.
   */
  recompile() {
    this.updateMaterial();
  }
  /**
   * Returns the current depth texture.
   *
   * @return {Texture} The current depth texture, or null if there is none.
   */
  getDepthTexture() {
    return this.fullscreenMaterial.depthBuffer;
  }
  /**
   * Sets the depth texture.
   *
   * @param {Texture} depthTexture - A depth texture.
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - The depth packing.
   */
  setDepthTexture(e, t = $) {
    this.fullscreenMaterial.depthBuffer = e, this.fullscreenMaterial.depthPacking = t;
    for (const r of this.effects)
      r.setDepthTexture(e, t);
  }
  /**
   * Renders the effect.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  afterRender(e, t, r, n, i, s) {
    if (!this.skipRendering || this.renderToScreen) {
      const o = this.fullscreenMaterial;
      o.inputBuffer = t.texture, o.time += n * this.timeScale, ve("EffectPass.finalRender"), e.setRenderTarget(this.renderToScreen ? null : r), e.render(this.scene, this.camera, {
        projectObject: !0,
        updateMatrixWorld: !1,
        useProgramCache: !0
      }), ge("EffectPass.finalRender");
    }
  }
  render(e, t, r, n, i, s, o = {}) {
    ve("EffectPass.render");
    for (const l of this.effects)
      ve(`EffectPass.${l.name}.update`), l.update(e, t, n, s, r), ge(`EffectPass.${l.name}.update`);
    this.afterRender(e, t, r, n, i, s), ge("EffectPass.render");
  }
  /**
   * Updates the size of this pass.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.fullscreenMaterial.setSize(e, t);
    for (const r of this.effects)
      r.setSize(e, t);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    this.renderer = e;
    for (const n of this.effects)
      n.initialize(e, t, r);
    this.updateMaterial(), r !== void 0 && r !== se && (this.fullscreenMaterial.defines.FRAMEBUFFER_PRECISION_HIGH = "1");
  }
  /**
   * Deletes disposable objects.
   */
  dispose() {
    super.dispose();
    for (const e of this.effects)
      e.removeEventListener("change", this.listener), e.removeEventListener("updateEffectPass", this.listener), e.dispose();
  }
  /**
   * Handles events.
   *
   * @param {Event} event - An event.
   */
  handleEvent(e) {
    switch (e.type) {
      case "change":
        this.recompile();
        break;
      case "updateEffectPass":
        const {
          renderer: t,
          inputBuffer: r,
          outputBuffer: n,
          deltaTime: i,
          stencilTest: s,
          depthPass: o
        } = e.map;
        this.afterRender(t, r, n, i, s, o);
        break;
    }
  }
}
class wd extends X {
  /**
   * Constructs a new G-Buffer pass.
   *
   * @param {Scene} scene - The scene to render.
   * @param {Camera} camera - The camera to use for rendering.
   * @param {Object} [options] - Additional options.
   * @param {Number} [options.resolutionScale=1.0] - The resolution scale.
   */
  constructor(e, t, { resolutionScale: r = 1 } = {}) {
    super("GBufferPass"), this.needsSwap = !1, this.needsDepthTexture = !1, this.scene = e, this.camera = t, this.resolutionScale = r, this.gBufferRenderTarget = null, this.clearPass = new De(), this.needsSwap = !1, this.gBufferMaterial = this.createGBufferMaterial();
  }
  /**
   * Creates the G-Buffer shader material.
   *
   * @returns {RawShaderMaterial} The G-Buffer material.
   * @private
   */
  createGBufferMaterial() {
    const e = (
      /* glsl */
      `
			in vec3 position;
			in vec3 normal;
			in vec2 uv;

			out vec3 vNormal;
			out vec3 vViewPosition;
			out vec2 vUv;
			out float vDepth;

			uniform mat4 modelViewMatrix;
			uniform mat4 projectionMatrix;
			uniform mat4 modelMatrix;
			uniform mat3 normalMatrix;
			uniform float cameraNear;
			uniform float cameraFar;

			void main() {
				vUv = uv;
				
				// 计算视图空间位置
				vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
				vViewPosition = mvPosition.xyz;
				
				// 计算世界空间法线
				vec3 transformedNormal = normalMatrix * normal;
				vNormal = normalize(transformedNormal);
				
				// 计算归一化深度 (0=near, 1=far)
				vDepth = (-mvPosition.z - cameraNear) / (cameraFar - cameraNear);
				vDepth = clamp(vDepth, 0.0, 1.0);
				
				gl_Position = projectionMatrix * mvPosition;
			}
		`
    ), t = (
      /* glsl */
      `
			precision highp float;
			precision highp int;

			// MRT 输出：同时输出到4个纹理
			layout(location = 0) out vec4 gColor;    // 颜色 + metalness (alpha)
			layout(location = 1) out vec4 gNormal;   // 法线 + roughness (alpha)
			layout(location = 2) out vec4 gDepth;    // 深度
			layout(location = 3) out vec4 gPosition; // 位置

			in vec3 vNormal;
			in vec3 vViewPosition;
			in vec2 vUv;
			in float vDepth;

			// 使用默认的材质属性
			uniform vec3 diffuse;
			uniform float metalness;
			uniform float roughness;

			void main() {
				// 基础颜色 - 使用uniform提供的默认值
				vec3 baseColor = diffuse;

				// 金属度
				float metallicValue = metalness;

				// 粗糙度  
				float roughnessValue = roughness;

				// 输出1：基础颜色 + metalness (在alpha通道)
				gColor = vec4(baseColor, metallicValue);
				
				// 输出2：视图空间法线 + roughness (在alpha通道)
				gNormal = vec4(normalize(vNormal) * 0.5 + 0.5, roughnessValue);
				
				// 输出3：归一化深度（只使用R通道）
				gDepth = vec4(vDepth, vDepth, vDepth, 1.0);
				
				// 输出4：视图空间位置
				gPosition = vec4(vViewPosition, 1.0);
			}
		`
    );
    return new vi({
      name: "G-Buffer Material",
      vertexShader: e,
      fragmentShader: t,
      uniforms: {
        // RawShaderMaterial需要手动提供所有uniform，包括Three.js内置uniform
        diffuse: { value: new y(1, 1, 1) },
        metalness: { value: 0 },
        roughness: { value: 1 },
        cameraNear: { value: 0.1 },
        cameraFar: { value: 100 },
        // Three.js内置uniform - RawShaderMaterial需要手动提供
        modelViewMatrix: { value: new C() },
        projectionMatrix: { value: new C() },
        modelMatrix: { value: new C() },
        normalMatrix: { value: new Ot() }
      },
      defines: {
        // 这些defines会根据材质属性自动设置
      },
      glslVersion: Te
    });
  }
  /**
   * Returns the G-Buffer textures.
   *
   * @returns {Object} An object containing the G-Buffer textures.
   */
  getGBufferTextures() {
    return this.gBufferRenderTarget ? {
      gColor: this.gBufferRenderTarget.textures[0],
      gNormal: this.gBufferRenderTarget.textures[1],
      gDepth: this.gBufferRenderTarget.textures[2],
      gPosition: this.gBufferRenderTarget.textures[3]
    } : null;
  }
  /**
   * 获取renderToScreen属性
   */
  get renderToScreen() {
    return super.renderToScreen;
  }
  /**
   * 设置renderToScreen属性
   */
  set renderToScreen(e) {
    super.renderToScreen = e, this.clearPass.renderToScreen = e;
  }
  /**
   * 准备G-Buffer材质用于override渲染
   * 使用更高效的方法，不需要修改场景中的所有材质
   *
   * @private
   */
  prepareGBufferMaterial() {
    this.gBufferMaterial.uniforms.cameraNear.value = this.camera.near, this.gBufferMaterial.uniforms.cameraFar.value = this.camera.far, this.gBufferMaterial.uniforms.projectionMatrix.value.copy(this.camera.projectionMatrix), this.gBufferMaterial.uniforms.diffuse.value.set(0.8, 0.8, 0.8), this.gBufferMaterial.uniforms.metalness.value = 0.1, this.gBufferMaterial.uniforms.roughness.value = 0.8, console.log("🎨 GBufferPass: 准备G-Buffer材质，使用RawShaderMaterial"), console.log("  - 相机near/far:", this.camera.near, this.camera.far), console.log("  - 投影矩阵已更新");
  }
  /**
   * Renders the G-Buffer.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i) {
    const s = e.getRenderTarget(), o = e.xr.enabled, l = e.shadowMap.autoUpdate, u = this.scene.overrideMaterial;
    this.scene.background, e.xr.enabled = !1, e.shadowMap.autoUpdate = !1;
    const f = this.renderToScreen ? null : r;
    this.clearPass.enabled && this.clearPass.render(e, r), e.setRenderTarget(f), e.render(this.scene, this.camera), this.prepareGBufferMaterial(), this.gBufferMaterial.uniformsNeedUpdate = !0, this.scene.overrideMaterial = this.gBufferMaterial, e.setRenderTarget(this.gBufferRenderTarget), e.clear(), e.render(this.scene, this.camera), this.scene.overrideMaterial = u, e.xr.enabled = o, e.shadowMap.autoUpdate = l, e.setRenderTarget(s);
  }
  /**
   * Updates the size of this pass.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = Math.max(1, Math.floor(e * this.resolutionScale)), n = Math.max(1, Math.floor(t * this.resolutionScale));
    this.gBufferRenderTarget && this.gBufferRenderTarget.dispose(), this.gBufferRenderTarget = new A(r, n, {
      count: 4,
      // 创建4个纹理附件
      minFilter: b,
      magFilter: b,
      type: Y
    }), this.gBufferRenderTarget.textures[0].name = "gColor", this.gBufferRenderTarget.textures[1].name = "gNormal", this.gBufferRenderTarget.textures[2].name = "gDepth", this.gBufferRenderTarget.textures[3].name = "gPosition";
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    if (!e.capabilities.isWebGL2) {
      console.warn("GBufferPass: WebGL 2.0 is required for Multiple Render Targets");
      return;
    }
    const n = e.getSize(new g());
    this.setSize(n.width, n.height);
  }
  /**
   * Disposes this pass.
   */
  dispose() {
    this.gBufferRenderTarget && (this.gBufferRenderTarget.dispose(), this.gBufferRenderTarget = null), this.gBufferMaterial.dispose(), this.clearPass.dispose();
  }
}
const Vc = `#ifdef FRAMEBUFFER_PRECISION_HIGH\r
\r
	uniform mediump sampler2D inputBuffer;\r
\r
#else\r
\r
	uniform lowp sampler2D inputBuffer;\r
\r
#endif\r
\r
uniform vec2 kernel[STEPS];\r
\r
varying vec2 vOffset;\r
varying vec2 vUv;\r
\r
void main() {\r
\r
	vec4 result = texture2D(inputBuffer, vUv) * kernel[0].y;\r
\r
	for(int i = 1; i < STEPS; ++i) {\r
\r
		vec2 offset = kernel[i].x * vOffset;\r
		vec4 c0 = texture2D(inputBuffer, vUv + offset);\r
		vec4 c1 = texture2D(inputBuffer, vUv - offset);\r
		result += (c0 + c1) * kernel[i].y;\r
\r
	}\r
\r
	gl_FragColor = result;\r
\r
	#include <colorspace_fragment>\r
\r
}\r
`, Hc = `uniform vec2 texelSize;\r
uniform vec2 direction;\r
uniform float scale;\r
\r
varying vec2 vOffset;\r
varying vec2 vUv;\r
\r
void main() {\r
\r
	vOffset = direction * texelSize * scale;\r
	vUv = position.xy * 0.5 + 0.5;\r
	gl_Position = vec4(position.xy, 1.0, 1.0);\r
\r
}\r
`;
class Gc extends N {
  /**
   * Constructs a new convolution material.
   *
   * @param {Object} [options] - The options.
   * @param {Number} [options.kernelSize=35] - The kernel size.
   */
  constructor({ kernelSize: e = 35 } = {}) {
    super({
      name: "GaussianBlurMaterial",
      uniforms: {
        inputBuffer: new a(null),
        texelSize: new a(new g()),
        direction: new a(new g()),
        kernel: new a(null),
        scale: new a(1)
      },
      blending: B,
      toneMapped: !1,
      depthWrite: !1,
      depthTest: !1,
      fragmentShader: Vc,
      vertexShader: Hc
    }), this._kernelSize = 0, this.kernelSize = e;
  }
  /**
   * The input buffer.
   *
   * @type {Texture}
   */
  set inputBuffer(e) {
    this.uniforms.inputBuffer.value = e;
  }
  /**
   * The kernel size.
   *
   * @type {Number}
   */
  get kernelSize() {
    return this._kernelSize;
  }
  set kernelSize(e) {
    this._kernelSize = e, this.generateKernel(e);
  }
  /**
   * The blur direction.
   *
   * @type {Vector2}
   */
  get direction() {
    return this.uniforms.direction.value;
  }
  /**
   * The blur kernel scale. Values greater than 1.0 may introduce artifacts.
   *
   * @type {Number}
   */
  get scale() {
    return this.uniforms.scale.value;
  }
  set scale(e) {
    this.uniforms.scale.value = e;
  }
  /**
   * Generates the Gauss kernel.
   *
   * @param {KernelSize} kernelSize - The kernel size. Should be an odd number.
   * @private
   */
  generateKernel(e) {
    const t = new Fs(e), r = t.linearSteps, n = new Float64Array(r * 2);
    for (let i = 0, s = 0; i < r; ++i)
      n[s++] = t.linearOffsets[i], n[s++] = t.linearWeights[i];
    this.uniforms.kernel.value = n, this.defines.STEPS = r.toFixed(0), this.needsUpdate = !0;
  }
  /**
   * Sets the size of this object.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    this.uniforms.texelSize.value.set(1 / e, 1 / t);
  }
}
class Td extends X {
  /**
   * Constructs a new Gaussian blur pass.
   *
   * @param {Object} [options] - The options.
   * @param {Number} [options.kernelSize=35] - The kernel size. Should be an odd number in the range [3, 1020].
   * @param {Number} [options.iterations=1] - The amount of times the blur should be applied.
   * @param {Number} [options.resolutionScale=1.0] - The resolution scale.
   * @param {Number} [options.resolutionX=Resolution.AUTO_SIZE] - The horizontal resolution.
   * @param {Number} [options.resolutionY=Resolution.AUTO_SIZE] - The vertical resolution.
   */
  constructor({
    kernelSize: e = 35,
    iterations: t = 1,
    resolutionScale: r = 1,
    resolutionX: n = W.AUTO_SIZE,
    resolutionY: i = W.AUTO_SIZE
  } = {}) {
    super("GaussianBlurPass"), this.renderTargetA = new A(1, 1, { depthBuffer: !1 }), this.renderTargetA.texture.name = "Blur.Target.A", this.renderTargetB = this.renderTargetA.clone(), this.renderTargetB.texture.name = "Blur.Target.B", this.blurMaterial = new Gc({ kernelSize: e }), this.copyMaterial = new dr(), this.copyMaterial.inputBuffer = this.renderTargetB.texture;
    const s = this.resolution = new W(this, n, i, r);
    s.addEventListener("change", (o) => this.setSize(s.baseWidth, s.baseHeight)), this.iterations = t;
  }
  /**
   * Renders the blur.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i) {
    const s = this.scene, o = this.camera, l = this.renderTargetA, u = this.renderTargetB, f = this.blurMaterial;
    this.fullscreenMaterial = f;
    let h = t;
    for (let m = 0, p = Math.max(this.iterations, 1); m < p; ++m)
      f.direction.set(1, 0), f.inputBuffer = h.texture, e.setRenderTarget(l), e.render(s, o), f.direction.set(0, 1), f.inputBuffer = l.texture, e.setRenderTarget(u), e.render(s, o), m === 0 && p > 1 && (h = u);
    this.fullscreenMaterial = this.copyMaterial, e.setRenderTarget(this.renderToScreen ? null : r), e.render(s, o);
  }
  /**
   * Updates the size of this pass.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.resolution;
    r.setBaseSize(e, t);
    const n = r.width, i = r.height;
    this.renderTargetA.setSize(n, i), this.renderTargetB.setSize(n, i), this.blurMaterial.setSize(e, t);
  }
  /**
   * Performs initialization tasks.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {Boolean} alpha - Whether the renderer uses the alpha channel or not.
   * @param {Number} frameBufferType - The type of the main frame buffers.
   */
  initialize(e, t, r) {
    r !== void 0 && (this.renderTargetA.texture.type = r, this.renderTargetB.texture.type = r, r !== se ? (this.blurMaterial.defines.FRAMEBUFFER_PRECISION_HIGH = "1", this.copyMaterial.defines.FRAMEBUFFER_PRECISION_HIGH = "1") : e !== null && e.outputColorSpace === V && (this.renderTargetA.texture.colorSpace = V, this.renderTargetB.texture.colorSpace = V));
  }
}
class bd extends X {
  /**
   * Constructs a new lambda pass.
   *
   * @param {Function} f - A function.
   */
  constructor(e) {
    super("LambdaPass", null, null), this.needsSwap = !1, this.f = e;
  }
  /**
   * Executes the function.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i) {
    this.f();
  }
}
class j extends X {
  isMRTRenderPass = !0;
  // 定义支持的通道类型常量
  static CHANNEL_COLOR = "color";
  // 颜色通道
  static CHANNEL_NORMAL = "normal";
  // 法线通道
  static CHANNEL_DEPTH = "depth";
  // 深度通道
  static CHANNEL_POSITION = "position";
  // 世界位置通道
  static CHANNEL_PBR = "pbr";
  // PBR属性通道(粗糙度/金属度/AO)
  static CHANNEL_ROUGHNESS = "roughness";
  // 粗糙度单独通道
  static CHANNEL_METALNESS = "metalness";
  // 金属度单独通道
  static CHANNEL_AO = "ao";
  // 环境光遮蔽单独通道
  static CHANNEL_MOTION = "motion";
  // 运动向量通道
  static CHANNEL_EMISSION = "emission";
  // 自发光通道
  static CHANNEL_ID = "id";
  // 对象ID通道
  static CHANNEL_MASK = "mask";
  // 遮罩通道
  static CHANNEL_SHADOW = "shadow";
  // 阴影通道
  static CHANNEL_VELOCITY = "velocity";
  // 速度通道(与motion类似但计算方式不同)
  static CHANNEL_CUSTOM = "custom";
  // 自定义通道
  static CHANNEL_ORIGINAL = "original";
  // 原始/正常画面通道 - 不使用特效着色器，直接显示物体的原始渲染
  // 默认通道配置
  static DEFAULT_CHANNELS = [
    j.CHANNEL_COLOR,
    j.CHANNEL_NORMAL,
    j.CHANNEL_DEPTH
  ];
  /**
      * 构造新的多渲染目标渲染通道
      *
      * @param {Scene} scene - 要渲染的场景
      * @param {Camera} camera - 用于渲染场景的相机
      * @param {Object} [options] - 渲染选项
      * @param {Number} [options.outputCount=2] - 输出渲染目标的数量
      * @param {Array} [options.channels] - 要生成的通道类型数组，如 ['color', 'normal', 'depth']
      * @param {Object} [options.formats] - 各个渲染目标的格式设置
      * @param {Material} [overrideMaterial=null] - 覆盖材质
      */
  constructor(e, t, r = {}, n = null) {
    super("MRTRenderPass", e, t), this.needsSwap = !1, this.channels = r.channels || j.DEFAULT_CHANNELS, this.outputCount = r.outputCount || Math.max(2, this.channels.length), this.formats = r.formats || null, this.renderTargets = null, this.clearPass = new De(), this.overrideMaterialManager = n === null ? null : new or(n), this.ignoreBackground = !1, this.skipShadowMapUpdate = !1, this.selection = null, this.buffers = [], this.channelMap = /* @__PURE__ */ new Map(), this.createMRTMaterial(), this.useMaterialPatching = !0, this.patchedMaterials = [], this.materialsPatched = !1, this.activeChannels = [];
  }
  set mainScene(e) {
    this.scene = e;
  }
  set mainCamera(e) {
    this.camera = e;
  }
  get renderToScreen() {
    return super.renderToScreen;
  }
  set renderToScreen(e) {
    super.renderToScreen = e, this.clearPass.renderToScreen = e;
  }
  /**
      * 当前覆盖材质
      *
      * @type {Material}
      */
  get overrideMaterial() {
    const e = this.overrideMaterialManager;
    return e !== null ? e.material : null;
  }
  set overrideMaterial(e) {
    const t = this.overrideMaterialManager;
    e !== null ? t !== null ? t.setMaterial(e) : this.overrideMaterialManager = new or(e) : t !== null && (t.dispose(), this.overrideMaterialManager = null);
  }
  /**
      * 是否应在渲染前清除目标缓冲区
      *
      * @type {Boolean}
      */
  get clear() {
    return this.clearPass.enabled;
  }
  set clear(e) {
    this.clearPass.enabled = e;
  }
  /**
      * 初始化多渲染目标
      *
      * @param {WebGLRenderer} renderer - 渲染器
      * @param {Number} width - 宽度
      * @param {Number} height - 高度
      */
  initRenderTargets(e, t, r) {
    this.renderTargets !== null && this.renderTargets.dispose();
    const n = e.getSize(new g()), i = e.getPixelRatio(), s = t || n.width * i, o = r || n.height * i;
    if (s <= 0 || o <= 0)
      return console.warn("MRTRenderPass: 尝试创建尺寸为零的渲染目标", s, o), null;
    const l = e.getContext(), u = l instanceof WebGL2RenderingContext, f = u ? l.getParameter(l.MAX_DRAW_BUFFERS) : 1;
    console.log(`WebGL信息: WebGL2=${u}, 最大绘制缓冲区=${f}`);
    const h = Math.min(this.outputCount, f);
    h < this.outputCount && (console.warn(`MRTRenderPass: 设备仅支持${f}个渲染目标，而不是请求的${this.outputCount}个`), this.outputCount = h), this.activeChannels.length !== this.outputCount && (console.warn(`MRTRenderPass: 活跃通道数量(${this.activeChannels.length})与输出数量(${this.outputCount})不匹配！`), this.activeChannels.length > 0 && (this.outputCount = this.activeChannels.length)), console.log(
      `MRTRenderPass: 创建${this.outputCount}个渲染目标, 当前活跃通道:`,
      this.activeChannels.map((m, p) => `${p}: ${m}`).join(", ")
    );
    try {
      this.renderTargets = new be(
        s,
        o,
        this.outputCount
      );
      for (let m = 0; m < this.outputCount; m++) {
        const p = this.renderTargets.texture[m];
        if (p.name = `MRTRenderPass.Target${m}`, this.formats && this.formats[m]) {
          const d = this.formats[m];
          d.format !== void 0 && (p.format = d.format), d.type !== void 0 && (p.type = d.type), d.colorSpace !== void 0 && (p.colorSpace = d.colorSpace), d.minFilter !== void 0 && (p.minFilter = d.minFilter), d.magFilter !== void 0 && (p.magFilter = d.magFilter);
        }
        !p.image || p.image.width <= 0 || p.image.height <= 0 ? console.warn(`MRTRenderPass: 纹理${m}初始化失败或尺寸为零`) : console.log(`MRTRenderPass: 纹理${m}初始化成功 (${p.image.width}x${p.image.height})`);
      }
      return this.renderTargets;
    } catch (m) {
      return console.error("MRTRenderPass: 初始化渲染目标时出错", m), null;
    }
  }
  /**
      * 获取渲染目标纹理
      *
      * @param {Number} index - 纹理索引
      * @returns {Texture} 指定索引的渲染目标纹理
      */
  getTexture(e = 0) {
    return this.renderTargets === null ? null : this.renderTargets.texture[e];
  }
  /**
      * 根据通道类型获取对应的渲染目标纹理
      *
      * @param {String} channelType - 通道类型，如 'color', 'normal', 'depth' 等
      * @returns {Texture} 对应通道类型的渲染目标纹理，如果未找到则返回null
      */
  getChannelTexture(e) {
    if (this.renderTargets === null || !this.channelMap.has(e))
      return null;
    const t = this.channelMap.get(e);
    return this.renderTargets.texture[t];
  }
  /**
      * 检查是否存在特定类型的通道
      *
      * @param {String} channelType - 要检查的通道类型
      * @returns {Boolean} 如果通道存在返回true，否则返回false
      */
  hasChannel(e) {
    return this.channelMap.has(e);
  }
  /**
      * 获取特定通道类型对应的索引
      *
      * @param {String} channelType - 通道类型
      * @returns {Number} 通道索引，如果未找到则返回-1
      */
  getChannelIndex(e) {
    return this.channelMap.has(e) ? this.channelMap.get(e) : -1;
  }
  /**
      * 获取所有可用的通道类型
      *
      * @returns {Array} 可用通道类型的数组
      */
  getAvailableChannels() {
    return [...this.channelMap.keys()];
  }
  /**
      * 创建多渲染目标(MRT)专用材质
      * 该材质会为每个渲染目标提供一个输出
      */
  createMRTMaterial() {
    let e = "", t = "";
    const r = Math.min(this.outputCount, 8);
    this.channelMap.clear();
    for (let n = 0; n < Math.min(this.channels.length, r); n++)
      this.channelMap.set(this.channels[n], n);
    for (let n = 0; n < r; n++)
      switch (e += `layout(location = ${n}) out vec4 gOutput${n};
`, n < this.channels.length ? this.channels[n] : null) {
        case j.CHANNEL_COLOR:
          t += `gOutput${n} = vec4(diffuseColor, 1.0);
`;
          break;
        case j.CHANNEL_NORMAL:
          t += `gOutput${n} = vec4(normalize(vNormal) * 0.5 + 0.5, 1.0);
`;
          break;
        case j.CHANNEL_DEPTH:
          t += `
                        // 计算到相机的距离
                        float depth = abs(vViewPosition.z);
                        // 应用非线性映射以增强近距离的变化
                        float mappedDepth = 1.0 - exp(-0.1 * depth);
                        gOutput${n} = vec4(vec3(mappedDepth), 1.0);
                    `;
          break;
        case j.CHANNEL_POSITION:
          t += `
                        // 将世界位置映射到合理颜色范围
                        vec3 worldPosNormalized = vWorldPosition * 0.1;
                        // 对每个分量进行循环映射，避免过大或过小的值
                        worldPosNormalized = fract(worldPosNormalized + 0.5);
                        gOutput${n} = vec4(worldPosNormalized, 1.0);
                    `;
          break;
        case j.CHANNEL_PBR:
          t += `
                        // 基于位置生成伪PBR属性
                        float pbr_roughness = fract(vWorldPosition.x * 0.5) * 0.8 + 0.2;
                        float pbr_metalness = fract(vWorldPosition.y * 0.5) * 0.8 + 0.2;
                        float pbr_ao = fract(vWorldPosition.z * 0.5) * 0.5 + 0.5;
                        gOutput${n} = vec4(pbr_roughness, pbr_metalness, pbr_ao, 1.0);
                    `;
          break;
        case j.CHANNEL_ROUGHNESS:
          t += `
                        // 基于位置生成粗糙度
                        float rough_value = fract(vWorldPosition.x * 0.5) * 0.8 + 0.2;
                        gOutput${n} = vec4(vec3(rough_value), 1.0);
                    `;
          break;
        case j.CHANNEL_METALNESS:
          t += `
                        // 基于位置生成金属度
                        float metal_value = fract(vWorldPosition.y * 0.5) * 0.8 + 0.2;
                        gOutput${n} = vec4(vec3(metal_value), 1.0);
                    `;
          break;
        case j.CHANNEL_AO:
          t += `
                        // 基于位置生成环境光遮蔽
                        float ao_value = fract(vWorldPosition.z * 0.5) * 0.5 + 0.5;
                        gOutput${n} = vec4(vec3(ao_value), 1.0);
                    `;
          break;
        case j.CHANNEL_MOTION:
          t += `
                        // 模拟运动向量 - 使用世界坐标的变化率作为运动方向
                        vec2 motion = vec2(
                            sin(vWorldPosition.x * 0.1 + time) * 0.5,
                            cos(vWorldPosition.y * 0.1 + time) * 0.5
                        ) * 0.1;
                        gOutput${n} = vec4(motion.x, motion.y, 0.0, 1.0);
                    `;
          break;
        case j.CHANNEL_EMISSION:
          t += `
                        // 基于UV坐标生成发光图案
                        float emissionPattern = 
                            max(0.0, 
                                sin(vUv.x * 20.0 + time) * sin(vUv.y * 20.0 + time) * 2.0
                            );
                        // 生成随机颜色发光
                        vec3 emissionColor = vBaseColor * emissionPattern * 2.0;
                        gOutput${n} = vec4(emissionColor, 1.0);
                    `;
          break;
        case j.CHANNEL_ID:
          t += `
                        // 使用世界位置的哈希作为伪对象ID
                        // 真实应用中应从uniform或顶点属性获取
                        float id = fract(
                            sin(dot(floor(vWorldPosition), vec3(12.9898, 78.233, 45.543))) * 43758.5453
                        );
                        // 使用不同颜色显示不同ID
                        vec3 idColor = vec3(id, fract(id * 13.0), fract(id * 27.0));
                        gOutput${n} = vec4(idColor, 1.0);
                    `;
          break;
        case j.CHANNEL_MASK:
          t += `
                        // 基于UV创建一个棋盘格图案作为遮罩示例
                        float mask = mod(floor(vUv.x * 10.0) + floor(vUv.y * 10.0), 2.0);
                        gOutput${n} = vec4(vec3(mask), 1.0);
                    `;
          break;
        case j.CHANNEL_SHADOW:
          t += `
                        // 使用简单的梯度作为阴影示例
                        // 真实应用中应获取实际阴影信息
                        float shadowValue = max(0.0, 1.0 - vViewPosition.z * 0.1);
                        gOutput${n} = vec4(vec3(shadowValue), 1.0);
                    `;
          break;
        case j.CHANNEL_VELOCITY:
          t += `
                        // 基于时间和位置计算的速度场
                        vec2 velocity = vec2(
                            sin(vWorldPosition.y * 0.2 + time * 2.0),
                            cos(vWorldPosition.x * 0.2 + time * 2.0)
                        ) * 0.15;
                        gOutput${n} = vec4(velocity.x, velocity.y, 0.0, 1.0);
                    `;
          break;
        case j.CHANNEL_CUSTOM:
          t += `
                        // 示例自定义输出 - 使用时间变化的花纹
                        vec3 customColor = vec3(
                            0.5 + 0.5 * sin(vUv.x * 20.0 + time),
                            0.5 + 0.5 * sin(vUv.y * 20.0 + time * 0.7),
                            0.5 + 0.5 * sin((vUv.x + vUv.y) * 10.0 + time * 1.3)
                        );
                        gOutput${n} = vec4(customColor, 1.0);
                    `;
          break;
        case j.CHANNEL_ORIGINAL:
          t += `
                       vec3 customColor = vec3(
                            0.5 + 0.5 * sin(vUv.x * 20.0 + time),
                            0.5 + 0.5 * sin(vUv.y * 20.0 + time * 0.7),
                            0.5 + 0.5 * sin((vUv.x + vUv.y) * 10.0 + time * 1.3)
                        );
                        gOutput${n} = vec4(customColor, 1.0);
                    `;
          break;
        default:
          t += `gOutput${n} = vec4(0.5, 0.5, 0.5, 1.0);
`;
          break;
      }
    this.mrtMaterial = new N({
      uniforms: {
        // 可以在这里添加额外的uniform
        time: { value: 0 }
      },
      vertexShader: `
                out vec3 vNormal;
                out vec2 vUv;
                out vec3 vViewPosition;
                out vec3 vWorldPosition;
                // 使用自定义属性传递颜色信息
                out vec3 vBaseColor;
                
                void main() {
                    vUv = uv;
                    vNormal = normalMatrix * normal;
                    
                    // 计算世界空间位置
                    vec4 worldPosition = modelMatrix * vec4(position, 1.0);
                    vWorldPosition = worldPosition.xyz;
                    
                    // 计算视图空间位置
                    vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
                    vViewPosition = mvPosition.xyz;
                    
                    // 生成一个基于位置的颜色 (在没有实际颜色输入时的替代方案)
                    // 这样每个物体都会根据其位置有不同的颜色
                    vBaseColor = normalize(abs(worldPosition.xyz)) * 0.5 + 0.5;
                    
                    // 最终投影
                    gl_Position = projectionMatrix * mvPosition;
                }
            `,
      fragmentShader: `
                precision highp float;
                precision highp int;
                
                ${e}
                
                // 来自顶点着色器的变量
                in vec3 vNormal;
                in vec2 vUv;
                in vec3 vViewPosition;
                in vec3 vWorldPosition;
                in vec3 vBaseColor;
                
                // Uniform变量
                uniform float time;
                uniform sampler2D tDiffuse;
                // 计算颜色 - 这里使用多种方法来确保颜色可见
                vec3 getDiffuseColor() {
                    // 基础颜色（来自顶点着色器）
                    vec3 baseColor = vBaseColor;
                    
                    // 调整颜色 - 增加饱和度
                    vec3 enhancedColor = baseColor;
                    
                    // 使用UV坐标添加变化
                    vec3 uvColor = vec3(
                        fract(vUv.x * 5.0),
                        fract(vUv.y * 5.0),
                        fract((vUv.x + vUv.y) * 2.5)
                    );
                    
                    // 混合颜色源以创建更鲜艳的结果
                    return mix(enhancedColor, uvColor, 0.3) * 0.8 + 0.2;
                }
                
                void main() {
                    // 获取漫反射颜色
                    vec3 diffuseColor = getDiffuseColor();
                    
                    // 为每个渲染目标提供输出
                    ${t}
                }
            `,
      glslVersion: Te,
      side: hr,
      blending: B
    });
  }
  /**
      * 修补场景中所有支持的材质，添加MRT输出
      * 这允许我们获取真实的材质属性，如金属度、粗糙度等
      *
      * @returns {Number} 被修改的材质数量
      */
  patchMaterials() {
    if (this.materialsPatched)
      return 0;
    this.patchedMaterials = [];
    const e = this.channels.slice(), t = e.includes(j.CHANNEL_ROUGHNESS) || e.includes(j.CHANNEL_PBR), r = e.includes(j.CHANNEL_METALNESS) || e.includes(j.CHANNEL_PBR), n = e.includes(j.CHANNEL_EMISSION);
    if (!t && !r && !n)
      return 0;
    const i = [];
    i.push(j.CHANNEL_COLOR), e.includes(j.CHANNEL_NORMAL) && i.push(j.CHANNEL_NORMAL), e.includes(j.CHANNEL_DEPTH) && i.push(j.CHANNEL_DEPTH), e.includes(j.CHANNEL_POSITION) && i.push(j.CHANNEL_POSITION), e.includes(j.CHANNEL_PBR) ? i.push(j.CHANNEL_PBR) : (t && i.push(j.CHANNEL_ROUGHNESS), r && i.push(j.CHANNEL_METALNESS)), n && i.push(j.CHANNEL_EMISSION), this.activeChannels = i, this.outputCount = i.length, console.log(`设置MRT输出数量: ${this.outputCount}`), i.forEach((p, d) => {
    });
    const s = (
      /* glsl */
      `
            varying vec2 vUv;
            varying vec3 vNormal;
            varying vec3 vViewPosition;
            varying vec3 vWorldPosition;
            
            void main() {
                vUv = uv;
                vec4 worldPosition = modelMatrix * vec4(position, 1.0);
                vWorldPosition = worldPosition.xyz;
                
                vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
                vViewPosition = -mvPosition.xyz;
                
                vNormal = normalMatrix * normal;
                gl_Position = projectionMatrix * mvPosition;
            }
        `
    ), o = (
      /* glsl */
      `
            precision highp float;
            precision highp int;
            
            // 输出变量声明
        `
    );
    let l = "";
    for (let p = 0; p < i.length; p++)
      l += `layout(location = ${p}) out vec4 gOutput${p};
`;
    const u = (
      /* glsl */
      `
            varying vec2 vUv;
            varying vec3 vNormal;
            varying vec3 vViewPosition;
            varying vec3 vWorldPosition;
            
            // 材质属性从uniforms获取
            uniform vec3 diffuseColor;
            uniform float roughness;
            uniform float metalness;
            uniform vec3 emissive;
            uniform float opacity;
            
            // 纹理uniforms
            uniform sampler2D map;
            uniform sampler2D normalMap;
            uniform sampler2D roughnessMap;
            uniform sampler2D metalnessMap;
            uniform sampler2D emissiveMap;
            uniform sampler2D aoMap;
            
            // 控制标志
            uniform bool useMap;
            uniform bool useNormalMap;
            uniform bool useRoughnessMap;
            uniform bool useMetalnessMap;
            uniform bool useEmissiveMap;
            uniform bool useAOMap;
        `
    );
    let f = (
      /* glsl */
      `
            void main() {
                // 基础颜色处理
                vec4 texelColor = vec4(1.0);
                if(useMap) {
                    texelColor = texture(map, vUv);
                }
                vec4 baseColor = vec4(diffuseColor, opacity) * texelColor;
                
                // 计算粗糙度
                float roughnessValue = roughness;
                if(useRoughnessMap) {
                    roughnessValue *= texture(roughnessMap, vUv).r;
                }
                
                // 计算金属度
                float metalnessValue = metalness;
                if(useMetalnessMap) {
                    metalnessValue *= texture(metalnessMap, vUv).r;
                }
                
                // 计算环境光遮蔽
                float aoValue = 1.0;
                if(useAOMap) {
                    aoValue = texture(aoMap, vUv).r;
                }
                
                // 计算自发光
                vec3 emissiveValue = emissive;
                if(useEmissiveMap) {
                    emissiveValue *= texture(emissiveMap, vUv).rgb;
                }
                
                // 计算法线信息
                vec3 normalVector = normalize(vNormal);
                
                // 输出到各个渲染目标
        `
    );
    for (let p = 0; p < i.length; p++) {
      const d = i[p];
      d === j.CHANNEL_COLOR ? f += `
                // 输出颜色
                gOutput${p} = baseColor;
                ` : d === j.CHANNEL_NORMAL ? f += `
                // 输出法线
                gOutput${p} = vec4(normalVector * 0.5 + 0.5, 1.0);
                ` : d === j.CHANNEL_DEPTH ? f += `
                // 输出深度
                float depth = abs(vViewPosition.z);
                float mappedDepth = 1.0 - exp(-0.1 * depth);
                gOutput${p} = vec4(vec3(mappedDepth), 1.0);
                ` : d === j.CHANNEL_POSITION ? f += `
                // 输出世界位置
                vec3 worldPosNormalized = vWorldPosition * 0.1;
                worldPosNormalized = fract(worldPosNormalized + 0.5);
                gOutput${p} = vec4(worldPosNormalized, 1.0);
                ` : d === j.CHANNEL_ROUGHNESS ? f += `
                // 输出真实粗糙度
                gOutput${p} = vec4(vec3(roughnessValue), 1.0);
                ` : d === j.CHANNEL_METALNESS ? f += `
                // 输出真实金属度
                gOutput${p} = vec4(vec3(metalnessValue), 1.0);
                ` : d === j.CHANNEL_PBR ? f += `
                // 输出真实PBR属性 (R=粗糙度, G=金属度, B=AO)
                gOutput${p} = vec4(roughnessValue, metalnessValue, aoValue, 1.0);
                ` : d === j.CHANNEL_EMISSION ? f += `
                // 输出真实自发光
                gOutput${p} = vec4(emissiveValue, 1.0);
                ` : f += `
                // 默认输出
                gOutput${p} = vec4(0.5, 0.5, 0.5, 1.0);
                `;
    }
    f += `
            }
        `;
    const h = o + l + u + f;
    console.log("MRT通道配置:", i.map((p, d) => `${d}: ${p}`).join(", ")), console.log("MRT输出数量:", i.length), console.log("着色器输出声明数量:", l.split("layout(location").length - 1);
    let m = 0;
    return this.scene.traverse((p) => {
      if (!p.isMesh || !p.material)
        return;
      const d = Array.isArray(p.material) ? p.material : [p.material], v = [];
      d.forEach((x, w) => {
        if (!x.isMeshStandardMaterial && !x.isMeshPhysicalMaterial) {
          v.push(null);
          return;
        }
        try {
          const M = new N({
            vertexShader: s,
            fragmentShader: h,
            glslVersion: Te,
            uniforms: {
              // 基础属性
              diffuseColor: { value: x.color ? x.color.clone() : new Color(1, 1, 1) },
              roughness: { value: x.roughness !== void 0 ? x.roughness : 0.5 },
              metalness: { value: x.metalness !== void 0 ? x.metalness : 0 },
              emissive: { value: x.emissive ? x.emissive.clone() : new Color(0, 0, 0) },
              opacity: { value: x.opacity !== void 0 ? x.opacity : 1 },
              // 纹理
              map: { value: x.map || null },
              normalMap: { value: x.normalMap || null },
              roughnessMap: { value: x.roughnessMap || null },
              metalnessMap: { value: x.metalnessMap || null },
              emissiveMap: { value: x.emissiveMap || null },
              aoMap: { value: x.aoMap || null },
              // 控制标志
              useMap: { value: !!x.map },
              useNormalMap: { value: !!x.normalMap },
              useRoughnessMap: { value: !!x.roughnessMap },
              useMetalnessMap: { value: !!x.metalnessMap },
              useEmissiveMap: { value: !!x.emissiveMap },
              useAOMap: { value: !!x.aoMap }
            },
            transparent: x.transparent,
            side: x.side
          });
          v.push(M), m++;
        } catch (M) {
          console.error("创建MRT材质失败:", M), v.push(null);
        }
      }), v.some((x) => x !== null) && this.patchedMaterials.push({
        object: p,
        originalMaterials: d.slice(),
        mrtMaterials: v
      });
    }), this.renderTargets !== null && (this.renderTargets.dispose(), this.renderTargets = null), this.channelMap.clear(), i.forEach((p, d) => {
      this.channelMap.set(p, d);
    }), this.materialsPatched = !0, m;
  }
  /**
      * 恢复所有被修补的材质到原始状态
      *
      * @returns {Number} 被恢复的材质数量
      */
  restoreMaterials() {
    if (!this.materialsPatched)
      return 0;
    let e = 0;
    return this.patchedMaterials.forEach(({ object: t, originalMaterials: r }) => {
      t && ((Array.isArray(r) || r) && (t.material = r), e++);
    }), this.patchedMaterials = [], this.materialsPatched = !1, e;
  }
  /**
      * 应用MRT材质到场景中的对象
      * 在渲染前调用
      *
      * @private
      */
  _applyMRTMaterials() {
    this.patchedMaterials.forEach(({ object: e, mrtMaterials: t }) => {
      if (e)
        if (Array.isArray(t)) {
          const r = t.map(
            (n, i) => n !== null ? n : e.material[i]
          );
          e.material = r;
        } else t && (e.material = t);
    });
  }
  /**
      * 恢复原始材质到场景中的对象
      * 在渲染后调用
      *
      * @private
      */
  _restoreOriginalMaterials() {
    this.patchedMaterials.forEach(({ object: e, originalMaterials: t }) => {
      e && (e.material = t);
    });
  }
  /**
      * 渲染场景到多个渲染目标
      *
      * @param {WebGLRenderer} renderer - 渲染器
      * @param {WebGLRenderTarget} inputBuffer - 包含上一个通道结果的帧缓冲区
      * @param {WebGLRenderTarget} outputBuffer - 作为输出渲染目标的帧缓冲区，除非此通道渲染到屏幕
      * @param {Number} [deltaTime] - 上一帧和当前帧之间的时间（秒）
      * @param {Boolean} [stencilTest] - 指示模板蒙版是否处于活动状态
      */
  render(e, t, r, n, i) {
    ve("MRTRenderPass.render");
    try {
      const s = this.scene, o = this.camera, l = this.selection, u = o.layers.mask, f = s.background, h = e.shadowMap.autoUpdate;
      if (this.useMaterialPatching && !this.materialsPatched) {
        const d = this.patchMaterials();
        d > 0 && console.log(`MRTRenderPass: 为${d}个材质创建了MRT支持`), this.renderTargets = null;
      }
      if ((this.renderTargets === null || this.renderTargets.width <= 0 || this.renderTargets.height <= 0) && this.initRenderTargets(e), this.renderTargets === null) {
        console.error("MRTRenderPass: 渲染目标初始化失败");
        return;
      }
      this.renderTargets.texture.length !== this.outputCount && console.error(`MRTRenderPass: 渲染目标数量(${this.renderTargets.texture.length})与输出数量(${this.outputCount})不匹配！`);
      let m = "无子对象";
      s && s.children && s.children.length > 0 && (m = s.children[0].constructor.name), bi(`MRTRenderPass 渲染场景, 第一个子对象类型: ${m}, 渲染目标数量: ${this.renderTargets.texture.length}`), this.mrtMaterial && this.mrtMaterial.uniforms && this.mrtMaterial.uniforms.time && (this.mrtMaterial.uniforms.time.value = performance.now() * 1e-3), l !== null && o.layers.set(l.getLayer()), this.skipShadowMapUpdate && (e.shadowMap.autoUpdate = !1), (this.ignoreBackground || this.clearPass.overrideClearColor !== null) && (s.background = null), this.clearPass.enabled && this.clearPass.render(e, this.renderTargets), e.setRenderTarget(this.renderTargets);
      const p = s.overrideMaterial;
      this.useMaterialPatching ? (this._applyMRTMaterials(), s.overrideMaterial = null) : s.overrideMaterial = this.mrtMaterial, console.log(`MRTRenderPass: 开始渲染, 输出数量: ${this.outputCount}, 材质已修补: ${this.materialsPatched}, 使用材质修补: ${this.useMaterialPatching}`);
      try {
        e.render(s, o), console.log("MRTRenderPass: 渲染成功");
      } catch (d) {
        console.error("MRTRenderPass: 渲染错误:", d);
      }
      this.useMaterialPatching && this._restoreOriginalMaterials(), s.overrideMaterial = p, this.renderToScreen, o.layers.mask = u, s.background = f, e.shadowMap.autoUpdate = h, this.buffers = [];
      for (let d = 0; d < this.outputCount; d++)
        this.buffers.push(this.renderTargets.texture[d]);
    } catch (s) {
      console.error("MRTRenderPass.render 执行错误:", s);
    }
    ge("MRTRenderPass.render");
  }
  /**
      * 更新通道的大小
      *
      * @param {Number} width - 宽度
      * @param {Number} height - 高度
      */
  setSize(e, t) {
    this.renderTargets !== null && this.renderTargets.setSize(e, t), this.clearPass.setSize(e, t);
  }
  /**
      * 释放资源
      */
  dispose() {
    this.materialsPatched && this.restoreMaterials(), super.dispose(), this.renderTargets !== null && this.renderTargets.dispose(), this.overrideMaterialManager !== null && this.overrideMaterialManager.dispose(), this.mrtMaterial !== null && this.mrtMaterial !== void 0 && this.mrtMaterial.dispose(), this.clearPass.dispose();
  }
}
class mr extends X {
  /**
   * Constructs a new normal pass.
   *
   * @param {Scene} scene - The scene to render.
   * @param {Camera} camera - The camera to use to render the scene.
   * @param {Object} [options] - The options.
   * @param {WebGLRenderTarget} [options.renderTarget] - A custom render target.
   * @param {Number} [options.resolutionScale=1.0] - The resolution scale.
   * @param {Number} [options.resolutionX=Resolution.AUTO_SIZE] - The horizontal resolution.
   * @param {Number} [options.resolutionY=Resolution.AUTO_SIZE] - The vertical resolution.
   * @param {Number} [options.width=Resolution.AUTO_SIZE] - Deprecated. Use resolutionX instead.
   * @param {Number} [options.height=Resolution.AUTO_SIZE] - Deprecated. Use resolutionY instead.
   */
  constructor(e, t, {
    renderTarget: r,
    resolutionScale: n = 1,
    width: i = W.AUTO_SIZE,
    height: s = W.AUTO_SIZE,
    resolutionX: o = i,
    resolutionY: l = s
  } = {}) {
    super("NormalPass"), this.needsSwap = !1, this.renderPass = new pe(e, t, new Kr());
    const u = this.renderPass;
    u.ignoreBackground = !0, u.skipShadowMapUpdate = !0;
    const f = u.getClearPass();
    f.overrideClearColor = new y(7829503), f.overrideClearAlpha = 1, this.renderTarget = r, this.renderTarget === void 0 && (this.renderTarget = new A(1, 1, {
      minFilter: b,
      magFilter: b
    }), this.renderTarget.texture.name = "NormalPass.Target");
    const h = this.resolution = new W(this, o, l, n);
    h.addEventListener("change", (m) => this.setSize(h.baseWidth, h.baseHeight));
  }
  set mainScene(e) {
    this.renderPass.mainScene = e;
  }
  set mainCamera(e) {
    this.renderPass.mainCamera = e;
  }
  /**
   * The normal texture.
   *
   * @type {Texture}
   */
  get texture() {
    return this.renderTarget.texture;
  }
  /**
   * The normal texture.
   *
   * @deprecated Use texture instead.
   * @return {Texture} The texture.
   */
  getTexture() {
    return this.renderTarget.texture;
  }
  /**
   * Returns the resolution settings.
   *
   * @deprecated Use resolution instead.
   * @return {Resolution} The resolution.
   */
  getResolution() {
    return this.resolution;
  }
  /**
   * Returns the current resolution scale.
   *
   * @return {Number} The resolution scale.
   * @deprecated Use resolution.preferredWidth or resolution.preferredHeight instead.
   */
  getResolutionScale() {
    return this.resolution.scale;
  }
  /**
   * Sets the resolution scale.
   *
   * @param {Number} scale - The new resolution scale.
   * @deprecated Use resolution.preferredWidth or resolution.preferredHeight instead.
   */
  setResolutionScale(e) {
    this.resolution.scale = e;
  }
  /**
   * Renders the scene normals.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   * @param {WebGLRenderTarget} inputBuffer - A frame buffer that contains the result of the previous pass.
   * @param {WebGLRenderTarget} outputBuffer - A frame buffer that serves as the output render target unless this pass renders to screen.
   * @param {Number} [deltaTime] - The time between the last frame and the current one in seconds.
   * @param {Boolean} [stencilTest] - Indicates whether a stencil mask is active.
   */
  render(e, t, r, n, i) {
    const s = this.renderToScreen ? null : this.renderTarget;
    this.renderPass.render(e, s, s);
  }
  /**
   * Updates the size of this pass.
   *
   * @param {Number} width - The width.
   * @param {Number} height - The height.
   */
  setSize(e, t) {
    const r = this.resolution;
    r.setBaseSize(e, t), this.renderTarget.setSize(r.width, r.height);
  }
}
class Dd extends X {
  /**
      * 构造一个新的Three.js Pass兼容层
      *
      * @param {Object} threePass - Three.js原生的Pass实例
      * @param {String} [name="ThreeCompatPass"] - Pass的名称
      */
  constructor(e, t = "ThreeCompatPass") {
    super(t), this.threePass = e, this.enabled = e.enabled, this.needsSwap = e.needsSwap, this.renderToScreen = !0;
  }
  /**
      * 设置渲染到屏幕的标志
      *
      * @type {Boolean}
      */
  set renderToScreen(e) {
    super.renderToScreen = e, this.threePass && (this.threePass.renderToScreen = e);
  }
  get renderToScreen() {
    return super.renderToScreen;
  }
  /**
      * 设置尺寸
      *
      * @param {Number} width - 宽度
      * @param {Number} height - 高度
      */
  setSize(e, t) {
    this.threePass && typeof this.threePass.setSize == "function" && this.threePass.setSize(e, t);
  }
  /**
      * 初始化Pass
      *
      * @param {WebGLRenderer} renderer - 渲染器
      * @param {Boolean} alpha - 渲染器是否使用alpha通道
      * @param {Number} frameBufferType - 主帧缓冲区的类型
      */
  initialize(e, t, r) {
    this.threePass && typeof this.threePass.setRenderer == "function" && this.threePass.setRenderer(e);
  }
  /**
      * 设置深度纹理
      *
      * @param {Texture} depthTexture - 深度纹理
      * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - 深度打包方式
      */
  setDepthTexture(e, t = $) {
  }
  /**
      * 渲染Pass
      *
      * @param {WebGLRenderer} renderer - 渲染器
      * @param {WebGLRenderTarget} inputBuffer - 包含上一个Pass结果的帧缓冲区
      * @param {WebGLRenderTarget} outputBuffer - 作为输出的渲染目标，除非此Pass直接渲染到屏幕
      * @param {Number} [deltaTime] - 上一帧与当前帧之间的时间差（秒）
      * @param {Boolean} [stencilTest] - 指示模板测试是否处于活动状态
      * @param {DepthPass} [depthPass] - 可选的共享深度Pass，用于优化多个效果
      */
  render(e, t, r, n, i, s) {
    const o = e.getRenderTarget(), l = this.renderToScreen ? null : r, u = t, f = i || !1;
    this.threePass.beautyRenderTarget = t, this.threePass.renderToScreen = this.renderToScreen, this.threePass.render(e, l, u, n, f), e.setRenderTarget(o);
  }
  /**
      * 销毁Pass
      */
  dispose() {
    this.threePass && typeof this.threePass.dispose == "function" && this.threePass.dispose(), super.dispose();
  }
}
class Pd extends X {
  /**
   * 构造一个新的Three.js Pass兼容层
   *
   * @param {Object} threePass - Three.js原生的Pass实例
   * @param {String} [name="EnhancedThreeCompatPass"] - Pass的名称
   * @param {String} [passType="generic"] - Pass的类型，用于特殊处理（可选值："ssr", "bloom", "generic"）
   */
  effects = [];
  constructor(e, t = "EnhancedThreeCompatPass", r = "generic") {
    super(t), this.threePass = e, this.effects.push(e), this.passType = r, this.enabled = e.enabled, this.needsSwap = e.needsSwap, this.renderToScreen = !1, this.debug = !1;
  }
  /**
   * 设置渲染到屏幕的标志
   *
   * @type {Boolean}
   */
  set renderToScreen(e) {
    super.renderToScreen = e, this.threePass && (this.threePass.renderToScreen = e);
  }
  get renderToScreen() {
    return super.renderToScreen;
  }
  /**
   * 设置调试模式
   *
   * @param {Boolean} enabled - 是否启用调试
   */
  setDebug(e) {
    this.debug = e;
  }
  /**
   * 设置尺寸
   *
   * @param {Number} width - 宽度
   * @param {Number} height - 高度
   */
  setSize(e, t) {
    this.threePass.setSize(e, t);
  }
  /**
   * 初始化Pass
   *
   * @param {WebGLRenderer} renderer - 渲染器
   * @param {Boolean} alpha - 渲染器是否使用alpha通道
   * @param {Number} frameBufferType - 主帧缓冲区的类型
   */
  initialize(e, t, r) {
    this.threePass && typeof this.threePass.setRenderer == "function" && this.threePass.setRenderer(e);
  }
  /**
   * 设置深度纹理
   *
   * @param {Texture} depthTexture - 深度纹理
   * @param {DepthPackingStrategies} [depthPacking=BasicDepthPacking] - 深度打包方式
   */
  setDepthTexture(e, t = $) {
    this.threePass && typeof this.threePass.setDepthTexture == "function" ? this.threePass.setDepthTexture(e, t) : this.passType === "ssr" && this.threePass && (this.threePass.setDepthTexture || (console.warn("SSRPass没有setDepthTexture方法，将尝试直接设置深度纹理"), this.threePass.ssrMaterial && this.threePass.ssrMaterial.uniforms.tDepth && (this.threePass.ssrMaterial.uniforms.tDepth.value = e), this.threePass.depthRenderMaterial && this.threePass.depthRenderMaterial.uniforms.tDepth && (this.threePass.depthRenderMaterial.uniforms.tDepth.value = e)));
  }
  /**
   * 根据Pass类型处理输入/输出缓冲区
   *
   * @private
   * @param {WebGLRenderer} renderer - 渲染器
   * @param {WebGLRenderTarget} inputBuffer - 输入缓冲区
   * @param {WebGLRenderTarget} outputBuffer - 输出缓冲区
   */
  setupBuffers(e, t, r) {
    switch (this.passType) {
      case "ssr":
        if (this.threePass.beautyRenderTarget) {
          const n = this.threePass.beautyRenderTarget.depthTexture;
          this.threePass.beautyRenderTarget, this.threePass.beautyRenderTarget = t, this.threePass.ssrMaterial && (t.depthTexture ? this.threePass.ssrMaterial.uniforms.tDepth.value = t.depthTexture : n && (this.threePass.ssrMaterial.uniforms.tDepth.value = n));
        }
        break;
      case "bloom":
        break;
      default:
        this.threePass.uniforms && this.threePass.uniforms.tDiffuse && (this.threePass.uniforms.tDiffuse.value = t.texture);
        break;
    }
  }
  /**
   * 渲染Pass
   *
   * @param {WebGLRenderer} renderer - 渲染器
   * @param {WebGLRenderTarget} inputBuffer - 包含上一个Pass结果的帧缓冲区
   * @param {WebGLRenderTarget} outputBuffer - 作为输出的渲染目标，除非此Pass直接渲染到屏幕
   * @param {Number} [deltaTime] - 上一帧与当前帧之间的时间差（秒）
   * @param {Boolean} [stencilTest] - 指示模板测试是否处于活动状态
   * @param {DepthPass} [depthPass] - 可选的共享深度Pass，用于优化多个效果
   */
  render(e, t, r, n, i, s, o) {
    if (!this.enabled) {
      e.setRenderTarget(this.renderToScreen ? null : r), e.copyFramebufferToTexture(0, r.texture);
      return;
    }
    const l = e.getRenderTarget();
    this.setupBuffers(e, t, r);
    const u = this.renderToScreen ? null : r, f = t, h = i || !1;
    this.threePass.renderToScreen = this.renderToScreen;
    try {
      this.threePass.render(e, u, f, n, h, s, o);
    } catch (m) {
      console.error(`渲染 ${this.name} 时出错:`, m);
    }
    e.setRenderTarget(l);
  }
  /**
   * 销毁Pass
   */
  dispose() {
    this.threePass && typeof this.threePass.dispose == "function" && this.threePass.dispose(), super.dispose();
  }
}
const jn = /* @__PURE__ */ new Ss(2, 2);
class Cd extends X {
  /**
   * @param {Scene} scene - 主场景。
   * @param {Camera} camera - 主相机。
   * @param {Object} [options.effect] - 传给 UnrealBloomEffect 的参数。
   * @param {Object} [options.debug] - 调试视窗相关配置。
   * @param {Boolean} [options.debug.showSelection=false] - 默认是否显示调试小窗。
   * @param {Number} [options.debug.inset=0.28] - 调试小窗相对宽度（相对于主画面宽度的比例）。
   * @param {Number} [options.debug.padding=18] - 调试小窗距离屏幕边缘的像素间距。
   * @param {Number} [options.debug.minWidth=160] - 调试小窗最小宽度，像素。
   */
  constructor(e, t, {
    effect: r = {},
    debug: {
      showSelection: n = !1,
      inset: i = 0.28,
      padding: s = 18,
      minWidth: o = 160
    } = {}
  } = {}) {
    super("SelectiveBloomPass"), this.effect = new me(r), this.effect.mainScene = e, this.effect.mainCamera = t, this.effectPass = new kc(t, this.effect), this.overlayEnabled = n, this.overlayInset = i, this.overlayPadding = s, this.overlayMinWidth = o, this.bufferWidth = 1, this.bufferHeight = 1, this.selectionViewport = {
      x: 0,
      y: 0,
      width: 0,
      height: 0
    }, this.overlayScene = new zt(), this.overlayScene.background = null, this.overlayCamera = new Nr(-1, 1, 1, -1, 0, 1), this.overlayMaterial = new gt({
      map: this.effect.selectionTexture,
      toneMapped: !1,
      depthTest: !1,
      depthWrite: !1
    }), this.overlayQuad = new Ft(jn, this.overlayMaterial), this.overlayScene.add(this.overlayQuad), this.selectionDisplayScene = new zt(), this.selectionDisplayCamera = new Nr(-1, 1, 1, -1, 0, 1), this.selectionDisplayMaterial = new gt({
      map: null,
      toneMapped: !1,
      depthTest: !1,
      depthWrite: !1
    }), this.selectionDisplayQuad = new Ft(jn, this.selectionDisplayMaterial), this.selectionDisplayScene.add(this.selectionDisplayQuad);
  }
  set renderToScreen(e) {
    super.renderToScreen = e, this.effectPass.renderToScreen = e;
  }
  get renderToScreen() {
    return super.renderToScreen;
  }
  set overlayVisible(e) {
    this.overlayEnabled = e;
  }
  get overlayVisible() {
    return this.overlayEnabled;
  }
  set mainScene(e) {
    this.effectPass.mainScene = e;
  }
  set mainCamera(e) {
    this.effectPass.mainCamera = e;
  }
  get selection() {
    return this.effect.getSelection();
  }
  get selectionTexture() {
    return this.effect.selectionTexture;
  }
  get selectionRenderTarget() {
    return this.effect.selectionRenderTarget;
  }
  render(e, t, r, n, i, s, o = {}) {
    if (this.effect.output === me.OUTPUT.Selection) {
      this.effect.update(e, t, n, s, r);
      const l = this.effect.selectionRenderTarget;
      if (!l || l.texture === null)
        return;
      this.selectionDisplayMaterial.map !== l.texture && (this.selectionDisplayMaterial.map = l.texture, this.selectionDisplayMaterial.needsUpdate = !0);
      const u = e.getRenderTarget(), f = e.getViewport(new ze()), h = e.getScissor(new ze());
      e.setRenderTarget(this.renderToScreen ? null : r), e.setViewport(0, 0, this.bufferWidth, this.bufferHeight), e.setScissor(0, 0, this.bufferWidth, this.bufferHeight), e.render(this.selectionDisplayScene, this.selectionDisplayCamera, {
        projectObject: !0,
        updateMatrixWorld: !1,
        useProgramCache: !1
      }), e.setViewport(f.x, f.y, f.z, f.w), e.setScissor(h.x, h.y, h.z, h.w), e.setRenderTarget(u), this._renderOverlay(e, r);
      return;
    }
    this.effectPass.render(e, t, r, n, i, s, o), this._renderOverlay(e, r);
  }
  /**
   * 渲染选择层调试 overlay
   * @private
   */
  _renderOverlay(e, t) {
    const r = this.effect.getSelection ? this.effect.getSelection() : this.effect.selection;
    if (!this.overlayEnabled || !r || r.size === 0)
      return;
    const n = this.effect.selectionRenderTarget;
    if (!n || n.texture === null)
      return;
    this.overlayMaterial.map !== n.texture && (this.overlayMaterial.map = n.texture, this.overlayMaterial.needsUpdate = !0);
    const i = this.selectionViewport;
    if (i.width <= 0 || i.height <= 0)
      return;
    const s = e.getRenderTarget(), o = e.getViewport(new ze()), l = e.getScissor(new ze()), u = e.getScissorTest();
    e.setRenderTarget(this.renderToScreen ? null : t), e.clearDepth(), e.setViewport(i.x, i.y, i.width, i.height), e.setScissor(i.x, i.y, i.width, i.height), e.setScissorTest(!0), e.render(this.overlayScene, this.overlayCamera, {
      projectObject: !0,
      updateMatrixWorld: !1,
      useProgramCache: !1
    }), e.setScissorTest(u), e.setViewport(o.x, o.y, o.z, o.w), e.setScissor(l.x, l.y, l.z, l.w), e.setRenderTarget(s);
  }
  setSize(e, t) {
    this.bufferWidth = e, this.bufferHeight = t, this.effectPass.setSize(e, t);
    const r = Math.max(0, e - this.overlayPadding * 2), n = Math.max(0, t - this.overlayPadding * 2);
    let i = Math.round(e * this.overlayInset);
    if (i = Math.max(this.overlayMinWidth, i), i = Math.min(i, r), i <= 0 || n <= 0) {
      this.selectionViewport.width = 0, this.selectionViewport.height = 0;
      return;
    }
    const s = t / Math.max(e, 1);
    let o = Math.round(i * s);
    o = Math.min(o, n), this.selectionViewport.width = i, this.selectionViewport.height = o, this.selectionViewport.x = e - i - this.overlayPadding, this.selectionViewport.y = this.overlayPadding;
  }
  initialize(e, t, r) {
    this.effectPass.initialize(e, t, r);
  }
  dispose() {
    super.dispose(), this.effectPass.dispose(), this.overlayMaterial && this.overlayMaterial.dispose(), this.selectionDisplayMaterial && this.selectionDisplayMaterial.dispose();
  }
}
class Ad extends X {
  /**
   * 构造SSR专用兼容Pass
   * 
   * @param {OptimizedSelectiveSSRPass|SelectiveSSRPass} ssrPass - SSR Pass实例
   * @param {String} [name="SSRCompatPass"] - Pass名称
   */
  constructor(e, t = "SSRCompatPass") {
    super(t), this.ssrPass = e, this.isSSRCompatPass = !0, this.enabled = !0, this.needsSwap = !1, this.renderToScreen = !1, this.performanceStats = {
      renderTime: 0,
      frameCount: 0,
      avgRenderTime: 0
    }, this.stateCache = {
      lastInputBuffer: null,
      lastOutputBuffer: null,
      lastCameraMatrix: null,
      lastProjectionMatrix: null
    }, this.tempMatrix1 = null, this.tempMatrix2 = null, console.log(`SSRCompatPass: 创建SSR专用兼容层 - ${t}`);
  }
  /**
   * 设置渲染到屏幕的标志
   * @type {Boolean}
   */
  set renderToScreen(e) {
    super.renderToScreen = e, this.ssrPass && (this.ssrPass.renderToScreen = e);
  }
  get renderToScreen() {
    return super.renderToScreen;
  }
  /**
   * 检查相机矩阵是否发生变化（用于优化）
   * @param {Camera} camera - 相机对象
   * @returns {Boolean} 是否发生变化
   * @private
   */
  _checkCameraMatrixChanged(e) {
    if (!this.tempMatrix1)
      return this.tempMatrix1 = e.matrixWorld.clone(), this.tempMatrix2 = e.projectionMatrix.clone(), !0;
    const t = !this.tempMatrix1.equals(e.matrixWorld), r = !this.tempMatrix2.equals(e.projectionMatrix);
    return t && this.tempMatrix1.copy(e.matrixWorld), r && this.tempMatrix2.copy(e.projectionMatrix), t || r;
  }
  /**
   * 批量保存渲染器状态
   * @param {WebGLRenderer} renderer - 渲染器
   * @returns {Object} 保存的状态
   * @private
   */
  _batchSaveState(e) {
    const t = new y();
    return {
      renderTarget: e.getRenderTarget(),
      autoClear: e.autoClear,
      shadowMapAutoUpdate: e.shadowMap.autoUpdate,
      clearColor: e.getClearColor(t).clone(),
      clearAlpha: e.getClearAlpha()
    };
  }
  /**
   * 批量恢复渲染器状态
   * @param {WebGLRenderer} renderer - 渲染器
   * @param {Object} state - 要恢复的状态
   * @private
   */
  _batchRestoreState(e, t) {
    e.setRenderTarget(t.renderTarget), e.autoClear = t.autoClear, e.shadowMap.autoUpdate = t.shadowMapAutoUpdate, e.setClearColor(t.clearColor), e.setClearAlpha(t.clearAlpha);
  }
  /**
   * 智能缓冲区设置（避免不必要的状态切换）
   * @param {WebGLRenderer} renderer - 渲染器
   * @param {WebGLRenderTarget} inputBuffer - 输入缓冲区
   * @param {WebGLRenderTarget} outputBuffer - 输出缓冲区
   * @private
   */
  _smartBufferSetup(e, t, r) {
    const n = this.stateCache.lastInputBuffer !== t, i = this.stateCache.lastOutputBuffer !== r;
    (n || i) && (this.ssrPass.setupBuffers && typeof this.ssrPass.setupBuffers == "function" && this.ssrPass.setupBuffers(e, t, r), this.stateCache.lastInputBuffer = t, this.stateCache.lastOutputBuffer = r, console.log("SSRCompatPass: 智能缓冲区设置完成"));
  }
  /**
   * 设置尺寸（传递给SSR Pass）
   * @param {Number} width - 宽度
   * @param {Number} height - 高度
   */
  setSize(e, t) {
    this.ssrPass && typeof this.ssrPass.setSize == "function" && (this.ssrPass.setSize(e, t), console.log(`SSRCompatPass: 设置尺寸 ${e}x${t}`));
  }
  /**
   * 初始化Pass（传递给SSR Pass）
   * @param {WebGLRenderer} renderer - 渲染器
   * @param {Boolean} alpha - 渲染器是否使用alpha通道
   * @param {Number} frameBufferType - 主帧缓冲区的类型
   */
  initialize(e, t, r) {
    this.ssrPass && typeof this.ssrPass.initialize == "function" && this.ssrPass.initialize(e, t, r), console.log("SSRCompatPass: 初始化完成");
  }
  /**
   * 设置深度纹理（传递给SSR Pass）
   * @param {Texture} depthTexture - 深度纹理
   * @param {Number} depthPacking - 深度打包方式
   */
  setDepthTexture(e, t) {
    this.ssrPass && typeof this.ssrPass.setDepthTexture == "function" && this.ssrPass.setDepthTexture(e, t);
  }
  /**
   * 渲染Pass
   * @param {WebGLRenderer} renderer - 渲染器
   * @param {WebGLRenderTarget} inputBuffer - 输入缓冲区
   * @param {WebGLRenderTarget} outputBuffer - 输出缓冲区
   * @param {Number} deltaTime - 帧时间差
   * @param {Boolean} stencilTest - 是否启用模板测试
   * @param {DepthPass} depthPass - 深度Pass
   * @param {Object} effectPassOpts - 效果Pass选项
   */
  render(e, t, r, n, i, s, o) {
    const l = performance.now();
    if (!this.enabled)
      return;
    const u = this._batchSaveState(e);
    try {
      this._smartBufferSetup(e, t, r), this._checkCameraMatrixChanged(this.ssrPass.camera) && this.ssrPass.markSceneChanged && this.ssrPass.markSceneChanged();
      const m = this.renderToScreen ? null : r, p = t;
      this.ssrPass.renderToScreen = this.renderToScreen, this.ssrPass.render(
        e,
        m,
        p,
        n,
        i || !1,
        s,
        o
      );
    } catch (h) {
      console.error("SSRCompatPass 渲染错误:", h);
    } finally {
      this._batchRestoreState(e, u);
    }
    const f = performance.now() - l;
    this._updatePerformanceStats(f);
  }
  /**
   * 更新性能统计
   * @param {Number} renderTime - 本帧渲染时间
   * @private
   */
  _updatePerformanceStats(e) {
    this.performanceStats.renderTime = e, this.performanceStats.frameCount++;
    const t = 0.1;
    this.performanceStats.avgRenderTime = this.performanceStats.avgRenderTime * (1 - t) + e * t, this.performanceStats.frameCount % 100 === 0 && console.log(`SSRCompatPass 性能统计: 平均渲染时间 ${this.performanceStats.avgRenderTime.toFixed(2)}ms`);
  }
  /**
   * 获取性能统计信息
   * @returns {Object} 性能统计数据
   */
  getPerformanceStats() {
    const e = this.ssrPass.getPerformanceStats ? this.ssrPass.getPerformanceStats() : {};
    return {
      compatPassStats: {
        avgRenderTime: this.performanceStats.avgRenderTime,
        frameCount: this.performanceStats.frameCount,
        lastRenderTime: this.performanceStats.renderTime
      },
      ssrPassStats: e
    };
  }
  /**
   * 设置SSR Pass的分辨率缩放（如果支持）
   * @param {Number} scale - 分辨率缩放比例
   */
  setSSRResolutionScale(e) {
    this.ssrPass && "ssrResolutionScale" in this.ssrPass ? (this.ssrPass.ssrResolutionScale = e, console.log(`SSRCompatPass: 设置SSR分辨率缩放为 ${e}`)) : console.warn("SSRCompatPass: SSR Pass不支持分辨率缩放");
  }
  /**
   * 启用/禁用自适应分辨率（如果支持）
   * @param {Boolean} enabled - 是否启用
   * @param {Number} targetFPS - 目标帧率
   */
  setAdaptiveResolution(e, t = 60) {
    this.ssrPass && "adaptiveResolution" in this.ssrPass ? (this.ssrPass.adaptiveResolution = e, "targetFPS" in this.ssrPass && (this.ssrPass.targetFPS = t), console.log(`SSRCompatPass: ${e ? "启用" : "禁用"}自适应分辨率，目标帧率 ${t}`)) : console.warn("SSRCompatPass: SSR Pass不支持自适应分辨率");
  }
  /**
   * 强制刷新SSR（如果支持）
   */
  forceRefresh() {
    this.ssrPass && (typeof this.ssrPass.forceRefreshNormals == "function" && this.ssrPass.forceRefreshNormals(), typeof this.ssrPass.markSceneChanged == "function" && this.ssrPass.markSceneChanged(), console.log("SSRCompatPass: 强制刷新SSR"));
  }
  /**
   * 获取SSR Pass的选择管理接口
   * @returns {Object} 选择管理接口
   */
  getSelectionAPI() {
    return this.ssrPass ? {
      addToSelection: (e) => this.ssrPass.addToSelection?.(e),
      removeFromSelection: (e) => this.ssrPass.removeFromSelection?.(e),
      clearSelection: () => this.ssrPass.clearSelection?.(),
      toggleSelection: (e) => this.ssrPass.toggleSelection?.(e),
      getSelectionItems: () => this.ssrPass.getSelectionItems?.() || [],
      setMetalnessThreshold: (e) => this.ssrPass.setMetalnessThreshold?.(e)
    } : null;
  }
  /**
   * 设置输出模式（如果支持）
   * @param {Number} mode - 输出模式
   */
  setOutputMode(e) {
    this.ssrPass && typeof this.ssrPass.setOutputMode == "function" ? this.ssrPass.setOutputMode(e) : this.ssrPass && "output" in this.ssrPass && (this.ssrPass.output = e);
  }
  /**
   * 获取可用的输出模式
   * @returns {Object} 输出模式枚举
   */
  getOutputModes() {
    return this.ssrPass && this.ssrPass.constructor.OUTPUT ? this.ssrPass.constructor.OUTPUT : null;
  }
  /**
   * 销毁Pass
   */
  dispose() {
    this.ssrPass && typeof this.ssrPass.dispose == "function" && this.ssrPass.dispose(), this.stateCache = {}, this.performanceStats = {}, this.tempMatrix1 && (this.tempMatrix1 = null), this.tempMatrix2 && (this.tempMatrix2 = null), console.log("SSRCompatPass: 销毁完成"), super.dispose();
  }
}
class Ve extends _e {
  /**
      * 构造一个新的多采样深度掩码材质
      */
  constructor() {
    super(), this.name = "MultiSampleDepthMaskMaterial", this.defines.USE_MULTISAMPLING = "", this.defines.SAMPLES = "9", this.uniforms.samplingRadius = new a(2), this.uniforms.samplingThreshold = new a(0.5), this.uniforms.resolution = new a(new g(1, 1)), this.fragmentShader = this.generateFragmentShader(), this.needsUpdate = !0;
  }
  /**
      * 采样数量
      *
      * @type {Number}
      */
  get samplingCount() {
    return Number(this.defines.SAMPLES);
  }
  set samplingCount(e) {
    this.defines.SAMPLES = Math.max(1, Math.floor(e)).toString(), this.needsUpdate = !0;
  }
  /**
      * 采样半径
      *
      * @type {Number}
      */
  get samplingRadius() {
    return this.uniforms.samplingRadius.value;
  }
  set samplingRadius(e) {
    this.uniforms.samplingRadius.value = Math.max(0.1, e);
  }
  /**
      * 采样匹配阈值
      *
      * @type {Number}
      */
  get samplingThreshold() {
    return this.uniforms.samplingThreshold.value;
  }
  set samplingThreshold(e) {
    this.uniforms.samplingThreshold.value = Math.min(Math.max(0, e), 1);
  }
  /**
      * 设置分辨率
      *
      * @param {Number} width - 宽度
      * @param {Number} height - 高度
      */
  setResolution(e, t) {
    this.uniforms.resolution.value.set(e, t);
  }
  /**
      * 生成带有多采样功能的片元着色器
      *
      * @return {String} 新的片元着色器代码
      */
  generateFragmentShader() {
    return `
#include <common>
#include <packing>

#ifdef GL_FRAGMENT_PRECISION_HIGH
    uniform highp sampler2D depthBuffer0;
    uniform highp sampler2D depthBuffer1;
#else
    uniform mediump sampler2D depthBuffer0;
    uniform mediump sampler2D depthBuffer1;
#endif

uniform sampler2D inputBuffer;
uniform vec2 cameraNearFar;
uniform float samplingRadius;
uniform float samplingThreshold;
uniform vec2 resolution;

float getViewZ(const in float depth) {
    #ifdef PERSPECTIVE_CAMERA
        return perspectiveDepthToViewZ(depth, cameraNearFar.x, cameraNearFar.y);
    #else
        return orthographicDepthToViewZ(depth, cameraNearFar.x, cameraNearFar.y);
    #endif
}

varying vec2 vUv;

#ifdef USE_MULTISAMPLING
    // 定义采样偏移数组
    vec2 offsets[9] = vec2[](
        vec2(-1, -1), vec2(0, -1), vec2(1, -1),
        vec2(-1,  0), vec2(0,  0), vec2(1,  0),
        vec2(-1,  1), vec2(0,  1), vec2(1,  1)
    );
    
    // 获取特定位置的深度值
    vec2 getDepthAt(vec2 uv) {
        vec2 depth;
        
        #if DEPTH_PACKING_0 == 3201
            depth.x = unpackRGBAToDepth(texture2D(depthBuffer0, uv));
        #else
            depth.x = texture2D(depthBuffer0, uv).r;
            #ifdef LOG_DEPTH
                float d = pow(2.0, depth.x * log2(cameraNearFar.y + 1.0)) - 1.0;
                float a = cameraNearFar.y / (cameraNearFar.y - cameraNearFar.x);
                float b = cameraNearFar.y * cameraNearFar.x / (cameraNearFar.x - cameraNearFar.y);
                depth.x = a + b / d;
            #endif
        #endif
        
        #if DEPTH_PACKING_1 == 3201
            depth.y = unpackRGBAToDepth(texture2D(depthBuffer1, uv));
        #else
            depth.y = texture2D(depthBuffer1, uv).r;
            #ifdef LOG_DEPTH
                float d = pow(2.0, depth.y * log2(cameraNearFar.y + 1.0)) - 1.0;
                float a = cameraNearFar.y / (cameraNearFar.y - cameraNearFar.x);
                float b = cameraNearFar.y * cameraNearFar.x / (cameraNearFar.x - cameraNearFar.y);
                depth.y = a + b / d;
            #endif
        #endif
        
        #ifdef PERSPECTIVE_CAMERA
            // Linearize.
            depth.x = viewZToOrthographicDepth(getViewZ(depth.x), cameraNearFar.x, cameraNearFar.y);
            depth.y = viewZToOrthographicDepth(getViewZ(depth.y), cameraNearFar.x, cameraNearFar.y);
        #endif
        
        return depth;
    }
    
    // 多采样深度比较
    float multiSampleDepthComparison(vec2 uv) {
        float matchCount = 0.0;
        float pixelSize = samplingRadius / max(resolution.x, resolution.y);
        
        for(int i = 0; i < SAMPLES; i++) {
            int idx = i % 9; // 循环使用偏移数组
            vec2 offset = offsets[idx] * pixelSize;
            vec2 sampleUv = uv + offset;
            
            // 确保采样点在有效范围内
            if(sampleUv.x >= 0.0 && sampleUv.x <= 1.0 && sampleUv.y >= 0.0 && sampleUv.y <= 1.0) {
                // 获取深度值
                vec2 sampleDepth = getDepthAt(sampleUv);
                
                bool isMaxDepth = (sampleDepth.x == 1.0);
                
                // 深度比较
                bool keep;
                
                #if DEPTH_TEST_STRATEGY == 0
                    // Decide based on depth test.
                    keep = depthTest(sampleDepth.x, sampleDepth.y);
                #elif DEPTH_TEST_STRATEGY == 1
                    // Always keep max depth.
                    keep = isMaxDepth || depthTest(sampleDepth.x, sampleDepth.y);
                #else
                    // Always discard max depth.
                    keep = !isMaxDepth && depthTest(sampleDepth.x, sampleDepth.y);
                #endif
                
                matchCount += keep ? 1.0 : 0.0;
            }
        }
        
        // 返回匹配率
        return matchCount / float(SAMPLES);
    }
#endif

void main() {
#ifdef USE_MULTISAMPLING
    // 使用多采样
    float matchRate = multiSampleDepthComparison(vUv);
    
    // 应用平滑过渡而非二值化结果
    if(matchRate >= samplingThreshold) {
        gl_FragColor = texture2D(inputBuffer, vUv);
    } else {
        discard;
    }
#else
    // 原始单采样逻辑
    vec2 depth;

    #if DEPTH_PACKING_0 == 3201
        depth.x = unpackRGBAToDepth(texture2D(depthBuffer0, vUv));
    #else
        depth.x = texture2D(depthBuffer0, vUv).r;
        #ifdef LOG_DEPTH
            float d = pow(2.0, depth.x * log2(cameraNearFar.y + 1.0)) - 1.0;
            float a = cameraNearFar.y / (cameraNearFar.y - cameraNearFar.x);
            float b = cameraNearFar.y * cameraNearFar.x / (cameraNearFar.x - cameraNearFar.y);
            depth.x = a + b / d;
        #endif
    #endif

    #if DEPTH_PACKING_1 == 3201
        depth.y = unpackRGBAToDepth(texture2D(depthBuffer1, vUv));
    #else
        depth.y = texture2D(depthBuffer1, vUv).r;
        #ifdef LOG_DEPTH
            float d = pow(2.0, depth.y * log2(cameraNearFar.y + 1.0)) - 1.0;
            float a = cameraNearFar.y / (cameraNearFar.y - cameraNearFar.x);
            float b = cameraNearFar.y * cameraNearFar.x / (cameraNearFar.x - cameraNearFar.y);
            depth.y = a + b / d;
        #endif
    #endif

    bool isMaxDepth = (depth.x == 1.0);

    #ifdef PERSPECTIVE_CAMERA
        // Linearize.
        depth.x = viewZToOrthographicDepth(getViewZ(depth.x), cameraNearFar.x, cameraNearFar.y);
        depth.y = viewZToOrthographicDepth(getViewZ(depth.y), cameraNearFar.x, cameraNearFar.y);
    #endif

    #if DEPTH_TEST_STRATEGY == 0
        // Decide based on depth test.
        bool keep = depthTest(depth.x, depth.y);
    #elif DEPTH_TEST_STRATEGY == 1
        // Always keep max depth.
        bool keep = isMaxDepth || depthTest(depth.x, depth.y);
    #else
        // Always discard max depth.
        bool keep = !isMaxDepth && depthTest(depth.x, depth.y);
    #endif

    if(keep) {
        gl_FragColor = texture2D(inputBuffer, vUv);
    } else {
        discard;
    }
#endif
}`;
  }
}
const Wc = `// 河流材质顶点着色器
// 基于"Where the River Goes"着色器实现

uniform float time;
uniform float wavesHeight;
// 这些是Three.js已经提供的uniform，不需要再次声明
// uniform mat4 modelMatrix;
// uniform mat4 viewMatrix;
// uniform mat4 projectionMatrix;

// Three.js 提供的变量
// attribute vec3 position;
// attribute vec2 uv;
// attribute vec3 normal;

// 传递给片段着色器的数据
varying vec2 vUv;
varying vec3 vPosition;       // 模型空间位置
varying vec3 vNormal;
varying vec3 vWorldPosition;  // 世界空间位置
varying vec3 vViewDirection;
varying mat3 vNormalMatrix;

// 哈希函数 - 用于随机化波动
float hash(float n) {
    return fract(sin(n) * 43758.5453);
}

// 基于原始着色器的河流波动函数 - 使用vec3模型空间坐标
float GetWave(vec3 pos, float time) {
    // 添加一些随机性，使波浪看起来不均匀
    float seed = dot(vec2(pos.x, pos.z), vec2(123.45, 678.91));
    float randomOffset = hash(seed) * 6.28;
    
    // 主要波浪
    float waveA = sin(pos.x * 2.0 + time * 0.5 + randomOffset) * 
                 cos(pos.z * 2.0 + time * 0.3) * 0.5;
                 
    // 次要波浪
    float waveB = sin(pos.x * 3.0 + time * 0.2 + randomOffset) * 
                 sin(pos.z * 3.0 + time * 0.5) * 0.25;
    
    // 细节波浪
    float waveC = sin(pos.x * 7.0 + time * 0.1 + randomOffset) * 
                 cos(pos.z * 6.0 + time * 0.2) * 0.125;
    
    return (waveA + waveB + waveC) * wavesHeight;
}

void main() {
    // 基础位置 - 使用模型空间坐标
    vec3 pos = position;
    
    // 现在直接使用模型空间的xz坐标
    vec2 modelXZ = vec2(position.x, position.z);
    
    // 应用波浪效果 - 直接使用vec3位置
    pos.y += GetWave(pos, time);
    
    // 计算法线 - 使用有限差分法 (基于模型空间)
    float epsilon = 0.01;
    vec3 pos1 = pos + vec3(epsilon, 0.0, 0.0);
    vec3 pos2 = pos + vec3(-epsilon, 0.0, 0.0);
    vec3 pos3 = pos + vec3(0.0, 0.0, epsilon);
    vec3 pos4 = pos + vec3(0.0, 0.0, -epsilon);
    
    // 使用相同的函数获取高度差
    float h1 = GetWave(pos1, time);
    float h2 = GetWave(pos2, time);
    float h3 = GetWave(pos3, time);
    float h4 = GetWave(pos4, time);
    
    vec3 tangent = normalize(vec3(2.0 * epsilon, h1 - h2, 0.0));
    vec3 bitangent = normalize(vec3(0.0, h3 - h4, 2.0 * epsilon));
    vec3 modifiedNormal = normalize(cross(bitangent, tangent));
    
    // 传递给片段着色器的数据
    vUv = uv;
    vPosition = pos;  // 传递模型空间坐标
    vNormal = normalMatrix * modifiedNormal;
    
    // 计算并传递世界空间位置
    vec4 worldPosition = modelMatrix * vec4(pos, 1.0);
    vWorldPosition = worldPosition.xyz;
    
    // 计算视线方向
    vec4 viewPos = viewMatrix * worldPosition;
    vViewDirection = normalize(-viewPos.xyz);
    
    // 传递法线矩阵给片段着色器
    vNormalMatrix = normalMatrix;
    
    // 最终顶点位置
    gl_Position = projectionMatrix * viewMatrix * worldPosition;
} `, jc = `// 河流材质片段着色器
// 直接基于"Where the River Goes"着色器实现

uniform float time;
uniform sampler2D flowMap;
uniform sampler2D normalMap;
uniform sampler2D foamMap;
uniform float flowSpeed;
uniform vec3 waterColor;
uniform float transparency;
uniform float foamIntensity;
uniform float waterDepth;
uniform vec2 textureSize;
uniform vec3 sunDirection;
uniform vec3 sunColor;
uniform vec3 skyColor;

// 来自顶点着色器的数据
varying vec2 vUv;
varying vec3 vPosition;       // 模型空间位置
varying vec3 vNormal;
varying vec3 vWorldPosition;  // 世界空间位置
varying vec3 vViewDirection;
varying mat3 vNormalMatrix;

// 常量定义
#define MOD2 vec2(4.438975, 3.972973)
#define FOAM_ENABLED 1

// 哈希函数 - 直接从原始着色器复制
float Hash(float p) {
    vec2 p2 = fract(vec2(p) * MOD2);
    p2 += dot(p2.yx, p2.xy + 19.19);
    return fract(p2.x * p2.y);
}

// 平滑噪声 - 直接从原始着色器复制
float SmoothNoise(in vec2 o) {
    vec2 p = floor(o);
    vec2 f = fract(o);
        
    float n = p.x + p.y * 57.0;

    float a = Hash(n + 0.0);
    float b = Hash(n + 1.0);
    float c = Hash(n + 57.0);
    float d = Hash(n + 58.0);
    
    vec2 f2 = f * f;
    vec2 f3 = f2 * f;
    
    vec2 t = 3.0 * f2 - 2.0 * f3;
    
    float u = t.x;
    float v = t.y;

    float res = a + (b-a)*u + (c-a)*v + (a-b+d-c)*u*v;
    
    return res;
}

// FBM - 直接从原始着色器复制
float FBM(vec2 p, float ps) {
    float f = 0.0;
    float tot = 0.0;
    float a = 1.0;
    for(int i=0; i<5; i++) {
        f += SmoothNoise(p) * a;
        p *= 2.0;
        tot += a;
        a *= ps;
    }
    return f / tot;
}

// 带梯度的噪声 - 直接从原始着色器复制
vec3 SmoothNoise_DXY(in vec2 o) {
    vec2 p = floor(o);
    vec2 f = fract(o);
        
    float n = p.x + p.y * 57.0;

    float a = Hash(n + 0.0);
    float b = Hash(n + 1.0);
    float c = Hash(n + 57.0);
    float d = Hash(n + 58.0);
    
    vec2 f2 = f * f;
    vec2 f3 = f2 * f;
    
    vec2 t = 3.0 * f2 - 2.0 * f3;
    vec2 dt = 6.0 * f - 6.0 * f2;
    
    float u = t.x;
    float v = t.y;
    float du = dt.x;    
    float dv = dt.y;    

    float res = a + (b-a)*u + (c-a)*v + (a-b+d-c)*u*v;
    
    float dx = (b-a)*du + (a-b+d-c)*du*v;
    float dy = (c-a)*dv + (a-b+d-c)*u*dv;
    
    return vec3(dx, dy, res);
}

// 带梯度的FBM - 直接从原始着色器复制
vec3 FBM_DXY(vec2 p, vec2 flow, float ps, float df) {
    vec3 f = vec3(0.0);
    float tot = 0.0;
    float a = 1.0;
    for(int i=0; i<4; i++) {
        p += flow;
        flow *= -0.75; // 每个八度反转流向
        vec3 v = SmoothNoise_DXY(p);
        f += v * a;
        p += v.xy * df;
        p *= 2.0;
        tot += a;
        a *= ps;
    }
    return f / tot;
}

// 获取河流蜿蜒 - 使用模型坐标
float GetRiverMeander(const float x) {
    return sin(x * 0.3) * 1.5;
}

float GetRiverMeanderDx(const float x) {
    return cos(x * 0.3) * 1.5 * 0.3;
}

// 获取河床偏移 - 使用模型坐标
float GetRiverBedOffset(const vec3 vPos) {
    float fRiverBedDepth = 0.3 + (0.5 + 0.5 * sin(vPos.x * 0.001 + 3.0)) * 0.4;
    float fRiverBedWidth = 2.0 + cos(vPos.x * 0.1) * 1.0;
    
    float fRiverBedAmount = smoothstep(fRiverBedWidth, fRiverBedWidth * 0.5, abs(vPos.z - GetRiverMeander(vPos.x)));
        
    return fRiverBedAmount * fRiverBedDepth;    
}

// 获取基础水流 - 使用模型坐标
vec2 GetBaseFlow(const vec2 vPos) {
    return vec2(1.0, GetRiverMeanderDx(vPos.x));
}

// 获取水流距离 - 使用模型坐标
float GetFlowDistance(const vec2 vPos) {
    float riverMeander = GetRiverMeander(vPos.x);
    float dist = abs(vPos.y - riverMeander);
    float riverWidth = 2.0 + cos(vPos.x * 0.1) * 1.0;
    return smoothstep(riverWidth, 0.0, dist);
}

// 获取梯度 - 使用模型坐标
vec2 GetGradient(const vec2 vPos) {
    vec2 vDelta = vec2(0.01, 0.00);
    float dx = GetFlowDistance(vPos + vDelta.xy) - GetFlowDistance(vPos - vDelta.xy);
    float dy = GetFlowDistance(vPos + vDelta.yx) - GetFlowDistance(vPos - vDelta.yx);
    return vec2(dx, dy);
}

// 获取流速 - 使用模型坐标
vec3 GetFlowRate(const vec2 vPos) {
    vec2 vBaseFlow = GetBaseFlow(vPos);
    vec2 vFlow = vBaseFlow;
    
    float fFoam = 0.0;
    float fDist = GetFlowDistance(vPos);
    vec2 vGradient = GetGradient(vPos);
    
    vFlow += -vGradient * 40.0 / (1.0 + fDist * 1.5);
    vFlow *= 1.0 / (1.0 + fDist * 0.5);

    float fBehindObstacle = 0.5 - dot(normalize(vGradient), -normalize(vFlow)) * 0.5;
    float fSlowDist = clamp(fDist * 5.0, 0.0, 1.0);
    fSlowDist = mix(fSlowDist * 0.9 + 0.1, 1.0, fBehindObstacle * 0.9);
    fSlowDist = 0.5 + fSlowDist * 0.5;
    vFlow *= fSlowDist;
    
    // 泡沫计算
    float fFoamScale1 = 0.5;
    float fFoamCutoff = 0.4;
    float fFoamScale2 = 0.35;
    
    fFoam = abs(length(vFlow)) * fFoamScale1;
    fFoam += clamp(fFoam - fFoamCutoff, 0.0, 1.0);
    fFoam = 1.0 - pow(fDist, fFoam * fFoamScale2);
    
    return vec3(vFlow * 0.6, fFoam);
}

// 采样水面法线
vec4 SampleWaterNormal(vec2 vUV, vec2 vFlowOffset, float fMag, float fFoam) {
    // 使用纹理或程序化生成法线
    vec4 normalSample = texture2D(normalMap, vUV + vFlowOffset);
    if(normalSample.r > 0.01 || normalSample.g > 0.01 || normalSample.b > 0.01) {
        vec3 normalFromMap = normalSample.rgb * 2.0 - 1.0;
        float fScale = max(0.25, 1.0 - fFoam * 5.0); // 减弱泡沫区域法线
        vec3 vBlended = mix(vec3(0.0, 1.0, 0.0), normalize(vec3(normalFromMap.x, fMag, normalFromMap.y)), fScale);
        return vec4(normalize(vBlended), normalSample.z * fScale);
    } else {
        vec2 vFilterWidth = max(abs(dFdx(vUV)), abs(dFdy(vUV)));
        float fFilterWidth = max(vFilterWidth.x, vFilterWidth.y);
        
        float fScale = (1.0 / (1.0 + fFilterWidth * fFilterWidth * 2000.0));
        float fGradientAscent = 0.25 + (fFoam * -1.5);
        vec3 dxy = FBM_DXY(vUV * 20.0, vFlowOffset * 20.0, 0.75 + fFoam * 0.25, fGradientAscent);
        fScale *= max(0.25, 1.0 - fFoam * 5.0); // 减弱泡沫区域法线
        vec3 vBlended = mix(vec3(0.0, 1.0, 0.0), normalize(vec3(dxy.x, fMag, dxy.y)), fScale);
        return vec4(normalize(vBlended), dxy.z * fScale);
    }
}

// 采样水面泡沫
float SampleWaterFoam(vec2 vUV, vec2 vFlowOffset, float fFoam) {
    vec4 foamSample = texture2D(foamMap, vUV + vFlowOffset * 0.5);
    if(foamSample.r > 0.01 || foamSample.g > 0.01 || foamSample.b > 0.01) {
        return foamSample.r;
    } else {
        float f = FBM_DXY(vUV * 30.0, vFlowOffset * 50.0, 0.8, -0.5).z;
        float fAmount = 0.2;
        f = max(0.0, (f - fAmount) / fAmount);
        return pow(0.5, f);
    }
}

// 采样流动法线
vec4 SampleFlowingNormal(const vec2 vUV, const vec2 vFlowRate, const float fFoam, const float time, out float fOutFoamTex) {
    float fMag = 2.5 / (1.0 + dot(vFlowRate, vFlowRate) * 5.0);
    float t0 = fract(time);
    float t1 = fract(time + 0.5);
    
    float i0 = floor(time);
    float i1 = floor(time + 0.5);
    
    float o0 = t0 - 0.5;
    float o1 = t1 - 0.5;
    
    vec2 vUV0 = vUV + vec2(Hash(i0), Hash(i0 + 42.0)) * 0.01;
    vec2 vUV1 = vUV + vec2(Hash(i1), Hash(i1 + 42.0)) * 0.01;
    
    vec4 sample0 = SampleWaterNormal(vUV0, vFlowRate * o0, fMag, fFoam);
    vec4 sample1 = SampleWaterNormal(vUV1, vFlowRate * o1, fMag, fFoam);

    float weight = abs(t0 - 0.5) * 2.0;

#if FOAM_ENABLED
    float foam0 = SampleWaterFoam(vUV0, vFlowRate * o0 * 0.25, fFoam);
    float foam1 = SampleWaterFoam(vUV1, vFlowRate * o1 * 0.25, fFoam);
    fOutFoamTex = mix(foam0, foam1, weight);
#else
    fOutFoamTex = 0.0;
#endif
    
    vec4 result = mix(sample0, sample1, weight);
    result.xyz = normalize(result.xyz);

    return result;
}

// 计算菲涅尔反射
vec3 GetFresnel(vec3 vView, vec3 vNormal, vec3 vR0, float fGloss) {
    float NdotV = max(0.0, dot(vView, vNormal));
    return vR0 + (vec3(1.0) - vR0) * pow(1.0 - NdotV, 5.0) * pow(fGloss, 20.0);
}

// 主函数
void main() {
    // 使用模型空间坐标而不是世界空间坐标
    vec2 modelPos = vec2(vPosition.x, vPosition.z);
    
    // 使用GetFlowRate获取流速和泡沫
    vec3 vFlowRateAndFoam = GetFlowRate(modelPos);
    vec2 vFlowRate = vFlowRateAndFoam.xy * flowSpeed;
    float fFoam = vFlowRateAndFoam.z * foamIntensity;
    
    // 时间缩放
    float scaledTime = time * 0.5;
    
    // 法线和泡沫采样 - 使用模型空间坐标
    float fWaterFoamTex = 0.0;
    vec4 vWaterNormalAndHeight = SampleFlowingNormal(modelPos, vFlowRate, fFoam, scaledTime, fWaterFoamTex);
    vec3 waterNormal = vWaterNormalAndHeight.xyz;
    
    // 转换法线到世界空间
    vec3 worldNormal = normalize(vNormalMatrix * waterNormal);
    
    // 光照计算
    float sunDiffuse = max(0.0, dot(worldNormal, sunDirection));
    vec3 halfVector = normalize(sunDirection + vViewDirection);
    float specular = pow(max(0.0, dot(worldNormal, halfVector)), 64.0);
    
    // 菲涅尔反射
    vec3 fresnelTerm = GetFresnel(vViewDirection, worldNormal, vec3(0.02), 0.95);
    
    // 反射方向
    vec3 reflectDir = reflect(-vViewDirection, worldNormal);
    
    // 环境反射 (天空)
    vec3 skyReflection = mix(skyColor, sunColor, pow(max(0.0, dot(reflectDir, sunDirection)), 8.0));
    
    // 水深计算 - 使用模型空间的流距离
    float depth = waterDepth * (1.0 - GetFlowDistance(modelPos));
    
    // 水颜色
    vec3 waterColorWithDepth = waterColor * (0.5 + 0.5 * depth);
    
    // 漫反射和环境光
    vec3 diffuseLight = sunColor * sunDiffuse * 0.5 + skyColor * 0.5;
    vec3 diffuseTerm = waterColorWithDepth * diffuseLight;
    
    // 镜面反射
    vec3 specularTerm = sunColor * specular * fresnelTerm;
    
    // 环境反射
    vec3 reflectionTerm = skyReflection * fresnelTerm;
    
    // 泡沫颜色
    vec3 foamColor = vec3(1.0) * (0.8 + 0.2 * sunDiffuse);
    
    // 泡沫强度调整
#if FOAM_ENABLED
    float foamStrength = fFoam * (1.0 - fWaterFoamTex);
#else
    float foamStrength = 0.0;
#endif
    
    // 最终颜色
    vec3 finalColor = mix(
        diffuseTerm + specularTerm + reflectionTerm,
        foamColor,
        foamStrength
    );
    
    // 水深衰减
    finalColor *= exp(-depth * 0.5);
    
    // 输出带透明度的颜色
    gl_FragColor = vec4(finalColor, transparency);
} `;
class Ed extends N {
  /**
      * 构造函数
      * @param {Object} [options] - 可选配置项
      * @param {Number} [options.flowSpeed=0.5] - 流动速度
      * @param {Number|Color} [options.waterColor=0x0055ff] - 水的颜色
      * @param {Number} [options.transparency=0.8] - 透明度
      * @param {Number} [options.foamIntensity=0.5] - 泡沫强度
      * @param {Number} [options.waterDepth=0.5] - 水深
      * @param {Number} [options.wavesHeight=0.2] - 波浪高度
      */
  constructor(e = {}) {
    const t = e.flowSpeed !== void 0 ? e.flowSpeed : 0.5, r = e.waterColor !== void 0 ? e.waterColor : 22015, n = e.transparency !== void 0 ? e.transparency : 0.8, i = e.foamIntensity !== void 0 ? e.foamIntensity : 0.5, s = e.waterDepth !== void 0 ? e.waterDepth : 0.5, o = e.wavesHeight !== void 0 ? e.wavesHeight : 0.2;
    super({
      name: "RiverMaterial",
      uniforms: {
        time: new a(0),
        flowMap: new a(null),
        normalMap: new a(null),
        foamMap: new a(null),
        flowSpeed: new a(t),
        waterColor: new a(new y(r)),
        transparency: new a(n),
        foamIntensity: new a(i),
        waterDepth: new a(s),
        wavesHeight: new a(o),
        textureSize: new a(new g(1, 1)),
        sunDirection: new a(new T(1, 0.7, 0.25).normalize()),
        sunColor: new a(new y(1, 0.85, 0.5).multiplyScalar(2)),
        skyColor: new a(new y(0.1, 0.5, 1)),
        modelMatrix: new a(new C()),
        modelMatrixInverse: new a(new C())
      },
      vertexShader: Wc,
      fragmentShader: jc,
      transparent: !0,
      side: hr
    });
  }
  /**
      * 设置流动贴图
      * @param {Texture} value - 流动方向贴图
      */
  set flowMap(e) {
    this.uniforms.flowMap.value = e, e && this.uniforms.textureSize.value.set(e.image.width, e.image.height);
  }
  /**
      * 设置法线贴图
      * @param {Texture} value - 水面法线贴图
      */
  set normalMap(e) {
    this.uniforms.normalMap.value = e;
  }
  /**
      * 设置泡沫贴图
      * @param {Texture} value - 泡沫贴图
      */
  set foamMap(e) {
    this.uniforms.foamMap.value = e;
  }
  /**
      * 更新材质
      * @param {Number} deltaTime - 时间差
      * @param {Matrix4} [modelMatrix] - 可选的模型矩阵，用于更新世界空间到模型空间的变换
      */
  update(e, t) {
    this.uniforms.time.value += e * this.uniforms.flowSpeed.value, t && (this.uniforms.modelMatrix.value.copy(t), this.uniforms.modelMatrixInverse.value.copy(t).invert());
  }
}
const Xc = `uniform float time;\r
uniform float cameraHeight;\r
uniform float waterDepth;\r
uniform float dragMult;\r
uniform int iterationsVertex;\r
\r
// Three.js 已提供的变量，不需要重新声明\r
// attribute vec3 position;\r
// attribute vec2 uv;\r
// attribute vec3 normal;\r
// uniform mat4 modelMatrix;\r
// uniform mat4 viewMatrix;\r
// uniform mat4 projectionMatrix;\r
// uniform mat3 normalMatrix;\r
// uniform vec3 cameraPosition;\r
\r
varying vec2 vUv;\r
varying vec3 vPosition;\r
varying vec3 vWorldPosition;\r
varying vec3 vViewDirection;\r
\r
// 哈希函数，返回随机向量\r
vec2 hashVec(vec2 p) {\r
  return sin(58322.51 * sin(vec2(\r
    dot(p + 0.01, vec2(123.4, 234.5)),\r
    dot(p + 0.01, vec2(234.5, 345.6))\r
  )) + time * 2.4);\r
}\r
\r
// 生成一个柏林噪声的八度音阶\r
float octave(vec2 uv, float gridSize) {\r
  vec2 gridUv = fract(uv * gridSize);\r
  vec2 cellId = floor(uv * gridSize);\r
  vec2 smoothUv = smoothstep(0.0, 1.0, gridUv);\r
  \r
  return mix(\r
    mix(\r
      dot(hashVec(cellId + vec2(0.0, 0.0)), gridUv - vec2(0.0, 0.0)),\r
      dot(hashVec(cellId + vec2(1.0, 0.0)), gridUv - vec2(1.0, 0.0)), \r
      smoothUv.x\r
    ),\r
    mix(\r
      dot(hashVec(cellId + vec2(0.0, 1.0)), gridUv - vec2(0.0, 1.0)),\r
      dot(hashVec(cellId + vec2(1.0, 1.0)), gridUv - vec2(1.0, 1.0)), \r
      smoothUv.x\r
    ), \r
    smoothUv.y\r
  );\r
}\r
\r
// 生成多个八度音阶的柏林噪声\r
float perlinNoise(vec2 uv, int iterations) {\r
  float noise = 0.0;\r
  float minOct = 3.0;\r
  float maxOct = float(iterations);\r
  float amp = 1.0;\r
  float totalAmp = 0.0;\r
  \r
  // 添加移动速度变量，使不同比例的波浪有不同的速度\r
  float mainSpeed = 0.3;\r
  float detailSpeed = 0.7;\r
  \r
  // 大尺度波浪 - 较慢移动\r
  for (float i = minOct; i < minOct + 4.0; i++) {\r
    if (i > maxOct) break;\r
    float freq = pow(2.0, i);\r
    vec2 offset = vec2(time * mainSpeed * (0.1 + 0.05 * i), \r
                       time * mainSpeed * (0.12 + 0.03 * i));\r
    float octaveValue = octave(uv + offset, freq);\r
    float weight = 1.0 / pow(2.0, 1.0 + i - minOct);\r
    noise += (octaveValue + 0.5) * weight * amp;\r
    totalAmp += weight * amp;\r
  }\r
  \r
  // 中尺度波浪 - 中等速度移动\r
  for (float i = minOct + 4.0; i < minOct + 8.0; i++) {\r
    if (i > maxOct) break;\r
    float freq = pow(2.0, i);\r
    vec2 offset = vec2(time * (mainSpeed + detailSpeed) * 0.15 * sin(i),\r
                       time * (mainSpeed + detailSpeed) * 0.17 * cos(i * 0.7));\r
    float octaveValue = octave(uv + offset, freq);\r
    float weight = 1.0 / pow(2.0, 1.0 + i - minOct);\r
    noise += (octaveValue + 0.5) * weight * amp * 0.8;\r
    totalAmp += weight * amp * 0.8;\r
  }\r
  \r
  // 小尺度波浪 - 较快移动\r
  for (float i = minOct + 8.0; i <= maxOct; i++) {\r
    if (i > maxOct) break;\r
    float freq = pow(2.0, i);\r
    vec2 offset = vec2(time * detailSpeed * 0.2 * sin(i * 1.3),\r
                       time * detailSpeed * 0.25 * cos(i * 0.9));\r
    float octaveValue = octave(uv + offset, freq);\r
    float weight = 1.0 / pow(2.0, 1.0 + i - minOct);\r
    noise += (octaveValue + 0.5) * weight * amp * 0.5;\r
    totalAmp += weight * amp * 0.5;\r
  }\r
  \r
  return noise / totalAmp;\r
}\r
\r
void main() {\r
  // 使用position变量为了不改变原始的position\r
  vec3 pos = position;\r
  \r
  // 添加随机偏移，避免格子状外观\r
  float randomOffset = sin(position.x * 123.4 + position.z * 97.12) * 0.01 + \r
                      cos(position.z * 89.3 + position.x * 67.7) * 0.01;\r
  \r
  // 应用波浪高度 - 对uv坐标进行缩放以控制波浪大小\r
  float scale = 0.1;\r
  vec2 noiseCoord = pos.xz * scale;\r
  \r
  // 生成波浪高度\r
  float waves = perlinNoise(noiseCoord, iterationsVertex);\r
  \r
  // 添加非对称性，使波浪看起来更自然\r
  float asymmetry = sin(pos.x * 0.1 + pos.z * 0.2 + time * 0.3) * 0.05;\r
  \r
  // 最终高度计算\r
  pos.y += waves * waterDepth + randomOffset + asymmetry;\r
  \r
  // 传递位置变量\r
  vWorldPosition = (modelMatrix * vec4(pos, 1.0)).xyz;\r
  vPosition = pos;\r
  vUv = uv;\r
  \r
  // 设置投影位置\r
  gl_Position = projectionMatrix * viewMatrix * modelMatrix * vec4(pos, 1.0);\r
} `, Yc = `uniform float time;\r
uniform float waterDepth;\r
uniform float dragMult;\r
uniform float cameraHeight;\r
uniform int iterationsRaymarch;\r
uniform int iterationsNormal;\r
uniform vec2 resolution;\r
uniform vec3 sunDirection;\r
uniform vec3 skyColor;\r
uniform vec3 waterColor;\r
\r
varying vec2 vUv;\r
varying vec3 vPosition;\r
varying vec3 vWorldPosition;\r
varying vec3 vViewDirection;\r
\r
// 哈希函数，返回随机向量\r
vec2 hashVec(vec2 p) {\r
  return sin(58322.51 * sin(vec2(\r
    dot(p + 0.01, vec2(123.4, 234.5)),\r
    dot(p + 0.01, vec2(234.5, 345.6))\r
  )) + time * 2.4);\r
}\r
\r
// 生成一个柏林噪声的八度音阶\r
float octave(vec2 uv, float gridSize) {\r
  vec2 gridUv = fract(uv * gridSize);\r
  vec2 cellId = floor(uv * gridSize);\r
  vec2 smoothUv = smoothstep(0.0, 1.0, gridUv);\r
  \r
  return mix(\r
    mix(\r
      dot(hashVec(cellId + vec2(0.0, 0.0)), gridUv - vec2(0.0, 0.0)),\r
      dot(hashVec(cellId + vec2(1.0, 0.0)), gridUv - vec2(1.0, 0.0)), \r
      smoothUv.x\r
    ),\r
    mix(\r
      dot(hashVec(cellId + vec2(0.0, 1.0)), gridUv - vec2(0.0, 1.0)),\r
      dot(hashVec(cellId + vec2(1.0, 1.0)), gridUv - vec2(1.0, 1.0)), \r
      smoothUv.x\r
    ), \r
    smoothUv.y\r
  );\r
}\r
\r
// 生成多个八度音阶的柏林噪声\r
float perlinNoise(vec2 uv, int iterations) {\r
  float noise = 0.0;\r
  float minOct = 3.0;\r
  float maxOct = float(iterations);\r
  float amp = 1.0;\r
  float totalAmp = 0.0;\r
  \r
  // 添加移动速度变量，使不同比例的波浪有不同的速度\r
  float mainSpeed = 0.3;\r
  float detailSpeed = 0.7;\r
  \r
  // 大尺度波浪 - 较慢移动\r
  for (float i = minOct; i < minOct + 4.0; i++) {\r
    if (i > maxOct) break;\r
    float freq = pow(2.0, i);\r
    vec2 offset = vec2(time * mainSpeed * (0.1 + 0.05 * i), \r
                       time * mainSpeed * (0.12 + 0.03 * i));\r
    float octaveValue = octave(uv + offset, freq);\r
    float weight = 1.0 / pow(2.0, 1.0 + i - minOct);\r
    noise += (octaveValue + 0.5) * weight * amp;\r
    totalAmp += weight * amp;\r
  }\r
  \r
  // 中尺度波浪 - 中等速度移动\r
  for (float i = minOct + 4.0; i < minOct + 8.0; i++) {\r
    if (i > maxOct) break;\r
    float freq = pow(2.0, i);\r
    vec2 offset = vec2(time * (mainSpeed + detailSpeed) * 0.15 * sin(i),\r
                       time * (mainSpeed + detailSpeed) * 0.17 * cos(i * 0.7));\r
    float octaveValue = octave(uv + offset, freq);\r
    float weight = 1.0 / pow(2.0, 1.0 + i - minOct);\r
    noise += (octaveValue + 0.5) * weight * amp * 0.8;\r
    totalAmp += weight * amp * 0.8;\r
  }\r
  \r
  // 小尺度波浪 - 较快移动\r
  for (float i = minOct + 8.0; i <= maxOct; i++) {\r
    if (i > maxOct) break;\r
    float freq = pow(2.0, i);\r
    vec2 offset = vec2(time * detailSpeed * 0.2 * sin(i * 1.3),\r
                       time * detailSpeed * 0.25 * cos(i * 0.9));\r
    float octaveValue = octave(uv + offset, freq);\r
    float weight = 1.0 / pow(2.0, 1.0 + i - minOct);\r
    noise += (octaveValue + 0.5) * weight * amp * 0.5;\r
    totalAmp += weight * amp * 0.5;\r
  }\r
  \r
  return noise / totalAmp;\r
}\r
\r
// 通过计算该点和周围点的高度来计算法线\r
vec3 calculateNormal(vec2 pos, float epsilon, float depth) {\r
  // 获取当前位置高度\r
  float scale = 0.1;\r
  float height = perlinNoise(pos * scale, iterationsNormal) * depth;\r
  \r
  // 使用相邻像素计算法线，类似于Shadertoy中的方法\r
  float rightHeight = perlinNoise((pos + vec2(epsilon, 0.0)) * scale, iterationsNormal) * depth;\r
  float upHeight = perlinNoise((pos + vec2(0.0, epsilon)) * scale, iterationsNormal) * depth;\r
  \r
  // 计算法线 - 这是一个标准的法线计算方法，通过中心差分计算切线\r
  vec3 normal = normalize(cross(\r
      vec3(epsilon, rightHeight - height, 0.0),\r
      vec3(0.0, upHeight - height, epsilon)\r
  ));\r
  \r
  // 添加高频细节，使得法线看起来更加丰富\r
  vec2 detailPos = pos * 5.0; // 高频细节使用更小的尺度\r
  float detailScale = 0.02; // 高频细节的影响强度\r
  \r
  float detailHeight = perlinNoise(detailPos, max(4, iterationsNormal / 3));\r
  normal.xz += detailHeight * detailScale * vec2(\r
      sin(pos.x * 37.0 + time * 2.0),\r
      cos(pos.y * 41.0 + time * 1.7)\r
  );\r
  \r
  return normalize(normal);\r
}\r
\r
// 一些非常简单但快速的大气近似\r
vec3 extra_cheap_atmosphere(vec3 raydir, vec3 sundir) {\r
  float special_trick = 1.0 / (raydir.y * 1.0 + 0.1);\r
  float special_trick2 = 1.0 / (sundir.y * 11.0 + 1.0);\r
  float raysundt = pow(abs(dot(sundir, raydir)), 2.0);\r
  float sundt = pow(max(0.0, dot(sundir, raydir)), 8.0);\r
  float mymie = sundt * special_trick * 0.2;\r
  vec3 suncolor = mix(vec3(1.0), max(vec3(0.0), vec3(1.0) - vec3(5.5, 13.0, 22.4) / 22.4), special_trick2);\r
  vec3 bluesky= vec3(5.5, 13.0, 22.4) / 22.4 * suncolor;\r
  vec3 bluesky2 = max(vec3(0.0), bluesky - vec3(5.5, 13.0, 22.4) * 0.002 * (special_trick + -6.0 * sundir.y * sundir.y));\r
  bluesky2 *= special_trick * (0.24 + raysundt * 0.24);\r
  return bluesky2 * (1.0 + 1.0 * pow(1.0 - raydir.y, 3.0));\r
} \r
\r
// 获取给定方向的大气颜色 (用于反射)\r
vec3 getAtmosphere(vec3 dir) {\r
   return extra_cheap_atmosphere(dir, sunDirection) * 0.5;\r
}\r
\r
// 获取给定方向的太阳颜色 (用于反射)\r
float getSun(vec3 dir) { \r
  return pow(max(0.0, dot(dir, sunDirection)), 720.0) * 210.0;\r
}\r
\r
// ACES色调映射函数\r
vec3 aces_tonemap(vec3 color) {  \r
  mat3 m1 = mat3(\r
    0.59719, 0.07600, 0.02840,\r
    0.35458, 0.90834, 0.13383,\r
    0.04823, 0.01566, 0.83777\r
  );\r
  mat3 m2 = mat3(\r
    1.60475, -0.10208, -0.00327,\r
    -0.53108,  1.10813, -0.07276,\r
    -0.07367, -0.00605,  1.07602\r
  );\r
  vec3 v = m1 * color;  \r
  vec3 a = v * (v + 0.0245786) - 0.000090537;\r
  vec3 b = v * (0.983729 * v + 0.4329510) + 0.238081;\r
  return pow(clamp(m2 * (a / b), 0.0, 1.0), vec3(1.0 / 2.2));  \r
}\r
\r
void main() {\r
  // 使用当前片段的位置计算噪声坐标\r
  vec2 pos = vPosition.xz;\r
  \r
  // 计算法线，使用更小的epsilon获得更精细的法线\r
  vec3 N = calculateNormal(pos, 0.002, waterDepth);\r
  \r
  // 计算视线方向（从片段到相机）\r
  vec3 viewDir = normalize(cameraPosition - vWorldPosition);\r
  \r
  // 计算菲涅耳反射系数 - 使用真实的水面菲涅耳公式\r
  float F0 = 0.02; // 水的基础反射率\r
  float fresnel = F0 + (1.0 - F0) * pow(1.0 - max(0.0, dot(N, viewDir)), 5.0);\r
  \r
  // 增加一些随机变化，给菲涅耳增加一点变化\r
  fresnel += perlinNoise(pos * 20.0, 4) * 0.03;\r
  \r
  // 计算反射方向\r
  vec3 R = normalize(reflect(-viewDir, N));\r
  \r
  // 确保反射方向向上 - 防止反射地下\r
  R.y = max(0.0, R.y);\r
  \r
  // 计算反射的天空和太阳光\r
  vec3 reflection = getAtmosphere(R) + getSun(R) * 0.25;\r
  \r
  // 水体的基础颜色\r
  vec3 waterBaseColor = waterColor * 0.4;\r
  \r
  // 对水面进行着色，根据法线与光线方向的夹角计算光照\r
  float lambertian = max(dot(N, sunDirection), 0.0);\r
  float specular = pow(max(0.0, dot(normalize(sunDirection + viewDir), N)), 64.0);\r
  \r
  // 添加更多的不均匀性，使水面看起来更有层次\r
  float depthVariation = perlinNoise(pos * 0.5, 4) * 0.2 + 0.8;\r
  \r
  // 模拟次表面散射\r
  vec3 subSurfaceScattering = waterColor * 0.2 * max(0.0, dot(sunDirection, -N) * 0.5 + 0.5);\r
  \r
  // 混合漫反射光照、次表面散射和水体颜色\r
  vec3 diffuseColor = waterBaseColor * lambertian * depthVariation + subSurfaceScattering;\r
  \r
  // 根据菲涅耳因子混合反射和漫反射\r
  vec3 finalColor = mix(diffuseColor, reflection, fresnel);\r
  \r
  // 添加高光反射\r
  finalColor += vec3(1.0) * specular * 0.5;\r
  \r
  // 添加波峰高光 - 当法线y分量接近1时（波峰处）\r
  float waveCrest = pow(max(0.0, N.y - 0.7) * 3.33, 5.0) * 0.3;\r
  finalColor += vec3(0.9, 0.95, 1.0) * waveCrest;\r
  \r
  // 应用色调映射，增强对比度和亮度\r
  gl_FragColor = vec4(aces_tonemap(finalColor * 2.4), 1.0);\r
} `;
class Rd extends N {
  /**
      * 构造函数
      * @param {Object} [options] - 可选配置项
      * @param {Number} [options.waterDepth=1.0] - 水的深度
      * @param {Number} [options.dragMult=0.38] - 波浪拉动水面的强度
      * @param {Number} [options.cameraHeight=1.5] - 相机高度，影响计算
      * @param {Number} [options.iterationsVertex=16] - 顶点着色器中的波浪迭代次数
      * @param {Number} [options.iterationsRaymarch=8] - 光线行进时的波浪迭代次数
      * @param {Number} [options.iterationsNormal=24] - 计算法线时的波浪迭代次数
      * @param {Number|Color} [options.waterColor=0x0055ff] - 水的颜色
      * @param {Vector3} [options.sunDirection] - 太阳方向，默认从右上方照射
      */
  constructor(e = {}) {
    const t = e.waterDepth !== void 0 ? e.waterDepth : 1, r = e.dragMult !== void 0 ? e.dragMult : 0.38, n = e.cameraHeight !== void 0 ? e.cameraHeight : 1.5, i = e.iterationsVertex !== void 0 ? e.iterationsVertex : 16, s = e.iterationsRaymarch !== void 0 ? e.iterationsRaymarch : 8, o = e.iterationsNormal !== void 0 ? e.iterationsNormal : 24, l = e.waterColor !== void 0 ? e.waterColor : 22015, u = new T(0.5, 1, 0.5).normalize(), f = e.sunDirection !== void 0 ? e.sunDirection : u;
    super({
      name: "OceanMaterial",
      uniforms: {
        time: new a(0),
        waterDepth: new a(t),
        dragMult: new a(r),
        cameraHeight: new a(n),
        iterationsVertex: new a(i),
        iterationsRaymarch: new a(s),
        iterationsNormal: new a(o),
        resolution: new a(new g(1, 1)),
        sunDirection: new a(f),
        waterColor: new a(new y(l)),
        skyColor: new a(new y(8900331))
      },
      vertexShader: Xc,
      fragmentShader: Yc,
      transparent: !1
    });
  }
  /**
      * 设置水深
      * @param {Number} value - 水深度值
      */
  set waterDepth(e) {
    this.uniforms.waterDepth.value = e;
  }
  /**
      * 获取水深
      * @return {Number} 当前水深度值
      */
  get waterDepth() {
    return this.uniforms.waterDepth.value;
  }
  /**
      * 设置拖动系数
      * @param {Number} value - 拖动系数值
      */
  set dragMult(e) {
    this.uniforms.dragMult.value = e;
  }
  /**
      * 获取拖动系数
      * @return {Number} 当前拖动系数值
      */
  get dragMult() {
    return this.uniforms.dragMult.value;
  }
  /**
      * 设置相机高度
      * @param {Number} value - 相机高度值
      */
  set cameraHeight(e) {
    this.uniforms.cameraHeight.value = e;
  }
  /**
      * 获取相机高度
      * @return {Number} 当前相机高度值
      */
  get cameraHeight() {
    return this.uniforms.cameraHeight.value;
  }
  /**
      * 设置顶点着色器的波浪迭代次数
      * @param {Number} value - 迭代次数
      */
  set iterationsVertex(e) {
    this.uniforms.iterationsVertex.value = e;
  }
  /**
      * 获取顶点着色器的波浪迭代次数
      * @return {Number} 当前迭代次数
      */
  get iterationsVertex() {
    return this.uniforms.iterationsVertex.value;
  }
  /**
      * 设置光线行进时的波浪迭代次数
      * @param {Number} value - 迭代次数
      */
  set iterationsRaymarch(e) {
    this.uniforms.iterationsRaymarch.value = e;
  }
  /**
      * 获取光线行进时的波浪迭代次数
      * @return {Number} 当前迭代次数
      */
  get iterationsRaymarch() {
    return this.uniforms.iterationsRaymarch.value;
  }
  /**
      * 设置计算法线时的波浪迭代次数
      * @param {Number} value - 迭代次数
      */
  set iterationsNormal(e) {
    this.uniforms.iterationsNormal.value = e;
  }
  /**
      * 获取计算法线时的波浪迭代次数
      * @return {Number} 当前迭代次数
      */
  get iterationsNormal() {
    return this.uniforms.iterationsNormal.value;
  }
  /**
      * 设置太阳方向
      * @param {Vector3} value - 太阳方向向量
      */
  set sunDirection(e) {
    this.uniforms.sunDirection.value.copy(e.normalize());
  }
  /**
      * 获取太阳方向
      * @return {Vector3} 当前太阳方向向量
      */
  get sunDirection() {
    return this.uniforms.sunDirection.value;
  }
  /**
      * 设置水体颜色
      * @param {Color|Number|String} value - 水体颜色
      */
  set waterColor(e) {
    this.uniforms.waterColor.value.set(e);
  }
  /**
      * 获取水体颜色
      * @return {Color} 当前水体颜色
      */
  get waterColor() {
    return this.uniforms.waterColor.value;
  }
  /**
      * 设置分辨率
      * @param {Number} width - 宽度
      * @param {Number} height - 高度
      */
  setResolution(e, t) {
    this.uniforms.resolution.value.set(e, t);
  }
  /**
      * 更新材质
      * @param {Number} deltaTime - 时间差
      */
  update(e) {
    this.uniforms.time.value += e;
  }
}
const Zc = `uniform float roomScale;

varying vec2 vUv;
varying vec3 vViewDir;
varying vec3 vWorldPosition;
varying vec3 vNormal;
varying vec3 vPosition;

#ifdef USE_OBJECTSPACE
varying vec3 vObjectPosition;
#endif

void main() {
    vUv = uv;
    
    // 确保法线正确归一化 - 这对于平面旋转时至关重要
    vNormal = normalize(normalMatrix * normal);
    vPosition = position;
    
    // 计算世界坐标和方向
    vec4 worldPosition = modelMatrix * vec4(position, 1.0);
    vWorldPosition = worldPosition.xyz;
    
    // 计算从顶点到相机的方向（而不是相机到顶点）
    // 这确保了视线方向在所有角度都正确
    vec3 cameraToVertex = normalize(worldPosition.xyz - cameraPosition);
    
    #ifdef USE_OBJECTSPACE
        // 对象空间模式
        vObjectPosition = position * roomScale;
        
        // 获取对象空间视线方向
        vec4 objCameraPos = inverse(modelMatrix) * vec4(cameraPosition, 1.0);
        vViewDir = normalize(position - objCameraPos.xyz) * roomScale;
    #else
        // 切线空间模式 - 确保TBN矩阵正确构建
        vec3 T = normalize(normalMatrix * tangent.xyz);
        vec3 B = normalize(cross(vNormal, T)) * tangent.w;
        
        // 构建TBN矩阵
        mat3 TBN = mat3(T, B, vNormal);
        
        // 转换视线方向到切线空间
        vViewDir = normalize(TBN * cameraToVertex) * roomScale;
    #endif
    
    gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
} `, Kc = `uniform samplerCube roomCube;
uniform sampler2D roomMap;
uniform bool useSingleTexture;
uniform float roomScale;
uniform float roomVariety;
uniform bool fillFace;
uniform float roomDepth;
uniform float visualDepth;
uniform float roomAspect;
uniform bool flipTextureY;
uniform float roomsX;
uniform float roomsY;
uniform float gapSize;
uniform vec3 gapColor;
uniform float glassStrength;
uniform vec3 glassColor;
uniform float glassBlurStrength;
uniform bool enhancedDepth; // 新增：增强深度模式标志

varying vec2 vUv;
varying vec3 vViewDir;
varying vec3 vWorldPosition;
varying vec3 vNormal;
varying vec3 vPosition; // 接收顶点位置

#ifdef USE_OBJECTSPACE
varying vec3 vObjectPosition;
#endif

// 伪随机函数 - 根据输入坐标生成随机数
vec3 rand3(float n) {
    return fract(sin(n * vec3(12.9898, 78.233, 43.2316)) * 43758.5453);
}

// 伪随机函数 - 根据2维向量生成随机数
vec3 rand3(vec2 co) {
    return fract(sin(dot(co, vec2(12.9898, 78.233)) + vec3(0.0, 23.45, 78.67)) * 43758.5453);
}

// 从单张图片立方体贴图中采样
// 参数dir为方向向量，必须是已归一化的向量
vec4 sampleSingleTextureCube(sampler2D tex, vec3 dir) {
    // 找出哪个分量的绝对值最大，确定我们采样的是哪个面
    vec3 absDir = abs(dir);
    float maxVal = max(max(absDir.x, absDir.y), absDir.z);
    
    // 计算2D纹理坐标
    vec2 uv;
    
    // 根据传入的贴图图片布局调整采样坐标
    // 布局与原图匹配：
    // +-----+-----+-----+-----+
    // |     |  +Y |     |     |
    // |     |     |     |     |
    // +-----+-----+-----+-----+
    // | -X  |  +Z |  +X | -Z  |
    // |     |     |     |     |
    // +-----+-----+-----+-----+
    // |     |  -Y |     |     |
    // |     |     |     |     |
    // +-----+-----+-----+-----+
    
    float faceWidth = 0.25;  // 每个面宽度占整个贴图的1/4
    float faceHeight = 0.33333333; // 每个面高度占整个贴图的1/3
    
    if (maxVal == absDir.x) {
        // X轴面
        if (dir.x > 0.0) {
            // +X 面（右）
            uv = vec2(-dir.z, -dir.y) / abs(dir.x);
            // 将规范化的坐标映射到+X面位置
            uv = vec2(
                (uv.x * 0.5 + 0.5) * faceWidth + 2.0 * faceWidth,
                (uv.y * 0.5 + 0.5) * faceHeight + 1.0 * faceHeight
            );
        } else {
            // -X 面（左）
            uv = vec2(dir.z, -dir.y) / abs(dir.x);
            // 将规范化的坐标映射到-X面位置
            uv = vec2(
                (uv.x * 0.5 + 0.5) * faceWidth + 0.0 * faceWidth,
                (uv.y * 0.5 + 0.5) * faceHeight + 1.0 * faceHeight
            );
        }
    } else if (maxVal == absDir.y) {
        // Y轴面
        if (dir.y > 0.0) {
            // +Y 面（上）
            uv = vec2(dir.x, dir.z) / abs(dir.y);
            // 将规范化的坐标映射到+Y面位置
            uv = vec2(
                (uv.x * 0.5 + 0.5) * faceWidth + 1.0 * faceWidth,
                (uv.y * 0.5 + 0.5) * faceHeight + 0.0 * faceHeight
            );
        } else {
            // -Y 面（下）
            uv = vec2(dir.x, -dir.z) / abs(dir.y);
            // 将规范化的坐标映射到-Y面位置
            uv = vec2(
                (uv.x * 0.5 + 0.5) * faceWidth + 1.0 * faceWidth,
                (uv.y * 0.5 + 0.5) * faceHeight + 2.0 * faceHeight
            );
        }
    } else {
        // Z轴面
        if (dir.z > 0.0) {
            // +Z 面（前）
            uv = vec2(dir.x, -dir.y) / abs(dir.z);
            // 将规范化的坐标映射到+Z面位置
            uv = vec2(
                (uv.x * 0.5 + 0.5) * faceWidth + 1.0 * faceWidth,
                (uv.y * 0.5 + 0.5) * faceHeight + 1.0 * faceHeight
            );
        } else {
            // -Z 面（后）
            uv = vec2(-dir.x, -dir.y) / abs(dir.z);
            // 将规范化的坐标映射到-Z面位置
            uv = vec2(
                (uv.x * 0.5 + 0.5) * faceWidth + 3.0 * faceWidth,
                (uv.y * 0.5 + 0.5) * faceHeight + 1.0 * faceHeight
            );
        }
    }
    
    // 处理贴图Y轴翻转问题
    // 默认情况下，Three.js的贴图是上下翻转的，这里我们在着色器中处理
    if (flipTextureY) {
        // Y坐标翻转：将它从顶部到底部的坐标转成从底部到顶部
        uv.y = 1.0 - uv.y;
    }
    
    // 返回采样结果
    return texture2D(tex, uv);
}

// 计算玻璃反射效果
vec3 calculateGlassEffect(vec3 baseColor, vec3 normal, vec3 viewDir) {
    // 法线和视线方向都需要归一化
    vec3 N = normalize(normal);
    vec3 V = normalize(-viewDir);
    
    // 计算菲涅尔效应 - 使用改进的Schlick近似
    float cosTheta = max(0.0, dot(N, V));
    // 基础反射率 - 即使在0度角也有一些反射
    float baseReflectivity = 0.04; 
    float fresnel = baseReflectivity + (1.0 - baseReflectivity) * pow(1.0 - cosTheta, 5.0);
    
    // 确保反射效果明显可见
    fresnel = max(fresnel, 0.1); 
    
    // 基于菲涅尔和反射强度混合玻璃颜色
    float reflectionFactor = fresnel * glassStrength;
    
    // 添加基于视角的高光效果 - 降低高光指数使效果更柔和
    vec3 lightDir = normalize(vec3(0.5, 1.0, 0.5)); // 假设的光源方向
    float specular = pow(max(0.0, dot(reflect(-lightDir, N), V)), 16.0) * glassStrength;
    
    // 防止玻璃颜色太暗导致画面变黑
    // 对白色(1,1,1)和接近白色的颜色进行特殊处理，确保高亮效果
    vec3 safeGlassColor;
    float colorBrightness = dot(glassColor, vec3(0.299, 0.587, 0.114));
    
    if (colorBrightness > 0.9) {
        // 对于接近白色的颜色，保持其亮度但略微增加高光
        safeGlassColor = glassColor + vec3(0.1);
    } else {
        // 对其他颜色设置最低亮度下限
        safeGlassColor = max(glassColor, vec3(0.3));
    }
    
    // 混合反射和基础颜色 - 使用加性混合保持亮度
    vec3 reflection = safeGlassColor * (reflectionFactor + specular);
    vec3 finalColor = mix(baseColor, baseColor + reflection, glassStrength);
    
    // 亮度保护 - 防止颜色过暗
    float originalLuminance = dot(baseColor, vec3(0.299, 0.587, 0.114));
    float newLuminance = dot(finalColor, vec3(0.299, 0.587, 0.114));
    
    // 如果亮度降低超过30%，进行亮度补偿
    if (newLuminance < originalLuminance * 0.7) {
        finalColor = finalColor * (originalLuminance * 0.7 / max(newLuminance, 0.001));
    }
    
    return finalColor;
}

// 对立方体贴图应用模糊效果
vec4 sampleCubeWithBlur(samplerCube cube, vec3 dir, float blurStrength) {
    if (blurStrength <= 0.001) {
        return textureCube(cube, dir);
    }

    // 创建正交基向量，用于在球面上采样
    vec3 tangent, bitangent;
    
    // 找到与dir不平行的向量用来构建正交基
    if (abs(dir.x) < 0.8) {
        tangent = normalize(cross(vec3(1.0, 0.0, 0.0), dir));
    } else {
        tangent = normalize(cross(vec3(0.0, 1.0, 0.0), dir));
    }
    
    bitangent = normalize(cross(dir, tangent));
    
    // 采样点数 - 越多越平滑但性能越低
    const int numSamples = 5;
    
    // 根据模糊强度计算采样范围，最大可达0.15弧度
    float radius = blurStrength * 0.15;
    
    vec4 result = vec4(0.0);
    float weight = 0.0;
    
    // 中心点权重最高
    result += textureCube(cube, dir) * 1.0;
    weight += 1.0;
    
    // 环绕中心点采样
    for (int i = 0; i < numSamples; i++) {
        // 计算随机偏移角度
        float angle = float(i) / float(numSamples) * 2.0 * 3.14159265359;
        float dist = radius * (0.5 + float(i % 3) / 3.0);
        
        vec3 offset = tangent * cos(angle) * dist + bitangent * sin(angle) * dist;
        vec3 sampleDir = normalize(dir + offset);
        
        // 权重根据距离中心的远近递减
        float sampleWeight = 1.0 - dist / radius * 0.5;
        result += textureCube(cube, sampleDir) * sampleWeight;
        weight += sampleWeight;
    }
    
    // 标准化结果
    return result / weight;
}

// 对单张图片立方体贴图应用模糊效果
vec4 sampleSingleTextureWithBlur(sampler2D tex, vec3 dir, float blurStrength) {
    if (blurStrength <= 0.001) {
        return sampleSingleTextureCube(tex, dir);
    }

    // 创建正交基向量，用于在球面上采样
    vec3 tangent, bitangent;
    
    // 找到与dir不平行的向量用来构建正交基
    if (abs(dir.x) < 0.8) {
        tangent = normalize(cross(vec3(1.0, 0.0, 0.0), dir));
    } else {
        tangent = normalize(cross(vec3(0.0, 1.0, 0.0), dir));
    }
    
    bitangent = normalize(cross(dir, tangent));
    
    // 采样点数 - 越多越平滑但性能越低
    const int numSamples = 5;
    
    // 根据模糊强度计算采样范围，最大可达0.15弧度
    float radius = blurStrength * 0.15;
    
    vec4 result = vec4(0.0);
    float weight = 0.0;
    
    // 中心点权重最高
    result += sampleSingleTextureCube(tex, dir) * 1.0;
    weight += 1.0;
    
    // 环绕中心点采样
    for (int i = 0; i < numSamples; i++) {
        // 计算随机偏移角度
        float angle = float(i) / float(numSamples) * 2.0 * 3.14159265359;
        float dist = radius * (0.5 + float(i % 3) / 3.0);
        
        vec3 offset = tangent * cos(angle) * dist + bitangent * sin(angle) * dist;
        vec3 sampleDir = normalize(dir + offset);
        
        // 权重根据距离中心的远近递减
        float sampleWeight = 1.0 - dist / radius * 0.5;
        result += sampleSingleTextureCube(tex, sampleDir) * sampleWeight;
        weight += sampleWeight;
    }
    
    // 标准化结果
    return result / weight;
}

void main() {
    vec3 viewDir = normalize(vViewDir);
    vec3 pos;
    
    #ifdef FILL_FACE
        // ===== 在fillFace模式下处理多房间分布 =====
        
        // 确定当前所在的面
        vec3 absNormal = abs(vNormal);
        float maxComp = max(max(absNormal.x, absNormal.y), absNormal.z);
        
        // 初始化立方体表面点 - 这个点将在立方体表面
        vec3 cubePos;
        vec3 normal = normalize(vNormal);
        
        // 创建一个正交坐标系来处理任何角度的平面
        vec3 upVector = abs(normal.y) > 0.9 ? vec3(1.0, 0.0, 0.0) : vec3(0.0, 1.0, 0.0);
        vec3 rightVector = normalize(cross(upVector, normal));
        vec3 upVectorOrtho = normalize(cross(normal, rightVector));
        
        // 计算多房间布局
        // 将UV坐标从[0,1]映射到[-1,1]
        float u = vUv.x * 2.0 - 1.0; 
        float v = vUv.y * 2.0 - 1.0;
        
        // 如果需要绘制多个房间
        bool isInGap = false;
        vec2 roomUVOrig = vec2(u, v);
        
        // 用于存储房间ID，以便生成随机值
        vec2 roomId = vec2(0.0, 0.0);
        
        if ((roomsX > 1.0 || roomsY > 1.0) && gapSize > 0.0) {
            // 计算单个房间的大小
            float roomWidth = 2.0 / roomsX;
            float roomHeight = 2.0 / roomsY;
            
            // 计算当前所在的房间索引 (0-based)
            roomId = floor((vec2(u, v) + 1.0) / vec2(roomWidth, roomHeight));
            
            // 检查是否在最外层边缘
            bool isOnLeftEdge = roomId.x == 0.0;
            bool isOnRightEdge = roomId.x == roomsX - 1.0;
            bool isOnTopEdge = roomId.y == 0.0;
            bool isOnBottomEdge = roomId.y == roomsY - 1.0;
            
            // 计算在整个UV空间中，当前房间的左右上下边界
            float leftBound = -1.0 + roomId.x * roomWidth;
            float rightBound = -1.0 + (roomId.x + 1.0) * roomWidth;
            float topBound = -1.0 + roomId.y * roomHeight;
            float bottomBound = -1.0 + (roomId.y + 1.0) * roomHeight;
            
            // 计算每个房间的间隔区域宽度
            float gapWidthHalf = roomWidth * gapSize * 0.5;
            float gapHeightHalf = roomHeight * gapSize * 0.5;
            
            // 只有不在边缘的右侧和下侧才需要添加间隔
            bool inRightGap = !isOnRightEdge && u > (rightBound - gapWidthHalf);
            bool inLeftGap = !isOnLeftEdge && u < (leftBound + gapWidthHalf);
            bool inTopGap = !isOnTopEdge && v < (topBound + gapHeightHalf);
            bool inBottomGap = !isOnBottomEdge && v > (bottomBound - gapHeightHalf);
            
            // 判断是否在间隔区域
            isInGap = inRightGap || inLeftGap || inTopGap || inBottomGap;
            
            // 计算房间中心点
            vec2 roomCenter = vec2(
                leftBound + roomWidth * 0.5,
                topBound + roomHeight * 0.5
            );
            
            // 计算房间内的局部坐标 (不考虑间隔)
            u = (u - roomCenter.x) / (roomWidth * 0.5 - gapWidthHalf) * roomWidth * 0.5;
            v = (v - roomCenter.y) / (roomHeight * 0.5 - gapHeightHalf) * roomHeight * 0.5;
            
            // 限制局部坐标范围
            u = clamp(u, -1.0, 1.0);
            v = clamp(v, -1.0, 1.0);
        } else if (roomsX > 1.0 || roomsY > 1.0) {
            // 没有间隔但有多个房间的情况
            float roomWidth = 2.0 / roomsX;
            float roomHeight = 2.0 / roomsY;
            
            // 计算当前所在的房间索引 (0-based)
            roomId = floor((vec2(u, v) + 1.0) / vec2(roomWidth, roomHeight));
            
            // 计算房间中心点
            vec2 roomCenter = vec2(
                -1.0 + (roomId.x + 0.5) * roomWidth,
                -1.0 + (roomId.y + 0.5) * roomHeight
            );
            
            // 计算房间内的局部坐标
            u = (u - roomCenter.x) / (roomWidth * 0.5) * 1.0;
            v = (v - roomCenter.y) / (roomHeight * 0.5) * 1.0;
            
            // 限制局部坐标范围
            u = clamp(u, -1.0, 1.0);
            v = clamp(v, -1.0, 1.0);
        }
        
        // 如果在间隔内，直接返回间隔颜色
        if (isInGap) {
            gl_FragColor = vec4(gapColor, 1.0);
            return;
        }
        
        // 应用房间纵横比
        u *= roomAspect;
        
        // 计算立方体表面上的点
        vec3 planePos = rightVector * u + upVectorOrtho * v;
        
        // 沿着法线方向突出到立方体表面
        float cubeSurfaceOffset = 1.0;
        cubePos = normal * cubeSurfaceOffset + planePos;
        
        // 标准化到单位立方体大小
        cubePos = normalize(cubePos);
        
        // 计算光线方向和相交
        vec3 rayDir = normalize(viewDir);
        vec3 invRayDir = 1.0 / rayDir;  
        vec3 tMax = (sign(rayDir) - cubePos) * invRayDir;
        float t = min(min(tMax.x, tMax.y), tMax.z);
        
        // 保持较小的roomDepth值以避免异常球形
        t = min(t, roomDepth);
        
        // 使用视觉深度参数来创建深度感
        float visualScale = visualDepth / max(roomDepth, 0.001);
        
        // 计算最终的采样位置
        pos = cubePos + rayDir * t;
        
        // 应用视觉深度缩放，但只缩放与表面法线垂直的分量
        vec3 normalComponent = normal * dot(pos - cubePos, normal);
        vec3 tangentComponent = (pos - cubePos) - normalComponent;
        pos = cubePos + normalComponent + tangentComponent * visualScale;
        
        // 为每个房间应用随机朝向（仅在需要变化时）
        if (roomVariety > 0.0 && (roomsX > 1.0 || roomsY > 1.0)) {
            // 使用房间ID为种子生成随机值
            vec3 r = rand3(roomId);
            
            // 基于roomVariety参数控制随机程度
            float variety = clamp(roomVariety, 0.0, 1.0);
            
            // 随机选择四个侧面之一作为房间正面（-X, +Z, +X, -Z）
            // 我们将随机值映射到0-3之间，对应四个侧面
            float faceChoice = floor(r.x * 4.0);
            
            // 只有当随机值小于variety时才应用随机朝向，以便可以控制随机程度
            if (r.y < variety) {
                // 创建一个临时变量存储原始pos
                vec3 origPos = pos;
                
                if (faceChoice < 0.5) {
                    // -X 面（左侧）作为正面
                    // x=-z, y=y, z=x
                    pos = vec3(-origPos.z, origPos.y, origPos.x);
                } else if (faceChoice < 1.5) {
                    // +Z 面（前面）作为正面
                    // 保持不变
                    // pos = origPos;
                } else if (faceChoice < 2.5) {
                    // +X 面（右侧）作为正面
                    // x=z, y=y, z=-x
                    pos = vec3(origPos.z, origPos.y, -origPos.x);
                } else {
                    // -Z 面（后面）作为正面
                    // x=-x, y=y, z=-z
                    pos = vec3(-origPos.x, origPos.y, -origPos.z);
                }
            }
        }
    #else
        // ===== 标准模式：多个房间 =====
        vec3 roomUV;
        
        #ifdef USE_OBJECTSPACE
            // 对象空间模式的房间坐标
            roomUV = vObjectPosition;
        #else
            // 切线空间模式 - 使用UV坐标
            roomUV = vec3(vUv * roomScale, 0.0);
        #endif
        
        // 获取房间单元坐标
        vec3 roomCell = floor(roomUV);
        roomUV = fract(roomUV);
        
        // 计算深度增强因子（新增）
        vec3 adjustedViewDir = viewDir;
        if(enhancedDepth) {
            // 增强的深度计算 - 参考用户提供的代码
            // 将[0,1]范围的roomDepth重映射到[+inf,0]
            // 如果roomDepth = 0 -> depthScale = 0（无限深度房间）
            // 如果roomDepth = 0.5 -> depthScale = 1（正常深度）
            // 如果roomDepth = 1 -> depthScale = +inf（零体积房间）
            float depthScale = 1.0 / (1.0 - roomDepth) - 1.0;
            
            // 只缩放视线的z分量，保持x,y不变
            adjustedViewDir.z *= depthScale;
        }
        
        // 将房间UV从[0,1]映射到[-1,1]范围
        // 使用增强的初始z值(新增)
        pos = enhancedDepth ? 
              vec3(roomUV * 2.0 - 1.0, 1.0) :  // 增强模式：z初始值为1.0
              roomUV * 2.0 - 1.0;             // 原始模式：所有分量从-1到1
        
        // 光线追踪参数
        vec3 id = 1.0 / adjustedViewDir;
        vec3 k = abs(id) - pos * id;
        float kMin = min(min(k.x, k.y), k.z);
        
        // 计算光线与房间内部的相交点
        pos += kMin * adjustedViewDir;
        
        // 为每个房间生成随机旋转和翻转
        #ifdef USE_OBJECTSPACE
            vec3 r = rand3(roomCell.x + roomCell.y + roomCell.z);
        #else
            vec3 r = rand3(roomCell.xy);
        #endif
        
        // 添加随机变换
        if(roomVariety > 0.0) {
            // 基于roomVariety参数控制随机程度
            float variety = clamp(roomVariety, 0.0, 1.0);
            
            // 随机选择四个侧面之一作为房间正面（-X, +Z, +X, -Z）
            // 我们将随机值映射到0-3之间，对应四个侧面
            float faceChoice = floor(r.x * 4.0);
            
            // 只有当随机值小于variety时才应用随机朝向，以便可以控制随机程度
            if (r.y < variety) {
                // 创建一个临时变量存储原始pos
                vec3 origPos = pos;
                
                if (faceChoice < 0.5) {
                    // -X 面（左侧）作为正面
                    // x=-z, y=y, z=x
                    pos = vec3(-origPos.z, origPos.y, origPos.x);
                } else if (faceChoice < 1.5) {
                    // +Z 面（前面）作为正面
                    // 保持不变
                    // pos = origPos;
                } else if (faceChoice < 2.5) {
                    // +X 面（右侧）作为正面
                    // x=z, y=y, z=-x
                    pos = vec3(origPos.z, origPos.y, -origPos.x);
                } else {
                    // -Z 面（后面）作为正面
                    // x=-x, y=y, z=-z
                    pos = vec3(-origPos.x, origPos.y, -origPos.z);
                }
            }
        }
    #endif
    
    // 采样获取房间内部颜色
    vec4 roomColor;
    
    if (useSingleTexture) {
        // 使用单张图片立方体贴图，应用模糊效果
        roomColor = sampleSingleTextureWithBlur(roomMap, pos, glassBlurStrength);
    } else {
        // 使用立方体贴图，应用模糊效果
        roomColor = sampleCubeWithBlur(roomCube, pos, glassBlurStrength);
    }
    
    // 应用玻璃效果
    vec3 finalColor = roomColor.rgb;
    if (glassStrength > 0.0) {
        vec3 normal = normalize(vNormal);
        finalColor = calculateGlassEffect(finalColor, normal, viewDir);
        
        // 确保颜色亮度不会大幅度降低
        float originalLuminance = dot(roomColor.rgb, vec3(0.299, 0.587, 0.114));
        float newLuminance = dot(finalColor, vec3(0.299, 0.587, 0.114));
        if (newLuminance < originalLuminance * 0.7) {
            // 如果亮度降低太多，进行补偿
            finalColor = finalColor * (originalLuminance * 0.7 / max(newLuminance, 0.001));
        }
    }
    
    gl_FragColor = vec4(finalColor, 1.0);
} `;
class Nd extends N {
  /**
   * 构造函数
   * @param {Object} [options] - 可选配置项
   * @param {Boolean} [options.useObjectSpace=false] - 是否使用对象空间坐标（否则使用切线空间）
   * @param {Number} [options.roomScale=1.0] - 房间尺寸缩放
   * @param {Number} [options.roomVariety=0.5] - 房间随机变化程度
   * @param {Boolean} [options.fillFace=false] - 是否让房间填满整个面（适用于长方形）
   * @param {Number} [options.roomDepth=0.02] - 房间实际深度 (fillFace模式下有效，保持低值避免异常)
   * @param {Number} [options.visualDepth=1.2] - 房间视觉深度 (控制深度感，不会产生异常)
   * @param {Number} [options.roomAspect=1.0] - 房间纵横比 (fillFace模式下有效)
   * @param {Boolean} [options.flipTextureY=true] - 是否在着色器中翻转贴图Y轴 (避免在外部设置flipY=false)
   * @param {Number} [options.roomsX=1] - 横向房间数量
   * @param {Number} [options.roomsY=1] - 纵向房间数量
   * @param {Number} [options.gapSize=0.05] - 房间间隔大小
   * @param {Number|Color} [options.gapColor=0x000000] - 房间间隔颜色
   * @param {Number} [options.glassStrength=0.25] - 前景玻璃反射强度 (0.0-1.0)
   * @param {Number|Color} [options.glassColor=0x88CCFF] - 前景玻璃颜色
   * @param {Number} [options.glassBlurStrength=0.0] - 玻璃模糊强度 (0.0-1.0)
   * @param {Boolean} [options.enhancedDepth=false] - 是否启用增强深度模式 (适用于非fillFace模式)
   */
  constructor(e = {}) {
    const t = e.useObjectSpace !== void 0 ? e.useObjectSpace : !0, r = e.roomScale !== void 0 ? e.roomScale : 1, n = e.roomVariety !== void 0 ? e.roomVariety : 0.5, i = e.fillFace !== void 0 ? e.fillFace : !0, s = e.roomDepth !== void 0 ? e.roomDepth : 0.02, o = e.visualDepth !== void 0 ? e.visualDepth : 1.2, l = e.roomAspect !== void 0 ? e.roomAspect : 1, u = e.flipTextureY !== void 0 ? e.flipTextureY : !0, f = e.roomsX !== void 0 ? e.roomsX : 1, h = e.roomsY !== void 0 ? e.roomsY : 1, m = e.gapSize !== void 0 ? e.gapSize : 0.05, p = e.gapColor !== void 0 ? e.gapColor : 0, d = e.glassStrength !== void 0 ? e.glassStrength : 0.25, v = e.glassColor !== void 0 ? e.glassColor : 16777215, x = e.glassBlurStrength !== void 0 ? e.glassBlurStrength : 0, w = e.enhancedDepth !== void 0 ? e.enhancedDepth : !1;
    super({
      name: "InteriorMappingMaterial",
      uniforms: {
        roomCube: new a(null),
        roomMap: new a(null),
        // 单张图片立方体贴图
        useSingleTexture: new a(!0),
        // 是否使用单张图片贴图
        useObjectSpace: new a(t),
        roomScale: new a(r),
        roomVariety: new a(n),
        fillFace: new a(i),
        // 是否让房间填满面
        roomDepth: new a(s),
        // 房间实际深度 (fillFace模式下有效)
        visualDepth: new a(o),
        // 房间视觉深度 (控制深度感)
        roomAspect: new a(l),
        // 房间纵横比 (fillFace模式下有效)
        flipTextureY: new a(u),
        // 是否翻转贴图Y轴
        roomsX: new a(f),
        // 横向房间数量
        roomsY: new a(h),
        // 纵向房间数量
        gapSize: new a(m),
        // 房间间隔大小
        gapColor: new a(new y(p)),
        // 房间间隔颜色
        glassStrength: new a(d),
        // 前景玻璃反射强度
        glassColor: new a(new y(v)),
        // 前景玻璃颜色
        glassBlurStrength: new a(x),
        // 玻璃模糊强度
        enhancedDepth: new a(w)
        // 是否启用增强深度模式
      },
      vertexShader: Zc,
      fragmentShader: Kc,
      defines: {
        USE_OBJECTSPACE: t,
        USE_TANGENT: !0,
        FILL_FACE: i
      }
    }), this.vertexTangents = !0;
  }
  /**
   * 设置房间立方体贴图
   * @param {CubeTexture} value - 房间内部的立方体贴图
   */
  set roomCube(e) {
    this.uniforms.roomCube.value = e, this._updateTextureMode();
  }
  /**
   * 获取房间立方体贴图
   * @return {CubeTexture} 房间立方体贴图
   */
  get roomCube() {
    return this.uniforms.roomCube.value;
  }
  /**
   * 设置房间单张图片立方体贴图
   * @param {Texture} value - 包含六个面的单张图片贴图
   */
  set roomMap(e) {
    this.uniforms.roomMap.value = e, this._updateTextureMode();
  }
  /**
   * 获取房间单张图片立方体贴图
   * @return {Texture} 单张图片立方体贴图
   */
  get roomMap() {
    return this.uniforms.roomMap.value;
  }
  /**
   * 更新纹理模式
   * 根据提供的纹理确定使用哪种模式
   * @private
   */
  _updateTextureMode() {
    const e = this.uniforms.roomCube.value !== null, t = this.uniforms.roomMap.value !== null;
    this.uniforms.useSingleTexture.value = !e && t, !e && !t && console.warn("InteriorMappingMaterial: 未提供任何房间贴图，请设置roomCube或roomMap"), this.defines.USE_SINGLE_TEXTURE !== this.uniforms.useSingleTexture.value && (this.defines.USE_SINGLE_TEXTURE = this.uniforms.useSingleTexture.value, this.needsUpdate = !0);
  }
  /**
   * 设置是否使用对象空间坐标
   * @param {Boolean} value - 是否使用对象空间（否则使用切线空间）
   */
  set useObjectSpace(e) {
    this.uniforms.useObjectSpace.value = e, this.defines.USE_OBJECTSPACE = e, this.needsUpdate = !0;
  }
  /**
   * 设置房间尺寸缩放
   * @param {Number} value - 房间尺寸缩放因子
   */
  set roomScale(e) {
    this.uniforms.roomScale.value = e;
  }
  /**
   * 获取房间尺寸缩放
   * @return {Number} 房间尺寸缩放因子
   */
  get roomScale() {
    return this.uniforms.roomScale.value;
  }
  /**
   * 设置房间随机变化程度
   * @param {Number} value - 房间随机变化程度 (0-1)
   */
  set roomVariety(e) {
    this.uniforms.roomVariety.value = e;
  }
  /**
   * 获取房间随机变化程度
   * @return {Number} 房间随机变化程度 (0-1)
   */
  get roomVariety() {
    return this.uniforms.roomVariety.value;
  }
  /**
   * 设置是否让房间填满整个面
   * @param {Boolean} value - 是否让房间填满面
   */
  set fillFace(e) {
    this.uniforms.fillFace.value = e, this.defines.FILL_FACE !== e && (this.defines.FILL_FACE = e, this.needsUpdate = !0);
  }
  /**
   * 获取是否让房间填满整个面
   * @return {Boolean} 是否让房间填满面
   */
  get fillFace() {
    return this.uniforms.fillFace.value;
  }
  /**
   * 设置房间深度
   * @param {Number} value - 房间深度值
   */
  set roomDepth(e) {
    this.uniforms.roomDepth.value = e;
  }
  /**
   * 获取房间深度
   * @return {Number} 房间深度值
   */
  get roomDepth() {
    return this.uniforms.roomDepth.value;
  }
  /**
   * 设置房间视觉深度
   * @param {Number} value - 房间视觉深度值
   */
  set visualDepth(e) {
    this.uniforms.visualDepth.value = e;
  }
  /**
   * 获取房间视觉深度
   * @return {Number} 房间视觉深度值
   */
  get visualDepth() {
    return this.uniforms.visualDepth.value;
  }
  /**
   * 设置房间纵横比
   * @param {Number} value - 房间纵横比值
   */
  set roomAspect(e) {
    this.uniforms.roomAspect.value = e;
  }
  /**
   * 获取房间纵横比
   * @return {Number} 房间纵横比值
   */
  get roomAspect() {
    return this.uniforms.roomAspect.value;
  }
  /**
   * 设置是否翻转贴图Y轴
   * @param {Boolean} value - 是否翻转贴图Y轴
   */
  set flipTextureY(e) {
    this.uniforms.flipTextureY.value = e;
  }
  /**
   * 获取是否翻转贴图Y轴
   * @return {Boolean} 是否翻转贴图Y轴
   */
  get flipTextureY() {
    return this.uniforms.flipTextureY.value;
  }
  /**
   * 设置横向房间数量
   * @param {Number} value - 横向房间数量
   */
  set roomsX(e) {
    this.uniforms.roomsX.value = e;
  }
  /**
   * 获取横向房间数量
   * @return {Number} 横向房间数量
   */
  get roomsX() {
    return this.uniforms.roomsX.value;
  }
  /**
   * 设置纵向房间数量
   * @param {Number} value - 纵向房间数量
   */
  set roomsY(e) {
    this.uniforms.roomsY.value = e;
  }
  /**
   * 获取纵向房间数量
   * @return {Number} 纵向房间数量
   */
  get roomsY() {
    return this.uniforms.roomsY.value;
  }
  /**
   * 设置房间间隔大小
   * @param {Number} value - 房间间隔大小
   */
  set gapSize(e) {
    this.uniforms.gapSize.value = e;
  }
  /**
   * 获取房间间隔大小
   * @return {Number} 房间间隔大小
   */
  get gapSize() {
    return this.uniforms.gapSize.value;
  }
  /**
   * 设置房间间隔颜色
   * @param {Color|Number} value - 房间间隔颜色
   */
  set gapColor(e) {
    typeof e == "number" ? this.uniforms.gapColor.value.setHex(e) : this.uniforms.gapColor.value.copy(e);
  }
  /**
   * 获取房间间隔颜色
   * @return {Color} 房间间隔颜色
   */
  get gapColor() {
    return this.uniforms.gapColor.value;
  }
  /**
   * 设置前景玻璃反射强度
   * @param {Number} value - 前景玻璃反射强度 (0.0-1.0)
   */
  set glassStrength(e) {
    this.uniforms.glassStrength.value = Math.max(0, Math.min(1, e));
  }
  /**
   * 获取前景玻璃反射强度
   * @return {Number} 前景玻璃反射强度
   */
  get glassStrength() {
    return this.uniforms.glassStrength.value;
  }
  /**
   * 设置前景玻璃颜色
   * @param {Color|Number|String} value - 前景玻璃颜色
   */
  set glassColor(e) {
    typeof e == "number" ? this.uniforms.glassColor.value.setHex(e) : typeof e == "string" ? this.uniforms.glassColor.value.set(e) : this.uniforms.glassColor.value.copy(e), this.uniformsNeedUpdate = !0;
  }
  /**
   * 获取前景玻璃颜色
   * @return {Color} 前景玻璃颜色
   */
  get glassColor() {
    return this.uniforms.glassColor.value;
  }
  /**
   * 设置玻璃模糊强度
   * @param {Number} value - 玻璃模糊强度 (0.0-1.0)
   */
  set glassBlurStrength(e) {
    this.uniforms.glassBlurStrength.value = Math.max(0, Math.min(1, e));
  }
  /**
   * 获取玻璃模糊强度
   * @return {Number} 玻璃模糊强度
   */
  get glassBlurStrength() {
    return this.uniforms.glassBlurStrength.value;
  }
  /**
   * 设置是否启用增强深度模式
   * @param {Boolean} value - 是否启用增强深度模式
   */
  set enhancedDepth(e) {
    this.uniforms.enhancedDepth.value = e;
  }
  /**
   * 获取是否启用增强深度模式
   * @return {Boolean} 是否启用增强深度模式
   */
  get enhancedDepth() {
    return this.uniforms.enhancedDepth.value;
  }
  /**
   * 更新材质
   * @param {Number} deltaTime - 时间差
   * @param {Matrix4} modelMatrix - 模型矩阵
   */
  update(e, t) {
  }
}
const Qc = `uniform float roomScale;

// 传递到片段着色器的变量
varying vec2 vUv;
varying vec3 vViewDir;
varying vec3 vNormal;
varying vec3 vWorldPosition;

void main() {
    // 计算顶点位置
    vec4 worldPosition = modelMatrix * vec4(position, 1.0);
    vWorldPosition = worldPosition.xyz;
    
    // 传递UV坐标
    vUv = uv;
    
    // 计算法线
    vNormal = normalMatrix * normal;
    
    // 在切线空间中计算视线方向，优化大角度视角下的计算
    // 首先获取世界空间中的视线方向
    vec3 worldViewDir = normalize(cameraPosition - worldPosition.xyz);
    
    // 构建增强版TBN矩阵，确保切线与法线严格正交
    vec3 N = normalize(vNormal);
    vec3 T = normalize(normalMatrix * tangent.xyz);
    // 确保T完全与N正交，避免在边缘处的畸变
    T = normalize(T - dot(T, N) * N);
    vec3 B = normalize(cross(N, T) * tangent.w);
    
    // 将世界空间视线方向转换到切线空间，使用改进的TBN矩阵
    vViewDir = vec3(
        dot(worldViewDir, T),
        dot(worldViewDir, B),
        dot(worldViewDir, N)
    );
    
    // 应用房间尺寸缩放，但保持Z分量的比例更大，增强大角度视角下的效果
    vViewDir.xy *= roomScale;
    // 确保z分量在大角度视角下不会消失
    vViewDir.z *= max(1.0, roomScale * 0.75);
    
    // 设置最终顶点位置
    gl_Position = projectionMatrix * viewMatrix * worldPosition;
} `, qc = `uniform samplerCube roomCube;
uniform sampler2D roomMap;
uniform bool useSingleTexture;
uniform float roomScale;
uniform float roomVariety;
uniform float roomDepth; // 房间深度 - 标准值为0.5
uniform float roomsX;
uniform float roomsY;
uniform float gapSize;
uniform vec3 gapColor;
uniform float glassStrength;
uniform vec3 glassColor;
uniform bool flipTextureY;
uniform float time;
uniform bool handleBackFaces; // 是否特殊处理背面
uniform bool invert3DDepthOnBackFace; // 是否在背面时反转3D深度方向
uniform bool useOriginalRaytracing; // 是否使用原始光线追踪算法

varying vec2 vUv;
varying vec3 vViewDir;
varying vec3 vNormal;
varying vec3 vWorldPosition;

// 伪随机函数 - 根据输入坐标生成随机数
vec3 rand3(float n) {
    return fract(sin(n * vec3(12.9898, 78.233, 43.2316)) * 43758.5453);
}

// 伪随机函数 - 根据2维向量生成随机数
vec3 rand3(vec2 co) {
    return fract(sin(dot(co, vec2(12.9898, 78.233)) + vec3(0.0, 23.45, 78.67)) * 43758.5453);
}

// 从单张图片立方体贴图中采样
// 参数dir为方向向量，必须是已归一化的向量
vec4 sampleSingleTextureCube(sampler2D tex, vec3 dir) {
    // 找出哪个分量的绝对值最大，确定我们采样的是哪个面
    vec3 absDir = abs(dir);
    float maxVal = max(max(absDir.x, absDir.y), absDir.z);
    
    // 计算2D纹理坐标
    vec2 uv;
    
    // 根据传入的贴图图片布局调整采样坐标
    // 布局与原图匹配：
    // +-----+-----+-----+-----+
    // |     |  +Y |     |     |
    // |     |     |     |     |
    // +-----+-----+-----+-----+
    // | -X  |  +Z |  +X | -Z  |
    // |     |     |     |     |
    // +-----+-----+-----+-----+
    // |     |  -Y |     |     |
    // |     |     |     |     |
    // +-----+-----+-----+-----+
    
    float faceWidth = 0.25;  // 每个面宽度占整个贴图的1/4
    float faceHeight = 0.33333333; // 每个面高度占整个贴图的1/3
    
    if (maxVal == absDir.x) {
        // X轴面
        if (dir.x > 0.0) {
            // +X 面（右）
            uv = vec2(-dir.z, -dir.y) / abs(dir.x);
            // 将规范化的坐标映射到+X面位置
            uv = vec2(
                (uv.x * 0.5 + 0.5) * faceWidth + 2.0 * faceWidth,
                (uv.y * 0.5 + 0.5) * faceHeight + 1.0 * faceHeight
            );
        } else {
            // -X 面（左）
            uv = vec2(dir.z, -dir.y) / abs(dir.x);
            // 将规范化的坐标映射到-X面位置
            uv = vec2(
                (uv.x * 0.5 + 0.5) * faceWidth + 0.0 * faceWidth,
                (uv.y * 0.5 + 0.5) * faceHeight + 1.0 * faceHeight
            );
        }
    } else if (maxVal == absDir.y) {
        // Y轴面
        if (dir.y > 0.0) {
            // +Y 面（上）
            uv = vec2(dir.x, dir.z) / abs(dir.y);
            // 将规范化的坐标映射到+Y面位置
            uv = vec2(
                (uv.x * 0.5 + 0.5) * faceWidth + 1.0 * faceWidth,
                (uv.y * 0.5 + 0.5) * faceHeight + 0.0 * faceHeight
            );
        } else {
            // -Y 面（下）
            uv = vec2(dir.x, -dir.z) / abs(dir.y);
            // 将规范化的坐标映射到-Y面位置
            uv = vec2(
                (uv.x * 0.5 + 0.5) * faceWidth + 1.0 * faceWidth,
                (uv.y * 0.5 + 0.5) * faceHeight + 2.0 * faceHeight
            );
        }
    } else {
        // Z轴面
        if (dir.z > 0.0) {
            // +Z 面（前）
            uv = vec2(dir.x, -dir.y) / abs(dir.z);
            // 将规范化的坐标映射到+Z面位置
            uv = vec2(
                (uv.x * 0.5 + 0.5) * faceWidth + 1.0 * faceWidth,
                (uv.y * 0.5 + 0.5) * faceHeight + 1.0 * faceHeight
            );
        } else {
            // -Z 面（后）
            uv = vec2(-dir.x, -dir.y) / abs(dir.z);
            // 将规范化的坐标映射到-Z面位置
            uv = vec2(
                (uv.x * 0.5 + 0.5) * faceWidth + 3.0 * faceWidth,
                (uv.y * 0.5 + 0.5) * faceHeight + 1.0 * faceHeight
            );
        }
    }
    
    // 处理贴图Y轴翻转问题
    if (flipTextureY) {
        // Y坐标翻转
        uv.y = 1.0 - uv.y;
    }
    
    // 返回采样结果
    return texture2D(tex, uv);
}

// 计算玻璃反射效果
vec3 calculateGlassEffect(vec3 baseColor, vec3 normal, vec3 viewDir) {
    // 法线和视线方向都需要归一化
    vec3 N = normalize(normal);
    vec3 V = normalize(-viewDir);
    
    // 计算菲涅尔效应 - 使用改进的Schlick近似
    float cosTheta = max(0.0, dot(N, V));
    // 基础反射率 - 即使在0度角也有一些反射
    float baseReflectivity = 0.04; 
    float fresnel = baseReflectivity + (1.0 - baseReflectivity) * pow(1.0 - cosTheta, 5.0);
    
    // 确保反射效果明显可见
    fresnel = max(fresnel, 0.1); 
    
    // 基于菲涅尔和反射强度混合玻璃颜色
    float reflectionFactor = fresnel * glassStrength;
    
    // 添加基于视角的高光效果 - 降低高光指数使效果更柔和
    vec3 lightDir = normalize(vec3(0.5, 1.0, 0.5)); // 假设的光源方向
    float specular = pow(max(0.0, dot(reflect(-lightDir, N), V)), 16.0) * glassStrength;
    
    // 防止玻璃颜色太暗导致画面变黑
    // 对白色(1,1,1)和接近白色的颜色进行特殊处理，确保高亮效果
    vec3 safeGlassColor;
    float colorBrightness = dot(glassColor, vec3(0.299, 0.587, 0.114));
    
    if (colorBrightness > 0.9) {
        // 对于接近白色的颜色，保持其亮度但略微增加高光
        safeGlassColor = glassColor + vec3(0.1);
    } else {
        // 对其他颜色设置最低亮度下限
        safeGlassColor = max(glassColor, vec3(0.3));
    }
    
    // 混合反射和基础颜色 - 使用加性混合保持亮度
    vec3 reflection = safeGlassColor * (reflectionFactor + specular);
    vec3 finalColor = mix(baseColor, baseColor + reflection, glassStrength);
    
    // 亮度保护 - 防止颜色过暗
    float originalLuminance = dot(baseColor, vec3(0.299, 0.587, 0.114));
    float newLuminance = dot(finalColor, vec3(0.299, 0.587, 0.114));
    
    // 如果亮度降低超过30%，进行亮度补偿
    if (newLuminance < originalLuminance * 0.7) {
        finalColor = finalColor * (originalLuminance * 0.7 / max(newLuminance, 0.001));
    }
    
    return finalColor;
}

void main() {
    // 规范化视线方向
    vec3 viewDir = normalize(vViewDir);
    
    // 应用深度缩放
    // 将[0,1]范围的roomDepth重映射到[+inf,0]
    // 如果roomDepth = 0 -> depthScale = 0（无限深度房间）
    // 如果roomDepth = 0.5 -> depthScale = 1（正常深度）
    // 如果roomDepth = 1 -> depthScale = +inf（零体积房间）
    float depthScale = 1.0 / (1.0 - roomDepth) - 1.0;
    vec3 adjustedViewDir = viewDir;
    adjustedViewDir.z *= depthScale;
    
    // 可选的背面处理 - 根据handleBackFaces参数决定
    bool isBackFace = adjustedViewDir.z < 0.0;
    
    // 如果需要处理背面
    if (handleBackFaces && isBackFace) {
        // 背面处理方法：对整个方向向量进行调整
        // 这样光线追踪算法可以正确处理背面
        
        // 首先确保深度方向为正 - 这是必须的
        adjustedViewDir.z = abs(adjustedViewDir.z);
        
        // 如果需要，还可以翻转Y轴（适用于一些情况）
        if (invert3DDepthOnBackFace) {
            adjustedViewDir.y = -adjustedViewDir.y;
        }
    }
    
    // 处理多房间逻辑
    vec2 roomCell = vec2(0.0);
    vec2 roomUV = vUv;
    
    // 如果需要绘制多个房间
    bool isInGap = false;
    
    if (roomsX > 1.0 || roomsY > 1.0) {
        // 计算单个房间的大小
        float roomWidth = 1.0 / roomsX;
        float roomHeight = 1.0 / roomsY;
        
        // 计算当前所在的房间索引 (0-based)
        roomCell = floor(vUv / vec2(roomWidth, roomHeight));
        roomUV = fract(vUv / vec2(roomWidth, roomHeight));
        
        // 处理间隔
        if (gapSize > 0.0) {
            // 计算在当前房间UV空间中的位置 (0-1)
            float gapWidthHalf = gapSize * 0.5;
            float gapHeightHalf = gapSize * 0.5;
            
            // 检查是否在间隙内
            bool inHorizontalGap = roomUV.x < gapWidthHalf || roomUV.x > (1.0 - gapWidthHalf);
            bool inVerticalGap = roomUV.y < gapHeightHalf || roomUV.y > (1.0 - gapHeightHalf);
            
            isInGap = inHorizontalGap || inVerticalGap;
            
            // 调整房间UV，去除间隙区域
            float roomCenterX = 0.5;
            float roomCenterY = 0.5;
            
            // 重新映射UV到间隙内部区域 (gapWidthHalf, 1-gapWidthHalf)
            roomUV = (roomUV - vec2(roomCenterX, roomCenterY)) / (1.0 - gapSize) + vec2(roomCenterX, roomCenterY);
        }
    }
    
    // 如果在间隔内，直接返回间隔颜色
    if (isInGap) {
        gl_FragColor = vec4(gapColor, 1.0);
        return;
    }
    
    // 切线空间光线追踪
    // 有两种模式：原始模式和增强深度模式
    vec3 pos;
    
    if (useOriginalRaytracing) {
        // 原始光线追踪算法使用-1.0的z值
        pos = vec3(roomUV * 2.0 - 1.0, -1.0);
        
        // 两步处理：
        // 1. 首先翻转方向 - 这样才能产生室内效果
        adjustedViewDir.xy = -adjustedViewDir.xy;
        
        // 2. 视角压缩 - 让相机旋转产生更平缓的效果变化
        // 计算原始视角（与Z轴的夹角）
        float viewAngle = acos(abs(adjustedViewDir.z));
        // 进行视角压缩 - 实际效果角度只有真实角度的一半
        float compressedAngle = viewAngle * 0.5;
        // 重建视线方向
        float xyMagnitude = length(adjustedViewDir.xy);
        if (xyMagnitude > 0.0) {
            // 计算新的xy分量和z分量
            float compressedZ = cos(compressedAngle);
            float compressedXYScale = sin(compressedAngle) / xyMagnitude;
            
            // 保持xy方向不变，只调整大小
            adjustedViewDir.xy *= compressedXYScale;
            adjustedViewDir.z = adjustedViewDir.z >= 0.0 ? compressedZ : -compressedZ;
            
            // 再应用放大效果
            float scaleFactor = 1.0 + 1.5 * sin(compressedAngle);
            adjustedViewDir.xy *= scaleFactor;
        }
        
        // 重新归一化
        adjustedViewDir = normalize(adjustedViewDir);
    } else {
        // 增强深度模式：z分量固定为-1.0，表示朝向房间内部
        pos = vec3(roomUV * 2.0 - 1.0, -1.0);
        
        // 同样使用视角压缩
        // 1. 翻转方向
        adjustedViewDir.xy = -adjustedViewDir.xy;
        
        // 2. 视角压缩处理
        float viewAngle = acos(abs(adjustedViewDir.z));
        // 压缩视角 - 只有实际角度的60%
        float compressedAngle = viewAngle * 0.4;
        // 重建视线方向
        float xyMagnitude = length(adjustedViewDir.xy);
        if (xyMagnitude > 0.0) {
            // 计算新的分量
            float compressedZ = cos(compressedAngle);
            float compressedXYScale = sin(compressedAngle) / xyMagnitude;
            
            // 保持xy方向不变，只调整大小
            adjustedViewDir.xy *= compressedXYScale;
            adjustedViewDir.z = adjustedViewDir.z >= 0.0 ? compressedZ : -compressedZ;
            
            // 再应用较小的放大效果
            float scaleFactor = 1.0 + sin(compressedAngle);
            adjustedViewDir.xy *= scaleFactor;
        }
        
        // 重新归一化
        adjustedViewDir = normalize(adjustedViewDir);
    }
    
    // 光线追踪参数计算 - 完全匹配Unity中的计算方式
    vec3 id = 1.0 / adjustedViewDir;
    vec3 k = abs(id) - pos * id;
    float kMin = min(min(k.x, k.y), k.z);
    
    // 计算光线与立方体内部的交点
    pos += kMin * adjustedViewDir;
    
    // 为房间添加随机变化
    if (roomVariety > 0.0) {
        // 使用房间单元坐标生成随机值
        vec3 r = rand3(roomCell);
        
        // 计算实际应用的随机程度
        float variety = clamp(roomVariety, 0.0, 1.0);
        
        // 随机选择房间方向和翻转 - 匹配Unity版本的随机化逻辑
        float faceChoice = floor(r.x * 4.0);
        
        // 只在随机值小于variety时应用随机变换
        if (r.y < variety) {
            // 存储原始位置
            vec3 origPos = pos;
            
            if (faceChoice < 0.5) {
                // -X 面（左侧）作为正面
                pos = vec3(-origPos.z, origPos.y, origPos.x);
            } else if (faceChoice < 1.5) {
                // +Z 面（前面）作为正面 - 保持不变
                // pos = origPos;
            } else if (faceChoice < 2.5) {
                // +X 面（右侧）作为正面
                pos = vec3(origPos.z, origPos.y, -origPos.x);
            } else {
                // -Z 面（后面）作为正面
                pos = vec3(-origPos.x, origPos.y, -origPos.z);
            }
            
            // 随机X-Z翻转 - 匹配Unity版本的随机化
            vec2 cubeflip = floor(r.xy * 2.0) * 2.0 - 1.0;
            pos.xz *= cubeflip;
            
            if (r.z > 0.5) {
                // 交换X和Z坐标
                float temp = pos.x;
                pos.x = pos.z;
                pos.z = temp;
            }
        }
    }
    
    // 采样获取房间内部颜色
    vec4 roomColor;
    
    if (useSingleTexture) {
        roomColor = sampleSingleTextureCube(roomMap, pos);
    } else {
        roomColor = textureCube(roomCube, pos);
    }
    
    // 应用玻璃效果
    vec3 finalColor = roomColor.rgb;
    if (glassStrength > 0.0) {
        vec3 normal = normalize(vNormal);
        finalColor = calculateGlassEffect(finalColor, normal, normalize(vWorldPosition - cameraPosition));
    }
    
    gl_FragColor = vec4(finalColor, 1.0);
} `;
class zd extends N {
  /**
   * 构造函数
   * @param {Object} [options] - 可选配置项
   * @param {Number} [options.roomScale=1.0] - 房间尺寸缩放
   * @param {Number} [options.roomVariety=0.5] - 房间随机变化程度
   * @param {Number} [options.roomDepth=0.5] - 房间深度参数 (0.5为标准深度)
   * @param {Number} [options.roomsX=1] - 横向房间数量
   * @param {Number} [options.roomsY=1] - 纵向房间数量
   * @param {Number} [options.gapSize=0.05] - 房间间隔大小
   * @param {Number|Color} [options.gapColor=0x000000] - 房间间隔颜色
   * @param {Number} [options.glassStrength=0.25] - 前景玻璃反射强度 (0.0-1.0)
   * @param {Number|Color} [options.glassColor=0x88CCFF] - 前景玻璃颜色
   * @param {Boolean} [options.flipTextureY=true] - 是否在着色器中翻转贴图Y轴
   * @param {Boolean} [options.handleBackFaces=false] - 是否特殊处理背面 (对立方体有帮助)
   * @param {Boolean} [options.invert3DDepthOnBackFace=true] - 是否在背面时翻转Y轴方向 (保持垂直方向一致)
   * @param {Boolean} [options.useOriginalRaytracing=false] - 是否使用原始光线追踪算法 (可能对某些模型效果更好)
   */
  constructor(e = {}) {
    const t = e.roomScale !== void 0 ? e.roomScale : 1, r = e.roomVariety !== void 0 ? e.roomVariety : 0.5, n = e.roomDepth !== void 0 ? e.roomDepth : 0.5, i = e.roomsX !== void 0 ? e.roomsX : 1, s = e.roomsY !== void 0 ? e.roomsY : 1, o = e.gapSize !== void 0 ? e.gapSize : 0.05, l = e.gapColor !== void 0 ? e.gapColor : 0, u = e.glassStrength !== void 0 ? e.glassStrength : 0.25, f = e.glassColor !== void 0 ? e.glassColor : 8965375, h = e.flipTextureY !== void 0 ? e.flipTextureY : !0, m = e.handleBackFaces !== void 0 ? e.handleBackFaces : !1, p = e.invert3DDepthOnBackFace !== void 0 ? e.invert3DDepthOnBackFace : !0, d = e.useOriginalRaytracing !== void 0 ? e.useOriginalRaytracing : !1;
    super({
      name: "TangentSpaceInteriorMaterial",
      uniforms: {
        roomCube: new a(null),
        roomMap: new a(null),
        // 单张图片立方体贴图
        useSingleTexture: new a(!0),
        // 是否使用单张图片贴图
        roomScale: new a(t),
        roomVariety: new a(r),
        roomDepth: new a(n),
        // 房间深度参数
        roomsX: new a(i),
        // 横向房间数量
        roomsY: new a(s),
        // 纵向房间数量
        gapSize: new a(o),
        // 房间间隔大小
        gapColor: new a(new y(l)),
        // 房间间隔颜色
        glassStrength: new a(u),
        // 前景玻璃反射强度
        glassColor: new a(new y(f)),
        // 前景玻璃颜色
        flipTextureY: new a(h),
        // 是否翻转贴图Y轴
        time: new a(0),
        // 时间，用于可能的动画效果
        handleBackFaces: new a(m),
        // 是否特殊处理背面
        invert3DDepthOnBackFace: new a(p),
        // 是否在背面时翻转Y轴方向
        useOriginalRaytracing: new a(d)
        // 是否使用原始光线追踪算法
      },
      vertexShader: Qc,
      fragmentShader: qc,
      defines: {
        USE_TANGENT: !0
      }
    }), this.vertexTangents = !0;
  }
  /**
   * 设置房间立方体贴图
   * @param {CubeTexture} value - 房间内部的立方体贴图
   */
  set roomCube(e) {
    this.uniforms.roomCube.value = e, this._updateTextureMode();
  }
  /**
   * 获取房间立方体贴图
   * @return {CubeTexture} 房间立方体贴图
   */
  get roomCube() {
    return this.uniforms.roomCube.value;
  }
  /**
   * 设置房间单张图片立方体贴图
   * @param {Texture} value - 包含六个面的单张图片贴图
   */
  set roomMap(e) {
    this.uniforms.roomMap.value = e, this._updateTextureMode();
  }
  /**
   * 获取房间单张图片立方体贴图
   * @return {Texture} 单张图片立方体贴图
   */
  get roomMap() {
    return this.uniforms.roomMap.value;
  }
  /**
   * 更新纹理模式
   * 根据提供的纹理确定使用哪种模式
   * @private
   */
  _updateTextureMode() {
    const e = this.uniforms.roomCube.value !== null, t = this.uniforms.roomMap.value !== null;
    this.uniforms.useSingleTexture.value = !e && t, !e && !t && console.warn("TangentSpaceInteriorMaterial: 未提供任何房间贴图，请设置roomCube或roomMap"), this.defines.USE_SINGLE_TEXTURE !== this.uniforms.useSingleTexture.value && (this.defines.USE_SINGLE_TEXTURE = this.uniforms.useSingleTexture.value, this.needsUpdate = !0);
  }
  /**
   * 设置房间尺寸缩放
   * @param {Number} value - 房间尺寸缩放因子
   */
  set roomScale(e) {
    this.uniforms.roomScale.value = e;
  }
  /**
   * 获取房间尺寸缩放
   * @return {Number} 房间尺寸缩放因子
   */
  get roomScale() {
    return this.uniforms.roomScale.value;
  }
  /**
   * 设置房间随机变化程度
   * @param {Number} value - 房间随机变化程度 (0-1)
   */
  set roomVariety(e) {
    this.uniforms.roomVariety.value = e;
  }
  /**
   * 获取房间随机变化程度
   * @return {Number} 房间随机变化程度 (0-1)
   */
  get roomVariety() {
    return this.uniforms.roomVariety.value;
  }
  /**
   * 设置房间深度
   * @param {Number} value - 房间深度值 (0.0-1.0, 0.5为标准)
   */
  set roomDepth(e) {
    this.uniforms.roomDepth.value = Math.max(1e-3, Math.min(0.999, e));
  }
  /**
   * 获取房间深度
   * @return {Number} 房间深度值
   */
  get roomDepth() {
    return this.uniforms.roomDepth.value;
  }
  /**
   * 设置是否翻转贴图Y轴
   * @param {Boolean} value - 是否翻转贴图Y轴
   */
  set flipTextureY(e) {
    this.uniforms.flipTextureY.value = e;
  }
  /**
   * 获取是否翻转贴图Y轴
   * @return {Boolean} 是否翻转贴图Y轴
   */
  get flipTextureY() {
    return this.uniforms.flipTextureY.value;
  }
  /**
   * 设置横向房间数量
   * @param {Number} value - 横向房间数量
   */
  set roomsX(e) {
    this.uniforms.roomsX.value = e;
  }
  /**
   * 获取横向房间数量
   * @return {Number} 横向房间数量
   */
  get roomsX() {
    return this.uniforms.roomsX.value;
  }
  /**
   * 设置纵向房间数量
   * @param {Number} value - 纵向房间数量
   */
  set roomsY(e) {
    this.uniforms.roomsY.value = e;
  }
  /**
   * 获取纵向房间数量
   * @return {Number} 纵向房间数量
   */
  get roomsY() {
    return this.uniforms.roomsY.value;
  }
  /**
   * 设置房间间隔大小
   * @param {Number} value - 房间间隔大小
   */
  set gapSize(e) {
    this.uniforms.gapSize.value = e;
  }
  /**
   * 获取房间间隔大小
   * @return {Number} 房间间隔大小
   */
  get gapSize() {
    return this.uniforms.gapSize.value;
  }
  /**
   * 设置房间间隔颜色
   * @param {Color|Number} value - 房间间隔颜色
   */
  set gapColor(e) {
    typeof e == "number" ? this.uniforms.gapColor.value.setHex(e) : this.uniforms.gapColor.value.copy(e);
  }
  /**
   * 获取房间间隔颜色
   * @return {Color} 房间间隔颜色
   */
  get gapColor() {
    return this.uniforms.gapColor.value;
  }
  /**
   * 设置前景玻璃反射强度
   * @param {Number} value - 前景玻璃反射强度 (0.0-1.0)
   */
  set glassStrength(e) {
    this.uniforms.glassStrength.value = Math.max(0, Math.min(1, e));
  }
  /**
   * 获取前景玻璃反射强度
   * @return {Number} 前景玻璃反射强度
   */
  get glassStrength() {
    return this.uniforms.glassStrength.value;
  }
  /**
   * 设置前景玻璃颜色
   * @param {Color|Number|String} value - 前景玻璃颜色
   */
  set glassColor(e) {
    typeof e == "number" ? this.uniforms.glassColor.value.setHex(e) : typeof e == "string" ? this.uniforms.glassColor.value.set(e) : this.uniforms.glassColor.value.copy(e);
  }
  /**
   * 获取前景玻璃颜色
   * @return {Color} 前景玻璃颜色
   */
  get glassColor() {
    return this.uniforms.glassColor.value;
  }
  /**
   * 设置是否特殊处理背面
   * @param {Boolean} value - 是否特殊处理背面
   */
  set handleBackFaces(e) {
    this.uniforms.handleBackFaces.value = e;
  }
  /**
   * 获取是否特殊处理背面
   * @return {Boolean} 是否特殊处理背面
   */
  get handleBackFaces() {
    return this.uniforms.handleBackFaces.value;
  }
  /**
   * 设置是否在背面时翻转Y轴方向
   * @param {Boolean} value - 是否在背面时翻转Y轴方向
   */
  set invert3DDepthOnBackFace(e) {
    this.uniforms.invert3DDepthOnBackFace.value = e;
  }
  /**
   * 获取是否在背面时翻转Y轴方向
   * @return {Boolean} 是否在背面时翻转Y轴方向
   */
  get invert3DDepthOnBackFace() {
    return this.uniforms.invert3DDepthOnBackFace.value;
  }
  /**
   * 设置是否使用原始光线追踪算法
   * @param {Boolean} value - 是否使用原始光线追踪算法
   */
  set useOriginalRaytracing(e) {
    this.uniforms.useOriginalRaytracing.value = e;
  }
  /**
   * 获取是否使用原始光线追踪算法
   * @return {Boolean} 是否使用原始光线追踪算法
   */
  get useOriginalRaytracing() {
    return this.uniforms.useOriginalRaytracing.value;
  }
  /**
   * 更新材质
   * @param {Number} deltaTime - 时间差
   */
  update(e) {
    this.uniforms.time.value += e;
  }
}
const Jc = `// 顶点着色器主要负责将顶点位置转换到裁剪空间\r
// 并传递必要的数据给片段着色器\r
\r
// 传递时间和相机位置\r
uniform float time;\r
\r
// 必要的输出变量\r
varying vec2 vUv;\r
varying vec3 vPosition;\r
varying vec3 vNormal;\r
varying vec3 vWorldPosition;\r
varying vec3 vViewDirection;\r
\r
void main() {\r
    // 计算世界空间位置\r
    vWorldPosition = (modelMatrix * vec4(position, 1.0)).xyz;\r
    \r
    // 计算从顶点到相机的方向（视线方向）\r
    vViewDirection = normalize(cameraPosition - vWorldPosition);\r
    \r
    // 传递UV坐标\r
    vUv = uv;\r
    \r
    // 传递位置和法线\r
    vPosition = position;\r
    vNormal = normalize(normalMatrix * normal);\r
    \r
    // 设置最终的顶点位置\r
    gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);\r
} `, _c = `// 假室内材质的片段着色器 - 单房间版本
// 基于Interior Mapping技术

uniform float time;
uniform vec3 roomSize;
uniform sampler2D wallTexture;
uniform bool useLayoutTexture;

// 墙壁颜色
uniform vec3 ceilingColor;
uniform vec3 floorColor;
uniform vec3 rightWallColor;
uniform vec3 leftWallColor;
uniform vec3 frontWallColor;
uniform vec3 backWallColor;

varying vec2 vUv;
varying vec3 vPosition;
varying vec3 vNormal;
varying vec3 vWorldPosition;
varying vec3 vViewDirection;

// 立方体内部映射 - 计算射线与房间的交点
vec3 interiorMapping(vec3 rayOrigin, vec3 rayDir) {
    // 反转射线方向，使其看起来是"凹进去"的而不是"突出来"的
    rayDir = -rayDir;
    
    // 计算单元格尺寸和局部坐标
    vec3 cellSize = roomSize;
    vec3 localPos = mod(rayOrigin, cellSize);
    
    // 计算到下一个单元格边界的距离
    vec3 nextBoundary = step(0.0, rayDir) * cellSize - mod(localPos, cellSize);
    vec3 prevBoundary = step(0.0, -rayDir) * cellSize - mod(localPos, cellSize);
    
    // 计算射线与单元格边界的相交
    vec3 intersectMax = nextBoundary / abs(rayDir);
    vec3 intersectMin = prevBoundary / abs(rayDir);
    
    // 找到最小的相交时间（即最近的交点）
    vec3 largestRayParams = max(intersectMax, intersectMin);
    float dist = min(min(largestRayParams.x, largestRayParams.y), largestRayParams.z);
    
    // 计算交点位置
    vec3 intersectPos = rayOrigin + rayDir * dist;
    
    // 计算交点在当前单元格中的相对位置，标准化到[-1,1]范围
    vec3 cellPoint = mod(intersectPos, cellSize) / cellSize * 2.0 - 1.0;
    
    return cellPoint;
}

// 2D映射 - 将立方体内部点映射到2D纹理坐标
vec2 mapToTexture(vec3 cubePoint, out int faceID) {
    // 计算每个面的贡献权重
    float eps = 0.001;
    
    // 确定哪个面被击中 (x, y, 或 z 轴)
    vec3 absPoint = abs(cubePoint);
    float maxCoord = max(max(absPoint.x, absPoint.y), absPoint.z);
    
    // 初始化2D纹理坐标
    vec2 texCoord;
    
    // 确定是哪个面，并计算对应的纹理坐标
    if(abs(maxCoord - absPoint.x) < eps) {
        // X轴墙面 (左右墙)
        texCoord = vec2(cubePoint.z, cubePoint.y);
        faceID = cubePoint.x > 0.0 ? 3 : 4; // +X右墙, -X左墙
    } else if(abs(maxCoord - absPoint.y) < eps) {
        // Y轴墙面 (天花板地板)
        texCoord = vec2(cubePoint.x, cubePoint.z);
        faceID = cubePoint.y > 0.0 ? 1 : 2; // +Y天花板, -Y地板
    } else {
        // Z轴墙面 (前后墙)
        texCoord = vec2(cubePoint.x, cubePoint.y);
        faceID = cubePoint.z > 0.0 ? 5 : 6; // +Z前墙, -Z后墙
    }
    
    // 将坐标范围从[-1,1]映射到[0,1]用于纹理采样
    texCoord = (texCoord + 1.0) * 0.5;
    
    // 如果使用布局纹理，则转换到相应的布局位置
    if(useLayoutTexture) {
        // 从布局贴图中获取对应面的纹理坐标
        // 布局:
        // +-----+-----+-----+-----+
        // |     |  +Y |     |     |
        // |     |     |     |     |
        // +-----+-----+-----+-----+
        // | -X  |  +Z |  +X | -Z  |
        // |     |     |     |     |
        // +-----+-----+-----+-----+
        // |     |  -Y |     |     |
        // |     |     |     |     |
        // +-----+-----+-----+-----+
        vec2 scale = vec2(0.25, 0.333333); // 1/4, 1/3
        vec2 offset;
        
        // 本地UV坐标需要翻转以匹配布局
        vec2 flippedUV = texCoord;
        
        if(faceID == 1) {  // +Y (天花板)
            offset = vec2(0.25, 0.0);
        } else if(faceID == 2) {  // -Y (地板)
            offset = vec2(0.25, 0.666667);
            flippedUV.y = 1.0 - flippedUV.y; // 翻转Y轴
        } else if(faceID == 3) {  // +X (右墙)
            offset = vec2(0.5, 0.333333);
        } else if(faceID == 4) {  // -X (左墙)
            offset = vec2(0.0, 0.333333);
            flippedUV.x = 1.0 - flippedUV.x; // 翻转X轴
        } else if(faceID == 5) {  // +Z (前墙)
            offset = vec2(0.25, 0.333333);
        } else if(faceID == 6) {  // -Z (后墙)
            offset = vec2(0.75, 0.333333);
            flippedUV.x = 1.0 - flippedUV.x; // 翻转X轴
        }
        
        // 计算在整个贴图中的坐标
        texCoord = offset + flippedUV * scale;
    }
    
    return texCoord;
}

// 根据面ID获取对应墙面的颜色
vec3 getFaceColor(int faceID) {
    if(faceID == 1) return ceilingColor;       // 天花板
    if(faceID == 2) return floorColor;         // 地板
    if(faceID == 3) return rightWallColor;     // 右墙
    if(faceID == 4) return leftWallColor;      // 左墙
    if(faceID == 5) return frontWallColor;     // 前墙
    if(faceID == 6) return backWallColor;      // 后墙
    
    return vec3(0.8); // 默认浅灰色
}

void main() {
    // 计算从表面点到相机的射线
    vec3 rayOrigin = vWorldPosition;
    vec3 rayDir = normalize(vViewDirection);
    
    // 室内点映射
    vec3 roomPoint = interiorMapping(rayOrigin, rayDir);
    
    // 确定是哪个面被击中（注意：我们现在直接在mapToTexture中计算faceID）
    int faceID = 0;
    vec2 texCoord = mapToTexture(roomPoint, faceID);
    
    // 采样纹理
    vec3 texColor = texture2D(wallTexture, texCoord).rgb;
    
    // 获取面颜色
    vec3 faceColor = getFaceColor(faceID);
    
    // 合成最终颜色
    vec3 finalColor = texColor * faceColor;
    
    // 添加光照效果以提高亮度
    // 简化的环境光遮蔽，让场景更亮
    float ao = 1.0 - 0.1 * (abs(roomPoint.x) + abs(roomPoint.y) + abs(roomPoint.z)) / 3.0;
    
    // 添加简单的环境光
    float ambientLight = 0.9; // 提高环境光强度
    
    // 主光源方向（简单定向光）
    vec3 lightDir = normalize(vec3(0.5, 1.0, 0.8));
    float diff = max(dot(normalize(vNormal), lightDir), 0.0);
    
    // 合成最终亮度
    float lighting = ambientLight + diff * 0.3;
    
    // 应用光照效果
    finalColor = finalColor * ao * lighting * 2.0; // 整体亮度提高
    
    // 输出最终颜色
    gl_FragColor = vec4(finalColor, 1.0);
}
`;
class Fd extends N {
  /**
   * 构造函数
   * @param {Object} [options] - 可选配置项
   * @param {Number} [options.roomSizeX=2.0] - 房间X轴尺寸
   * @param {Number} [options.roomSizeY=2.0] - 房间Y轴尺寸
   * @param {Number} [options.roomSizeZ=2.0] - 房间Z轴尺寸
   * @param {Color|Number} [options.ceilingColor=0x6BD8A5] - 天花板颜色
   * @param {Color|Number} [options.floorColor=0xFFAB8C] - 地板颜色
   * @param {Color|Number} [options.rightWallColor=0xFFE57D] - 右墙颜色
   * @param {Color|Number} [options.leftWallColor=0xFFC580] - 左墙颜色
   * @param {Color|Number} [options.frontWallColor=0x80C7FF] - 前墙颜色
   * @param {Color|Number} [options.backWallColor=0x968EEB] - 后墙颜色
   * @param {Boolean} [options.useLayoutTexture=true] - 是否使用布局纹理图片 (默认使用)
   */
  constructor(e = {}) {
    const t = e.roomSizeX !== void 0 ? e.roomSizeX : 2, r = e.roomSizeY !== void 0 ? e.roomSizeY : 2, n = e.roomSizeZ !== void 0 ? e.roomSizeZ : 2, i = e.ceilingColor !== void 0 ? e.ceilingColor : 7067813, s = e.floorColor !== void 0 ? e.floorColor : 16755596, o = e.rightWallColor !== void 0 ? e.rightWallColor : 16770429, l = e.leftWallColor !== void 0 ? e.leftWallColor : 16762240, u = e.frontWallColor !== void 0 ? e.frontWallColor : 8439807, f = e.backWallColor !== void 0 ? e.backWallColor : 9866987, h = e.useLayoutTexture !== void 0 ? e.useLayoutTexture : !0;
    super({
      name: "FakeInteriorMaterial",
      uniforms: {
        time: new a(0),
        cameraPosition: new a(new T(0, 0, 0)),
        roomSize: new a(new T(t, r, n)),
        wallTexture: new a(null),
        // 各个墙面颜色
        ceilingColor: new a(new y(i)),
        floorColor: new a(new y(s)),
        rightWallColor: new a(new y(o)),
        leftWallColor: new a(new y(l)),
        frontWallColor: new a(new y(u)),
        backWallColor: new a(new y(f)),
        // 是否使用布局纹理
        useLayoutTexture: new a(h)
      },
      vertexShader: Jc,
      fragmentShader: _c
    });
  }
  /**
   * 设置墙壁纹理 (布局纹理)
   * @param {Texture} value - 布局纹理贴图
   */
  set wallTexture(e) {
    this.uniforms.wallTexture.value = e;
  }
  /**
   * 设置房间尺寸
   * @param {Number} x - X轴房间尺寸
   * @param {Number} y - Y轴房间尺寸
   * @param {Number} z - Z轴房间尺寸
   */
  setRoomSize(e, t, r) {
    this.uniforms.roomSize.value.set(e, t, r);
  }
  /**
   * 设置是否使用布局纹理
   * @param {Boolean} value - 是否使用布局纹理
   */
  set useLayoutTexture(e) {
    this.uniforms.useLayoutTexture.value = e;
  }
  /**
   * 获取是否使用布局纹理
   * @return {Boolean} 是否使用布局纹理
   */
  get useLayoutTexture() {
    return this.uniforms.useLayoutTexture.value;
  }
  /**
   * 更新材质
   * @param {Number} deltaTime - 时间差
   * @param {Vector3} cameraPosition - 相机位置
   */
  update(e, t) {
    this.uniforms.time.value += e, t && this.uniforms.cameraPosition.value.copy(t);
  }
}
const $c = `varying vec2 vUv;\r
varying vec3 vPosition;\r
varying vec3 vWorldPosition;\r
varying vec3 vViewDir;\r
\r
void main() {\r
    vUv = uv;\r
    vPosition = position;\r
    vWorldPosition = (modelMatrix * vec4(position, 1.0)).xyz;\r
    \r
    // 计算从顶点到相机的视线方向\r
    vec3 worldPos = (modelMatrix * vec4(position, 1.0)).xyz;\r
    vViewDir = normalize(cameraPosition - worldPos);\r
    \r
    gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);\r
} `, eu = `uniform float time;\r
uniform vec2 resolution;\r
uniform sampler2D interiorTexture;\r
uniform float roomDepth;\r
\r
varying vec2 vUv;\r
varying vec3 vPosition;\r
varying vec3 vWorldPosition;\r
varying vec3 vViewDir;\r
\r
// 室内立方体映射函数 - 计算射线与立方体的交点\r
vec3 interiorMapping(in vec2 p) {\r
    // 创建固定房间深度效果\r
    vec3 pdepth = vec3(p, -roomDepth);\r
    \r
    // 修正视线方向 - 简化计算，避免错误\r
    vec3 directv = normalize(vec3(vViewDir.xy, -abs(vViewDir.z)));\r
    \r
    // 射线相交计算 - 保证有效结果\r
    vec3 t = 1.0 / max(abs(directv), 0.001);\r
    vec3 k = abs(t) - pdepth * t;\r
    \r
    float kmin = min(min(k.x, k.y), k.z);\r
    vec3 cubep = kmin * directv + pdepth;\r
    \r
    return cubep;\r
}\r
\r
// 立方体到2D纹理映射函数\r
vec2 twoDMapping(in vec3 cubep) {\r
    // 立方体到UV映射的简化实现\r
    // 确定哪个面被击中\r
    vec3 absCubep = abs(cubep);\r
    float maxComp = max(max(absCubep.x, absCubep.y), absCubep.z);\r
    \r
    vec2 uv;\r
    \r
    // 根据最大分量确定击中的面，并创建适当的UV映射\r
    if (maxComp == absCubep.x) {\r
        // X轴面 (左/右墙)\r
        uv = vec2(cubep.z, cubep.y) / roomDepth * 0.5 + 0.5;\r
        uv.x = cubep.x > 0.0 ? uv.x : 1.0 - uv.x;\r
    } else if (maxComp == absCubep.y) {\r
        // Y轴面 (上/下墙)\r
        uv = vec2(cubep.x, cubep.z) / roomDepth * 0.5 + 0.5;\r
        uv.y = cubep.y > 0.0 ? uv.y : 1.0 - uv.y;\r
    } else {\r
        // Z轴面 (前/后墙)\r
        uv = vec2(cubep.x, cubep.y) / roomDepth * 0.5 + 0.5;\r
        uv.x = cubep.z > 0.0 ? uv.x : 1.0 - uv.x;\r
    }\r
    \r
    return fract(uv);\r
}\r
\r
void main() {\r
    // 使用归一化的UV坐标，确保比例正确\r
    vec2 p = vUv * 2.0 - 1.0;\r
    p.x *= resolution.x / resolution.y;\r
    \r
    vec2 p1 = vUv;\r
    \r
    // 计算晕影效果\r
    float vignetting = 16.0 * (p1.x * p1.y * (1.0 - p1.x) * (1.0 - p1.y));\r
    \r
    // 计算立方体映射坐标\r
    vec3 cubemapping = interiorMapping(p);\r
    \r
    // 计算2D纹理采样坐标\r
    vec2 twodsample = twoDMapping(cubemapping);\r
    \r
    // 调试 - 如果没有纹理，使用UV坐标作为颜色显示\r
    vec3 debugColor = vec3(twodsample, 0.0);\r
    \r
    // 采样室内纹理\r
    vec3 interiorColor = texture2D(interiorTexture, twodsample).rgb;\r
    if (length(interiorColor) < 0.01) {\r
        interiorColor = debugColor;\r
    }\r
    \r
    // 添加晕影效果\r
    vec3 finalColor = interiorColor * sqrt(vignetting);\r
    \r
    gl_FragColor = vec4(finalColor, 1.0);\r
} `;
class Id extends N {
  /**
   * 构造函数
   * @param {Object} [options] - 可选配置项
   * @param {Number} [options.roomDepth=5.0] - 房间深度
   * @param {Texture} [options.interiorTexture=null] - 室内纹理
   */
  constructor(e = {}) {
    const t = e.roomDepth !== void 0 ? e.roomDepth : 5;
    super({
      name: "InteriorRoomMaterial",
      uniforms: {
        time: new a(0),
        resolution: new a(new g(1, 1)),
        roomDepth: new a(t),
        interiorTexture: new a(e.interiorTexture || null)
      },
      vertexShader: $c,
      fragmentShader: eu,
      transparent: !1
    });
  }
  /**
   * 设置室内纹理
   * @param {Texture} value - 室内纹理
   */
  set interiorTexture(e) {
    this.uniforms.interiorTexture.value = e;
  }
  /**
   * 获取室内纹理
   * @return {Texture} 室内纹理
   */
  get interiorTexture() {
    return this.uniforms.interiorTexture.value;
  }
  /**
   * 设置房间深度
   * @param {Number} value - 房间深度
   */
  set roomDepth(e) {
    this.uniforms.roomDepth.value = e;
  }
  /**
   * 获取房间深度
   * @return {Number} 房间深度
   */
  get roomDepth() {
    return this.uniforms.roomDepth.value;
  }
  /**
   * 更新材质
   * @param {Number} deltaTime - 时间差
   */
  update(e) {
    this.uniforms.time.value += e;
  }
  /**
   * 设置分辨率
   * @param {Number} width - 渲染宽度
   * @param {Number} height - 渲染高度
   */
  setResolution(e, t) {
    this.uniforms.resolution.value.set(e, t);
  }
}
const tu = `// 流动河流材质顶点着色器\r
\r
uniform float time;\r
uniform float waveHeight;\r
uniform float waveScale;\r
uniform float waveSpeed;\r
\r
// 传递给片段着色器的数据\r
varying vec2 vUv;\r
varying vec3 vPosition;\r
varying vec3 vNormal;\r
varying vec3 vWorldPosition;\r
\r
// 旋转矩阵函数\r
mat2 rotate2D(float r) {\r
    return mat2(cos(r), sin(r), -sin(r), cos(r));\r
}\r
\r
// 噪声函数\r
float hash(float n) {\r
    return fract(sin(n) * 43758.5453);\r
}\r
\r
// 波浪计算函数\r
float getWave(vec3 pos, float time) {\r
    // 添加随机性\r
    float seed = dot(vec2(pos.x, pos.z), vec2(123.45, 678.91));\r
    float randomOffset = hash(seed) * 6.28;\r
    \r
    // 主要波浪\r
    float waveA = sin(pos.x * 2.0 * waveScale + time * waveSpeed + randomOffset) * \r
                 cos(pos.z * 2.0 * waveScale + time * waveSpeed * 0.6) * 0.5;\r
                 \r
    // 次要波浪\r
    float waveB = sin(pos.x * 3.0 * waveScale + time * waveSpeed * 0.4 + randomOffset) * \r
                 sin(pos.z * 3.0 * waveScale + time * waveSpeed * 0.8) * 0.25;\r
    \r
    // 细节波浪\r
    float waveC = sin(pos.x * 7.0 * waveScale + time * waveSpeed * 0.2 + randomOffset) * \r
                 cos(pos.z * 6.0 * waveScale + time * waveSpeed * 0.4) * 0.125;\r
    \r
    return (waveA + waveB + waveC) * waveHeight;\r
}\r
\r
void main() {\r
    // 基础位置\r
    vec3 pos = position;\r
    \r
    // 应用波浪效果\r
    pos.y += getWave(pos, time);\r
    \r
    // 计算法线\r
    float epsilon = 0.01;\r
    vec3 pos1 = pos + vec3(epsilon, 0.0, 0.0);\r
    vec3 pos2 = pos + vec3(-epsilon, 0.0, 0.0);\r
    vec3 pos3 = pos + vec3(0.0, 0.0, epsilon);\r
    vec3 pos4 = pos + vec3(0.0, 0.0, -epsilon);\r
    \r
    float h1 = getWave(pos1, time);\r
    float h2 = getWave(pos2, time);\r
    float h3 = getWave(pos3, time);\r
    float h4 = getWave(pos4, time);\r
    \r
    vec3 tangent = normalize(vec3(2.0 * epsilon, h1 - h2, 0.0));\r
    vec3 bitangent = normalize(vec3(0.0, h3 - h4, 2.0 * epsilon));\r
    vec3 modifiedNormal = normalize(cross(bitangent, tangent));\r
    \r
    // 传递数据到片段着色器\r
    vUv = uv;\r
    vPosition = pos;\r
    vNormal = normalMatrix * modifiedNormal;\r
    \r
    // 计算世界空间位置\r
    vec4 worldPosition = modelMatrix * vec4(pos, 1.0);\r
    vWorldPosition = worldPosition.xyz;\r
    \r
    // 最终顶点位置\r
    gl_Position = projectionMatrix * viewMatrix * worldPosition;\r
} `, ru = `// 流动河流材质片段着色器\r
\r
uniform float time;\r
uniform float flowSpeed;\r
uniform vec3 waterColor;\r
uniform float transparency;\r
uniform vec2 resolution;\r
\r
// 从顶点着色器接收的数据\r
varying vec2 vUv;\r
varying vec3 vPosition;\r
varying vec3 vNormal;\r
varying vec3 vWorldPosition;\r
\r
// 常量定义\r
#define MOD2 vec2(4.438975, 3.972973)\r
\r
// 旋转矩阵函数\r
mat2 rotate2D(float r) {\r
    return mat2(cos(r), sin(r), -sin(r), cos(r));\r
}\r
\r
// 哈希函数\r
float hash(float p) {\r
    vec2 p2 = fract(vec2(p) * MOD2);\r
    p2 += dot(p2.yx, p2.xy + 19.19);\r
    return fract(p2.x * p2.y);\r
}\r
\r
// 平滑噪声\r
float smoothNoise(in vec2 o) {\r
    vec2 p = floor(o);\r
    vec2 f = fract(o);\r
    \r
    float n = p.x + p.y * 57.0;\r
    \r
    float a = hash(n + 0.0);\r
    float b = hash(n + 1.0);\r
    float c = hash(n + 57.0);\r
    float d = hash(n + 58.0);\r
    \r
    vec2 f2 = f * f;\r
    vec2 f3 = f2 * f;\r
    \r
    vec2 t = 3.0 * f2 - 2.0 * f3;\r
    \r
    float u = t.x;\r
    float v = t.y;\r
    \r
    float res = a + (b-a)*u + (c-a)*v + (a-b+d-c)*u*v;\r
    \r
    return res;\r
}\r
\r
// FBM (分形布朗运动)\r
float fbm(vec2 p, float ps) {\r
    float f = 0.0;\r
    float tot = 0.0;\r
    float a = 1.0;\r
    for(int i=0; i<5; i++) {\r
        f += smoothNoise(p) * a;\r
        p *= 2.0;\r
        tot += a;\r
        a *= ps;\r
    }\r
    return f / tot;\r
}\r
\r
// 带梯度的噪声\r
vec3 smoothNoiseDXY(in vec2 o) {\r
    vec2 p = floor(o);\r
    vec2 f = fract(o);\r
    \r
    float n = p.x + p.y * 57.0;\r
    \r
    float a = hash(n + 0.0);\r
    float b = hash(n + 1.0);\r
    float c = hash(n + 57.0);\r
    float d = hash(n + 58.0);\r
    \r
    vec2 f2 = f * f;\r
    vec2 f3 = f2 * f;\r
    \r
    vec2 t = 3.0 * f2 - 2.0 * f3;\r
    vec2 dt = 6.0 * f - 6.0 * f2;\r
    \r
    float u = t.x;\r
    float v = t.y;\r
    float du = dt.x;\r
    float dv = dt.y;\r
    \r
    float res = a + (b-a)*u + (c-a)*v + (a-b+d-c)*u*v;\r
    \r
    float dx = (b-a)*du + (a-b+d-c)*du*v;\r
    float dy = (c-a)*dv + (a-b+d-c)*u*dv;\r
    \r
    return vec3(dx, dy, res);\r
}\r
\r
// 带梯度的FBM\r
vec3 fbmDXY(vec2 p, vec2 flow, float ps, float df) {\r
    vec3 f = vec3(0.0);\r
    float tot = 0.0;\r
    float a = 1.0;\r
    for(int i=0; i<4; i++) {\r
        p += flow;\r
        flow *= -0.75;\r
        vec3 v = smoothNoiseDXY(p);\r
        f += v * a;\r
        p += v.xy * df;\r
        p *= 2.0;\r
        tot += a;\r
        a *= ps;\r
    }\r
    return f / tot;\r
}\r
\r
void main() {\r
    // 计算流动的UV坐标\r
    vec2 uv = vUv;\r
    vec2 flow = vec2(1.0, 0.0) * flowSpeed;\r
    \r
    // 计算分形噪声\r
    float noise = fbm(uv * 10.0 + time * flow, 0.5);\r
    vec3 noiseGradient = fbmDXY(uv * 10.0 + time * flow, flow * 0.1, 0.5, 0.1);\r
    \r
    // 计算法线\r
    vec3 normal = normalize(vNormal + vec3(noiseGradient.xy * 0.1, 1.0));\r
    \r
    // 计算光照\r
    vec3 lightDir = normalize(vec3(1.0, 1.0, 1.0));\r
    float diffuse = max(dot(normal, lightDir), 0.0);\r
    \r
    // 计算反射\r
    vec3 viewDir = normalize(cameraPosition - vWorldPosition);\r
    vec3 reflectDir = reflect(-lightDir, normal);\r
    float specular = pow(max(dot(viewDir, reflectDir), 0.0), 32.0);\r
    \r
    // 计算最终颜色\r
    vec3 color = waterColor;\r
    color = mix(color, color * 1.2, diffuse);\r
    color += vec3(1.0) * specular * 0.5;\r
    color = mix(color, vec3(1.0), noise * 0.1);\r
    \r
    // 输出带透明度的颜色\r
    gl_FragColor = vec4(color, transparency);\r
} `;
class Bd extends N {
  /**
   * 构造函数
   * @param {Object} [options] - 可选配置项
   * @param {Number} [options.flowSpeed=0.5] - 流动速度
   * @param {Number|Color} [options.waterColor=0x0055ff] - 水的颜色
   * @param {Number} [options.transparency=0.8] - 透明度
   * @param {Number} [options.waveHeight=0.2] - 波浪高度
   * @param {Number} [options.waveScale=1.0] - 波浪缩放
   * @param {Number} [options.waveSpeed=1.0] - 波浪速度
   */
  constructor(e = {}) {
    const t = e.flowSpeed !== void 0 ? e.flowSpeed : 0.5, r = e.waterColor !== void 0 ? e.waterColor : 22015, n = e.transparency !== void 0 ? e.transparency : 0.8, i = e.waveHeight !== void 0 ? e.waveHeight : 0.2, s = e.waveScale !== void 0 ? e.waveScale : 1, o = e.waveSpeed !== void 0 ? e.waveSpeed : 1;
    super({
      name: "FlowRiverMaterial",
      uniforms: {
        time: new a(0),
        flowSpeed: new a(t),
        waterColor: new a(new y(r)),
        transparency: new a(n),
        waveHeight: new a(i),
        waveScale: new a(s),
        waveSpeed: new a(o),
        resolution: new a(new g(1, 1))
      },
      vertexShader: tu,
      fragmentShader: ru,
      transparent: !0,
      side: hr
    });
  }
  /**
   * 更新材质
   * @param {Number} deltaTime - 时间差
   */
  update(e) {
    this.uniforms.time.value += e * this.uniforms.flowSpeed.value;
  }
}
class Ld {
  /**
   * 构造MRT G-Buffer材质补丁器
   * 
   * @param {ObjectIdManager} objectIdManager - 对象ID管理器
   */
  constructor(e) {
    this.objectIdManager = e, this.patchedMaterials = /* @__PURE__ */ new WeakSet(), this.originalOnBeforeCompile = /* @__PURE__ */ new WeakMap(), this.debug = !1;
  }
  /**
   * 为场景中的所有材质注入MRT输出代码
   * 
   * @param {Scene} scene - Three.js场景
   * @param {Camera} camera - 相机（用于深度计算）
   * @returns {number} 注入的材质数量
   */
  patchScene(e, t) {
    let r = 0;
    return e.traverse((n) => {
      n.isMesh && n.visible && n.material && (Array.isArray(n.material) ? n.material : [n.material]).forEach((s) => {
        this.patchMaterial(s, n, t) && r++;
      });
    }), console.log(`🔧 MRTGBufferMaterialPatcher: 已注入${r}个材质`), r === 0 && console.warn("⚠️ 警告：没有找到任何材质需要注入！"), r;
  }
  /**
   * 为单个材质注入MRT输出代码
   * 
   * @param {Material} material - 要注入的材质
   * @param {Object3D} object - 材质所属的对象
   * @param {Camera} camera - 相机
   * @returns {boolean} 是否成功注入
   */
  patchMaterial(e, t, r) {
    if (this.patchedMaterials.has(e))
      return !1;
    const n = this.objectIdManager ? this.objectIdManager.getObjectId(t) : 0;
    e.onBeforeCompile && e.onBeforeCompile !== Function.prototype && this.originalOnBeforeCompile.set(e, e.onBeforeCompile);
    const i = e.customProgramCacheKey, s = this.originalOnBeforeCompile.get(e);
    return e.onBeforeCompile = (o, l) => {
      if (console.log("Log-- ", o.fragmentShader, "shader.fragmentShader"), s && s(o, l), o.uniforms.objectId = { value: n }, o.uniforms.cameraNear = { value: r.near }, o.uniforms.cameraFar = { value: r.far }, l.capabilities.isWebGL2) {
        o.vertexShader = o.vertexShader.replace(
          "void main() {",
          /* glsl */
          `
uniform float cameraNear;
uniform float cameraFar;
varying vec3 vViewNormal;
varying float vLinearDepth;

void main() {
`
        ), o.vertexShader = o.vertexShader.replace(
          "#include <fog_vertex>",
          /* glsl */
          `
#include <fog_vertex>

// 计算视图空间法线
vViewNormal = normalize(normalMatrix * normal);

// 计算视图空间位置
vViewPosition = -mvPosition.xyz;

// 计算线性深度
float depth = -mvPosition.z;
vLinearDepth = (depth - cameraNear) / (cameraFar - cameraNear);
vLinearDepth = clamp(vLinearDepth, 0.0, 1.0);
`
        ), o.fragmentShader = o.fragmentShader.replace(
          "#include <common>",
          `#include <common>

// MRT输出声明（Three.js会自动处理location 0）
layout(location = 1) out vec4 gNormal;
layout(location = 2) out vec4 gDepth;
layout(location = 3) out vec4 gPosition;
layout(location = 4) out vec4 gObjectId;

// Uniform和varying
varying vec3 vViewNormal;
varying float vLinearDepth;
uniform float objectId;
`
        );
        const f = o.fragmentShader;
        o.fragmentShader = o.fragmentShader.replace(
          "#include <dithering_fragment>",
          /* glsl */
          `
#include <dithering_fragment>

// 输出G-Buffer数据
gNormal = vec4(normalize(vViewNormal) * 0.5 + 0.5, 1.0);
gDepth = vec4(vec3(vLinearDepth), 1.0);
gPosition = vec4(vViewPosition, 1.0);

// 输出对象ID（归一化到0-1）
float normalizedObjectId = objectId / 255.0;
gObjectId = vec4(vec3(normalizedObjectId), 1.0);
`
        ), f === o.fragmentShader && (console.warn("⚠️ 警告：#include <dithering_fragment> 替换失败，尝试备用方案"), o.fragmentShader = o.fragmentShader.replace(
          "#include <tonemapping_fragment>",
          /* glsl */
          `
#include <tonemapping_fragment>

// 输出G-Buffer数据
gNormal = vec4(normalize(vViewNormal) * 0.5 + 0.5, 1.0);
gDepth = vec4(vec3(vLinearDepth), 1.0);
gPosition = vec4(vViewPosition, 1.0);

// 输出对象ID（归一化到0-1）
float normalizedObjectId = objectId / 255.0;
gObjectId = vec4(vec3(normalizedObjectId), 1.0);
`
        ));
      } else
        o.vertexShader = o.vertexShader.replace(
          "#include <common>",
          /* glsl */
          `
#include <common>
uniform float cameraNear;
uniform float cameraFar;
varying vec3 vViewNormal;
varying float vLinearDepth;
`
        ), o.vertexShader = o.vertexShader.replace(
          "#include <fog_vertex>",
          /* glsl */
          `
#include <fog_vertex>

// 计算视图空间法线
vViewNormal = normalize(normalMatrix * normal);

// 计算视图空间位置  
vViewPosition = -mvPosition.xyz;

// 计算线性深度
float depth = -mvPosition.z;
vLinearDepth = (depth - cameraNear) / (cameraFar - cameraNear);
vLinearDepth = clamp(vLinearDepth, 0.0, 1.0);
`
        ), o.fragmentShader = o.fragmentShader.replace(
          "#include <common>",
          /* glsl */
          `
#include <common>
varying vec3 vViewNormal;
varying float vLinearDepth;
uniform float objectId;

// MRT输出声明（GLSL1使用gl_FragData）
#define gLitColor gl_FragData[0]
#define gNormal gl_FragData[1]
#define gDepth gl_FragData[2]
#define gPosition gl_FragData[3]
#define gObjectId gl_FragData[4]
`
        ), o.fragmentShader = o.fragmentShader.replace(
          /gl_FragColor/g,
          "gLitColor"
        ), o.fragmentShader = o.fragmentShader.replace(
          /\}[\s]*$/,
          /* glsl */
          `
	// 输出G-Buffer数据
	gNormal = vec4(normalize(vViewNormal) * 0.5 + 0.5, 1.0);
	gDepth = vec4(vec3(vLinearDepth), 1.0);
	gPosition = vec4(vViewPosition, 1.0);
	
	// 输出对象ID（归一化到0-1）
	float normalizedObjectId = objectId / 255.0;
	gObjectId = vec4(vec3(normalizedObjectId), 1.0);
}
`
        );
      console.log(`📝 已注入材质: ${e.name || e.type} (ObjectId: ${n})`), this.debug && (console.log("=== Modified Fragment Shader (first 500 chars) ==="), console.log(o.fragmentShader.substring(0, 500)));
    }, e.customProgramCacheKey = function() {
      return (i ? i.call(this) : "") + "_mrtGBuffer_" + n;
    }, this.patchedMaterials.add(e), e.needsUpdate = !0, e.program && (e.program = null), !0;
  }
  /**
   * 恢复场景中所有材质的原始onBeforeCompile
   * 
   * @param {Scene} scene - Three.js场景
   * @returns {number} 恢复的材质数量
   */
  unpatchScene(e) {
    let t = 0;
    return e.traverse((r) => {
      r.isMesh && r.material && (Array.isArray(r.material) ? r.material : [r.material]).forEach((i) => {
        this.unpatchMaterial(i) && t++;
      });
    }), this.debug && t > 0 && console.log(`🔧 MRTGBufferMaterialPatcher: 已恢复${t}个材质`), t;
  }
  /**
   * 恢复单个材质
   * 
   * @param {Material} material - 要恢复的材质
   * @returns {boolean} 是否成功恢复
   */
  unpatchMaterial(e) {
    if (!this.patchedMaterials.has(e))
      return !1;
    const t = this.originalOnBeforeCompile.get(e);
    return t ? (e.onBeforeCompile = t, this.originalOnBeforeCompile.delete(e)) : e.onBeforeCompile = function() {
    }, delete e.customProgramCacheKey, this.patchedMaterials.delete(e), e.needsUpdate = !0, !0;
  }
  /**
   * 清除所有注入
   */
  clear() {
    this.patchedMaterials = /* @__PURE__ */ new WeakSet(), this.originalOnBeforeCompile = /* @__PURE__ */ new WeakMap();
  }
  /**
   * 设置调试模式
   * 
   * @param {boolean} enabled - 是否启用
   */
  setDebug(e) {
    this.debug = e;
  }
}
const Xn = [
  new Float32Array(3),
  new Float32Array(3)
], re = [
  new Float32Array(3),
  new Float32Array(3),
  new Float32Array(3),
  new Float32Array(3)
], ct = [
  [
    new Float32Array([0, 0, 0]),
    new Float32Array([1, 0, 0]),
    new Float32Array([1, 1, 0]),
    new Float32Array([1, 1, 1])
  ],
  [
    new Float32Array([0, 0, 0]),
    new Float32Array([1, 0, 0]),
    new Float32Array([1, 0, 1]),
    new Float32Array([1, 1, 1])
  ],
  [
    new Float32Array([0, 0, 0]),
    new Float32Array([0, 0, 1]),
    new Float32Array([1, 0, 1]),
    new Float32Array([1, 1, 1])
  ],
  [
    new Float32Array([0, 0, 0]),
    new Float32Array([0, 1, 0]),
    new Float32Array([1, 1, 0]),
    new Float32Array([1, 1, 1])
  ],
  [
    new Float32Array([0, 0, 0]),
    new Float32Array([0, 1, 0]),
    new Float32Array([0, 1, 1]),
    new Float32Array([1, 1, 1])
  ],
  [
    new Float32Array([0, 0, 0]),
    new Float32Array([0, 0, 1]),
    new Float32Array([0, 1, 1]),
    new Float32Array([1, 1, 1])
  ]
];
function Zt(c, e, t, r) {
  const n = t[0] - e[0], i = t[1] - e[1], s = t[2] - e[2], o = c[0] - e[0], l = c[1] - e[1], u = c[2] - e[2], f = i * u - s * l, h = s * o - n * u, m = n * l - i * o, p = Math.sqrt(f * f + h * h + m * m), d = p * 0.5, v = f / p, x = h / p, w = m / p, M = -(c[0] * v + c[1] * x + c[2] * w), D = r[0] * v + r[1] * x + r[2] * w;
  return Math.abs(D + M) * d / 3;
}
function At(c, e, t, r, n, i) {
  const s = (t + r * e + n * e * e) * 4;
  i[0] = c[s + 0], i[1] = c[s + 1], i[2] = c[s + 2];
}
function nu(c, e, t, r, n, i) {
  const s = t * (e - 1), o = r * (e - 1), l = n * (e - 1), u = Math.floor(s), f = Math.floor(o), h = Math.floor(l), m = Math.ceil(s), p = Math.ceil(o), d = Math.ceil(l), v = s - u, x = o - f, w = l - h;
  if (u === s && f === o && h === l)
    At(c, e, s, o, l, i);
  else {
    let M;
    v >= x && x >= w ? M = ct[0] : v >= w && w >= x ? M = ct[1] : w >= v && v >= x ? M = ct[2] : x >= v && v >= w ? M = ct[3] : x >= w && w >= v ? M = ct[4] : w >= x && x >= v && (M = ct[5]);
    const [D, P, O, S] = M, L = Xn[0];
    L[0] = v, L[1] = x, L[2] = w;
    const H = Xn[1], Z = m - u, z = p - f, k = d - h;
    H[0] = Z * D[0] + u, H[1] = z * D[1] + f, H[2] = k * D[2] + h, At(c, e, H[0], H[1], H[2], re[0]), H[0] = Z * P[0] + u, H[1] = z * P[1] + f, H[2] = k * P[2] + h, At(c, e, H[0], H[1], H[2], re[1]), H[0] = Z * O[0] + u, H[1] = z * O[1] + f, H[2] = k * O[2] + h, At(c, e, H[0], H[1], H[2], re[2]), H[0] = Z * S[0] + u, H[1] = z * S[1] + f, H[2] = k * S[2] + h, At(c, e, H[0], H[1], H[2], re[3]);
    const R = Zt(P, O, S, L) * 6, U = Zt(D, O, S, L) * 6, q = Zt(D, P, S, L) * 6, ie = Zt(D, P, O, L) * 6;
    re[0][0] *= R, re[0][1] *= R, re[0][2] *= R, re[1][0] *= U, re[1][1] *= U, re[1][2] *= U, re[2][0] *= q, re[2][1] *= q, re[2][2] *= q, re[3][0] *= ie, re[3][1] *= ie, re[3][2] *= ie, i[0] = re[0][0] + re[1][0] + re[2][0] + re[3][0], i[1] = re[0][1] + re[1][1] + re[2][1] + re[3][1], i[2] = re[0][2] + re[1][2] + re[2][2] + re[3][2];
  }
}
class Ud {
  /**
   * Expands the given data to the target size.
   *
   * @param {TypedArray} data - The input RGBA data. Assumed to be cubic.
   * @param {Number} size - The target size.
   * @return {TypedArray} The new data.
   */
  static expand(e, t) {
    const r = Math.cbrt(e.length / 4), n = new Float32Array(3), i = new e.constructor(t ** 3 * 4), s = e instanceof Uint8Array ? 255 : 1, o = t ** 2, l = 1 / (t - 1);
    for (let u = 0; u < t; ++u)
      for (let f = 0; f < t; ++f)
        for (let h = 0; h < t; ++h) {
          const m = h * l, p = f * l, d = u * l, v = Math.round(h + f * t + u * o) * 4;
          nu(e, r, m, p, d, n), i[v + 0] = n[0], i[v + 1] = n[1], i[v + 2] = n[2], i[v + 3] = s;
        }
    return i;
  }
}
const St = [
  new Float32Array(2),
  new Float32Array(2)
], ut = 16, Cr = 20, ft = 30, iu = 32, Ar = new Float32Array([
  0,
  -0.25,
  0.25,
  -0.125,
  0.125,
  -0.375,
  0.375
]), Yn = [
  new Float32Array([0, 0]),
  new Float32Array([0.25, -0.25]),
  new Float32Array([-0.25, 0.25]),
  new Float32Array([0.125, -0.125]),
  new Float32Array([-0.125, 0.125])
], su = [
  new Uint8Array([0, 0]),
  new Uint8Array([3, 0]),
  new Uint8Array([0, 3]),
  new Uint8Array([3, 3]),
  new Uint8Array([1, 0]),
  new Uint8Array([4, 0]),
  new Uint8Array([1, 3]),
  new Uint8Array([4, 3]),
  new Uint8Array([0, 1]),
  new Uint8Array([3, 1]),
  new Uint8Array([0, 4]),
  new Uint8Array([3, 4]),
  new Uint8Array([1, 1]),
  new Uint8Array([4, 1]),
  new Uint8Array([1, 4]),
  new Uint8Array([4, 4])
], Yi = [
  new Uint8Array([0, 0]),
  new Uint8Array([1, 0]),
  new Uint8Array([0, 2]),
  new Uint8Array([1, 2]),
  new Uint8Array([2, 0]),
  new Uint8Array([3, 0]),
  new Uint8Array([2, 2]),
  new Uint8Array([3, 2]),
  new Uint8Array([0, 1]),
  new Uint8Array([1, 1]),
  new Uint8Array([0, 3]),
  new Uint8Array([1, 3]),
  new Uint8Array([2, 1]),
  new Uint8Array([3, 1]),
  new Uint8Array([2, 3]),
  new Uint8Array([3, 3])
];
function Kt(c, e, t) {
  return c + (e - c) * t;
}
function au(c) {
  return Math.min(Math.max(c, 0), 1);
}
function Zn(c) {
  const e = St[0], t = St[1], r = Math.sqrt(e[0] * 2) * 0.5, n = Math.sqrt(e[1] * 2) * 0.5, i = Math.sqrt(t[0] * 2) * 0.5, s = Math.sqrt(t[1] * 2) * 0.5, o = au(c / iu);
  e[0] = Kt(r, e[0], o), e[1] = Kt(n, e[1], o), t[0] = Kt(i, t[0], o), t[1] = Kt(s, t[1], o);
}
function ae(c, e, t, r, n, i) {
  const s = t - c, o = r - e, l = n, u = n + 1, f = e + o * (l - c) / s, h = e + o * (u - c) / s;
  if (l >= c && l < t || u > c && u <= t)
    if (Math.sign(f) === Math.sign(h) || Math.abs(f) < 1e-4 || Math.abs(h) < 1e-4) {
      const m = (f + h) / 2;
      m < 0 ? (i[0] = Math.abs(m), i[1] = 0) : (i[0] = 0, i[1] = Math.abs(m));
    } else {
      const m = -e * s / o + c, p = Math.trunc(m), d = m > c ? f * (m - p) / 2 : 0, v = m < t ? h * (1 - (m - p)) / 2 : 0;
      (Math.abs(d) > Math.abs(v) ? d : -v) < 0 ? (i[0] = Math.abs(d), i[1] = Math.abs(v)) : (i[0] = Math.abs(v), i[1] = Math.abs(d));
    }
  else
    i[0] = 0, i[1] = 0;
  return i;
}
function ou(c, e, t, r, n) {
  const i = St[0], s = St[1], o = 0.5 + r, l = 0.5 + r - 1, u = e + t + 1;
  switch (c) {
    case 0: {
      n[0] = 0, n[1] = 0;
      break;
    }
    case 1: {
      e <= t ? ae(0, l, u / 2, 0, e, n) : (n[0] = 0, n[1] = 0);
      break;
    }
    case 2: {
      e >= t ? ae(u / 2, 0, u, l, e, n) : (n[0] = 0, n[1] = 0);
      break;
    }
    case 3: {
      ae(0, l, u / 2, 0, e, i), ae(u / 2, 0, u, l, e, s), Zn(u), n[0] = i[0] + s[0], n[1] = i[1] + s[1];
      break;
    }
    case 4: {
      e <= t ? ae(0, o, u / 2, 0, e, n) : (n[0] = 0, n[1] = 0);
      break;
    }
    case 5: {
      n[0] = 0, n[1] = 0;
      break;
    }
    case 6: {
      Math.abs(r) > 0 ? (ae(0, o, u, l, e, i), ae(0, o, u / 2, 0, e, s), ae(u / 2, 0, u, l, e, n), s[0] = s[0] + n[0], s[1] = s[1] + n[1], n[0] = (i[0] + s[0]) / 2, n[1] = (i[1] + s[1]) / 2) : ae(0, o, u, l, e, n);
      break;
    }
    case 7: {
      ae(0, o, u, l, e, n);
      break;
    }
    case 8: {
      e >= t ? ae(u / 2, 0, u, o, e, n) : (n[0] = 0, n[1] = 0);
      break;
    }
    case 9: {
      Math.abs(r) > 0 ? (ae(0, l, u, o, e, i), ae(0, l, u / 2, 0, e, s), ae(u / 2, 0, u, o, e, n), s[0] = s[0] + n[0], s[1] = s[1] + n[1], n[0] = (i[0] + s[0]) / 2, n[1] = (i[1] + s[1]) / 2) : ae(0, l, u, o, e, n);
      break;
    }
    case 10: {
      n[0] = 0, n[1] = 0;
      break;
    }
    case 11: {
      ae(0, l, u, o, e, n);
      break;
    }
    case 12: {
      ae(0, o, u / 2, 0, e, i), ae(u / 2, 0, u, o, e, s), Zn(u), n[0] = i[0] + s[0], n[1] = i[1] + s[1];
      break;
    }
    case 13: {
      ae(0, l, u, o, e, n);
      break;
    }
    case 14: {
      ae(0, o, u, l, e, n);
      break;
    }
    case 15: {
      n[0] = 0, n[1] = 0;
      break;
    }
  }
  return n;
}
function lu(c, e, t, r, n, i) {
  let s = c === t && e === r;
  if (!s) {
    const o = (c + t) / 2, l = (e + r) / 2, u = r - e, f = c - t;
    s = u * (n - o) + f * (i - l) > 0;
  }
  return s;
}
function Kn(c, e, t, r, n, i) {
  let s = 0;
  for (let o = 0; o < ft; ++o)
    for (let l = 0; l < ft; ++l) {
      const u = l / (ft - 1), f = o / (ft - 1);
      lu(c, e, t, r, n + u, i + f) && ++s;
    }
  return s / (ft * ft);
}
function te(c, e, t, r, n, i, s, o) {
  const l = Yi[c], u = l[0], f = l[1];
  return u > 0 && (e += s[0], t += s[1]), f > 0 && (r += s[0], n += s[1]), o[0] = 1 - Kn(e, t, r, n, 1 + i, 0 + i), o[1] = Kn(e, t, r, n, 1 + i, 1 + i), o;
}
function cu(c, e, t, r, n) {
  const i = St[0], s = St[1], o = e + t + 1;
  switch (c) {
    case 0: {
      te(c, 1, 1, 1 + o, 1 + o, e, r, i), te(c, 1, 0, 1 + o, 0 + o, e, r, s), n[0] = (i[0] + s[0]) / 2, n[1] = (i[1] + s[1]) / 2;
      break;
    }
    case 1: {
      te(c, 1, 0, 0 + o, 0 + o, e, r, i), te(c, 1, 0, 1 + o, 0 + o, e, r, s), n[0] = (i[0] + s[0]) / 2, n[1] = (i[1] + s[1]) / 2;
      break;
    }
    case 2: {
      te(c, 0, 0, 1 + o, 0 + o, e, r, i), te(c, 1, 0, 1 + o, 0 + o, e, r, s), n[0] = (i[0] + s[0]) / 2, n[1] = (i[1] + s[1]) / 2;
      break;
    }
    case 3: {
      te(c, 1, 0, 1 + o, 0 + o, e, r, n);
      break;
    }
    case 4: {
      te(c, 1, 1, 0 + o, 0 + o, e, r, i), te(c, 1, 1, 1 + o, 0 + o, e, r, s), n[0] = (i[0] + s[0]) / 2, n[1] = (i[1] + s[1]) / 2;
      break;
    }
    case 5: {
      te(c, 1, 1, 0 + o, 0 + o, e, r, i), te(c, 1, 0, 1 + o, 0 + o, e, r, s), n[0] = (i[0] + s[0]) / 2, n[1] = (i[1] + s[1]) / 2;
      break;
    }
    case 6: {
      te(c, 1, 1, 1 + o, 0 + o, e, r, n);
      break;
    }
    case 7: {
      te(c, 1, 1, 1 + o, 0 + o, e, r, i), te(c, 1, 0, 1 + o, 0 + o, e, r, s), n[0] = (i[0] + s[0]) / 2, n[1] = (i[1] + s[1]) / 2;
      break;
    }
    case 8: {
      te(c, 0, 0, 1 + o, 1 + o, e, r, i), te(c, 1, 0, 1 + o, 1 + o, e, r, s), n[0] = (i[0] + s[0]) / 2, n[1] = (i[1] + s[1]) / 2;
      break;
    }
    case 9: {
      te(c, 1, 0, 1 + o, 1 + o, e, r, n), te(c, 1, 0, 1 + o, 1 + o, e, r, n);
      break;
    }
    case 10: {
      te(c, 0, 0, 1 + o, 1 + o, e, r, i), te(c, 1, 0, 1 + o, 0 + o, e, r, s), n[0] = (i[0] + s[0]) / 2, n[1] = (i[1] + s[1]) / 2;
      break;
    }
    case 11: {
      te(c, 1, 0, 1 + o, 1 + o, e, r, i), te(c, 1, 0, 1 + o, 0 + o, e, r, s), n[0] = (i[0] + s[0]) / 2, n[1] = (i[1] + s[1]) / 2;
      break;
    }
    case 12: {
      te(c, 1, 1, 1 + o, 1 + o, e, r, n);
      break;
    }
    case 13: {
      te(c, 1, 1, 1 + o, 1 + o, e, r, i), te(c, 1, 0, 1 + o, 1 + o, e, r, s), n[0] = (i[0] + s[0]) / 2, n[1] = (i[1] + s[1]) / 2;
      break;
    }
    case 14: {
      te(c, 1, 1, 1 + o, 1 + o, e, r, i), te(c, 1, 1, 1 + o, 0 + o, e, r, s), n[0] = (i[0] + s[0]) / 2, n[1] = (i[1] + s[1]) / 2;
      break;
    }
    case 15: {
      te(c, 1, 1, 1 + o, 1 + o, e, r, i), te(c, 1, 0, 1 + o, 0 + o, e, r, s), n[0] = (i[0] + s[0]) / 2, n[1] = (i[1] + s[1]) / 2;
      break;
    }
  }
  return n;
}
function Qn(c, e, t) {
  const r = new Float32Array(2);
  for (let n = 0, i = c.length; n < i; ++n) {
    const s = c[n], o = s.data, l = s.width;
    for (let u = 0; u < l; ++u)
      for (let f = 0; f < l; ++f) {
        t ? ou(n, f, u, e, r) : cu(n, f, u, e, r);
        const h = (u * l + f) * 2;
        o[h] = r[0] * 255, o[h + 1] = r[1] * 255;
      }
  }
}
function qn(c, e, t, r, n, i, s) {
  const o = s.data, l = s.width;
  for (let u = 0, f = t.length; u < f; ++u) {
    const h = r[u], m = t[u], p = m.data, d = m.width;
    for (let v = 0; v < n; ++v)
      for (let x = 0; x < n; ++x) {
        const w = h[0] * n + c + x, D = ((h[1] * n + e + v) * l + w) * 4, P = i ? (v * v * d + x * x) * 2 : (v * d + x) * 2;
        o[D] = p[P], o[D + 1] = p[P + 1], o[D + 2] = 0, o[D + 3] = 255;
      }
  }
}
class Od {
  /**
   * Creates a new area image.
   *
   * @return {RawImageData} The generated image data.
   */
  static generate() {
    const e = 10 * ut, t = Ar.length * 5 * ut, r = new Uint8ClampedArray(e * t * 4), n = new Ge(e, t, r), i = Math.pow(ut - 1, 2) + 1, s = Cr, o = [], l = [];
    for (let u = 3, f = r.length; u < f; u += 4)
      r[u] = 255;
    for (let u = 0; u < 16; ++u)
      o.push(new Ge(
        i,
        i,
        new Uint8ClampedArray(i * i * 2),
        2
      )), l.push(new Ge(
        s,
        s,
        new Uint8ClampedArray(s * s * 2),
        2
      ));
    for (let u = 0, f = Ar.length; u < f; ++u)
      Qn(o, Ar[u], !0), qn(
        0,
        5 * ut * u,
        o,
        su,
        ut,
        !0,
        n
      );
    for (let u = 0, f = Yn.length; u < f; ++u)
      Qn(l, Yn[u], !1), qn(
        5 * ut,
        4 * Cr * u,
        l,
        Yi,
        Cr,
        !1,
        n
      );
    return n;
  }
}
const uu = "data:text/plain;base64,";
function fu(c = !0) {
  const e = URL.createObjectURL(new Blob([uu], {
    type: "text/javascript"
  })), t = new Worker(e);
  return URL.revokeObjectURL(e), new Promise((r, n) => {
    t.addEventListener("error", (i) => n(i.error)), t.addEventListener("message", (i) => {
      const s = Ge.from(i.data.searchImageData), o = Ge.from(i.data.areaImageData), l = [
        s.toCanvas().toDataURL("image/png", 1),
        o.toCanvas().toDataURL("image/png", 1)
      ];
      c && (localStorage.setItem("smaa-search", l[0]), localStorage.setItem("smaa-area", l[1])), r(l);
    }), t.postMessage(null);
  });
}
class kd {
  /**
   * Constructs a new SMAA image generator.
   */
  constructor() {
    this.disableCache = !1;
  }
  /**
   * Enables or disables caching via localStorage.
   *
   * @param {Boolean} value - Whether the cache should be enabled.
   */
  setCacheEnabled(e) {
    this.disableCache = !e;
  }
  /**
   * Generates the SMAA data images.
   *
   * @example
   * SMAAImageGenerator.generate().then(([search, area]) => {
   *   const smaaEffect = new SMAAEffect(search, area);
   * });
   * @return {Promise<Image[]>} A promise that returns the search image and area image as a pair.
   */
  generate() {
    const e = !this.disableCache && window.localStorage !== void 0, t = e ? [
      localStorage.getItem("smaa-search"),
      localStorage.getItem("smaa-area")
    ] : [null, null];
    return (t[0] !== null && t[1] !== null ? Promise.resolve(t) : fu(e)).then((n) => new Promise((i, s) => {
      const o = new Image(), l = new Image(), u = new kt();
      u.onLoad = () => i([o, l]), u.onError = s, o.addEventListener("error", (f) => u.itemError("smaa-search")), l.addEventListener("error", (f) => u.itemError("smaa-area")), o.addEventListener("load", () => u.itemEnd("smaa-search")), l.addEventListener("load", () => u.itemEnd("smaa-area")), u.itemStart("smaa-search"), u.itemStart("smaa-area"), o.src = n[0], l.src = n[1];
    }));
  }
}
const Qt = /* @__PURE__ */ new Map([
  [he(0, 0, 0, 0), new Float32Array([0, 0, 0, 0])],
  [he(0, 0, 0, 1), new Float32Array([0, 0, 0, 1])],
  [he(0, 0, 1, 0), new Float32Array([0, 0, 1, 0])],
  [he(0, 0, 1, 1), new Float32Array([0, 0, 1, 1])],
  [he(0, 1, 0, 0), new Float32Array([0, 1, 0, 0])],
  [he(0, 1, 0, 1), new Float32Array([0, 1, 0, 1])],
  [he(0, 1, 1, 0), new Float32Array([0, 1, 1, 0])],
  [he(0, 1, 1, 1), new Float32Array([0, 1, 1, 1])],
  [he(1, 0, 0, 0), new Float32Array([1, 0, 0, 0])],
  [he(1, 0, 0, 1), new Float32Array([1, 0, 0, 1])],
  [he(1, 0, 1, 0), new Float32Array([1, 0, 1, 0])],
  [he(1, 0, 1, 1), new Float32Array([1, 0, 1, 1])],
  [he(1, 1, 0, 0), new Float32Array([1, 1, 0, 0])],
  [he(1, 1, 0, 1), new Float32Array([1, 1, 0, 1])],
  [he(1, 1, 1, 0), new Float32Array([1, 1, 1, 0])],
  [he(1, 1, 1, 1), new Float32Array([1, 1, 1, 1])]
]);
function Er(c, e, t) {
  return c + (e - c) * t;
}
function he(c, e, t, r) {
  const n = Er(c, e, 0.75), i = Er(t, r, 1 - 0.25);
  return Er(n, i, 1 - 0.125);
}
function hu(c, e) {
  let t = 0;
  return e[3] === 1 && (t += 1), t === 1 && e[2] === 1 && c[1] !== 1 && c[3] !== 1 && (t += 1), t;
}
function du(c, e) {
  let t = 0;
  return e[3] === 1 && c[1] !== 1 && c[3] !== 1 && (t += 1), t === 1 && e[2] === 1 && c[0] !== 1 && c[2] !== 1 && (t += 1), t;
}
class Vd {
  /**
   * Creates a new search image.
   *
   * @return {RawImageData} The generated image data.
   */
  static generate() {
    const s = new Uint8ClampedArray(2178), o = new Uint8ClampedArray(1024 * 4);
    for (let l = 0; l < 33; ++l)
      for (let u = 0; u < 66; ++u) {
        const f = 0.03125 * u, h = 0.03125 * l;
        if (Qt.has(f) && Qt.has(h)) {
          const m = Qt.get(f), p = Qt.get(h), d = l * 66 + u;
          s[d] = 127 * hu(m, p), s[d + 33] = 127 * du(m, p);
        }
      }
    for (let l = 0, u = 17; u < 33; ++u)
      for (let f = 0; f < 64; ++f, l += 4)
        o[l] = s[u * 66 + f], o[l + 3] = 255;
    return new Ge(64, 16, o);
  }
}
class Hd {
  /**
   * 创建性能监控器
   * @param {Object} options - 配置选项
   */
  constructor(e = {}) {
    this.enabled = e.enabled !== !1, this.maxSamples = e.maxSamples || 60, this.updateInterval = e.updateInterval || 1e3, this.frameCount = 0, this.lastTime = performance.now(), this.fps = 0, this.deltaTime = 0, this.fpsHistory = [], this.renderTimeHistory = [], this.memoryHistory = [], this.renderTimes = {
      frame: [],
      // 整帧渲染时间
      ssr: [],
      // SSR Pass时间
      blur: [],
      // 模糊Pass时间
      composite: []
      // 合成时间
    }, this.memoryUsage = {
      used: 0,
      total: 0,
      limit: 0
    }, this.gpuStats = {
      drawCalls: 0,
      triangles: 0,
      geometries: 0,
      textures: 0
    }, this.thresholds = {
      fps: {
        good: e.fpsGood || 55,
        warning: e.fpsWarning || 45,
        critical: e.fpsCritical || 30
      },
      renderTime: {
        good: e.renderGood || 8,
        // ms
        warning: e.renderWarning || 12,
        critical: e.renderCritical || 20
      },
      memory: {
        warning: e.memoryWarning || 100,
        // MB
        critical: e.memoryCritical || 200
      }
    }, this.alerts = [], this.alertCallbacks = [], this.autoReportInterval = null, e.autoReport && this.startAutoReport(e.reportInterval || 5e3), console.log("PerformanceMonitor: 初始化完成");
  }
  /**
   * 更新帧统计
   * @param {Number} deltaTime - 帧时间差（可选）
   */
  update(e = null) {
    if (!this.enabled) return;
    const t = performance.now();
    e === null && (e = t - this.lastTime), this.deltaTime = e, this.frameCount++, t - this.lastTime >= this.updateInterval && (this.fps = this.frameCount * 1e3 / (t - this.lastTime), this.frameCount = 0, this.lastTime = t, this._updateHistory(), this._checkAlerts());
  }
  /**
   * 记录渲染时间
   * @param {String} type - 渲染类型 ('frame', 'ssr', 'blur', 'composite')
   * @param {Number} time - 渲染时间（毫秒）
   */
  recordRenderTime(e, t) {
    !this.enabled || !this.renderTimes[e] || (this.renderTimes[e].push(t), this.renderTimes[e].length > this.maxSamples && this.renderTimes[e].shift());
  }
  /**
   * 开始计时
   * @param {String} label - 计时标签
   * @returns {Function} 结束计时的函数
   */
  startTimer(e) {
    if (!this.enabled) return () => {
    };
    const t = performance.now();
    return () => {
      const n = performance.now() - t;
      return this.recordRenderTime(e, n), n;
    };
  }
  /**
   * 记录GPU统计
   * @param {Object} stats - GPU统计数据
   */
  recordGPUStats(e) {
    this.enabled && Object.assign(this.gpuStats, e);
  }
  /**
   * 更新内存使用统计
   */
  updateMemoryUsage() {
    !this.enabled || !performance.memory || (this.memoryUsage = {
      used: performance.memory.usedJSHeapSize / (1024 * 1024),
      // MB
      total: performance.memory.totalJSHeapSize / (1024 * 1024),
      limit: performance.memory.jsHeapSizeLimit / (1024 * 1024)
    });
  }
  /**
   * 更新历史数据
   * @private
   */
  _updateHistory() {
    this.fpsHistory.push(this.fps), this.fpsHistory.length > this.maxSamples && this.fpsHistory.shift();
    const e = this.getAverageRenderTime("frame");
    this.renderTimeHistory.push(e), this.renderTimeHistory.length > this.maxSamples && this.renderTimeHistory.shift(), this.updateMemoryUsage(), this.memoryUsage.used > 0 && (this.memoryHistory.push(this.memoryUsage.used), this.memoryHistory.length > this.maxSamples && this.memoryHistory.shift());
  }
  /**
   * 检查性能警报
   * @private
   */
  _checkAlerts() {
    const e = [];
    this.fps < this.thresholds.fps.critical ? e.push({
      type: "fps",
      level: "critical",
      message: `FPS严重不足: ${this.fps.toFixed(1)}`,
      value: this.fps,
      threshold: this.thresholds.fps.critical
    }) : this.fps < this.thresholds.fps.warning && e.push({
      type: "fps",
      level: "warning",
      message: `FPS偏低: ${this.fps.toFixed(1)}`,
      value: this.fps,
      threshold: this.thresholds.fps.warning
    });
    const t = this.getAverageRenderTime("frame");
    t > this.thresholds.renderTime.critical ? e.push({
      type: "renderTime",
      level: "critical",
      message: `渲染时间过长: ${t.toFixed(2)}ms`,
      value: t,
      threshold: this.thresholds.renderTime.critical
    }) : t > this.thresholds.renderTime.warning && e.push({
      type: "renderTime",
      level: "warning",
      message: `渲染时间偏高: ${t.toFixed(2)}ms`,
      value: t,
      threshold: this.thresholds.renderTime.warning
    }), this.memoryUsage.used > this.thresholds.memory.critical ? e.push({
      type: "memory",
      level: "critical",
      message: `内存使用过高: ${this.memoryUsage.used.toFixed(1)}MB`,
      value: this.memoryUsage.used,
      threshold: this.thresholds.memory.critical
    }) : this.memoryUsage.used > this.thresholds.memory.warning && e.push({
      type: "memory",
      level: "warning",
      message: `内存使用偏高: ${this.memoryUsage.used.toFixed(1)}MB`,
      value: this.memoryUsage.used,
      threshold: this.thresholds.memory.warning
    }), e.length > 0 ? (this.alerts = e, this.alertCallbacks.forEach((r) => {
      try {
        r(e);
      } catch (n) {
        console.error("PerformanceMonitor: 警报回调执行失败", n);
      }
    })) : this.alerts = [];
  }
  /**
   * 获取平均渲染时间
   * @param {String} type - 渲染类型
   * @returns {Number} 平均时间（毫秒）
   */
  getAverageRenderTime(e = "frame") {
    const t = this.renderTimes[e];
    return !t || t.length === 0 ? 0 : t.reduce((r, n) => r + n, 0) / t.length;
  }
  /**
   * 获取最大渲染时间
   * @param {String} type - 渲染类型
   * @returns {Number} 最大时间（毫秒）
   */
  getMaxRenderTime(e = "frame") {
    const t = this.renderTimes[e];
    return !t || t.length === 0 ? 0 : Math.max(...t);
  }
  /**
   * 获取最小渲染时间
   * @param {String} type - 渲染类型
   * @returns {Number} 最小时间（毫秒）
   */
  getMinRenderTime(e = "frame") {
    const t = this.renderTimes[e];
    return !t || t.length === 0 ? 0 : Math.min(...t);
  }
  /**
   * 获取性能等级
   * @returns {String} 'good', 'warning', 'critical'
   */
  getPerformanceLevel() {
    return this.alerts.some((e) => e.level === "critical") ? "critical" : this.alerts.some((e) => e.level === "warning") ? "warning" : "good";
  }
  /**
   * 获取完整统计数据
   * @returns {Object} 统计数据
   */
  getStats() {
    return {
      // 基础性能指标
      fps: this.fps,
      deltaTime: this.deltaTime,
      performanceLevel: this.getPerformanceLevel(),
      // 渲染时间统计
      renderTime: {
        frame: {
          avg: this.getAverageRenderTime("frame"),
          max: this.getMaxRenderTime("frame"),
          min: this.getMinRenderTime("frame"),
          current: this.renderTimes.frame[this.renderTimes.frame.length - 1] || 0
        },
        ssr: {
          avg: this.getAverageRenderTime("ssr"),
          max: this.getMaxRenderTime("ssr"),
          min: this.getMinRenderTime("ssr"),
          current: this.renderTimes.ssr[this.renderTimes.ssr.length - 1] || 0
        },
        blur: {
          avg: this.getAverageRenderTime("blur"),
          max: this.getMaxRenderTime("blur"),
          min: this.getMinRenderTime("blur"),
          current: this.renderTimes.blur[this.renderTimes.blur.length - 1] || 0
        },
        composite: {
          avg: this.getAverageRenderTime("composite"),
          max: this.getMaxRenderTime("composite"),
          min: this.getMinRenderTime("composite"),
          current: this.renderTimes.composite[this.renderTimes.composite.length - 1] || 0
        }
      },
      // 内存统计
      memory: {
        ...this.memoryUsage,
        efficiency: this.memoryUsage.total > 0 ? this.memoryUsage.used / this.memoryUsage.total : 0
      },
      // GPU统计
      gpu: { ...this.gpuStats },
      // 历史数据
      history: {
        fps: [...this.fpsHistory],
        renderTime: [...this.renderTimeHistory],
        memory: [...this.memoryHistory]
      },
      // 警报信息
      alerts: [...this.alerts],
      // 阈值设置
      thresholds: { ...this.thresholds }
    };
  }
  /**
   * 添加警报回调
   * @param {Function} callback - 警报回调函数
   */
  addAlertCallback(e) {
    typeof e == "function" && this.alertCallbacks.push(e);
  }
  /**
   * 移除警报回调
   * @param {Function} callback - 要移除的回调函数
   */
  removeAlertCallback(e) {
    const t = this.alertCallbacks.indexOf(e);
    t !== -1 && this.alertCallbacks.splice(t, 1);
  }
  /**
   * 开始自动报告
   * @param {Number} interval - 报告间隔（毫秒）
   */
  startAutoReport(e = 5e3) {
    this.stopAutoReport(), this.autoReportInterval = setInterval(() => {
      this.printReport();
    }, e), console.log(`PerformanceMonitor: 开始自动报告，间隔 ${e}ms`);
  }
  /**
   * 停止自动报告
   */
  stopAutoReport() {
    this.autoReportInterval && (clearInterval(this.autoReportInterval), this.autoReportInterval = null);
  }
  /**
   * 打印性能报告
   */
  printReport() {
    if (!this.enabled) return;
    const e = this.getStats();
    console.group("🔍 性能监控报告"), console.log("📊 基础指标:"), console.log(`  FPS: ${e.fps.toFixed(1)} (${e.performanceLevel})`), console.log(`  帧时间: ${e.deltaTime.toFixed(2)}ms`), console.log("⏱️ 渲染时间:"), console.log(`  整帧: ${e.renderTime.frame.avg.toFixed(2)}ms (平均)`), console.log(`  SSR: ${e.renderTime.ssr.avg.toFixed(2)}ms (平均)`), console.log(`  模糊: ${e.renderTime.blur.avg.toFixed(2)}ms (平均)`), console.log(`  合成: ${e.renderTime.composite.avg.toFixed(2)}ms (平均)`), console.log("💾 内存使用:"), console.log(`  已用: ${e.memory.used.toFixed(1)}MB`), console.log(`  总计: ${e.memory.total.toFixed(1)}MB`), console.log(`  效率: ${(e.memory.efficiency * 100).toFixed(1)}%`), e.alerts.length > 0 && (console.warn("⚠️ 性能警报:"), e.alerts.forEach((t) => {
      console.warn(`  ${t.message}`);
    })), console.groupEnd();
  }
  /**
   * 重置统计数据
   */
  reset() {
    this.frameCount = 0, this.lastTime = performance.now(), this.fps = 0, this.deltaTime = 0, this.fpsHistory = [], this.renderTimeHistory = [], this.memoryHistory = [], Object.keys(this.renderTimes).forEach((e) => {
      this.renderTimes[e] = [];
    }), this.alerts = [], console.log("PerformanceMonitor: 统计数据已重置");
  }
  /**
   * 启用性能监控
   */
  enable() {
    this.enabled = !0, console.log("PerformanceMonitor: 已启用");
  }
  /**
   * 禁用性能监控
   */
  disable() {
    this.enabled = !1, this.stopAutoReport(), console.log("PerformanceMonitor: 已禁用");
  }
  /**
   * 销毁监控器
   */
  dispose() {
    this.stopAutoReport(), this.alertCallbacks = [], this.reset(), this.enabled = !1, console.log("PerformanceMonitor: 已销毁");
  }
  /**
   * 创建性能图表数据
   * @param {String} type - 数据类型 ('fps', 'renderTime', 'memory')
   * @returns {Object} 图表数据
   */
  getChartData(e) {
    const t = Array.from({ length: this.maxSamples }, (r, n) => n + 1);
    switch (e) {
      case "fps":
        return {
          labels: t,
          datasets: [{
            label: "FPS",
            data: this.fpsHistory,
            borderColor: "rgb(75, 192, 192)",
            backgroundColor: "rgba(75, 192, 192, 0.2)",
            tension: 0.1
          }]
        };
      case "renderTime":
        return {
          labels: t,
          datasets: [
            {
              label: "整帧",
              data: this.renderTimeHistory,
              borderColor: "rgb(255, 99, 132)",
              backgroundColor: "rgba(255, 99, 132, 0.2)",
              tension: 0.1
            },
            {
              label: "SSR",
              data: this.renderTimes.ssr.slice(-this.maxSamples),
              borderColor: "rgb(54, 162, 235)",
              backgroundColor: "rgba(54, 162, 235, 0.2)",
              tension: 0.1
            }
          ]
        };
      case "memory":
        return {
          labels: t,
          datasets: [{
            label: "内存使用 (MB)",
            data: this.memoryHistory,
            borderColor: "rgb(153, 102, 255)",
            backgroundColor: "rgba(153, 102, 255, 0.2)",
            tension: 0.1
          }]
        };
      default:
        return null;
    }
  }
}
let Gd = class extends an {
  constructor(e, t, r, n = {}) {
    n.mode = "ssr", super(e, t, r, n);
  }
}, Zi = class extends X {
  constructor(e, t, r) {
    super(), this._camera = e, this._scene = t, this.renderTarget = new A(1, 1, {
      type: K,
      depthBuffer: !1
    });
    const n = r;
    this.fullscreenMaterial = new N({
      fragmentShader: n,
      vertexShader: We,
      uniforms: {
        depthTexture: { value: null },
        normalTexture: { value: null },
        cameraNear: { value: 0 },
        cameraFar: { value: 0 },
        viewMatrix: { value: this._camera.matrixWorldInverse },
        projectionViewMatrix: { value: new C() },
        projectionMatrixInverse: { value: this._camera.projectionMatrixInverse },
        cameraMatrixWorld: { value: this._camera.matrixWorld },
        resolution: { value: new g() },
        blueNoiseTexture: { value: null },
        blueNoiseRepeat: { value: new g() },
        aoDistance: { value: 0 },
        distancePower: { value: 0 },
        bias: { value: 0 },
        thickness: { value: 0 },
        power: { value: 0 },
        frame: { value: 0 }
      },
      blending: B,
      depthWrite: !1,
      depthTest: !1,
      toneMapped: !1
    }), en(this.fullscreenMaterial), new Ie().load(wt, (i) => {
      i.minFilter = b, i.magFilter = b, i.wrapS = G, i.wrapT = G, i.colorSpace = Fe, this.fullscreenMaterial.uniforms.blueNoiseTexture.value = i;
    });
  }
  get texture() {
    return this.renderTarget.texture;
  }
  setSize(e, t) {
    this.renderTarget.setSize(e, t), this.fullscreenMaterial.uniforms.resolution.value.set(this.renderTarget.width, this.renderTarget.height);
  }
  render(e) {
    const t = +this.fullscreenMaterial.defines.spp;
    this.fullscreenMaterial.uniforms.frame.value = (this.fullscreenMaterial.uniforms.frame.value + t) % 4096, this.fullscreenMaterial.uniforms.cameraNear.value = this._camera.near, this.fullscreenMaterial.uniforms.cameraFar.value = this._camera.far, this.fullscreenMaterial.uniforms.projectionViewMatrix.value.multiplyMatrices(
      this._camera.projectionMatrix,
      this._camera.matrixWorldInverse
    );
    const r = this.fullscreenMaterial.uniforms.blueNoiseTexture.value;
    if (r) {
      const { width: n, height: i } = r.source.data;
      this.fullscreenMaterial.uniforms.blueNoiseRepeat.value.set(
        this.renderTarget.width / n,
        this.renderTarget.height / i
      );
    }
    e.setRenderTarget(this.renderTarget), e.render(this.scene, this.camera);
  }
};
const mu = `\r
uniform sampler2D normalTexture;\r
uniform float cameraNear;\r
uniform float cameraFar;\r
uniform mat4 projectionMatrixInverse;\r
uniform mat4 cameraMatrixWorld;\r
\r
// source: https://github.com/mrdoob/three.js/blob/342946c8392639028da439b6dc0597e58209c696/examples/js/shaders/SAOShader.js#L123\r
float getViewZ(const float depth) {\r
#ifdef PERSPECTIVE_CAMERA\r
    return perspectiveDepthToViewZ(depth, cameraNear, cameraFar);\r
#else\r
    return orthographicDepthToViewZ(depth, cameraNear, cameraFar);\r
#endif\r
}\r
\r
// source: https://github.com/N8python/ssao/blob/master/EffectShader.js#L52\r
vec3 getWorldPos(const float depth, const vec2 coord) {\r
    float z = depth * 2.0 - 1.0;\r
    vec4 clipSpacePosition = vec4(coord * 2.0 - 1.0, z, 1.0);\r
    vec4 viewSpacePosition = projectionMatrixInverse * clipSpacePosition;\r
\r
    // Perspective division\r
    vec4 worldSpacePosition = cameraMatrixWorld * viewSpacePosition;\r
    worldSpacePosition.xyz /= worldSpacePosition.w;\r
\r
    return worldSpacePosition.xyz;\r
}\r
\r
vec3 slerp(const vec3 a, const vec3 b, const float t) {\r
    float cosAngle = dot(a, b);\r
    float angle = acos(cosAngle);\r
\r
    if (abs(angle) < 0.001) {\r
        return mix(a, b, t);\r
    }\r
\r
    float sinAngle = sin(angle);\r
    float t1 = sin((1.0 - t) * angle) / sinAngle;\r
    float t2 = sin(t * angle) / sinAngle;\r
\r
    return (a * t1) + (b * t2);\r
}\r
\r
vec3 computeWorldNormal() {\r
    vec2 size = vec2(textureSize(depthTexture, 0));\r
    ivec2 p = ivec2(vUv * size);\r
    float c0 = texelFetch(depthTexture, p, 0).x;\r
    float l2 = texelFetch(depthTexture, p - ivec2(2, 0), 0).x;\r
    float l1 = texelFetch(depthTexture, p - ivec2(1, 0), 0).x;\r
    float r1 = texelFetch(depthTexture, p + ivec2(1, 0), 0).x;\r
    float r2 = texelFetch(depthTexture, p + ivec2(2, 0), 0).x;\r
    float b2 = texelFetch(depthTexture, p - ivec2(0, 2), 0).x;\r
    float b1 = texelFetch(depthTexture, p - ivec2(0, 1), 0).x;\r
    float t1 = texelFetch(depthTexture, p + ivec2(0, 1), 0).x;\r
    float t2 = texelFetch(depthTexture, p + ivec2(0, 2), 0).x;\r
    float dl = abs((2.0 * l1 - l2) - c0);\r
    float dr = abs((2.0 * r1 - r2) - c0);\r
    float db = abs((2.0 * b1 - b2) - c0);\r
    float dt = abs((2.0 * t1 - t2) - c0);\r
    vec3 ce = getWorldPos(c0, vUv).xyz;\r
    vec3 dpdx = (dl < dr) ? ce - getWorldPos(l1, (vUv - vec2(1.0 / size.x, 0.0))).xyz\r
                          : -ce + getWorldPos(r1, (vUv + vec2(1.0 / size.x, 0.0))).xyz;\r
    vec3 dpdy = (db < dt) ? ce - getWorldPos(b1, (vUv - vec2(0.0, 1.0 / size.y))).xyz\r
                          : -ce + getWorldPos(t1, (vUv + vec2(0.0, 1.0 / size.y))).xyz;\r
    return normalize(cross(dpdx, dpdy));\r
}\r
\r
vec3 getWorldNormal(const vec2 uv) {\r
#ifdef useNormalTexture\r
    vec3 worldNormal = unpackRGBToNormal(textureLod(normalTexture, uv, 0.).rgb);\r
\r
    worldNormal = (vec4(worldNormal, 1.) * viewMatrix).xyz;  // view-space to world-space\r
    return normalize(worldNormal);\r
#else\r
    return computeWorldNormal();  // compute world normal from depth\r
#endif\r
}\r
\r
#define PI 3.14159265358979323846264338327950288\r
\r
// source: https://www.shadertoy.com/view/cll3R4\r
vec3 cosineSampleHemisphere(const vec3 n, const vec2 u) {\r
    float r = sqrt(u.x);\r
    float theta = 2.0 * PI * u.y;\r
\r
    vec3 b = normalize(cross(n, vec3(0.0, 1.0, 1.0)));\r
    vec3 t = cross(b, n);\r
\r
    return normalize(r * sin(theta) * b + sqrt(1.0 - u.x) * n + r * cos(theta) * t);\r
}\r
`, pu = `varying vec2 vUv;\r
\r
uniform highp sampler2D depthTexture;\r
\r
uniform mat4 projectionViewMatrix;\r
uniform int frame;\r
\r
uniform vec2 blueNoiseRepeat;\r
uniform vec2 resolution;\r
\r
uniform float aoDistance;\r
uniform float distancePower;\r
uniform float bias;\r
uniform float thickness;\r
\r
#include <packing>\r
// HBAO Utils\r
#include <hbao_utils>\r
\r
float getOcclusion(const vec3 cameraPosition, const vec3 worldPos, const vec3 worldNormal, const float depth, const int seed,\r
                   inout float totalWeight) {\r
  vec4 blueNoise = blueNoise();\r
\r
  vec3 sampleWorldDir = cosineSampleHemisphere(worldNormal, blueNoise.rg);\r
\r
  vec3 sampleWorldPos = worldPos + aoDistance * pow(blueNoise.b, distancePower + 1.0) * sampleWorldDir;\r
\r
  // Project the sample position to screen space\r
  vec4 sampleUv = projectionViewMatrix * vec4(sampleWorldPos, 1.);\r
  sampleUv.xy /= sampleUv.w;\r
  sampleUv.xy = sampleUv.xy * 0.5 + 0.5;\r
\r
  // Get the depth of the sample position\r
  float sampleDepth = textureLod(depthTexture, sampleUv.xy, 0.0).r;\r
\r
  // Compute the horizon line\r
  float deltaDepth = depth - sampleDepth;\r
\r
  // distance based bias\r
  float d = distance(sampleWorldPos, cameraPosition);\r
  deltaDepth *= 0.001 * d * d;\r
\r
  float th = thickness * 0.01;\r
\r
  float theta = dot(worldNormal, sampleWorldDir);\r
  totalWeight += theta;\r
\r
  if (deltaDepth < th) {\r
    float horizon = sampleDepth + deltaDepth * bias * 1000.;\r
\r
    float occlusion = max(0.0, horizon - depth) * theta;\r
\r
    float m = max(0., 1. - deltaDepth / th);\r
    occlusion = 10. * occlusion * m / d;\r
\r
    occlusion = sqrt(occlusion);\r
    return occlusion;\r
  }\r
\r
  return 0.;\r
}\r
\r
void main() {\r
  float depth = textureLod(depthTexture, vUv, 0.0).r;\r
\r
  // filter out background\r
  if (depth == 1.0) {\r
    discard;\r
    return;\r
  }\r
\r
  vec4 cameraPosition = cameraMatrixWorld * vec4(0.0, 0.0, 0.0, 1.0);\r
\r
  vec3 worldPos = getWorldPos(depth, vUv);\r
  vec3 worldNormal = getWorldNormal(vUv);\r
\r
  float ao = 0.0, totalWeight = 0.0;\r
\r
  for (int i = 0; i < spp; i++) {\r
    int seed = i;\r
#ifdef animatedNoise\r
    seed += frame;\r
#endif\r
\r
    float occlusion = getOcclusion(cameraPosition.xyz, worldPos, worldNormal, depth, seed, totalWeight);\r
    ao += occlusion;\r
  }\r
\r
  if (totalWeight > 0.)\r
    ao /= totalWeight;\r
\r
  // clamp ao to [0, 1]\r
  ao = clamp(1. - ao, 0., 1.);\r
\r
  gl_FragColor = vec4(worldNormal, ao);\r
}`, vu = pu.replace("#include <hbao_utils>", mu);
let gu = class extends Zi {
  constructor(e, t) {
    super(e, t, vu);
  }
};
const xu = `uniform sampler2D inputTexture;\r
uniform highp sampler2D depthTexture;\r
uniform float power;\r
uniform vec3 color;\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
  float unpackedDepth = textureLod(depthTexture, uv, 0.).r;\r
\r
  float ao = unpackedDepth > 0.9999 ? 1.0 : textureLod(inputTexture, uv, 0.0).a;\r
  ao = pow(ao, power);\r
\r
  vec3 aoColor = mix(color, vec3(1.), ao);\r
\r
  aoColor *= inputColor.rgb;\r
\r
  outputColor = vec4(aoColor, inputColor.a);\r
}`, Su = `uniform sampler2D accumulatedTexture;\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
  vec4 accumulatedTexel = textureLod(accumulatedTexture, uv, 0.);\r
\r
  outputColor = vec4(accumulatedTexel.rgb, 1.);\r
}`;
let Ki = class extends I {
  constructor(e, t, r, n = Ut) {
    super("TRAAEffect", Su, {
      type: "FinalTRAAEffectMaterial",
      uniforms: /* @__PURE__ */ new Map([["accumulatedTexture", new a(null)]])
    }), this._scene = e, this._camera = t, this.velocityDepthNormalPass = r, n = {
      ...n,
      maxBlend: 0.9,
      neighborhoodClamp: !0,
      neighborhoodClampIntensity: 1,
      neighborhoodClampRadius: 1,
      logTransform: !0,
      confidencePower: 4
    }, this.options = { ...Ut, ...n }, this.setSize(n.width, n.height);
  }
  setSize(e, t) {
    this.temporalReprojectPass?.setSize(e, t);
  }
  dispose() {
    super.dispose(), this.temporalReprojectPass.dispose();
  }
  reset() {
    this.temporalReprojectPass.reset();
  }
  update(e, t) {
    this.temporalReprojectPass || (this.temporalReprojectPass = new nn(
      this._scene,
      this._camera,
      this.velocityDepthNormalPass,
      t.texture,
      1,
      this.options
    ), this.temporalReprojectPass.setSize(t.width, t.height), this.uniforms.get("accumulatedTexture").value = this.temporalReprojectPass.texture), this.temporalReprojectPass.unjitter(), this.unjitteredProjectionMatrix = this._camera.projectionMatrix.clone(), this._camera.projectionMatrix.copy(this.unjitteredProjectionMatrix), this.temporalReprojectPass.jitter(), this.temporalReprojectPass.render(e);
  }
};
Ki.DefaultOptions = Ut;
const Or = {
  resolutionScale: 1,
  spp: 8,
  distance: 2,
  distancePower: 1,
  power: 2,
  bias: 40,
  thickness: 0.075,
  color: new y("black"),
  useNormalPass: !1,
  velocityDepthNormalPass: null,
  normalTexture: null,
  useExcludeMask: !1,
  excludedObjects: [],
  ...xt.DefaultOptions
};
let Nt = class extends I {
  lastSize = { width: 0, height: 0, resolutionScale: 0 };
  constructor(e, t, r, n, i = Or) {
    if (super("AOEffect", xu, {
      type: "FinalAOMaterial",
      uniforms: /* @__PURE__ */ new Map([
        ["inputTexture", new a(null)],
        ["depthTexture", new a(null)],
        ["power", new a(0)],
        ["color", new a(new y("black"))],
        ["excludeMaskTexture", new a(null)]
      ])
    }), this.composer = e, this.aoPass = n, this.scene = r, this.camera = t, i = { ...Or, ...i }, this.excludeMaskTarget = null, e.depthTexture || e.createDepthTexture(), this.aoPass.fullscreenMaterial.uniforms.depthTexture.value = e.depthTexture, this.uniforms.get("depthTexture").value = e.depthTexture, i.useNormalPass || i.normalTexture) {
      i.useNormalPass && (this.normalPass = new mr(r, t));
      const s = i.normalTexture ?? this.normalPass.texture;
      this.aoPass.fullscreenMaterial.uniforms.normalTexture.value = s, this.aoPass.fullscreenMaterial.defines.useNormalTexture = "";
    }
    this.PoissonDenoisePass = new xt(t, this.aoPass.texture, e.depthTexture, {
      normalInRgb: !0
    }), this.makeOptionsReactive(i), i.useExcludeMask && this.initExcludeMask();
  }
  initExcludeMask() {
    this.defines = this.defines || /* @__PURE__ */ new Map(), this.defines.set("USE_EXCLUDE_MASK", "1");
    const e = this.lastSize.width || 512, t = this.lastSize.height || 512;
    this.excludeMaskTarget = new A(e, t, {
      format: ne,
      type: Y,
      minFilter: b,
      magFilter: b
    }), this.uniforms.get("excludeMaskTexture").value = this.excludeMaskTarget.texture;
  }
  // 设置要排除的对象
  setExcludedObjects(e) {
    this.excludedObjects = Array.isArray(e) ? e : [e], this.useExcludeMask && !this.excludeMaskTarget && this.initExcludeMask();
  }
  // 渲染排除标识到纹理
  renderExcludeMask(e) {
    if (!this.excludeMaskTarget || !this.useExcludeMask || !this.excludedObjects.length)
      return;
    const t = e.getRenderTarget(), r = e.autoClear;
    e.setRenderTarget(this.excludeMaskTarget), e.autoClear = !0, e.clear();
    const n = /* @__PURE__ */ new Map();
    this.scene.traverse((i) => {
      i.isMesh && (n.set(i, i.visible), i.visible = !1);
    });
    for (const i of this.excludedObjects)
      i && (i.visible = !0, i.isGroup && i.traverse((s) => {
        s.isMesh && (s.visible = !0);
      }));
    e.render(this.scene, this.camera), this.scene.traverse((i) => {
      i.isMesh && n.has(i) && (i.visible = n.get(i));
    }), e.setRenderTarget(t), e.autoClear = r;
  }
  makeOptionsReactive(e) {
    for (const t of Object.keys(e))
      Object.defineProperty(this, t, {
        get() {
          return e[t];
        },
        set(r) {
          if (r != null)
            switch (e[t] = r, t) {
              case "spp":
                this.aoPass.fullscreenMaterial.defines.spp = r.toFixed(0), this.aoPass.fullscreenMaterial.needsUpdate = !0;
                break;
              case "distance":
                this.aoPass.fullscreenMaterial.uniforms.aoDistance.value = r;
                break;
              case "resolutionScale":
                this.setSize(this.lastSize.width, this.lastSize.height);
                break;
              case "power":
                this.uniforms.get("power").value = r;
                break;
              case "color":
                this.uniforms.get("color").value.copy(new y(r));
                break;
              case "useExcludeMask":
                r ? (this.defines = this.defines || /* @__PURE__ */ new Map(), this.defines.set("USE_EXCLUDE_MASK", "1"), this.excludeMaskTarget || this.initExcludeMask()) : this.defines && this.defines.delete("USE_EXCLUDE_MASK"), this.setSize(this.lastSize.width, this.lastSize.height);
                break;
              // denoiser
              case "iterations":
              case "radius":
              case "rings":
              case "samples":
                this.PoissonDenoisePass[t] = r;
                break;
              case "lumaPhi":
              case "depthPhi":
              case "normalPhi":
                this.PoissonDenoisePass.fullscreenMaterial.uniforms[t].value = Math.max(r, 1e-4);
                break;
              default:
                t in this.aoPass.fullscreenMaterial.uniforms && (this.aoPass.fullscreenMaterial.uniforms[t].value = r);
            }
        },
        configurable: !0
      }), this[t] = e[t];
  }
  setSize(e, t) {
    e === void 0 || t === void 0 || e === this.lastSize.width && t === this.lastSize.height && this.resolutionScale === this.lastSize.resolutionScale || (this.normalPass?.setSize(e, t), this.aoPass.setSize(e * this.resolutionScale, t * this.resolutionScale), this.PoissonDenoisePass.setSize(e, t), this.excludeMaskTarget && this.excludeMaskTarget.setSize(e, t), this.lastSize = {
      width: e,
      height: t,
      resolutionScale: this.resolutionScale
    });
  }
  get texture() {
    return this.iterations > 0 ? this.PoissonDenoisePass.texture : this.aoPass.texture;
  }
  update(e) {
    this.useExcludeMask && this.excludeMaskTarget && this.renderExcludeMask(e);
    const t = this.composer.passes.some((r) => r.enabled && !r.skipRendering && r.effects?.some((n) => n instanceof Ki));
    t && !("animatedNoise" in this.aoPass.fullscreenMaterial.defines) ? (this.aoPass.fullscreenMaterial.defines.animatedNoise = "", this.aoPass.fullscreenMaterial.needsUpdate = !0) : !t && "animatedNoise" in this.aoPass.fullscreenMaterial.defines && (delete this.aoPass.fullscreenMaterial.defines.animatedNoise, this.aoPass.fullscreenMaterial.needsUpdate = !0), this.uniforms.get("inputTexture").value = this.texture, this.normalPass?.render(e), this.aoPass.render(e), this.PoissonDenoisePass.render(e);
  }
};
Nt.DefaultOptions = Or;
let Kd = class Qi extends Nt {
  lastSize = { width: 0, height: 0, resolutionScale: 0 };
  constructor(e, t, r, n = Nt.DefaultOptions) {
    const i = new gu(t, r);
    n = {
      ...Nt.DefaultOptions,
      ...Qi.DefaultOptions,
      ...n
    }, super(e, t, r, i, n), n = { ...Nt.DefaultOptions, ...n };
  }
};
const pr = (c) => {
  const e = [c], t = [];
  for (; e.length !== 0; ) {
    const r = e.shift();
    r.material && t.push(r);
    for (const n of r.children)
      n.visible && e.push(n);
  }
  return t;
}, ht = (c, e, t, r, n) => {
  e[t] !== c[t] && (c[t] = e[t], c.uniforms[t].value = e[t], e[t] ? (c.defines[r] = "", r === "USE_NORMALMAP" && (c.defines.TANGENTSPACE_NORMALMAP = "")) : delete c.defines[r], c.needsUpdate = !0);
}, yu = (c) => {
  const {
    width: e,
    height: t
  } = c.image;
  return Math.floor(Math.log2(Math.max(e, t))) + 1;
}, qi = (c) => {
  let e = c.material.uniforms.prevBoneTexture.value;
  if (e && e.image.width === c.skeleton.boneTexture.width)
    e = c.material.uniforms.prevBoneTexture.value, e.image.data.set(c.skeleton.boneTexture.image.data);
  else {
    let t;
    (t = e) == null || t.dispose();
    const r = c.skeleton.boneTexture.image.data.slice(), n = c.skeleton.boneTexture.image.width;
    e = new le(r, n, n, ne, Y), c.material.uniforms.prevBoneTexture.value = e, e.needsUpdate = !0;
  }
}, Mu = (c, e) => {
  let t;
  (t = c.skeleton) != null && t.boneTexture && (c.material.uniforms.boneTexture.value = c.skeleton.boneTexture, "USE_SKINNING" in c.material.defines || (c.material.defines.USE_SKINNING = "", c.material.defines.BONE_TEXTURE = "", c.material.needsUpdate = !0)), c.modelViewMatrix.multiplyMatrices(e.matrixWorldInverse, c.matrixWorld), c.material.uniforms.velocityMatrix.value.multiplyMatrices(e.projectionMatrix, c.modelViewMatrix);
}, wu = (c, e) => {
  let t;
  c.material.uniforms.prevVelocityMatrix.value.multiplyMatrices(e.projectionMatrix, c.modelViewMatrix), (t = c.skeleton) != null && t.boneTexture && qi(c);
}, Tu = () => {
  if (ue.envmap_physical_pars_fragment.includes("iblRadianceDisabled") || (ue.envmap_physical_pars_fragment = ue.envmap_physical_pars_fragment.replace(
    "vec3 getIBLRadiance( const in vec3 viewDir, const in vec3 normal, const in float roughness ) {",
    /* glsl */
    `
		uniform bool iblRadianceDisabled;
	
		vec3 getIBLRadiance( const in vec3 viewDir, const in vec3 normal, const in float roughness ) {
		 if(iblRadianceDisabled) return vec3(0.);
		`
  )), "iblRadianceDisabled" in He.physical.uniforms)
    return He.physical.uniforms.iblRadianceDisabled;
  const c = {
    value: !1
  };
  He.physical.uniforms.iblRadianceDisabled = c;
  const {
    clone: e
  } = oe;
  return oe.clone = (t) => {
    const r = e(t);
    return "iblRadianceDisabled" in t && (r.iblRadianceDisabled = c), r;
  }, c;
}, bu = () => {
  if (ue.envmap_physical_pars_fragment.includes("iblIrradianceDisabled") || (ue.envmap_physical_pars_fragment = ue.envmap_physical_pars_fragment.replace(
    "vec3 getIBLIrradiance( const in vec3 normal ) {",
    /* glsl */
    `
			uniform bool iblIrradianceDisabled;
		
			vec3 getIBLIrradiance( const in vec3 normal ) {
			 if(iblIrradianceDisabled) return vec3(0.);
			`
  )), "iblIrradianceDisabled" in He.physical.uniforms)
    return He.physical.uniforms.iblIrradianceDisabled;
  const c = {
    value: !1
  };
  He.physical.uniforms.iblIrradianceDisabled = c;
  const {
    clone: e
  } = oe;
  return oe.clone = (t) => {
    const r = e(t);
    return "iblIrradianceDisabled" in t && (r.iblIrradianceDisabled = c), r;
  }, c;
}, Du = /#pragma unroll_loop_start\s+for\s*\(\s*int\s+i\s*=\s*(\d+)\s*;\s*i\s*<\s*(\d+)\s*;\s*i\s*\+\+\s*\)\s*{([\s\S]+?)}\s+#pragma unroll_loop_end/g;
function on(c) {
  return c.replace(Du, Pu);
}
function Pu(c, e, t, r) {
  let n = "";
  for (let i = parseInt(e); i < parseInt(t); i++)
    n += r.replace(/\[\s*i\s*\]/g, "[ " + i + " ]").replace(/UNROLLED_LOOP_INDEX/g, i);
  return n;
}
const Ji = (c) => {
  let e;
  return (e = c.material.fragmentShader) == null ? void 0 : e.includes("float intersection2 = diskIntersectWithBackFaceCulling( camPos, p, h, vec3( 0.0, 1.0, 0.0 ), radius );");
}, ln = (c, e = c.material) => e.visible && e.depthWrite && e.depthTest && (!e.transparent || e.opacity > 0) && !Ji(c), Cu = ["vertexTangent", "vertexColors", "vertexAlphas", "vertexUvs", "uvsVertexOnly", "supportsVertexTextures", "instancing", "instancingColor", "side", "flatShading", "skinning", "doubleSided", "flipSided"], _i = (c, e) => {
  for (const t of Cu)
    e[t] = c[t];
};
var lt = `#define GLSLIFY 1
varying vec2 vUv;void main(){vUv=position.xy*0.5+0.5;gl_Position=vec4(position.xy,1.0,1.0);}`;
class Au extends X {
  constructor(e = 1) {
    super("CopyPass"), this.needsSwap = !1, this.renderTarget = new be(1, 1, 1, {
      depthBuffer: !1
    }), this.setTextureCount(e);
  }
  setTextureCount(e) {
    let t, r = "", n = "";
    for (let i = 0; i < e; i++)
      r += /* glsl */
      `
				uniform sampler2D inputTexture${i};
				layout(location = ${i}) out vec4 gOutput${i};
			`, n += /* glsl */
      `gOutput${i} = textureLod(inputTexture${i}, vUv, 0.);`;
    (t = this.fullscreenMaterial) == null || t.dispose(), this.fullscreenMaterial = new N({
      fragmentShader: (
        /* glsl */
        `
            varying vec2 vUv;
			
			${r}

            void main() {
				${n}
            }
            `
      ),
      vertexShader: lt,
      glslVersion: Te,
      blending: B,
      depthWrite: !1,
      depthTest: !1,
      toneMapped: !1
    });
    for (let i = 0; i < e; i++)
      if (this.fullscreenMaterial.uniforms["inputTexture" + i] = new a(null), i >= this.renderTarget.texture.length) {
        const s = this.renderTarget.texture[0].clone();
        s.isRenderTargetTexture = !0, this.renderTarget.texture.push(s);
      }
  }
  setSize(e, t) {
    this.renderTarget.setSize(e, t);
  }
  render(e) {
    e.setRenderTarget(this.renderTarget), e.render(this.scene, this.camera);
  }
}
var Eu = `#define GLSLIFY 1
varying vec2 vUv;uniform sampler2D velocityTexture;uniform sampler2D depthTexture;uniform sampler2D lastDepthTexture;uniform float blend;uniform float neighborhoodClampIntensity;uniform bool constantBlend;uniform bool fullAccumulate;uniform vec2 invTexSize;uniform mat4 projectionMatrix;uniform mat4 projectionMatrixInverse;uniform mat4 cameraMatrixWorld;uniform vec3 cameraPos;uniform mat4 prevViewMatrix;uniform mat4 prevCameraMatrixWorld;uniform mat4 prevProjectionMatrix;uniform mat4 prevProjectionMatrixInverse;uniform bool reset;uniform float delta;
#define EPSILON 0.00001
#include <packing>
#include <reproject>
void main(){getDepthAndDilatedUVOffset(depthTexture,vUv,depth,dilatedDepth,depthTexel);if(dot(depthTexel.rgb,depthTexel.rgb)==0.0){
#ifdef neighborhoodClamp
#pragma unroll_loop_start
for(int i=0;i<textureCount;i++){gOutput[i]=textureLod(inputTexture[i],vUv,0.0);}
#pragma unroll_loop_end
#else
discard;
#endif
return;}vec2 dilatedUv=vUv+dilatedUvOffset;edgeStrength=computeEdgeStrength(depth,invTexSize);vec4 inputTexel[textureCount];vec4 accumulatedTexel[textureCount];bool textureSampledThisFrame[textureCount];
#pragma unroll_loop_start
for(int i=0;i<textureCount;i++){inputTexel[i]=textureLod(inputTexture[i],vUv,0.0);doColorTransform[i]=luminance(inputTexel[i].rgb)>0.0;textureSampledThisFrame[i]=inputTexel[i].r>=0.;if(textureSampledThisFrame[i]){transformColor(inputTexel[i].rgb);}else{inputTexel[i].rgb=vec3(0.0);}texIndex++;}
#pragma unroll_loop_end
texIndex=0;velocityTexel=textureLod(velocityTexture,vUv,0.0);didMove=dot(velocityTexel.xy,velocityTexel.xy)>0.000000001;
#ifdef dilation
vec2 octahedronEncodedNormal=textureLod(velocityTexture,dilatedUv,0.0).ba;
#else
vec2 octahedronEncodedNormal=velocityTexel.ba;
#endif
vec3 worldNormal=Decode(octahedronEncodedNormal);vec3 worldPos=screenSpaceToWorldSpace(vUv,depth,cameraMatrixWorld,projectionMatrixInverse);vec2 reprojectedUvDiffuse=vec2(-10.0);vec2 reprojectedUvSpecular[textureCount];vec2 reprojectedUv;bool reprojectHitPoint;
#pragma unroll_loop_start
for(int i=0;i<textureCount;i++){reprojectHitPoint=reprojectSpecular[i]&&inputTexel[i].a>0.0;if(reprojectHitPoint){reprojectedUvSpecular[i]=getReprojectedUV(depth,worldPos,worldNormal,inputTexel[i].a);}else{reprojectedUvSpecular[i]=vec2(-1.0);}if(reprojectedUvDiffuse.x==-10.0&&reprojectedUvSpecular[i].x<0.0){reprojectedUvDiffuse=getReprojectedUV(depth,worldPos,worldNormal,0.0);}reprojectedUv=reprojectedUvSpecular[i].x>=0.0 ? reprojectedUvSpecular[i]: reprojectedUvDiffuse;if(reprojectedUv.x<0.0){accumulatedTexel[i]=vec4(inputTexel[i].rgb,0.0);}else{accumulatedTexel[i]=sampleReprojectedTexture(accumulatedTexture[i],reprojectedUv);transformColor(accumulatedTexel[i].rgb);if(textureSampledThisFrame[i]){accumulatedTexel[i].a++;if(neighborhoodClamp[i]){vec3 clampedColor=accumulatedTexel[i].rgb;clampNeighborhood(inputTexture[i],clampedColor,inputTexel[i].rgb);accumulatedTexel[i].rgb=mix(accumulatedTexel[i].rgb,clampedColor,neighborhoodClampIntensity);}}else{inputTexel[i].rgb=accumulatedTexel[i].rgb;}}texIndex++;}
#pragma unroll_loop_end
texIndex=0;float m=1.-delta/(1./60.);float fpsAdjustedBlend=blend+max(0.,(1.-blend)*m);float maxValue=(fullAccumulate&&!didMove)? 1.0 : fpsAdjustedBlend;vec3 outputColor;float temporalReprojectMix;
#pragma unroll_loop_start
for(int i=0;i<textureCount;i++){if(constantBlend){temporalReprojectMix=accumulatedTexel[i].a==0.0 ? 0.0 : fpsAdjustedBlend;}else{temporalReprojectMix=fpsAdjustedBlend;if(reset)accumulatedTexel[i].a=0.0;temporalReprojectMix=min(1.-1./(accumulatedTexel[i].a+1.0),maxValue);}outputColor=mix(inputTexel[i].rgb,accumulatedTexel[i].rgb,temporalReprojectMix);undoColorTransform(outputColor);gOutput[i]=vec4(outputColor,accumulatedTexel[i].a);texIndex++;}
#pragma unroll_loop_end
#ifdef useTemporalReprojectCustomComposeShader
temporalReprojectCustomComposeShader
#endif
}`, Ru = `#define GLSLIFY 1
vec4 velocityTexel;float dilatedDepth;vec2 dilatedUvOffset;int texIndex;bool didMove;vec4 depthTexel;float depth;float edgeStrength;
#define luminance(a) dot(vec3(0.2125, 0.7154, 0.0721), a)
vec3 screenSpaceToWorldSpace(const vec2 uv,const float depth,mat4 curMatrixWorld,const mat4 projMatrixInverse){vec4 ndc=vec4((uv.x-0.5)*2.0,(uv.y-0.5)*2.0,(depth-0.5)*2.0,1.0);vec4 clip=projMatrixInverse*ndc;vec4 view=curMatrixWorld*(clip/clip.w);return view.xyz;}vec2 viewSpaceToScreenSpace(const vec3 position,const mat4 projMatrix){vec4 projectedCoord=projMatrix*vec4(position,1.0);projectedCoord.xy/=projectedCoord.w;projectedCoord.xy=projectedCoord.xy*0.5+0.5;return projectedCoord.xy;}bool doColorTransform[textureCount];
#ifdef logTransform
void transformColor(inout vec3 color){if(!doColorTransform[texIndex])return;float lum=luminance(color);float diff=min(1.0,lum-0.99);if(diff>0.0){color=vec3(diff*0.1);return;}color=log(max(color,vec3(EPSILON)));}void undoColorTransform(inout vec3 color){if(!doColorTransform[texIndex])return;color=exp(color);}
#else
#define transformColor
#define undoColorTransform
#endif
void getNeighborhoodAABB(const sampler2D tex,inout vec3 minNeighborColor,inout vec3 maxNeighborColor){for(int x=-neighborhoodClampRadius;x<=neighborhoodClampRadius;x++){for(int y=-neighborhoodClampRadius;y<=neighborhoodClampRadius;y++){if(x!=0||y!=0){vec2 offset=vec2(x,y)*invTexSize;vec2 neighborUv=vUv+offset;vec4 neighborTexel=textureLod(tex,neighborUv,0.0);transformColor(neighborTexel.rgb);minNeighborColor=min(neighborTexel.rgb,minNeighborColor);maxNeighborColor=max(neighborTexel.rgb,maxNeighborColor);}}}}void clampNeighborhood(const sampler2D tex,inout vec3 color,const vec3 inputColor){vec3 minNeighborColor=inputColor;vec3 maxNeighborColor=inputColor;getNeighborhoodAABB(tex,minNeighborColor,maxNeighborColor);color=clamp(color,minNeighborColor,maxNeighborColor);}
#ifdef dilation
void getDilatedDepthUVOffset(const sampler2D tex,const vec2 centerUv,out float depth,out float dilatedDepth,out vec4 closestDepthTexel){float closestDepth=0.0;for(int x=-1;x<=1;x++){for(int y=-1;y<=1;y++){vec2 offset=vec2(x,y)*invTexSize;vec2 neighborUv=centerUv+offset;vec4 neighborDepthTexel=textureLod(tex,neighborUv,0.0);float neighborDepth=unpackRGBAToDepth(neighborDepthTexel);if(x==0&&y==0)depth=neighborDepth;if(neighborDepth>closestDepth){closestDepth=neighborDepth;closestDepthTexel=neighborDepthTexel;dilatedUvOffset=offset;}}}dilatedDepth=closestDepth;}
#endif
void getDepthAndDilatedUVOffset(sampler2D depthTex,vec2 uv,out float depth,out float dilatedDepth,out vec4 depthTexel){
#ifdef dilation
getDilatedDepthUVOffset(depthTex,uv,depth,dilatedDepth,depthTexel);
#else
depthTexel=textureLod(depthTex,uv,0.);depth=unpackRGBAToDepth(depthTexel);dilatedDepth=depth;
#endif
}bool planeDistanceDisocclusionCheck(const vec3 worldPos,const vec3 lastWorldPos,const vec3 worldNormal,const float worldDistFactor){if(abs(dot(worldNormal,worldPos))==0.0)return false;vec3 toCurrent=worldPos-lastWorldPos;float distToPlane=abs(dot(toCurrent,worldNormal));return distToPlane>depthDistance*worldDistFactor;}bool worldDistanceDisocclusionCheck(const vec3 worldPos,const vec3 lastWorldPos,const float worldDistFactor){return distance(worldPos,lastWorldPos)>worldDistance*worldDistFactor;}bool validateReprojectedUV(const vec2 reprojectedUv,const vec3 worldPos,const vec3 worldNormal){if(reprojectedUv.x>1.0||reprojectedUv.x<0.0||reprojectedUv.y>1.0||reprojectedUv.y<0.0)return false;vec3 dilatedWorldPos=worldPos;vec3 lastWorldPos;float dilatedLastDepth,lastDepth;vec4 lastDepthTexel;vec2 dilatedReprojectedUv;
#ifdef dilation
dilatedWorldPos=screenSpaceToWorldSpace(vUv+dilatedUvOffset,dilatedDepth,cameraMatrixWorld,projectionMatrixInverse);getDepthAndDilatedUVOffset(lastDepthTexture,reprojectedUv,lastDepth,dilatedLastDepth,lastDepthTexel);dilatedReprojectedUv=reprojectedUv+dilatedUvOffset;
#else
lastDepthTexel=textureLod(lastDepthTexture,reprojectedUv,0.);lastDepth=unpackRGBAToDepth(lastDepthTexel);dilatedLastDepth=lastDepth;dilatedReprojectedUv=reprojectedUv;
#endif
lastWorldPos=screenSpaceToWorldSpace(dilatedReprojectedUv,dilatedLastDepth,prevCameraMatrixWorld,prevProjectionMatrixInverse);float worldDistFactor=clamp((50.0+distance(dilatedWorldPos,cameraPos))/100.,0.25,1.);if(worldDistanceDisocclusionCheck(dilatedWorldPos,lastWorldPos,worldDistFactor))return false;return!planeDistanceDisocclusionCheck(dilatedWorldPos,lastWorldPos,worldNormal,worldDistFactor);}vec2 reprojectHitPoint(const vec3 rayOrig,const float rayLength,const float depth){vec3 cameraRay=normalize(rayOrig-cameraPos);float cameraRayLength=distance(rayOrig,cameraPos);vec3 parallaxHitPoint=cameraPos+cameraRay*(cameraRayLength+rayLength);vec4 reprojectedParallaxHitPoint=prevViewMatrix*vec4(parallaxHitPoint,1.0);vec2 hitPointUv=viewSpaceToScreenSpace(reprojectedParallaxHitPoint.xyz,prevProjectionMatrix);return hitPointUv;}vec2 getReprojectedUV(const float depth,const vec3 worldPos,const vec3 worldNormal,const float rayLength){if(rayLength!=0.0){vec2 reprojectedUv=reprojectHitPoint(worldPos,rayLength,depth);if(validateReprojectedUV(reprojectedUv,worldPos,worldNormal)){return reprojectedUv;}return vec2(-1.);}vec2 reprojectedUv=vUv-velocityTexel.rg;if(validateReprojectedUV(reprojectedUv,worldPos,worldNormal)){return reprojectedUv;}return vec2(-1.);}vec4 SampleTextureCatmullRom(const sampler2D tex,const vec2 uv,const vec2 texSize){vec2 samplePos=uv*texSize;vec2 texPos1=floor(samplePos-0.5f)+0.5f;vec2 f=samplePos-texPos1;vec2 w0=f*(-0.5f+f*(1.0f-0.5f*f));vec2 w1=1.0f+f*f*(-2.5f+1.5f*f);vec2 w2=f*(0.5f+f*(2.0f-1.5f*f));vec2 w3=f*f*(-0.5f+0.5f*f);vec2 w12=w1+w2;vec2 offset12=w2/(w1+w2);vec2 texPos0=texPos1-1.;vec2 texPos3=texPos1+2.;vec2 texPos12=texPos1+offset12;texPos0/=texSize;texPos3/=texSize;texPos12/=texSize;vec4 result=vec4(0.0);result+=textureLod(tex,vec2(texPos0.x,texPos0.y),0.0f)*w0.x*w0.y;result+=textureLod(tex,vec2(texPos12.x,texPos0.y),0.0f)*w12.x*w0.y;result+=textureLod(tex,vec2(texPos3.x,texPos0.y),0.0f)*w3.x*w0.y;result+=textureLod(tex,vec2(texPos0.x,texPos12.y),0.0f)*w0.x*w12.y;result+=textureLod(tex,vec2(texPos12.x,texPos12.y),0.0f)*w12.x*w12.y;result+=textureLod(tex,vec2(texPos3.x,texPos12.y),0.0f)*w3.x*w12.y;result+=textureLod(tex,vec2(texPos0.x,texPos3.y),0.0f)*w0.x*w3.y;result+=textureLod(tex,vec2(texPos12.x,texPos3.y),0.0f)*w12.x*w3.y;result+=textureLod(tex,vec2(texPos3.x,texPos3.y),0.0f)*w3.x*w3.y;result=max(result,vec4(0.));return result;}vec2 sampleBlocky(vec2 p){vec2 d=vec2(dFdx(p.x),dFdy(p.y))/invTexSize;p/=invTexSize;vec2 fA=p-0.5*d,iA=floor(fA);vec2 fB=p+0.5*d,iB=floor(fB);return(iA+(iB-iA)*(fB-iB)/d+0.5)*invTexSize;}float computeEdgeStrength(float unpackedDepth,vec2 texelSize){float depthX=unpackRGBAToDepth(textureLod(depthTexture,vUv+vec2(texelSize.x,0.0),0.0))-unpackRGBAToDepth(textureLod(depthTexture,vUv-vec2(texelSize.x,0.0),0.0));float depthY=unpackRGBAToDepth(textureLod(depthTexture,vUv+vec2(0.0,texelSize.y),0.0))-unpackRGBAToDepth(textureLod(depthTexture,vUv-vec2(0.0,texelSize.y),0.0));float gradientMagnitude=sqrt(depthX*depthX+depthY*depthY);float edgeStrength=min(100000.*gradientMagnitude/(unpackedDepth+0.001),1.);return edgeStrength*edgeStrength;}float computeEdgeStrengthFast(float unpackedDepth){float depthX=dFdx(unpackedDepth);float depthY=dFdy(unpackedDepth);float edgeStrength=depthX*depthX+depthY*depthY;return min(1.,pow(pow(edgeStrength,0.25)*500.,4.));}vec4 sampleReprojectedTexture(const sampler2D tex,const vec2 reprojectedUv){vec4 catmull=SampleTextureCatmullRom(tex,reprojectedUv,1.0/invTexSize);vec4 blocky=SampleTextureCatmullRom(tex,sampleBlocky(reprojectedUv),1.0/invTexSize);vec4 reprojectedTexel=mix(catmull,blocky,edgeStrength);reprojectedTexel.a=min(catmull.a,blocky.a);return reprojectedTexel;}vec2 OctWrap(vec2 v){vec2 w=1.0-abs(v.yx);if(v.x<0.0)w.x=-w.x;if(v.y<0.0)w.y=-w.y;return w;}vec2 Encode(vec3 n){n/=(abs(n.x)+abs(n.y)+abs(n.z));n.xy=n.z>0.0 ? n.xy : OctWrap(n.xy);n.xy=n.xy*0.5+0.5;return n.xy;}vec3 Decode(vec2 f){f=f*2.0-1.0;vec3 n=vec3(f.x,f.y,1.0-abs(f.x)-abs(f.y));float t=max(-n.z,0.0);n.x+=n.x>=0.0 ?-t : t;n.y+=n.y>=0.0 ?-t : t;return normalize(n);}`;
class Nu extends N {
  constructor(e = 1, t = "") {
    let r = Eu.replace("#include <reproject>", Ru);
    typeof t == "string" && (r = r.replace("temporalReprojectCustomComposeShader", t));
    let n = "";
    for (let l = 0; l < e; l++)
      n += /* glsl */
      `
				uniform sampler2D inputTexture${l};
				uniform sampler2D accumulatedTexture${l};

				layout(location = ${l}) out vec4 gOutput${l};
			`;
    r = n + r.replaceAll("textureCount", e), r = on(r);
    const i = r.matchAll(/inputTexture\[\s*[0-9]+\s*]/g);
    for (const [l] of i) {
      const u = l.replace(/[^0-9]/g, "");
      r = r.replace(l, "inputTexture" + u);
    }
    const s = r.matchAll(/accumulatedTexture\[\s*[0-9]+\s*]/g);
    for (const [l] of s) {
      const u = l.replace(/[^0-9]/g, "");
      r = r.replace(l, "accumulatedTexture" + u);
    }
    const o = r.matchAll(/gOutput\[\s*[0-9]+\s*]/g);
    for (const [l] of o) {
      const u = l.replace(/[^0-9]/g, "");
      r = r.replace(l, "gOutput" + u);
    }
    super({
      type: "TemporalReprojectMaterial",
      uniforms: {
        velocityTexture: new a(null),
        depthTexture: new a(null),
        lastDepthTexture: new a(null),
        blend: new a(0),
        neighborhoodClampIntensity: new a(0),
        constantBlend: new a(!1),
        fullAccumulate: new a(!1),
        reset: new a(!1),
        delta: new a(0),
        invTexSize: new a(new g()),
        projectionMatrix: new a(new C()),
        projectionMatrixInverse: new a(new C()),
        cameraMatrixWorld: new a(new C()),
        viewMatrix: new a(new C()),
        prevViewMatrix: new a(new C()),
        prevCameraMatrixWorld: new a(new C()),
        prevProjectionMatrix: new a(new C()),
        prevProjectionMatrixInverse: new a(new C()),
        cameraPos: new a(new T())
      },
      vertexShader: lt,
      fragmentShader: r,
      blending: B,
      depthWrite: !1,
      depthTest: !1,
      toneMapped: !1,
      glslVersion: Te
    });
    for (let l = 0; l < e; l++)
      this.uniforms["inputTexture" + l] = new a(null), this.uniforms["accumulatedTexture" + l] = new a(null);
    typeof t == "string" && (this.defines.useTemporalReprojectCustomComposeShader = "");
  }
}
const kr = 1.324717957244746, zu = 1 / kr, Fu = 1 / (kr * kr), Jn = 1.1127756842787055, Iu = (c) => {
  const e = [];
  for (let t = 0; t < c; t++)
    e.push([(Jn + zu * t) % 1, (Jn + Fu * t) % 1]);
  return e;
}, _n = {
  blend: 0.9,
  dilation: !1,
  constantBlend: !1,
  fullAccumulate: !1,
  neighborhoodClamp: !1,
  neighborhoodClampRadius: 1,
  neighborhoodClampIntensity: 1,
  logTransform: !1,
  depthDistance: 0.25,
  worldDistance: 0.375,
  reprojectSpecular: !1,
  temporalReprojectCustomComposeShader: null,
  renderTarget: null
}, $n = new C(), ei = new C();
class cn extends X {
  constructor(e, t, r, n = 1, i = _n) {
    super("TemporalReprojectPass"), this.needsSwap = !1, this.clock = new wi(), this.r2Sequence = [], this.pointsIndex = 0, this.lastCameraTransform = {
      position: new T(),
      quaternion: new Yr()
    }, this._scene = e, this._camera = t, this.textureCount = n, i = {
      ..._n,
      ...i
    }, this.renderTarget = new be(1, 1, n, {
      minFilter: F,
      magFilter: F,
      type: K,
      depthBuffer: !1
    }), this.renderTarget.texture.map((s, o) => s.name = "TemporalReprojectPass.accumulatedTexture" + o), this.fullscreenMaterial = new Nu(n, i.temporalReprojectCustomComposeShader), this.fullscreenMaterial.defines.textureCount = n, i.dilation && (this.fullscreenMaterial.defines.dilation = ""), i.neighborhoodClamp && (this.fullscreenMaterial.defines.neighborhoodClamp = ""), i.logTransform && (this.fullscreenMaterial.defines.logTransform = ""), this.fullscreenMaterial.defines.neighborhoodClampRadius = parseInt(i.neighborhoodClampRadius), this.fullscreenMaterial.defines.depthDistance = i.depthDistance.toPrecision(5), this.fullscreenMaterial.defines.worldDistance = i.worldDistance.toPrecision(5), this.fullscreenMaterial.uniforms.blend.value = i.blend, this.fullscreenMaterial.uniforms.constantBlend.value = i.constantBlend, this.fullscreenMaterial.uniforms.fullAccumulate.value = i.fullAccumulate, this.fullscreenMaterial.uniforms.neighborhoodClampIntensity.value = i.neighborhoodClampIntensity, this.fullscreenMaterial.uniforms.projectionMatrix.value = t.projectionMatrix.clone(), this.fullscreenMaterial.uniforms.projectionMatrixInverse.value = t.projectionMatrixInverse.clone(), this.fullscreenMaterial.uniforms.cameraMatrixWorld.value = t.matrixWorld, this.fullscreenMaterial.uniforms.viewMatrix.value = t.matrixWorldInverse, this.fullscreenMaterial.uniforms.cameraPos.value = t.position, this.fullscreenMaterial.uniforms.prevViewMatrix.value = t.matrixWorldInverse.clone(), this.fullscreenMaterial.uniforms.prevCameraMatrixWorld.value = t.matrixWorld.clone(), this.fullscreenMaterial.uniforms.prevProjectionMatrix.value = t.projectionMatrix.clone(), this.fullscreenMaterial.uniforms.prevProjectionMatrixInverse.value = t.projectionMatrixInverse.clone(), this.copyPass = new Au(n);
    for (let s = 0; s < n; s++) {
      const o = this.copyPass.renderTarget.texture[s];
      o.type = K, o.minFilter = F, o.magFilter = F, o.needsUpdate = !0;
    }
    this.fullscreenMaterial.uniforms.velocityTexture.value = r.texture, this.fullscreenMaterial.uniforms.depthTexture.value = r.depthTexture;
    for (const s of ["reprojectSpecular", "neighborhoodClamp"]) {
      let o = i[s];
      typeof o != "array" && (o = Array(n).fill(o)), this.fullscreenMaterial.defines[s] = /* glsl */
      `bool[](${o.join(", ")})`;
    }
    this.options = i, this.velocityDepthNormalPass = r;
  }
  setTextures(e) {
    Array.isArray(e) || (e = [e]);
    for (let t = 0; t < e.length; t++) {
      const r = e[t];
      this.fullscreenMaterial.uniforms["inputTexture" + t] = new a(r);
    }
  }
  dispose() {
    super.dispose(), this.renderTarget.dispose(), this.copyPass.dispose(), this.fullscreenMaterial.dispose();
  }
  setSize(e, t) {
    this.renderTarget.setSize(e, t), this.copyPass.setSize(e, t), this.fullscreenMaterial.uniforms.invTexSize.value.set(1 / e, 1 / t);
  }
  get texture() {
    return this.renderTarget.texture[0];
  }
  reset() {
    this.fullscreenMaterial.uniforms.reset.value = !0;
  }
  render(e) {
    const t = Math.min(0.1, this.clock.getDelta());
    this.fullscreenMaterial.uniforms.delta.value = t, $n.copy(this._camera.projectionMatrix), ei.copy(this._camera.projectionMatrixInverse), this._camera.view && (this._camera.view.enabled = !1), this._camera.updateProjectionMatrix(), this.fullscreenMaterial.uniforms.projectionMatrix.value.copy(this._camera.projectionMatrix), this.fullscreenMaterial.uniforms.projectionMatrixInverse.value.copy(this._camera.projectionMatrixInverse), this.fullscreenMaterial.uniforms.lastDepthTexture.value = this.velocityDepthNormalPass.lastDepthTexture, this._camera.view && (this._camera.view.enabled = !0), this._camera.projectionMatrix.copy($n), this._camera.projectionMatrixInverse.copy(ei), e.setRenderTarget(this.renderTarget), e.render(this.scene, this.camera), this.fullscreenMaterial.uniforms.reset.value = !1;
    for (let r = 0; r < this.textureCount; r++)
      this.copyPass.fullscreenMaterial.uniforms["inputTexture" + r].value = this.renderTarget.texture[r], this.fullscreenMaterial.uniforms["accumulatedTexture" + r].value = this.copyPass.renderTarget.texture[r];
    this.copyPass.render(e), this.fullscreenMaterial.uniforms.prevCameraMatrixWorld.value.copy(this._camera.matrixWorld), this.fullscreenMaterial.uniforms.prevViewMatrix.value.copy(this._camera.matrixWorldInverse), this.fullscreenMaterial.uniforms.prevProjectionMatrix.value.copy(this.fullscreenMaterial.uniforms.projectionMatrix.value), this.fullscreenMaterial.uniforms.prevProjectionMatrixInverse.value.copy(this.fullscreenMaterial.uniforms.projectionMatrixInverse.value);
  }
  jitter(e = 1) {
    this.unjitter(), this.r2Sequence.length === 0 && (this.r2Sequence = Iu(256).map(([s, o]) => [s - 0.5, o - 0.5])), this.pointsIndex = (this.pointsIndex + 1) % this.r2Sequence.length;
    const [t, r] = this.r2Sequence[this.pointsIndex], {
      width: n,
      height: i
    } = this.renderTarget;
    this._camera.setViewOffset && this._camera.setViewOffset(n, i, t * e, r * e, n, i);
  }
  unjitter() {
    this._camera.clearViewOffset && this._camera.clearViewOffset();
  }
}
var Bu = `#define GLSLIFY 1
uniform sampler2D inputTexture;void mainImage(const in vec4 inputColor,const in vec2 uv,out vec4 outputColor){vec4 accumulatedTexel=textureLod(inputTexture,vUv,0.);outputColor=vec4(accumulatedTexel.rgb,1.);}`;
const Vr = {
  blend: 0.8,
  constantBlend: !0,
  dilation: !0,
  logTransform: !1,
  // ! todo: check if can use logTransform without artifacts
  neighborhoodClampRadius: 2,
  depthDistance: 10,
  worldDistance: 5,
  neighborhoodClamp: !0
};
class Ht extends I {
  constructor(e, t, r, n = Vr) {
    super("TRAAEffect", Bu, {
      type: "FinalTRAAEffectMaterial",
      uniforms: /* @__PURE__ */ new Map([["inputTexture", new a(null)]])
    }), this._scene = e, this._camera = t, n = {
      ...Vr,
      ...n
    }, this.temporalReprojectPass = new cn(e, t, r, 1, n), this.uniforms.get("inputTexture").value = this.temporalReprojectPass.texture, this.setSize(n.width, n.height);
  }
  setSize(e, t) {
    this.temporalReprojectPass.setSize(e, t);
  }
  dispose() {
    super.dispose(), this.temporalReprojectPass.dispose();
  }
  update(e, t) {
    this.temporalReprojectPass.unjitter(), this.unjitteredProjectionMatrix = this._camera.projectionMatrix.clone(), this._camera.projectionMatrix.copy(this.unjitteredProjectionMatrix);
    const r = pr(this._scene).filter((n) => Ji(n));
    for (const n of r) {
      const i = e.properties.get(n.material);
      if (!(i != null && i.programs))
        continue;
      const s = Array.from(i.programs.values())[0].getUniforms();
      if (!s._patchedProjectionMatrix) {
        const o = s.setValue.bind(s);
        s._oldSetValue = o, s.setValue = (l, u, f, ...h) => {
          u === "projectionMatrix" && (f = this.unjitteredProjectionMatrix), o(l, u, f, ...h);
        }, s._patchedProjectionMatrix = !0;
      }
      cancelAnimationFrame(s._destroyPatchRAF), cancelAnimationFrame(s._destroyPatchRAF2), s._destroyPatchRAF = requestAnimationFrame(() => {
        s._destroyPatchRAF2 = requestAnimationFrame(() => {
          s.setValue = s._oldSetValue, delete s._oldSetValue, delete s._patchedProjectionMatrix;
        });
      });
    }
    this.temporalReprojectPass.fullscreenMaterial.uniforms.inputTexture0.value = t.texture, this.temporalReprojectPass.jitter(), this.temporalReprojectPass.render(e);
  }
}
Ht.DefaultOptions = Vr;
var Lu = `#define GLSLIFY 1
varying vec2 vUv;uniform sampler2D depthTexture;uniform sampler2D normalTexture;uniform sampler2D momentTexture;uniform vec2 invTexSize;uniform bool horizontal;uniform bool blurHorizontal;uniform float denoise[textureCount];uniform float depthPhi;uniform float normalPhi;uniform float roughnessPhi;uniform float denoiseKernel;uniform float stepSize;uniform mat4 projectionMatrixInverse;uniform mat4 projectionMatrix;uniform mat4 cameraMatrixWorld;uniform bool isFirstIteration;uniform bool isLastIteration;
#include <packing>
#define EPSILON 0.00001
#define M_PI 3.1415926535897932384626433832795
#define PI M_PI
#define luminance(a) dot(a, vec3(0.2125, 0.7154, 0.0721))
#include <denoiseCustomComposeShaderFunctions>
vec3 screenSpaceToWorldSpace(const vec2 uv,const float depth,const mat4 curMatrixWorld){vec4 ndc=vec4((uv.x-0.5)*2.0,(uv.y-0.5)*2.0,(depth-0.5)*2.0,1.0);vec4 clip=projectionMatrixInverse*ndc;vec4 view=curMatrixWorld*(clip/clip.w);return view.xyz;}float distToPlane(const vec3 worldPos,const vec3 neighborWorldPos,const vec3 worldNormal){vec3 toCurrent=worldPos-neighborWorldPos;float distToPlane=abs(dot(toCurrent,worldNormal));return distToPlane;}float getDisocclusionBoostVariance(float visibleFrames){
#ifdef useTemporalReprojectTextures
return max(0.,-pow(visibleFrames,2.0)+50.0);
#else
return 0.;
#endif
}void tap(const vec2 neighborVec,const vec2 pixelStepOffset,const vec3 normal,const float depth,const float roughness,const vec3 worldPos,const float luma[textureCount],const float colorPhi[textureCount],inout vec3 denoisedColor[textureCount],inout float totalWeight[textureCount],inout float sumVariance[textureCount],inout float variance[textureCount]){vec2 fullNeighborUv=neighborVec*pixelStepOffset;vec2 neighborUvNearest=vUv+fullNeighborUv;vec2 bilinearOffset=neighborVec.y>0. ? invTexSize :-invTexSize;vec2 neighborUv=vUv+fullNeighborUv+bilinearOffset*0.5;vec2 neighborUvRoughness=vUv+fullNeighborUv*(roughness<0.15 ? roughness/0.15 : 1.)+bilinearOffset*0.5;float basicWeight=1.0;
#ifdef useDepth
vec4 neighborDepthTexel=textureLod(depthTexture,neighborUvNearest,0.);
#ifdef RGBA_DEPTH_PACKING
float neighborDepth=unpackRGBAToDepth(neighborDepthTexel);
#else
float neighborDepth=neighborDepthTexel.r;
#endif
vec3 neighborWorldPos=screenSpaceToWorldSpace(neighborUvNearest,neighborDepth,cameraMatrixWorld);
#ifdef useNormal
float depthDiff=1.-distToPlane(worldPos,neighborWorldPos,normal);
#else
float depthDiff=1.-abs(depth-neighborDepth);
#endif
float depthSimilarity=max(depthDiff/depthPhi,0.);basicWeight*=depthSimilarity;
#endif
#if defined(useNormal) || defined(useRoughness)
vec4 neighborNormalTexel=textureLod(normalTexture,neighborUvNearest,0.);
#endif
#ifdef useNormal
vec3 neighborNormal=neighborNormalTexel.xyz;float normalDiff=dot(neighborNormal,normal);float normalSimilarity=pow(max(0.,normalDiff),normalPhi);basicWeight*=normalSimilarity;
#endif
#ifdef useRoughness
float neighborRoughness=neighborNormalTexel.a;neighborRoughness*=neighborRoughness;float roughnessDiff=abs(roughness-neighborRoughness);float roughnessSimilarity=exp(-roughnessDiff*roughnessPhi);basicWeight*=roughnessSimilarity;
#endif
vec4 neighborInputTexel[textureCount];vec3 neighborColor;float neighborLuma,lumaDiff,lumaSimilarity,disocclusionBoost;float weight[textureCount];
#pragma unroll_loop_start
for(int i=0;i<textureCount;i++){neighborInputTexel[i]=textureLod(inputTexture[i],roughnessDependent[i]? neighborUvRoughness : neighborUv,0.);neighborColor=neighborInputTexel[i].rgb;neighborLuma=luminance(neighborColor);lumaDiff=abs(luma[i]-neighborLuma);lumaSimilarity=max(1.0-lumaDiff/colorPhi[i],0.0);weight[i]=min(basicWeight*lumaSimilarity,1.0);disocclusionBoost=variance[i]/1000.;weight[i]=mix(weight[i],1.,disocclusionBoost);denoisedColor[i]+=neighborColor*weight[i];totalWeight[i]+=weight[i];}
#pragma unroll_loop_end
if(isFirstIteration){
#ifdef useMoment
vec4 neighborMoment=textureLod(momentTexture,neighborUvNearest,0.);neighborInputTexel[0].a=neighborMoment.g-neighborMoment.r*neighborMoment.r;
#if momentTextureCount > 1
neighborInputTexel[1].a=neighborMoment.a-neighborMoment.b*neighborMoment.b;
#endif
#else
for(int i=0;i<textureCount;i++){neighborInputTexel[i].a=getDisocclusionBoostVariance(neighborInputTexel[i].a);}
#endif
}
#pragma unroll_loop_start
for(int i=0;i<momentTextureCount;i++){
#ifndef useMoment
if(isFirstIteration)neighborInputTexel[i].a=1.0;
#endif
sumVariance[i]+=weight[i]*weight[i]*neighborInputTexel[i].a;}
#pragma unroll_loop_end
}void main(){vec4 depthTexel=textureLod(depthTexture,vUv,0.);if(depthTexel.r>0.9999||dot(depthTexel.rgb,depthTexel.rgb)==0.){discard;return;}vec3 worldPos;float depth;
#ifdef useDepth
#ifdef RGBA_DEPTH_PACKING
depth=unpackRGBAToDepth(depthTexel);
#else
depth=depthTexel.r;
#endif
worldPos=screenSpaceToWorldSpace(vUv,depth,cameraMatrixWorld);
#endif
vec3 normal;float roughness;
#ifdef useNormal
vec4 normalTexel=textureLod(normalTexture,vUv,0.);normal=normalTexel.xyz;
#endif
#ifdef useRoughness
roughness=normalTexel.a;roughness*=roughness;
#endif
vec3 denoisedColor[textureCount];float sumVariance[textureCount];float variance[textureCount];
#ifdef doDenoise
vec4 texel[textureCount];float luma[textureCount];float totalWeight[textureCount];float colorPhi[textureCount];
#pragma unroll_loop_start
for(int i=0;i<textureCount;i++){totalWeight[i]=1.0;texel[i]=textureLod(inputTexture[i],vUv,0.);denoisedColor[i]=texel[i].rgb;luma[i]=luminance(texel[i].rgb);}
#pragma unroll_loop_end
if(isFirstIteration){
#ifdef useMoment
vec4 moment=textureLod(momentTexture,vUv,0.);texel[0].a=max(0.0,moment.g-moment.r*moment.r);
#if momentTextureCount > 1
texel[1].a=max(0.0,moment.a-moment.b*moment.b);
#endif
#else
for(int i=0;i<textureCount;i++){texel[i].a=getDisocclusionBoostVariance(texel[i].a);}
#endif
}
#pragma unroll_loop_start
for(int i=0;i<momentTextureCount;i++){variance[i]=min(1000.,texel[i].a);sumVariance[i]=texel[i].a;if(roughnessDependent[i]){colorPhi[i]=denoise[i]*sqrt(basicVariance[i]*roughness+sumVariance[i]);}else{colorPhi[i]=denoise[i]*sqrt(basicVariance[i]+sumVariance[i]);}}
#pragma unroll_loop_end
vec2 pixelStepOffset=invTexSize*stepSize;if(blurHorizontal){for(float i=-denoiseKernel;i<=denoiseKernel;i++){if(i!=0.){vec2 neighborVec=horizontal ? vec2(i,0.): vec2(0.,i);tap(neighborVec,pixelStepOffset,normal,depth,roughness,worldPos,luma,colorPhi,denoisedColor,totalWeight,sumVariance,variance);}}}else{for(float i=-denoiseKernel;i<=denoiseKernel;i++){if(i!=0.){vec2 neighborVec=horizontal ? vec2(-i,-i): vec2(i,-i);tap(neighborVec,pixelStepOffset,normal,depth,roughness,worldPos,luma,colorPhi,denoisedColor,totalWeight,sumVariance,variance);}}}
#pragma unroll_loop_start
for(int i=0;i<textureCount;i++){sumVariance[i]/=totalWeight[i]*totalWeight[i];denoisedColor[i]/=totalWeight[i];}
#pragma unroll_loop_end
#else
#pragma unroll_loop_start
for(int i=0;i<textureCount;i++){denoisedColor[i]=textureLod(inputTexture[i],vUv,0.).rgb;}
#pragma unroll_loop_end
#endif
if(isLastIteration){
#include <denoiseCustomComposeShader>
}
#include <outputShader>
}`;
const ti = {
  moment: !1,
  depth: !1,
  normal: !1,
  roughness: !1,
  diffuse: !0,
  roughnessDependent: !1,
  basicVariance: 5e-4,
  denoiseCustomComposeShader: "",
  denoiseCustomComposeShaderFunctions: ""
}, Uu = [["moment", "", "useMoment"], ["depth", "depthPhi", "useDepth"], ["normal", "normalPhi", "useNormal"], ["roughness", "roughnessPhi", "useRoughness"]];
class Ou extends X {
  constructor(e, t = [], r = ti) {
    super("DenoisePass"), this.iterations = 1, Array.isArray(t) || (t = [t]), r = {
      ...ti,
      ...r
    }, this.options = r, this.fullscreenMaterial = new N({
      fragmentShader: "",
      vertexShader: lt,
      uniforms: {
        depthTexture: new a(null),
        normalTexture: new a(null),
        momentTexture: new a(null),
        invTexSize: new a(new g()),
        horizontal: new a(!0),
        blurHorizontal: new a(!0),
        denoiseKernel: new a(1),
        denoiseDiffuse: new a(1),
        denoise: new a([0]),
        depthPhi: new a(1),
        normalPhi: new a(1),
        roughnessPhi: new a(1),
        stepSize: new a(1),
        isFirstIteration: new a(!1),
        isLastIteration: new a(!1),
        viewMatrix: new a(e.matrixWorldInverse),
        projectionMatrix: new a(e.projectionMatrix),
        cameraMatrixWorld: new a(e.matrixWorld),
        projectionMatrixInverse: new a(e.projectionMatrixInverse)
      },
      glslVersion: Te,
      blending: B,
      depthWrite: !1,
      depthTest: !1,
      toneMapped: !1
    });
    const n = {
      type: K,
      depthBuffer: !1
    };
    this.renderTargetA = new be(1, 1, t.length, n), this.renderTargetB = new be(1, 1, t.length, n), typeof r.roughnessDependent == "boolean" && (r.roughnessDependent = Array(t.length).fill(r.roughnessDependent)), this.fullscreenMaterial.defines.roughnessDependent = /* glsl */
    `bool[](${r.roughnessDependent.join(", ")})`, typeof r.basicVariance == "number" && (r.basicVariance = Array(t.length).fill(r.basicVariance)), this.fullscreenMaterial.defines.basicVariance = /* glsl */
    `float[](${r.basicVariance.map((i) => i.toPrecision(5)).join(", ")})`, this.setTextures(t);
  }
  setTextures(e) {
    Array.isArray(e) || (e = [e]), this.textures = e;
    let t = "", r = "";
    for (let s = 0; s < this.textures.length; s++)
      t += /* glsl */
      `layout(location = ${s}) out vec4 gTexture${s};
`, t += /* glsl */
      `uniform sampler2D inputTexture${s};
`, r += /* glsl */
      `gTexture${s} = vec4(denoisedColor[${s}], sumVariance[${s}]);
`;
    let n = t + Lu.replace("#include <denoiseCustomComposeShaderFunctions>", this.options.denoiseCustomComposeShaderFunctions).replace("#include <denoiseCustomComposeShader>", this.options.denoiseCustomComposeShader).replace("#include <outputShader>", r).replaceAll("textureCount", this.textures.length).replaceAll("momentTextureCount", Math.min(this.textures.length, 2));
    n = on(n);
    const i = n.matchAll(/inputTexture\[\s*[0-9]+\s*]/g);
    for (const [s] of i) {
      const o = s.replace(/[^0-9]/g, "");
      n = n.replace(s, "inputTexture" + o);
    }
    delete this.fullscreenMaterial.defines.useTemporalReprojectTextures;
    for (let s = 0; s < e.length; s++) {
      const o = e[s];
      this.fullscreenMaterial.uniforms["inputTexture" + s] = new a(o), o.name.includes("TemporalReprojectPass.accumulatedTexture") && (this.fullscreenMaterial.defines.useTemporalReprojectTextures = "");
    }
    this.fullscreenMaterial.fragmentShader = n, this.fullscreenMaterial.needsUpdate = !0;
  }
  setDepthTexture(e) {
    this.fullscreenMaterial.uniforms.depthTexture.value = e, (e.type === se ? ce : $) === ce ? this.fullscreenMaterial.defines.RGBA_DEPTH_PACKING = "" : delete this.fullscreenMaterial.defines.RGBA_DEPTH_PACKING, this.options.depth = !0;
  }
  setNormalTexture(e, {
    useRoughnessInAlphaChannel: t = !1
  } = {}) {
    this.fullscreenMaterial.uniforms.normalTexture.value = e, this.options.normal = !0, this.options.roughness = t;
  }
  setMomentTexture(e) {
    this.fullscreenMaterial.uniforms.momentTexture.value = e, this.options.moment = !0;
  }
  setSize(e, t) {
    this.renderTargetA.setSize(e, t), this.renderTargetB.setSize(e, t), this.fullscreenMaterial.uniforms.invTexSize.value.set(1 / e, 1 / t);
  }
  dispose() {
    super.dispose(), this.renderTargetA.dispose(), this.renderTargetB.dispose();
  }
  keepEdgeStoppingDefinesUpdated() {
    for (const [t, r, n] of Uu) {
      var e;
      const i = this.options[t] && (r === "" || ((e = this.fullscreenMaterial.uniforms[r]) == null ? void 0 : e.value) > 1e-3);
      i !== n in this.fullscreenMaterial.defines && (i ? this.fullscreenMaterial.defines[n] = "" : delete this.fullscreenMaterial.defines[n], this.fullscreenMaterial.needsUpdate = !0);
    }
  }
  render(e) {
    this.keepEdgeStoppingDefinesUpdated();
    const t = this.fullscreenMaterial.uniforms.denoiseKernel.value;
    if (this.iterations > 0) {
      "doDenoise" in this.fullscreenMaterial.defines || (this.fullscreenMaterial.defines.doDenoise = "", this.fullscreenMaterial.needsUpdate = !0);
      for (let r = 0; r < 2 * this.iterations; r++) {
        const n = r % 2 === 0, i = 2 ** ~~(r / 2), o = Math.log2(i) % 2 == 0;
        this.fullscreenMaterial.uniforms.horizontal.value = n, this.fullscreenMaterial.uniforms.blurHorizontal.value = o, this.fullscreenMaterial.uniforms.stepSize.value = i, this.fullscreenMaterial.uniforms.isFirstIteration.value = r === 0, this.fullscreenMaterial.uniforms.isLastIteration.value = r === 2 * this.iterations - 1;
        const l = n ? this.renderTargetA : this.renderTargetB;
        for (let u = 0; u < this.textures.length; u++)
          this.fullscreenMaterial.uniforms["inputTexture" + u].value = n ? r === 0 ? this.textures[u] : this.renderTargetB.texture[u] : this.renderTargetA.texture[u];
        e.setRenderTarget(l), e.render(this.scene, this.camera);
      }
    } else
      "doDenoise" in this.fullscreenMaterial.defines && (delete this.fullscreenMaterial.defines.doDenoise, this.fullscreenMaterial.needsUpdate = !0), e.setRenderTarget(this.renderTargetB), e.render(this.scene, this.camera), this.fullscreenMaterial.uniforms.denoiseKernel.value = t;
    for (let r = 0; r < this.textures.length; r++)
      this.fullscreenMaterial.uniforms["inputTexture" + r].value = this.textures[r];
  }
  // final composition will be written to buffer 0
  get texture() {
    return this.renderTargetB.texture[0];
  }
}
var ku = `#define GLSLIFY 1
vec4 moment;if(!reset&&reprojectedUvDiffuse.x>=0.0){vec4 historyMoment=sampleReprojectedTexture(lastMomentTexture,reprojectedUvDiffuse);moment.r=luminance(gOutput[0].rgb);moment.g=moment.r*moment.r;
#if textureCount > 1
moment.b=luminance(gOutput[1].rgb);moment.a=moment.b*moment.b;
#endif
gMoment=mix(moment,historyMoment,0.8);}else{moment.rg=vec2(0.,1000.);moment.ba=vec2(0.,1000.);gMoment=moment;return;}`;
const ri = {
  fullAccumulate: !0,
  temporalReprojectCustomComposeShader: ku
};
class Vu extends cn {
  constructor(e, t, r, n = 1, i = ri) {
    i = {
      ...ri,
      ...i
    }, super(e, t, r, n, i), this.momentTexture = this.renderTarget.texture[0].clone(), this.momentTexture.isRenderTargetTexture = !0, this.momentTexture.type = Y, this.momentTexture.minFilter = b, this.momentTexture.magFilter = b, this.momentTexture.needsUpdate = !0, this.renderTarget.texture.push(this.momentTexture);
    const s = (
      /* glsl */
      `
		layout(location = ${n}) out vec4 gMoment;

		uniform sampler2D lastMomentTexture;
		`
    );
    this.fullscreenMaterial.fragmentShader = s + this.fullscreenMaterial.fragmentShader, this.fullscreenMaterial.uniforms = {
      ...this.fullscreenMaterial.uniforms,
      lastMomentTexture: new a(null)
    };
    const o = n + 1;
    this.copyPass.setTextureCount(o), this.copyPass.fullscreenMaterial.uniforms["inputTexture" + (o - 1)].value = this.momentTexture;
    const l = this.copyPass.renderTarget.texture[o - 1];
    l.type = Y, l.minFilter = F, l.magFilter = F, l.needsUpdate = !0, this.fullscreenMaterial.uniforms.lastMomentTexture.value = l, this.fullscreenMaterial.defines.momentTextureCount = Math.min(2, n);
  }
}
class $i {
  constructor(e, t, r, n = 1, i = {}) {
    this.svgfTemporalReprojectPass = new Vu(e, t, r, n, i);
    const s = this.svgfTemporalReprojectPass.renderTarget.texture.slice(0, n);
    this.denoisePass = new Ou(t, s, i), this.denoisePass.setMomentTexture(this.svgfTemporalReprojectPass.momentTexture), this.setNonJitteredDepthTexture(r.depthTexture);
  }
  // the denoised texture
  get texture() {
    return this.denoisePass.texture;
  }
  setGBuffers(e, t) {
    this.setJitteredGBuffers(e, t), this.setNonJitteredGBuffers(e, t);
  }
  setJitteredGBuffers(e, t, {
    useRoughnessInAlphaChannel: r = !1
  } = {}) {
    this.denoisePass.setDepthTexture(e), this.denoisePass.setNormalTexture(t, {
      useRoughnessInAlphaChannel: r
    });
  }
  setNonJitteredDepthTexture(e) {
    this.svgfTemporalReprojectPass.fullscreenMaterial.uniforms.depthTexture.value = e;
  }
  setVelocityTexture(e) {
    this.svgfTemporalReprojectPass.fullscreenMaterial.uniforms.velocityTexture.value = e;
  }
  setSize(e, t) {
    this.denoisePass.setSize(e, t), this.svgfTemporalReprojectPass.setSize(e, t);
  }
  dispose() {
    this.denoisePass.dispose(), this.svgfTemporalReprojectPass.dispose();
  }
  render(e) {
    this.svgfTemporalReprojectPass.render(e), this.denoisePass.render(e);
  }
}
class Hu extends X {
  constructor() {
    super("CubeToEquirectEnvPass"), this.renderTarget = new A(1, 1, {
      depthBuffer: !1,
      type: Y
    }), this.fullscreenMaterial = new N({
      fragmentShader: (
        /* glsl */
        `
            varying vec2 vUv;
			uniform samplerCube cubeMap;

			#define M_PI 3.1415926535897932384626433832795
			
			// source: https://github.com/spite/CubemapToEquirectangular/blob/master/src/CubemapToEquirectangular.js
            void main() {
				float longitude = vUv.x * 2. * M_PI - M_PI + M_PI / 2.;
				float latitude = vUv.y * M_PI;

				vec3 dir = vec3(
					- sin( longitude ) * sin( latitude ),
					cos( latitude ),
					- cos( longitude ) * sin( latitude )
				);

				dir.y = -dir.y;

				gl_FragColor = textureCube( cubeMap, dir );
            }
            `
      ),
      vertexShader: lt,
      uniforms: {
        cubeMap: {
          value: null
        }
      },
      blending: B,
      depthWrite: !1,
      depthTest: !1,
      toneMapped: !1
    });
  }
  dispose() {
    this.renderTarget.dispose();
  }
  generateEquirectEnvMap(e, t, r = null, n = null, i = 4096) {
    if (r === null && n === null) {
      const u = t.source.data[0].width, f = 2 ** Math.ceil(Math.log2(2 * u * 3 ** 0.5)), h = 2 ** Math.ceil(Math.log2(u * 3 ** 0.5));
      r = f, n = h;
    }
    r > i && (r = i, n = i / 2), this.renderTarget.setSize(r, n), this.fullscreenMaterial.uniforms.cubeMap.value = t;
    const {
      renderTarget: s
    } = this;
    e.setRenderTarget(s), e.render(this.scene, this.camera);
    const o = new Float32Array(r * n * 4);
    e.readRenderTargetPixels(s, 0, 0, r, n, o);
    const l = new le(o, r, n, ne, Y);
    return l.wrapS = tt, l.wrapT = tt, l.minFilter = nt, l.magFilter = nt, l.needsUpdate = !0, l.mapping = Mi, l;
  }
}
class Gu extends N {
  constructor() {
    super({
      type: "MRTMaterial",
      defines: {
        USE_UV: "",
        TEMPORAL_RESOLVE: ""
      },
      uniforms: {
        color: new a(new y()),
        emissive: new a(new y()),
        map: new a(null),
        roughnessMap: new a(null),
        metalnessMap: new a(null),
        emissiveMap: new a(null),
        alphaMap: new a(null),
        normalMap: new a(null),
        normalScale: new a(new g(1, 1)),
        roughness: new a(0),
        metalness: new a(0),
        emissiveIntensity: new a(0),
        uvTransform: new a(new Ot()),
        boneTexture: new a(null),
        blueNoiseTexture: new a(null),
        blueNoiseRepeat: new a(new g(1, 1)),
        texSize: new a(new g(1, 1)),
        frame: new a(0)
      },
      vertexShader: (
        /* glsl */
        `
                varying vec2 vHighPrecisionZW;

                #define NORMAL
                #if defined( FLAT_SHADED ) || defined( USE_BUMPMAP ) || defined( TANGENTSPACE_NORMALMAP )
                    varying vec3 vViewPosition;
                #endif
                
                #include <common>
                #include <uv_pars_vertex>
                #include <displacementmap_pars_vertex>
                #include <normal_pars_vertex>
                #include <morphtarget_pars_vertex>
                #include <logdepthbuf_pars_vertex>
                #include <clipping_planes_pars_vertex>
                #include <skinning_pars_vertex>
                #include <color_pars_vertex>

                varying vec2 screenUv;

                void main() {
                    #include <uv_vertex>
                    
                    #include <skinbase_vertex>
                    #include <beginnormal_vertex>
                    #include <skinnormal_vertex>
                    #include <defaultnormal_vertex>

                    #include <morphnormal_vertex>
                    #include <normal_vertex>
                    #include <begin_vertex>
                    #include <morphtarget_vertex>

                    #include <skinning_vertex>

                    #include <displacementmap_vertex>
                    #include <project_vertex>
                    #include <logdepthbuf_vertex>
                    #include <clipping_planes_vertex>

                    #include <color_vertex>
                    
                    #if defined( FLAT_SHADED ) || defined( USE_BUMPMAP ) || defined( TANGENTSPACE_NORMALMAP )
                        vViewPosition = - mvPosition.xyz;
                    #endif

                    screenUv = gl_Position.xy * 0.5 + 0.5;

                    vHighPrecisionZW = gl_Position.zw;
                }
            `
      ),
      fragmentShader: (
        /* glsl */
        `
                #define NORMAL
                #if defined( FLAT_SHADED ) || defined( USE_BUMPMAP ) || defined( TANGENTSPACE_NORMALMAP )
                    varying vec3 vViewPosition;
                #endif
                #include <packing>
                #include <uv_pars_fragment>
                #include <normal_pars_fragment>
                #include <bumpmap_pars_fragment>
                #include <normalmap_pars_fragment>
                #include <logdepthbuf_pars_fragment>
                #include <clipping_planes_pars_fragment>
                #include <color_pars_fragment>
                #include <alphamap_pars_fragment>
                
                layout(location = 0) out vec4 gDepth;
                layout(location = 1) out vec4 gNormal;
                layout(location = 2) out vec4 gDiffuse;
                layout(location = 3) out vec4 gEmissive;

                #include <map_pars_fragment>
                uniform vec3 color;

                varying vec2 vHighPrecisionZW;

                #include <metalnessmap_pars_fragment>
                uniform float metalness;

                #include <roughnessmap_pars_fragment>
                uniform float roughness;

                #include <emissivemap_pars_fragment>
                uniform vec3 emissive;
                uniform float emissiveIntensity;

#ifdef USE_ALPHAMAP
                uniform sampler2D blueNoiseTexture;
                uniform vec2 blueNoiseRepeat;
                uniform vec2 texSize;
                uniform int frame;

                varying vec2 screenUv;

                const float g = 1.6180339887498948482;
                const float a1 = 1.0 / g;

                // reference: https://extremelearning.com.au/unreasonable-effectiveness-of-quasirandom-sequences/
                float r1(float n) {
                    // 7th harmonious number
                    return fract(1.1127756842787055 + a1 * n);
                }

                const vec4 hn = vec4(0.618033988749895, 0.3247179572447458, 0.2207440846057596, 0.1673039782614187);

                vec4 sampleBlueNoise(vec2 uv, int seed) {
                    vec2 size = uv * texSize;
                    vec2 blueNoiseSize = texSize / blueNoiseRepeat;
                    float blueNoiseIndex = floor(floor(size.y / blueNoiseSize.y) * blueNoiseRepeat.x) + floor(size.x / blueNoiseSize.x);

                    // get the offset of this pixel's blue noise tile
                    int blueNoiseTileOffset = int(r1(blueNoiseIndex + 1.0) * 65536.);

                    vec2 blueNoiseUv = uv * blueNoiseRepeat;

                    // fetch blue noise for this pixel
                    vec4 blueNoise = textureLod(blueNoiseTexture, blueNoiseUv, 0.);

                    // animate blue noise
                    blueNoise = fract(blueNoise + hn * float(seed + blueNoiseTileOffset));

                    blueNoise.r = (blueNoise.r > 0.5 ? 1.0 - blueNoise.r : blueNoise.r) * 2.0;
                    blueNoise.g = (blueNoise.g > 0.5 ? 1.0 - blueNoise.g : blueNoise.g) * 2.0;
                    blueNoise.b = (blueNoise.b > 0.5 ? 1.0 - blueNoise.b : blueNoise.b) * 2.0;
                    blueNoise.a = (blueNoise.a > 0.5 ? 1.0 - blueNoise.a : blueNoise.a) * 2.0;

                    return blueNoise;
                }
#endif

                void main() {
                    // !todo: properly implement alpha hashing
                    // #ifdef USE_ALPHAMAP
                    // float alpha = textureLod( alphaMap, vUv, 0. ).g;

                    // float alphaThreshold = sampleBlueNoise(screenUv, frame).a;
                    // if(alpha < alphaThreshold){
                    //     discard;
                    //     return;
                    // }metalnessnor
                    // #endif

                    //! todo: find better solution
                    #define vMapUv vUv
                    #define vMetalnessMapUv vUv
                    #define vRoughnessMapUv vUv
                    #define vNormalMapUv vUv
                    #define vEmissiveMapUv vUv

                    #include <clipping_planes_fragment>
                    #include <logdepthbuf_fragment>
                    #include <normal_fragment_begin>
                    #include <normal_fragment_maps>

                    float roughnessFactor = roughness;
                    bool isDeselected = roughness > 10.0e9;
                    
                    if(isDeselected){
                        roughnessFactor = 1.;
                        gNormal = vec4(0.);
                    }else{
                        #ifdef USE_ROUGHNESSMAP
                            vec4 texelRoughness = textureLod( roughnessMap, vUv, 0. );
                            // reads channel G, compatible with a combined OcclusionRoughnessMetallic (RGB) texture
                            roughnessFactor *= texelRoughness.g;
                        #endif

                        // roughness of 1.0 is reserved for deselected meshes
                        roughnessFactor = min(0.99, roughnessFactor);

                        vec3 worldNormal = normalize((vec4(normal, 1.) * viewMatrix).xyz);
                        gNormal = vec4( worldNormal, roughnessFactor );
                    }
                    

                    if(isDeselected){
                        discard;
                        return;
                    }

                    float fragCoordZ = 0.5 * vHighPrecisionZW[0] / vHighPrecisionZW[1] + 0.5;

                    vec4 depthColor = packDepthToRGBA( fragCoordZ );
                    gDepth = depthColor;

                    #include <metalnessmap_fragment>

                    vec4 diffuseColor = vec4(color, metalnessFactor);

                    #include <map_fragment>
                    #include <color_fragment>

                    gDiffuse = diffuseColor;

                    vec3 totalEmissiveRadiance = emissive * emissiveIntensity;
                    #include <emissivemap_fragment>
                    
                    gEmissive = vec4(totalEmissiveRadiance, 0.);
                }
            `
      ),
      glslVersion: Te,
      toneMapped: !1,
      alphaTest: !1,
      fog: !1,
      lights: !1
    }), this.normalMapType = Ms, this.normalScale = new g(1, 1);
  }
}
var Wu = `#define GLSLIFY 1
#if !defined(diffuseOnly) && !defined(specularOnly)
layout(location=0)out vec4 gDiffuse;layout(location=1)out vec4 gSpecular;
#else
#ifdef diffuseOnly
layout(location=0)out vec4 gDiffuse;
#else
layout(location=0)out vec4 gSpecular;
#endif
#endif
varying vec2 vUv;uniform sampler2D directLightTexture;uniform sampler2D accumulatedTexture;uniform sampler2D normalTexture;uniform sampler2D depthTexture;uniform sampler2D diffuseTexture;uniform sampler2D emissiveTexture;uniform sampler2D blueNoiseTexture;uniform sampler2D velocityTexture;
#ifdef autoThickness
uniform sampler2D backSideDepthTexture;
#endif
uniform mat4 projectionMatrix;uniform mat4 inverseProjectionMatrix;uniform mat4 cameraMatrixWorld;uniform float cameraNear;uniform float cameraFar;uniform float maxEnvMapMipLevel;uniform float rayDistance;uniform float maxRoughness;uniform float thickness;uniform float envBlur;uniform int frame;uniform vec2 texSize;uniform vec2 blueNoiseRepeat;struct EquirectHdrInfo{sampler2D marginalWeights;sampler2D conditionalWeights;sampler2D map;vec2 size;float totalSumWhole;float totalSumDecimal;};uniform EquirectHdrInfo envMapInfo;
#define INVALID_RAY_COORDS vec2(-1.0);
#define EPSILON 0.00001
#define ONE_MINUS_EPSILON 1.0 - EPSILON
float nearMinusFar;float nearMulFar;float farMinusNear;vec2 invTexSize;
#include <packing>
#include <sampleBlueNoise>
#include <ssgi_utils>
vec2 RayMarch(inout vec3 dir,inout vec3 hitPos);vec2 BinarySearch(inout vec3 dir,inout vec3 hitPos);float fastGetViewZ(const float depth);vec3 doSample(const vec3 viewPos,const vec3 viewDir,const vec3 viewNormal,const vec3 worldPosition,const float metalness,const float roughness,const bool isDiffuseSample,const bool isEnvMisSample,const float NoV,const float NoL,const float NoH,const float LoH,const float VoH,const vec2 random,inout vec3 l,inout vec3 hitPos,out bool isMissedRay,out vec3 brdf,out float pdf);void main(){vec4 depthTexel=textureLod(depthTexture,vUv,0.0);if(dot(depthTexel.rgb,depthTexel.rgb)==0.){discard;return;}vec4 normalTexel=textureLod(normalTexture,vUv,0.0);float roughness=normalTexel.a;if(roughness==1.0||roughness>maxRoughness){discard;return;}invTexSize=1./texSize;roughness=clamp(roughness*roughness,0.0001,1.0);nearMinusFar=cameraNear-cameraFar;nearMulFar=cameraNear*cameraFar;farMinusNear=cameraFar-cameraNear;float unpackedDepth=unpackRGBAToDepth(depthTexel);float depth=fastGetViewZ(unpackedDepth);vec3 viewPos=getViewPosition(depth);vec3 viewDir=normalize(viewPos);vec3 worldNormal=normalTexel.xyz;vec3 viewNormal=normalize((vec4(worldNormal,0.)*cameraMatrixWorld).xyz);vec3 worldPos=vec4(vec4(viewPos,1.)*viewMatrix).xyz;vec4 diffuseTexel=textureLod(diffuseTexture,vUv,0.);vec3 diffuse=diffuseTexel.rgb;float metalness=diffuseTexel.a;vec3 n=viewNormal;vec3 v=-viewDir;float NoV=max(EPSILON,dot(n,v));vec3 V=(vec4(v,0.)*viewMatrix).xyz;vec3 N=worldNormal;vec4 blueNoise;vec3 H,l,h,F,T,B,envMisDir,gi;vec3 diffuseGI,specularGI,brdf,hitPos;Onb(N,T,B);V=ToLocal(T,B,N,V);vec3 f0=mix(vec3(0.04),diffuse,metalness);float NoL,NoH,LoH,VoH,diffW,specW,invW,pdf,envPdf,diffuseSamples,specularSamples,envMisProbability,envMisMultiplier;bool isDiffuseSample,isEnvMisSample,isMissedRay;int sampleCounter=0;
#pragma unroll_loop_start
for(int i=0;i<spp;i++){blueNoise=sampleBlueNoise(blueNoiseTexture,frame+sampleCounter++,blueNoiseRepeat,texSize);H=SampleGGXVNDF(V,roughness,roughness,blueNoise.r,blueNoise.g);if(H.z<0.0)H=-H;l=normalize(reflect(-V,H));l=ToWorld(T,B,N,l);l=(vec4(l,0.)*cameraMatrixWorld).xyz;l=normalize(l);h=normalize(v+l);NoL=clamp(dot(n,l),EPSILON,ONE_MINUS_EPSILON);NoH=clamp(dot(n,h),EPSILON,ONE_MINUS_EPSILON);LoH=clamp(dot(l,h),EPSILON,ONE_MINUS_EPSILON);VoH=clamp(dot(v,h),EPSILON,ONE_MINUS_EPSILON);
#if !defined(diffuseOnly) && !defined(specularOnly)
F=F_Schlick(f0,VoH);diffW=(1.-metalness)*luminance(diffuse);specW=luminance(F);diffW=max(diffW,EPSILON);specW=max(specW,EPSILON);invW=1./(diffW+specW);diffW*=invW;specW*=invW;isDiffuseSample=blueNoise.b<diffW;
#else
#ifdef diffuseOnly
isDiffuseSample=true;
#else
isDiffuseSample=false;
#endif
#endif
envMisDir=vec3(0.0);
#ifdef importanceSampling
envPdf=sampleEquirectProbability(envMapInfo,blueNoise.rg,envMisDir);envMisDir=normalize((vec4(envMisDir,0.)*cameraMatrixWorld).xyz);envMisProbability=0.25+dot(envMisDir,viewNormal)*0.5;isEnvMisSample=blueNoise.a<envMisProbability;envMisMultiplier=1./(1.-envMisProbability);if(isEnvMisSample){envPdf/=1.-envMisProbability;}else{envPdf=0.0001;}
#else
envPdf=0.0;envMisMultiplier=1.;
#endif
if(isDiffuseSample){if(isEnvMisSample){l=envMisDir;}else{l=cosineSampleHemisphere(viewNormal,blueNoise.rg);}h=normalize(v+l);NoL=clamp(dot(n,l),EPSILON,ONE_MINUS_EPSILON);NoH=clamp(dot(n,h),EPSILON,ONE_MINUS_EPSILON);LoH=clamp(dot(l,h),EPSILON,ONE_MINUS_EPSILON);VoH=clamp(dot(v,h),EPSILON,ONE_MINUS_EPSILON);gi=doSample(viewPos,viewDir,viewNormal,worldPos,metalness,roughness,isDiffuseSample,isEnvMisSample,NoV,NoL,NoH,LoH,VoH,blueNoise.rg,l,hitPos,isMissedRay,brdf,pdf);gi*=brdf;if(isEnvMisSample){gi*=misHeuristic(envPdf,pdf);gi/=envPdf;}else{gi/=pdf;gi*=envMisMultiplier;}diffuseSamples++;diffuseGI=mix(diffuseGI,gi,1./diffuseSamples);}else{isEnvMisSample=isEnvMisSample&&roughness>=0.025;if(isEnvMisSample){l=envMisDir;h=normalize(v+l);NoL=clamp(dot(n,l),EPSILON,ONE_MINUS_EPSILON);NoH=clamp(dot(n,h),EPSILON,ONE_MINUS_EPSILON);LoH=clamp(dot(l,h),EPSILON,ONE_MINUS_EPSILON);VoH=clamp(dot(v,h),EPSILON,ONE_MINUS_EPSILON);}gi=doSample(viewPos,viewDir,viewNormal,worldPos,metalness,roughness,isDiffuseSample,isEnvMisSample,NoV,NoL,NoH,LoH,VoH,blueNoise.rg,l,hitPos,isMissedRay,brdf,pdf);gi*=brdf;if(isEnvMisSample){gi*=misHeuristic(envPdf,pdf);gi/=envPdf;}else{gi/=pdf;gi*=envMisMultiplier;}specularSamples++;specularGI=mix(specularGI,gi,1./specularSamples);}}
#pragma unroll_loop_end
roughness=sqrt(roughness);
#ifndef specularOnly
if(diffuseSamples==0.0)diffuseGI=vec3(-1.0);gDiffuse=vec4(diffuseGI,roughness);
#endif
#ifndef diffuseOnly
float rayLength=0.0;if(!isMissedRay&&roughness<0.375&&getCurvature(viewNormal,depth)<0.0005){vec3 hitPosWS=(vec4(hitPos,1.)*viewMatrix).xyz;rayLength=distance(worldPos,hitPosWS);}if(specularSamples==0.0)specularGI=vec3(-1.0);gSpecular=vec4(specularGI,rayLength);
#endif
}vec3 doSample(const vec3 viewPos,const vec3 viewDir,const vec3 viewNormal,const vec3 worldPosition,const float metalness,const float roughness,const bool isDiffuseSample,const bool isEnvMisSample,const float NoV,const float NoL,const float NoH,const float LoH,const float VoH,const vec2 random,inout vec3 l,inout vec3 hitPos,out bool isMissedRay,out vec3 brdf,out float pdf){float cosTheta=max(0.0,dot(viewNormal,l));if(isDiffuseSample){vec3 diffuseBrdf=vec3(evalDisneyDiffuse(NoL,NoV,LoH,roughness,metalness));pdf=NoL/M_PI;pdf=max(EPSILON,pdf);brdf=diffuseBrdf;}else{vec3 specularBrdf=evalDisneySpecular(roughness,NoH,NoV,NoL);pdf=GGXVNDFPdf(NoH,NoV,roughness);pdf=max(EPSILON,pdf);brdf=specularBrdf;}brdf*=cosTheta;hitPos=viewPos;
#if steps == 0
hitPos+=l;vec2 coords=viewSpaceToScreenSpace(hitPos);
#else
vec2 coords=RayMarch(l,hitPos);
#endif
bool allowMissedRays=false;
#ifdef missedRays
allowMissedRays=true;
#endif
isMissedRay=coords.x==-1.0;vec3 envMapSample=vec3(0.);if(isMissedRay||allowMissedRays){
#ifdef USE_ENVMAP
vec3 reflectedWS=normalize((vec4(l,0.)*viewMatrix).xyz);
#ifdef BOX_PROJECTED_ENV_MAP
reflectedWS=parallaxCorrectNormal(reflectedWS.xyz,envMapSize,envMapPosition,worldPosition);reflectedWS=normalize(reflectedWS.xyz);
#endif
float mip=envBlur*maxEnvMapMipLevel;if(!isDiffuseSample&&roughness<0.15)mip*=roughness/0.15;envMapSample=sampleEquirectEnvMapColor(reflectedWS,envMapInfo.map,mip);float maxEnvLum=isEnvMisSample ? 50.0 : 5.0;if(maxEnvLum!=0.0){float envLum=luminance(envMapSample);if(envLum>maxEnvLum){envMapSample*=maxEnvLum/envLum;}}return envMapSample;
#else
return vec3(0.0);
#endif
}vec4 velocity=textureLod(velocityTexture,coords.xy,0.0);vec2 reprojectedUv=coords.xy-velocity.xy;vec3 SSGI;if(reprojectedUv.x>=0.0&&reprojectedUv.x<=1.0&&reprojectedUv.y>=0.0&&reprojectedUv.y<=1.0){vec4 emissiveTexel=textureLod(emissiveTexture,coords.xy,0.);vec3 emissiveColor=emissiveTexel.rgb*10.;vec3 reprojectedGI=getTexel(accumulatedTexture,reprojectedUv,0.).rgb;SSGI=reprojectedGI+emissiveColor;
#ifdef useDirectLight
SSGI+=textureLod(directLightTexture,coords.xy,0.).rgb*directLightMultiplier;
#endif
}else{SSGI=textureLod(directLightTexture,vUv,0.).rgb;}if(allowMissedRays){float ssgiLum=luminance(SSGI);float envLum=luminance(envMapSample);if(envLum>ssgiLum)SSGI=envMapSample;}return SSGI;}vec2 RayMarch(inout vec3 dir,inout vec3 hitPos){float rayHitDepthDifference;dir*=rayDistance/float(steps);vec2 uv;for(int i=1;i<steps;i++){float m=exp(pow(float(i)/4.0,0.05))-2.0;hitPos+=dir*min(m,1.);if(hitPos.z>0.0)return INVALID_RAY_COORDS;uv=viewSpaceToScreenSpace(hitPos);
#ifndef missedRays
if(uv.x<0.||uv.y<0.||uv.x>1.||uv.y>1.)return INVALID_RAY_COORDS;
#endif
float unpackedDepth=unpackRGBAToDepth(textureLod(depthTexture,uv,0.0));float depth=fastGetViewZ(unpackedDepth);
#ifdef autoThickness
float unpackedBackSideDepth=unpackRGBAToDepth(textureLod(backSideDepthTexture,uv,0.0));float backSideDepth=fastGetViewZ(unpackedBackSideDepth);float currentThickness=max(abs(depth-backSideDepth),thickness);
#else
float currentThickness=thickness;
#endif
rayHitDepthDifference=depth-hitPos.z;if(rayHitDepthDifference>=0.0&&rayHitDepthDifference<currentThickness){
#if refineSteps == 0
return uv;
#else
return BinarySearch(dir,hitPos);
#endif
}}
#ifndef missedRays
return INVALID_RAY_COORDS;
#endif
return uv;}vec2 BinarySearch(inout vec3 dir,inout vec3 hitPos){float rayHitDepthDifference;vec2 uv;dir*=0.5;hitPos-=dir;for(int i=0;i<refineSteps;i++){uv=viewSpaceToScreenSpace(hitPos);float unpackedDepth=unpackRGBAToDepth(textureLod(depthTexture,uv,0.0));float depth=fastGetViewZ(unpackedDepth);rayHitDepthDifference=depth-hitPos.z;dir*=0.5;hitPos+=rayHitDepthDifference>0.0 ?-dir : dir;}uv=viewSpaceToScreenSpace(hitPos);return uv;}float fastGetViewZ(const float depth){
#ifdef PERSPECTIVE_CAMERA
return nearMulFar/(farMinusNear*depth-cameraFar);
#else
return depth*nearMinusFar-cameraNear;
#endif
}`, ju = `#define GLSLIFY 1
#define PI M_PI
#define luminance(a) dot(vec3(0.2125, 0.7154, 0.0721), a)
vec4 getTexel(const sampler2D tex,vec2 p,const float mip){p=p/invTexSize+0.5;vec2 i=floor(p);vec2 f=p-i;f=f*f*f*(f*(f*6.0-15.0)+10.0);p=i+f;p=(p-0.5)*invTexSize;return textureLod(tex,p,mip);}float getViewZ(const float depth){
#ifdef PERSPECTIVE_CAMERA
return perspectiveDepthToViewZ(depth,cameraNear,cameraFar);
#else
return orthographicDepthToViewZ(depth,cameraNear,cameraFar);
#endif
}
#ifdef PERSPECTIVE_CAMERA
vec3 getViewPosition(const float depth){float clipW=projectionMatrix[2][3]*depth+projectionMatrix[3][3];vec4 clipPosition=vec4((vec3(vUv,depth)-0.5)*2.0,1.0);clipPosition*=clipW;return(inverseProjectionMatrix*clipPosition).xyz;}
#else
vec3 getViewPosition(const float depth){float z=getViewZ(depth);vec4 clipPosition=vec4(vUv*2.0-1.0,z,1.0);clipPosition*=clipPosition.z;return(inverseProjectionMatrix*clipPosition).xyz;}
#endif
vec2 viewSpaceToScreenSpace(const vec3 position){vec4 projectedCoord=projectionMatrix*vec4(position,1.0);projectedCoord.xy/=projectedCoord.w;projectedCoord.xy=projectedCoord.xy*0.5+0.5;return projectedCoord.xy;}
#ifdef BOX_PROJECTED_ENV_MAP
uniform vec3 envMapSize;uniform vec3 envMapPosition;vec3 parallaxCorrectNormal(const vec3 v,const vec3 cubeSize,const vec3 cubePos,const vec3 worldPosition){vec3 nDir=normalize(v);vec3 rbmax=(.5*cubeSize+cubePos-worldPosition)/nDir;vec3 rbmin=(-.5*cubeSize+cubePos-worldPosition)/nDir;vec3 rbminmax;rbminmax.x=(nDir.x>0.)? rbmax.x : rbmin.x;rbminmax.y=(nDir.y>0.)? rbmax.y : rbmin.y;rbminmax.z=(nDir.z>0.)? rbmax.z : rbmin.z;float correction=min(min(rbminmax.x,rbminmax.y),rbminmax.z);vec3 boxIntersection=worldPosition+nDir*correction;return boxIntersection-cubePos;}
#endif
#define M_PI 3.1415926535897932384626433832795
vec2 equirectDirectionToUv(const vec3 direction){vec2 uv=vec2(atan(direction.z,direction.x),acos(direction.y));uv/=vec2(2.0*M_PI,M_PI);uv.x+=0.5;uv.y=1.0-uv.y;return uv;}vec3 equirectUvToDirection(vec2 uv){uv.x-=0.5;uv.y=1.0-uv.y;float theta=uv.x*2.0*PI;float phi=uv.y*PI;float sinPhi=sin(phi);return vec3(sinPhi*cos(theta),cos(phi),sinPhi*sin(theta));}vec3 sampleEquirectEnvMapColor(const vec3 direction,const sampler2D map,const float lod){return getTexel(map,equirectDirectionToUv(direction),lod).rgb;}mat3 getBasisFromNormal(const vec3 normal){vec3 other;if(abs(normal.x)>0.5){other=vec3(0.0,1.0,0.0);}else{other=vec3(1.0,0.0,0.0);}vec3 ortho=normalize(cross(normal,other));vec3 ortho2=normalize(cross(normal,ortho));return mat3(ortho2,ortho,normal);}vec3 F_Schlick(const vec3 f0,const float theta){return f0+(1.-f0)*pow(1.0-theta,5.);}float F_Schlick(const float f0,const float f90,const float theta){return f0+(f90-f0)*pow(1.0-theta,5.0);}float D_GTR(const float roughness,const float NoH,const float k){float a2=pow(roughness,2.);return a2/(PI*pow((NoH*NoH)*(a2*a2-1.)+1.,k));}float SmithG(const float NDotV,const float alphaG){float a=alphaG*alphaG;float b=NDotV*NDotV;return(2.0*NDotV)/(NDotV+sqrt(a+b-a*b));}float GGXVNDFPdf(const float NoH,const float NoV,const float roughness){float D=D_GTR(roughness,NoH,2.);float G1=SmithG(NoV,roughness*roughness);return(D*G1)/max(0.00001,4.0f*NoV);}float GeometryTerm(const float NoL,const float NoV,const float roughness){float a2=roughness*roughness;float G1=SmithG(NoV,a2);float G2=SmithG(NoL,a2);return G1*G2;}float evalDisneyDiffuse(const float NoL,const float NoV,const float LoH,const float roughness,const float metalness){float FD90=0.5+2.*roughness*pow(LoH,2.);float a=F_Schlick(1.,FD90,NoL);float b=F_Schlick(1.,FD90,NoV);return(a*b/PI)*(1.-metalness);}vec3 evalDisneySpecular(const float roughness,const float NoH,const float NoV,const float NoL){float D=D_GTR(roughness,NoH,2.);float G=GeometryTerm(NoL,NoV,pow(0.5+roughness*.5,2.));vec3 spec=vec3(D*G/(4.*NoL*NoV));return spec;}vec3 SampleGGXVNDF(const vec3 V,const float ax,const float ay,const float r1,const float r2){vec3 Vh=normalize(vec3(ax*V.x,ay*V.y,V.z));float lensq=Vh.x*Vh.x+Vh.y*Vh.y;vec3 T1=lensq>0. ? vec3(-Vh.y,Vh.x,0.)*inversesqrt(lensq): vec3(1.,0.,0.);vec3 T2=cross(Vh,T1);float r=sqrt(r1);float phi=2.0*PI*r2;float t1=r*cos(phi);float t2=r*sin(phi);float s=0.5*(1.0+Vh.z);t2=(1.0-s)*sqrt(1.0-t1*t1)+s*t2;vec3 Nh=t1*T1+t2*T2+sqrt(max(0.0,1.0-t1*t1-t2*t2))*Vh;return normalize(vec3(ax*Nh.x,ay*Nh.y,max(0.0,Nh.z)));}void Onb(const vec3 N,inout vec3 T,inout vec3 B){vec3 up=abs(N.z)<0.9999999 ? vec3(0,0,1): vec3(1,0,0);T=normalize(cross(up,N));B=cross(N,T);}vec3 ToLocal(const vec3 X,const vec3 Y,const vec3 Z,const vec3 V){return vec3(dot(V,X),dot(V,Y),dot(V,Z));}vec3 ToWorld(const vec3 X,const vec3 Y,const vec3 Z,const vec3 V){return V.x*X+V.y*Y+V.z*Z;}vec3 cosineSampleHemisphere(const vec3 n,const vec2 u){float r=sqrt(u.x);float theta=2.0*PI*u.y;vec3 b=normalize(cross(n,vec3(0.0,1.0,1.0)));vec3 t=cross(b,n);return normalize(r*sin(theta)*b+sqrt(1.0-u.x)*n+r*cos(theta)*t);}float equirectDirectionPdf(vec3 direction){vec2 uv=equirectDirectionToUv(direction);float theta=uv.y*PI;float sinTheta=sin(theta);if(sinTheta==0.0){return 0.0;}return 1.0/(2.0*PI*PI*sinTheta);}float sampleEquirectProbability(EquirectHdrInfo info,vec2 r,out vec3 direction){float v=textureLod(info.marginalWeights,vec2(r.x,0.0),0.).x;float u=textureLod(info.conditionalWeights,vec2(r.y,v),0.).x;vec2 uv=vec2(u,v);vec3 derivedDirection=equirectUvToDirection(uv);direction=derivedDirection;vec3 color=texture(info.map,uv).rgb;float totalSum=info.totalSumWhole+info.totalSumDecimal;float lum=luminance(color);float pdf=lum/totalSum;return info.size.x*info.size.y*pdf;}float misHeuristic(float a,float b){float aa=a*a;float bb=b*b;return aa/(aa+bb);}float getCurvature(const vec3 n,const float depth){vec3 dx=dFdx(n);vec3 dy=dFdy(n);vec3 xneg=n-dx;vec3 xpos=n+dx;vec3 yneg=n-dy;vec3 ypos=n+dy;float curvature=(cross(xneg,xpos).y-cross(yneg,ypos).x)*4.0/depth;return curvature;}`, it = `#define GLSLIFY 1
const float g=1.6180339887498948482;const float a1=1.0/g;float r1(float n){return fract(1.1127756842787055+a1*n);}const vec4 hn=vec4(0.618033988749895,0.3247179572447458,0.2207440846057596,0.1673039782614187);vec4 sampleBlueNoise(sampler2D texture,int seed,vec2 repeat,vec2 texSize){vec2 size=vUv*texSize;vec2 blueNoiseSize=texSize/repeat;float blueNoiseIndex=floor(floor(size.y/blueNoiseSize.y)*repeat.x)+floor(size.x/blueNoiseSize.x);vec2 blueNoiseUv=vUv*repeat;vec4 blueNoise=textureLod(texture,blueNoiseUv,0.);if(seed!=0){blueNoise=fract(blueNoise+hn*float(seed));blueNoise.r=(blueNoise.r>0.5 ? 1.0-blueNoise.r : blueNoise.r)*2.0;blueNoise.g=(blueNoise.g>0.5 ? 1.0-blueNoise.g : blueNoise.g)*2.0;blueNoise.b=(blueNoise.b>0.5 ? 1.0-blueNoise.b : blueNoise.b)*2.0;blueNoise.a=(blueNoise.a>0.5 ? 1.0-blueNoise.a : blueNoise.a)*2.0;}return blueNoise;}`;
const Xu = ({
  data: {
    width: c,
    height: e,
    isFloatType: t,
    flipY: r,
    data: n
  }
}) => {
  const i = /* @__PURE__ */ s();
  function s() {
    const d = new ArrayBuffer(4), v = new Float32Array(d), x = new Uint32Array(d), w = new Uint32Array(512), M = new Uint32Array(512);
    for (let S = 0; S < 256; ++S) {
      const L = S - 127;
      L < -27 ? (w[S] = 0, w[S | 256] = 32768, M[S] = 24, M[S | 256] = 24) : L < -14 ? (w[S] = 1024 >> -L - 14, w[S | 256] = 1024 >> -L - 14 | 32768, M[S] = -L - 1, M[S | 256] = -L - 1) : L <= 15 ? (w[S] = L + 15 << 10, w[S | 256] = L + 15 << 10 | 32768, M[S] = 13, M[S | 256] = 13) : L < 128 ? (w[S] = 31744, w[S | 256] = 64512, M[S] = 24, M[S | 256] = 24) : (w[S] = 31744, w[S | 256] = 64512, M[S] = 13, M[S | 256] = 13);
    }
    const D = new Uint32Array(2048), P = new Uint32Array(64), O = new Uint32Array(64);
    for (let S = 1; S < 1024; ++S) {
      let L = S << 13, H = 0;
      for (; (L & 8388608) === 0; )
        L <<= 1, H -= 8388608;
      L &= -8388609, H += 947912704, D[S] = L | H;
    }
    for (let S = 1024; S < 2048; ++S)
      D[S] = 939524096 + (S - 1024 << 13);
    for (let S = 1; S < 31; ++S)
      P[S] = S << 23;
    P[31] = 1199570944, P[32] = 2147483648;
    for (let S = 33; S < 63; ++S)
      P[S] = 2147483648 + (S - 32 << 23);
    P[63] = 3347054592;
    for (let S = 1; S < 64; ++S)
      S !== 32 && (O[S] = 1024);
    return {
      floatView: v,
      uint32View: x,
      baseTable: w,
      shiftTable: M,
      mantissaTable: D,
      exponentTable: P,
      offsetTable: O
    };
  }
  function o(d) {
    const v = d >> 10;
    return i.uint32View[0] = i.mantissaTable[i.offsetTable[v] + (d & 1023)] + i.exponentTable[v], i.floatView[0];
  }
  function l(d, v, x) {
    return 0.2126 * d + 0.7152 * v + 0.0722 * x;
  }
  const u = (d, v, x = 0, w = d.length) => {
    let M = x, D = x + w - 1;
    for (; M < D; ) {
      const P = M + D >> 1;
      d[P] < v ? M = P + 1 : D = P;
    }
    return M - x;
  }, f = (d, v, x, w, M, D) => {
    if (w)
      for (let z = 0, k = x - 1; z <= k; z++)
        for (let R = 0, U = v * 4; R < U; R += 4) {
          const q = k - z, ie = z * U + R, J = q * U + R;
          d[J] = d[ie], d[J + 1] = d[ie + 1], d[J + 2] = d[ie + 2], d[J + 3] = d[ie + 3];
        }
    const P = new Float32Array(v * x), O = new Float32Array(v * x), S = new Float32Array(x), L = new Float32Array(x);
    let H = 0, Z = 0;
    for (let z = 0; z < x; z++) {
      let k = 0;
      for (let R = 0; R < v; R++) {
        const U = z * v + R, q = d[4 * U + 0], ie = d[4 * U + 1], J = d[4 * U + 2], Pe = l(q, ie, J);
        k += Pe, H += Pe, P[U] = Pe, O[U] = k;
      }
      if (k !== 0)
        for (let R = z * v, U = z * v + v; R < U; R++)
          P[R] /= k, O[R] /= k;
      Z += k, S[z] = k, L[z] = Z;
    }
    if (Z !== 0)
      for (let z = 0, k = S.length; z < k; z++)
        S[z] /= Z, L[z] /= Z;
    for (let z = 0; z < x; z++) {
      const k = (z + 1) / x, R = u(L, k);
      M[z] = (R + 0.5) / x;
    }
    for (let z = 0; z < x; z++)
      for (let k = 0; k < v; k++) {
        const R = z * v + k, U = (k + 1) / v, q = u(O, U, z * v, v);
        D[R] = (q + 0.5) / v;
      }
    return H;
  };
  if (!t) {
    const d = new Float32Array(n.length);
    for (const v in n)
      d[v] = o(n[v]);
    n = d;
  }
  const h = new Float32Array(e), m = new Float32Array(c * e), p = f(n, c, e, r, h, m);
  postMessage(t ? {
    totalSumValue: p,
    marginalDataArray: h,
    conditionalDataArray: m
  } : {
    data: n,
    totalSumValue: p,
    marginalDataArray: h,
    conditionalDataArray: m
  });
}, Yu = new Blob(["onmessage = " + Xu], {
  type: "application/javascript"
}), Zu = URL.createObjectURL(Yu);
class Ku {
  constructor() {
    const e = new le(new Float32Array([1, 1, 1, 1]), 1, 1);
    e.type = Y, e.format = ne, e.minFilter = F, e.magFilter = F, e.wrapS = G, e.wrapT = G, e.generateMipmaps = !1, e.needsUpdate = !0;
    const t = new le(new Float32Array([0, 1]), 1, 2);
    t.type = Y, t.format = Bt, t.minFilter = F, t.magFilter = F, t.generateMipmaps = !1, t.needsUpdate = !0;
    const r = new le(new Float32Array([0, 0, 1, 1]), 2, 2);
    r.type = Y, r.format = Bt, r.minFilter = F, r.magFilter = F, r.generateMipmaps = !1, r.needsUpdate = !0, this.map = e, this.marginalWeights = t, this.conditionalWeights = r, this.totalSumWhole = 1, this.totalSumDecimal = 0, this.size = new g();
  }
  dispose() {
    this.marginalWeights.dispose(), this.conditionalWeights.dispose(), this.map.dispose();
  }
  updateFrom(e) {
    e = e.clone();
    const {
      width: t,
      height: r,
      data: n
    } = e.image, {
      type: i
    } = e;
    return this.size.set(t, r), new Promise((s) => {
      let o;
      (o = this.worker) == null || o.terminate(), this.worker = new Worker(Zu), this.worker.postMessage({
        width: t,
        height: r,
        isFloatType: i === Y,
        flipY: e.flipY,
        data: n
      }), this.worker.onmessage = ({
        data: {
          data: l,
          totalSumValue: u,
          marginalDataArray: f,
          conditionalDataArray: h
        }
      }) => {
        this.dispose();
        const {
          marginalWeights: m,
          conditionalWeights: p
        } = this;
        m.image = {
          width: r,
          height: 1,
          data: f
        }, m.needsUpdate = !0, p.image = {
          width: t,
          height: r,
          data: h
        }, p.needsUpdate = !0;
        const d = ~~u, v = u - d;
        this.totalSumWhole = d, this.totalSumDecimal = v, l && (e.source = new ws({
          ...e.image
        }), e.image = {
          width: t,
          height: r,
          data: l
        }, e.type = Y), this.map = e, this.worker = null, s(e);
      };
    });
  }
}
class Qu extends N {
  constructor() {
    super({
      type: "SSGIMaterial",
      uniforms: {
        directLightTexture: new a(null),
        accumulatedTexture: new a(null),
        normalTexture: new a(null),
        depthTexture: new a(null),
        diffuseTexture: new a(null),
        emissiveTexture: new a(null),
        velocityTexture: new a(null),
        blueNoiseTexture: new a(null),
        backSideDepthTexture: new a(null),
        projectionMatrix: new a(new C()),
        inverseProjectionMatrix: new a(new C()),
        cameraMatrixWorld: new a(new C()),
        viewMatrix: new a(new C()),
        cameraNear: new a(0),
        cameraFar: new a(0),
        rayDistance: new a(0),
        thickness: new a(0),
        frame: new a(0),
        envBlur: new a(0),
        maxRoughness: new a(0),
        maxEnvMapMipLevel: new a(0),
        envMapInfo: {
          value: new Ku()
        },
        envMapPosition: new a(new T()),
        envMapSize: new a(new T()),
        viewMatrix: new a(new C()),
        texSize: new a(new g()),
        blueNoiseRepeat: new a(new g())
      },
      defines: {
        steps: 20,
        refineSteps: 5,
        spp: 1,
        directLightMultiplier: 1,
        CUBEUV_TEXEL_WIDTH: 0,
        CUBEUV_TEXEL_HEIGHT: 0,
        CUBEUV_MAX_MIP: 0,
        vWorldPosition: "worldPos"
      },
      fragmentShader: Wu.replace("#include <ssgi_utils>", ju).replace("#include <sampleBlueNoise>", it),
      vertexShader: lt,
      blending: B,
      depthWrite: !1,
      depthTest: !1,
      toneMapped: !1,
      glslVersion: Te
    });
  }
}
const qu = new y(0), Ju = new gi({
  depthPacking: ce,
  side: Et
});
class _u extends X {
  constructor(e, t) {
    super("BackSideDepthPass"), this._scene = e, this._camera = t, this.renderTarget = new A(1, 1, {
      minFilter: b,
      magFilter: b
    });
  }
  setSize(e, t) {
    this.renderTarget.setSize(e, t);
  }
  dispose() {
    super.dispose(), this.renderTarget.dispose();
  }
  render(e) {
    const {
      background: t
    } = this._scene;
    this._scene.background = qu, this._scene.overrideMaterial = Ju, e.setRenderTarget(this.renderTarget), e.render(this._scene, this._camera), this._scene.background = t, this._scene.overrideMaterial = null;
  }
}
let $u = "data:image/png;base64,", vr = $u;
const ef = new y(0);
class tf extends X {
  constructor(e, t) {
    super("SSGIPass"), this.needsSwap = !1, this.defaultFragmentShader = "", this.frame = 0, this.cachedMaterials = /* @__PURE__ */ new WeakMap(), this.visibleMeshes = [], this.ssgiEffect = e, this._scene = e._scene, this._camera = e._camera, this.fullscreenMaterial = new Qu(), this.defaultFragmentShader = this.fullscreenMaterial.fragmentShader;
    const r = !t.diffuseOnly && !t.specularOnly ? 2 : 1;
    this.renderTarget = new be(1, 1, r, {
      type: K,
      depthBuffer: !1
    }), this.fullscreenMaterial.uniforms.cameraMatrixWorld.value = this._camera.matrixWorld, this.fullscreenMaterial.uniforms.viewMatrix.value = this._camera.matrixWorldInverse, this.fullscreenMaterial.uniforms.projectionMatrix.value = this._camera.projectionMatrix, this.fullscreenMaterial.uniforms.inverseProjectionMatrix.value = this._camera.projectionMatrixInverse, e._camera.isPerspectiveCamera && (this.fullscreenMaterial.defines.PERSPECTIVE_CAMERA = ""), t.diffuseOnly && (this.fullscreenMaterial.defines.diffuseOnly = ""), t.specularOnly && (this.fullscreenMaterial.defines.specularOnly = ""), this.initMRTRenderTarget();
  }
  initialize(e, ...t) {
    super.initialize(e, ...t), new Ie().load(vr, (r) => {
      r.minFilter = b, r.magFilter = b, r.wrapS = G, r.wrapT = G, r.colorSpace = Fe, this.fullscreenMaterial.uniforms.blueNoiseTexture.value = r;
    });
  }
  get texture() {
    return this.renderTarget.texture[0];
  }
  get specularTexture() {
    const e = "specularOnly" in this.fullscreenMaterial.defines ? 0 : 1;
    return this.renderTarget.texture[e];
  }
  initMRTRenderTarget() {
    this.gBuffersRenderTarget = new be(1, 1, 4, {
      minFilter: b,
      magFilter: b
    }), this.gBuffersRenderTarget.depthTexture = new Oe(1, 1), this.gBuffersRenderTarget.depthTexture.type = Y, this.backSideDepthPass = new _u(this._scene, this._camera), this.depthTexture = this.gBuffersRenderTarget.texture[0], this.normalTexture = this.gBuffersRenderTarget.texture[1], this.diffuseTexture = this.gBuffersRenderTarget.texture[2], this.emissiveTexture = this.gBuffersRenderTarget.texture[3], this.diffuseTexture.minFilter = F, this.diffuseTexture.magFilter = F, this.diffuseTexture.colorSpace = V, this.diffuseTexture.needsUpdate = !0, this.emissiveTexture.minFilter = F, this.emissiveTexture.magFilter = F, this.emissiveTexture.type = K, this.emissiveTexture.needsUpdate = !0, this.normalTexture.type = K, this.normalTexture.needsUpdate = !0, this.fullscreenMaterial.uniforms.normalTexture.value = this.normalTexture, this.fullscreenMaterial.uniforms.depthTexture.value = this.depthTexture, this.fullscreenMaterial.uniforms.diffuseTexture.value = this.diffuseTexture, this.fullscreenMaterial.uniforms.emissiveTexture.value = this.emissiveTexture, this.fullscreenMaterial.uniforms.backSideDepthTexture.value = this.backSideDepthPass.renderTarget.texture;
  }
  setSize(e, t) {
    this.renderTarget.setSize(e * this.ssgiEffect.resolutionScale, t * this.ssgiEffect.resolutionScale), this.gBuffersRenderTarget.setSize(e, t), this.backSideDepthPass.setSize(e, t), this.fullscreenMaterial.uniforms.texSize.value.set(this.renderTarget.width, this.renderTarget.height);
  }
  dispose() {
    super.dispose(), this.renderTarget.dispose(), this.gBuffersRenderTarget.dispose(), this.backSideDepthPass.dispose(), this.fullscreenMaterial.dispose(), this.normalTexture = null, this.depthTexture = null, this.diffuseTexture = null, this.emissiveTexture = null;
  }
  setMRTMaterialInScene() {
    this.visibleMeshes = pr(this._scene);
    for (const i of this.visibleMeshes) {
      var e, t, r;
      const s = i.material;
      let [o, l] = this.cachedMaterials.get(i) || [];
      if (s !== o) {
        var n;
        l && l.dispose(), l = new Gu(), _i(s, l), l.uniforms.normalScale.value = s.normalScale, (n = i.skeleton) != null && n.boneTexture && (l.defines.USE_SKINNING = "", l.defines.BONE_TEXTURE = "", l.uniforms.boneTexture.value = i.skeleton.boneTexture, l.needsUpdate = !0);
        const h = Object.keys(s).find((m) => {
          const p = s[m];
          return p instanceof It && p.matrix;
        });
        h && (l.uniforms.uvTransform.value = s[h].matrix), this.cachedMaterials.set(i, [s, l]);
      }
      s.emissive && (l.uniforms.emissive.value = s.emissive), s.color && (l.uniforms.color.value = s.color), ht(l, s, "normalMap", "USE_NORMALMAP"), ht(l, s, "roughnessMap", "USE_ROUGHNESSMAP"), ht(l, s, "metalnessMap", "USE_	METALNESSMAP"), ht(l, s, "map", "USE_MAP"), ht(l, s, "emissiveMap", "USE_EMISSIVEMAP"), ht(l, s, "alphaMap", "USE_ALPHAMAP");
      const u = this.fullscreenMaterial.uniforms.blueNoiseTexture.value;
      if (u) {
        const {
          width: h,
          height: m
        } = u.source.data;
        l.uniforms.blueNoiseTexture.value = u, l.uniforms.blueNoiseRepeat.value.set(this.renderTarget.width / h, this.renderTarget.height / m);
      }
      l.uniforms.texSize.value.set(this.renderTarget.width, this.renderTarget.height), l.uniforms.frame.value = this.frame, i.visible = ln(i, s);
      const f = (e = s.roughness) !== null && e !== void 0 ? e : 1;
      l.uniforms.roughness.value = this.ssgiEffect.selection.size === 0 || this.ssgiEffect.selection.has(i) ? f : 1e11, l.uniforms.metalness.value = (t = i.material.metalness) !== null && t !== void 0 ? t : 0, l.uniforms.emissiveIntensity.value = (r = i.material.emissiveIntensity) !== null && r !== void 0 ? r : 0, i.material = l;
    }
  }
  unsetMRTMaterialInScene() {
    for (const e of this.visibleMeshes) {
      e.visible = !0;
      const [t] = this.cachedMaterials.get(e);
      e.material = t;
    }
  }
  render(e) {
    this.frame = (this.frame + this.ssgiEffect.spp) % 65536;
    const {
      background: t
    } = this._scene;
    this._scene.background = ef, this.setMRTMaterialInScene(), e.setRenderTarget(this.gBuffersRenderTarget), e.render(this._scene, this._camera), this.unsetMRTMaterialInScene(), this.ssgiEffect.autoThickness && this.backSideDepthPass.render(e), this.fullscreenMaterial.uniforms.frame.value = this.frame, this.fullscreenMaterial.uniforms.cameraNear.value = this._camera.near, this.fullscreenMaterial.uniforms.cameraFar.value = this._camera.far, this.fullscreenMaterial.uniforms.viewMatrix.value.copy(this._camera.matrixWorldInverse), this.fullscreenMaterial.uniforms.accumulatedTexture.value = this.ssgiEffect.svgf.denoisePass.texture;
    const r = this.fullscreenMaterial.uniforms.blueNoiseTexture.value;
    if (r) {
      const {
        width: n,
        height: i
      } = r.source.data;
      this.fullscreenMaterial.uniforms.blueNoiseRepeat.value.set(this.renderTarget.width / n, this.renderTarget.height / i);
    }
    e.setRenderTarget(this.renderTarget), e.render(this.scene, this.camera), this._scene.background = t;
  }
}
var rf = `#define GLSLIFY 1
uniform sampler2D inputTexture;uniform sampler2D sceneTexture;uniform sampler2D depthTexture;uniform int toneMapping;
#include <tonemapping_pars_fragment>
#pragma tonemapping_pars_fragment
void mainImage(const in vec4 inputColor,const in vec2 uv,out vec4 outputColor){vec4 depthTexel=textureLod(depthTexture,uv,0.);vec3 ssgiClr;if(dot(depthTexel.rgb,depthTexel.rgb)==0.){ssgiClr=textureLod(sceneTexture,uv,0.).rgb;}else{ssgiClr=textureLod(inputTexture,uv,0.).rgb;switch(toneMapping){case 1:ssgiClr=LinearToneMapping(ssgiClr);break;case 2:ssgiClr=ReinhardToneMapping(ssgiClr);break;case 3:ssgiClr=OptimizedCineonToneMapping(ssgiClr);break;case 4:ssgiClr=ACESFilmicToneMapping(ssgiClr);break;case 5:ssgiClr=CustomToneMapping(ssgiClr);break;}ssgiClr*=toneMappingExposure;}outputColor=vec4(ssgiClr,1.0);}`, nf = `#define GLSLIFY 1
vec3 viewNormal=(vec4(normal,0.)*cameraMatrixWorld).xyz;roughness*=roughness;vec3 viewPos=getViewPosition(depth);vec3 viewDir=normalize(viewPos);vec3 T,B;vec3 n=viewNormal;vec3 v=viewDir;vec3 V=(vec4(v,0.)*viewMatrix).xyz;vec3 N=normal;Onb(N,T,B);V=ToLocal(T,B,N,V);vec3 H=SampleGGXVNDF(V,roughness,roughness,0.25,0.25);if(H.z<0.0)H=-H;vec3 l=normalize(reflect(-V,H));l=ToWorld(T,B,N,l);l=(vec4(l,1.)*cameraMatrixWorld).xyz;l=normalize(l);if(dot(viewNormal,l)<0.)l=-l;vec3 h=normalize(v+l);float VoH=max(EPSILON,dot(v,h));VoH=pow(VoH,0.875);vec4 diffuseTexel=textureLod(diffuseTexture,vUv,0.);vec3 diffuse=diffuseTexel.rgb;float metalness=diffuseTexel.a;vec3 f0=mix(vec3(0.04),diffuse,metalness);vec3 F=F_Schlick(f0,VoH);vec3 directLight=textureLod(directLightTexture,vUv,0.).rgb;
#ifdef ssgi
vec3 diffuseLightingColor=denoisedColor[0];vec3 diffuseComponent=diffuse*(1.-metalness)*(1.-F)*diffuseLightingColor;vec3 specularLightingColor=denoisedColor[1];vec3 specularComponent=specularLightingColor*F;denoisedColor[0]=diffuseComponent+specularComponent;
#endif
#ifdef ssdgi
vec3 diffuseLightingColor=denoisedColor[0];vec3 diffuseComponent=diffuse*(1.-metalness)*(1.-F)*diffuseLightingColor;denoisedColor[0]=diffuseComponent;
#endif
#ifdef ssr
vec3 specularLightingColor=denoisedColor[0];vec3 specularComponent=specularLightingColor*F;denoisedColor[0]=specularComponent;
#endif
#ifdef useDirectLight
denoisedColor[0]+=directLight;
#endif
`, sf = `#define GLSLIFY 1
uniform sampler2D diffuseTexture;uniform sampler2D directLightTexture;vec3 getViewPosition(const float depth){float clipW=projectionMatrix[2][3]*depth+projectionMatrix[3][3];vec4 clipPosition=vec4((vec3(vUv,depth)-0.5)*2.0,1.0);clipPosition*=clipW;return(projectionMatrixInverse*clipPosition).xyz;}vec3 F_Schlick(const vec3 f0,const float theta){return f0+(1.-f0)*pow(1.0-theta,5.);}vec3 SampleGGXVNDF(const vec3 V,const float ax,const float ay,const float r1,const float r2){vec3 Vh=normalize(vec3(ax*V.x,ay*V.y,V.z));float lensq=Vh.x*Vh.x+Vh.y*Vh.y;vec3 T1=lensq>0. ? vec3(-Vh.y,Vh.x,0.)*inversesqrt(lensq): vec3(1.,0.,0.);vec3 T2=cross(Vh,T1);float r=sqrt(r1);float phi=2.0*PI*r2;float t1=r*cos(phi);float t2=r*sin(phi);float s=0.5*(1.0+Vh.z);t2=(1.0-s)*sqrt(1.0-t1*t1)+s*t2;vec3 Nh=t1*T1+t2*T2+sqrt(max(0.0,1.0-t1*t1-t2*t2))*Vh;return normalize(vec3(ax*Nh.x,ay*Nh.y,max(0.0,Nh.z)));}void Onb(const vec3 N,inout vec3 T,inout vec3 B){vec3 up=abs(N.z)<0.9999999 ? vec3(0,0,1): vec3(1,0,0);T=normalize(cross(up,N));B=cross(N,T);}vec3 ToLocal(const vec3 X,const vec3 Y,const vec3 Z,const vec3 V){return vec3(dot(V,X),dot(V,Y),dot(V,Z));}vec3 ToWorld(const vec3 X,const vec3 Y,const vec3 Z,const vec3 V){return V.x*X+V.y*Y+V.z*Z;}`;
const st = {
  distance: 10,
  thickness: 10,
  autoThickness: !1,
  maxRoughness: 1,
  blend: 0.9,
  denoiseIterations: 1,
  denoiseKernel: 2,
  denoiseDiffuse: 10,
  denoiseSpecular: 10,
  depthPhi: 2,
  normalPhi: 50,
  roughnessPhi: 1,
  envBlur: 0.5,
  importanceSampling: !0,
  directLightMultiplier: 1,
  steps: 20,
  refineSteps: 5,
  spp: 1,
  resolutionScale: 1,
  missedRays: !1
}, {
  render: ni
} = pe.prototype, ii = bu(), si = Tu();
class gr extends I {
  /**
    * @param {THREE.Scene} scene The scene of the SSGI effect
    * @param {THREE.Camera} camera The camera with which SSGI is being rendered
    * @param {velocityDepthNormalPass} velocityDepthNormalPass Required velocity pass
    * @param {SSGIOptions} [options] The optional options for the SSGI effect
    */
  constructor(e, t, r, n = st) {
    if (n = {
      ...st,
      ...n
    }, super("SSGIEffect", rf, {
      type: "FinalSSGIMaterial",
      uniforms: /* @__PURE__ */ new Map([["inputTexture", new a(null)], ["sceneTexture", new a(null)], ["depthTexture", new a(null)], ["toneMapping", new a(ys)]])
    }), this.selection = new Se(), this.isUsingRenderPass = !0, !(t instanceof ke))
      throw new Error(this.constructor.name + " doesn't support cameras of type '" + t.constructor.name + "' yet. Only cameras of type 'PerspectiveCamera' are supported.");
    this._scene = e, this._camera = t;
    let i;
    n.diffuseOnly ? (i = "ssdgi", n.reprojectSpecular = !1, n.roughnessDependent = !1, n.basicVariance = 25e-5, n.neighborhoodClamp = !1) : n.specularOnly ? (i = "ssr", n.reprojectSpecular = !0, n.roughnessDependent = !0, n.basicVariance = 25e-5, n.neighborhoodClamp = !0) : (i = "ssgi", n.reprojectSpecular = [!1, !0], n.neighborhoodClamp = [!1, !0], n.roughnessDependent = [!1, !0], n.basicVariance = [25e-5, 25e-5]), n.neighborhoodClampRadius = 2, n.neighborhoodClampIntensity = 0.5;
    const s = n.diffuseOnly || n.specularOnly ? 1 : 2;
    n = {
      ...n,
      denoiseCustomComposeShader: nf,
      denoiseCustomComposeShaderFunctions: sf
    }, this.svgf = new $i(e, t, r, s, n), i === "ssgi" ? this.svgf.svgfTemporalReprojectPass.fullscreenMaterial.fragmentShader = this.svgf.svgfTemporalReprojectPass.fullscreenMaterial.fragmentShader.replace("accumulatedTexel[ 1 ].rgb = clampedColor;", `
						float roughness = inputTexel[ 0 ].a;
						accumulatedTexel[ 1 ].rgb = mix(accumulatedTexel[ 1 ].rgb, clampedColor, 1. - sqrt(roughness));
						`) : i === "ssr" && (this.svgf.svgfTemporalReprojectPass.fullscreenMaterial.fragmentShader = this.svgf.svgfTemporalReprojectPass.fullscreenMaterial.fragmentShader.replace("accumulatedTexel[ 0 ].rgb = clampedColor;", `
					accumulatedTexel[ 0 ].rgb = mix(accumulatedTexel[ 0 ].rgb, clampedColor, 0.5);
					`)), this.svgf.svgfTemporalReprojectPass.fullscreenMaterial.needsUpdate = !0, this.ssgiPass = new tf(this, n), n.diffuseOnly ? this.svgf.svgfTemporalReprojectPass.fullscreenMaterial.uniforms.inputTexture0.value = this.ssgiPass.texture : n.specularOnly ? this.svgf.svgfTemporalReprojectPass.fullscreenMaterial.uniforms.inputTexture0.value = this.ssgiPass.specularTexture : (this.svgf.svgfTemporalReprojectPass.fullscreenMaterial.uniforms.inputTexture0.value = this.ssgiPass.texture, this.svgf.svgfTemporalReprojectPass.fullscreenMaterial.uniforms.inputTexture1.value = this.ssgiPass.specularTexture), this.svgf.setJitteredGBuffers(this.ssgiPass.depthTexture, this.ssgiPass.normalTexture, {
      useRoughnessInAlphaChannel: !0
    }), this.svgf.denoisePass.fullscreenMaterial.uniforms = {
      ...this.svgf.denoisePass.fullscreenMaterial.uniforms,
      diffuseTexture: new a(null),
      directLightTexture: new a(null)
    }, this.svgf.denoisePass.fullscreenMaterial.defines[i] = "", this.svgf.denoisePass.fullscreenMaterial.uniforms.diffuseTexture.value = this.ssgiPass.diffuseTexture, this.lastSize = {
      width: n.width,
      height: n.height,
      resolutionScale: n.resolutionScale
    }, this.sceneRenderTarget = new A(1, 1, {
      colorSpace: V
    }), this.renderPass = new pe(this._scene, this._camera), this.renderPass.renderToScreen = !1, this.setSize(n.width, n.height);
    const o = this, l = this.renderPass;
    pe.prototype.render = function(...u) {
      if (this !== l) {
        const f = o.isUsingRenderPass;
        o.isUsingRenderPass = !0, f != o.isUsingRenderPass && o.updateUsingRenderPass();
      }
      ni.call(this, ...u);
    }, this.makeOptionsReactive(n);
  }
  updateUsingRenderPass() {
    this.isUsingRenderPass ? (this.ssgiPass.fullscreenMaterial.defines.useDirectLight = "", this.svgf.denoisePass.fullscreenMaterial.defines.useDirectLight = "") : (delete this.ssgiPass.fullscreenMaterial.defines.useDirectLight, delete this.svgf.denoisePass.fullscreenMaterial.defines.useDirectLight), this.ssgiPass.fullscreenMaterial.needsUpdate = !0, this.svgf.denoisePass.fullscreenMaterial.needsUpdate = !0;
  }
  makeOptionsReactive(e) {
    let t = !1;
    const r = this.ssgiPass.fullscreenMaterial.uniforms, n = Object.keys(r), i = this.svgf.svgfTemporalReprojectPass;
    for (const s of Object.keys(e))
      Object.defineProperty(this, s, {
        get() {
          return e[s];
        },
        set(o) {
          if (!(e[s] === o && t))
            switch (e[s] = o, s) {
              // denoiser
              case "denoiseIterations":
                this.svgf.denoisePass.iterations = o;
                break;
              case "denoiseDiffuse":
                this.svgf.denoisePass.fullscreenMaterial.uniforms.denoise.value[0] = o;
                break;
              case "denoiseSpecular":
                this.svgf.denoisePass.fullscreenMaterial.uniforms.denoise.value[1] = o;
                break;
              case "denoiseKernel":
              case "depthPhi":
              case "normalPhi":
              case "roughnessPhi":
                this.svgf.denoisePass.fullscreenMaterial.uniforms[s].value = o;
                break;
              // SSGI
              case "resolutionScale":
                this.setSize(this.lastSize.width, this.lastSize.height), i.reset();
                break;
              // defines
              case "spp":
                this.ssgiPass.fullscreenMaterial.fragmentShader = this.ssgiPass.defaultFragmentShader.replaceAll("spp", o), o !== 1 && (this.ssgiPass.fullscreenMaterial.fragmentShader = on(this.ssgiPass.fullscreenMaterial.fragmentShader.replace("#pragma unroll_loop_start", "").replace("#pragma unroll_loop_end", ""))), this.ssgiPass.fullscreenMaterial.needsUpdate = t, i.reset();
                break;
              case "steps":
              case "refineSteps":
                this.ssgiPass.fullscreenMaterial.defines[s] = parseInt(o), this.ssgiPass.fullscreenMaterial.needsUpdate = t, i.reset();
                break;
              case "directLightMultiplier":
                this.ssgiPass.fullscreenMaterial.defines[s] = o.toPrecision(5), this.ssgiPass.fullscreenMaterial.needsUpdate = t, i.reset();
                break;
              case "importanceSampling":
              case "missedRays":
              case "autoThickness":
                o ? this.ssgiPass.fullscreenMaterial.defines[s] = "" : delete this.ssgiPass.fullscreenMaterial.defines[s], this.ssgiPass.fullscreenMaterial.needsUpdate = t, i.reset();
                break;
              case "blend":
                this.svgf.svgfTemporalReprojectPass.fullscreenMaterial.uniforms[s].value = o, i.reset();
                break;
              case "distance":
                r.rayDistance.value = o, i.reset();
                break;
              // must be a uniform
              default:
                n.includes(s) && (r[s].value = o, i.reset());
            }
        }
      }), this[s] = e[s];
    t = !0;
  }
  initialize(e, ...t) {
    super.initialize(e, ...t), this.ssgiPass.initialize(e, ...t);
  }
  setSize(e, t, r = !1) {
    let n;
    e === void 0 && t === void 0 || !r && e === this.lastSize.width && t === this.lastSize.height && this.resolutionScale === this.lastSize.resolutionScale || (this.ssgiPass.setSize(e, t), this.svgf.setSize(e, t), this.sceneRenderTarget.setSize(e, t), (n = this.cubeToEquirectEnvPass) == null || n.setSize(e, t), this.lastSize = {
      width: e,
      height: t,
      resolutionScale: this.resolutionScale
    });
  }
  dispose() {
    let e;
    super.dispose(), this.ssgiPass.dispose(), this.svgf.dispose(), (e = this.cubeToEquirectEnvPass) == null || e.dispose(), pe.prototype.render = ni;
  }
  keepEnvMapUpdated(e) {
    const t = this.ssgiPass.fullscreenMaterial;
    let r = this._scene.environment;
    if (r) {
      if (t.uniforms.envMapInfo.value.mapUuid !== r.uuid) {
        r.isCubeTexture && (this.cubeToEquirectEnvPass || (this.cubeToEquirectEnvPass = new Hu()), r = this.cubeToEquirectEnvPass.generateEquirectEnvMap(e, r), r.uuid = this._scene.environment.uuid), r.generateMipmaps || (r.generateMipmaps = !0, r.minFilter = nt, r.magFilter = nt, r.needsUpdate = !0), t.uniforms.envMapInfo.value.mapUuid = r.uuid;
        const n = yu(r);
        t.uniforms.maxEnvMapMipLevel.value = n, t.uniforms.envMapInfo.value.map = r, t.defines.USE_ENVMAP = "", delete t.defines.importanceSampling, this.importanceSampling ? t.uniforms.envMapInfo.value.updateFrom(r, e).then(() => {
          t.defines.importanceSampling = "", t.needsUpdate = !0;
        }) : t.uniforms.envMapInfo.value.map = r, this.svgf.svgfTemporalReprojectPass.reset(), t.needsUpdate = !0;
      }
    } else "USE_ENVMAP" in t.defines && (delete t.defines.USE_ENVMAP, delete t.defines.importanceSampling, t.needsUpdate = !0);
  }
  update(e, t) {
    this.keepEnvMapUpdated(e);
    const r = this.isUsingRenderPass ? t : this.sceneRenderTarget, n = [];
    if (!this.isUsingRenderPass) {
      const s = [];
      for (const o of pr(this._scene)) {
        if (o.isScene)
          return;
        o.visible = !ln(o), o.visible ? n.push(o) : s.push(o);
      }
      this.renderPass.render(e, this.sceneRenderTarget);
      for (const o of s)
        o.visible = !0;
      for (const o of n)
        o.visible = !1;
    }
    this.ssgiPass.fullscreenMaterial.uniforms.directLightTexture.value = r.texture, this.svgf.denoisePass.fullscreenMaterial.uniforms.directLightTexture.value = r.texture, this.ssgiPass.render(e), this.svgf.render(e), this.uniforms.get("inputTexture").value = this.svgf.texture, this.uniforms.get("sceneTexture").value = r.texture, this.uniforms.get("depthTexture").value = this.ssgiPass.depthTexture, this.uniforms.get("toneMapping").value = e.toneMapping;
    for (const s of n)
      s.visible = !0;
    const i = !this.diffuseOnly && !this.specularOnly;
    ii.value = i || this.diffuseOnly === !0, si.value = i || this.specularOnly == !0, cancelAnimationFrame(this.rAF2), cancelAnimationFrame(this.rAF), cancelAnimationFrame(this.usingRenderPassRAF), this.rAF = requestAnimationFrame(() => {
      this.rAF2 = requestAnimationFrame(() => {
        ii.value = !1, si.value = !1;
      });
    }), this.usingRenderPassRAF = requestAnimationFrame(() => {
      const s = this.isUsingRenderPass;
      this.isUsingRenderPass = !1, s != this.isUsingRenderPass && this.updateUsingRenderPass();
    });
  }
}
gr.DefaultOptions = st;
class af extends gr {
  constructor(e, t, r, n = st) {
    n = {
      ...st,
      ...n
    }, n.specularOnly = !0, super(e, t, r, n);
  }
}
class of extends gr {
  constructor(e, t, r, n = st) {
    n = {
      ...st,
      ...n
    }, n.diffuseOnly = !0, super(e, t, r, n);
  }
}
var lf = `#define GLSLIFY 1
uniform sampler2D inputTexture;uniform sampler2D velocityTexture;uniform sampler2D blueNoiseTexture;uniform ivec2 blueNoiseSize;uniform vec2 texSize;uniform float intensity;uniform float jitter;uniform float deltaTime;uniform int frame;uvec4 s0,s1;ivec2 pixel;void rng_initialize(vec2 p,int frame){pixel=ivec2(p);s0=uvec4(p,uint(frame),uint(p.x)+uint(p.y));s1=uvec4(frame,frame*15843,frame*31+4566,frame*2345+58585);}void pcg4d(inout uvec4 v){v=v*1664525u+1013904223u;v.x+=v.y*v.w;v.y+=v.z*v.x;v.z+=v.x*v.y;v.w+=v.y*v.z;v=v ^(v>>16u);v.x+=v.y*v.w;v.y+=v.z*v.x;v.z+=v.x*v.y;v.w+=v.y*v.z;}ivec2 shift2(){pcg4d(s1);return(pixel+ivec2(s1.xy % 0x0fffffffu))% blueNoiseSize;}void mainImage(const in vec4 inputColor,const in vec2 uv,out vec4 outputColor){vec4 velocity=textureLod(velocityTexture,vUv,0.0);if(dot(velocity.xyz,velocity.xyz)==0.0){outputColor=inputColor;return;}velocity.xy*=intensity;rng_initialize(vUv*texSize,frame);vec2 blueNoise=texelFetch(blueNoiseTexture,shift2(),0).rg-0.5;vec2 jitterOffset=jitter*velocity.xy*blueNoise;float frameSpeed=(1./100.)/deltaTime;vec2 startUv=vUv+(jitterOffset-velocity.xy*0.5)*frameSpeed;vec2 endUv=vUv+(jitterOffset+velocity.xy*0.5)*frameSpeed;startUv=max(vec2(0.),startUv);endUv=min(vec2(1.),endUv);vec3 motionBlurredColor;for(float i=0.0;i<=samplesFloat;i++){vec2 reprojectedUv=mix(startUv,endUv,i/samplesFloat);vec3 neighborColor=textureLod(inputTexture,reprojectedUv,0.0).rgb;motionBlurredColor+=neighborColor;}motionBlurredColor/=samplesFloat;outputColor=vec4(motionBlurredColor,inputColor.a);}`;
const ai = {
  intensity: 1,
  jitter: 1,
  samples: 16
};
let cf = class extends I {
  constructor(e, t = ai) {
    t = {
      ...ai,
      ...t
    }, super("MotionBlurEffect", lf, {
      type: "MotionBlurMaterial",
      uniforms: /* @__PURE__ */ new Map([["inputTexture", new a(null)], ["velocityTexture", new a(e.texture)], ["blueNoiseTexture", new a(null)], ["blueNoiseSize", new a(new g())], ["texSize", new a(new g())], ["intensity", new a(1)], ["jitter", new a(1)], ["frame", new a(0)], ["deltaTime", new a(0)]]),
      defines: /* @__PURE__ */ new Map([["samples", t.samples.toFixed(0)], ["samplesFloat", t.samples.toFixed(0) + ".0"]])
    }), this.pointsIndex = 0, this.makeOptionsReactive(t);
  }
  makeOptionsReactive(e) {
    for (const t of Object.keys(e))
      Object.defineProperty(this, t, {
        get() {
          return e[t];
        },
        set(r) {
          switch (e[t] = r, t) {
            case "intensity":
            case "jitter":
              this.uniforms.get(t).value = r;
              break;
          }
        }
      }), this[t] = e[t];
  }
  initialize(e, ...t) {
    super.initialize(e, ...t), new Ie().load(vr, (r) => {
      r.minFilter = b, r.magFilter = b, r.wrapS = G, r.wrapT = G, r.colorSpace = Fe, this.uniforms.get("blueNoiseTexture").value = r;
    });
  }
  update(e, t, r) {
    this.uniforms.get("inputTexture").value = t.texture, this.uniforms.get("deltaTime").value = Math.max(1 / 1e3, r);
    const n = e.info.render.frame % 65536;
    this.uniforms.get("frame").value = n, this.uniforms.get("texSize").value.set(window.innerWidth, window.innerHeight);
    const i = this.uniforms.get("blueNoiseTexture").value;
    if (i) {
      const {
        width: s,
        height: o
      } = i.source.data;
      this.uniforms.get("blueNoiseSize").value.set(s, o);
    }
  }
};
const uf = (
  /* glsl */
  `
		#ifdef USE_SKINNING
		#ifdef BONE_TEXTURE
			uniform sampler2D prevBoneTexture;
			mat4 getPrevBoneMatrix( const in float i ) {
				float j = i * 4.0;
				float x = mod( j, float( boneTextureSize ) );
				float y = floor( j / float( boneTextureSize ) );
				float dx = 1.0 / float( boneTextureSize );
				float dy = 1.0 / float( boneTextureSize );
				y = dy * ( y + 0.5 );
				vec4 v1 = textureLod( prevBoneTexture, vec2( dx * ( x + 0.5 ), y ), 0. );
				vec4 v2 = textureLod( prevBoneTexture, vec2( dx * ( x + 1.5 ), y ), 0. );
				vec4 v3 = textureLod( prevBoneTexture, vec2( dx * ( x + 2.5 ), y ), 0. );
				vec4 v4 = textureLod( prevBoneTexture, vec2( dx * ( x + 3.5 ), y ), 0. );
				mat4 bone = mat4( v1, v2, v3, v4 );
				return bone;
			}
		#else
			uniform mat4 prevBoneMatrices[ MAX_BONES ];
			mat4 getPrevBoneMatrix( const in float i ) {
				mat4 bone = prevBoneMatrices[ int(i) ];
				return bone;
			}
		#endif
		#endif
`
), ff = (
  /* glsl */
  `
#define MAX_BONES 64
                    
${ue.skinning_pars_vertex}
${uf}

uniform mat4 velocityMatrix;
uniform mat4 prevVelocityMatrix;
varying vec4 prevPosition;
varying vec4 newPosition;

#ifdef renderDepth
varying vec2 vHighPrecisionZW;
#endif
`
), hf = (
  /* glsl */
  `
// Get the current vertex position
transformed = vec3( position );
${ue.skinning_vertex}
newPosition = velocityMatrix * vec4( transformed, 1.0 );

// Get the previous vertex position
transformed = vec3( position );
${ue.skinbase_vertex.replace(/mat4 /g, "").replace(/getBoneMatrix/g, "getPrevBoneMatrix")}
${ue.skinning_vertex.replace(/vec4 /g, "")}
prevPosition = prevVelocityMatrix * vec4( transformed, 1.0 );

gl_Position = newPosition;

#ifdef renderDepth
vHighPrecisionZW = gl_Position.zw;
#endif
`
), df = (
  /* glsl */
  `
varying vec4 prevPosition;
varying vec4 newPosition;

#ifdef renderDepth
varying vec2 vHighPrecisionZW;
#endif
`
), mf = (
  /* glsl */
  `
vec2 pos0 = (prevPosition.xy / prevPosition.w) * 0.5 + 0.5;
vec2 pos1 = (newPosition.xy / newPosition.w) * 0.5 + 0.5;

vec2 vel = pos1 - pos0;

#ifdef renderDepth
float fragCoordZ = 0.5 * vHighPrecisionZW[0] / vHighPrecisionZW[1] + 0.5;
#endif

gl_FragColor = vec4(vel.x, vel.y, 0., 0.);
`
), pf = {
  prevVelocityMatrix: {
    value: new C()
  },
  velocityMatrix: {
    value: new C()
  },
  prevBoneTexture: {
    value: null
  },
  boneTexture: {
    value: null
  },
  normalMap: {
    value: null
  },
  normalScale: {
    value: new g()
  },
  uvTransform: {
    value: new Ot()
  }
};
class vf extends N {
  constructor() {
    super({
      uniforms: oe.clone(pf),
      glslVersion: Te,
      vertexShader: (
        /* glsl */
        `
					#include <common>
					#include <uv_pars_vertex>
					#include <displacementmap_pars_vertex>
					#include <normal_pars_vertex>
					#include <morphtarget_pars_vertex>
					#include <logdepthbuf_pars_vertex>
					#include <clipping_planes_pars_vertex>

					#if defined( FLAT_SHADED ) || defined( USE_BUMPMAP ) || defined( TANGENTSPACE_NORMALMAP )
						varying vec3 vViewPosition;
					#endif
					
                    ${ff}
        
                    void main() {
						vec3 transformed;

						#include <uv_vertex>

						#include <skinbase_vertex>
						#include <beginnormal_vertex>
						#include <skinnormal_vertex>
						#include <defaultnormal_vertex>

						#include <morphnormal_vertex>
						#include <normal_vertex>
						#include <morphtarget_vertex>
						#include <displacementmap_vertex>
						#include <project_vertex>
						#include <logdepthbuf_vertex>
						#include <clipping_planes_vertex>

						${hf}

						#if defined( FLAT_SHADED ) || defined( USE_BUMPMAP ) || defined( TANGENTSPACE_NORMALMAP )
							vViewPosition = - mvPosition.xyz;
						#endif

                    }`
      ),
      fragmentShader: (
        /* glsl */
        `
					#if defined( FLAT_SHADED ) || defined( USE_BUMPMAP ) || defined( TANGENTSPACE_NORMALMAP )
						varying vec3 vViewPosition;
					#endif

					#ifdef renderDepth
					layout(location = 0) out vec4 gDepth;
					layout(location = 1) out vec4 gVelocity;
					#else
					#define gVelocity gl_FragColor
					#endif

					${df}
					#include <packing>

					#include <uv_pars_fragment>
					#include <normal_pars_fragment>

					// source: https://knarkowicz.wordpress.com/2014/04/16/octahedron-normal-vector-encoding/
					vec2 OctWrap( vec2 v ) {
						vec2 w = 1.0 - abs( v.yx );
						if (v.x < 0.0) w.x = -w.x;
						if (v.y < 0.0) w.y = -w.y;
						return w;
					}

					vec2 Encode( vec3 n ) {
						n /= ( abs( n.x ) + abs( n.y ) + abs( n.z ) );
						n.xy = n.z > 0.0 ? n.xy : OctWrap( n.xy );
						n.xy = n.xy * 0.5 + 0.5;
						return n.xy;
					}

                    void main() {
						#include <normal_fragment_begin>
                    	#include <normal_fragment_maps>

						${mf.replaceAll("gl_FragColor", "gVelocity")}
						vec3 worldNormal = normalize((vec4(normal, 0.) * viewMatrix).xyz);
						gVelocity.ba = Encode(worldNormal);

						#ifdef renderDepth
						gDepth = packDepthToRGBA(fragCoordZ);
						#endif
                    }`
      )
    }), this.isVelocityMaterial = !0;
  }
}
const gf = new y(0), xf = new g(), oi = new C(), li = new C();
class es extends X {
  constructor(e, t, r = !0) {
    if (super("velocityDepthNormalPass"), this.cachedMaterials = /* @__PURE__ */ new WeakMap(), this.visibleMeshes = [], this.needsSwap = !1, !(t instanceof ke))
      throw new Error(this.constructor.name + " doesn't support cameras of type '" + t.constructor.name + "' yet. Only cameras of type 'PerspectiveCamera' are supported.");
    this._scene = e, this._camera = t;
    const n = r ? 2 : 1;
    this.renderTarget = new be(1, 1, n, {
      minFilter: b,
      magFilter: b
    }), this.renderTarget.depthTexture = new Oe(1, 1), this.renderTarget.depthTexture.type = Y, r && (this.renderTarget.texture[0].type = se, this.renderTarget.texture[0].needsUpdate = !0, this.renderTarget.texture[1].type = Y, this.renderTarget.texture[1].needsUpdate = !0), this.renderDepth = r;
  }
  setVelocityDepthNormalMaterialInScene() {
    this.visibleMeshes = pr(this._scene);
    for (const t of this.visibleMeshes) {
      const r = t.material;
      let [n, i] = this.cachedMaterials.get(t) || [];
      if (r !== n) {
        var e;
        i = new vf(), _i(r, i), t.material = i, (e = t.skeleton) != null && e.boneTexture && qi(t), this.cachedMaterials.set(t, [r, i]);
      }
      t.material = i, t.visible = ln(t, r), this.renderDepth && (i.defines.renderDepth = "");
      const s = r.map || r.normalMap || r.roughnessMap || r.metalnessMap;
      s && (i.uniforms.uvTransform.value = s.matrix), Mu(t, this._camera);
    }
  }
  unsetVelocityDepthNormalMaterialInScene() {
    for (const e of this.visibleMeshes)
      e.visible = !0, wu(e, this._camera), e.material = this.cachedMaterials.get(e)[0];
  }
  setSize(e, t) {
    let r;
    this.renderTarget.setSize(e, t), (r = this.lastDepthTexture) == null || r.dispose(), this.lastDepthTexture = new Zr(e, t, ne), this.lastDepthTexture.minFilter = b, this.lastDepthTexture.magFilter = b;
  }
  dispose() {
    super.dispose(), this.renderTarget.dispose();
  }
  get texture() {
    return Array.isArray(this.renderTarget.texture) ? this.renderTarget.texture[1] : this.renderTarget.texture;
  }
  get depthTexture() {
    return this.renderTarget.texture[0];
  }
  render(e) {
    oi.copy(this._camera.projectionMatrix), li.copy(this._camera.projectionMatrixInverse), this._camera.view && (this._camera.view.enabled = !1), this._camera.updateProjectionMatrix(), this._camera.updateMatrixWorld(), this.setVelocityDepthNormalMaterialInScene();
    const {
      background: t
    } = this._scene;
    this._scene.background = gf, e.setRenderTarget(this.renderTarget), e.copyFramebufferToTexture(xf, this.lastDepthTexture), e.render(this._scene, this._camera), this._scene.background = t, this.unsetVelocityDepthNormalMaterialInScene(), this._camera.view && (this._camera.view.enabled = !0), this._camera.projectionMatrix.copy(oi), this._camera.projectionMatrixInverse.copy(li);
  }
}
let Sf = class extends es {
  constructor(e, t) {
    super(e, t, !1);
  }
};
var yf = `#define GLSLIFY 1
varying vec2 vUv;uniform sampler2D inputTexture;uniform sampler2D depthTexture;uniform sampler2D normalTexture;uniform mat4 projectionMatrixInverse;uniform mat4 cameraMatrixWorld;uniform float lumaPhi;uniform float depthPhi;uniform float normalPhi;uniform sampler2D blueNoiseTexture;uniform vec2 blueNoiseRepeat;uniform int index;uniform vec2 resolution;
#include <common>
#include <sampleBlueNoise>
vec3 getWorldPos(float depth,vec2 coord){float z=depth*2.0-1.0;vec4 clipSpacePosition=vec4(coord*2.0-1.0,z,1.0);vec4 viewSpacePosition=projectionMatrixInverse*clipSpacePosition;vec4 worldSpacePosition=cameraMatrixWorld*viewSpacePosition;worldSpacePosition.xyz/=worldSpacePosition.w;return worldSpacePosition.xyz;}
#define luminance(a) dot(vec3(0.2125, 0.7154, 0.0721), a)
vec3 getNormal(vec2 uv,vec4 texel){
#ifdef NORMAL_IN_RGB
return texel.rgb;
#else
return normalize(textureLod(normalTexture,uv,0.).xyz*2.0-1.0);
#endif
}float distToPlane(const vec3 worldPos,const vec3 neighborWorldPos,const vec3 worldNormal){vec3 toCurrent=worldPos-neighborWorldPos;float distToPlane=abs(dot(toCurrent,worldNormal));return distToPlane;}void main(){vec4 depthTexel=textureLod(depthTexture,vUv,0.);if(depthTexel.r==1.0||dot(depthTexel.rgb,depthTexel.rgb)==0.){discard;return;}vec4 texel=textureLod(inputTexture,vUv,0.0);vec3 normal=getNormal(vUv,texel);
#ifdef NORMAL_IN_RGB
float denoised=texel.a;float center=texel.a;
#else
vec3 denoised=texel.rgb;float center=texel.rgb;
#endif
float depth=depthTexel.x;vec3 worldPos=getWorldPos(depth,vUv);float totalWeight=1.0;vec4 blueNoise=sampleBlueNoise(blueNoiseTexture,0,blueNoiseRepeat,resolution);float angle=blueNoise[index];float s=sin(angle),c=cos(angle);mat2 rotationMatrix=mat2(c,-s,s,c);for(int i=0;i<samples;i++){vec2 offset=rotationMatrix*poissonDisk[i];vec2 neighborUv=vUv+offset;vec4 neighborTexel=textureLod(inputTexture,neighborUv,0.0);vec3 neighborNormal=getNormal(neighborUv,neighborTexel);float neighborColor=neighborTexel.a;float sampleDepth=textureLod(depthTexture,neighborUv,0.0).x;vec3 worldPosSample=getWorldPos(sampleDepth,neighborUv);float tangentPlaneDist=abs(dot(worldPos-worldPosSample,normal));float normalDiff=dot(normal,neighborNormal);float normalSimilarity=pow(max(normalDiff,0.),normalPhi);
#ifdef NORMAL_IN_RGB
float lumaDiff=abs(neighborColor-center);
#else
float lumaDiff=abs(luminance(neighborColor)-luminance(center));
#endif
float lumaSimilarity=max(1.0-lumaDiff/lumaPhi,0.0);float depthDiff=1.-distToPlane(worldPos,worldPosSample,normal);float depthSimilarity=max(depthDiff/depthPhi,0.);float w=lumaSimilarity*depthSimilarity*normalSimilarity;denoised+=w*neighborColor;totalWeight+=w;}if(totalWeight>0.)denoised/=totalWeight;
#ifdef NORMAL_IN_RGB
gl_FragColor=vec4(normal,denoised);
#else
gl_FragColor=vec4(denoised,1.);
#endif
}`;
function un(c, e, t, r) {
  const n = 2 * Math.PI * e / c, i = 1 / c, s = i, o = [];
  let l = i, u = 0;
  for (let f = 0; f < c; f++) {
    const h = new g(Math.cos(u), Math.sin(u)).multiplyScalar(Math.pow(l, 0.75)).multiply(r).multiplyScalar(t);
    o.push(h), l += s, u += n;
  }
  return o;
}
function fn(c) {
  const e = c.length;
  let t = `const vec2 poissonDisk[samples] = vec2[samples](
`;
  for (let r = 0; r < e; r++) {
    const n = c[r];
    t += `    vec2(${n.x}, ${n.y})`, r < e - 1 && (t += ","), t += `
`;
  }
  return t += ");", t;
}
const ci = yf.replace("#include <sampleBlueNoise>", it), tr = {
  iterations: 1,
  radius: 8,
  rings: 5.625,
  lumaPhi: 10,
  depthPhi: 2,
  normalPhi: 3.25,
  samples: 16,
  normalTexture: null
};
let xr = class extends X {
  constructor(e, t, r, n = tr) {
    super("PoissionBlurPass"), this.iterations = tr.iterations, this.index = 0, n = {
      ...tr,
      ...n
    }, this.inputTexture = t, this.fullscreenMaterial = new N({
      fragmentShader: ci,
      vertexShader: lt,
      uniforms: {
        depthTexture: {
          value: null
        },
        inputTexture: {
          value: null
        },
        projectionMatrixInverse: {
          value: new C()
        },
        cameraMatrixWorld: {
          value: new C()
        },
        lumaPhi: {
          value: 5
        },
        depthPhi: {
          value: 5
        },
        normalPhi: {
          value: 5
        },
        resolution: {
          value: new g()
        },
        blueNoiseTexture: {
          value: null
        },
        index: {
          value: 0
        },
        blueNoiseRepeat: {
          value: new g()
        }
      }
    });
    const i = {
      type: K,
      depthBuffer: !1
    };
    this.renderTargetA = new A(1, 1, i), this.renderTargetB = new A(1, 1, i);
    const {
      uniforms: s
    } = this.fullscreenMaterial;
    s.inputTexture.value = this.inputTexture, s.depthTexture.value = r, s.projectionMatrixInverse.value = e.projectionMatrixInverse, s.cameraMatrixWorld.value = e.matrixWorld, s.depthPhi.value = n.depthPhi, s.normalPhi.value = n.normalPhi, n.normalTexture ? s.normalTexture.value = n.normalTexture : this.fullscreenMaterial.defines.NORMAL_IN_RGB = "";
    for (const o of ["radius", "rings", "samples"])
      Object.defineProperty(this, o, {
        get: () => n[o],
        set: (l) => {
          n[o] = l, this.setSize(this.renderTargetA.width, this.renderTargetA.height);
        }
      });
    new Ie().load(vr, (o) => {
      o.minFilter = b, o.magFilter = b, o.wrapS = G, o.wrapT = G, o.colorSpace = Fe, this.fullscreenMaterial.uniforms.blueNoiseTexture.value = o;
    });
  }
  setSize(e, t) {
    this.renderTargetA.setSize(e, t), this.renderTargetB.setSize(e, t), this.fullscreenMaterial.uniforms.resolution.value.set(e, t);
    const r = un(this.samples, this.rings, this.radius, new g(1 / e, 1 / t)), n = `const int samples = ${this.samples};
`, i = fn(r);
    this.fullscreenMaterial.fragmentShader = n + i + `
` + ci, this.fullscreenMaterial.needsUpdate = !0;
  }
  get texture() {
    return this.renderTargetB.texture;
  }
  render(e) {
    this.fullscreenMaterial.uniforms.index.value = 0;
    const t = this.fullscreenMaterial.uniforms.blueNoiseTexture.value;
    if (t) {
      const {
        width: r,
        height: n
      } = t.source.data;
      this.fullscreenMaterial.uniforms.blueNoiseRepeat.value.set(this.renderTargetA.width / r, this.renderTargetA.height / n);
    }
    for (let r = 0; r < 2 * this.iterations; r++) {
      const n = r % 2 === 0, i = n ? this.renderTargetB : this.renderTargetA;
      this.fullscreenMaterial.uniforms.inputTexture.value = r === 0 ? this.inputTexture : i.texture;
      const s = n ? this.renderTargetA : this.renderTargetB;
      e.setRenderTarget(s), e.render(this.scene, this.camera), this.fullscreenMaterial.uniforms.index.value = (this.fullscreenMaterial.uniforms.index.value + 1) % 4;
    }
  }
};
xr.DefaultOptions = tr;
class ts extends X {
  constructor(e, t, r) {
    super(), this._camera = e, this._scene = t, this.renderTarget = new A(1, 1, {
      type: K,
      depthBuffer: !1
    });
    const n = r.replace("#include <sampleBlueNoise>", it);
    this.fullscreenMaterial = new N({
      fragmentShader: n,
      vertexShader: lt,
      uniforms: {
        depthTexture: {
          value: null
        },
        normalTexture: {
          value: null
        },
        cameraNear: {
          value: 0
        },
        cameraFar: {
          value: 0
        },
        viewMatrix: {
          value: this._camera.matrixWorldInverse
        },
        projectionViewMatrix: {
          value: new C()
        },
        projectionMatrixInverse: {
          value: this._camera.projectionMatrixInverse
        },
        cameraMatrixWorld: {
          value: this._camera.matrixWorld
        },
        texSize: {
          value: new g()
        },
        blueNoiseTexture: {
          value: null
        },
        blueNoiseRepeat: {
          value: new g()
        },
        aoDistance: {
          value: 0
        },
        distancePower: {
          value: 0
        },
        bias: {
          value: 0
        },
        thickness: {
          value: 0
        },
        power: {
          value: 0
        },
        frame: {
          value: 0
        }
      },
      blending: B,
      depthWrite: !1,
      depthTest: !1,
      toneMapped: !1
    }), new Ie().load(vr, (i) => {
      i.minFilter = b, i.magFilter = b, i.wrapS = G, i.wrapT = G, i.colorSpace = Fe, this.fullscreenMaterial.uniforms.blueNoiseTexture.value = i;
    });
  }
  get texture() {
    return this.renderTarget.texture;
  }
  setSize(e, t) {
    this.renderTarget.setSize(e, t), this.fullscreenMaterial.uniforms.texSize.value.set(this.renderTarget.width, this.renderTarget.height);
  }
  render(e) {
    const t = +this.fullscreenMaterial.defines.spp;
    this.fullscreenMaterial.uniforms.frame.value = (this.fullscreenMaterial.uniforms.frame.value + t) % 65536, this.fullscreenMaterial.uniforms.cameraNear.value = this._camera.near, this.fullscreenMaterial.uniforms.cameraFar.value = this._camera.far, this.fullscreenMaterial.uniforms.projectionViewMatrix.value.multiplyMatrices(this._camera.projectionMatrix, this._camera.matrixWorldInverse);
    const r = this.fullscreenMaterial.uniforms.blueNoiseTexture.value;
    if (r) {
      const {
        width: n,
        height: i
      } = r.source.data;
      this.fullscreenMaterial.uniforms.blueNoiseRepeat.value.set(this.renderTarget.width / n, this.renderTarget.height / i);
    }
    e.setRenderTarget(this.renderTarget), e.render(this.scene, this.camera);
  }
}
var Mf = `#define GLSLIFY 1
#include <sampleBlueNoise>
uniform sampler2D normalTexture;uniform float cameraNear;uniform float cameraFar;uniform mat4 projectionMatrixInverse;uniform mat4 cameraMatrixWorld;float getViewZ(const float depth){
#ifdef PERSPECTIVE_CAMERA
return perspectiveDepthToViewZ(depth,cameraNear,cameraFar);
#else
return orthographicDepthToViewZ(depth,cameraNear,cameraFar);
#endif
}vec3 getWorldPos(const float depth,const vec2 coord){float z=depth*2.0-1.0;vec4 clipSpacePosition=vec4(coord*2.0-1.0,z,1.0);vec4 viewSpacePosition=projectionMatrixInverse*clipSpacePosition;vec4 worldSpacePosition=cameraMatrixWorld*viewSpacePosition;worldSpacePosition.xyz/=worldSpacePosition.w;return worldSpacePosition.xyz;}vec3 slerp(const vec3 a,const vec3 b,const float t){float cosAngle=dot(a,b);float angle=acos(cosAngle);if(abs(angle)<0.001){return mix(a,b,t);}float sinAngle=sin(angle);float t1=sin((1.0-t)*angle)/sinAngle;float t2=sin(t*angle)/sinAngle;return(a*t1)+(b*t2);}vec3 computeWorldNormal(){vec2 size=vec2(textureSize(depthTexture,0));ivec2 p=ivec2(vUv*size);float c0=texelFetch(depthTexture,p,0).x;float l2=texelFetch(depthTexture,p-ivec2(2,0),0).x;float l1=texelFetch(depthTexture,p-ivec2(1,0),0).x;float r1=texelFetch(depthTexture,p+ivec2(1,0),0).x;float r2=texelFetch(depthTexture,p+ivec2(2,0),0).x;float b2=texelFetch(depthTexture,p-ivec2(0,2),0).x;float b1=texelFetch(depthTexture,p-ivec2(0,1),0).x;float t1=texelFetch(depthTexture,p+ivec2(0,1),0).x;float t2=texelFetch(depthTexture,p+ivec2(0,2),0).x;float dl=abs((2.0*l1-l2)-c0);float dr=abs((2.0*r1-r2)-c0);float db=abs((2.0*b1-b2)-c0);float dt=abs((2.0*t1-t2)-c0);vec3 ce=getWorldPos(c0,vUv).xyz;vec3 dpdx=(dl<dr)? ce-getWorldPos(l1,(vUv-vec2(1.0/size.x,0.0))).xyz:-ce+getWorldPos(r1,(vUv+vec2(1.0/size.x,0.0))).xyz;vec3 dpdy=(db<dt)? ce-getWorldPos(b1,(vUv-vec2(0.0,1.0/size.y))).xyz:-ce+getWorldPos(t1,(vUv+vec2(0.0,1.0/size.y))).xyz;return normalize(cross(dpdx,dpdy));}vec3 getWorldNormal(const vec2 uv){
#ifdef useNormalTexture
vec3 worldNormal=unpackRGBToNormal(textureLod(normalTexture,uv,0.).rgb);worldNormal=(vec4(worldNormal,1.)*viewMatrix).xyz;return normalize(worldNormal);
#else
return computeWorldNormal();
#endif
}
#define PI 3.14159265358979323846264338327950288
vec3 cosineSampleHemisphere(const vec3 n,const vec2 u){float r=sqrt(u.x);float theta=2.0*PI*u.y;vec3 b=normalize(cross(n,vec3(0.0,1.0,1.0)));vec3 t=cross(b,n);return normalize(r*sin(theta)*b+sqrt(1.0-u.x)*n+r*cos(theta)*t);}`, wf = `#define GLSLIFY 1
varying vec2 vUv;uniform sampler2D depthTexture;uniform mat4 projectionViewMatrix;uniform int frame;uniform sampler2D blueNoiseTexture;uniform vec2 blueNoiseRepeat;uniform vec2 texSize;uniform float aoDistance;uniform float distancePower;uniform float bias;uniform float thickness;
#include <packing>
#include <hbao_utils>
float getOcclusion(const vec3 cameraPosition,const vec3 worldPos,const vec3 worldNormal,const float depth,const int seed,inout float totalWeight){vec4 blueNoise=sampleBlueNoise(blueNoiseTexture,seed,blueNoiseRepeat,texSize);vec3 sampleWorldDir=cosineSampleHemisphere(worldNormal,blueNoise.rg);vec3 sampleWorldPos=worldPos+aoDistance*pow(blueNoise.b,distancePower+1.0)*sampleWorldDir;vec4 sampleUv=projectionViewMatrix*vec4(sampleWorldPos,1.);sampleUv.xy/=sampleUv.w;sampleUv.xy=sampleUv.xy*0.5+0.5;float sampleDepth=textureLod(depthTexture,sampleUv.xy,0.0).r;float deltaDepth=depth-sampleDepth;float d=distance(sampleWorldPos,cameraPosition);deltaDepth*=0.001*d*d;float th=thickness*0.01;float theta=dot(worldNormal,sampleWorldDir);totalWeight+=theta;if(deltaDepth<th){float horizon=sampleDepth+deltaDepth*bias*1000.;float occlusion=max(0.0,horizon-depth)*theta;float m=max(0.,1.-deltaDepth/th);occlusion=10.*occlusion*m/d;occlusion=sqrt(occlusion);return occlusion;}return 0.;}void main(){float depth=textureLod(depthTexture,vUv,0.0).r;if(depth==1.0){discard;return;}vec4 cameraPosition=cameraMatrixWorld*vec4(0.0,0.0,0.0,1.0);vec3 worldPos=getWorldPos(depth,vUv);vec3 worldNormal=getWorldNormal(vUv);float ao=0.0,totalWeight=0.0;for(int i=0;i<spp;i++){int seed=i;
#ifdef animatedNoise
seed+=frame;
#endif
float occlusion=getOcclusion(cameraPosition.xyz,worldPos,worldNormal,depth,seed,totalWeight);ao+=occlusion;}if(totalWeight>0.)ao/=totalWeight;ao=clamp(1.-ao,0.,1.);gl_FragColor=vec4(worldNormal,ao);}`;
const Tf = wf.replace("#include <hbao_utils>", Mf);
class bf extends ts {
  constructor(e, t) {
    super(e, t, Tf);
  }
}
var rs = `#define GLSLIFY 1
uniform sampler2D inputTexture;uniform sampler2D depthTexture;uniform float power;uniform vec3 color;void mainImage(const in vec4 inputColor,const in vec2 uv,out vec4 outputColor){float unpackedDepth=textureLod(depthTexture,uv,0.).r;float ao=unpackedDepth>0.9999 ? 1.0 : textureLod(inputTexture,uv,0.0).a;ao=pow(ao,power);vec3 aoColor=mix(color,vec3(1.),ao);aoColor*=inputColor.rgb;outputColor=vec4(aoColor,inputColor.a);}`;
const fr = {
  resolutionScale: 1,
  spp: 8,
  distance: 2,
  distancePower: 1,
  power: 2,
  bias: 40,
  thickness: 0.075,
  color: new y("black"),
  useNormalPass: !1,
  velocityDepthNormalPass: null,
  normalTexture: null,
  ...xr.DefaultOptions
};
let qe = class extends I {
  constructor(e, t, r, n, i = fr) {
    if (super("AOEffect", rs, {
      type: "FinalAOMaterial",
      uniforms: /* @__PURE__ */ new Map([["inputTexture", new a(null)], ["depthTexture", new a(null)], ["power", new a(0)], ["color", new a(new y("black"))]])
    }), this.lastSize = {
      width: 0,
      height: 0,
      resolutionScale: 0
    }, this.composer = e, this.aoPass = n, i = {
      ...fr,
      ...i
    }, this.options = i, this.scene = r, e.depthTexture || e.createDepthTexture(), this.aoPass.fullscreenMaterial.uniforms.depthTexture.value = e.depthTexture, this.uniforms.get("depthTexture").value = e.depthTexture, i.useNormalPass || i.normalTexture) {
      let s;
      i.useNormalPass && (this.normalPass = new mr(r, t));
      const o = (s = i.normalTexture) !== null && s !== void 0 ? s : this.normalPass.texture;
      this.aoPass.fullscreenMaterial.uniforms.normalTexture.value = o, this.aoPass.fullscreenMaterial.defines.useNormalTexture = "";
    }
    this.poissionDenoisePass = new xr(t, this.aoPass.texture, e.depthTexture), this.makeOptionsReactive(i);
  }
  makeOptionsReactive(e) {
    for (const t of Object.keys(e))
      Object.defineProperty(this, t, {
        get() {
          return e[t];
        },
        set(r) {
          if (r != null)
            switch (e[t] = r, t) {
              case "spp":
                this.aoPass.fullscreenMaterial.defines.spp = r.toFixed(0), this.aoPass.fullscreenMaterial.needsUpdate = !0;
                break;
              case "distance":
                this.aoPass.fullscreenMaterial.uniforms.aoDistance.value = r;
                break;
              case "resolutionScale":
                this.setSize(this.lastSize.width, this.lastSize.height);
                break;
              case "power":
                this.uniforms.get("power").value = r;
                break;
              case "color":
                this.uniforms.get("color").value.copy(new y(r));
                break;
              // denoiser
              case "iterations":
              case "radius":
              case "rings":
              case "samples":
                this.poissionDenoisePass[t] = r;
                break;
              case "lumaPhi":
              case "depthPhi":
              case "normalPhi":
                this.poissionDenoisePass.fullscreenMaterial.uniforms[t].value = Math.max(r, 1e-4);
                break;
              default:
                t in this.aoPass.fullscreenMaterial.uniforms && (this.aoPass.fullscreenMaterial.uniforms[t].value = r);
            }
        },
        configurable: !0
      }), this[t] = e[t];
  }
  setSize(e, t) {
    let r;
    e === void 0 || t === void 0 || e === this.lastSize.width && t === this.lastSize.height && this.resolutionScale === this.lastSize.resolutionScale || ((r = this.normalPass) == null || r.setSize(e, t), this.aoPass.setSize(e * this.resolutionScale, t * this.resolutionScale), this.poissionDenoisePass.setSize(e, t), this.lastSize = {
      width: e,
      height: t,
      resolutionScale: this.resolutionScale
    });
  }
  update(e, t, r) {
    let n;
    this.options?.renderBefore?.(this.scene);
    const i = this.composer.passes.some((s) => {
      let o;
      return s.enabled && !s.skipRendering && ((o = s.effects) == null ? void 0 : o.some((l) => l instanceof Ht));
    });
    i && !("animatedNoise" in this.aoPass.fullscreenMaterial.defines) ? (this.aoPass.fullscreenMaterial.defines.animatedNoise = "", this.aoPass.fullscreenMaterial.needsUpdate = !0) : !i && "animatedNoise" in this.aoPass.fullscreenMaterial.defines && (delete this.aoPass.fullscreenMaterial.defines.animatedNoise, this.aoPass.fullscreenMaterial.needsUpdate = !0), this.iterations > 0 ? this.uniforms.get("inputTexture").value = this.poissionDenoisePass.texture : this.uniforms.get("inputTexture").value = this.aoPass.texture, (n = this.normalPass) == null || n.render(e), this.aoPass.render(e), this.poissionDenoisePass.render(e);
  }
};
qe.DefaultOptions = fr;
class hn extends qe {
  constructor(e, t, r, n = qe.DefaultOptions) {
    const i = new bf(t, r);
    n = {
      ...qe.DefaultOptions,
      ...hn.DefaultOptions,
      ...n
    }, super(e, t, r, i, n), this.lastSize = {
      width: 0,
      height: 0,
      resolutionScale: 0
    }, n = {
      ...qe.DefaultOptions,
      ...n
    };
  }
}
var Df = `#define GLSLIFY 1
varying vec2 vUv;uniform sampler2D depthTexture;uniform sampler2D normalTexture;uniform mat4 projectionViewMatrix;uniform mat4 cameraMatrixWorld;uniform sampler2D blueNoiseTexture;uniform vec2 blueNoiseRepeat;uniform vec2 texSize;uniform mat4 projectionMatrixInverse;uniform float aoDistance;uniform float distancePower;uniform float cameraNear;uniform float cameraFar;uniform int frame;uniform vec3[spp]samples;uniform float[spp]samplesR;
#include <common>
#include <packing>
#include <sampleBlueNoise>
vec3 getWorldPos(const float depth,const vec2 coord){float z=depth*2.0-1.0;vec4 clipSpacePosition=vec4(coord*2.0-1.0,z,1.0);vec4 viewSpacePosition=projectionMatrixInverse*clipSpacePosition;vec4 worldSpacePosition=cameraMatrixWorld*viewSpacePosition;worldSpacePosition.xyz/=worldSpacePosition.w;return worldSpacePosition.xyz;}vec3 computeNormal(vec3 worldPos,vec2 vUv){vec2 size=vec2(textureSize(depthTexture,0));ivec2 p=ivec2(vUv*size);float c0=texelFetch(depthTexture,p,0).x;float l2=texelFetch(depthTexture,p-ivec2(2,0),0).x;float l1=texelFetch(depthTexture,p-ivec2(1,0),0).x;float r1=texelFetch(depthTexture,p+ivec2(1,0),0).x;float r2=texelFetch(depthTexture,p+ivec2(2,0),0).x;float b2=texelFetch(depthTexture,p-ivec2(0,2),0).x;float b1=texelFetch(depthTexture,p-ivec2(0,1),0).x;float t1=texelFetch(depthTexture,p+ivec2(0,1),0).x;float t2=texelFetch(depthTexture,p+ivec2(0,2),0).x;float dl=abs((2.0*l1-l2)-c0);float dr=abs((2.0*r1-r2)-c0);float db=abs((2.0*b1-b2)-c0);float dt=abs((2.0*t1-t2)-c0);vec3 ce=getWorldPos(c0,vUv).xyz;vec3 dpdx=(dl<dr)? ce-getWorldPos(l1,(vUv-vec2(1.0/size.x,0.0))).xyz:-ce+getWorldPos(r1,(vUv+vec2(1.0/size.x,0.0))).xyz;vec3 dpdy=(db<dt)? ce-getWorldPos(b1,(vUv-vec2(0.0,1.0/size.y))).xyz:-ce+getWorldPos(t1,(vUv+vec2(0.0,1.0/size.y))).xyz;return normalize(cross(dpdx,dpdy));}highp float linearize_depth(highp float d,highp float zNear,highp float zFar){highp float z_n=2.0*d-1.0;return 2.0*zNear*zFar/(zFar+zNear-z_n*(zFar-zNear));}void main(){float depth=textureLod(depthTexture,vUv,0.).x;if(depth==1.0){discard;return;}vec3 worldPos=getWorldPos(depth,vUv);vec3 normal=computeNormal(worldPos,vUv);
#ifdef animatedNoise
int seed=frame;
#else
int seed=0;
#endif
vec4 noise=sampleBlueNoise(blueNoiseTexture,seed,blueNoiseRepeat,texSize);vec3 randomVec=normalize(noise.rgb*2.0-1.0);vec3 tangent=normalize(randomVec-normal*dot(randomVec,normal));vec3 bitangent=cross(normal,tangent);mat3 tbn=mat3(tangent,bitangent,normal);float occluded=0.0;float totalWeight=0.0;vec3 samplePos;float sppF=float(spp);for(float i=0.0;i<sppF;i++){vec3 sampleDirection=tbn*samples[int(i)];if(dot(sampleDirection,normal)<0.0)sampleDirection*=-1.0;float moveAmt=samplesR[int(mod(i+noise.a*sppF,sppF))];samplePos=worldPos+aoDistance*moveAmt*sampleDirection;vec4 offset=projectionViewMatrix*vec4(samplePos,1.0);offset.xyz/=offset.w;offset.xyz=offset.xyz*0.5+0.5;float sampleDepth=textureLod(depthTexture,offset.xy,0.0).x;float distSample=linearize_depth(sampleDepth,cameraNear,cameraFar);float distWorld=linearize_depth(offset.z,cameraNear,cameraFar);float rangeCheck=smoothstep(0.0,1.0,aoDistance/(aoDistance*abs(distSample-distWorld)));rangeCheck=pow(rangeCheck,distancePower);float weight=dot(sampleDirection,normal);occluded+=rangeCheck*weight*(distSample<distWorld ? 1.0 : 0.0);totalWeight+=weight;}float occ=clamp(1.0-occluded/totalWeight,0.0,1.0);gl_FragColor=vec4(normal,occ);}`;
class Sr extends ts {
  constructor(e, t) {
    super(e, t, Df);
  }
}
function Pf(c) {
  const e = [], t = Math.PI * (3 - Math.sqrt(5)), r = 2 / c;
  for (let n = 0; n < c; n++) {
    const i = n * r - 1 + r / 2, s = Math.sqrt(1 - i * i), o = n * t;
    e.push(new T(Math.cos(o) * s, i, Math.sin(o) * s));
  }
  return e;
}
let Cf = class Hr extends qe {
  constructor(e, t, r, n = {}) {
    Hr.DefaultOptions = {
      ...qe.DefaultOptions,
      spp: 16,
      distance: 1,
      distancePower: 0.25,
      power: 2
    }, n = {
      ...Hr.DefaultOptions,
      ...n
    };
    const i = new Sr(t, r);
    super(e, t, r, i, n);
  }
  makeOptionsReactive(e) {
    super.makeOptionsReactive(e);
    for (const t of ["spp"])
      Object.defineProperty(this, t, {
        get() {
          return e[t];
        },
        set(r) {
          if (r != null)
            switch (e[t] = r, t) {
              case "spp":
                this.aoPass.fullscreenMaterial.defines.spp = r.toFixed(0);
                const n = Pf(r), i = [];
                for (let s = 0; s < r; s++)
                  i.push((s + 1) / r);
                this.aoPass.fullscreenMaterial.uniforms.samples = {
                  value: n
                }, this.aoPass.fullscreenMaterial.uniforms.samplesR = {
                  value: i
                }, this.aoPass.fullscreenMaterial.needsUpdate = !0;
                break;
            }
        },
        configurable: !0
      });
    this.spp = e.spp;
  }
};
const $d = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  AOEffect: qe,
  HBAOEffect: hn,
  MotionBlurEffect: cf,
  PoissionDenoisePass: xr,
  SSAOEffect: Cf,
  SSAOPass: Sr,
  SSDGIEffect: of,
  SSGIEffect: gr,
  SSREffect: af,
  SVGF: $i,
  TRAAEffect: Ht,
  TemporalReprojectPass: cn,
  VelocityDepthNormalPass: es,
  VelocityPass: Sf,
  ao_compose: rs,
  defaultAOOptions: fr,
  generateDenoiseSamples: un,
  generatePoissonDiskConstant: fn,
  sampleBlueNoise: it
}, Symbol.toStringTag, { value: "Module" })), Af = `#define GLSLIFY 1\r
varying vec2 vUv;\r
\r
void main() {\r
    vUv = position.xy * 0.5 + 0.5;\r
    gl_Position = vec4(position.xy, 1.0, 1.0);\r
} `, ui = `#define GLSLIFY 1\r
\r
// 忽略超亮物体的阈值\r
#define IGNORE_BRIGHTNESS_THRESHOLD 20.0\r
\r
varying vec2 vUv;\r
uniform sampler2D inputTexture;\r
uniform sampler2D depthTexture;\r
uniform sampler2D normalTexture;\r
uniform mat4 projectionMatrixInverse;\r
uniform mat4 cameraMatrixWorld;\r
uniform float lumaPhi;\r
uniform float depthPhi;\r
uniform float normalPhi;\r
uniform sampler2D blueNoiseTexture;\r
uniform vec2 blueNoiseRepeat;\r
uniform int index;\r
uniform vec2 resolution;\r
\r
#include <common>\r
#include <sampleBlueNoise>\r
\r
vec3 getWorldPos(float depth, vec2 coord) {\r
    float z = depth * 2.0 - 1.0;\r
    vec4 clipSpacePosition = vec4(coord * 2.0 - 1.0, z, 1.0);\r
    vec4 viewSpacePosition = projectionMatrixInverse * clipSpacePosition;\r
    vec4 worldSpacePosition = cameraMatrixWorld * viewSpacePosition;\r
    worldSpacePosition.xyz /= worldSpacePosition.w;\r
    return worldSpacePosition.xyz;\r
}\r
\r
#define luminance(a) dot(vec3(0.2125, 0.7154, 0.0721), a)\r
\r
vec3 getNormal(vec2 uv, vec4 texel) {\r
#ifdef NORMAL_IN_RGB\r
    return texel.rgb;\r
#else\r
    return normalize(textureLod(normalTexture, uv, 0.).xyz * 2.0 - 1.0);\r
#endif\r
}\r
\r
float distToPlane(const vec3 worldPos, const vec3 neighborWorldPos, const vec3 worldNormal) {\r
    vec3 toCurrent = worldPos - neighborWorldPos;\r
    float distToPlane = abs(dot(toCurrent, worldNormal));\r
    return distToPlane;\r
}\r
\r
void main() {\r
    // 首先读取输入纹理样本，检查是否为高亮标记物体\r
    vec4 inputSample = texture2D(inputTexture, vUv);\r
    float brightness = max(max(inputSample.r, inputSample.g), inputSample.b);\r
    \r
    // 如果检测到被标记为忽略AO的超亮物体，则保留原样\r
    if (brightness > IGNORE_BRIGHTNESS_THRESHOLD) {\r
        gl_FragColor = inputSample;\r
        return;\r
    }\r
\r
    vec4 depthTexel = textureLod(depthTexture, vUv, 0.);\r
    if (depthTexel.r == 1.0 || dot(depthTexel.rgb, depthTexel.rgb) == 0.) {\r
        discard;\r
        return;\r
    }\r
    \r
    vec4 texel = textureLod(inputTexture, vUv, 0.0);\r
    vec3 normal = getNormal(vUv, texel);\r
    \r
#ifdef NORMAL_IN_RGB\r
    float denoised = texel.a;\r
    float center = texel.a;\r
#else\r
    vec3 denoised = texel.rgb;\r
    float center = texel.rgb;\r
#endif\r
\r
    float depth = depthTexel.x;\r
    vec3 worldPos = getWorldPos(depth, vUv);\r
    float totalWeight = 1.0;\r
    \r
    vec4 blueNoise = sampleBlueNoise(blueNoiseTexture, 0, blueNoiseRepeat, resolution);\r
    float angle = blueNoise[index];\r
    float s = sin(angle), c = cos(angle);\r
    mat2 rotationMatrix = mat2(c, -s, s, c);\r
    \r
    for (int i = 0; i < samples; i++) {\r
        vec2 offset = rotationMatrix * poissonDisk[i];\r
        vec2 neighborUv = vUv + offset;\r
        \r
        // 检查邻居像素是否是高亮物体\r
        vec4 neighborInputSample = texture2D(inputTexture, neighborUv);\r
        float neighborBrightness = max(max(neighborInputSample.r, neighborInputSample.g), neighborInputSample.b);\r
        if (neighborBrightness > IGNORE_BRIGHTNESS_THRESHOLD) {\r
            continue; // 跳过高亮像素\r
        }\r
        \r
        vec4 neighborTexel = textureLod(inputTexture, neighborUv, 0.0);\r
        vec3 neighborNormal = getNormal(neighborUv, neighborTexel);\r
        float neighborColor = neighborTexel.a;\r
        float sampleDepth = textureLod(depthTexture, neighborUv, 0.0).x;\r
        vec3 worldPosSample = getWorldPos(sampleDepth, neighborUv);\r
        float tangentPlaneDist = abs(dot(worldPos - worldPosSample, normal));\r
        float normalDiff = dot(normal, neighborNormal);\r
        float normalSimilarity = pow(max(normalDiff, 0.), normalPhi);\r
        \r
#ifdef NORMAL_IN_RGB\r
        float lumaDiff = abs(neighborColor - center);\r
#else\r
        float lumaDiff = abs(luminance(neighborColor) - luminance(center));\r
#endif\r
\r
        float lumaSimilarity = max(1.0 - lumaDiff / lumaPhi, 0.0);\r
        float depthDiff = 1. - distToPlane(worldPos, worldPosSample, normal);\r
        float depthSimilarity = max(depthDiff / depthPhi, 0.);\r
        float w = lumaSimilarity * depthSimilarity * normalSimilarity;\r
        denoised += w * neighborColor;\r
        totalWeight += w;\r
    }\r
    \r
    if (totalWeight > 0.) denoised /= totalWeight;\r
    \r
#ifdef NORMAL_IN_RGB\r
    gl_FragColor = vec4(normal, denoised);\r
#else\r
    gl_FragColor = vec4(denoised, 1.);\r
#endif\r
} `, rr = {
  iterations: 1,
  radius: 8,
  rings: 5.625,
  lumaPhi: 10,
  depthPhi: 2,
  normalPhi: 3.25,
  samples: 16,
  normalTexture: null
};
class Gt extends X {
  constructor(e, t, r, n = rr) {
    super("PoissionBlurPass"), this.iterations = rr.iterations, this.index = 0, n = {
      ...rr,
      ...n
    }, this.inputTexture = t;
    const i = ui.replace("#include <sampleBlueNoise>", it);
    this.fullscreenMaterial = new N({
      fragmentShader: i,
      vertexShader: Af,
      uniforms: {
        depthTexture: {
          value: null
        },
        inputTexture: {
          value: null
        },
        projectionMatrixInverse: {
          value: new C()
        },
        cameraMatrixWorld: {
          value: new C()
        },
        lumaPhi: {
          value: 5
        },
        depthPhi: {
          value: 5
        },
        normalPhi: {
          value: 5
        },
        resolution: {
          value: new g()
        },
        blueNoiseTexture: {
          value: null
        },
        index: {
          value: 0
        },
        blueNoiseRepeat: {
          value: new g()
        }
      }
    }), this.fullscreenMaterial.fragmentShader.includes("IGNORE_BRIGHTNESS_THRESHOLD");
    const s = {
      type: K,
      depthBuffer: !1
    };
    this.renderTargetA = new A(1, 1, s), this.renderTargetB = new A(1, 1, s);
    const {
      uniforms: o
    } = this.fullscreenMaterial;
    o.inputTexture.value = this.inputTexture, o.depthTexture.value = r, o.projectionMatrixInverse.value = e.projectionMatrixInverse, o.cameraMatrixWorld.value = e.matrixWorld, o.depthPhi.value = n.depthPhi, o.normalPhi.value = n.normalPhi, n.normalTexture ? o.normalTexture.value = n.normalTexture : this.fullscreenMaterial.defines.NORMAL_IN_RGB = "";
    for (const l of ["radius", "rings", "samples"])
      Object.defineProperty(this, l, {
        get: () => n[l],
        set: (u) => {
          n[l] = u, this.setSize(this.renderTargetA.width, this.renderTargetA.height);
        }
      });
    new Ie().load(wt, (l) => {
      l.minFilter = b, l.magFilter = b, l.wrapS = G, l.wrapT = G, l.colorSpace = Fe, this.fullscreenMaterial.uniforms.blueNoiseTexture.value = l;
    });
  }
  setSize(e, t) {
    this.renderTargetA.setSize(e, t), this.renderTargetB.setSize(e, t), this.fullscreenMaterial.uniforms.resolution.value.set(e, t);
    const r = un(this.samples, this.rings, this.radius, new g(1 / e, 1 / t)), n = `const int samples = ${this.samples};
`, i = fn(r);
    let s = ui;
    s.includes("#include <sampleBlueNoise>") && (s = s.replace("#include <sampleBlueNoise>", it)), this.fullscreenMaterial.fragmentShader = n + i + `
` + s, this.fullscreenMaterial.needsUpdate = !0, this.fullscreenMaterial.fragmentShader.includes("IGNORE_BRIGHTNESS_THRESHOLD") || console.warn("警告: 在setSize后，降噪着色器中未找到亮度阈值定义");
  }
  get texture() {
    return this.renderTargetB.texture;
  }
  render(e) {
    this.fullscreenMaterial.uniforms.index.value = 0;
    const t = this.fullscreenMaterial.uniforms.blueNoiseTexture.value;
    if (t) {
      const {
        width: r,
        height: n
      } = t.source.data;
      this.fullscreenMaterial.uniforms.blueNoiseRepeat.value.set(this.renderTargetA.width / r, this.renderTargetA.height / n);
    }
    for (let r = 0; r < 2 * this.iterations; r++) {
      const n = r % 2 === 0, i = n ? this.renderTargetB : this.renderTargetA;
      this.fullscreenMaterial.uniforms.inputTexture.value = r === 0 ? this.inputTexture : i.texture;
      const s = n ? this.renderTargetA : this.renderTargetB;
      e.setRenderTarget(s), e.clear(!0, !0, !0), e.render(this.scene, this.camera), this.fullscreenMaterial.uniforms.index.value = (this.fullscreenMaterial.uniforms.index.value + 1) % 4;
    }
  }
}
Gt.DefaultOptions = rr;
const Ef = `uniform sampler2D inputTexture;
uniform highp sampler2D depthTexture;
uniform float power;
uniform vec3 color;

void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
  float unpackedDepth = textureLod(depthTexture, uv, 0.).r;

  float ao = unpackedDepth > 0.9999 ? 1.0 : textureLod(inputTexture, uv, 0.0).a;
  ao = pow(ao, power);

  vec3 aoColor = mix(color, vec3(1.), ao);

  aoColor *= inputColor.rgb;

  outputColor = vec4(aoColor, inputColor.a);
}`, Gr = {
  resolutionScale: 1,
  spp: 8,
  distance: 2,
  distancePower: 1,
  power: 2,
  bias: 40,
  thickness: 0.075,
  color: new y("black"),
  useNormalPass: !1,
  velocityDepthNormalPass: null,
  normalTexture: null,
  ...Gt.DefaultOptions
};
class yt extends I {
  constructor(e, t, r, n, i = Gr) {
    if (super("AOEffect", Ef, {
      type: "FinalAOMaterial",
      uniforms: /* @__PURE__ */ new Map([["inputTexture", new a(null)], ["depthTexture", new a(null)], ["power", new a(0)], ["color", new a(new y("black"))]])
    }), this.lastSize = {
      width: 0,
      height: 0,
      resolutionScale: 0
    }, this.composer = e, this.aoPass = n, i = {
      ...Gr,
      ...i
    }, this.options = i, this.scene = r, e.depthTexture || e.createDepthTexture(), this.aoPass.fullscreenMaterial.uniforms.depthTexture.value = e.depthTexture, this.uniforms.get("depthTexture").value = e.depthTexture, i.useNormalPass || i.normalTexture) {
      let s;
      i.useNormalPass && (this.normalPass = new mr(r, t));
      const o = (s = i.normalTexture) !== null && s !== void 0 ? s : this.normalPass.texture;
      this.aoPass.fullscreenMaterial.uniforms.normalTexture.value = o, this.aoPass.fullscreenMaterial.defines.useNormalTexture = "";
    }
    this.poissionDenoisePass = new Gt(t, this.aoPass.texture, e.depthTexture), this.makeOptionsReactive(i);
  }
  makeOptionsReactive(e) {
    for (const t of Object.keys(e))
      Object.defineProperty(this, t, {
        get() {
          return e[t];
        },
        set(r) {
          if (r != null)
            switch (e[t] = r, t) {
              case "spp":
                this.aoPass.fullscreenMaterial.defines.spp = r.toFixed(0), this.aoPass.fullscreenMaterial.needsUpdate = !0;
                break;
              case "distance":
                this.aoPass.fullscreenMaterial.uniforms.aoDistance.value = r;
                break;
              case "resolutionScale":
                this.setSize(this.lastSize.width, this.lastSize.height);
                break;
              case "power":
                this.uniforms.get("power").value = r;
                break;
              case "color":
                this.uniforms.get("color").value.copy(new y(r));
                break;
              // denoiser
              case "iterations":
              case "radius":
              case "rings":
              case "samples":
                this.poissionDenoisePass[t] = r;
                break;
              case "lumaPhi":
              case "depthPhi":
              case "normalPhi":
                this.poissionDenoisePass.fullscreenMaterial.uniforms[t].value = Math.max(r, 1e-4);
                break;
              default:
                t in this.aoPass.fullscreenMaterial.uniforms && (this.aoPass.fullscreenMaterial.uniforms[t].value = r);
            }
        },
        configurable: !0
      }), this[t] = e[t];
  }
  setSize(e, t) {
    let r;
    e === void 0 || t === void 0 || e === this.lastSize.width && t === this.lastSize.height && this.resolutionScale === this.lastSize.resolutionScale || ((r = this.normalPass) == null || r.setSize(e, t), this.aoPass.setSize(e * this.resolutionScale, t * this.resolutionScale), this.poissionDenoisePass.setSize(e, t), this.lastSize = {
      width: e,
      height: t,
      resolutionScale: this.resolutionScale
    });
  }
  update(e, t, r) {
    let n;
    this.options?.renderBefore?.(this.scene);
    const i = this.composer.passes.some((s) => {
      let o;
      return s.enabled && !s.skipRendering && ((o = s.effects) == null ? void 0 : o.some((l) => l instanceof Ht));
    });
    i && !("animatedNoise" in this.aoPass.fullscreenMaterial.defines) ? (this.aoPass.fullscreenMaterial.defines.animatedNoise = "", this.aoPass.fullscreenMaterial.needsUpdate = !0) : !i && "animatedNoise" in this.aoPass.fullscreenMaterial.defines && (delete this.aoPass.fullscreenMaterial.defines.animatedNoise, this.aoPass.fullscreenMaterial.needsUpdate = !0), this.iterations > 0 ? this.uniforms.get("inputTexture").value = this.poissionDenoisePass.texture : this.uniforms.get("inputTexture").value = this.aoPass.texture, (n = this.normalPass) == null || n.render(e), this.aoPass.render(e), this.poissionDenoisePass.render(e);
  }
}
yt.DefaultOptions = Gr;
function Rf(c) {
  const e = [], t = Math.PI * (3 - Math.sqrt(5)), r = 2 / c;
  for (let n = 0; n < c; n++) {
    const i = n * r - 1 + r / 2, s = Math.sqrt(1 - i * i), o = n * t;
    e.push(new T(Math.cos(o) * s, i, Math.sin(o) * s));
  }
  return e;
}
class Wr extends yt {
  constructor(e, t, r, n = {}) {
    Wr.DefaultOptions = {
      ...yt.DefaultOptions,
      spp: 16,
      distance: 1,
      distancePower: 0.25,
      power: 2
    }, n = {
      ...Wr.DefaultOptions,
      ...n
    };
    const i = new Sr(t, r);
    super(e, t, r, i, n);
  }
  makeOptionsReactive(e) {
    super.makeOptionsReactive(e);
    for (const t of ["spp"])
      Object.defineProperty(this, t, {
        get() {
          return e[t];
        },
        set(r) {
          if (r != null)
            switch (e[t] = r, t) {
              case "spp":
                this.aoPass.fullscreenMaterial.defines.spp = r.toFixed(0);
                const n = Rf(r), i = [];
                for (let s = 0; s < r; s++)
                  i.push((s + 1) / r);
                this.aoPass.fullscreenMaterial.uniforms.samples = {
                  value: n
                }, this.aoPass.fullscreenMaterial.uniforms.samplesR = {
                  value: i
                }, this.aoPass.fullscreenMaterial.needsUpdate = !0;
                break;
            }
        },
        configurable: !0
      });
    this.spp = e.spp;
  }
}
const Nf = `uniform sampler2D inputTexture;  // AO的纹理\r
uniform sampler2D inputBuffer;   // 原始场景的颜色纹理\r
uniform sampler2D maskPass;   // 原始场景的颜色纹理\r
\r
uniform highp sampler2D depthPass1;\r
uniform highp sampler2D depthTexture;\r
uniform sampler2D maskTexture;\r
uniform float power;\r
uniform vec3 color;\r
uniform float brightnessThreshold; // 亮度阈值，超过此值将减少AO效果\r
uniform float maskThreshold;       // 遮罩阈值，低于此值应用AO效果\r
uniform int debugMode; // 调试模式: 0=正常, 1=显示亮度\r
\r
// 深度可视化参数\r
uniform float depthNear;\r
uniform float depthFar;\r
\r
// 忽略超亮物体的阈值\r
#define IGNORE_BRIGHTNESS_THRESHOLD 20.0\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
  // 读取原始场景颜色\r
  vec4 sceneColor = texture2D(inputBuffer, uv);\r
  \r
  // 读取深度值\r
  float depth = texture2D(depthTexture, uv).r;\r
  float linearDepth = (2.0 * depthNear) / (depthFar + depthNear - depth * (depthFar - depthNear));\r
  \r
  // 读取遮罩值\r
  float maskValue = texture2D(maskTexture, uv).r;\r
  \r
  // 根据调试模式显示不同信息\r
  if (debugMode == 1) {\r
    // 亮度可视化\r
    float brightness = max(max(sceneColor.r, sceneColor.g), sceneColor.b);\r
    outputColor = vec4(vec3(brightness), 1.0);\r
    return;\r
  } else if (debugMode == 2) {\r
    // AO强度可视化\r
    vec4 aoSample = texture2D(inputTexture, uv);\r
    outputColor = vec4(vec3(aoSample.a), 1.0);\r
    return;\r
  } else if (debugMode == 3) {\r
    // AO值可视化\r
    vec4 aoSample = texture2D(inputTexture, uv);\r
    outputColor = vec4(vec3(pow(aoSample.a, power)), 1.0);\r
    return;\r
  } else if (debugMode == 4) {\r
    // 遮罩可视化 - 增强对比度使黑色区域更明显\r
    float depthGradient = length(vec2(dFdx(linearDepth), dFdy(linearDepth)));\r
    float isAoArea = maskValue <= maskThreshold ? 0.0 : 1.0; // 黑色区域(会应用AO)为0，其他区域为1\r
    outputColor = vec4(vec3(maskValue<0.000001?0.0:1.0), 1.0);\r
    return;\r
  } else if (debugMode == 5) {\r
    // 深度(灰度)\r
    outputColor = vec4(vec3(linearDepth), 1.0);\r
    return;\r
  } else if (debugMode == 6) {\r
    // 深度(彩色)\r
    float normalizedDepth = linearDepth * 2.0 - 1.0;\r
    vec3 depthColor = vec3(\r
      smoothstep(-1.0, 0.0, normalizedDepth),\r
      smoothstep(0.0, 1.0, normalizedDepth),\r
      smoothstep(1.0, 2.0, normalizedDepth)\r
    );\r
    outputColor = vec4(depthColor, 1.0);\r
    return;\r
  } else if (debugMode == 7) {\r
    // 遮罩和深度对比\r
    vec3 depthColor = vec3(linearDepth);\r
    vec3 maskColor = vec3(maskValue);\r
    outputColor = vec4(mix(depthColor, maskColor, 0.5), 1.0);\r
    return;\r
  }\r
  \r
  // 正常渲染模式\r
  float ao = 1.0;\r
  if (depth < 1.0) {\r
    vec4 aoSample = texture2D(inputTexture, uv);\r
    ao = aoSample.a;\r
    ao = pow(ao, power);\r
  }\r
  \r
  // 根据亮度调整AO强度\r
  vec3 colorForBrightness = sceneColor.rgb;\r
  float perceptualBrightness = dot(colorForBrightness, vec3(0.299, 0.587, 0.114));\r
  \r
  // 亮度阈值处理 - 使用更陡峭的曲线以增强对比度\r
  float aoStrength = clamp(1.0 - pow(perceptualBrightness / max(brightnessThreshold, 0.001), 0.5), 0.0, 1.0);\r
  ao = mix(1.0, ao, aoStrength);\r
  \r
  // 遮罩处理 - 只在遮罩黑色区域应用AO\r
  // 使用遮罩阈值来确定黑色区域\r
  if (maskValue < 0.000001){\r
    // 非黑色区域 - 不应用AO效果\r
    //ao = 1.0;\r
    outputColor =inputColor;\r
    return;\r
  }\r
  \r
  // 最终合成\r
  vec3 aoColor = mix(color, vec3(1.0), ao);\r
  aoColor *= inputColor.rgb;\r
  outputColor = vec4(aoColor, inputColor.a);\r
}`, fi = {
  resolutionScale: 1,
  spp: 8,
  distance: 2,
  distancePower: 1,
  power: 4,
  bias: 40,
  thickness: 0.075,
  color: new y("black"),
  brightnessThreshold: 0.7,
  maskThreshold: 0.01,
  // 添加遮罩阈值参数
  debugMode: 0,
  ignoreSelection: null,
  highlightValue: 1e3,
  closeAutoUpdate: !1,
  useNormalPass: !1,
  velocityDepthNormalPass: null,
  normalTexture: null,
  // 添加多采样相关参数
  useMultisampling: !0,
  // 是否启用多采样
  samplingCount: 9,
  // 采样数量
  samplingRadius: 2,
  // 采样半径
  samplingThreshold: 0.5,
  // 采样匹配阈值
  renderBefore: () => {
  },
  ...Gt.DefaultOptions
};
class Rr extends I {
  constructor(e, t, r, n, i = fi) {
    if (i = {
      ...fi,
      ...i
    }, super("AOEffect", Nf, {
      type: "FinalAOMaterial",
      uniforms: /* @__PURE__ */ new Map([
        ["inputTexture", new a(null)],
        ["inputBuffer", new a(null)],
        ["depthTexture", new a(null)],
        ["maskTexture", new a(null)],
        ["power", new a(0)],
        ["color", new a(new y("black"))],
        ["brightnessThreshold", new a(0.7)],
        ["maskThreshold", new a(0.01)],
        // 添加遮罩阈值统一变量
        ["debugMode", new a(0)],
        ["depthNear", new a(0.1)],
        ["depthFar", new a(1e3)]
      ])
    }), this.lastSize = {
      width: 0,
      height: 0,
      resolutionScale: 0
    }, this.composer = e, this.aoPass = n, this.options = i, this.scene = r, this.camera = t, this._ignoreSelection = i.ignoreSelection || new Se(), this.renderTargetMask = new A(1, 1, {
      minFilter: F,
      magFilter: F,
      type: K,
      depthBuffer: !0
    }), this.renderTargetMask.texture.name = "AO.Mask", this.uniforms.get("maskTexture").value = this.renderTargetMask.texture, this.originalMaterials = /* @__PURE__ */ new Map(), this.createRenderTargets(), this.renderPass = new pe(r, t), this.renderPass.clear = !0, this.depthPass = new Me(r, t), i.useMultisampling ? this.depthMaskMaterial = new Ve() : this.depthMaskMaterial = new _e(), this.depthMaskMaterial.copyCameraSettings(t), this.depthMaskMaterial.depthBuffer0 = e.depthTexture, this.depthMaskMaterial.depthPacking0 = $, this.depthMaskMaterial.depthBuffer1 = this.depthPass.texture, this.depthMaskMaterial.depthPacking1 = ce, this.depthMaskMaterial.depthMode = Wt.EqualDepth, this.depthMaskMaterial.epsilon = 9e-6, i.useMultisampling && this.depthMaskMaterial instanceof Ve && (this.depthMaskMaterial.samplingCount = i.samplingCount || 9, this.depthMaskMaterial.samplingRadius = i.samplingRadius || 2, this.depthMaskMaterial.samplingThreshold = i.samplingThreshold || 0.5), this.maskPass = new ee(this.depthMaskMaterial), this.maskPass.clear = !0, this.clearPass = new De(!0, !1, !1), this.clearPass.overrideClearColor = new y(0), this.clearPass.overrideClearAlpha = 1, this._inverted = !1, this._ignoreBackground = !1, this.originalEmissives = /* @__PURE__ */ new Map(), e.depthTexture || e.createDepthTexture(), this.aoPass.fullscreenMaterial.uniforms.depthTexture.value = e.depthTexture, this.uniforms.get("depthTexture").value = e.depthTexture, i.useNormalPass || i.normalTexture) {
      i.useNormalPass && (this.normalPass = new mr(r, t));
      const s = i.normalTexture || this.normalPass.texture;
      this.aoPass.fullscreenMaterial.uniforms.normalTexture.value = s, this.aoPass.fullscreenMaterial.defines.useNormalTexture = "";
    }
    if (this.poissionDenoisePass = new Gt(t, this.aoPass.texture, e.depthTexture), this.poissionDenoisePass) {
      if (!this.poissionDenoisePass.renderTarget)
        try {
          this.poissionDenoisePass.setSize(1, 1);
        } catch (l) {
          console.error("初始化PoissionDenoisePass渲染目标失败:", l);
        }
    } else
      console.error("降噪通道(PoissionDenoisePass)创建失败");
    this.makeOptionsReactive(i), this.initializeSelectionLayer();
  }
  // 初始化Selection对象，移除图层依赖
  initializeSelectionLayer() {
    this._ignoreSelection && (!this._ignoreSelection.add && typeof this._ignoreSelection.add != "function" && console.warn("Selection对象缺少add方法，可能无法正常添加对象"), this.getSelectionItems().length);
  }
  // 创建渲染目标
  createRenderTargets() {
    this.renderTargetAO = new A(1, 1, {
      minFilter: F,
      magFilter: F,
      type: K,
      depthBuffer: !0,
      depthTexture: new Oe()
      // 关键：添加深度纹理
    }), this.renderTargetAO.texture.name = "AO.Target", this.renderTargetAO.depthTexture.name = "AO.Depth";
  }
  makeOptionsReactive(e) {
    for (const t of Object.keys(e))
      Object.defineProperty(this, t, {
        get() {
          return t === "ignoreSelection" ? this._ignoreSelection : e[t];
        },
        set(r) {
          if (r != null)
            switch (e[t] = r, t) {
              case "spp":
                this.aoPass.fullscreenMaterial.defines.spp = r.toFixed(0), this.aoPass.fullscreenMaterial.needsUpdate = !0;
                break;
              case "distance":
                this.aoPass.fullscreenMaterial.uniforms.aoDistance.value = r;
                break;
              case "resolutionScale":
                this.setSize(this.lastSize.width, this.lastSize.height);
                break;
              case "power":
                this.uniforms.get("power").value = r;
                break;
              case "color":
                this.uniforms.get("color").value.copy(new y(r));
                break;
              case "brightnessThreshold":
                this.uniforms.get("brightnessThreshold").value = r;
                break;
              case "debugMode":
                this.uniforms.get("debugMode").value = r;
                break;
              // 处理closeAutoUpdate变化
              case "closeAutoUpdate":
                break;
              case "ignoreSelection":
                this._ignoreSelection = r || new Se();
                break;
              case "highlightValue":
                break;
              // 降噪参数
              case "iterations":
              case "radius":
              case "rings":
              case "samples":
                this.poissionDenoisePass[t] = r;
                break;
              case "lumaPhi":
              case "depthPhi":
              case "normalPhi":
                this.poissionDenoisePass.fullscreenMaterial.uniforms[t].value = Math.max(r, 1e-4);
                break;
              case "useMultisampling":
                if (r && !(this.depthMaskMaterial instanceof Ve)) {
                  const n = this.depthMaskMaterial;
                  this.depthMaskMaterial = new Ve(), this.depthMaskMaterial.copyCameraSettings(this.camera), this.depthMaskMaterial.depthBuffer0 = n.uniforms.depthBuffer0.value, this.depthMaskMaterial.depthPacking0 = Number(n.defines.DEPTH_PACKING_0), this.depthMaskMaterial.depthBuffer1 = n.uniforms.depthBuffer1.value, this.depthMaskMaterial.depthPacking1 = Number(n.defines.DEPTH_PACKING_1), this.depthMaskMaterial.depthMode = n.depthMode, this.depthMaskMaterial.epsilon = n.epsilon, this.depthMaskMaterial.samplingCount = this.samplingCount, this.depthMaskMaterial.samplingRadius = this.samplingRadius, this.depthMaskMaterial.samplingThreshold = this.samplingThreshold, this.maskPass.fullscreenMaterial = this.depthMaskMaterial;
                } else if (!r && this.depthMaskMaterial instanceof Ve) {
                  const n = this.depthMaskMaterial;
                  this.depthMaskMaterial = new _e(), this.depthMaskMaterial.copyCameraSettings(this.camera), this.depthMaskMaterial.depthBuffer0 = n.uniforms.depthBuffer0.value, this.depthMaskMaterial.depthPacking0 = Number(n.defines.DEPTH_PACKING_0), this.depthMaskMaterial.depthBuffer1 = n.uniforms.depthBuffer1.value, this.depthMaskMaterial.depthPacking1 = Number(n.defines.DEPTH_PACKING_1), this.depthMaskMaterial.depthMode = n.depthMode, this.depthMaskMaterial.epsilon = n.epsilon, this.maskPass.fullscreenMaterial = this.depthMaskMaterial;
                }
                break;
              case "samplingCount":
                this.depthMaskMaterial instanceof Ve && (this.depthMaskMaterial.samplingCount = r);
                break;
              case "samplingRadius":
                this.depthMaskMaterial instanceof Ve && (this.depthMaskMaterial.samplingRadius = r);
                break;
              case "samplingThreshold":
                this.depthMaskMaterial instanceof Ve && (this.depthMaskMaterial.samplingThreshold = r);
                break;
              default:
                t in this.aoPass.fullscreenMaterial.uniforms && (this.aoPass.fullscreenMaterial.uniforms[t].value = r);
            }
        },
        configurable: !0
      }), this[t] = e[t];
  }
  setSize(e, t) {
    if (!(e === void 0 || t === void 0) && !(e === this.lastSize.width && t === this.lastSize.height && this.resolutionScale === this.lastSize.resolutionScale)) {
      if (this.normalPass && this.normalPass.setSize(e, t), this.aoPass && this.aoPass.setSize(e * this.resolutionScale, t * this.resolutionScale), this.renderTargetMask && this.renderTargetMask.setSize(e * this.resolutionScale, t * this.resolutionScale), this.poissionDenoisePass)
        try {
          this.poissionDenoisePass.setSize(e, t), this.poissionDenoisePass.renderTarget;
        } catch (r) {
          console.error("设置PoissionDenoisePass大小时出错:", r);
        }
      this.renderTargetAO && this.renderTargetAO.setSize(e * this.resolutionScale, t * this.resolutionScale), this.depthPass && this.depthPass.setSize(e, t), this.maskPass && this.maskPass.setSize(e, t), this.lastSize = {
        width: e,
        height: t,
        resolutionScale: this.resolutionScale
      };
    }
  }
  // 获取Selection中的对象
  getSelectionItems() {
    return this._ignoreSelection ? Array.isArray(this._ignoreSelection.items) ? this._ignoreSelection.items : Array.isArray(this._ignoreSelection.objects) ? this._ignoreSelection.objects : typeof this._ignoreSelection.getItems == "function" ? this._ignoreSelection.getItems() : typeof this._ignoreSelection.getSelection == "function" ? this._ignoreSelection.getSelection() : typeof this._ignoreSelection[Symbol.iterator] == "function" ? Array.from(this._ignoreSelection) : (console.warn("无法确定Selection API的使用方式"), []) : [];
  }
  // 准备场景，设置对象图层
  prepareScene() {
    this.originalMaterials = /* @__PURE__ */ new Map(), this.ignoreMaterial || (this.ignoreMaterial = {
      originalMaterialEnabled: !0,
      emissive: new y(1e3, 1e3, 1e3)
      // 超亮颜色，便于在着色器中识别
    }), this.getSelectionItems().forEach((t) => {
      t && t.isMesh && (this.originalMaterials.set(t.uuid, {
        material: t.material,
        emissive: t.material.emissive ? t.material.emissive.clone() : null
      }), Array.isArray(t.material) ? t.material.forEach((r) => {
        r.emissive && (r._originalEmissive = r.emissive.clone(), r.emissive.set(2e3, 2e3, 2e3));
      }) : t.material.emissive && (t.material._originalEmissive = t.material.emissive.clone(), t.material.emissive.set(2e3, 2e3, 2e3)));
    });
  }
  // 恢复场景原始状态
  restoreScene() {
    if (this.originalMaterials) {
      for (const [e, t] of this.originalMaterials.entries()) {
        const r = this.scene.getObjectByProperty("uuid", e);
        r && (Array.isArray(r.material) ? r.material.forEach((n) => {
          n._originalEmissive && (n.emissive.copy(n._originalEmissive), delete n._originalEmissive);
        }) : r.material._originalEmissive && (r.material.emissive.copy(r.material._originalEmissive), delete r.material._originalEmissive));
      }
      this.originalMaterials.clear();
    }
  }
  update(e, t, r, n, i, s) {
    if (this.depthMaskMaterial.copyCameraSettings(this.camera), this.depthMaskMaterial instanceof Ve) {
      const p = e.getSize(new Wt.Vector2());
      if (this.depthMaskMaterial.setResolution(p.x, p.y), this.camera) {
        const d = new Wt.Vector3(0, 0, -1);
        d.applyQuaternion(this.camera.quaternion);
        const v = new Wt.Vector3(0, 1, 0);
        Math.abs(d.dot(v)) < 0.3 ? (this.depthMaskMaterial.samplingCount = Math.max(9, this.samplingCount), this.depthMaskMaterial.samplingRadius = Math.max(2, this.samplingRadius)) : (this.depthMaskMaterial.samplingCount = this.samplingCount, this.depthMaskMaterial.samplingRadius = this.samplingRadius);
      }
    }
    this.aoPass.fullscreenMaterial.uniforms.inputBuffer || (this.aoPass.fullscreenMaterial.uniforms.inputBuffer = {
      value: null
    }), this.aoPass.fullscreenMaterial.uniforms.inputBuffer.value = t.texture, this.camera && (this.uniforms.get("depthNear").value = this.camera.near, this.uniforms.get("depthFar").value = this.camera.far), this.options?.renderBefore?.(this.scene);
    const o = this.composer.passes.some((p) => {
      const d = p.effects;
      return p.enabled && !p.skipRendering && d && d.some((v) => v instanceof Ht);
    });
    this.aoPass.fullscreenMaterial.needsUpdate = !0, o && !("animatedNoise" in this.aoPass.fullscreenMaterial.defines) ? (this.aoPass.fullscreenMaterial.defines.animatedNoise = "", this.aoPass.fullscreenMaterial.needsUpdate = !0) : !o && "animatedNoise" in this.aoPass.fullscreenMaterial.defines && delete this.aoPass.fullscreenMaterial.defines.animatedNoise;
    const l = e.getRenderTarget(), u = this.scene.background, f = this.camera.layers.mask;
    this.scene.background = null;
    const h = this._ignoreSelection, m = [];
    try {
      this.clearAllRenderTargets(e), this.depthMaskMaterial.depthBuffer0 = this.composer.depthTexture, this.depthMaskMaterial.inputBuffer = t.texture, this.camera.layers.set(h.layer), h.forEach((p) => {
        p.children.length > 0 && p.traverse((d) => {
          d.isMesh && !d.layers.isEnabled(h.layer) && d.visible && (m.push({
            model: d,
            oldLayerMask: d.layers.mask
            // 保存原始的完整层掩码
          }), d.layers.set(h.layer));
        });
      }), this.depthPass.render(e, t, void 0, void 0, void 0, void 0, {
        projectObject: !0,
        updateMatrixWorld: !1,
        useProgramCache: !1
      }), this.aoPass.fullscreenMaterial.uniforms.depthPass1 = new a(this.depthPass.renderTarget.texture), this.camera.layers.mask = f, m.forEach(({ model: p, oldLayerMask: d }) => {
        p.layers.mask = d;
      }), e.setRenderTarget(this.renderTargetMask), e.clear(!0, !0, !0), this.maskPass.render(e, t, this.renderTargetMask, void 0, void 0, {
        projectObject: !0,
        updateMatrixWorld: !1,
        useProgramCache: !1
      }), this.scene.background = u, this.aoPass.fullscreenMaterial.uniforms.maskTexture = {
        value: this.renderTargetMask.texture
      }, e.setRenderTarget(this.aoPass.renderTarget), e.clear(!0, !0, !0), this.aoPass.render(e), e.setRenderTarget(this.poissionDenoisePass.renderTarget), this.poissionDenoisePass.render(e), this.uniforms.get("inputTexture").value = this.poissionDenoisePass.texture, this.uniforms.get("inputBuffer").value = t.texture;
    } catch (p) {
      console.error("渲染AO效果时出错:", p);
    } finally {
      e.setRenderTarget(l);
    }
  }
  // 清理所有渲染目标
  clearAllRenderTargets(e) {
    const t = e.getRenderTarget();
    try {
      const r = [
        this.renderTargetAO,
        this.renderTargetMask,
        this.aoPass?.renderTarget,
        this.poissionDenoisePass?.renderTarget
      ];
      for (const n of r)
        n && (e.setRenderTarget(n), e.clear(!0, !0, !0));
    } catch (r) {
      console.error("清理渲染目标时出错:", r);
    } finally {
      e.setRenderTarget(t);
    }
  }
  /**
   * 初始化效果
   *
   * @param {WebGLRenderer} renderer - 渲染器
   * @param {Boolean} alpha - 是否有alpha通道
   * @param {Number} frameBufferType - 帧缓冲区类型
   */
  initialize(e, t, r) {
    this.depthPass.initialize(e, t, r), this.maskPass.initialize(e, t, r), this.aoPass.initialize(e, t, r), this.poissionDenoisePass && this.poissionDenoisePass.initialize(e, t, r), e.capabilities.logarithmicDepthBuffer && this.depthMaskMaterial && (this.depthMaskMaterial.defines.LOG_DEPTH = "1", this.depthMaskMaterial.needsUpdate = !0);
  }
  // 存储原始发光值的方法
  storeOriginalEmissive(e) {
    if (!this.originalEmissives.has(e.uuid) && e.material) {
      if (e.material.emissive)
        this.originalEmissives.set(e.uuid, e.material.emissive.clone());
      else if (Array.isArray(e.material)) {
        const t = [];
        e.material.forEach((r) => {
          r.emissive ? t.push(r.emissive.clone()) : t.push(null);
        }), this.originalEmissives.set(e.uuid, t);
      }
    }
  }
  // 设置对象高亮的方法
  setObjectHighlight(e, t) {
    if (this.storeOriginalEmissive(e), e.material) {
      if (e.material.emissive)
        if (t)
          e.material.emissive.set(
            this.highlightValue,
            this.highlightValue,
            this.highlightValue
          );
        else {
          const r = this.originalEmissives.get(e.uuid);
          r && e.material.emissive.copy(r);
        }
      else if (Array.isArray(e.material)) {
        const r = this.originalEmissives.get(e.uuid);
        e.material.forEach((n, i) => {
          n.emissive && (t ? n.emissive.set(
            this.highlightValue,
            this.highlightValue,
            this.highlightValue
          ) : r && r[i] && n.emissive.copy(r[i]));
        });
      }
    }
  }
  // 添加一个对象到忽略列表
  addToIgnoreList(e) {
    e && (this._ignoreSelection && typeof this._ignoreSelection.add == "function" ? (this._ignoreSelection.add(e), console.log(`已添加对象到AO忽略列表: ${e.name || e.uuid}`), this.setChanged()) : console.warn("无法添加对象到忽略列表，Selection对象不可用"));
  }
  // 从忽略列表中移除对象
  removeFromIgnoreList(e) {
    e && (this._ignoreSelection && typeof this._ignoreSelection.delete == "function" ? (this._ignoreSelection.delete(e), console.log(`已从AO忽略列表移除对象: ${e.name || e.uuid}`), this.setChanged()) : console.warn("无法从忽略列表移除对象，Selection对象不可用"));
  }
  // 清空忽略列表
  clearIgnoreList() {
    this._ignoreSelection && typeof this._ignoreSelection.clear == "function" ? (this._ignoreSelection.clear(), console.log("已清空AO忽略列表"), this.setChanged()) : console.warn("无法清空忽略列表，Selection对象不可用");
  }
  // 触发效果更新
  forceUpdate() {
    this.setChanged(), this.composer && typeof this.composer.render == "function" && requestAnimationFrame(() => {
      this.composer.render();
    });
  }
  // 设置调试模式的方法
  setDebugMode(e) {
    e >= 0 && e <= 7 ? (this.debugMode = e, console.log(`AO调试模式已切换到: ${this.getDebugModeName(e)}`)) : console.warn(`无效的调试模式: ${e}，有效范围: 0-7`);
  }
  // 获取调试模式名称
  getDebugModeName(e) {
    return [
      "正常",
      "亮度",
      "AO强度",
      "AO值",
      "遮罩",
      "深度(灰度)",
      "深度(彩色)",
      "遮罩和深度对比"
    ][e] || "未知";
  }
  // 循环切换调试模式
  cycleDebugMode() {
    const e = (this.debugMode + 1) % 8;
    return this.setDebugMode(e), e;
  }
  // 显示所有调试模式
  listDebugModes() {
    console.log("可用的AO调试模式:");
    for (let e = 0; e < 8; e++)
      console.log(`${e}: ${this.getDebugModeName(e)}`);
  }
  /**
   * 指示遮罩是否应该反转（反转选择物体的AO效果）
   *
   * @type {Boolean}
   */
  get inverted() {
    return this._inverted;
  }
  set inverted(e) {
    this._inverted = e, this.depthMaskMaterial.depthMode = e ? Mt : Ue;
  }
  /**
   * 指示是否忽略背景（不对背景应用AO效果）
   *
   * @type {Boolean}
   */
  get ignoreBackground() {
    return this._ignoreBackground;
  }
  set ignoreBackground(e) {
    this._ignoreBackground = e, this.depthMaskMaterial.maxDepthStrategy = e ? we.DISCARD_MAX_DEPTH : we.KEEP_MAX_DEPTH;
  }
}
function zf(c) {
  const e = [], t = Math.PI * (3 - Math.sqrt(5)), r = 2 / c;
  for (let n = 0; n < c; n++) {
    const i = n * r - 1 + r / 2, s = Math.sqrt(1 - i * i), o = n * t;
    e.push(new T(Math.cos(o) * s, i, Math.sin(o) * s));
  }
  return e;
}
class ns extends Rr {
  constructor(e, t, r, n = {}) {
    Rr.DefaultOptions = {
      ...Rr.DefaultOptions,
      spp: 16,
      distance: 1,
      distancePower: 0.25,
      power: 2
    }, n = {
      ...ns.DefaultOptions,
      ...n
    };
    const i = new Sr(t, r);
    super(e, t, r, i, n);
  }
  makeOptionsReactive(e) {
    super.makeOptionsReactive(e);
    for (const t of ["spp"])
      Object.defineProperty(this, t, {
        get() {
          return e[t];
        },
        set(r) {
          if (r != null)
            switch (e[t] = r, t) {
              case "spp":
                this.aoPass.fullscreenMaterial.defines.spp = r.toFixed(0);
                const n = zf(r), i = [];
                for (let s = 0; s < r; s++)
                  i.push((s + 1) / r);
                this.aoPass.fullscreenMaterial.uniforms.samples = {
                  value: n
                }, this.aoPass.fullscreenMaterial.uniforms.samplesR = {
                  value: i
                }, this.aoPass.fullscreenMaterial.needsUpdate = !0;
                break;
            }
        },
        configurable: !0
      });
    this.spp = e.spp;
  }
}
const Ff = `uniform sampler2D inputTexture;\r
uniform highp sampler2D velocityTexture;\r
uniform vec2 resolution;\r
uniform float intensity;\r
uniform float jitter;\r
\r
uniform float deltaTime;\r
uniform int frame;\r
uniform vec2 texSize;\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
  vec2 velocity = textureLod(velocityTexture, vUv, 0.0).xy;\r
  bool didMove = dot(velocity, velocity) > 0.000000001;\r
\r
  if (!didMove) {\r
    outputColor = inputColor;\r
    return;\r
  }\r
\r
  velocity *= intensity;\r
\r
  vec4 blueNoise = blueNoise(vUv, frame);\r
  vec2 jitterOffset = jitter * velocity * blueNoise.xy;\r
  float frameSpeed = (1. / 100.) / deltaTime;\r
\r
  // UVs will be centered around the target pixel (see\r
  // http://john-chapman-graphics.blogspot.com/2013/01/per-object-motion-blur.html)\r
  vec2 startUv = vUv + (jitterOffset - velocity * 0.5) * frameSpeed;\r
  vec2 endUv = vUv + (jitterOffset + velocity * 0.5) * frameSpeed;\r
\r
  startUv = max(vec2(0.), startUv);\r
  endUv = min(vec2(1.), endUv);\r
\r
  vec3 motionBlurredColor = inputColor.rgb;\r
  for (float i = 0.0; i <= samplesFloat; i++) {\r
    vec2 reprojectedUv = mix(startUv, endUv, i / samplesFloat);\r
    vec3 neighborColor = textureLod(inputTexture, reprojectedUv, 0.0).rgb;\r
\r
    motionBlurredColor += neighborColor;\r
  }\r
\r
  motionBlurredColor /= samplesFloat + 2.;\r
\r
  outputColor = vec4(motionBlurredColor, inputColor.a);\r
}`, hi = { intensity: 1, jitter: 1, samples: 16 };
class em extends I {
  pointsIndex = 0;
  constructor(e, t = hi) {
    t = { ...hi, ...t };
    const { fragmentShader: r, uniforms: n } = $r(Ff), i = [];
    for (const s of Object.keys(n))
      i.push([s, n[s]]);
    super("MotionBlurEffect", r, {
      type: "MotionBlurMaterial",
      uniforms: new Map([
        ...i,
        ["inputTexture", new a(null)],
        ["velocityTexture", new a(e.texture)],
        ["resolution", new a(new g())],
        ["intensity", new a(1)],
        ["jitter", new a(1)],
        ["frame", new a(0)],
        ["deltaTime", new a(0)]
      ]),
      defines: /* @__PURE__ */ new Map([
        ["samples", t.samples.toFixed(0)],
        ["samplesFloat", t.samples.toFixed(0) + ".0"]
      ])
    }), this.makeOptionsReactive(t);
  }
  makeOptionsReactive(e) {
    for (const t of Object.keys(e))
      Object.defineProperty(this, t, {
        get() {
          return e[t];
        },
        set(r) {
          switch (e[t] = r, t) {
            case "intensity":
            case "jitter":
              this.uniforms.get(t).value = r;
              break;
          }
        }
      }), this[t] = e[t];
  }
  initialize(e, ...t) {
    super.initialize(e, ...t), new Ie().load(wt, (r) => {
      r.minFilter = b, r.magFilter = b, r.wrapS = G, r.wrapT = G, r.colorSpace = Fe, this.uniforms.get("blueNoiseTexture").value = r;
    });
  }
  update(e, t, r) {
    this.uniforms.get("inputTexture").value = t.texture, this.uniforms.get("deltaTime").value = Math.max(1 / 1e3, r);
    const n = e.info.render.frame % 4096;
    this.uniforms.get("frame").value = n, this.uniforms.get("resolution").value.set(window.innerWidth, window.innerHeight);
    const i = this.uniforms.get("blueNoiseTexture").value;
    if (i) {
      const { width: s, height: o } = i.source.data;
      this.uniforms.get("blueNoiseSize").value.set(s, o);
    }
  }
}
class tm extends sn {
  constructor(e, t) {
    super(e, t, !1);
  }
}
const If = (
  /* glsl */
  `
    uniform sampler2D inputTexture;
    // uniform sampler2D chessboardTexture;
    uniform vec2 resolution;

    uniform float alphax;
    uniform float alphay;
    uniform float aberration;

    void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
        // source: https://marcodiiga.github.io/radial-lens-undistortion-filtering
        float x = (2.0 * vUv.x - 1.0) / 1.0;
        float y = (2.0 * vUv.y - 1.0) / 1.0;
        
        // Calculate l2 norm
        float r = x*x + y*y;
        
        // Calculate the deflated or inflated new coordinate (reverse transform)
        float x3 = x / (1.0 - alphax * r);
        float y3 = y / (1.0 - alphay * r); 
        float x2 = x / (1.0 - alphax * (x3 * x3 + y3 * y3));
        float y2 = y / (1.0 - alphay * (x3 * x3 + y3 * y3));

        // De-normalize to the original range
        float i2 = (x2 + 1.0) * 1.0 / 2.0;
        float j2 = (y2 + 1.0) * 1.0 / 2.0;

        vec2 duv = vec2(i2, j2);

        // source: https://stackoverflow.com/questions/9841863/reflection-refraction-with-chromatic-aberration-eye-correction
        vec2 rOffset = vec2(1.0 / resolution.x, 0.0);
        vec2 gOffset = vec2(0.0, 1.0 / resolution.y);
        vec2 bOffset = vec2(1.0 / resolution.x, 1.0 / resolution.y);

        vec4 rValue = texture2D(inputTexture, duv - aberration * rOffset);  
        vec4 gValue = texture2D(inputTexture, duv - aberration * gOffset);
        vec4 bValue = texture2D(inputTexture, duv - aberration * bOffset); 

        outputColor = vec4(rValue.r, gValue.g, bValue.b, 1.0);
    }
`
);
class rm extends I {
  constructor({ alphax: e = -0.05, alphay: t = -0.05, aberration: r = 1 } = {}) {
    super("LensDistortionEffect", If, {
      uniforms: /* @__PURE__ */ new Map([
        ["inputTexture", { value: null }],
        // ["chessboardTexture", { value: chessboardTexture }],
        ["resolution", { value: new g() }],
        ["alphax", { value: e }],
        ["alphay", { value: t }],
        ["aberration", { value: r }]
      ])
    });
  }
  update(e, t) {
    this.uniforms.get("inputTexture").value = t.texture, this.uniforms.get("resolution").value.set(t.width, t.height);
  }
  setAlphaX(e) {
    this.uniforms.get("alphax").value = e;
  }
  setAlphaY(e) {
    this.uniforms.get("alphay").value = e;
  }
}
const Bf = (
  /* glsl */
  `
uniform sampler2D inputTexture;
uniform float sharpness;

void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
    vec4 blurredPixel = texture(inputTexture, uv - 1.0 * texelSize);
    blurredPixel += texture(inputTexture, uv + vec2(0.0, -1.0) * texelSize);
    blurredPixel += texture(inputTexture, uv + vec2(1.0, -1.0) * texelSize);
    blurredPixel += texture(inputTexture, uv + vec2(-1.0, 0.0) * texelSize);
    blurredPixel += inputColor;
    blurredPixel += texture(inputTexture, uv + vec2(1.0, 0.0) * texelSize);
    blurredPixel += texture(inputTexture, uv + vec2(-1.0, 1.0) * texelSize);
    blurredPixel += texture(inputTexture, uv + vec2(0.0, 1.0) * texelSize);
    blurredPixel += texture(inputTexture, uv + 1.0 * texelSize);
    blurredPixel /= 9.0;

    // Calculate the sharpness difference
    vec4 sharpDiff = inputColor - blurredPixel;

    // Apply the sharpness effect by adding the difference scaled by the sharpness value
    vec4 sharpenedPixel = inputColor + sharpDiff * sharpness;

	// set minimun to 0 as otherwise we get severe edge artifacts on Mac Sonoma Firefox v120 for example
	sharpenedPixel.rgb = max(sharpenedPixel.rgb, vec3(0.0));

    outputColor = sharpenedPixel;
}
`
), di = {
  sharpness: 1
};
class nm extends I {
  constructor(e = di) {
    e = { ...di, ...e }, super("SharpnessEffect", Bf, {
      uniforms: /* @__PURE__ */ new Map([
        ["sharpness", new a(e.sharpness)],
        ["inputTexture", new a(null)]
      ])
    }), this.setSharpness(e.sharpness);
  }
  setSharpness(e) {
    this.uniforms.get("sharpness").value = e;
  }
  update(e, t) {
    this.uniforms.get("inputTexture").value = t.texture;
  }
}
class is {
  constructor() {
    this.isPass = !0, this.enabled = !0, this.needsSwap = !0, this.clear = !1, this.renderToScreen = !1;
  }
  setSize() {
  }
  render() {
    console.error("THREE.Pass: .render() must be implemented in derived pass.");
  }
  dispose() {
  }
}
const Lf = new Nr(-1, 1, 1, -1, 0, 1);
class Uf extends pi {
  constructor() {
    super(), this.setAttribute("position", new Sn([-1, 3, 0, -1, -1, 0, 3, -1, 0], 3)), this.setAttribute("uv", new Sn([0, 2, 0, 0, 2, 0], 2));
  }
}
const Of = new Uf();
class ss {
  constructor(e) {
    this._mesh = new Ft(Of, e);
  }
  dispose() {
    this._mesh.geometry.dispose();
  }
  render(e, t) {
    e.render(this._mesh, Lf, t);
  }
  get material() {
    return this._mesh.material;
  }
  set material(e) {
    this._mesh.material = e;
  }
}
const Ce = {
  defines: {
    MAX_STEP: 0,
    PERSPECTIVE_CAMERA: !0,
    DISTANCE_ATTENUATION: !0,
    FRESNEL: !0,
    INFINITE_THICK: !1,
    SELECTIVE: !1
  },
  uniforms: {
    tDiffuse: { value: null },
    tNormal: { value: null },
    tMetalness: { value: null },
    tDepth: { value: null },
    cameraNear: { value: null },
    cameraFar: { value: null },
    resolution: { value: new g() },
    cameraProjectionMatrix: { value: new C() },
    cameraInverseProjectionMatrix: { value: new C() },
    opacity: { value: 0.5 },
    maxDistance: { value: 180 },
    cameraRange: { value: 0 },
    thickness: { value: 0.018 },
    reflectionStrength: { value: 1 },
    metalnessThreshold: { value: 1 },
    debugMode: { value: 0 },
    brightnessThreshold: { value: 1 },
    readBuffer: { value: null },
    normalBuffer: { value: null }
  },
  vertexShader: (
    /* glsl */
    `

		varying vec2 vUv;

		void main() {

			vUv = uv;

			gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

		}

	`
  ),
  fragmentShader: (
    /* glsl */
    `
		// precision highp float;
		precision highp sampler2D;
		varying vec2 vUv;
		uniform sampler2D tDepth;
		uniform sampler2D tNormal;
		uniform sampler2D tMetalness;
		uniform sampler2D tDiffuse;
		uniform float cameraRange;
		uniform vec2 resolution;
		uniform float opacity;
		uniform float cameraNear;
		uniform float cameraFar;
		uniform float maxDistance;
		uniform float thickness;
		uniform float reflectionStrength;
		uniform mat4 cameraProjectionMatrix;
		uniform mat4 cameraInverseProjectionMatrix;
		#include <packing>
		float pointToLineDistance(vec3 x0, vec3 x1, vec3 x2) {
			//x0: point, x1: linePointA, x2: linePointB
			//https://mathworld.wolfram.com/Point-LineDistance3-Dimensional.html
			return length(cross(x0-x1,x0-x2))/length(x2-x1);
		}
		float pointPlaneDistance(vec3 point,vec3 planePoint,vec3 planeNormal){
			// https://mathworld.wolfram.com/Point-PlaneDistance.html
			//// https://en.wikipedia.org/wiki/Plane_(geometry)
			//// http://paulbourke.net/geometry/pointlineplane/
			float a=planeNormal.x,b=planeNormal.y,c=planeNormal.z;
			float x0=point.x,y0=point.y,z0=point.z;
			float x=planePoint.x,y=planePoint.y,z=planePoint.z;
			float d=-(a*x+b*y+c*z);
			float distance=(a*x0+b*y0+c*z0+d)/sqrt(a*a+b*b+c*c);
			return distance;
		}
		float getDepth( const in vec2 uv ) {
			return texture2D( tDepth, uv ).x;
		}
		float getViewZ( const in float depth ) {
			#ifdef PERSPECTIVE_CAMERA
				return perspectiveDepthToViewZ( depth, cameraNear, cameraFar );
			#else
				return orthographicDepthToViewZ( depth, cameraNear, cameraFar );
			#endif
		}
		vec3 getViewPosition( const in vec2 uv, const in float depth/*clip space*/, const in float clipW ) {
			vec4 clipPosition = vec4( ( vec3( uv, depth ) - 0.5 ) * 2.0, 1.0 );//ndc
			clipPosition *= clipW; //clip
			return ( cameraInverseProjectionMatrix * clipPosition ).xyz;//view
		}
		vec3 getViewNormal( const in vec2 uv ) {
			return unpackRGBToNormal( texture2D( tNormal, uv ).xyz );
		}
		vec2 viewPositionToXY(vec3 viewPosition){
			vec2 xy;
			vec4 clip=cameraProjectionMatrix*vec4(viewPosition,1);
			xy=clip.xy;//clip
			float clipW=clip.w;
			xy/=clipW;//NDC
			xy=(xy+1.)/2.;//uv
			xy*=resolution;//screen
			return xy;
		}
		void main(){
			#ifdef SELECTIVE
				float metalness=texture2D(tMetalness,vUv).r;
				if(metalness==0.0) return;
			#endif

			float depth = getDepth( vUv );
			float viewZ = getViewZ( depth );
			if(-viewZ>=cameraFar) return;

			float clipW = cameraProjectionMatrix[2][3] * viewZ+cameraProjectionMatrix[3][3];
			vec3 viewPosition=getViewPosition( vUv, depth, clipW );

			vec2 d0=gl_FragCoord.xy;
			vec2 d1;

			vec3 viewNormal=getViewNormal( vUv );

			#ifdef PERSPECTIVE_CAMERA
				vec3 viewIncidentDir=normalize(viewPosition);
				vec3 viewReflectDir=reflect(viewIncidentDir,viewNormal);
			#else
				vec3 viewIncidentDir=vec3(0,0,-1);
				vec3 viewReflectDir=reflect(viewIncidentDir,viewNormal);
			#endif

			float maxReflectRayLen=maxDistance/dot(-viewIncidentDir,viewNormal);
			// dot(a,b)==length(a)*length(b)*cos(theta) // https://www.mathsisfun.com/algebra/vectors-dot-product.html
			// if(a.isNormalized&&b.isNormalized) dot(a,b)==cos(theta)
			// maxDistance/maxReflectRayLen=cos(theta)
			// maxDistance/maxReflectRayLen==dot(a,b)
			// maxReflectRayLen==maxDistance/dot(a,b)

			vec3 d1viewPosition=viewPosition+viewReflectDir*maxReflectRayLen;
			#ifdef PERSPECTIVE_CAMERA
				if(d1viewPosition.z>-cameraNear){
					//https://tutorial.math.lamar.edu/Classes/CalcIII/EqnsOfLines.aspx
					float t=(-cameraNear-viewPosition.z)/viewReflectDir.z;
					d1viewPosition=viewPosition+viewReflectDir*t;
				}
			#endif
			d1=viewPositionToXY(d1viewPosition);

			float totalLen=length(d1-d0);
			float xLen=d1.x-d0.x;
			float yLen=d1.y-d0.y;
			float totalStep=max(abs(xLen),abs(yLen));
			float xSpan=xLen/totalStep;
			float ySpan=yLen/totalStep;
			for(float i=0.;i<float(MAX_STEP);i++){
				if(i>=totalStep) break;
				vec2 xy=vec2(d0.x+i*xSpan,d0.y+i*ySpan);
				if(xy.x<0.||xy.x>resolution.x||xy.y<0.||xy.y>resolution.y) break;
				float s=length(xy-d0)/totalLen;
				vec2 uv=xy/resolution;

				float d = getDepth(uv);
				float vZ = getViewZ( d );
				if(-vZ>=cameraFar) continue;
				float cW = cameraProjectionMatrix[2][3] * vZ+cameraProjectionMatrix[3][3];
				vec3 vP=getViewPosition( uv, d, cW );

				#ifdef PERSPECTIVE_CAMERA
					// https://comp.nus.edu.sg/~lowkl/publications/lowk_persp_interp_techrep.pdf
					float recipVPZ=1./viewPosition.z;
					float viewReflectRayZ=1./(recipVPZ+s*(1./d1viewPosition.z-recipVPZ));
				#else
					float viewReflectRayZ=viewPosition.z+s*(d1viewPosition.z-viewPosition.z);
				#endif

				// if(viewReflectRayZ>vZ) continue; // will cause "npm run make-screenshot webgl_postprocessing_ssr" high probability hang.
				// https://github.com/mrdoob/three.js/pull/21539#issuecomment-821061164
				if(viewReflectRayZ<=vZ){

					bool hit;
					#ifdef INFINITE_THICK
						hit=true;
					#else
						float away=pointToLineDistance(vP,viewPosition,d1viewPosition);

						float minThickness;
						vec2 xyNeighbor=xy;
						xyNeighbor.x+=1.;
						vec2 uvNeighbor=xyNeighbor/resolution;
						vec3 vPNeighbor=getViewPosition(uvNeighbor,d,cW);
						minThickness=vPNeighbor.x-vP.x;
						minThickness*=3.;
						float tk=max(minThickness,thickness);

						hit=away<=tk;
					#endif

					if(hit){
						vec3 vN=getViewNormal( uv );
						if(dot(viewReflectDir,vN)>=0.) continue;
						float distance=pointPlaneDistance(vP,viewPosition,viewNormal);
						if(distance>maxDistance) break;
						float op=opacity;
						#ifdef DISTANCE_ATTENUATION
							float ratio=1.-(distance/maxDistance);
							float attenuation=ratio*ratio;
							op=opacity*attenuation;
						#endif
						#ifdef FRESNEL
							float fresnelCoe=(dot(viewIncidentDir,viewReflectDir)+1.)/2.;
							op*=fresnelCoe;
						#endif
						vec4 reflectColor=texture2D(tDiffuse,uv);
						gl_FragColor.xyz=reflectColor.xyz * reflectionStrength;
						gl_FragColor.a=op;
						break;
					}
				}
			}
		}
	`
  )
}, qt = {
  defines: {
    PERSPECTIVE_CAMERA: 1
  },
  uniforms: {
    tDepth: { value: null },
    cameraNear: { value: null },
    cameraFar: { value: null }
  },
  vertexShader: (
    /* glsl */
    `

		varying vec2 vUv;

		void main() {

			vUv = uv;
			gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

		}

	`
  ),
  fragmentShader: (
    /* glsl */
    `

		uniform sampler2D tDepth;

		uniform float cameraNear;
		uniform float cameraFar;

		varying vec2 vUv;

		#include <packing>

		float getLinearDepth( const in vec2 uv ) {

			#if PERSPECTIVE_CAMERA == 1

				float fragCoordZ = texture2D( tDepth, uv ).x;
				float viewZ = perspectiveDepthToViewZ( fragCoordZ, cameraNear, cameraFar );
				return viewZToOrthographicDepth( viewZ, cameraNear, cameraFar );

			#else

				return texture2D( tDepth, uv ).x;

			#endif

		}

		void main() {

			float depth = getLinearDepth( vUv );
			float d = 1.0 - depth;
			// d=(d-.999)*1000.;
			gl_FragColor = vec4( vec3( d ), 1.0 );

		}

	`
  )
}, Xe = {
  uniforms: {
    tDiffuse: { value: null },
    resolution: { value: new g() },
    opacity: { value: 0.5 }
  },
  vertexShader: (
    /* glsl */
    `

		varying vec2 vUv;

		void main() {

			vUv = uv;
			gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

		}

	`
  ),
  fragmentShader: (
    /* glsl */
    `

		uniform sampler2D tDiffuse;
		uniform vec2 resolution;
		varying vec2 vUv;
		void main() {
			//reverse engineering from PhotoShop blur filter, then change coefficient

			vec2 texelSize = ( 1.0 / resolution );

			vec4 c=texture2D(tDiffuse,vUv);

			vec2 offset;

			offset=(vec2(-1,0))*texelSize;
			vec4 cl=texture2D(tDiffuse,vUv+offset);

			offset=(vec2(1,0))*texelSize;
			vec4 cr=texture2D(tDiffuse,vUv+offset);

			offset=(vec2(0,-1))*texelSize;
			vec4 cb=texture2D(tDiffuse,vUv+offset);

			offset=(vec2(0,1))*texelSize;
			vec4 ct=texture2D(tDiffuse,vUv+offset);

			// float coeCenter=.5;
			// float coeSide=.125;
			float coeCenter=.2;
			float coeSide=.2;
			float a=c.a*coeCenter+cl.a*coeSide+cr.a*coeSide+cb.a*coeSide+ct.a*coeSide;
			vec3 rgb=(c.rgb*c.a*coeCenter+cl.rgb*cl.a*coeSide+cr.rgb*cr.a*coeSide+cb.rgb*cb.a*coeSide+ct.rgb*ct.a*coeSide)/a;
			gl_FragColor=vec4(rgb,a);

		}
	`
  )
}, vt = {
  uniforms: {
    tDiffuse: { value: null },
    opacity: { value: 1 }
  },
  vertexShader: (
    /* glsl */
    `

		varying vec2 vUv;

		void main() {

			vUv = uv;
			gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

		}`
  ),
  fragmentShader: (
    /* glsl */
    `

		uniform float opacity;

		uniform sampler2D tDiffuse;

		varying vec2 vUv;

		void main() {

			vec4 texel = texture2D( tDiffuse, vUv );
			gl_FragColor = opacity * texel;


		}`
  )
};
class Ze extends is {
  constructor({ renderer: e, scene: t, camera: r, width: n, height: i, selects: s, bouncing: o = !1, groundReflector: l }) {
    super(), this.width = n !== void 0 ? n : 512, this.height = i !== void 0 ? i : 512, this.clear = !0, this.renderer = e, this.scene = t, this.camera = r, this.groundReflector = l, this.opacity = Ce.uniforms.opacity.value, this.output = 0, this.maxDistance = Ce.uniforms.maxDistance.value, this.thickness = Ce.uniforms.thickness.value, this.reflectionStrength = Ce.uniforms.reflectionStrength.value, this.tempColor = new y(), this.externalDepthTexture = null, this.useExternalDepth = !1, this._selects = s, this.selective = Array.isArray(this._selects), Object.defineProperty(this, "selects", {
      get() {
        return this._selects;
      },
      set(f) {
        this._selects !== f && (this._selects = f, Array.isArray(f) ? (this.selective = !0, this.ssrMaterial.defines.SELECTIVE = !0, this.ssrMaterial.needsUpdate = !0) : (this.selective = !1, this.ssrMaterial.defines.SELECTIVE = !1, this.ssrMaterial.needsUpdate = !0));
      }
    }), this._bouncing = o, Object.defineProperty(this, "bouncing", {
      get() {
        return this._bouncing;
      },
      set(f) {
        this.setBouncing(f);
      }
    }), this.blur = !0, this._distanceAttenuation = Ce.defines.DISTANCE_ATTENUATION, Object.defineProperty(this, "distanceAttenuation", {
      get() {
        return this._distanceAttenuation;
      },
      set(f) {
        this._distanceAttenuation !== f && (this._distanceAttenuation = f, this.ssrMaterial.defines.DISTANCE_ATTENUATION = f, this.ssrMaterial.needsUpdate = !0);
      }
    }), this._fresnel = Ce.defines.FRESNEL, Object.defineProperty(this, "fresnel", {
      get() {
        return this._fresnel;
      },
      set(f) {
        this._fresnel !== f && (this._fresnel = f, this.ssrMaterial.defines.FRESNEL = f, this.ssrMaterial.needsUpdate = !0);
      }
    }), this._infiniteThick = Ce.defines.INFINITE_THICK, Object.defineProperty(this, "infiniteThick", {
      get() {
        return this._infiniteThick;
      },
      set(f) {
        this._infiniteThick !== f && (this._infiniteThick = f, this.ssrMaterial.defines.INFINITE_THICK = f, this.ssrMaterial.needsUpdate = !0);
      }
    }), this._reflectionStrength = Ce.uniforms.reflectionStrength.value, Object.defineProperty(this, "reflectionStrength", {
      get() {
        return this._reflectionStrength;
      },
      set(f) {
        this._reflectionStrength !== f && (this._reflectionStrength = f, this.ssrMaterial && (this.ssrMaterial.uniforms.reflectionStrength.value = f));
      }
    });
    const u = new Oe();
    u.type = Qr, u.minFilter = b, u.magFilter = b, this.beautyRenderTarget = new A(this.width, this.height, {
      minFilter: b,
      magFilter: b,
      type: K,
      depthTexture: u,
      depthBuffer: !0
    }), this.prevRenderTarget = new A(this.width, this.height, {
      minFilter: b,
      magFilter: b
    }), this.normalRenderTarget = new A(this.width, this.height, {
      minFilter: b,
      magFilter: b,
      type: K
    }), this.metalnessRenderTarget = new A(this.width, this.height, {
      minFilter: b,
      magFilter: b,
      type: K
    }), this.ssrRenderTarget = new A(this.width, this.height, {
      minFilter: b,
      magFilter: b
    }), this.blurRenderTarget = this.ssrRenderTarget.clone(), this.blurRenderTarget2 = this.ssrRenderTarget.clone(), this.ssrMaterial = new N({
      defines: Object.assign({}, Ce.defines, {
        MAX_STEP: Math.sqrt(this.width * this.width + this.height * this.height)
      }),
      uniforms: oe.clone(Ce.uniforms),
      vertexShader: Ce.vertexShader,
      fragmentShader: Ce.fragmentShader,
      blending: B
    }), this.ssrMaterial.uniforms.tDiffuse.value = this.beautyRenderTarget.texture, this.ssrMaterial.uniforms.tNormal.value = this.normalRenderTarget.texture, this.ssrMaterial.defines.SELECTIVE = this.selective, this.ssrMaterial.needsUpdate = !0, this.ssrMaterial.uniforms.tMetalness.value = this.metalnessRenderTarget.texture, this.ssrMaterial.uniforms.tDepth.value = this.beautyRenderTarget.depthTexture, this.ssrMaterial.uniforms.cameraNear.value = this.camera.near, this.ssrMaterial.uniforms.cameraFar.value = this.camera.far, this.ssrMaterial.uniforms.thickness.value = this.thickness, this.ssrMaterial.uniforms.resolution.value.set(this.width, this.height), this.ssrMaterial.uniforms.cameraProjectionMatrix.value.copy(this.camera.projectionMatrix), this.ssrMaterial.uniforms.cameraInverseProjectionMatrix.value.copy(this.camera.projectionMatrixInverse), this.ssrMaterial.uniforms.reflectionStrength.value = this.reflectionStrength, this.normalMaterial = new Kr(), this.normalMaterial.blending = B, this.metalnessOnMaterial = new gt({
      color: "white"
    }), this.metalnessOffMaterial = new gt({
      color: "black"
    }), this.blurMaterial = new N({
      defines: Object.assign({}, Xe.defines),
      uniforms: oe.clone(Xe.uniforms),
      vertexShader: Xe.vertexShader,
      fragmentShader: Xe.fragmentShader
    }), this.blurMaterial.uniforms.tDiffuse.value = this.ssrRenderTarget.texture, this.blurMaterial.uniforms.resolution.value.set(this.width, this.height), this.blurMaterial2 = new N({
      defines: Object.assign({}, Xe.defines),
      uniforms: oe.clone(Xe.uniforms),
      vertexShader: Xe.vertexShader,
      fragmentShader: Xe.fragmentShader
    }), this.blurMaterial2.uniforms.tDiffuse.value = this.blurRenderTarget.texture, this.blurMaterial2.uniforms.resolution.value.set(this.width, this.height), this.depthRenderMaterial = new N({
      defines: Object.assign({}, qt.defines),
      uniforms: oe.clone(qt.uniforms),
      vertexShader: qt.vertexShader,
      fragmentShader: qt.fragmentShader,
      blending: B
    }), this.depthRenderMaterial.uniforms.tDepth.value = this.beautyRenderTarget.depthTexture, this.depthRenderMaterial.uniforms.cameraNear.value = this.camera.near, this.depthRenderMaterial.uniforms.cameraFar.value = this.camera.far, this.copyMaterial = new N({
      uniforms: oe.clone(vt.uniforms),
      vertexShader: vt.vertexShader,
      fragmentShader: vt.fragmentShader,
      transparent: !0,
      depthTest: !1,
      depthWrite: !1,
      blendSrc: ar,
      blendDst: sr,
      blendEquation: Lt,
      blendSrcAlpha: ar,
      blendDstAlpha: sr,
      blendEquationAlpha: Lt
      // premultipliedAlpha:true,
    }), this.fsQuad = new ss(null), this.originalClearColor = new y();
  }
  dispose() {
    this.beautyRenderTarget.dispose(), this.prevRenderTarget.dispose(), this.normalRenderTarget.dispose(), this.metalnessRenderTarget.dispose(), this.ssrRenderTarget.dispose(), this.blurRenderTarget.dispose(), this.blurRenderTarget2.dispose(), this.normalMaterial.dispose(), this.metalnessOnMaterial.dispose(), this.metalnessOffMaterial.dispose(), this.blurMaterial.dispose(), this.blurMaterial2.dispose(), this.copyMaterial.dispose(), this.depthRenderMaterial.dispose(), this.fsQuad.dispose();
  }
  render(e, t) {
    switch (e.setRenderTarget(this.beautyRenderTarget), e.clear(), this.groundReflector && (this.groundReflector.visible = !1, this.groundReflector.doRender(this.renderer, this.scene, this.camera), this.groundReflector.visible = !0), this.groundReflector && (this.groundReflector.visible = !1), this.renderOverride(e, this.normalMaterial, this.normalRenderTarget, 0, 0), this.selective && this.renderMetalness(e, this.metalnessOnMaterial, this.metalnessRenderTarget, 0, 0), this.ssrMaterial.uniforms.opacity.value = this.opacity, this.ssrMaterial.uniforms.maxDistance.value = this.maxDistance, this.ssrMaterial.uniforms.thickness.value = this.thickness, this.ssrMaterial.uniforms.reflectionStrength.value = this.reflectionStrength, this.useExternalDepth && this.externalDepthTexture ? this.ssrMaterial.uniforms.tDepth.value = this.externalDepthTexture : this.ssrMaterial.uniforms.tDepth.value = this.beautyRenderTarget.depthTexture, this.renderPass(e, this.ssrMaterial, this.ssrRenderTarget), this.blur && (this.renderPass(e, this.blurMaterial, this.blurRenderTarget), this.renderPass(e, this.blurMaterial2, this.blurRenderTarget2)), this.output) {
      case Ze.OUTPUT.Default:
        this.bouncing ? (this.copyMaterial.uniforms.tDiffuse.value = this.beautyRenderTarget.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.prevRenderTarget), this.blur ? this.copyMaterial.uniforms.tDiffuse.value = this.blurRenderTarget2.texture : this.copyMaterial.uniforms.tDiffuse.value = this.ssrRenderTarget.texture, this.copyMaterial.blending = pt, this.renderPass(e, this.copyMaterial, this.prevRenderTarget), this.copyMaterial.uniforms.tDiffuse.value = this.prevRenderTarget.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t)) : (this.copyMaterial.uniforms.tDiffuse.value = this.beautyRenderTarget.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t), this.blur ? this.copyMaterial.uniforms.tDiffuse.value = this.blurRenderTarget2.texture : this.copyMaterial.uniforms.tDiffuse.value = this.ssrRenderTarget.texture, this.copyMaterial.blending = pt, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t));
        break;
      case Ze.OUTPUT.SSR:
        this.blur ? this.copyMaterial.uniforms.tDiffuse.value = this.blurRenderTarget2.texture : this.copyMaterial.uniforms.tDiffuse.value = this.ssrRenderTarget.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t), this.bouncing && (this.blur ? this.copyMaterial.uniforms.tDiffuse.value = this.blurRenderTarget2.texture : this.copyMaterial.uniforms.tDiffuse.value = this.beautyRenderTarget.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.prevRenderTarget), this.copyMaterial.uniforms.tDiffuse.value = this.ssrRenderTarget.texture, this.copyMaterial.blending = pt, this.renderPass(e, this.copyMaterial, this.prevRenderTarget));
        break;
      case Ze.OUTPUT.Beauty:
        this.copyMaterial.uniforms.tDiffuse.value = this.beautyRenderTarget.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t);
        break;
      case Ze.OUTPUT.Depth:
        this.useExternalDepth && this.externalDepthTexture ? this.depthRenderMaterial.uniforms.tDepth.value = this.externalDepthTexture : this.depthRenderMaterial.uniforms.tDepth.value = this.beautyRenderTarget.depthTexture, this.renderPass(e, this.depthRenderMaterial, this.renderToScreen ? null : t);
        break;
      case Ze.OUTPUT.Normal:
        this.copyMaterial.uniforms.tDiffuse.value = this.normalRenderTarget.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t);
        break;
      case Ze.OUTPUT.Metalness:
        this.copyMaterial.uniforms.tDiffuse.value = this.metalnessRenderTarget.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t);
        break;
      default:
        console.warn("THREE.SSRPass: Unknown output type.");
    }
  }
  renderPass(e, t, r, n, i) {
    this.originalClearColor.copy(e.getClearColor(this.tempColor));
    const s = e.getClearAlpha(this.tempColor), o = e.autoClear;
    e.setRenderTarget(r), e.autoClear = !1, n != null && (e.setClearColor(n), e.setClearAlpha(i || 0), e.clear()), this.fsQuad.material = t, this.fsQuad.render(e), e.autoClear = o, e.setClearColor(this.originalClearColor), e.setClearAlpha(s);
  }
  renderOverride(e, t, r, n, i) {
    this.originalClearColor.copy(e.getClearColor(this.tempColor));
    const s = e.getClearAlpha(this.tempColor), o = e.autoClear;
    e.setRenderTarget(r), e.autoClear = !1, n = t.clearColor || n, i = t.clearAlpha || i, n != null && (e.setClearColor(n), e.setClearAlpha(i || 0), e.clear()), this.scene.overrideMaterial = t, e.shadowMap.autoUpdate = !1;
    const l = this.scene.matrixWorldAutoUpdate;
    this.scene.matrixWorldAutoUpdate = !1, e.render(this.scene, this.camera), this.scene.overrideMaterial = null, this.scene.matrixWorldAutoUpdate = l, e.autoClear = o, e.setClearColor(this.originalClearColor), e.setClearAlpha(s);
  }
  renderMetalness(e, t, r, n, i) {
    this.originalClearColor.copy(e.getClearColor(this.tempColor));
    const s = e.getClearAlpha(this.tempColor), o = e.autoClear;
    e.setRenderTarget(r), e.autoClear = !1, n = t.clearColor || n, i = t.clearAlpha || i, n != null && (e.setClearColor(n), e.setClearAlpha(i || 0), e.clear()), this.scene.traverseVisible((u) => {
      u._SSRPassBackupMaterial = u.material, this._selects.includes(u) ? u.material = this.metalnessOnMaterial : u.material = this.metalnessOffMaterial;
    }), e.shadowMap.autoUpdate = !1;
    const l = this.scene.matrixWorldAutoUpdate;
    this.scene.matrixWorldAutoUpdate = !1, e.render(this.scene, this.camera), this.scene.matrixWorldAutoUpdate = l, this.scene.traverseVisible((u) => {
      u.material = u._SSRPassBackupMaterial;
    }), e.autoClear = o, e.setClearColor(this.originalClearColor), e.setClearAlpha(s);
  }
  setSize(e, t) {
    this.width = e, this.height = t, this.ssrMaterial.defines.MAX_STEP = Math.sqrt(e * e + t * t), this.ssrMaterial.needsUpdate = !0, this.beautyRenderTarget.setSize(e, t), this.prevRenderTarget.setSize(e, t), this.ssrRenderTarget.setSize(e, t), this.normalRenderTarget.setSize(e, t), this.metalnessRenderTarget.setSize(e, t), this.blurRenderTarget.setSize(e, t), this.blurRenderTarget2.setSize(e, t), this.ssrMaterial.uniforms.resolution.value.set(e, t), this.ssrMaterial.uniforms.cameraProjectionMatrix.value.copy(this.camera.projectionMatrix), this.ssrMaterial.uniforms.cameraInverseProjectionMatrix.value.copy(this.camera.projectionMatrixInverse), this.blurMaterial.uniforms.resolution.value.set(e, t), this.blurMaterial2.uniforms.resolution.value.set(e, t);
  }
  /**
   * 设置外部深度纹理
   * @param {DepthTexture} depthTexture - 外部深度纹理
   * @param {number} depthPacking - 深度打包格式 (e.g., BasicDepthPacking)
   */
  setDepthTexture(e, t) {
    e ? (this.externalDepthTexture = e, this.useExternalDepth = !0, this.ssrMaterial && (this.ssrMaterial.uniforms.tDepth.value = e), this.depthRenderMaterial && (this.depthRenderMaterial.uniforms.tDepth.value = e)) : (this.useExternalDepth = !1, this.ssrMaterial && this.beautyRenderTarget && (this.ssrMaterial.uniforms.tDepth.value = this.beautyRenderTarget.depthTexture), this.depthRenderMaterial && this.beautyRenderTarget && (this.depthRenderMaterial.uniforms.tDepth.value = this.beautyRenderTarget.depthTexture));
  }
  /**
   * 刷新反射效果，适用于修改selects后需要重新计算反射的情况
   * 临时开启bouncing，渲染一帧，然后自动关闭
   * @param {WebGLRenderer} renderer - 渲染器
   * @param {WebGLRenderTarget} writeBuffer - 写入缓冲区
   * @param {number} [refreshFrames=1] - 刷新帧数，默认为1
   * @returns {Promise} 返回一个Promise，当刷新完成时解析
   */
  refring = !1;
  refreshReflection() {
    if (this.refring)
      return;
    this.refring = !0;
    const e = this._bouncing;
    this.setBouncing(!0), setTimeout(() => {
      this.setBouncing(e), this.refring = !1;
    });
  }
  setBouncing(e) {
    this._bouncing !== e && (this._bouncing = e, e ? this.ssrMaterial.uniforms.tDiffuse.value = this.prevRenderTarget.texture : this.ssrMaterial.uniforms.tDiffuse.value = this.beautyRenderTarget.texture);
  }
}
Ze.OUTPUT = {
  Default: 0,
  SSR: 1,
  Beauty: 3,
  Depth: 4,
  Normal: 5,
  Metalness: 7
};
class Ke extends Ft {
  constructor(e, t = {}) {
    super(e), this.isReflectorForSSRPass = !0, this.type = "ReflectorForSSRPass";
    const r = this, n = t.color !== void 0 ? new y(t.color) : new y(8355711), i = t.textureWidth || 512, s = t.textureHeight || 512, o = t.clipBias || 0, l = t.shader || Ke.ReflectorShader, u = t.useDepthTexture === !0, f = new T(0, 1, 0), h = new T(), m = new T();
    r.needsUpdate = !1, r.maxDistance = Ke.ReflectorShader.uniforms.maxDistance.value, r.opacity = Ke.ReflectorShader.uniforms.opacity.value, r.color = n, r.resolution = t.resolution || new g(window.innerWidth, window.innerHeight), r._distanceAttenuation = Ke.ReflectorShader.defines.DISTANCE_ATTENUATION, Object.defineProperty(r, "distanceAttenuation", {
      get() {
        return r._distanceAttenuation;
      },
      set(R) {
        r._distanceAttenuation !== R && (r._distanceAttenuation = R, r.material.defines.DISTANCE_ATTENUATION = R, r.material.needsUpdate = !0);
      }
    }), r._fresnel = Ke.ReflectorShader.defines.FRESNEL, Object.defineProperty(r, "fresnel", {
      get() {
        return r._fresnel;
      },
      set(R) {
        r._fresnel !== R && (r._fresnel = R, r.material.defines.FRESNEL = R, r.material.needsUpdate = !0);
      }
    });
    const p = new T(), d = new T(), v = new T(), x = new C(), w = new T(0, 0, -1), M = new T(), D = new T(), P = new C(), O = new ke();
    let S;
    u && (S = new Oe(), S.type = Qr, S.minFilter = b, S.magFilter = b);
    const L = {
      depthTexture: u ? S : null,
      type: K
    }, H = new A(i, s, L), Z = new N({
      name: l.name !== void 0 ? l.name : "unspecified",
      transparent: u,
      defines: Object.assign({}, Ke.ReflectorShader.defines, {
        useDepthTexture: u
      }),
      uniforms: oe.clone(l.uniforms),
      fragmentShader: l.fragmentShader,
      vertexShader: l.vertexShader
    });
    Z.uniforms.tDiffuse.value = H.texture, Z.uniforms.color.value = r.color, Z.uniforms.textureMatrix.value = P, u && (Z.uniforms.tDepth.value = H.depthTexture), this.material = Z;
    const k = [new Ts(new T(0, 1, 0), o)];
    this.doRender = function(R, U, q) {
      if (Z.uniforms.maxDistance.value = r.maxDistance, Z.uniforms.color.value = r.color, Z.uniforms.opacity.value = r.opacity, h.copy(q.position).normalize(), m.copy(h).reflect(f), Z.uniforms.fresnelCoe.value = (h.dot(m) + 1) / 2, d.setFromMatrixPosition(r.matrixWorld), v.setFromMatrixPosition(q.matrixWorld), x.extractRotation(r.matrixWorld), p.set(0, 0, 1), p.applyMatrix4(x), M.subVectors(d, v), M.dot(p) > 0)
        return;
      M.reflect(p).negate(), M.add(d), x.extractRotation(q.matrixWorld), w.set(0, 0, -1), w.applyMatrix4(x), w.add(v), D.subVectors(d, w), D.reflect(p).negate(), D.add(d), O.position.copy(M), O.up.set(0, 1, 0), O.up.applyMatrix4(x), O.up.reflect(p), O.lookAt(D), O.far = q.far, O.updateMatrixWorld(), O.projectionMatrix.copy(q.projectionMatrix), Z.uniforms.virtualCameraNear.value = q.near, Z.uniforms.virtualCameraFar.value = q.far, Z.uniforms.virtualCameraMatrixWorld.value = O.matrixWorld, Z.uniforms.virtualCameraProjectionMatrix.value = q.projectionMatrix, Z.uniforms.virtualCameraProjectionMatrixInverse.value = q.projectionMatrixInverse, Z.uniforms.resolution.value = r.resolution, P.set(
        0.5,
        0,
        0,
        0.5,
        0,
        0.5,
        0,
        0.5,
        0,
        0,
        0.5,
        0.5,
        0,
        0,
        0,
        1
      ), P.multiply(O.projectionMatrix), P.multiply(O.matrixWorldInverse), P.multiply(r.matrixWorld);
      const ie = R.getRenderTarget(), J = R.xr.enabled, Pe = R.shadowMap.autoUpdate, yr = R.clippingPlanes;
      R.xr.enabled = !1, R.shadowMap.autoUpdate = !1, R.clippingPlanes = k, R.setRenderTarget(H), R.state.buffers.depth.setMask(!0), R.autoClear === !1 && R.clear(), R.render(U, O), R.xr.enabled = J, R.shadowMap.autoUpdate = Pe, R.clippingPlanes = yr, R.setRenderTarget(ie);
      const Tt = q.viewport;
      Tt !== void 0 && R.state.viewport(Tt);
    }, this.getRenderTarget = function() {
      return H;
    };
  }
}
Ke.ReflectorShader = {
  name: "ReflectorShader",
  defines: {
    DISTANCE_ATTENUATION: !0,
    FRESNEL: !0
  },
  uniforms: {
    color: { value: null },
    tDiffuse: { value: null },
    tDepth: { value: null },
    textureMatrix: { value: new C() },
    maxDistance: { value: 180 },
    opacity: { value: 0.5 },
    fresnelCoe: { value: null },
    virtualCameraNear: { value: null },
    virtualCameraFar: { value: null },
    virtualCameraProjectionMatrix: { value: new C() },
    virtualCameraMatrixWorld: { value: new C() },
    virtualCameraProjectionMatrixInverse: { value: new C() },
    resolution: { value: new g() }
  },
  vertexShader: (
    /* glsl */
    `
		uniform mat4 textureMatrix;
		varying vec4 vUv;

		void main() {

			vUv = textureMatrix * vec4( position, 1.0 );

			gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

		}`
  ),
  fragmentShader: (
    /* glsl */
    `
		uniform vec3 color;
		uniform sampler2D tDiffuse;
		uniform sampler2D tDepth;
		uniform float maxDistance;
		uniform float opacity;
		uniform float fresnelCoe;
		uniform float virtualCameraNear;
		uniform float virtualCameraFar;
		uniform mat4 virtualCameraProjectionMatrix;
		uniform mat4 virtualCameraProjectionMatrixInverse;
		uniform mat4 virtualCameraMatrixWorld;
		uniform vec2 resolution;
		varying vec4 vUv;
		#include <packing>
		float blendOverlay( float base, float blend ) {
			return( base < 0.5 ? ( 2.0 * base * blend ) : ( 1.0 - 2.0 * ( 1.0 - base ) * ( 1.0 - blend ) ) );
		}
		vec3 blendOverlay( vec3 base, vec3 blend ) {
			return vec3( blendOverlay( base.r, blend.r ), blendOverlay( base.g, blend.g ), blendOverlay( base.b, blend.b ) );
		}
		float getDepth( const in vec2 uv ) {
			return texture2D( tDepth, uv ).x;
		}
		float getViewZ( const in float depth ) {
			return perspectiveDepthToViewZ( depth, virtualCameraNear, virtualCameraFar );
		}
		vec3 getViewPosition( const in vec2 uv, const in float depth/*clip space*/, const in float clipW ) {
			vec4 clipPosition = vec4( ( vec3( uv, depth ) - 0.5 ) * 2.0, 1.0 );//ndc
			clipPosition *= clipW; //clip
			return ( virtualCameraProjectionMatrixInverse * clipPosition ).xyz;//view
		}
		void main() {
			vec4 base = texture2DProj( tDiffuse, vUv );
			#ifdef useDepthTexture
				vec2 uv=(gl_FragCoord.xy-.5)/resolution.xy;
				uv.x=1.-uv.x;
				float depth = texture2DProj( tDepth, vUv ).r;
				float viewZ = getViewZ( depth );
				float clipW = virtualCameraProjectionMatrix[2][3] * viewZ+virtualCameraProjectionMatrix[3][3];
				vec3 viewPosition=getViewPosition( uv, depth, clipW );
				vec3 worldPosition=(virtualCameraMatrixWorld*vec4(viewPosition,1)).xyz;
				if(worldPosition.y>maxDistance) discard;
				float op=opacity;
				#ifdef DISTANCE_ATTENUATION
					float ratio=1.-(worldPosition.y/maxDistance);
					float attenuation=ratio*ratio;
					op=opacity*attenuation;
				#endif
				#ifdef FRESNEL
					op*=fresnelCoe;
				#endif
				gl_FragColor = vec4( blendOverlay( base.rgb, color ), op );
			#else
				gl_FragColor = vec4( blendOverlay( base.rgb, color ), 1.0 );
			#endif
		}
	`
  )
};
const Ae = {
  defines: {
    MAX_STEP: 0,
    PERSPECTIVE_CAMERA: !0,
    DISTANCE_ATTENUATION: !0,
    FRESNEL: !0,
    INFINITE_THICK: !1,
    SELECTIVE: !1
  },
  uniforms: {
    tDiffuse: { value: null },
    tNormal: { value: null },
    tMetalness: { value: null },
    tDepth: { value: null },
    maskTexture: { value: null },
    cameraNear: { value: null },
    cameraFar: { value: null },
    resolution: { value: new g() },
    cameraProjectionMatrix: { value: new C() },
    cameraInverseProjectionMatrix: { value: new C() },
    opacity: { value: 0.5 },
    maxDistance: { value: 180 },
    cameraRange: { value: 0 },
    thickness: { value: 0.018 },
    reflectionStrength: { value: 1 },
    metalnessThreshold: { value: 1 },
    debugMode: { value: 0 },
    brightnessThreshold: { value: 1 },
    maskThreshold: { value: 0.01 },
    readBuffer: { value: null },
    normalBuffer: { value: null },
    depthTexture: { value: null },
    depthPass1: { value: null }
  },
  vertexShader: (
    /* glsl */
    `

		varying vec2 vUv;

		void main() {

			vUv = uv;

			gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

		}

	`
  ),
  fragmentShader: (
    /* glsl */
    `
	// precision highp float;
	precision highp sampler2D;
	varying vec2 vUv;
	uniform sampler2D tDepth;
	uniform sampler2D tNormal;
	uniform sampler2D tMetalness;
	uniform sampler2D tDiffuse;
	uniform sampler2D maskTexture;
	uniform float cameraRange;
	uniform vec2 resolution;
	uniform float opacity;
	uniform float cameraNear;
	uniform float cameraFar;
	uniform float maxDistance;
	uniform float thickness;
	uniform float reflectionStrength;
	uniform mat4 cameraProjectionMatrix;
	uniform mat4 cameraInverseProjectionMatrix;
	#include <packing>
	float pointToLineDistance(vec3 x0, vec3 x1, vec3 x2) {
		//x0: point, x1: linePointA, x2: linePointB
		//https://mathworld.wolfram.com/Point-LineDistance3-Dimensional.html
		return length(cross(x0-x1,x0-x2))/length(x2-x1);
	}
	float pointPlaneDistance(vec3 point,vec3 planePoint,vec3 planeNormal){
		// https://mathworld.wolfram.com/Point-PlaneDistance.html
		//// https://en.wikipedia.org/wiki/Plane_(geometry)
		//// http://paulbourke.net/geometry/pointlineplane/
		float a=planeNormal.x,b=planeNormal.y,c=planeNormal.z;
		float x0=point.x,y0=point.y,z0=point.z;
		float x=planePoint.x,y=planePoint.y,z=planePoint.z;
		float d=-(a*x+b*y+c*z);
		float distance=(a*x0+b*y0+c*z0+d)/sqrt(a*a+b*b+c*c);
		return distance;
	}
	float getDepth( const in vec2 uv ) {
		return texture2D( tDepth, uv ).x;
	}
	float getViewZ( const in float depth ) {
		#ifdef PERSPECTIVE_CAMERA
			return perspectiveDepthToViewZ( depth, cameraNear, cameraFar );
		#else
			return orthographicDepthToViewZ( depth, cameraNear, cameraFar );
		#endif
	}
	vec3 getViewPosition( const in vec2 uv, const in float depth/*clip space*/, const in float clipW ) {
		vec4 clipPosition = vec4( ( vec3( uv, depth ) - 0.5 ) * 2.0, 1.0 );//ndc
		clipPosition *= clipW; //clip
		return ( cameraInverseProjectionMatrix * clipPosition ).xyz;//view
	}
	vec3 getViewNormal( const in vec2 uv ) {
		return unpackRGBToNormal( texture2D( tNormal, uv ).xyz );
	}
	vec2 viewPositionToXY(vec3 viewPosition){
		vec2 xy;
		vec4 clip=cameraProjectionMatrix*vec4(viewPosition,1);
		xy=clip.xy;//clip
		float clipW=clip.w;
		xy/=clipW;//NDC
		xy=(xy+1.)/2.;//uv
		xy*=resolution;//screen
		return xy;
	}
	void main(){
			float metalness=texture2D(maskTexture,vUv).r;
			if(metalness==0.0) return;

		float depth = getDepth( vUv );
		float viewZ = getViewZ( depth );
		if(-viewZ>=cameraFar) return;

		float clipW = cameraProjectionMatrix[2][3] * viewZ+cameraProjectionMatrix[3][3];
		vec3 viewPosition=getViewPosition( vUv, depth, clipW );

		vec2 d0=gl_FragCoord.xy;
		vec2 d1;

		vec3 viewNormal=getViewNormal( vUv );

		#ifdef PERSPECTIVE_CAMERA
			vec3 viewIncidentDir=normalize(viewPosition);
			vec3 viewReflectDir=reflect(viewIncidentDir,viewNormal);
		#else
			vec3 viewIncidentDir=vec3(0,0,-1);
			vec3 viewReflectDir=reflect(viewIncidentDir,viewNormal);
		#endif

		float maxReflectRayLen=maxDistance/dot(-viewIncidentDir,viewNormal);
		// dot(a,b)==length(a)*length(b)*cos(theta) // https://www.mathsisfun.com/algebra/vectors-dot-product.html
		// if(a.isNormalized&&b.isNormalized) dot(a,b)==cos(theta)
		// maxDistance/maxReflectRayLen=cos(theta)
		// maxDistance/maxReflectRayLen==dot(a,b)
		// maxReflectRayLen==maxDistance/dot(a,b)

		vec3 d1viewPosition=viewPosition+viewReflectDir*maxReflectRayLen;
		#ifdef PERSPECTIVE_CAMERA
			if(d1viewPosition.z>-cameraNear){
				//https://tutorial.math.lamar.edu/Classes/CalcIII/EqnsOfLines.aspx
				float t=(-cameraNear-viewPosition.z)/viewReflectDir.z;
				d1viewPosition=viewPosition+viewReflectDir*t;
			}
		#endif
		d1=viewPositionToXY(d1viewPosition);

		float totalLen=length(d1-d0);
		float xLen=d1.x-d0.x;
		float yLen=d1.y-d0.y;
		float totalStep=max(abs(xLen),abs(yLen));
		float xSpan=xLen/totalStep;
		float ySpan=yLen/totalStep;
		for(float i=0.;i<float(MAX_STEP);i++){
			if(i>=totalStep) break;
			vec2 xy=vec2(d0.x+i*xSpan,d0.y+i*ySpan);
			if(xy.x<0.||xy.x>resolution.x||xy.y<0.||xy.y>resolution.y) break;
			float s=length(xy-d0)/totalLen;
			vec2 uv=xy/resolution;

			float d = getDepth(uv);
			float vZ = getViewZ( d );
			if(-vZ>=cameraFar) continue;
			float cW = cameraProjectionMatrix[2][3] * vZ+cameraProjectionMatrix[3][3];
			vec3 vP=getViewPosition( uv, d, cW );

			#ifdef PERSPECTIVE_CAMERA
				// https://comp.nus.edu.sg/~lowkl/publications/lowk_persp_interp_techrep.pdf
				float recipVPZ=1./viewPosition.z;
				float viewReflectRayZ=1./(recipVPZ+s*(1./d1viewPosition.z-recipVPZ));
			#else
				float viewReflectRayZ=viewPosition.z+s*(d1viewPosition.z-viewPosition.z);
			#endif

			// if(viewReflectRayZ>vZ) continue; // will cause "npm run make-screenshot webgl_postprocessing_ssr" high probability hang.
			// https://github.com/mrdoob/three.js/pull/21539#issuecomment-821061164
			if(viewReflectRayZ<=vZ){

				bool hit;
				#ifdef INFINITE_THICK
					hit=true;
				#else
					float away=pointToLineDistance(vP,viewPosition,d1viewPosition);

					float minThickness;
					vec2 xyNeighbor=xy;
					xyNeighbor.x+=1.;
					vec2 uvNeighbor=xyNeighbor/resolution;
					vec3 vPNeighbor=getViewPosition(uvNeighbor,d,cW);
					minThickness=vPNeighbor.x-vP.x;
					minThickness*=3.;
					float tk=max(minThickness,thickness);

					hit=away<=tk;
				#endif

				if(hit){
					vec3 vN=getViewNormal( uv );
					if(dot(viewReflectDir,vN)>=0.) continue;
					float distance=pointPlaneDistance(vP,viewPosition,viewNormal);
					if(distance>maxDistance) break;
					float op=opacity;
					#ifdef DISTANCE_ATTENUATION
						float ratio=1.-(distance/maxDistance);
						float attenuation=ratio*ratio;
						op=opacity*attenuation;
					#endif
					#ifdef FRESNEL
						float fresnelCoe=(dot(viewIncidentDir,viewReflectDir)+1.)/2.;
						op*=fresnelCoe;
					#endif
					vec4 reflectColor=texture2D(tDiffuse,uv);
					gl_FragColor.xyz=reflectColor.xyz * reflectionStrength;
					gl_FragColor.a=op;
					break;
				}
			}
		}
	}
`
  )
}, Jt = {
  defines: {
    PERSPECTIVE_CAMERA: 1
  },
  uniforms: {
    tDepth: { value: null },
    cameraNear: { value: null },
    cameraFar: { value: null }
  },
  vertexShader: (
    /* glsl */
    `

		varying vec2 vUv;

		void main() {

			vUv = uv;
			gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

		}

	`
  ),
  fragmentShader: (
    /* glsl */
    `

		uniform sampler2D tDepth;

		uniform float cameraNear;
		uniform float cameraFar;

		varying vec2 vUv;

		#include <packing>

		float getLinearDepth( const in vec2 uv ) {

			#if PERSPECTIVE_CAMERA == 1

				float fragCoordZ = texture2D( tDepth, uv ).x;
				float viewZ = perspectiveDepthToViewZ( fragCoordZ, cameraNear, cameraFar );
				return viewZToOrthographicDepth( viewZ, cameraNear, cameraFar );

			#else

				return texture2D( tDepth, uv ).x;

			#endif

		}

		void main() {

			float depth = getLinearDepth( vUv );
			float d = 1.0 - depth;
			// d=(d-.999)*1000.;
			gl_FragColor = vec4( vec3( d ), 1.0 );

		}

	`
  )
}, Ye = {
  uniforms: {
    tDiffuse: { value: null },
    resolution: { value: new g() },
    opacity: { value: 0.5 }
  },
  vertexShader: (
    /* glsl */
    `

		varying vec2 vUv;

		void main() {

			vUv = uv;
			gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

		}

	`
  ),
  fragmentShader: (
    /* glsl */
    `

		uniform sampler2D tDiffuse;
		uniform vec2 resolution;
		varying vec2 vUv;
		void main() {
			//reverse engineering from PhotoShop blur filter, then change coefficient

			vec2 texelSize = ( 1.0 / resolution );

			vec4 c=texture2D(tDiffuse,vUv);

			vec2 offset;

			offset=(vec2(-1,0))*texelSize;
			vec4 cl=texture2D(tDiffuse,vUv+offset);

			offset=(vec2(1,0))*texelSize;
			vec4 cr=texture2D(tDiffuse,vUv+offset);

			offset=(vec2(0,-1))*texelSize;
			vec4 cb=texture2D(tDiffuse,vUv+offset);

			offset=(vec2(0,1))*texelSize;
			vec4 ct=texture2D(tDiffuse,vUv+offset);

			// float coeCenter=.5;
			// float coeSide=.125;
			float coeCenter=.2;
			float coeSide=.2;
			float a=c.a*coeCenter+cl.a*coeSide+cr.a*coeSide+cb.a*coeSide+ct.a*coeSide;
			vec3 rgb=(c.rgb*c.a*coeCenter+cl.rgb*cl.a*coeSide+cr.rgb*cr.a*coeSide+cb.rgb*cb.a*coeSide+ct.rgb*ct.a*coeSide)/a;
			gl_FragColor=vec4(rgb,a);

		}
	`
  )
}, nr = {
  log: () => {
  },
  warn: () => {
  },
  error: () => {
  }
};
class fe extends is {
  constructor({ renderer: e, scene: t, camera: r, width: n, height: i, selection: s, bouncing: o = !1, groundReflector: l, composer: u, gBufferTextures: f = null }) {
    super(), this.width = n !== void 0 ? n : 512, this.height = i !== void 0 ? i : 512, this.gBufferTextures = f, this.usingGBuffer = !!(f && f.gNormal && f.gDepth), this.clear = !0, this.renderer = e, this.scene = t, this.camera = r, this.composer = u, this.groundReflector = l, this.opacity = Ae.uniforms.opacity.value, this.output = 0, this.maxDistance = Ae.uniforms.maxDistance.value, this.thickness = Ae.uniforms.thickness.value, this.reflectionStrength = Ae.uniforms.reflectionStrength.value, this.tempColor = new y(), this.externalDepthTexture = null, this.useExternalDepth = !1, this._selection = s || new Se(), this.selective = !0, this._bouncing = o, Object.defineProperty(this, "bouncing", {
      get() {
        return this._bouncing;
      },
      set(m) {
        this.setBouncing(m);
      }
    }), this.blur = !0, this._distanceAttenuation = Ae.defines.DISTANCE_ATTENUATION, Object.defineProperty(this, "distanceAttenuation", {
      get() {
        return this._distanceAttenuation;
      },
      set(m) {
        this._distanceAttenuation !== m && (this._distanceAttenuation = m, this.ssrMaterial.defines.DISTANCE_ATTENUATION = m, this.ssrMaterial.needsUpdate = !0);
      }
    }), this._fresnel = Ae.defines.FRESNEL, Object.defineProperty(this, "fresnel", {
      get() {
        return this._fresnel;
      },
      set(m) {
        this._fresnel !== m && (this._fresnel = m, this.ssrMaterial.defines.FRESNEL = m, this.ssrMaterial.needsUpdate = !0);
      }
    }), this._infiniteThick = Ae.defines.INFINITE_THICK, Object.defineProperty(this, "infiniteThick", {
      get() {
        return this._infiniteThick;
      },
      set(m) {
        this._infiniteThick !== m && (this._infiniteThick = m, this.ssrMaterial.defines.INFINITE_THICK = m, this.ssrMaterial.needsUpdate = !0);
      }
    }), this._reflectionStrength = Ae.uniforms.reflectionStrength.value, Object.defineProperty(this, "reflectionStrength", {
      get() {
        return this._reflectionStrength;
      },
      set(m) {
        this._reflectionStrength !== m && (this._reflectionStrength = m, this.ssrMaterial && (this.ssrMaterial.uniforms.reflectionStrength.value = m));
      }
    });
    const h = new Oe();
    h.type = Qr, h.minFilter = F, h.magFilter = F, this.beautyRenderTarget = new A(this.width, this.height, {
      minFilter: F,
      magFilter: F,
      type: K,
      depthTexture: h,
      depthBuffer: !0
    }), this.prevRenderTarget = new A(this.width, this.height, {
      minFilter: F,
      magFilter: F,
      type: K
    }), this.usingGBuffer ? this.normalRenderTarget = null : this.normalRenderTarget = new A(this.width, this.height, {
      minFilter: F,
      magFilter: F,
      type: K
    }), this.metalnessRenderTarget = new A(this.width, this.height, {
      minFilter: b,
      magFilter: b,
      type: K
    }), this.ssrRenderTarget = new A(this.width, this.height, {
      minFilter: b,
      magFilter: b
    }), this.blurRenderTarget = this.ssrRenderTarget.clone(), this.blurRenderTarget2 = this.ssrRenderTarget.clone(), this.ssrMaterial = new N({
      defines: Object.assign({}, Ae.defines, {
        MAX_STEP: Math.sqrt(this.width * this.width + this.height * this.height)
      }),
      uniforms: oe.clone(Ae.uniforms),
      vertexShader: Ae.vertexShader,
      fragmentShader: Ae.fragmentShader,
      blending: B
    }), u.depthTexture || u.createDepthTexture(), this.ssrMaterial.uniforms.depthTexture = new a(u.depthTexture), this.ssrMaterial.uniforms.tDiffuse.value = this.beautyRenderTarget.texture, this.usingGBuffer ? this.ssrMaterial.uniforms.tNormal.value = this.gBufferTextures.gNormal : this.ssrMaterial.uniforms.tNormal.value = this.normalRenderTarget?.texture, this.ssrMaterial.defines.SELECTIVE = this.selective, this.ssrMaterial.needsUpdate = !0, this.ssrMaterial.uniforms.tMetalness.value = this.metalnessRenderTarget.texture, this.usingGBuffer && this.gBufferTextures.gDepth ? this.ssrMaterial.uniforms.tDepth.value = this.gBufferTextures.gDepth : this.ssrMaterial.uniforms.tDepth.value = this.beautyRenderTarget.depthTexture, this.ssrMaterial.uniforms.cameraNear.value = this.camera.near, this.ssrMaterial.uniforms.cameraFar.value = this.camera.far, this.ssrMaterial.uniforms.thickness.value = this.thickness, this.ssrMaterial.uniforms.resolution.value.set(this.width, this.height), this.ssrMaterial.uniforms.cameraProjectionMatrix.value.copy(this.camera.projectionMatrix), this.ssrMaterial.uniforms.cameraInverseProjectionMatrix.value.copy(this.camera.projectionMatrixInverse), this.ssrMaterial.uniforms.reflectionStrength.value = this.reflectionStrength, this.renderTargetMask = new A(this.width, this.height, {
      minFilter: F,
      magFilter: F,
      type: K,
      depthBuffer: !0
    }), this.renderTargetMask.texture.name = "SSR.Mask", this.depthPass = new Me(t, r), this.depthMaskMaterial = new _e(), this.depthMaskMaterial.copyCameraSettings(r), this.depthMaskMaterial.depthBuffer0 = u.depthTexture, this.depthMaskMaterial.depthPacking0 = $, this.depthMaskMaterial.depthBuffer1 = this.depthPass.texture, this.depthMaskMaterial.depthPacking1 = ce, this.depthMaskMaterial.depthMode = Ue, this.depthMaskMaterial.epsilon = 9e-6, this.maskPass = new ee(this.depthMaskMaterial), this.maskPass.clear = !0, this._inverted = !1, this._ignoreBackground = !1, this.normalMaterial = new Kr(), this.normalMaterial.blending = B, this.metalnessOnMaterial = new gt({
      color: "white"
    }), this.metalnessOffMaterial = new gt({
      color: "black"
    }), this.blurMaterial = new N({
      defines: Object.assign({}, Ye.defines),
      uniforms: oe.clone(Ye.uniforms),
      vertexShader: Ye.vertexShader,
      fragmentShader: Ye.fragmentShader
    }), this.blurMaterial.uniforms.tDiffuse.value = this.ssrRenderTarget.texture, this.blurMaterial.uniforms.resolution.value.set(this.width, this.height), this.blurMaterial2 = new N({
      defines: Object.assign({}, Ye.defines),
      uniforms: oe.clone(Ye.uniforms),
      vertexShader: Ye.vertexShader,
      fragmentShader: Ye.fragmentShader
    }), this.blurMaterial2.uniforms.tDiffuse.value = this.blurRenderTarget.texture, this.blurMaterial2.uniforms.resolution.value.set(this.width, this.height), this.depthRenderMaterial = new N({
      defines: Object.assign({}, Jt.defines),
      uniforms: oe.clone(Jt.uniforms),
      vertexShader: Jt.vertexShader,
      fragmentShader: Jt.fragmentShader,
      blending: B
    }), this.depthRenderMaterial.uniforms.tDepth.value = this.beautyRenderTarget.depthTexture, this.depthRenderMaterial.uniforms.cameraNear.value = this.camera.near, this.depthRenderMaterial.uniforms.cameraFar.value = this.camera.far, this.copyMaterial = new N({
      uniforms: oe.clone(vt.uniforms),
      vertexShader: vt.vertexShader,
      fragmentShader: vt.fragmentShader,
      transparent: !0,
      depthTest: !1,
      depthWrite: !1,
      blendSrc: ar,
      blendDst: sr,
      blendEquation: Lt,
      blendSrcAlpha: ar,
      blendDstAlpha: sr,
      blendEquationAlpha: Lt
      // premultipliedAlpha:true,
    }), this.fsQuad = new ss(null), this.originalClearColor = new y();
  }
  dispose() {
    this.beautyRenderTarget?.dispose(), this.prevRenderTarget?.dispose(), this.normalRenderTarget?.dispose(), this.metalnessRenderTarget?.dispose(), this.ssrRenderTarget?.dispose(), this.blurRenderTarget?.dispose(), this.blurRenderTarget2?.dispose(), this.normalMaterial.dispose(), this.metalnessOnMaterial.dispose(), this.metalnessOffMaterial.dispose(), this.blurMaterial.dispose(), this.blurMaterial2.dispose(), this.copyMaterial.dispose(), this.depthRenderMaterial.dispose(), this.fsQuad.dispose();
  }
  renderOpts = {
    projectObject: !0,
    // 控制是否执行对象投影
    updateMatrixWorld: !1,
    useProgramCache: !1
    // 控制是否使用着色器缓存和快速路径
  };
  render(e, t, r, n, i, s, o) {
    const l = this.scene.matrixAutoUpdate, u = this.camera.matrixAutoUpdate, f = e.shadowMap.needsUpdate;
    this.scene.matrixAutoUpdate = !1, this.camera.matrixAutoUpdate = !1, e.shadowMap.needsUpdate = !1, this.originalClearColor.copy(e.getClearColor(this.tempColor)), e.getRenderTarget();
    const h = this.scene.background;
    this.scene.background = null, this.groundReflector && (this.groundReflector.visible = !1), this.usingGBuffer || this.renderOverride(
      e,
      this.normalMaterial,
      this.normalRenderTarget,
      0,
      0,
      o
    ), this.depthMaskMaterial.depthBuffer0 = this.composer.depthTexture, this.depthMaskMaterial.inputBuffer = r.texture;
    const m = this.camera.layers.mask;
    this.camera.layers.set(this._selection.layer);
    const p = [];
    switch (this._selection.forEach((d) => {
      d.children.length > 0 && d.traverse((v) => {
        v.isMesh && !v.layers.isEnabled(this._selection.layer) && v.visible && (p.push({
          model: v,
          oldLayerMask: v.layers.mask
          // 保存原始的完整层掩码
        }), v.layers.set(this._selection.layer));
      });
    }), this.depthPass.render(e, r, void 0, void 0, void 0, void 0, {
      ...Je.opts.getDepthParamsOpts()
    }), this.ssrMaterial.uniforms.depthPass1 = new a(this.depthPass.renderTarget.texture), this.camera.layers.mask = m, p.forEach(({ model: d, oldLayerMask: v }) => {
      d.layers.mask = v;
    }), e.setRenderTarget(this.renderTargetMask), e.clear(!0, !0, !0), this.maskPass.render(e, r, this.renderTargetMask, void 0, void 0, {
      projectObject: !0,
      updateMatrixWorld: !1,
      useProgramCache: !1,
      ...Je.getSubOpths(!1)
    }), this.scene.background = h, this.ssrMaterial.uniforms.maskTexture = {
      value: this.renderTargetMask.texture
    }, this.ssrMaterial.uniforms.opacity.value = this.opacity, this.ssrMaterial.uniforms.maxDistance.value = this.maxDistance, this.ssrMaterial.uniforms.thickness.value = this.thickness, this.ssrMaterial.uniforms.reflectionStrength.value = this.reflectionStrength, this.ssrMaterial.uniforms.maskTexture = { value: this.renderTargetMask.texture }, this.ssrMaterial.uniforms.maskThreshold = { value: this.maskThreshold }, this.ssrMaterial.defines.SELECTIVE = !0, this.ssrMaterial.needsUpdate = !0, this.useExternalDepth && this.externalDepthTexture ? this.ssrMaterial.uniforms.tDepth.value = this.externalDepthTexture : this.usingGBuffer && this.gBufferTextures.gDepth ? this.ssrMaterial.uniforms.tDepth.value = this.gBufferTextures.gDepth : this.ssrMaterial.uniforms.tDepth.value = this.composer.depthTexture, this.renderPass(e, this.ssrMaterial, this.ssrRenderTarget), this.blur && (this.renderPass(e, this.blurMaterial, this.blurRenderTarget), this.renderPass(e, this.blurMaterial2, this.blurRenderTarget2)), this.output) {
      case fe.OUTPUT.Default:
        this.bouncing ? (this.copyMaterial.uniforms.tDiffuse.value = r.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.prevRenderTarget), this.blur ? this.copyMaterial.uniforms.tDiffuse.value = this.blurRenderTarget2.texture : this.copyMaterial.uniforms.tDiffuse.value = this.ssrRenderTarget.texture, this.copyMaterial.blending = pt, this.renderPass(e, this.copyMaterial, this.prevRenderTarget), this.copyMaterial.uniforms.tDiffuse.value = this.prevRenderTarget.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t)) : (this.copyMaterial.uniforms.tDiffuse.value = r.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t), this.blur ? this.copyMaterial.uniforms.tDiffuse.value = this.blurRenderTarget2.texture : this.copyMaterial.uniforms.tDiffuse.value = this.ssrRenderTarget.texture, this.copyMaterial.blending = pt, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t));
        break;
      case fe.OUTPUT.SSR:
        this.blur ? this.copyMaterial.uniforms.tDiffuse.value = this.blurRenderTarget2.texture : this.copyMaterial.uniforms.tDiffuse.value = this.ssrRenderTarget.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t), this.bouncing && (this.blur ? this.copyMaterial.uniforms.tDiffuse.value = this.blurRenderTarget2.texture : this.copyMaterial.uniforms.tDiffuse.value = this.beautyRenderTarget.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.prevRenderTarget), this.copyMaterial.uniforms.tDiffuse.value = this.ssrRenderTarget.texture, this.copyMaterial.blending = pt, this.renderPass(e, this.copyMaterial, this.prevRenderTarget));
        break;
      case fe.OUTPUT.Beauty:
        this.copyMaterial.uniforms.tDiffuse.value = this.beautyRenderTarget.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t);
        break;
      case fe.OUTPUT.Depth:
        this.usingGBuffer ? (this.gBufferDepthMaterial || (this.gBufferDepthMaterial = new N({
          uniforms: {
            tDepth: { value: null },
            cameraNear: { value: this.camera.near },
            cameraFar: { value: this.camera.far }
          },
          vertexShader: (
            /* glsl */
            `
								varying vec2 vUv;
								void main() {
									vUv = uv;
									gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
								}
							`
          ),
          fragmentShader: (
            /* glsl */
            `
								uniform sampler2D tDepth;
								uniform float cameraNear;
								uniform float cameraFar;
								varying vec2 vUv;
								
								void main() {
									// 读取G-Buffer深度值（已经是归一化的线性深度）
									float depth = texture2D(tDepth, vUv).r;
									
									// 增强对比度以便更好地可视化
									depth = pow(depth, 0.5);
									
									gl_FragColor = vec4(vec3(depth), 1.0);
								}
							`
          ),
          depthTest: !1,
          depthWrite: !1
        })), this.gBufferDepthMaterial.uniforms.tDepth.value = this.gBufferTextures.gDepth, this.gBufferDepthMaterial.uniforms.cameraNear.value = this.camera.near, this.gBufferDepthMaterial.uniforms.cameraFar.value = this.camera.far, this.renderPass(e, this.gBufferDepthMaterial, this.renderToScreen ? null : t)) : (this.depthRenderMaterial.uniforms.tDepth.value = this.composer.depthTexture, this.renderPass(e, this.depthRenderMaterial, this.renderToScreen ? null : t));
        break;
      case fe.OUTPUT.Normal:
        this.usingGBuffer ? this.copyMaterial.uniforms.tDiffuse.value = this.gBufferTextures.gNormal : this.copyMaterial.uniforms.tDiffuse.value = this.normalRenderTarget.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t);
        break;
      case fe.OUTPUT.Metalness:
        this.copyMaterial.uniforms.tDiffuse.value = this.metalnessRenderTarget.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t);
        break;
      case fe.OUTPUT.Mask:
        this.copyMaterial.uniforms.tDiffuse.value = this.depthPass.renderTarget.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t);
        break;
      case fe.OUTPUT.Debug:
        this.renderPass(e, this.ssrMaterial, this.renderToScreen ? null : t);
        break;
      case fe.OUTPUT.GBufferColor:
        this.usingGBuffer && this.gBufferTextures && this.gBufferTextures.gColor ? (this.copyMaterial.uniforms.tDiffuse.value = this.gBufferTextures.gColor, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t)) : (this.copyMaterial.uniforms.tDiffuse.value = this.beautyRenderTarget.texture, this.copyMaterial.blending = B, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t));
        break;
      case fe.OUTPUT.GBufferPosition:
        this.usingGBuffer && this.gBufferTextures && this.gBufferTextures.gPosition ? (this.gBufferPositionMaterial || (this.gBufferPositionMaterial = new N({
          uniforms: {
            tDiffuse: { value: null },
            cameraNear: { value: 0.1 },
            cameraFar: { value: 1e3 }
          },
          vertexShader: (
            /* glsl */
            `
								varying vec2 vUv;
								void main() {
									vUv = uv;
									gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
								}
							`
          ),
          fragmentShader: (
            /* glsl */
            `
								uniform sampler2D tDiffuse;
								uniform float cameraNear;
								uniform float cameraFar;
								varying vec2 vUv;
								
								void main() {
									vec4 positionData = texture2D(tDiffuse, vUv);
									vec3 viewPosition = positionData.xyz;
									
									// 将视图空间位置转换为可视化的颜色
									// 使用距离来着色
									float distance = length(viewPosition);
									float normalizedDistance = (distance - cameraNear) / (cameraFar - cameraNear);
									normalizedDistance = clamp(normalizedDistance, 0.0, 1.0);
									
									// 创建彩色可视化
									vec3 color = vec3(
										normalizedDistance,
										1.0 - normalizedDistance,
										0.5
									);
									
									gl_FragColor = vec4(color, 1.0);
								}
							`
          )
        })), this.gBufferPositionMaterial.uniforms.tDiffuse.value = this.gBufferTextures.gPosition, this.gBufferPositionMaterial.uniforms.cameraNear.value = this.camera.near, this.gBufferPositionMaterial.uniforms.cameraFar.value = this.camera.far, this.renderPass(e, this.gBufferPositionMaterial, this.renderToScreen ? null : t)) : this.usingGBuffer && this.gBufferTextures && this.gBufferTextures.gDepth && (this.gBufferDepthMaterial || (this.gBufferDepthMaterial = new N({
          uniforms: { tDiffuse: { value: null } },
          vertexShader: `
									varying vec2 vUv;
									void main() {
										vUv = uv;
										gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
									}
								`,
          fragmentShader: `
									uniform sampler2D tDiffuse;
									varying vec2 vUv;
									void main() {
										float depth = texture2D(tDiffuse, vUv).r;
										gl_FragColor = vec4(depth, depth, depth, 1.0);
									}
								`
        })), this.gBufferDepthMaterial.uniforms.tDiffuse.value = this.gBufferTextures.gDepth, this.renderPass(e, this.gBufferDepthMaterial, this.renderToScreen ? null : t));
        break;
      case fe.OUTPUT.GBufferObjectId:
        if (this.usingGBuffer && this.gBufferTextures && this.gBufferTextures.gObjectId)
          this.gBufferObjectIdMaterial || (this.gBufferObjectIdMaterial = new N({
            uniforms: {
              tDiffuse: { value: null },
              visualizationMode: { value: 0 }
              // 0=彩色, 1=灰度
            },
            vertexShader: (
              /* glsl */
              `
						varying vec2 vUv;
						void main() {
							vUv = uv;
							gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
						}
					`
            ),
            fragmentShader: (
              /* glsl */
              `
						uniform sampler2D tDiffuse;
						uniform int visualizationMode;
						varying vec2 vUv;
						
						// HSV转RGB函数
						vec3 hsv2rgb(vec3 c) {
							vec4 K = vec4(1.0, 2.0 / 3.0, 1.0 / 3.0, 3.0);
							vec3 p = abs(fract(c.xxx + K.xyz) * 6.0 - K.www);
							return c.z * mix(K.xxx, clamp(p - K.xxx, 0.0, 1.0), c.y);
						}
						
							void main() {
								vec4 objectIdData = texture2D(tDiffuse, vUv);
								float normalizedId = objectIdData.r; // 对象ID已经归一化到[0,1]
								
								// 临时调试：放大ID值显示
								float amplifiedId = normalizedId * 10.0; // 放大10倍
								
								if (normalizedId < 0.001) {
									// 背景（ID=0）显示为深蓝色
									gl_FragColor = vec4(0.0, 0.0, 0.3, 1.0);
								} else {
									if (visualizationMode == 0) {
										// 彩色模式：使用HSV色相环为不同ID生成不同颜色
										float hue = fract(normalizedId * 50.0); // 增加颜色差异
										vec3 hsvColor = vec3(hue, 1.0, 1.0); // 满饱和度和亮度
										vec3 rgbColor = hsv2rgb(hsvColor);
										gl_FragColor = vec4(rgbColor, 1.0);
									} else {
										// 灰度模式：放大ID值以供调试
										gl_FragColor = vec4(amplifiedId, amplifiedId, amplifiedId, 1.0);
									}
								}
							}
					`
            )
          })), this.gBufferObjectIdMaterial.uniforms.tDiffuse.value = this.gBufferTextures.gObjectId, this.renderPass(e, this.gBufferObjectIdMaterial, this.renderToScreen ? null : t);
        else {
          this.copyMaterial.uniforms.tDiffuse.value = null, this.copyMaterial.blending = B;
          const d = document.createElement("canvas");
          d.width = d.height = 1;
          const v = d.getContext("2d");
          v.fillStyle = "black", v.fillRect(0, 0, 1, 1);
          const x = new Si(d);
          this.copyMaterial.uniforms.tDiffuse.value = x, this.renderPass(e, this.copyMaterial, this.renderToScreen ? null : t);
        }
        break;
    }
    e.setRenderTarget(this.ssrRenderTarget), this.scene.matrixAutoUpdate = l, this.camera.matrixAutoUpdate = u, e.shadowMap.needsUpdate = f;
  }
  renderPass(e, t, r, n, i) {
    this.originalClearColor.copy(e.getClearColor(this.tempColor));
    const s = e.getClearAlpha(this.tempColor), o = e.autoClear;
    e.setRenderTarget(r), e.autoClear = !1, n != null && (e.setClearColor(n), e.setClearAlpha(i || 0), e.clear()), this.fsQuad.material = t, this.fsQuad.render(e, {
      projectObject: !0,
      updateMatrixWorld: !1,
      useProgramCache: !1
    }), e.autoClear = o, e.setClearColor(this.originalClearColor), e.setClearAlpha(s);
  }
  renderOverride(e, t, r, n, i, s) {
    this.originalClearColor.copy(e.getClearColor(this.tempColor));
    const o = e.getClearAlpha(this.tempColor), l = e.autoClear;
    e.setRenderTarget(r), e.autoClear = !1, n = t.clearColor || n, i = t.clearAlpha || i, n != null && (e.setClearColor(n), e.setClearAlpha(i || 0), e.clear()), this.scene.overrideMaterial = t, e.shadowMap.autoUpdate = !1, e.render(
      this.scene,
      this.camera,
      Je.getStandardOpts(
        {
          projectObject: !0,
          updateMatrixWorld: !1,
          useProgramCache: !1
        },
        {
          subOptsState: !1
        }
      )
    ), this.scene.overrideMaterial = null, e.autoClear = l, e.setClearColor(this.originalClearColor), e.setClearAlpha(o);
  }
  renderMetalness(e, t, r, n, i) {
    this.originalClearColor.copy(e.getClearColor(this.tempColor));
    const s = e.getClearAlpha(this.tempColor), o = e.autoClear;
    e.setRenderTarget(r), e.autoClear = !1, n = t.clearColor || n, i = t.clearAlpha || i, n != null && (e.setClearColor(n), e.setClearAlpha(i || 0), e.clear());
    const l = Array.from(this._selection);
    this.scene.traverseVisible((f) => {
      f._SSRPassBackupMaterial = f.material, l.includes(f) ? f.material = this.metalnessOnMaterial : f.material = this.metalnessOffMaterial;
    }), e.shadowMap.autoUpdate = !1;
    const u = this.scene.matrixWorldAutoUpdate;
    this.scene.matrixWorldAutoUpdate = !1, e.render(this.scene, this.camera), this.scene.matrixWorldAutoUpdate = u, this.scene.traverseVisible((f) => {
      f.material = f._SSRPassBackupMaterial;
    }), e.autoClear = o, e.setClearColor(this.originalClearColor), e.setClearAlpha(s);
  }
  setSize(e, t) {
    this.width = e, this.height = t, this.ssrMaterial.defines.MAX_STEP = Math.sqrt(e * e + t * t), this.ssrMaterial.needsUpdate = !0, this.beautyRenderTarget.setSize(e, t), this.prevRenderTarget.setSize(e, t), this.ssrRenderTarget.setSize(e, t), this.normalRenderTarget && this.normalRenderTarget.setSize(e, t), this.metalnessRenderTarget.setSize(e, t), this.blurRenderTarget.setSize(e, t), this.blurRenderTarget2.setSize(e, t), this.renderTargetMask && this.renderTargetMask.setSize(e, t), this.depthPass && this.depthPass.setSize(e, t), this.maskPass && this.maskPass.setSize(e, t), this.ssrMaterial.uniforms.resolution.value.set(e, t), this.ssrMaterial.uniforms.cameraProjectionMatrix.value.copy(this.camera.projectionMatrix), this.ssrMaterial.uniforms.cameraInverseProjectionMatrix.value.copy(this.camera.projectionMatrixInverse), this.blurMaterial.uniforms.resolution.value.set(e, t), this.blurMaterial2.uniforms.resolution.value.set(e, t);
  }
  /**
   * 设置外部深度纹理
   * @param {DepthTexture} depthTexture - 外部深度纹理
   * @param {number} depthPacking - 深度打包格式 (e.g., BasicDepthPacking)
   */
  setDepthTexture(e, t) {
    e ? (this.externalDepthTexture = e, this.useExternalDepth = !0, this.ssrMaterial && (this.ssrMaterial.uniforms.tDepth.value = e), this.depthRenderMaterial && (this.depthRenderMaterial.uniforms.tDepth.value = e)) : (this.useExternalDepth = !1, this.ssrMaterial && this.beautyRenderTarget && (this.ssrMaterial.uniforms.tDepth.value = this.beautyRenderTarget.depthTexture), this.depthRenderMaterial && this.beautyRenderTarget && (this.depthRenderMaterial.uniforms.tDepth.value = this.beautyRenderTarget.depthTexture));
  }
  /**
   * 刷新反射效果，适用于修改selects后需要重新计算反射的情况
   * 临时开启bouncing，渲染一帧，然后自动关闭
   * @param {WebGLRenderer} renderer - 渲染器
   * @param {WebGLRenderTarget} writeBuffer - 写入缓冲区
   * @param {number} [refreshFrames=1] - 刷新帧数，默认为1
   * @returns {Promise} 返回一个Promise，当刷新完成时解析
   */
  refring = !1;
  refreshReflection() {
    if (this.refring)
      return;
    this.refring = !0;
    const e = this._bouncing;
    this.setBouncing(!0), setTimeout(() => {
      this.setBouncing(e), this.refring = !1;
    });
  }
  setBouncing(e) {
    this._bouncing !== e && (this._bouncing = e, e ? this.ssrMaterial.uniforms.tDiffuse.value = this.prevRenderTarget.texture : this.ssrMaterial.uniforms.tDiffuse.value = this.beautyRenderTarget.texture);
  }
  /**
   * 指示遮罩是否应该反转（反转选择物体的反射效果）
   *
   * @type {Boolean}
   */
  get inverted() {
    return this._inverted;
  }
  set inverted(e) {
    this._inverted = e, this.depthMaskMaterial && (this.depthMaskMaterial.depthMode = e ? Mt : Ue);
  }
  /**
   * 指示是否忽略背景（不对背景应用反射效果）
   *
   * @type {Boolean}
   */
  get ignoreBackground() {
    return this._ignoreBackground;
  }
  set ignoreBackground(e) {
    this._ignoreBackground = e, this.depthMaskMaterial && (this.depthMaskMaterial.maxDepthStrategy = e ? we.DISCARD_MAX_DEPTH : we.KEEP_MAX_DEPTH);
  }
  /**
   * 向选择集添加对象
   * @param {THREE.Object3D} object - 要添加的对象
   */
  addToSelection(e) {
    e && this._selection && typeof this._selection.add == "function" && (this._selection.add(e), nr.log(`已添加对象到SSR选择集: ${e.name || e.uuid}`));
  }
  /**
   * 从选择集中移除对象
   * @param {THREE.Object3D} object - 要移除的对象
   */
  removeFromSelection(e) {
    e && this._selection && typeof this._selection.delete == "function" && (this._selection.delete(e), nr.log(`已从SSR选择集移除对象: ${e.name || e.uuid}`));
  }
  /**
   * 清空选择集
   */
  clearSelection() {
    this._selection && typeof this._selection.clear == "function" && this._selection.clear();
  }
  /**
   * 切换对象在选择集中的状态
   * @param {THREE.Object3D} object - 要切换状态的对象
   */
  toggleSelection(e) {
    e && this._selection && (this._selection.has(e) ? this.removeFromSelection(e) : this.addToSelection(e));
  }
  /**
   * 获取当前选择集中的所有对象
   * @returns {Array} - 选择集中的对象数组
   */
  getSelectionItems() {
    return this._selection ? Array.isArray(this._selection.items) ? this._selection.items : Array.isArray(this._selection.objects) ? this._selection.objects : typeof this._selection.getItems == "function" ? this._selection.getItems() : typeof this._selection.getSelection == "function" ? this._selection.getSelection() : typeof this._selection[Symbol.iterator] == "function" ? Array.from(this._selection) : [] : [];
  }
  /**
   * 初始化效果
   *
   * @param {WebGLRenderer} renderer - 渲染器
   * @param {Boolean} alpha - 是否有alpha通道
   * @param {Number} frameBufferType - 帧缓冲区类型
   */
  initialize(e, t, r) {
    this.depthPass && this.depthPass.initialize(e, t, r), this.maskPass && this.maskPass.initialize(e, t, r), e.capabilities.logarithmicDepthBuffer && this.depthMaskMaterial && (this.depthMaskMaterial.defines.LOG_DEPTH = "1", this.depthMaskMaterial.needsUpdate = !0);
  }
  /**
   * 基于金属度自动选择对象并添加到selection
   * @param {Number} threshold - 金属度阈值，超过该值的对象将被选中
   */
  updateSelectionBasedOnMetalness(e = 0.5) {
    !this._selection || !this.scene || (this.clearSelection(), this.scene.traverseVisible((t) => {
      if (t.isMesh && t.material) {
        let r = 0;
        if (!Array.isArray(t.material))
          t.material.metalness !== void 0 && (r = t.material.metalness);
        else {
          let n = 0, i = 0;
          for (const s of t.material)
            s.metalness !== void 0 && (n += s.metalness, i++);
          i > 0 && (r = n / i);
        }
        r >= e && this.addToSelection(t);
      }
    }), nr.log(`基于金属度 >= ${e} 更新选择，共选中 ${this.getSelectionItems().length} 个对象`));
  }
  /**
   * 设置金属度阈值，并更新选择
   * @param {Number} threshold - 金属度阈值
   */
  setMetalnessThreshold(e) {
    this.updateSelectionBasedOnMetalness(e);
  }
  /**
   * 设置是否高亮显示选中的对象
   * @param {Boolean} highlight - 是否高亮显示
   * @param {Number} [emissiveValue=2.0] - 高亮的发光值
   */
  setHighlightSelected(e, t = 2) {
    const r = this.getSelectionItems();
    this.originalEmissives || (this.originalEmissives = /* @__PURE__ */ new Map()), r.forEach((n) => {
      if (n.isMesh && n.material) {
        if (!this.originalEmissives.has(n.uuid))
          if (Array.isArray(n.material)) {
            const i = [];
            n.material.forEach((s) => {
              s.emissive ? i.push(s.emissive.clone()) : i.push(null);
            }), this.originalEmissives.set(n.uuid, i);
          } else n.material.emissive && this.originalEmissives.set(n.uuid, n.material.emissive.clone());
        if (Array.isArray(n.material))
          n.material.forEach((i, s) => {
            if (i.emissive)
              if (e)
                i.emissive.set(t, t, t);
              else {
                const o = this.originalEmissives.get(n.uuid);
                o && o[s] && i.emissive.copy(o[s]);
              }
          });
        else if (n.material.emissive)
          if (e)
            n.material.emissive.set(t, t, t);
          else {
            const i = this.originalEmissives.get(n.uuid);
            i && n.material.emissive.copy(i);
          }
      }
    });
  }
  /**
   * 设置掩码阈值
   * @param {Number} threshold - 掩码阈值
   */
  setMaskThreshold(e) {
    this.ssrMaterial && (this.ssrMaterial.uniforms.maskThreshold.value = e);
  }
}
fe.OUTPUT = {
  Default: 0,
  SSR: 1,
  Beauty: 3,
  Depth: 4,
  Normal: 5,
  Metalness: 7,
  Mask: 8,
  Debug: 9,
  // 新增Debug模式用于直接显示着色器效果
  GBufferColor: 10,
  // G-Buffer颜色通道
  GBufferPosition: 11,
  // G-Buffer位置通道
  GBufferObjectId: 12
  // G-Buffer对象ID通道
};
fe.prototype.setOutputMode = function(c) {
  this.output = c, nr.log(`已设置输出模式：${Object.keys(fe.OUTPUT).find((e) => fe.OUTPUT[e] === c) || "未知"}`);
};
fe.prototype.cycleOutputMode = function() {
  const c = Object.values(fe.OUTPUT), t = (c.indexOf(this.output) + 1) % c.length;
  this.setOutputMode(c[t]);
};
function kf(c) {
  const e = [], t = Math.PI * (3 - Math.sqrt(5)), r = 2 / c;
  for (let n = 0; n < c; n++) {
    const i = n * r - 1 + r / 2, s = Math.sqrt(1 - i * i), o = n * t;
    e.push(new T(Math.cos(o) * s, i, Math.sin(o) * s));
  }
  return e;
}
const Vf = (
  /* glsl */
  `
#define GLSLIFY 1
varying vec2 vUv;

uniform sampler2D depthTexture;
uniform sampler2D normalTexture;
uniform sampler2D colorTexture;

uniform mat4 projectionViewMatrix;
uniform mat4 cameraMatrixWorld;
uniform vec2 blueNoiseRepeat;
uniform vec2 resolution;

uniform mat4 projectionMatrixInverse;
uniform float aoDistance;
uniform float distancePower;
uniform float cameraNear;
uniform float cameraFar;
uniform float bias;
uniform float thickness;
uniform int frame;
uniform vec3 samples[16];
uniform float samplesR[16];

// 间接光相关参数
uniform float indirectLightIntensity;
uniform float indirectLightDistance;
uniform bool colorBleeding;

#include <common>
#include <packing>
${it}

// 从深度获取世界空间位置
vec3 getWorldPos(const float depth, const vec2 coord) {
    float z = depth * 2.0 - 1.0;
    vec4 clipSpacePosition = vec4(coord * 2.0 - 1.0, z, 1.0);
    vec4 viewSpacePosition = projectionMatrixInverse * clipSpacePosition;
    vec4 worldSpacePosition = cameraMatrixWorld * viewSpacePosition;
    worldSpacePosition.xyz /= worldSpacePosition.w;
    return worldSpacePosition.xyz;
}

// 计算法线
vec3 computeNormal(vec3 worldPos, vec2 vUv) {
    vec2 size = vec2(textureSize(depthTexture, 0));
    ivec2 p = ivec2(vUv * size);
    
    float c0 = texelFetch(depthTexture, p, 0).x;
    float l2 = texelFetch(depthTexture, p - ivec2(2, 0), 0).x;
    float l1 = texelFetch(depthTexture, p - ivec2(1, 0), 0).x;
    float r1 = texelFetch(depthTexture, p + ivec2(1, 0), 0).x;
    float r2 = texelFetch(depthTexture, p + ivec2(2, 0), 0).x;
    float b2 = texelFetch(depthTexture, p - ivec2(0, 2), 0).x;
    float b1 = texelFetch(depthTexture, p - ivec2(0, 1), 0).x;
    float t1 = texelFetch(depthTexture, p + ivec2(0, 1), 0).x;
    float t2 = texelFetch(depthTexture, p + ivec2(0, 2), 0).x;
    
    float dl = abs((2.0 * l1 - l2) - c0);
    float dr = abs((2.0 * r1 - r2) - c0);
    float db = abs((2.0 * b1 - b2) - c0);
    float dt = abs((2.0 * t1 - t2) - c0);
    
    vec3 ce = getWorldPos(c0, vUv).xyz;
    
    vec3 dpdx = (dl < dr) ? 
        ce - getWorldPos(l1, (vUv - vec2(1.0 / size.x, 0.0))).xyz : 
        -ce + getWorldPos(r1, (vUv + vec2(1.0 / size.x, 0.0))).xyz;
    
    vec3 dpdy = (db < dt) ?
        ce - getWorldPos(b1, (vUv - vec2(0.0, 1.0 / size.y))).xyz : 
        -ce + getWorldPos(t1, (vUv + vec2(0.0, 1.0 / size.y))).xyz;
    
    return normalize(cross(dpdx, dpdy));
}

// 线性化深度
highp float linearize_depth(highp float d, highp float zNear, highp float zFar) {
    highp float z_n = 2.0 * d - 1.0;
    return 2.0 * zNear * zFar / (zFar + zNear - z_n * (zFar - zNear));
}

void main() {
    float depth = textureLod(depthTexture, vUv, 0.).x;
    
    // 跳过天空盒
    if (depth == 1.0) {
        discard;
        return;
    }
    
    vec3 worldPos = getWorldPos(depth, vUv);
    vec3 normal = computeNormal(worldPos, vUv);
    
    // 随机数生成
    #ifdef animatedNoise
        int seed = frame;
    #else
        int seed = 0;
    #endif
    
    vec4 noise = sampleBlueNoise(blueNoiseTexture, seed, blueNoiseRepeat, resolution);
    
    // 创建TBN矩阵
    vec3 randomVec = normalize(noise.rgb * 2.0 - 1.0);
    vec3 tangent = normalize(randomVec - normal * dot(randomVec, normal));
    vec3 bitangent = cross(normal, tangent);
    mat3 tbn = mat3(tangent, bitangent, normal);
    
    // 结果累积
    float occluded = 0.0;
    float totalWeight = 0.0;
    vec3 indirectLight = vec3(0.0);
    vec3 samplePos;
    
    float sppF = float(spp);
    
    for (float i = 0.0; i < sppF; i++) {
        // 获取采样方向
        vec3 sampleDirection = tbn * samples[int(i)];
        
        // 确保采样点在法线半球
        if (dot(sampleDirection, normal) < 0.0) 
            sampleDirection *= -1.0;
        
        // 随机化采样距离
        float moveAmt = samplesR[int(mod(i + noise.a * sppF, sppF))];
        samplePos = worldPos + aoDistance * moveAmt * sampleDirection;
        
        // 投影到屏幕空间
        vec4 offset = projectionViewMatrix * vec4(samplePos, 1.0);
        offset.xyz /= offset.w;
        offset.xyz = offset.xyz * 0.5 + 0.5;
        
        // 获取样本深度
        float sampleDepth = textureLod(depthTexture, offset.xy, 0.0).x;
        float distSample = linearize_depth(sampleDepth, cameraNear, cameraFar);
        float distWorld = linearize_depth(offset.z, cameraNear, cameraFar);
        
        // 范围检查
        float rangeCheck = smoothstep(0.0, 1.0, aoDistance / (aoDistance * abs(distSample - distWorld)));
        rangeCheck = pow(rangeCheck, distancePower);
        
        // 权重计算
        float weight = dot(sampleDirection, normal);
        float isOccluded = sampleDepth < distWorld - bias ? 1.0 : 0.0;
        
        // 累积遮蔽
        occluded += rangeCheck * weight * isOccluded;
        totalWeight += weight;
        
        // 添加间接光 - 移除条件检查让间接光始终计算
        if (colorBleeding && isOccluded > 0.5) {
            vec3 sampleColor = texture2D(colorTexture, offset.xy).rgb;
            indirectLight += sampleColor * weight * rangeCheck;
        }
    }
    
    // 归一化结果
    float occ = clamp(1.0 - occluded / totalWeight, 0.0, 1.0);
    indirectLight = indirectLight * indirectLightIntensity / sppF;
    
    // 输出: r通道为AO值，gba通道为间接光
    gl_FragColor = vec4(1.0 - occ, indirectLight);
}
`
);
class Hf extends Zi {
  constructor(e, t) {
    const r = Vf;
    super(e, t, r), this.fullscreenMaterial.uniforms.colorTexture = { value: null }, this.fullscreenMaterial.uniforms.indirectLightIntensity = { value: 1 }, this.fullscreenMaterial.uniforms.indirectLightDistance = { value: 1 }, this.fullscreenMaterial.uniforms.colorBleeding = { value: !0 }, this.fullscreenMaterial.defines.spp = "16", this.fullscreenMaterial.defines.USE_COLOR_TEXTURE = "", this.fullscreenMaterial.needsUpdate = !0, this.setSamples(16);
  }
  setSamples(e) {
    const t = kf(e), r = [];
    for (let n = 0; n < e; n++)
      r.push((n + 1) / e);
    this.fullscreenMaterial.uniforms.samples = { value: t }, this.fullscreenMaterial.uniforms.samplesR = { value: r }, this.fullscreenMaterial.defines.spp = e.toFixed(0), this.fullscreenMaterial.needsUpdate = !0;
  }
}
class Gf extends yt {
  /**
   * 创建SSDO效果
   *
   * @param {Camera} camera - 相机
   * @param {Texture} normalBuffer - 场景法线纹理
   * @param {Texture} colorBuffer - 场景颜色纹理
   * @param {Object} options - 配置选项
   */
  constructor(e, t, r, n = {}) {
    n = {
      ...{
        ...yt.DefaultOptions,
        indirectLightIntensity: 1,
        indirectLightDistance: 1,
        colorBleeding: !0,
        spp: 16,
        rings: 7
      },
      ...n
    };
    const s = new Hf(t, r);
    super(e, t, r, s, n), this.uniforms.set("indirectLightIntensity", new a(n.indirectLightIntensity));
  }
  /**
   * 设置反应式属性
   */
  makeOptionsReactive(e) {
    super.makeOptionsReactive(e);
    const t = [
      "indirectLightIntensity",
      "indirectLightDistance",
      "colorBleeding"
    ];
    for (const r of t)
      Object.defineProperty(this, r, {
        get() {
          return e[r];
        },
        set(n) {
          if (n != null)
            switch (e[r] = n, r) {
              case "indirectLightIntensity":
                this.uniforms.get("indirectLightIntensity") && (this.uniforms.get("indirectLightIntensity").value = n), this.aoPass && this.aoPass.fullscreenMaterial.uniforms.indirectLightIntensity && (this.aoPass.fullscreenMaterial.uniforms.indirectLightIntensity.value = n);
                break;
              case "indirectLightDistance":
                this.aoPass && this.aoPass.fullscreenMaterial.uniforms.indirectLightDistance && (this.aoPass.fullscreenMaterial.uniforms.indirectLightDistance.value = n);
                break;
              case "colorBleeding":
                this.aoPass && this.aoPass.fullscreenMaterial.uniforms.colorBleeding && (this.aoPass.fullscreenMaterial.uniforms.colorBleeding.value = n);
                break;
            }
        },
        configurable: !0
      }), this[r] = e[r];
  }
}
Gf.DefaultOptions = {
  ...yt.DefaultOptions,
  indirectLightIntensity: 1,
  indirectLightDistance: 1,
  colorBleeding: !0,
  spp: 16,
  rings: 7
};
const Wf = `uniform vec2 texelSize;
uniform float intensity;
uniform float kernelSize;

void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
    // 中心像素
    vec4 center = texture2D(inputBuffer, uv);
    
    // 采样周围像素
    vec4 top = texture2D(inputBuffer, uv + vec2(0.0, -1.0) * texelSize * kernelSize);
    vec4 bottom = texture2D(inputBuffer, uv + vec2(0.0, 1.0) * texelSize * kernelSize);
    vec4 left = texture2D(inputBuffer, uv + vec2(-1.0, 0.0) * texelSize * kernelSize);
    vec4 right = texture2D(inputBuffer, uv + vec2(1.0, 0.0) * texelSize * kernelSize);
    
    // 计算拉普拉斯算子
    vec4 laplacian = 4.0 * center - top - bottom - left - right;
    
    // 应用锐化
    outputColor = center + laplacian * intensity;
}`;
class im extends I {
  /**
      * 构造一个新的锐化效果
      *
      * @param {Object} [options] - 效果选项
      * @param {BlendFunction} [options.blendFunction=BlendFunction.NORMAL] - 混合模式
      * @param {Number} [options.intensity=0.5] - 锐化强度，值越大效果越明显
      * @param {Number} [options.kernelSize=1.0] - 锐化核大小，控制影响范围
      */
  constructor({
    blendFunction: e = E.NORMAL,
    intensity: t = 0.5,
    kernelSize: r = 1
  } = {}) {
    super("SharpenEffect", Wf, {
      blendFunction: e,
      uniforms: /* @__PURE__ */ new Map([
        ["intensity", new a(t)],
        ["texelSize", new a(new g())],
        ["kernelSize", new a(r)]
      ])
    });
  }
  /**
      * 锐化强度
      *
      * @type {Number}
      */
  get intensity() {
    return this.uniforms.get("intensity").value;
  }
  set intensity(e) {
    this.uniforms.get("intensity").value = e;
  }
  /**
      * 锐化核大小
      *
      * @type {Number}
      */
  get kernelSize() {
    return this.uniforms.get("kernelSize").value;
  }
  set kernelSize(e) {
    this.uniforms.get("kernelSize").value = e;
  }
  /**
      * 当渲染尺寸改变时更新纹素大小
      *
      * @param {Number} width - 宽度
      * @param {Number} height - 高度
      */
  setSize(e, t) {
    this.uniforms.get("texelSize").value.set(1 / e, 1 / t);
  }
}
const jf = `uniform vec2 texelSize;
uniform float intensity;
uniform float kernelSize;
uniform bool adaptiveSharpening;
uniform float threshold;

// 亮度计算
float getLuminance(const in vec3 color) {
    return dot(color, vec3(0.2126, 0.7152, 0.0722));
}

void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
    // 中心像素
    vec4 center = texture2D(inputBuffer, uv);
    
    // 采样3x3网格周围像素
    vec4 tl = texture2D(inputBuffer, uv + vec2(-1.0, -1.0) * texelSize * kernelSize);
    vec4 t = texture2D(inputBuffer, uv + vec2(0.0, -1.0) * texelSize * kernelSize);
    vec4 tr = texture2D(inputBuffer, uv + vec2(1.0, -1.0) * texelSize * kernelSize);
    vec4 l = texture2D(inputBuffer, uv + vec2(-1.0, 0.0) * texelSize * kernelSize);
    vec4 r = texture2D(inputBuffer, uv + vec2(1.0, 0.0) * texelSize * kernelSize);
    vec4 bl = texture2D(inputBuffer, uv + vec2(-1.0, 1.0) * texelSize * kernelSize);
    vec4 b = texture2D(inputBuffer, uv + vec2(0.0, 1.0) * texelSize * kernelSize);
    vec4 br = texture2D(inputBuffer, uv + vec2(1.0, 1.0) * texelSize * kernelSize);
    
    // 基本3x3锐化卷积核
    vec4 sharpen = 9.0 * center - tl - t - tr - l - r - bl - b - br;
    
    // 自适应锐化 - 根据局部对比度调整强度
    float currentIntensity = intensity;
    
    if (adaptiveSharpening) {
        float centerLuma = getLuminance(center.rgb);
        float avgLuma = (
            getLuminance(tl.rgb) + getLuminance(t.rgb) + getLuminance(tr.rgb) +
            getLuminance(l.rgb) + getLuminance(r.rgb) +
            getLuminance(bl.rgb) + getLuminance(b.rgb) + getLuminance(br.rgb)
        ) / 8.0;
        
        // 局部对比度
        float contrast = abs(centerLuma - avgLuma);
        
        // 根据对比度调整强度 - 高对比度区域少锐化，低对比度区域多锐化
        currentIntensity *= smoothstep(0.0, threshold, contrast);
    }
    
    // 应用锐化，保证结果在有效范围内
    outputColor = center + sharpen * currentIntensity;
}`;
class sm extends I {
  /**
      * 构造一个新的自适应锐化效果
      *
      * @param {Object} [options] - 效果选项
      * @param {BlendFunction} [options.blendFunction=BlendFunction.NORMAL] - 混合模式
      * @param {Number} [options.intensity=0.5] - 锐化强度，值越大效果越明显
      * @param {Number} [options.kernelSize=1.0] - 锐化核大小，控制影响范围
      * @param {Boolean} [options.adaptiveSharpening=true] - 是否启用自适应锐化
      * @param {Number} [options.threshold=0.1] - 对比度阈值，用于自适应锐化
      */
  constructor({
    blendFunction: e = E.NORMAL,
    intensity: t = 0.5,
    kernelSize: r = 1,
    adaptiveSharpening: n = !0,
    threshold: i = 0.1
  } = {}) {
    super("AdaptiveSharpenEffect", jf, {
      blendFunction: e,
      uniforms: /* @__PURE__ */ new Map([
        ["intensity", new a(t)],
        ["texelSize", new a(new g())],
        ["kernelSize", new a(r)],
        ["adaptiveSharpening", new a(n)],
        ["threshold", new a(i)]
      ])
    });
  }
  /**
      * 锐化强度
      *
      * @type {Number}
      */
  get intensity() {
    return this.uniforms.get("intensity").value;
  }
  set intensity(e) {
    this.uniforms.get("intensity").value = e;
  }
  /**
      * 锐化核大小
      *
      * @type {Number}
      */
  get kernelSize() {
    return this.uniforms.get("kernelSize").value;
  }
  set kernelSize(e) {
    this.uniforms.get("kernelSize").value = e;
  }
  /**
      * 是否启用自适应锐化
      *
      * @type {Boolean}
      */
  get adaptiveSharpening() {
    return this.uniforms.get("adaptiveSharpening").value;
  }
  set adaptiveSharpening(e) {
    this.uniforms.get("adaptiveSharpening").value = e;
  }
  /**
      * 对比度阈值
      *
      * @type {Number}
      */
  get threshold() {
    return this.uniforms.get("threshold").value;
  }
  set threshold(e) {
    this.uniforms.get("threshold").value = e;
  }
  /**
      * 当渲染尺寸改变时更新纹素大小
      *
      * @param {Number} width - 宽度
      * @param {Number} height - 高度
      */
  setSize(e, t) {
    this.uniforms.get("texelSize").value.set(1 / e, 1 / t);
  }
}
const Xf = `uniform vec2 texelSize;\r
uniform float radius;\r
uniform float strength;\r
uniform bool luminanceOnly;\r
uniform float edgePreservation;\r
\r
// 获取亮度函数\r
float getLuminance(const in vec3 color) {\r
    return dot(color, vec3(0.2126, 0.7152, 0.0722));\r
}\r
\r
// RGB转YCbCr（如果仅处理亮度通道）\r
vec3 rgbToYCbCr(const in vec3 rgb) {\r
    float y = getLuminance(rgb);\r
    float cb = 0.5 + (rgb.b - y) * 0.564;\r
    float cr = 0.5 + (rgb.r - y) * 0.713;\r
    return vec3(y, cb, cr);\r
}\r
\r
// YCbCr转RGB\r
vec3 yCbCrToRgb(const in vec3 yCbCr) {\r
    float y = yCbCr.x;\r
    float cb = yCbCr.y - 0.5;\r
    float cr = yCbCr.z - 0.5;\r
    \r
    float r = y + 1.403 * cr;\r
    float g = y - 0.344 * cb - 0.714 * cr;\r
    float b = y + 1.770 * cb;\r
    \r
    return vec3(r, g, b);\r
}\r
\r
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {\r
    // 中心像素\r
    vec4 centerColor = texture2D(inputBuffer, uv);\r
    \r
    // 如果仅处理亮度通道，则转换为YCbCr\r
    vec3 centerYCbCr = luminanceOnly ? rgbToYCbCr(centerColor.rgb) : centerColor.rgb;\r
    \r
    // 双边滤波设置\r
    float sigmaSpace = radius;\r
    float sigmaColor = 0.1 + (1.0 - edgePreservation) * 0.2;\r
    \r
    // 定义滤波内核大小\r
    const int kernelSize = 9; // 实际半径: (kernelSize - 1) / 2\r
    const int kernelRadius = 4;\r
    \r
    // 累积变量\r
    vec3 filteredColor = vec3(0.0);\r
    float totalWeight = 0.0;\r
    \r
    // 双边滤波循环\r
    for(int offsetX = -kernelRadius; offsetX <= kernelRadius; offsetX++) {\r
        for(int offsetY = -kernelRadius; offsetY <= kernelRadius; offsetY++) {\r
            // 样本位置\r
            vec2 sampleUv = uv + vec2(float(offsetX), float(offsetY)) * texelSize * radius / float(kernelRadius);\r
            \r
            // 样本颜色\r
            vec4 sampleColor = texture2D(inputBuffer, sampleUv);\r
            vec3 sampleYCbCr = luminanceOnly ? rgbToYCbCr(sampleColor.rgb) : sampleColor.rgb;\r
            \r
            // 计算空间权重（基于距离）\r
            float distSquared = float(offsetX * offsetX + offsetY * offsetY);\r
            float spatialWeight = exp(-distSquared / (2.0 * sigmaSpace * sigmaSpace));\r
            \r
            // 计算范围权重（基于颜色差异）\r
            vec3 colorDiff = centerYCbCr - sampleYCbCr;\r
            float colorDistSquared = dot(colorDiff, colorDiff);\r
            float rangeWeight = exp(-colorDistSquared / (2.0 * sigmaColor * sigmaColor));\r
            \r
            // 计算总权重\r
            float weight = spatialWeight * rangeWeight;\r
            \r
            // 累积加权颜色\r
            filteredColor += (luminanceOnly ? sampleYCbCr : sampleColor.rgb) * weight;\r
            totalWeight += weight;\r
        }\r
    }\r
    \r
    // 归一化结果\r
    filteredColor /= totalWeight;\r
    \r
    // 如果仅处理亮度通道，则转换回RGB\r
    vec3 resultColor = luminanceOnly ? yCbCrToRgb(filteredColor) : filteredColor;\r
    \r
    // 混合原始颜色和滤波后的颜色\r
    vec3 finalColor = mix(centerColor.rgb, resultColor, strength);\r
    \r
    // 输出结果\r
    outputColor = vec4(finalColor, centerColor.a);\r
} `;
class am extends I {
  /**
      * 构造一个新的双边滤波降噪效果
      *
      * @param {Object} [options] - 效果选项
      * @param {BlendFunction} [options.blendFunction=BlendFunction.NORMAL] - 混合模式
      * @param {Number} [options.radius=4.0] - 滤波半径，值越大影响范围越广
      * @param {Number} [options.strength=0.5] - 降噪强度，值越大效果越明显
      * @param {Boolean} [options.luminanceOnly=false] - 是否仅对亮度通道应用降噪
      * @param {Number} [options.edgePreservation=0.5] - 边缘保留强度，值越大边缘越锐利
      */
  constructor({
    blendFunction: e = E.NORMAL,
    radius: t = 4,
    strength: r = 0.5,
    luminanceOnly: n = !1,
    edgePreservation: i = 0.5
  } = {}) {
    super("DenoiseEffect", Xf, {
      blendFunction: e,
      uniforms: /* @__PURE__ */ new Map([
        ["texelSize", new a(new g())],
        ["radius", new a(t)],
        ["strength", new a(r)],
        ["luminanceOnly", new a(n)],
        ["edgePreservation", new a(i)]
      ])
    });
  }
  /**
      * 降噪半径
      *
      * @type {Number}
      */
  get radius() {
    return this.uniforms.get("radius").value;
  }
  set radius(e) {
    this.uniforms.get("radius").value = e;
  }
  /**
      * 降噪强度
      *
      * @type {Number}
      */
  get strength() {
    return this.uniforms.get("strength").value;
  }
  set strength(e) {
    this.uniforms.get("strength").value = e;
  }
  /**
      * 是否仅对亮度通道应用降噪
      *
      * @type {Boolean}
      */
  get luminanceOnly() {
    return this.uniforms.get("luminanceOnly").value;
  }
  set luminanceOnly(e) {
    this.uniforms.get("luminanceOnly").value = e;
  }
  /**
      * 边缘保留强度
      *
      * @type {Number}
      */
  get edgePreservation() {
    return this.uniforms.get("edgePreservation").value;
  }
  set edgePreservation(e) {
    this.uniforms.get("edgePreservation").value = e;
  }
  /**
      * 当渲染尺寸改变时更新纹素大小
      *
      * @param {Number} width - 宽度
      * @param {Number} height - 高度
      */
  setSize(e, t) {
    this.uniforms.get("texelSize").value.set(1 / e, 1 / t);
  }
}
export {
  dh as ASCIIEffect,
  ga as ASCIITexture,
  rl as AdaptiveLuminanceMaterial,
  nl as AdaptiveLuminancePass,
  sm as AdaptiveSharpenEffect,
  E as BlendFunction,
  va as BlendMode,
  za as BloomEffect,
  at as BlurPass,
  vh as BokehEffect,
  Xt as BokehMaterial,
  Pc as BoxBlurMaterial,
  yd as BoxBlurPass,
  gh as BrightnessContrastEffect,
  yh as ChromaticAberrationEffect,
  qa as CircleOfConfusionMaterial,
  Cs as ClearMaskPass,
  De as ClearPass,
  xh as ColorAverageEffect,
  mt as ColorChannel,
  Sh as ColorDepthEffect,
  zo as ColorEdgesMaterial,
  Di as ConvolutionMaterial,
  dr as CopyMaterial,
  Ti as CopyPass,
  ud as CustomSSGIEffect,
  Mh as DayNightCycleEffect,
  am as DenoiseEffect,
  qr as DepthComparisonMaterial,
  Ec as DepthCopyMaterial,
  Rt as DepthCopyMode,
  Rc as DepthCopyPass,
  Yo as DepthDownsamplingMaterial,
  Li as DepthDownsamplingPass,
  wh as DepthEffect,
  _e as DepthMaskMaterial,
  Th as DepthOfFieldEffect,
  Me as DepthPass,
  Md as DepthPickingPass,
  Rc as DepthSavePass,
  we as DepthTestStrategy,
  Kf as Disposable,
  bh as DotScreenEffect,
  Pa as DownsamplingMaterial,
  zo as EdgeDetectionMaterial,
  zi as EdgeDetectionMode,
  I as Effect,
  Q as EffectAttribute,
  sh as EffectComposer,
  Uc as EffectMaterial,
  kc as EffectPass,
  zs as EffectShaderData,
  _ as EffectShaderSection,
  Pd as EnhancedThreeCompatPass,
  Ch as FXAAEffect,
  Fd as FakeInteriorMaterial,
  Bd as FlowRiverMaterial,
  pd as FoggyFlyoverEffect,
  Ph as FoggyMountainsEffect,
  gd as FoggyTerrainEffect,
  Dh as FrozenWastelandEffect,
  wd as GBufferPass,
  Ah as GammaCorrectionEffect,
  Fs as GaussKernel,
  Gc as GaussianBlurMaterial,
  Td as GaussianBlurPass,
  Eh as GlitchEffect,
  bt as GlitchMode,
  Rh as GodRaysEffect,
  co as GodRaysMaterial,
  Nh as GridEffect,
  Kd as HBAOEffect,
  zh as HueSaturationEffect,
  Is as IdManager,
  ah as ImmutableTimer,
  oh as Initializable,
  Nd as InteriorMappingMaterial,
  Id as InteriorRoomMaterial,
  Di as KawaseBlurMaterial,
  at as KawaseBlurPass,
  Le as KernelSize,
  Fh as LUT1DEffect,
  Ih as LUT3DEffect,
  uh as LUT3dlLoader,
  fh as LUTCubeLoader,
  Ih as LUTEffect,
  Bs as LUTOperation,
  dd as LakesMountainsEffect,
  bd as LambdaPass,
  ph as LayerBasedGlowEffect,
  rm as LensDistortionEffect,
  rt as LookupTexture,
  rt as LookupTexture3D,
  Ta as LuminanceMaterial,
  Pi as LuminancePass,
  Ci as LuminosityHighPassMaterial,
  Ld as MRTGBufferMaterialPatcher,
  j as MRTRenderPass,
  Ni as MaskFunction,
  _a as MaskMaterial,
  Rs as MaskPass,
  Ra as MipmapBlurPass,
  em as MotionBlurEffect,
  Ve as MultiSampleDepthMaskMaterial,
  Lh as NoiseEffect,
  lr as NoiseTexture,
  mr as NormalPass,
  Rd as OceanMaterial,
  Jr as OutlineEdgesMaterial,
  Uh as OutlineEffect,
  $t as OutlineManager,
  Jr as OutlineMaterial,
  Oh as OutlineMultiEffect,
  or as OverrideMaterialManager,
  X as Pass,
  dt as PerformanceLogger,
  Hd as PerformanceMonitor,
  kh as PixelationEffect,
  Gt as PoissionDenoisePass,
  xt as PoissonDenoisePass,
  vo as PredicationMode,
  Zi as RAOPass,
  Wr as RSSAOEffect,
  pc as RainfallEffect,
  Ge as RawImageData,
  $d as RealismNew,
  Wr as RealismSSAOEffect,
  Vh as RealisticBokehEffect,
  Ke as ReflectorForSSRPass,
  pe as RenderPass,
  lh as Resizable,
  W as Resizer,
  W as Resolution,
  Ed as RiverMaterial,
  Od as SMAAAreaImageData,
  Xh as SMAAEffect,
  kd as SMAAImageGenerator,
  hh as SMAAImageLoader,
  Dt as SMAAPreset,
  Vd as SMAASearchImageData,
  Bo as SMAAWeightsMaterial,
  Zh as SSAOEffect,
  Qh as SSAOEffectGBufferEnhanced,
  Bi as SSAOMaterial,
  Gf as SSDOEffect,
  an as SSGIEffect,
  Ad as SSRCompatPass,
  Gd as SSREffect,
  Ze as SSRPass,
  vd as SandStormEffect,
  Ti as SavePass,
  Hh as ScanlineEffect,
  _ as Section,
  Se as Selection,
  Wh as SelectiveBloomEffect,
  ns as SelectiveSSAOEffect,
  fe as SelectiveSSRPass,
  mh as SelectiveUnrealBloomEffect,
  Cd as SelectiveUnrealBloomPass,
  Ai as SeparableGaussianBlurMaterial,
  jh as SepiaEffect,
  ee as ShaderPass,
  de as SharedOutlineEffect,
  im as SharpenEffect,
  nm as SharpnessEffect,
  Gh as ShockWaveEffect,
  fc as SkyAtmosphereEffect,
  Pr as SkyAtmosphereUtils,
  Yh as SnowOverlayEffect,
  Ho as SnowfallEffect,
  Ki as TRAAEffect,
  zd as TangentSpaceInteriorMaterial,
  Ud as TetrahedralUpscaler,
  qh as TextureEffect,
  Dd as ThreeCompatPass,
  Jo as TiltShiftBlurMaterial,
  _o as TiltShiftBlurPass,
  Jh as TiltShiftEffect,
  Ds as Timer,
  _h as ToneMappingEffect,
  ye as ToneMappingMode,
  Sd as UniformFogEffect,
  Ei as UnrealBloomCompositeMaterial,
  me as UnrealBloomEffect,
  Ea as UpsamplingMaterial,
  sn as VelocityDepthNormalPass,
  tm as VelocityPass,
  $h as VignetteEffect,
  Pt as VignetteTechnique,
  md as VolumetricAtmosphereEffect,
  Gn as VolumetricFogEffect,
  xd as VolumetricShadowEffect,
  hd as WeatherManager,
  fd as WeatherSystemEffect,
  Bh as WebGLExtension,
  th as clearTimeData,
  Jf as disableLogs,
  $f as disableTimeCollection,
  qf as enableLogs,
  _f as enableTimeCollection,
  ih as error,
  ch as getAssets,
  eh as getTimeData,
  bi as log,
  $e as orthographicDepthToViewZ,
  rh as printSummary,
  Je as renderUtils,
  ge as timeEndLog,
  ve as timeLog,
  Zf as version,
  Qe as viewZToOrthographicDepth,
  nh as warn
};
//# sourceMappingURL=index.js.map
